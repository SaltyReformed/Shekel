> **ARCHIVED. Historical record only -- this document governs nothing and
> may be out of date.** The live plan is `docs/plans/steps.md`; the code as
> committed is the source of truth for what the app does.

# 38 shipped balance steps archived under rule 5 on 2026-09-16

The balance README stood at 1,310 lines, the ceiling its headroom arm allows, for the third time
in a day. These 38 SHIPPED steps are cited as a wait or an alias by NO other row of
`docs/plans/steps.md`, so under `docs/plans/conventions.md` rule 5 their rows leave the index and
their README entries leave section 5, one line per step here: its id, its commit and what it
did, as its index row said on dev `4f61467a`. Carried WITHOUT re-verification. A live sentence
may cite one of these ids for how something came to be, never as a plan of record. (`X-au-f-2`
stays in the index: it is the live specimen `tools/plan_gate/test_shipped_against_git.py` anchors
on; `X-ad-a` stays because `pay_calendar:C3` waits on it.)

- **X-au-g-1** `af61263d` -- Delete the pricing cycle by reading a loan's monthly P&I from its TERMS rather than from its own payment history, so the loan no longer prices the rows its price was derived from.
- **X-au-g-2a** `b16908f7` -- Move amount rule 4's producer DOWN into the amount model, `loan_payment_service._basis` / `._pricing` becoming `cash_ledger._loan_installment` / `._loan_pricing`, so the lower tier stops reaching UP into a loan service to price a row -- the unwind `row_valuation.py` says this arc owes, and what lets the loan stack name the resolver at all. Closes **N-266**(b).
- **X-au-g-2b** `6cd0ad44` -- Put a loan's contractual terms on the INSTALLMENT they govern rather than on a read date (**R-IJ**) at three sites, after which `cash_ledger` makes no clock call at all; the escrow-subtraction threshold was built, MEASURED a regression and reverted, so **N-409** stands re-owned. Closed **N-40** and **N-410**.
- **X-au-g-2c-1** `cdc2c7d9` -- Routed BOTH readers of a projected loan-side shadow through the amount model, `get_payment_history` and the plan tier, threading an `AmountBasis` through `load_loan_context` and its three callers; the finding named ONE and a census of nine call sites found TWO, seven settled-only. Byte-identical on production's 58 shadows. Closed **N-266**; opened **N-432**.
- **X-au-g-2c-2** `1f2b98a4` -- Declared EVERY transfer shadow DERIVED (**R-IN**), not a loan payment's two legs alone, making Transfer Invariant 3 STRUCTURAL and deleting both repairs that maintained it. The owner's typed figure survives any save stating no NEW one (**R-IO**): two reviews found two doors handing a taken leg back, the second inside the first's fix. 350 rows, `$0.00`. Closed **N-401**, opened **N-436**.
- **X-au-g-2c-3a** `becf76f8` -- Moved the ONE allocation down to `app/utils/money.py`, the leaf every walk reaches, and DELETED the three restatements the old placement FORCED -- two walks sat BELOW it, so reaching it was an import cycle. `$0.00`; 0 differences over 200,000 trials against `HEAD`.
- **X-au-g-2c-3b-1** `fd3afc59` -- Moved the CHARGE calendar to `loan_ledger._charges` -- the same inversion `2c-3a` fixed, one tier up: it sat in `balance_at._plan`, whose closure REACHES the settled walk, so that walk could not call it and restated the rule per payment instead. **This is what makes `3b-2` a taking rather than a rebuild.** `$0.00`; 0 mismatches over 20,000 trials against `HEAD`'s `_charges_for`.
- **X-au-g-2c-3c** `cb6469b2` -- Routed `debt_strategy_service._accrue_interest` through the ONE accrual primitive, deleting a FIFTH spelling that associated `(b*r)/12` against `b*(r/12)`. **`recurrence:D52`'s "agree on 200,000 randomised draws" is REFUTED**: at 500,000 they part, `$565.37` against `$565.36`. `$0.00` on both live loans.
- **X-au-k** `7315ecd9` -- Made `amount_ownership`'s "ONE writer" claim STRUCTURAL: a SQLAlchemy `composite()` maps a row's figure and its `amount_source_id` as ONE attribute over a value object total across R-FI's two states, so the illegal pair has no expression and the two splat sites a census cannot see are refused (**R-IW**). No migration. Closed **N-293**; opened **N-437** and **N-440**.
- **X-bi-1** `18f9efac` -- Sealed the row's `is_envelope` cell on X-au-k's pattern (private column, no migration): the public name READS the one accessor `tracks_purchases` and only WRITES the cell, and every derived flag's class-level name refuses to key a query, so the 4-where-there-are-238 misread is unwritable in Python, Jinja and the ORM (**R-JQ**, **R-BAL18**); the last two second spellings moved onto the accessor.
- **X-bi-1b** `71e09443` -- `companion_service` loads the period's baseline rows and keeps those whose `visible_to_companion` answers True, the SQL visibility clause and its join deleted (**R-BAL19**); `companion_visible` took `is_envelope`'s seal (no migration), the one-consumer mixin dissolved, the query tie-breaks by id. Companion row set on a production restore: 64 pages, 232 of 951, byte-identical. Closes **BAL-482**.
- **X-bi-3a** `65045062` -- Gave every movement its figure's SOURCE (`ref.movement_figure_sources`: resolved / typed / observed, NOT NULL, backfilled by the row's own day basis; migration `b5c7e9a1d2f4`) and made the status seam write ONE covering movement per manual settle of an expense row, readers pricing a settled row at its FAMILY (**R-BAL39**); the ledger report gained its purchase bucket (**BAL-502**). `$0.00`.
- **X-bi-3b** `56a8d6d7` -- Gave a movement's figure ONE producer in its PARENT's direction (`cash_ledger.movement_cash_leg`, **R-BAL35**), replacing six `-amount` spellings in the walk, the posting writer, the seam, the matcher and the accepted view; signed `posting_reads.posted_purchase_effect` by type; covered INCOME parents through the same seam, a settled paycheck reading `+figure`. `$0.00`.
- **X-bi-7a** `eecef63d` -- Added ONE accessor `recurs` on the definition and the row (`DerivedFlag`, so neither class-level name keys a query), re-keyed the seven "no cadence" sites and the account-delete refusal onto it, and gave the transaction side `propagate_to_unruled_definition`; a rule-less definition's row hard-deletes, takes a date, moves whole, is counted unrecoverable and hears its definition.
- **X-bi-7b-1** `7a2fe751` -- Built the ONE producer of a one-off (a rule-less definition, its one-version series dated on the row's due date, its row placed at the paycheck's start) and put both grid create doors and `mint_uncategorized` on it, `category_id` nullable (migration `9c1e4b7a2d3f`); a one-off's definition goes with its LAST row unless a merchant rule names it; *Does not repeat* gone (**R-BAL32**).
- **X-bi-7b-2** `e27e5134` -- Put the popover's item edits on a one-off's DEFINITION (name, category, both flags: **R-BAL36**), a typed figure restating the version the row's date reads and re-attaching a detached row (**R-BAL37**), a moved one-off re-placed at both doors (**R-BAL33**), the grid grouped by `(category_id, name)` (**R-BAL34**); BAL-484's writer went. Opened **BAL-496**, **BAL-499**.
- **X-bi-7b-3** `321bf2e4` -- Put `_create_envelope` on the producer: a NEW-ENVELOPE merchant answer mints once and names the definition (the rule flipped to TEMPLATE at first mint), a TEMPLATE answer on a rule-less definition PLACES its row (`PlacementKind.PLACE`), `MintedEnvelopes` keyed on the definition, 7b-1's two `recurs` keys gone; **R-BAL43**, **R-BAL44** at its two forks. Closed **N-328**, **BAL-496**, **BAL-499**.
- **X-bl-2a** `ee4fc2d7` -- Gave the loan payment feed ONE date producer and ONE settled/projected partition, and moved the amount model's eager-load set off the row loader onto the CALLER (**R-BAL7**, **R-BAL8**). Figures byte-identical; the displayed loan balance costs 4 fewer statements. Carries **N-432**.
- **X-bv** `66ff070b` -- Made a carried-forward leftover row carry the date its definition's own rule gives for that paycheck (`compute_due_date`), so the chooser's `declare_derived` can no longer leave a template-linked row that amount rule 3 must price on a due date it does not have. Closes **BAL-463**, whose written remedy aimed at migrations while the strand producer was application code.
- **X-bv-2** `6fa17bac` -- Bound `template_id IS NULL OR due_date IS NOT NULL` on `budget.transactions` and its twin on `budget.transfers` in one migration (`4d7123cd9803`), deleted `_stated_amount`'s no-due-date arm with its test and the two frozen-migration `due_date IS NULL` tests, and tightened the three derived-date types to `date`; production read 0 of 636 and 0 of 177 undated, and **BAL-463** closed.
- **X-bx** `f7b9e094` -- DELETE the fall-through by which the settled-only contribution accessor read a row's plan column by hand, so a reader answering what a row's money DID refuses an unsettled row rather than pricing its forecast (**R-BAL5**), and rename it `settled_contribution`. Moves `own_figure` to `_amount_source` as `_own_figure`. Closes **BAL-465**.
- **X-bz** `8e5c3ea5` -- Retired the one-time `occurs_on` backfill -- `scripts/stamp_occurrences.py`, its `entrypoint.sh` block and sentinel, and its tests -- because `occurs_on IS NULL` meant two things at once and the provenance filter was a fence over that conflation, which `X-bv`'s dating would have turned into a mis-stamp. Its window had closed.
- **X-ca** `e1cc26b6` -- Moved `budget.transactions`' `__table_args__` into `app/models/_transaction_table_args.py` as a pure move, 997 lines to 667 with a 392-line sibling, because the file sat at 999 of the 1000-line ceiling and the `X-bv` CHECK leaf has a `CheckConstraint` to add.
- **X-cf** `ccf88c01` -- The DECOMPOSED parent of the suite's ONE builder for a row of a definition (**R-BAL17**), split 2026-09-11 into the builder with the shared fixtures, the model, script and util suites, the service suites, the three plan-figure builders, and the route suites, so `X-bv-2`'s CHECK binds on rows the engine wrote rather than 68 hand-dated copies. Carried **BAL-480**, closed at its last leaf.
- **X-cf-3b** `699bd05d` -- Deleted the three plan-figure builders and moved their 164 sites onto the engine's row, with the owner's re-price and move spelled once each (`repriced_by_the_owner`, `moved_by_the_owner`), the pinned calendar's definition firing once before its pair, and a stale index restore deleted; the census after it is X-cf-4's 28 route sites.
- **X-cf-4** `ccf88c01` -- Converted the route suites' 38 hand-built rows of a definition (28 undated, 5 hand-dated, 5 `txn.template_id = ...` assignments) and four owner-act spellings onto the engine's row, so all three censuses read zero and **BAL-480** closed; `make_expense_template` takes a `category=` row beside `account=`, and three column reads became the definition's stated figure.
- **X-ch** `28a6317e` -- Gave the suite `generate_transfer_of(template, period)`, calling the transfer engine through the private body `generate_row_of` already had, plus `transfer_repriced_by_the_owner`, and moved all 23 hand-built linked-transfer sites onto them, so **BAL-488** closed and `X-bv-2`'s twin CHECK binds on the engine's rows; its PR also folded the envelope-expense fixture into one (**BAL-490**).
- **X-f3c-1** `2dad8512` -- The assertion RESET left the kind-blind walk for the fold that owns the running total: `walk_cash_ledger` yields FACTS, `balance_at._assertions` states what an assertion does to a balance, and each fold applies the policy its own kind needs -- which is what makes the flip a deletion rather than a rewrite of a leaf **R-FO** keeps the reset for. Byte-identical.
- **X-f3c-2a** `2aa2296d` -- Make opening equity the account's OWN stored fact (**R-GX**, shaped by **R-HE**): an append-only `budget.account_openings` read by BOTH the balance fold and the posted ledger, so the quantity has ONE definition and `is_opening` decides no figure. Seeded at the derived value. Carries **N-379**.
- **X-f3c-2b-1** `2cf2ac0a` -- Make a movement dated on or before its account's books opening UNSTORABLE (**R-HG**), refusing it at the one settle-day writer, at the reconcile panel's bulk writer and at three deferrable constraint triggers over all three tables, then legalise the twelve production rows by moving five openings back. **MOVES MONEY**: two balance sheets gain an opening at `$4,879.26`. Closes **N-378**.
- **X-f3c-2b-2a** `59b485df` -- Give an owner the DOOR that restates an account's opening -- append-only, carrying the day, the equity and a `user_declared` provenance, through the table's ONE writer, bounded by the movements the account records and by today but NOT by the calendar -- reachable from the shared account edit page and linked to from the balance-history card's opening row (**R-IE**).
- **X-f3c-2b-2b** `7ef63899` -- Made a matched bank LINE bound its account's opening at both tiers: three import doors and the restatement door refuse a line the books cannot hold, deferred triggers on `statement_match_members` and `bank_statement_lines` make the state unstorable, and the boundary's builders became ARM-EXPLICIT so a revision installs only the arm it censused (**R-IG**, **R-IH**).  Closed **N-383**.
- **X-f3c-2b-2d** `1f0c1cd7` -- Gave an archived account's drawer card the live cell's own **Edit** link, ungated and carrying no `#books-opening` fragment (**R-JT**), since the restatement door's two entrances were both unreachable once an account was archived and the account-10 runbook would otherwise unarchive and re-archive the twin to reach one. Closed **N-430**; opened **N-453**, **N-454**.
- **X-f3c-2c** `930f06fc` -- The DECOMPOSED parent of the append-only refusal, split 2026-08-30 (**R-HZ**): the fixtures stop editing an assertion in one commit, and the refusal that makes editing one impossible is the next, on one branch so `dev` never carries the gap between them. Carried **N-287**.
- **X-f3c-2c-1** `930f06fc` -- Stop every test fixture EDITING a stored balance assertion: the three re-stamping helpers become one that APPENDS, the three account factories take their day at `create_account`, and the seeded owner's origination assertion stays on the bootstrap day the account was opened on. Carries **N-392** and **N-393**.
- **X-f3c-2c-2** `930f06fc` -- Give `budget.account_anchor_history`, `budget.account_openings` and `budget.loan_anchor_events` ONE shared append-only refusal -- a database trigger refusing every actor and every spelling, plus the named object-layer exception (**R-HY**) -- with `Account.anchor_history` taking `passive_deletes` so an account can still be disposed of. Closes **N-287**.
- **X-f3c-2d** `249f66a7` -- Give the append-only refusal the three arms its three questions need -- `BEFORE UPDATE` per row, a DEFERRED constraint trigger `AFTER DELETE` because "is the owning account gone" is about the transaction's END state, and `BEFORE TRUNCATE` per statement (**R-IC**) -- closing the TRUNCATE that emptied a table unrefused and UNRECORDED and the recreate that destroyed an account's assertions.
- **X-f3c-3** `a7dbfdca` -- Derived the account's OUTSTANDING DIFFERENCE -- its latest asserted balance less its opening equity plus the postings through that day, ONE figure and not the per-assertion plug -- and put it on its own cash-detail card beside whether an imported statement's lines account for the span. Opened **N-433**, **N-434**.
