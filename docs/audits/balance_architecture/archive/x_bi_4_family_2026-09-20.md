> **ARCHIVED. Historical record only -- this document governs nothing and
> may be out of date.** The live plan is `docs/plans/steps.md`; the code as
> committed is the source of truth for what the app does.

# The X-bi-4 family as it stood in the README when it shipped whole (2026-09-20)

Archived under rule 5 at the tick of X-bi-4b-2 (the leaf `32c65cf1`), verbatim from section 5 except THREE
edits: the X-bi-4 and X-bi-4b boxes ticked with the leaf's hash, and the X-bi-4b-2 entry REWRITTEN as
built (its pre-tick three-line specification is the steps.md row at `4435054f`). The commits are the
record; the sentences below are what the README said about them the day the family closed. The family's
rulings are `../../plans/rulings.md` R-BAL75..R-BAL84 (the design loop's seven forks R-BAL75..R-BAL80;
R-BAL81 4a's review; R-BAL82 / R-BAL83 4b's loop; R-BAL84 4b-2's build); the build logs and the tick
drafts are `~/projects/shekel-handoffs/HANDOFF-X-bi-4.md` and `HANDOFF-X-bi-4b.md`.

  * [x] **X-bi-4** `32c65cf1` -- the DECOMPOSED parent of the fold's re-point onto movements, its design loop closed
    2026-09-18 (**R-BAL75**..**R-BAL80**, seven forks with worked dollars, `HANDOFF-X-bi-4.md`);
    ticks with its last leaf.
    * [x] **X-bi-4a** `babae2e3` -- the fold and the ledger read movements only (R-BAL75, R-BAL77,
      R-BAL78, R-BAL79, R-BAL80); the in-flight tier; DC-11; the matcher priced at the covering
      movement (R-BAL81); `bank_import:R-FX` / `R-GG` amended. On the restore Checking's actual
      line reads `+$137.93` and the 09-16 ledger nets move by the same (R-BAL80's third equality
      deviates; R-BAL77 explains it); the grid, every other account and the statements are
      byte-identical; `$0.00` written. Closed BAL-500, BAL-501, BAL-507, BAL-523 (born here).
    * [x] **X-bi-4b** `32c65cf1` -- the DECOMPOSED parent of the figure columns' deletion, split 2026-09-19
      (R-BAL83): the readers onto the covering movement (4b-1) and the migration with the sweep
      (4b-2); the settle DAY pair and the clearing link are NOT X-bi-4's; ticks with 4b-2.
      * [x] **X-bi-4b-1** `21709195` -- every reader of the record onto the covering movement
        (R-BAL80, R-BAL82, R-BAL83); baselines byte-identical on the 2026-09-19 07:21 restore.
      * [x] **X-bi-4b-2** `32c65cf1` -- the migration `45f10b870c8b`: the columns, the three CHECKs,
        the FK, the basis enum and ref table, the seam's write, the retention arms' basis read, DC-11
        arm 2 and the suite's column lays gone; the lock-counter defect the columns absorbed fixed at
        the seam; 40 stranded tests deleted (R-BAL84); every baseline byte-identical up/down/up on
        the 07:21 restore (229 settled rows, 1,083 rows' rebuilt columns byte-identical after the
        downgrade). Closed BAL-516, BAL-517 (their subject deleted, not consolidated).
    The parent-account key's end shape is two plain FKs plus a co-located OWNER key, dropped at the
    first cross-account writer, `credit_card:CC-5` (R-BAL76). The fold re-points to movements in ONE
    commit for every account kind at once.
    **Its design loop rules BOTH halves of the fork the card arc owes it** (`credit_card:R-CC15`, 2026-09-18; `credit_card:CC-5` builds on the answer, **R-BAL75** / **R-BAL76**): the KEY -- whether `fk_transaction_entries_parent_account` relaxes so a movement may sit on an account other than its plan row's (a card charge is the plan row's covering movement ON THE CARD; the design re-cuts it to a plain `transaction_id` FK) -- AND the fold's MOVEMENT PREDICATE: `TransactionEntry.account_id`, which `cash_ledger/_events.py:750` reads as the parent's today and the posted ledger reads as the movement's (`_posting_purchases.py:180`).
    After X-bi-3 every settled row has exactly one covering movement, so `opening + SUM(movements)`
    is an identity provable against the pre-state. **A per-kind cut was REJECTED**: the fold's
    predicate is kind-blind across 66 sites, so that cut would erect a discriminator only to delete
    it, and run two balance semantics live on one account meanwhile.

## Ledger rows CLOSED at this tick (moved here under the same id)

**BAL-516** and **BAL-517** CLOSED at `balance:X-bi-4b-2` `32c65cf1`: `tests/_test_helpers.settlement_columns`
and every one of its copied setattr loops are GONE with the two columns they laid -- deleted, not
consolidated -- and a renamed column now reaches every fixture through `cover_bare_settled_row`, the one
door that writes a bare-built settled row's covering movement. X-cp keeps BAL-518 and BAL-519. The rows
as they stood:

| arc | id | also | finding (one line) | worst measured | status | owner |
|---|---|---|---|---|---|---|
| balance | BAL-516 (`balance:X-bi-7c-3`'s adversarial review 2026-09-16) | -- | **`add_txn`'S SETTLE-STATE BODY -- `for column, value in settlement_columns(...).items(): setattr(txn, column, value)` -- IS COPIED INTO 16 TEST FILES, SO A RENAMED OR DROPPED SETTLEMENT COLUMN IS SILENT AT EVERY COPY.** `settlement_columns` exists so the three columns a settled bare row must carry together are resolved in ONE place (`ck_transactions_settle_day_needs_a_record`, `ck_transactions_settled_amount_needs_basis`); the loop that lands them on the row was not given a home and spread by copy. Measured 2026-09-18 on dev `da208ce5`: 24 files call `settlement_columns` (23 besides `_test_helpers.py` itself), 16 of them through their own `for ... in settlement_columns(` loop (`test_computed_properties`, `test_savings`, `test_analytics`, `test_statement_agreement`, `test_transfer_service`, `test_integrity_check`, `test_grid`, `test_dashboard_pulse`, `test_bank_agreement`, `test_projection_inputs`, `test_cash_day_facts`, `test_daily_balance_series`, `test_credit_workflow`, `test_calendar_service`, `test_spending_report_service`, `test_carry_forward_service`) | `$0.00`; a column rename reaches 16 copies by hand or not at all | **OPEN, born with an owner** (developer 2026-09-18, ruled in the coordinator session) | X-cp |
| balance | BAL-517 (`balance:X-bi-7c-3`'s adversarial review 2026-09-16) | -- | **`tests/_test_helpers.settlement_columns`' DOCSTRING SAYS IT HAS "ONE caller" (`add_txn`) AND THAT ITS INABILITY TO EXPRESS THE `purchases` BASIS IS CORRECT FOR THAT CALLER; 24 FILES CALL IT.** The sentence was true when the helper was born (plan step X-au-c3) and is the reason the docstring gives for a design limit; a reader deciding whether the limit still holds reads a false premise. Measured 2026-09-18 on dev `da208ce5`: 51 call sites in 24 files (23 besides the helper's own module); `add_txn`'s docstring in `tests/_test_helpers.py` unchanged | `$0.00`; a design limit justified by a caller count that is off by 23 files | **OPEN, born with an owner** (developer 2026-09-18, ruled in the coordinator session) | X-cp |
