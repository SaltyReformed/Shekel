> **ARCHIVED. Historical record only -- this document governs nothing and
> may be out of date.** The live plan is `docs/plans/steps.md`; the code as
> committed is the source of truth for what the app does.

# X-bi-6-3 as built: the ledger takes shape C (2026-09-22)

Archived at the tick of `balance:X-bi-6-3`, the third of the five leaves **R-BAL86** cut `X-bi-6` into, built in
three sessions and six commits on `feat/balance-x-bi-6-3`: leaf 1 `1c3b05c8` (the shape and the writer; `5320632a`
re-worded two docstrings after a dev merge), leaf 2 `c9c63e61` (the loan split a derivation; the transfer readers
deleted), leaf 3 `6a0b5154` (the resync re-books every source, then re-checks the anchors once), leaf 3b
`e2275ba9` (the legacy arm moved into `_posting_legacy`, a pure move) and `f1abdbbb` (the resync refuses while a
legacy net stands). The commits are the record; the designs, the rehearsal harness, the reviews and the suite logs are
`~/projects/shekel-handoffs/HANDOFF-X-bi-6.md` s.0-6-3-L1..L3b and `balance-2026-09-21/xbi6-3/`,
`balance-2026-09-22/xbi6-3-leaf3/`, `balance-2026-09-22/xbi6-3-leaf3b/`. Measured on the 2026-09-21 10:33 and 2026-09-22 17:06 production dumps (the 6-3 release's
rehearsal, 48/48 on both): 19 settled transfers (38 movements) re-booked in 57 entries (19 legacy reversals + 38
movement entries), no anchor correction, the 25 loan splits unlinked with identical nets, a redeploy writing
nothing.

Below, verbatim: the README entry and the `steps.md` rows as they stood; `R-BAL86` as it stood before the
amendment and `R-BAL102` as the lane drafted it (both trimmed in `rulings.md` to the gate's 2,000 characters);
the design's FUTURE STEP text `X-cu` was cut from and the cold start's `X-cv` text; the developer's first rank
picks, his re-ruling of them and his DC-12 answer; and `R-BAL98` as the lane drafted it (amended and trimmed
in `rulings.md`).

## The README entry as it stood, verbatim

    * [ ] **X-bi-6-3** the ledger takes shape C (**R-BAL45**): a clearing `LedgerAccountKindEnum` member and
      one Transfers-in-transit account per owner; a per-movement posting source keyed by `transaction_entry_id`
      (`posting_service.sync_transfer_postings`, `ledger_report_service/_attribution`,
      `account_posting_service/_walk`, `loan_posting_service/*` re-keyed onto the loan-side movement); a data
      migration re-books the 19 settled transfers (38 Paid shadows, 38 movements) into per-movement postings, graded ledger-net per (real account, day) byte-identical,
      Transit netting zero per transfer, the trial balance closing. **OWN PR, OWN RELEASE.** After `credit_card:CC-5-1` (shipped `4045a9b1`; R-BAL86's "after CC-5-1 merges").

## The `steps.md` rows as they stood, verbatim

| arc | id | also | what this step does | order | commit | starts |
|---|---|---|---|---|---|---|
| balance | X-bi-6-3 | -- | Give the ledger shape C (**R-BAL45**): a Transfers-in-transit clearing account per owner, one posting per movement on its own day, the loan probes re-keyed onto the loan-side movement, and a data migration re-booking the 19 settled transfers (38 movements) graded ledger-net per (real account, day) byte-identical; it **RE-BOOKS THE LEDGER**, so its own PR and its own release. | #1 | -- | NOW / credit_card:CC-5-1 (shipped; R-BAL86's own words, "after CC-5-1 merges") |
| balance | X-bi-6-5 | -- | Delete the 354 shadow rows and `transactions.transfer_id` with its three constraints by migration; the remaining branches (census 11 code lines `transfer_id is not None` in `app/**/*.py`), the exclusions, guards and Jinja branches its specification censuses die with them; the leg twins fold; the Transfer Invariants rewritten; byte-identical. Closes **BAL-475**, **BAL-526**, **BAL-527**. | #13 | -- | after #12 / balance:X-bi-6-2 / balance:X-bi-6-3 / balance:X-bi-6-4 |
| bank_import | X-gy | -- | Measure where the hosted runner's 5-13x per-test ratio over this host comes from (the 3x core oversubscription of `-n 12`, the per-item drop-and-reclone, the service container's disk; its durability is already off) in a matched A/B on the runner itself, fix the cause it names, and re-size `pytest.ini`'s per-test cap from CI's own `--durations` table. Closes **BI-496**. | #138 | -- | NOW |

## `R-BAL86` as it stood before the "AMENDED BY R-BAL98" head (the 6-1b year-end aside was trimmed to fit)

| arc | id | also | date | what was ruled |
|---|---|---|---|---|
| balance | R-BAL86 | -- | 2026-09-20 (developer) | **`X-bi-6` IS FIVE LEAVES, THE GRID FIRST, AND NO LEAF MOVES MONEY.** Picked (verbatim in `archive/x_bi_6_readers_as_built_2026-09-20.md`) **6-1** THE GRID READS LEGS (a `TransferLeg` view object, a planned leg or a settled leg = transfer + side + its covering movement through X-bi-6a's one walk, replaces the shadow row in the grid window, mobile cards and companion; the cell's doors are the transfer routes; the shadow-mutation module and 5 guards go; closes N-303, N-420; no schema; buildable today); **6-1b** if needed, the other display readers (calendar, spending, year-end, pulse; year-end IS the Spending report, `year_end_summary_service` gone at `3aecceb0`); **6-2** STATEMENT MATCH + RECONCILE PANEL offer legs keyed (transfer, account) settling through `settle_transfer`; after bank-import 3c; **6-3** THE LEDGER TAKES SHAPE C (a Transfers-in-transit clearing account per owner, a posting per movement on its day, loan probes re-keyed onto the loan-side movement, a migration re-booking the 38 settled transfers [= 19: 38 Paid shadows] graded ledger-net per (real account, day) byte-identical; OWN PR; after CC-5-1); **6-4** THE RECORD HALF RE-PARENTS + THE WRITERS STOP MIRRORING (movements gain a transfer link; its shape, and what ONE status means over two clearing days, are 6-4's forks with worked dollars; `transaction_id` nullable under an exactly-one-parent CHECK; 38 movements backfilled; the seam GAINS a Transfer arm; the mirror and five restore clauses GO; carry-forward and recurrence walk `budget.transfers`; closes N-304, BAL-503; after CC-5-1); **6-5** THE ROWS GO (the 354 rows and `transactions.transfer_id` with its three constraints deleted by migration; exclusions and guards die with them; the Transfer Invariants rewritten; byte-identical on both baselines). REJECTED: *"Schema first"* (both peers' migration chains; the link's fork first), *"Two leaves, X-bi-4's shape"* (296 sites in one session; R-BAL80) |

## `R-BAL102` as the lane drafted it (2,036 characters by the gate's width, its 1,991 the rule cell alone; the registry row drops the fixture's `$100,000` @ 6% and one adverb, and reads `--` in `also`, as `R-BAL105` does, because `also` holds aliases and the key arm refuses another row's own id there)

| arc | id | also | date | what was ruled |
|---|---|---|---|---|
| balance | R-BAL102 | R-BAL100 | 2026-09-21 (developer) | **THE LOAN PAYMENT SPLIT IS A DERIVATION, KEYED (`loan_payment` KIND, THE PAYMENT'S PAY PERIOD, ITS VISIBLE DAY) WITH NO ROW LINK; RE-RULES R-BAL100.** Asked after R-BAL100's premise was MEASURED false: "exactly ONE covering movement each" was production's 11 / 11, not the domain. A loan payment settled at `$0.00` has NO movement yet the walk correctly posts a nonzero split, `{Loan -500.00, Interest +500.00}` on the `$100,000` @ 6% fixture: the missed month's interest charged to the debt, the ONLY way to accrue a missed installment. The principle: a RECORD is keyed by its row (a movement is re-dated and withdrawn; the link follows it), a DERIVATION by (kind, period, date), as the opening and true-ups are. As picked: *"The split is keyed (loan_payment source kind, the payment's pay_period_id, the payment's visible day) with NO row link -- transaction_id, transfer_id and transaction_entry_id all NULL -- and reconciled in the SAME loop as the anchor corrections (one posted read over the three kinds, one emit_correction_deltas, one walk). Two payments on one day in one period merge into one entry. The payment-history table reads the walk's per-payment figures; the E1a assert stays as the ledger's grade. DELETED: the stale-shadow detector, reverse_loan_payment_postings_for_shadow and the delete/re-point door's reversal, the per-shadow ledger readers, the single-half sync doors, the fence allowlist entry, _attribution's transfer bucket and _walk's transfer loader. Migration: the 25 split entries are UNLINKED (transaction_id -> NULL, graded 25); their (period, date) already equal the key, so the first sync writes nothing: byte-identical; downgrade deletes them whole and the old image's loan hook re-posts them. After the resync no live entry carries transaction_id or transfer_id; 6-5 drops both."* Rejected: the transfer key (a row key keeps the detector and the ordering); the movement key with `$0.00` refused. R-BAL98 / R-BAL101 stand. Record: `design_6_3.md` s.10. |

## The design's FUTURE STEP text, verbatim (`balance-2026-09-21/xbi6-3/design_6_3.md` s.10), cut into `X-cu`

**FUTURE STEP (Josh, 14:1x: "should be documented as a future step"; id to be minted by the
coordinator, `X-cu` next free):** the true from-scratch loan ledger posts each ACCRUAL as its own
event on its own date -- `{loan_interest +I, loan_escrow +E, linked -(I+E)}` keyed
`(loan_charge, period, charge date)`, a date-keyed correction like Z's -- and each payment as a
PURE movement `{linked +X, transit -X}` (shape C, nothing else); the `loan_payment` split and its
source kind are deleted; the payoff overpayment is a fork (a per-date `loan_refund` correction, or
a negative liability presented as a receivable). It DEPENDS on `recurrence:R16-c-2` (charges every
contractual installment in the one stream) and on the fold DATING a charge at its own day in
`dated_deltas` (today a payment's whole effect counts at its `visible_on`; the E1a assert makes
the ledger follow whatever the fold dates, so the fold's dating and the ledger's shape move in ONE
step). It MOVES POSTED MONEY (balances between an installment and its settle; a skipped month
accrues), so its own PR and a worked example per option before the fork is asked.

## The cold start's `X-cv` text, verbatim (`HANDOFF-X-bi-6.md` s.0-6-3-L3b), cut into `X-cv`

**X-cv MINTED (future step; the coordinator ranks it at the tick).** Its steps.md sentence (354
chars): "Make every deploy all-or-nothing: `scripts/init_database.py` runs the Alembic migrations and
the three deploy hooks in ONE database transaction (`migrations/env.py` takes the caller's
connection, Alembic's shared-connection recipe), so a hook that refuses leaves the stamp unmoved
and `shekel-deploy` re-pins the previous image on its own (**R-BAL105**)." Its README
specification (for the balance README, 5 lines): **X-cv -- every deploy all-or-nothing (future;
R-BAL105).** Today `init_database.py` commits the migrations (in `env.py`'s own transaction)
BEFORE the three hooks run, so a hook that refuses (R-BAL104's legacy-net refusal, an unbalanced
entry at commit, the anchor walk's refusals) leaves a stamp the previous image cannot resolve and
`shekel-deploy.sh` refuses to re-pin: a manual dump restore. The step: one connection and ONE
transaction for entrypoint step 3 -- `env.py` configured with the caller's connection, `ref_cache`
and the three hooks on that session, one commit, each hook's leading `rollback()` gone -- graded by
a rehearsal in which a forged hook refusal leaves `alembic_version` at the pre-deploy stamp and the
re-pin fires. Precondition checked 2026-09-22: no migration uses `autocommit_block` or
`CONCURRENTLY`. It moves no money; it changes how every release deploys.

## The developer on the ranks and on DC-12 (2026-09-22, the coordinator's AskUserQuestions; `COORDINATOR.md` s.2 20:54 and 21:29)

**The first picks, as the coordinator recorded them.** They could not be written: the tier arm refuses a row no
outcome waits on inside the path (#1-#29 on this tree), and `X-cu` cannot rank at or before its own blocker
`recurrence:R16-c-2` (#23).

* `X-gy` right after `credit_card:CC-5-5c`: *"Right after the card leaves"*.
* `X-cv` next to `bank_import:X-gy`: *"Next to the CI speed-up"*.
* `X-cu` right after `balance:X-bi-6-5` (the option's own words are not in the coordinator's record).
* **DC-12 takes NO ruling row**: *"No, the check is the record"*. `scripts/integrity_check.py`'s DC-12 is
  Transfer Invariant 1's detector since leaf 1 (a live transfer whose live shadows number other than two,
  critical) and dies with the shadow rows at `X-bi-6-5`.

**The re-ask, verbatim.** Picked *"First after the outcome block (Recommended)"*: *"X-gy #30, X-cv #31, X-cu #32:
the earliest ranks the rule allows, in your order. X-gy is being built right now anyway (a rank only orders what
starts next), and the plan's preamble records that you placed these three by hand."* Refused: *"Make X-gy an
outcome-path step"*; *"Change the ordering rule"*.

## `R-BAL98` as the lane drafted it (`rulings.md` adds the "AMENDED 2026-09-21 BY R-BAL102" head and, to stay under the gate's 2,000, drops the deliberation aside "(the second with advantages / disadvantages in prose first)")

| arc | id | also | date | what was ruled |
|---|---|---|---|---|
| balance | R-BAL98 | -- | 2026-09-21 (developer) | **`X-bi-6-3`'S RE-BOOK RUNS IN THE DEPLOY'S GO-FORWARD RESYNC, NOT IN SQL; AMENDS R-BAL86'S "A DATA MIGRATION RE-BOOKING".** Fork 1, asked twice (the second with advantages / disadvantages in prose first). Worked: transfer 53, `$1,910.95` Checking -> Mortgage, both movements dated 2026-04-01 -- today ONE entry `{Checking -1,910.95, Mortgage +1,910.95}`; after, `{Checking -1,910.95, Transit +1,910.95}` and `{Mortgage +1,910.95, Transit -1,910.95}` on the same day; each real account's net per day unchanged, Transit 0.00; measured true for all 38 movements. The option text as picked: *"ONE producer: the migration seeds the ref rows, applies fork 2's singleton, re-keys the 25 loan-split links and owns the downgrade teardown; the deploy's existing first hook (resync_all_cash_postings) walks the 19 settled transfers through the NEW sync_transfer_postings, which reverses each old entry at its own date and posts the two per-movement entries -- the same code every future settle runs, no SQL restatement. Fail-closed at deploy for the 11 loan payments through the loan checked-projection assert in hook 2; for the 8 non-loan transfers the byte-identical grade is a CI gate (a suite test plants the old shape and runs the real resync, asserting per-(real account, day) nets equal, Transit zero per transfer, trial balance 0.00) plus the clone rehearsal's before/after JSON. Idempotent and self-healing. journal_entries.transfer_id stays this release (the reversed pairs carry it and net zero) and drops at 6-5. R-BAL86's 'a data migration re-booking' is amended by this ruling to name the hook."* Rejected: a raw-SQL migration graded fail-closed (a second spelling of the sign rule, gate, key, kinds, link and description; the nets grade blind to the rest, which the first resync would silently re-post); rewrite in place (append-only). Record: `HANDOFF-X-bi-6.md` s.3-DESIGN, `balance-2026-09-21/xbi6-3/`. |
