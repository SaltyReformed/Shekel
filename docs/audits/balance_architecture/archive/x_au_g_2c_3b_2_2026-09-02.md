> **ARCHIVED. Historical record only -- this document governs nothing and
> may be out of date.** The live plan is `docs/plans/steps.md`; the code as
> committed is the source of truth for what the app does.

# balance:X-au-g-2c-3b-2 as built (2026-09-02)

Archived out of `docs/audits/balance_architecture/README.md` on 2026-09-02 on the
developer's ruling that an as-built is unnecessary where it survives in the committed
code. The code is the record; this is the argument that produced it.

Shipped at **`3b7716f8`**, merged in PR #202.

* [x] **X-au-g-2c-3b-2** `3b7716f8` -- the settled walk takes the charge
  calendar: ONE interest accrual and ONE escrow per INSTALLMENT rather
  than per PAYMENT, and `split_payment_cash` is DELETED. **`$0.00` on
  production, and the scope of that zero is wider than the step
  predicted**: all 58 loan-payment rows across both live loans --
  SETTLED and PROJECTED -- sit in distinct months (`rows ==
  distinct_months` in every band), re-measured on a clone at
  `b7a41e2c9d63`, and an A/B of the whole walk over both loans reported
  **0 differences**. **That zero is only worth quoting because the
  harness was made to FAIL**: re-run against a clone with one due-month
  collision forced, it reports the second Mortgage payment moving from
  interest `$1,014.06` / escrow `$616.99` / principal `$279.90` to
  `$0.00` / `$0.00` / `$1,910.95` -- `$1,631.05` that was booked as
  expense now paying the loan down. Carries **N-409**'s second half and
  CLOSES **recurrence:D51**.
  **IT ABSORBED THE FOLD UNIFICATION on the developer's ruling, and that
  was NOT the step as specified.** The specification said the settled
  walk takes the calendar and filed the consequence; the trace found
  that after taking it, the settled walk and
  `balance_at._plan_fold._split_plan` would hold TWO implementations of
  one charge-then-allocate rule -- the THIRD instance in this arc of
  duplication forced by layering, after the ALLOCATION (`2c-3a`) and the
  CHARGE CALENDAR (`2c-3b-1`). `loan_ledger._replay.replay_loan_events`
  is now the ONE replay and both tiers are adapters onto it. **This is
  `CLAUDE.md` rule 14 exactly** -- *"Where a layer puts the shared leaf
  out of reach, MOVE THE LEAF: the placement is the root cause and the
  duplication is its symptom"* -- and the third instance is what argues
  the shape is structural rather than incidental.
  **Ruling R-IX** (developer, 2026-09-02): an accrual period's rate is
  resolved ONCE, on the charge. `AccrualCharge` carries the whole
  governing `RatePeriod` and `LoanPaymentSplit.period` is the charge's,
  not one re-resolved on the payment's own due date -- so a CONFIRMED
  row's displayed rate is provably the rate its interest accrued at.
  Reachable only in the shape this step creates: a rate change effective
  INSIDE an accrual period would otherwise tag a second payment's row
  with the NEW rate while its (`$0.00`) interest belonged to the old one.
  **The step's OTHER open question DISSOLVED rather than being ruled**,
  and the developer accepted the trace: *"does an assertion discard a
  standing charge or carry it across?"* has no reachable answer, because
  every charge is dated at the EARLIEST installment due in its period,
  so a payment always shares the charge's own date and clears it before
  any reset (order `charge -> payment -> reset`). No branch is written
  for it (rule 13), no ruling is filed, and `_replay`'s module docstring
  carries the proof plus the change that would make the state reachable.
  **THREE SHIPPED TESTS ASSERTED THE PRE-CHANGE RULE**, re-anchored under
  the developer's explicit confirmation that the expected behaviour
  changed (rule 5's only exception):
  `test_confirmed_view.test_two_payments_in_one_due_month_show_as_two_rows_in_that_month`
  and `..._a_late_settled_payment_is_dated_at_its_installment_but_visible_late`,
  and `test_posting_ledger_loan_reconciliation.test_biweekly_due_month_collision_reconciles_and_only_row_dates_differ`.
  The last pinned `ledger == resolver == reader`; it now pins the
  divergence BY ITS MECHANISM, reading the gap off the resolver's own
  second row. **Nothing rendered moves**: the resolver's balance is
  consumed at exactly one site, `_build_forward_inputs`'s
  `replay.balance_as_of if confirmed_view is None`, and every production
  read supplies a view since plan step E1d-b. Filed as **N-439**.
  **WHAT THREE ADVERSARIAL REVIEWS FOUND IN WORK ALREADY CALLED GREEN,
  and two of the three were claims this change wrote about ITSELF**
  (`feedback_a_fix_describes_itself_ungraded`, measured twice here):
  (a) *"This is the only place in the suite where the two rules give
  different money"* -- FALSE, four places, three of which FAILED;
  corrected IN PLACE with a note on what it was, because the note is
  what stops the next one. (b) The fence ruling for `replay_loan_events`
  argued from two premises false of this very code (*"it answers no
  DATE"*; *"a consumer cannot reach a balance without the seam-private
  prefix-sum"*) when five public calls yield `[(date, owed)]`; restated
  on the ground that holds -- it exposes nothing `dated_deltas` did not,
  and its keying is CONTRACT time, further from balance-at-T than the
  public name beside it. (c) A SECOND association rule in the walk
  (charge-to-payment by slot equality) agreeing with the replay's
  accumulate-and-clear only by construction -- DELETED; the replay now
  returns the association it already knew.
  **A LATER STEP MUST OBEY:** the retired one-payment-per-month
  composition is `tests/oracles/loan_monthly_composition.py`, and two
  suites plus a 300-trial sweep still grade the producer against it --
  deleting it leaves the producer grading itself. It composes the
  production PRIMITIVES deliberately: spelling `accrue_monthly_interest`
  inline would re-create the `(b*r)/12` vs `b*(r/12)` association
  `2c-3c` deleted. And the CUTOVER's real risk had no coverage until the
  test review found it -- a payment that becomes a period FOLLOWER when
  an earlier installment arrives later must have its posted interest and
  escrow REVERSED. Now pinned, and proven to FIRE: with a non-reversing
  writer injected it fails `PostingError: walk 1000.00 vs posted 400.00`,
  which is what a user's settle click would have raised.
