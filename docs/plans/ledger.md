# The findings ledger

**Every open finding in every arc, in ONE table, and every row has an OWNER.** A row leaves when its
fix ships, not when a ruling settles it -- a decided-but-unbuilt defect is still ahead of you.
Closed registers stay in each arc's as-built records; ids keep their names, so a reference to any
finding resolves in whichever file holds it.

**The arc is a COLUMN, never a prefix.** The key is `(arc, id)`. Bare ids keep their exact spelling
because they are cited in commit messages (immutable), in code comments and in the archived as-built
records -- a rename would orphan every one. Where a bare id is ambiguous across the corpus the
citation must name its arc: `D4` alone names three different findings.

**The `also` column carries two DIFFERENT relations and conflating them would delete work:**

- `= arc:id` -- the SAME claim, recorded in another arc's ledger and MERGED into this row on
  2026-08-09. Two rows worded differently cannot be combined mechanically, so each merge was a
  reviewed edit; the absorbed id stays here because commit messages citing it cannot be edited.
- `~ arc:id` -- a DISTINCT finding sharing a root cause, usually cause and symptom, usually with
  different owners and different closing steps. These must NOT be merged. `N-128` says so itself:
  *its CAUSE is F-10*.

Rules for this table are `conventions.md`; the steps its owners name are `steps.md`; each arc's
argument stays in its own document.

**The ledger stands at 166 rows.**

| arc | id | also | finding (one line) | worst measured | status | owner |
|---|---|---|---|---|---|---|
| balance | FU-1 / F1 | -- | The Van Loan's one unexplained true-up STEP -- an operator question, not a code fix. Prod account 8 carries exactly three anchors | `$905.33` against the servicer's statement | OPEN -- awaiting the operator. Blocks nothing; the ledger is self-consistent either way | operator (unchanged by the R-AO triage) |
| balance | FU-3 | -- | Standing overpayment resolves at TODAY for any as-of: `_resolution.py:294` reads the CURRENT template row with no as-of, inside a pass the context pins | latent | OPEN, re-verified 2026-07-27 | X-i2 |
| balance | N-96 | -- | `balance_at.interest_by_period_for_account` is a public seam entry with ZERO `app/` callers, and `__init__.py` states as fact that two screens read it | a seam entry no screen can reach, documented as one two screens read | OPEN, AST-verified 2026-07-27 | X-e |
| balance | N-97 | -- | `app/utils/dates.py:314` cites `balance_resolver.daily_cash_balance_series` as a live consumer of `attribution_date`; that producer was deleted at X-c2b3. The rule it states is still true | a present-tense claim naming a producer deleted a month earlier | OPEN, reported not fixed | X-p |
| balance | N-18 | -- | The recurrence bound and what was GENERATED can disagree, in both directions: nothing backfills or prunes when `RecurrenceRule.end_date` moves | bound 1 `2026-04-01` vs bound 2 `2026-03-01` | OPEN, ruled to X-k (R-AP). **Its citation named `match_periods`, which the recurrence arc DELETED at R4b-2 (`75346625`)** -- the N-97 shape, one arc over, corrected 2026-08-08. The defect survives the deletion (a stored bound nothing reconciles), but the measurement predates the forward engine, so X-k must RE-MEASURE it rather than carry it | X-k |
| balance | N-19 | -- | A RETIRED loan's recurrence bound does not exclude the CURRENT pay period (`period.start_date <= end_date` still matches) | -- | OPEN, ruled to X-k | X-k |
| balance | N-23 | -- | A refused loan payment fails an entire carry-forward batch | whole batch lost | OPEN; batch semantics ruled at X-k's trace, not folded into a prune commit | X-k |
| balance | N-24 | -- | Three generation call sites have no `ValidationError` handler, so a refused write 500s | 500 on pay-period extend and on unarchive | OPEN, ruled to X-k. **Its ANSWER already exists and X-k should adopt rather than re-invent it** (noted 2026-08-08): the recurrence arc hit the identical problem at R4a and solved it with ONE application-level handler (`RecurrenceCadenceUnsupported`, `app/error_handlers.py:134`) on the `BaselineMissingError` / R-BW pattern, precisely because eleven call sites deciding separately is the failure R-BW catalogued. Per-call-site handlers are the shape to avoid | X-k |
| balance | N-25 | -- | A real runtime import cycle was invisible to `cyclic-import`, because a TYPE-ONLY import of the same module excluded the edge for every import of it | a cycle plus an inverted dependency, gate-green | instance closed (`8285fcad`); the CLASS is an upstream limitation with no shared root here | developer-decision (dated 2026-07-27) |
| balance | N-33 | -- | 13 cross-package private-NAME imports: four route modules import 13 private names from `app.utils.account_validation`, so the names lie about their visibility | -- | OPEN, ruled to E2-0, which asks the same question one level down | E2-0 / E2-n (R-AO) |
| balance | N-35 | -- | The statement tier `ledger_report_service` is not W9909-scoped, so a public balance-at-T born there would be unguarded | a balance-at-T outside the seam with every gate green | recorded, NOT fixed; the false absolute claim was corrected in-commit | E2-0 |
| balance | N-36 | -- | The resolver's money-blind replay keys its rate on the PAY-PERIOD START where the genesis walk keys on the DUE date -- one question, two rules, deliberately. **WIDENED at X-an-a**: that key's INPUT DOMAIN grew. Eligibility used to require `period_start <= as_of`, so `period_for_date` was never asked about a date in the read's future; an EARLY-settled payment now replays while its funding period is still ahead, and on an ARM adjusting between the two the payment is priced at the POST-adjustment rate. **X-n also inherits a design option X-an-a surfaced**: the reason given for keeping the rate off `due_date` is that redistribution INVENTS due dates while the pay-period start "is always a fact" -- and `settled_on` is also always a fact, and is now on the record, so a TWO-date `ConfirmedPayment` keyed on it satisfies the stated criterion. Neither considered nor ruled out | none measured; the divergent surface is discarded on every production read, and `next_pay_date` / `remaining_months_as_of` are rate-independent except through the payoff break | recorded, deliberate, stated at the site so it cannot be rediscovered as an accident | X-n |
| balance | N-42 | -- | Nothing in the app records WHEN money moved, so the balance engine's ACTUAL clock is a data-entry click | `$36,323.99` gross swing against a `-$159.73` true error | RULED 2026-07-25 (R-N); R-EB re-scoped the fix into X-f1..X-f5 | X-f |
| balance | N-40 | -- | `live_amount_overrides` reads the wall clock, so a fold given an explicit `as_of` is not fully as-of-pure | latent; derive-mode loan-payment shadows on a historical read | OPEN, re-verified live 2026-07-27 | X-i2 |
| balance | N-56 | -- | The desktop grid's two self-refresh endpoints compute the SAME per-period view twice per `balanceChanged` | `165.6 ms` of duplicate producer work per refresh | OPEN, and NOT closed by X-i1's memo: two HTTP requests, two contexts | X-i |
| balance | N-58 | -- | The analytics calendar renders a flow on one day and the balance step for it on another, with no row explaining the gap | chip and step up to 25 days apart; median 2 | recorded, NOT fixed; three options open, ruled at its own step | X-p |
| balance | N-14 | -- | `contractual_schedule_from_origination` is computed twice per pass on the property page | -- | OPEN; its condition (prove both call sites' rate inputs identical) is X-i1's work | X-i1 |
| balance | E2 | -- | The super-package boundary: the option that would dissolve the last name-keyed gate. Row kept so the id resolves | -- | RATIFIED 2026-07-26 and promoted back into Section 5 as Phase E2 | E2-0 / E2-n |
| balance | N-72 | -- | A modelled asset's balance was three producers merged by a preference order; the merge is deleted, the `/savings` + `/investment` REDUNDANCY half is not | the modelled base built 14 times for 4 accounts on one render | merge half closed with the module; redundancy half open | X-i1 (the redundancy) |
| balance | X5 | -- | Anchor `effective_date`: a user cannot enter a balance read off last month's statement and have it land on last month | -- | OPEN, optional, NOT committed to; largely answered by `observed_on` -- re-verify before scoping | X-e (widened 2026-07-27) |
| balance | N-79 | -- | The investment chart's projection axis and its contribution timeline are on two different calendars, so the chart answers differently by the day it is opened | not measured in dollars | recorded, NOT fixed; inside the forward WHAT-IF engine ruling **R-U** deliberately keeps on `growth_engine` | X-l |
| balance | N-82 | ~ pay_calendar:P7 | Past the pay-period horizon the replay's ACCRUAL keeps running while its CONTRIBUTION tier stops -- a HALF model, with nothing on screen saying so | `+$5,427.07` at six months past the horizon | RULED at R-AG: let the fold answer and record it. Not live today | X-l |
| balance | N-83 | -- | A Property's value is answered two ways on adjacent screens -- the modelled map against the account's asserted balance | `$965.03`, growing with time since the last assertion | recorded; X-g2b TRIED the fix and reverted, because pointing the hero at the seam produced two new defects. **The CACHE half shipped at X-f1c3a** (`c16bdb3b`) -- there is no `current_anchor_balance` column to diverge from any more -- and what survives is the DISPLAY question | X-j |
| balance | N-85 | -- | `interest_by_period_for_account` has no production caller and survives on its own tests | -- | recorded; the same defect as N-96, de-duplicated against it | X-e |
| balance | N-87 | -- | The dashboard pulse justifies its cash basis by agreement with a grid that stopped agreeing at PR #47 | `$704.72` live; `$21,856.66` at the last projected column | RULED at R-AK; both false comment clauses are deleted, the divergence itself is X-j's | X-j |
| balance | N-89 | -- | The modelled contribution tier re-queries the whole pay-period calendar its caller has already loaded | 1 redundant calendar query per modelled read; 4 per grid refresh | recorded, NOT fixed | X-i1 |
| balance | N-90 | -- | R-K's identity is a property of the construction only in its BOUNDARY form; the form the screens render needs contiguous periods and is unverifiable at the leftmost column | none measured; every production caller passes the full contiguous set | recorded, NOT fixed; closing it changes what the grid displays | X-j |
| balance | N-91 | -- | The modelled-contribution feed is measured against a clock nobody pinned: the employer-match basis resolves at an implicit `date.today()` and across all scenarios | `$3,631.74` today against `$0` for a pre-horizon read | recorded; threading `ctx` MOVES MONEY and needs its own measurement and ruling | X-i2 |
| balance | N-92 | -- | The contribution feed is the seam's one un-memoized per-pass derivation and the most expensive loader in the set | `~9.4 ms` per investment account per seam read | recorded; shares a fix with N-91 rather than competing with it | X-i1 |
| balance | N-93 | -- | Every grid render entry pays the modelled contribution load, including the entry that reads none of it | `2.7 -> 14.8 ms` per modelled grid render entry | recorded; the load is not waste, the per-pass repetition is | X-i1 |
| balance | N-86 | -- | The `/investment` limit CARD and the projection beside it read two different YTD boundaries, and only one is a function of the window | `$1,000.00` of annual-limit room per period of divergence, compounded | recorded; correct on both surfaces today and pinned in both directions | X-m |
| balance | N-115 | -- | The dashboard tracks section pays TWICE for three loaders, and the expensive one is a full paycheck-engine run | the second breakdown alone: `7.2 ms / 7 SQL` | OPEN; widens X-i1's input tier | X-i1 |
| balance | N-116 | -- | The period preconditions are the baseline precondition's twin: 96 branches in 49 `app/` files, resolving to ~50 answers over FIVE distinct questions | `+$3,228.55` of net worth on a 5-day calendar hole | OPEN | X-x |
| balance | N-117 | -- | Fifteen surfaces decide the no-baseline state WITHOUT the balance seam, and neither instrument X-v built could see one of them | `$0.00` today and unreachable, for the same measured reason as N-112 | OPEN | X-y |
| balance | N-121 | -- | The cockpit reduces its liquid-savings total TWICE per render and publishes it under two context keys | `$0.00` today -- one function, one input, equal by construction | OPEN | X-ac |
| balance | N-122 | -- | The asset-vs-liability rule has a SECOND home on the WRITE path, and X-z's own docstring said it could not | `$0.00` today: both spellings compare against the same cached id | OPEN, found independently by both of X-z's adversarial reviews | X-ab |
| balance | N-124 | -- | The forward rolling top-up backfills HISTORY: a lapsed schedule generates past periods and populates them | `$0.00` in balance terms; the generated rows are Projected. The COST is 991 transaction rows and 71 pay periods per lapsed owner, and a budget history showing planned spending in months the app was not being used | OPEN. Ruled **R-EZ** 2026-08-10: an automatic writer may not create a period that has already ended, so the top-up creates nothing on a lapsed schedule and the lapse is surfaced instead | X-ad-b |
| balance | N-128 | ~ pay_calendar:P2 | A pay-period hole breaks R-K's reconciliation identity, and it is in the FOLD, not above it | `-$140.63` on the gapped clone | OPEN; X-ad stops new holes reaching it. **Its CAUSE is the recurrence arc's finding F-10** -- `pay_period_service._reject_overlapping_batch` refuses an OVERLAPPING batch and not a GAPPED one, so `latest_end + 5 days` is accepted. One defect recorded in two plans, matched 2026-08-08; closing F-10 closes this row's generator and makes `PlacementOutcome.SCHEDULE_GAP` unconstructible with it | X-l |
| balance | N-129 | -- | Converting a page to "raise" silently blinds every test whose fixture calendar does not cover today, including security controls | 84 unique node ids fire the precondition; ~80 controls blinded | OPEN, measured with an instrumented full run | X-x |
| balance | N-125 | -- | The salary cockpit is the SEVENTH answer to "no pay period covers today" | `$0.00` -- an empty page rather than a wrong number | OPEN, measured at X-x2 and deliberately not widened into | X-x |
| balance | N-126 | -- | A public contribution producer has no caller in `app/`, and its whole body is a fabrication the trace was about to fix | `$0.00`: no `app/` path reaches it | OPEN, found by the call-graph pass | X-x |
| balance | N-135 | -- | The partition's fence covers the derived boundary and NOT the two bare fact fields, so the line step 3 deleted still compiles | `$0.00` today: every remaining read is a legitimate raw-date use | OPEN, RULED 2026-08-01: wrap both fields, in two distinct types | X-d |
| balance | N-138 | -- | The app has TWO "today"s, no enforced rule about which is which, and NO INSTRUMENT THAT CAN SEE THE DIFFERENCE: 78 `date.today()` calls in `app/`, 191 in `tests/` | `$0.00` today: production pins the zone | OPEN, and a DECISION before it is work -- **the first task is the INSTRUMENT, not the sweep** | developer-decision (2026-08-02) |
| balance | N-142 | -- | `request.args.get(..., type=int)` is the one submitted-id surface X-ae did not convert, and it is lax in the same way | `$0.00` and no crash; `'١٠٦'` returns 106, `' 2026 '` returns 2026 | OPEN; needs a per-site ruling, id against non-id | X-ah |
| balance | N-144 | -- | `settled=` is a caller's OPINION about a row that already knows its own status | `$0.00` today | OPEN; R-DU upgrades the remedy from removable to unrepresentable | X-ai |
| balance | N-145 | -- | `transfer_service.py` sat at 999 of pylint's 1000-line ceiling, blocking X-d | `$0.00` -- a size gate, not a money defect | ANSWERED by R-DN; X-aj1 took it to 987, which N-152 says is not a solution | X-aj |
| balance | N-147 | -- | Two custom checkers still enforce a rule with a list of module names, which is prose plus a detector that must be kept complete | `$0.00` directly; each list is a rule that can rot | OPEN, ruled into Phase G, which runs inside E2 | G1 |
| balance | N-148 | -- | The transfer -> shadow mirror rule is written THREE times and the three already disagree on `scenario_id` | `$0.00` on today's data -- nothing edits a transfer's scenario | OPEN, ruled into its OWN step rather than folded into X-aj | X-ak |
| balance | N-149 | -- | `create_transfer` applies NO transition check, so a transfer can be BORN in a status the state machine exists to exclude | `$0.00` today, a property of the callers rather than of the design | OPEN; carried by X-aj2, because the fix is the born-status RULE | X-aj2 |
| balance | N-150 | -- | A transfer shadow STORES five fields Transfer Invariant 4 says must always equal its parent's, and NOTHING enforces the equality | `$0.00` today | OPEN, and it REVERSES X-ak's scope: rule the copy before the copiers | X-ak |
| balance | N-151 | -- | The two `mark_done` routes pass an explicit `paid_at=now()`, which wins over the seam, so a replayed settle still re-dates a settled transfer | unmeasured, deliberately: the same unbounded gap N-146 carried | OPEN; the fix is a rule about what `mark_done` MEANS on an already-settled row | X-aj2 |
| balance | N-152 | -- | `transfer_service.py` lands at 987 of 1000, so the size gate is answered for X-d and NOT solved | `$0.00` -- a size gate; 13 lines of headroom against X-d's ~9 | OPEN; the structural answer is a PACKAGE, one private leaf per verb | X-ak |
| balance | N-153 | -- | `_reconcile_postings_after_update` re-syncs both endpoints' anchors, and ruling R-DK dissolved the reason its docstring gives for existing | `$0.00`; both calls are idempotent and land on the same state | OPEN, deliberately not taken at X-d (R-DT) | X-ai |
| balance | N-154 | -- | `useless-suppression` does not report a stale `duplicate-code` disable, so one that suppresses nothing survives every gate | `$0.00`: a stale suppression writes no wrong figure | OPEN; the instrument is undecided, which is why it is a step | X-al |
| balance | N-155 | -- | The checked-projection assert grades a HALF-FINISHED operation in every batch loop, not only in the delete window R-DM ordered around | three CONFIRMED production defects and one plausible | OPEN, and it PARKS X-d | X-ai |
| balance | N-156 | -- | The 1000-line ceiling has split a SECOND module, and that split was recorded in an as-built rather than as a finding | `$0.00`: a public module writes no wrong figure | OPEN; same class and same answer as N-152 | X-ak |
| balance | N-157 | -- | `resync_anchor_postings` is a NAME, not a chokepoint, and its docstring claims otherwise: FIVE entry points reach the assert, not three | `$0.00` today | OPEN; R-DU deletes the rule instead of relocating it | X-ai |
| balance | N-158 | -- | The shared checked-projection assert leaves its SIGN convention as an unnamed operator inside each caller's loop | `$0.00` today; a sign flip still balances every entry | OPEN; the option not considered is a per-package `posting_deltas(walk)` accessor | X-ai |
| balance | N-159 | -- | Transfer retirement stays TWO halves a caller must remember to pair, which is the obligation `retire_transaction` was built to make structural | `$0.00` today: `delete_transfer` is the single path and pairs them correctly | OPEN; R-DU may dissolve the question before X-ak reaches it | X-ak |
| balance | N-160 | -- | Two exported loan writers reconcile the ledger without ever grading it | `$0.00`, and unreachable from `app/` -- an unwired writer posts nothing | OPEN; under the one verb a half-ledger writer cannot exist | X-ai |
| balance | N-161 | -- | The anchor-correction reconcile violated the R2 attribution rule the source reconcile obeys, in the one branch that did not run | `$0.00`; no rendered figure moved, tested rather than asserted | RULE half CLOSED at X-ai-r (PR #81, in production); the family it belongs to dies at X-f4 | X-ai |
| balance | N-162 | -- | A walk-driven whole-account re-derive cannot see a source that LEFT the settled set, so X-ai-a's defining sentence has a hole in it | `$0.00` today and unreachable from `app/` | OPEN, and a REQUIREMENT on X-ai-a rather than a defect to fix afterwards | X-ai |
| balance | N-163 | -- | A registry-scoped commit-boundary grader is BLIND to a bulk `UPDATE` or `DELETE` of a source table, and the door is 20 call sites wide | unquantified today, and that is the finding | OPEN, owned by X-ai-g, which classifies all 20. The remedy is NOT a checker forbidding bulk statements | X-ai |
| balance | N-164 | -- | A transfer's posted effect is computed by TWO rules, and account-owned re-derivation would turn their disagreement into a write OSCILLATION | `$0.00` today; Transfer Invariant 3 holds | OPEN, ruled RESOLVED BY DESIGN at X-ai-a (R-DV / R-DW) | X-ai |
| balance | N-165 | -- | A whole-account re-derive writes on OTHER accounts' ledgers, and the design gives no rule for grading them | `$0.00` today and unreachable from `app/` | OPEN, and a REQUIREMENT on X-ai-a: return the pairs touched and re-enqueue | X-ai |
| balance | N-166 | -- | Two concurrent re-derives of one account double-post its correction, and no DB constraint can catch it | `$0.00` today; the per-row writers are narrow enough that `version_id` covers the window | OPEN, and a REQUIREMENT on X-ai-a: a per-`(account, scenario)` advisory lock | X-ai |
| balance | N-167 | -- | The append-only ledger has no reversal linkage, so "which entry undoes which" is recoverable only by re-running the reconcile arithmetic | `$0.00`; the cost is diagnostic and this arc paid it twice in one week | OPEN, owned by X-ai-s, the only cheap moment to add it | X-ai |
| balance | N-171 | -- | The true-up residual is booked to EQUITY, so real economic activity is structurally invisible on the income statement | `$15,065.08` gross / `-$1,495.10` net over four months on Checking | OPEN; resolved by R-EB (d), and only correctly AFTER the date work | X-f3 |
| balance | N-172 | -- | The book-vs-bank gap at an assertion IS the rendered balance error immediately before it, and it is not small | `$321.52` average, `$1,853.92` worst, non-zero on 96% of assertion days | OPEN; the churn half is X-f1/X-f2's, the residue half X-f3's | X-f3 |
| balance | N-173 | -- | For two thirds of settled Checking rows the settle date is a bookkeeping-session artifact: 88 of 135 share a click-minute with another row | 65.2% of the account | OPEN; this is the GENERATOR of N-172's churn. **Re-pointed from the ticked X-f1 at X-f1e3**: that step shipped the MECHANISM, and its backfill was the deleted derivation verbatim, so not one stored date improved | X-f6 |
| balance | N-174 | -- | The PROJECTED END BALANCE -- the figure the developer actually budgets against -- inherits the whole gap, and the invariant protecting it is unbuilt | the affordability decision itself | OPEN; X-f3's ship gate is that invariant passing as a TEST without a true-up, in both orders | X-f3 |
| balance | N-176 | -- | Five posted correction days on Checking carry no surviving anchor history row, and all five self-healed to `$0.00` | `$0.00`, by construction and by measurement | OPEN as a RECORD, not a defect: X-f4 deletes a branch that has demonstrably fired in production | X-f4 |
| balance | N-177 | -- | The `Settled` status has no writer, no reader that distinguishes it, and zero rows on both tables | `$0.00`; a THIRD member in the predicate every balance rule in this arc is written against | OPEN; ruled its own step rather than taken inside X-f1 | X-am |
| balance | N-188 | -- | The W9907 checker still describes the seam as maintaining `paid_at`, in the message it shows the developer | `$0.00` -- the checker's behaviour is correct | OPEN; not fixed inside X-f1c because X-aj2 DELETES this checker | X-aj2 |
| balance | N-185 | -- | `settled_on` is now exactly as load-bearing as `status_id`, and only one of the two has a fence | `$0.00` today | OPEN; do NOT grow W9907 an arm -- X-aj2 replaces its write door with a read-only attribute | X-aj2 |
| balance | N-180 | -- | A de-duplication rationale in `balance_at/_loan_interest.py` was falsified by R-DH, and whether the two sets can still differ for any other reason is UNVERIFIED | `$0.00` -- the code was never wrong, only the reason beside it | OPEN; the paragraph is replaced in place, the question survives | X-e |
| balance | N-139 | -- | Nothing prevents a submitted digit string being parsed laxly again, and a checker on the method NAME does not prevent it | `$0.00` today; the exposure is the NEXT id parse | OPEN, and the INSTRUMENT is undecided, which is why it is a step | X-ag |
| balance | N-191 (X-f1c3c's residue pass 2026-08-04, found while re-justifying a comment ruling R-EO had falsified; census by grep over `app/`) | -- | **The app's civil day rests on a compose environment variable rather than on a rule, at 113 call sites.** `app/utils/dates.display_today()` is the user's civil day; `date.today()` is the PROCESS-local day, and `app/` calls it **113 times**. Production pins `TZ: America/New_York`, so the two are equal there and none of the 113 is a live defect -- which is exactly the problem: their correctness is a property of the deployment, not of the code, and finding N-138 already records that neither clock gate can detect a process-vs-display SPLIT. Two sites found in this pass decide something against the user's CALENDAR and so look like the wrong side of the line: `pay_period_admin.top_up_rolling_window` (`as_of` for "keep N periods ahead of today") and `classify_periods_bulk` (`as_of` for whether a period is HISTORICAL, which is a truncate LOCK decision). `display_today`'s own docstring calls `date.today()` the UTC day, which is false in production, where the process clock is Eastern | `$0.00` today and on any TZ-pinned deployment. The exposure is a one-day boundary error in whether a period is historical (a truncate refusal) or whether the rolling window is short, for the hours the two clocks disagree -- reachable the moment a container runs unpinned, which CI already does on purpose | **OPEN.** Not fixed here: 113 sites is a systemic surface far outside a leaf that deletes two columns, and each needs a per-site ruling on which day it means -- the same shape N-142 has for query-string ids. Recorded with the census rather than the two anecdotes | X-ak |
| balance | N-192 (X-f1c3c's residue pass 2026-08-04) | -- | **A `PostingError` that was referentially unreachable is now held out of reach by code alone, and its comment still cited the deleted constraint.** `pay_calendar.PayCalendar.filing_period` raises when an account has anchor corrections to post while its owner has no materialised pay period (**the raise MOVED at C2-d**: `reconcile_account_anchor_corrections` held it, a second copy at the shared loader was deleted on the 2026-08-10 ruling because it could not fire where this one would not, and the two predicates had drifted). That was impossible under the FK: an assertion carried a NOT NULL `pay_period_id` pointing at one of those periods. Ruling R-EO deleted the column, so what keeps the state out of reach is now three separate code paths -- registration opens a bootstrap period before creating the default account, `truncate_pay_periods` only deletes indices ABOVE the one kept so index 0 survives, and `reset_pay_periods` regenerates before returning | `$0.00`; unreachable through every UI path traced. A 500 on account create or on any true-up for the affected user if it is ever reached | **OPEN, deliberately as a loud failure.** The comment is corrected to state what actually holds it out of reach; the raise STAYS, because this reconcile derives each correction's period from its day and an empty calendar is the one state that could silently mis-file every correction an account has. Worth a schema-level guarantee if one exists that does not re-file a bank fact under a budgeting artifact -- which is what R-EO deleted | X-ak |
| balance | N-193 (X-f1c3c's concurrency review 2026-08-04; the deadlock REPRODUCED against a real PostgreSQL with its own `DeadlockDetected` DETAIL line, the statement ordering CAPTURED from a real loan-payment settle) | -- | **The per-user write lock closes the reconcile race and opens an advisory-vs-ROW-lock cycle, because it is not the FIRST lock its transaction takes.**  A settle takes row locks first -- `update_transfer` UPDATEs the transfer and both shadows, they flush, and the posting sync reaches `lock_user_writes` only afterwards (measured: statements 2-4 against statement 19) -- while `truncate_pay_periods` / `regenerate` / `reset` take the lock first and then bulk-DELETE pay periods, which CASCADEs to `budget.transactions` and locks exactly the rows the settle may hold.  Same user, opposite orders.  **A first version of this step's docstrings and record called deadlock "structurally impossible on every request path"**, an argument that considered only advisory-vs-advisory ordering | PostgreSQL detects it and aborts one transaction: an unhandled 500 on a money route, **no money corrupted** (the loser rolls back atomically).  Needs a settle and a schedule rebuild for one user to overlap -- two browser tabs.  `$0.00` of ledger divergence, against the silent permanent divergence the lock replaces, which is why the lock ships anyway | **OPEN.**  The fix is the invariant stated in `user_write_lock`'s docstring -- *this lock must be the FIRST lock a transaction takes* -- which means acquiring at the write-SERVICE entry (the status seam, `update_transfer`, the delete/restore paths) rather than inside the reconcile.  Deliberately NOT done in this leaf: it is a different change with its own blast radius and needs its own review, and the decompose-at-the-leaf-boundary lesson is this step's own.  What ships here is the correct claim, not the correct lock placement.  **NARROWED at X-f1c4b**, which moves acquisition to the TOP of both anchor doors so those two satisfy the invariant; the settle paths still do not, and they are what this row is now about | X-ak |
| balance | N-197 (X-f1c4c's trace 2026-08-04, read off the two doors) | -- | **The app's two anchor doors bound "not in the future" on two DIFFERENT clocks, and one of them also sets a form's `max`.** The cash door refuses through `display_today()` (the user's civil day); the loan door refuses through `date.today()` in BOTH its schema (`schemas/validation/loans.py:161`) and the input bound its form renders (`routes/loan/dashboard.py:548`), which is the process day. One question -- "has this day happened for this user" -- with two answers, on the two surfaces that ask it about a balance assertion. A specific instance of **N-191**'s 113-site census, kept as its own row because it is a DIVERGENCE between two doors rather than one site's choice, and because X-f1c4c is what puts the cash clock on a user-visible form | `$0.00` on any TZ-pinned deployment, and production pins `TZ: America/New_York`, so the two are equal there. Unpinned (CI does this on purpose), the loan door accepts and pre-fills a day the user has not seen, for the hours the clocks disagree | **OPEN.** Not fixed in X-f1c4c: it is the LOAN door, and moving its refusal boundary is a behaviour change on a write path that needs its own measurement rather than riding a cash-form commit | X-ak |
| balance | N-200 (X-f1e2's design review 2026-08-05, raised independently by the step's own trace) | -- | **A back-dated assertion can take the OPENING away from the origination, and the opening's posted correction moves with it.** `cash_anchor_facts` marks the opening POSITIONALLY -- `is_opening = index == 0` over `(observed_on, created_at, id)` (`cash_ledger/_events.py:370`) -- while `resolve_observation_day`'s floor is the SCHEDULE's earliest start, not the account's own opening day. So a user may assert a balance for a day before the account was originated, and `_anchors._account_correction_kinds` then books that row as `account_opening` and the origination as `account_trueup`, re-keying both entries. Deleting the `notes` label removed the SECOND definition of "the opening"; it did not close the mechanism by which the surviving positional one moves | `$0.00` measured: production carries no assertion dated before its account's origination, and the reconcile is a reconcile-to-target so the re-key self-heals rather than double-posting. Untested either way -- no fixture covers it | **OPEN.** Not a bug to patch at the floor: back-dating below the origination is a LEGITIMATE entry (an account that existed before Shekel knew about it), so the question is what "the opening" MEANS, not what the bound is | X-f3 |
| balance | N-201 (X-f1e2's build 2026-08-05, measured by the pylint ceiling) | -- | **`anchor_service.py` lands at 998 of pylint's 1000-line ceiling, and it is the THIRD module to reach it.** Adding `ObservationDay` took it there, and three separate prose trims during one build is the shape N-152 and N-156 already record for `transfer_service.py` (987) and one other. The module holds two cohesive halves that share only `AnchorTrueUpOutcome` -- the cash anchor door and the loan anchor door -- which is the `cash_ledger` / `loan_ledger` split one tier up | `$0.00`: a size gate writes no wrong figure. The cost is that the next edit to either door is forced to shave prose from the other, which is how a measured claim gets deleted for length | **OPEN.** Same class and same answer as N-152 / N-156: the module becomes a PACKAGE rather than being shaved a fourth time. Recorded rather than done here -- a package split moves W9909 / W9910 fence membership and is its own change | X-ak |
| balance | N-202 (X-f1e2's concurrency review 2026-08-05; the deadlock REPRODUCED against a real PostgreSQL with its own `DeadlockDetected` DETAIL line) | -- | **Account CREATION and a same-name account RENAME deadlock, and a live comment says they cannot.** `create_account` INSERTs `budget.accounts` before it reaches `lock_user_writes` (measured: INSERT at statement 1, advisory at 3), taking an index lock on `uq_accounts_user_name`; `routes/accounts/crud.update_account` takes the advisory lock at its own top and THEN updates `budget.accounts`. Same user, opposite orders. `crud.py`'s comment states that "no other `lock_user_writes` caller is known to take a `budget.accounts` row lock, so naming one here would be a claim nobody has tested" -- now tested, and false. N-193's shape on a second pair | PostgreSQL aborts one transaction: an unhandled 500 on a money-adjacent route, **no money corrupted** (the loser rolls back atomically). Needs two tabs creating and renaming to the same name for one user. Proven NOT to reach N-193's named antagonists: a pay-period truncate / reset / regenerate CASCADEs to `journal_entries`, `transfers`, `transactions` and `recurrence_rules`, never to `accounts` | **OPEN, and PRE-EXISTING -- X-f1e2 makes it strictly less likely** (the create path's unlocked window shrank from 5 statements to 0). The fix is N-193's own invariant, acquisition at the write-SERVICE entry, which is why it is owned with it rather than separately. The false comment clause was corrected in the same commit that reproduced it | X-ak |
| balance | N-203 (X-f1e3's design review 2026-08-05, verified against the tree) | -- | **The investment page's Growth chip is OUTSIDE the region that refreshes it, so it names a stale anchor day beside a fresh hero.** `investment/dashboard.html` renders `Growth since <day>` from the same `anchor_as_of` the hero reads, but in `.pulse-statrow`, a SIBLING of `#investment-hero-region` -- so no `balanceChanged` listener redraws it. Its own comment claims growth + contributed reconciles with the hero balance minus the anchor to the cent, which is visibly false until reload | `$0.00` of stored data; the rendered growth figure is measured from a day the page no longer shows, until any full page load | **OPEN, PRE-EXISTING.** No version of the `#anchor-as-of` snippet ever targeted this chip, so the X-f1e3 narrowing neither caused nor could fix it; the fix is that page's region boundary. It falsified a claim X-f1e3 had written, which was corrected in the same commit | X-j |
| balance | N-205 (X-f1e3's design review 2026-08-05; the overlay confirmed in a Chromium screenshot. NARROWED and re-pointed 2026-08-10 by X-f2-b, which closed its cash half) | -- | **The only evidence a back-dated assertion landed auto-dismisses after 8s, and on the MODELLED account kinds nothing durable records it anywhere.** The original AST pass found NO read path from `AccountAnchorHistory` into a template except the governing-assertion captions -- which by definition are not the back-dated row. X-f2-b built that read path for the CASH detail page (Checking / HYSA / Money Market), so what survives is the set that page 404s: an assertion written through the SAME anchor door on a Roth IRA, a Traditional IRA, a 401(k) or a Property has no durable record on any screen. The loan page has had its own since Commit 16 | **16 of production's 79 assertion rows**, measured 2026-08-10 off `shekel-prod-db`: 61 are on the three kinds the cash card now serves (Checking 57, HYSA 1, Money Market 3) and 2 on loans, leaving 15 retirement / investment and 1 Property with no retrieval path. `$0.00` of stored data; the cost is a re-entered balance, which is the churn N-172 measures at `$321.52` average | **OPEN, NARROWED.** The 8s autohide was the developer's explicit choice at X-f1e3's design fork and is NOT reversed. Re-pointed to **X-j** by developer ruling 2026-08-10 rather than widening X-f2-b: the card's `correction` column is `recorded - balance_before`, which has no meaning on a modelled account (there is no records-balance to correct against), so the modelled card is a different card and a different ruling -- and X-j already owns **N-213** (the same pages' missing difference preview) and **N-83** / **N-87** (the modelled-vs-asserted display question) | X-j |
| balance | N-207 (X-an-a's production measurement 2026-08-09, read off `shekel_f3_final` on every day of a 306-day span) | -- | **The resolver picks the LATEST anchor a loan will ever have, whatever date the read asks about.** `loan_resolver._periods.select_latest_anchor` takes `max(anchor_events, key=(anchor_date, created_at))` over `load_loan_anchor_facts(params)` -- every anchor the loan carries -- and `_replay_from_anchor` never passes `ctx.as_of`. So a read of a PAST date seeds and bounds its replay from a balance asserted AFTER it, and the `anchor_date < due_date` bound then excludes every installment due before that future assertion. Same class as FU-3 and N-40: a producer inside a pass the context has pinned resolving at a moment the context did not choose | `$0.00` of BALANCE: `positions()` folds the walk, which resets at each anchor in date order, and it reads correctly ($202,000.00 at 2026-03-01 on the clone). Measured wrong: the loan's SCHEDULE and its `next_pay_date`, which come from the replay -- the Mortgage reports its next installment as 2026-06-01 for EVERY read date back to 2026-03-01, because the governing anchor is a true-up dated 2026-05-22, up to 82 days after the read | **OPEN, found while measuring X-an-a and deliberately not taken in it**: X-an-a moves WHICH DAY a payment counts from, this is WHICH ANCHOR a pass resolves against, and changing it moves the replayed balance on every historical read | X-i2 |
| balance | N-208 (X-an-b's census 2026-08-09, read off the query) | -- | **The integrity checker orders a cash account's assertions by the RECORDING instant, so "consecutive entries" there means a different sequence than everywhere else.** `scripts/integrity_check.py:378-391`'s BA-05 ("large anchor balance jumps") computes `LAG(anchor_balance) OVER (PARTITION BY account_id ORDER BY created_at)` over `budget.account_anchor_history` -- no `observed_on` term and no `id` term. That is the key `cash_ledger` replaced at plan step 2 precisely because a user-supplied business date and a recording instant can order differently, and the app's two live cash orderings both key `(observed_on, created_at, id)`. X-an-b's census was scoped to `app/`, which is how a `scripts/` copy survived the sweep that closed N-196 | `$0.00`: BA-05 is a warning-severity heuristic that renders no figure. The exposure is that it flags and misses the WRONG PAIRS the moment an account carries a back-dated assertion -- which plan step X-f1c4c made an ordinary gesture | OPEN, found 2026-08-09 by X-an-b's census and deliberately not fixed in it (a `scripts/` checker is a different subject from the loan read path) | X-f4 |
| balance | N-209 (X-an-b's build 2026-08-09, measured against the harness) | -- | **The test suite CANNOT construct the `created_at` tie production produces routinely, so the whole anchor tie-break class was invisible to it.** `tests/_test_helpers._FrozenDbClock.stamp` issues each frozen `now()` one microsecond past the last, DELIBERATELY -- its own docstring says a flat instant would make an `ORDER BY created_at DESC` tie "a coin flip". But production's `now()` is TRANSACTION START, so every row a backfill writes in one transaction shares an instant: `shekel-prod-db` carries four (ids 1-4, all `2026-05-22 02:41:22.187019+00`). So the instrument that makes fixtures deterministic also makes the defect class unreachable, and X-an-b's firing control had to set `created_at` explicitly to build the state it tests. Third gap in the clock-instrument family, with **N-138**'s process-vs-display split | `$0.00` directly; the cost is ORACLE COVERAGE -- N-196 could not have been found by the suite, and was not: it was found by reading | OPEN, found 2026-08-09. NOT a defect in the rewriter (its determinism is load-bearing); the question is whether the suite needs a sanctioned way to build a same-instant pair, so the next tie-break defect has a home | developer-decision (2026-08-09; the same instrument family as N-138) |
| balance | N-210 (X-an-b's adversarial review 2026-08-09) | -- | **Two package exports have no caller outside their own package, and one of them now carries an unenforceable ordering precondition at that public boundary.** `loan_resolver.select_latest_anchor` (exported at `loan_resolver/__init__.py:105,125`) has ONE `app/` caller, `_periods._replay_from_anchor`, in the same module; `loan_ledger.merge_anchor_and_payment_events` (`loan_ledger/__init__.py:77,110`) has ONE, `_walk.walk_loan_ledger`, twelve lines away. X-an-b made the second REQUIRE a pre-ordered anchor list -- correct for its one caller, which loads from the loader, and a trap for any future out-of-package caller. Separately, `select_latest_anchor`'s input is duck-typed by convention: X-an-b tightened it to need `event_id`, and `app.utils.dates.DatedAssertion` now names the shape structurally without forcing it. The N-96 / N-85 shape (a public entry no screen reaches), here about package membership rather than the seam | `$0.00`: every production caller is in-package and correct. The exposure is the next caller | OPEN, found 2026-08-09 by adversarial review. The from-scratch answer named at X-an-b and deliberately deferred: hand the resolver the GOVERNING ANCHOR rather than a list it reduces (`LoanInputs.anchor_events` is consumed in exactly one place), which deletes the export, the reduce and the precondition together -- 4 `app/` sites and 68 `tests/` sites | E2-0 |
| balance | N-211 (X-an-b's adversarial review 2026-08-09) | -- | **A full anchor tie means two contradictory balance assertions for one day, and nothing tells the operator one exists.** Since X-an-b the app resolves such a tie deterministically (the later INSERT governs), which is the right behaviour and also silences the only symptom. The HTTP door cannot produce the state -- `anchor_service._append_loan_anchor_and_sync` writes at most one row per transaction and is the only `LoanAnchorEvent(` construction in `app/` -- so a real tie means a backfill, a migration or hand-SQL wrote two different balances for one date. That is a data-integrity event, and the app's posture elsewhere is to fail loud or at minimum `log_event()` it | `$0.00` on production, which carries no tie. The cost is that the FIRST one will be invisible | OPEN, found 2026-08-09. Recorded rather than built: a detector on a read path costs a scan per load and the honest home may be `integrity_check` (a BA- rule) rather than the loader | X-f4 (with N-208, the other integrity-checker row) |
| balance | N-212 (X-f2-a's build 2026-08-09, measured against the live schema and against `shekel-prod-db`) | -- | **Five schema fields round money with BANKER'S rounding, and the checker built to forbid exactly that cannot see them.** `fields.Decimal(places=2)` quantizes inside Marshmallow with the decimal context default (`ROUND_HALF_EVEN`), so the write door accepts `1157.385` and stores `$1,157.38` where `app.utils.money.round_money` (`ROUND_HALF_UP`, the project's rule) gives `$1,157.39`. W9904 `shekel-bare-money-quantize` exists for this exact divergence and reports CLEAN, because the `.quantize()` is a library call its AST scan never reaches -- a rule with a detector that cannot see the surface, which is the fence shape this arc keeps finding. The five: `accounts.AnchorUpdateSchema.anchor_balance` and `AccountCreateSchema.anchor_balance` (both halves of a BALANCE ASSERTION, the figure this whole arc reconciles against), `templates.default_amount`, `loans.extra_monthly`, `transactions.estimated_amount`. **The other 99 `fields.Decimal` carry no `places` and are correct BY ACCIDENT**: they hand full precision to a `numeric(12,2)` column, and PostgreSQL rounds half away from zero -- verified on `shekel-prod-db`, `1157.385 -> 1157.39` -- which happens to be the project's rule | `$0.01` per submission, at every half-cent boundary. Not reachable through the anchor form in a conforming browser (`<input type="number" step="0.01">` refuses a third decimal), and the schema is the guard rather than the input, so any non-browser client reaches it on all five | OPEN, found 2026-08-09 while proving X-f2-a's preview needs no rounding of its own (it does not -- both operands are cent-exact BECAUSE of these quantizes, which is how the divergence surfaced). Deliberately not fixed there: swapping one field's rounding mode is a band-aid on five sites, and the structural answer is already scheduled | G2 |
| balance | N-213 (X-f2-a's rebuild 2026-08-09, scoped deliberately) | -- | **The true-up preview answers nothing for a MODELLED account, and six of the seven editable accounts are modelled.** `balance_at.records_balance_at` returns `None` unless `classify_account` says `PLAIN`, because "what do your records produce" is a fraction of what an HYSA / brokerage / Property renders -- so a difference drawn against it would caption a MODEL-vs-MARKET gap with copy about untracked spend. The scope is right and the SILENCE is the finding: the editor opens on Checking, HYSA, Money Market, Roth, Traditional, 401(k) and Home, and six of those now show an empty region where the seventh shows a reconciliation | `$0.00` -- no wrong figure is rendered, which is the point of the scope. The cost is that a Property or 401(k) true-up gets no feedback at all, and the figure those accounts DO want (the modelled value against the market value the user is typing) is a real and useful one | OPEN, recorded at the moment the scope was drawn rather than discovered later. Production bears out the priority: **57 of the 78 non-loan assertions are on Checking**, the one PLAIN account, and the modelled accounts carry 1-6 each | X-j |
| balance | N-214 (X-f2-a's adversarial code review 2026-08-09, read off the limiter config) | -- | **A read that fires while the user types shares one rate-limit ceiling with the write it precedes.** `RATELIMIT_DEFAULT = "200 per hour;30 per minute"` (`app/config.py:241`) applies to every route without an explicit limit, and only `health` / `static_pass` are exempt -- so `accounts.anchor_difference` and `accounts.true_up` draw from the same per-IP bucket. Editing several balances in one minute now spends previews out of the budget the SAVE needs, and `base.html` leaves 4xx non-swapping, so the refused save renders nothing at all rather than a message | not priced. The trigger set was narrowed at the rebuild and the result MEASURED in Chromium against the repo's own vendored htmx -- 0 requests on form-open, 1 after typing a balance, 0 more on blur, 1 more when the date changes -- so the profile is exactly one extra request per edited field, against the two per field a `keyup` + `change` pair cost. Reaching 30/minute needs a deliberate burst | OPEN. The fix is a per-route ceiling for the read, or an exemption, or a ruling that the shared bucket is acceptable -- and it is a judgement about the app's rate-limit posture rather than about this preview. **A related exposure is already mitigated and is recorded here so it is not re-discovered**: the preview carries the balance in the QUERY STRING, and production nginx logs `$uri` only (`deploy/nginx-bundled/nginx.conf:74`), so no figure reaches an access log | operator (give the preview its own ceiling, exempt it, or rule the shared bucket acceptable?) |
| balance | N-218 (X-f2-c1's adversarial design review 2026-08-10, census by grep over `app/`, `tests/`, `docs/`, `tools/`, `scripts/`) | -- | **Twelve files cite a design document at a path that does not exist.** `docs/audits/balance_architecture/anchor_settle_partition.md` was ARCHIVED; it lives at `.../archive/anchor_settle_partition.md`, and the un-archived path resolves to nothing. The citations are in the modules the partition is ABOUT -- `app/models/transaction_entry.py:30`, `app/services/posting_service.py:747`, `app/services/anchor_service.py:21`, `account_posting_service/_walk.py:23`, `cash_ledger/_amounts.py:83`, `cash_ledger/_events.py:32` and six test files -- so the reader most likely to follow one is a reader trying to understand ruling R-DH | `$0.00`: a dead path writes no wrong figure. The cost is that a docstring's PROOF is unreachable, which is this project's own standard of evidence (N-97 is the same shape one level down -- a live citation naming a producer deleted a month earlier) | OPEN, born with an owner. The thirteenth citation was in the template X-f2-c1 rewrote and is corrected there, under the rule that editing a file clears that file's residue in the same pass. The rest are a mechanical sweep and are owned by **X-f4**, which deletes the `ReconciledThrough` family across 14 files and touches most of them anyway | X-f4 |
| balance | N-217 (X-f2-c1's adversarial design review 2026-08-10, confirmed by an id census over the rulings table) | -- | **A ruling id can resolve to TWO different rulings, and no gate can see it.** `tools/plan_gate` grades `ledger.md`'s finding ids, `steps.md`'s step ids and the dependency graph between them -- the arc documents' RULINGS tables are not parsed at all. Two sessions working the same day both appended a ruling as **R-EX**: X-ad's *"Registration ASKS rather than inventing"* and X-f2-c's *"what settling a row MEANS is a service verb"*. Both reached `dev`, both are cited by immutable commit messages (`87d6a70b`, `daa9c402`), and the collision was found by a reviewer reading the table, not by any gate | `$0.00` of money; the cost is that **X-f2-c2** and **X-f2-c3** cited `R-EX` and resolved AMBIGUOUSLY -- a step specification pointing at the wrong rule is how a leaf gets built to the wrong ruling. Census: exactly ONE collision in 69 rulings, so the instrument is missing rather than the discipline | OPEN, born with an owner. The instance is fixed here (this arc's ruling renamed **R-FA**, the shipped-step citation keeps `R-EX`, and the renamed row names its old id so a commit message still resolves); the CLASS wants an arm, which is ~15 lines against the parser the other arms already use | X-ao |
| balance | N-219 (X-f2-c2's adversarial correctness + design reviews 2026-08-10, both found it independently; reproduced on this tree AND on the merge-base `6498d34c`) | -- | **A transaction has THREE settle doors and one of them books a different figure.** Ruling **R-FA** named "two route branches" -- `mutations.py`'s mark-done and `_shadow_mutations.py`'s -- and X-f2-c2 put both on `transaction_service.settle_transaction`. The third is `mutations._apply_regular_update`, reached by the Status dropdown on the full-edit popover (`_transaction_full_edit.html:163`, where Paid is NOT disabled for a purchase-tracked row). It flips the status through the seam and reconciles the ledger, but never consults the entries, so it settles at `COALESCE(actual, estimated)` where Mark Paid settles at `sum(entries)`. The finalised-edit lock does not bite: it grades the CURRENT status, and the row is Projected on the way in | **Measured, one envelope, `$400.00` estimated, one `$25.00` purchase: Mark Paid books `-$25.00 / +$25.00`, the dropdown books `-$400.00 / +$400.00`. `$375.00` of phantom spend from two controls in the same card**, and the double-entry ledger follows whichever was used | **OPEN, born with an owner.** Pre-existing -- neither caused nor worsened by X-f2-c2, which is why it is not folded into it. Routing the door onto the verb CHANGES what a full-edit Save books, so it MOVES MONEY and takes its own commit and its own before/after figures. Developer ruled 2026-08-10: own step, after X-f2-c2's money commit and before X-f2-c3 | X-ap |
| balance | N-220 (found while archiving ten shipped rulings at X-f2-c2, by a backtick-parity scan over every rulings table in the corpus) | -- | **Seven archived rulings state an id and no rule.** A `\|` inside a backticked identifier truncated the cell when the rows were condensed into `archive/phase_x_as_built_2026-08-04.md` Section 3: `R-AS` reads `` `_interest ``, `R-BX` reads `` `BalanceContext ``, `R-CL` reads `` `TrendPoint ``, and `R-AJ` / `R-AU` / `R-CQ` / `R-DJ` are cut the same way. **This is N-217's mirror** -- that one is an id resolving to TWO rulings, this is an id resolving to NONE -- and the same missing gate covers both: no rulings table is parsed at all | `$0.00` of money. The cost is that `R-CQ` and `R-DJ` are cited by LIVE code (`status_seam.py`, X-f4's `ReconciledThrough` deletion set) and a reader following the citation finds an id with no rule. All seven are recoverable from git -- verified for `R-AS` (`d56995d7`) and `R-BX` (`7d4e4986`) | **OPEN, born with an owner.** The repair is mechanical (restore from the pre-archive revision, escape the pipe); the ARM that stops the next one is the same one X-ao already owes, widened by one predicate: a ruling's cell must state a rule, not just an id. **Writing THIS row demonstrated both halves at once**: its first draft carried a literal `\|` and so held 9 pipes, `ledger.md`'s parser dropped it, and the stated-count arm (rule 3) failed the commit -- the registry catches the defect the arc documents do not, which is the asymmetry X-ao closes | X-ao |
| balance | N-221 (X-f2-c2's adversarial design review 2026-08-10) | -- | **The settle verb reconciles the ledger once PER ROW, and the reconcile tick calls it in a loop.** `transaction_service.settle_transaction` ends in `posting_service.sync_transaction_postings`, which per call resolves the account's ledger account, gets-or-creates the category ledger account, reads the posted set for the period and runs the anchor-correction self-heal. This is ruling **R-DL**'s shape one tier up -- that ruling made the anchor reconcile resolve its two ledger accounts once per ACCOUNT rather than once per correction -- and it is why `carry_forward_service` could not use the verb | `$0.00` and no wrong figure; a latency and statement-count cost. Production's replay offers up to **30** tickable rows on one assertion day, so one tick can mean 30 reconciles inside one request | **ANSWERED at X-f2-c2 and RE-POINTED, not closed.** N reconciles accepted with the reason in `_transactions.record_settled_transactions`: a batch sibling already has an owner -- X-ai-a's stated mandate IS the batched cash reconcile, measured at 8 statements against 696 assembled -- and a second batch implementation here is the duplication that step exists to remove. Bounded by data rather than hope: replayed over 53 production assertion days the worst offers 9 transaction rows and the mean is 4.02 over the 46 that carry any, and `carry_forward_service` already loops the same reconcile per envelope with no bound at all | X-ai-a |
| balance | N-224 (X-f2-c2's design 2026-08-11, ruled the same day as **R-FE**) | -- | **A projected row's amount has TWO answers and nothing reconciles them.** `transactions.estimated_amount` is a CACHE of a derivation (the salary profile's net pay for the period). `income_service.live_projected_net` recomputes it at READ time and writes nothing back, so `sum_projected` and every balance surface show the live figure while every WRITER -- the grid's Mark Paid, the reconcile tick, `_apply_regular_update` -- books the stored one. Its own docstring names the cause: a profile, calibration or financial-calc CODE change that fired no regeneration. The same shape carries the loan half (`live_loan_transfer_amounts`), which is **N-40** | a settle can move the projected end balance by the whole gap, silently, in either direction. **The hand-correction figures this row first cited do NOT measure it** and were withdrawn 2026-08-11 by an adversarial review: the owner corrected 3 of 10 settled paychecks by `-$38.05` / `-$7.57` / `-$33.15`, but all three carry `is_override`, which `live_projected_net` excludes by design -- so on precisely those rows the two answers cannot differ. They measure "the real deposit differed from the projection", a different quantity. What IS measured: `$0.00` of stored drift TODAY -- the non-overridden rows were generated from the current profile -- which is exactly what `conventions.md` rule 8 says is not resolved | **OPEN, born with an owner.** **X-aq** makes every door agree (one rule, in the verb) and is sequenced ahead of X-f2-c2's money commit; **X-ar** deletes the second answer, which is what closes this row and N-40 | X-ar |
| balance | N-225 (X-f2-c2's adversarial design review 2026-08-11) | -- | **The reconcile arms are cut on the wrong axis, and the next leaf pays for it.** `_purchases` and `_transactions` are split by ROW KIND, justified in the package docstring by "three row kinds whose settle verbs are genuinely different". Measured, only ONE is different: the purchase arm is a bulk `UPDATE`, while the transaction arm and X-f2-c3's transfer arm are both "query `Transaction` under a scope, narrow in Python by attribution date, loop dispatching to a per-row service verb" | `$0.00`. The cost is ~250 lines of `_transactions.py` copied into `_transfers.py` so a docstring's "three arms" claim stays true, and two scopes that can then drift apart | **OPEN, born with an owner.** NOT extracted at X-f2-c2: there is one arm of that shape today, and parameterising for a caller that does not exist is rule 13's speculative structure. X-f2-c3 EXTRACTS rather than copies -- the shared shape is (extra scope clause, settle callable, `OfferKind`), and its own settle differs enough (a transfer id, both legs, the loan-payment freeze) that the extraction is the leaf's design question rather than a mechanical one | X-f2-c3 |
| balance | N-226 (X-f2-c2's adversarial reviews 2026-08-11, both lenses) | -- | **An envelope's offered figure counts credit-card purchases the checking statement can never show.** `settle_amount` -> `entry_service.compute_actual_from_entries` sums ALL entries including `is_credit`, while the purchase arm deliberately EXCLUDES them from the offer list with a docstring explaining why (a credit purchase leaves through its own CC Payback sibling and never touches checking). So an envelope holding a `$40` debit and a `$60` credit is offered at `$100.00` on a screen captioned "tick everything your statement shows", against a statement showing `$40`, and `payment_total` aggregates the `$100` | `$0.00` booked -- `settled_cash_leg` subtracts the credit sum, so the LEDGER is right and only the panel's figure is wrong. Latent: production carries 18 credit entries in history and **0 on a Projected envelope** today | **OPEN, born with an owner.** Not fixable by changing the offered figure alone: the panel's amount is what a tick BOOKS, and `actual_amount` legitimately IS total spend. It needs the panel to render the CASH effect beside it, or the credit half to be captioned -- a display ruling, and X-f2-c3 is the leaf that already owes the panel a second money column for a transfer's two legs | X-f2-c3 |
| balance | N-228 (X-f2-c2's adversarial reviews 2026-08-11) | -- | **`_freshest_amount` defeats the batching its own producer was built for.** `live_amount_overrides` is a BATCH producer -- `ProjectedBasis` exists so it is built once per account per request -- and this calls it with a one-element list. Per offered row per reconcile POST it runs up to twice (the panel read and the verb), and for a salary-linked row each call runs `paycheck_calculator.project_salary` over the full pay-period set | `$0.00`; latency. Free today only by an accident of the data: all three of production's offered income rows are ad-hoc (`template_id IS NULL`), so no call is a candidate. The worst historical day carried 3 income offers | **OPEN, born with an owner.** Closed by deletion rather than by threading a map: **X-ar** removes the read-time override entirely, so the N+1 disappears with the mechanism. Threading an optional pre-resolved map into `settle_amount` meanwhile would be an argument a caller can get wrong, which Section 8 rules a defect rather than a contract | X-ar |
| balance | N-229 (X-f2-c2's adversarial reviews 2026-08-11, traced from N-227's hazard claim) | -- | **An entry recorded against a SETTLED-status envelope is accepted, persisted, and silently inert.** `entry_service.create_entry` blocks only Cancelled and Credit, so a row in the terminal `Settled` status takes new purchases. `_update_actual_if_paid` is gated on `is_done` -- exactly `Paid` -- so `actual_amount` is NOT recomputed; `_resync_postings_if_settled` is gated on the whole settled BAND, so it fires anyway and reconciles the postings to the now-stale `effective_amount`. The two halves of one act use two different status predicates. `done -> settled` is a legal transition and `Settled` is documented as terminal (`state_machine`) | `$0.00` today -- production carries NO envelope in `Settled` status (24 of 24 settled envelopes with entries are `Paid`, and all 24 have `actual_amount == sum(entries)`, so the recompute path is live and working). Latent: the grid's envelope progress reads the ENTRY sum, so a user would see spend the balance and the ledger do not have -- two answers one screen apart | **OPEN, born with an owner.** Zero coverage: `StatusEnum.SETTLED` appears in neither `test_entry_service` nor `test_mark_paid_entries`. Pre-existing -- neither caused nor worsened by X-f2-c2, whose close writes `Paid`. The repair is one status predicate for both halves, and it belongs with the step that is already making every settle door agree | X-ap |
| balance | N-230 (X-f2-c2's adversarial correctness review 2026-08-11) | -- | **An envelope with NO entries settles at two different figures depending on the door.** `settles_from_entries` is `tracks_purchases AND entries`, so an entry-less envelope takes the MANUAL branch and books `effective_amount` -- its budget. `settle_from_entries`, which `carry_forward_service` calls directly, states the opposite rule for the identical act in its own docstring: "Empty entries on an envelope row settle at zero spend", and books `Decimal("0")`. Two doors, one row kind, a full estimate apart | Live on production: `Kayla's Spending Money` is envelope-tracked, budgeted `$100.00`, carries ZERO entries, and is one of the three rows the panel offers against the 2026-08-06 assertion. Ticking it books a `$100.00` category expense for spending that did not happen. The projected balance is unaffected (an absorbed source contributes nothing forward), so the cost is on the ledger and in `/analytics`, not on the balance | **OPEN, born with an owner.** Both readings are defensible -- a cash envelope really did cost its budget; a carried-forward one really did cost nothing -- so this is a RULING owed before it is a fix, and the two callers may legitimately differ. What is not defensible is that the difference is undocumented and falls out of which function a caller happened to reach for | X-ap |
| balance | N-231 (X-f2-c2's adversarial correctness review 2026-08-11) | -- | **The reconcile writer's `corrected_count` counts machine writes as hand-typed corrections.** `_transactions.record_settled_transactions` counts rows whose `actual_amount` CHANGED, but an envelope's close always writes that column (`settle_from_entries` sets it to `sum(entries)`), so every envelope tick increments the count. The `transactions_reconciled` event describes the field as "some carrying a corrected amount", and ruling **R-FB**'s production measurement ("11 of 93 settled bills carry a hand-typed correction") is made of exactly this signal | `$0.00`. Observed directly: a probe settling one envelope with no correction submitted logged `corrected_count: 1`. The cost is that the only telemetry distinguishing a correction from a settle is wrong in the direction that over-reports | **OPEN, born with an owner.** The fix is to count what the CALLER supplied and the verb applied, rather than what the column did -- which the arm cannot currently see, so it wants the verb to say. X-f2-c3 is the next leaf inside this writer | X-f2-c3 |
| balance | N-232 (X-f2-c2's adversarial correctness review 2026-08-11) | -- | **Three branches of the reconcile route are graded by nothing that can fail.** The partial-landing notice (`recorded < asked`) has ZERO coverage -- the one test that reaches this code takes the nothing-landed arm, so deleting the partial branch or swapping the two message constants leaves it green. The `StaleDataError` refusal has zero coverage. And no test asserts that an amount box submitted WITHOUT its checkbox is inert, which is the modal case: the panel renders a box on every correctable row and an HTML form posts them all, so a five-row panel posts five boxes on every submit | `$0.00` -- the code is correct under trace (a correction is only reachable for a row the writer re-derived through its own scope), and the partial arm is the one the route's own docstring argues matters most for this arm, because it hides a status change, an amount and a ledger posting rather than a column stamp | **OPEN, born with an owner.** Two ticks where one is out of scope asserting the partial message specifically; a forged `actual_amount-<id>` for an unticked row and for another account's row; and a negative control that a NON-correctable row renders no box at all | X-f2-c3 |
| balance | N-233 (X-f2-c2's adversarial design review 2026-08-11, verified pre-existing against the merge-base) | -- | **`settle_transaction`'s two branches disagree about whether a soft-deleted row may settle.** The envelope branch refuses `is_deleted` by precondition, with its own docstring giving the reason (a status change must not resurrect a deleted row). The MANUAL branch has no such check, and `get_accessible_transaction` does not filter `is_deleted`, so `POST /transactions/<id>/mark-done` on a soft-deleted NON-envelope row flips it into the settled band and stamps `settled_on` | `$0.00` -- a deleted row values at zero through `effective_amount`, so the ledger stays flat. The cost is data integrity, and that the same commit hoisted `reject_transfer_shadow` onto the verb arguing "a verb owns its own preconditions" while leaving its neighbour behind | **OPEN, born with an owner.** Pre-existing (reproduced on the merge-base), so reported rather than folded in per rule 6. The fix is to hoist the `is_deleted` refusal beside `reject_transfer_shadow` so it guards both branches, at the step that next revisits this verb's doors | X-ap |
| balance | N-215 (X-f2-b's design review 2026-08-05, VERIFIED against the fence and both producers 2026-08-10) | -- | **One rule about an assertion log is stated by two producers, and the fence registry rules the same COLUMN two opposite ways.** `loan_posting_service.loan_balance_anchor_history` and `balance_at.cash_anchor_history` both return every balance an account was told it held beside the running total immediately before it -- the loan calls that column `owed_before` (rendered as `computed`, `_display.py:279`), the cash card calls it `ledger`. `balance_seam.py:471` lists the loan one as a ruled NON-producer ("the anchor EVENT rows ... not the balance itself") while `balance_at/__init__.py:83` puts the cash one INSIDE the seam on the opposite ground -- "its `ledger` column IS a balance-at-T". One of the two rulings is wrong. The producers differ in exactly one documented way (the loan filters `anchor_date <= as_of` because a loan anchor may be dated ahead of a display date; a cash assertion cannot be), which is a parameter, not two subjects | `$0.00`: both figures are correct and each is the walk's own arithmetic. The cost is that W9906 / W9909 protect one spelling of a balance-at-T and exempt the other, so the NEXT such column lands on whichever ruling its author reads first | OPEN, born with an owner. The fence half is exactly the trace **G1** is specified to run -- boundary genuinely absent, member spelled as an outsider, or a value-level invariant a TYPE could carry -- and the producer half is whether the two become one parameterised entry | G1 (the two rulings) / E2-0 (whether the two producers become one) |
| recurrence | D2 | -- | an edit ignores the chosen "First paycheck": `effective_from` overrides `start_period_id` | 4 rows materialised in excluded periods | OPEN, NARROWED at R4b-1 -- the bound is no longer bypassable by a caller's window, because the start period is looked up in the OWNER's schedule; what survives is the FIELD, which R7b deletes | R7b (the form rewrite that retires `start_period_id`) |
| recurrence | D4 | -- | a loan's cash date and contractual installment date cannot differ | $0 -- labels only (payoff date, schedule rows, history) | OPEN | R6 |
| recurrence | D6 | -- | folding `start_date` into `anchor_date` is lossy for PERIOD-unit rules | the loan origination bound stops being an exact date | OPEN, NARROWED TWICE -- R-R8 made an anchor a date, and R2d made it COMPUTED, so the "a rebuild strands the anchor" half is dead: there is no stored anchor to strand, and `_repoint_recurrence_rules` narrowed back to the rules the wipe actually nulled | R9 (re-check before dropping `start_date`) |
| recurrence | F-2 | -- | the ref-seed parity scan's last `INSERT` body runs to end-of-file | none today; a literal quoted below the seed would pass | OPEN | R-F2 |
| recurrence | F-3 | -- | `ref` tables use auto-named constraints while the database rule says name them | none -- the names are never referenced | OPEN | R-F3 (developer ruling first) |
| recurrence | F-6 | -- | a hard-deleted template leaves its recurrence rule behind forever | 3 orphaned rules on production today (was 5; R2e-3 deleted 2 of them) | OPEN | R-F6 |
| recurrence | D8 | -- | `offset_periods` is a declared schema field (`templates.py:67`, `transfers.py:65`) that NO template renders, so only a crafted POST can author a phase | none -- it is the one derived value still stored, re-derived on EVERY write since R2c-1, and the anchor carries whatever phase it states; vestigial, not wrong | OPEN | R7b (the form rewrite that deletes it) |
| recurrence | D18 | -- | `compute_due_date` picks its base month from a period's two ENDPOINT months and never consults `month_of_year`, so a rule firing in a month that is neither endpoint is dated in the wrong month | measured on the R1 blob's new long-cadence shapes: a January annual rule (`moy=1`) is dated **1 March** at a 90-day cadence, twice over the captured span. Latent at 14 days, where the firing month always IS an endpoint | OPEN | R5 (the step that rewrites the due-date model) |
| recurrence | D19 | -- | `idx_transactions_template_period_scenario` is UNIQUE over `(template, period, scenario)`, so one template can owe a paycheck only ONCE -- but a monthly bill legitimately occurs several times in one paycheck at a cadence >= 30 days | the index is a generation-idempotency guard keyed on the wrong column: a generated row's identity is its OCCURRENCE, not its paycheck. Measured: a monthly bill at a 90-day cadence occurs 3x per paycheck, and `Monthly First` defers 2-3 months onto one. Rows stay SEPARATE and only the grid sums them (developer ruling 2026-08-07), so the fix is to re-key onto `(template, scenario, occurs_on)` -- which needs **R-R12, not merely D18**, and that correction is dated 2026-08-08: fixing the endpoint-month scan leaves a DAY-LESS rule dating every row `period.start_date`, so three deferred `Monthly First` occurrences still collide on exactly the data D19 describes. Only writing the OCCURRENCE into the column gives them distinct dates. Generation REFUSES meanwhile, naming the definition, the paycheck and every occurrence DATE inside it -- the count at R4a, the dates since R4b-2 gave generation the pairs (developer ruling 2026-08-08); the alternatives measured were an IntegrityError 500 that aborts the enclosing transaction, or a silently under-budgeted paycheck | OPEN | R5 (re-key it, after D18 gives the rows distinct dates) |
| recurrence | D20 | -- | the placement axis has no "the LAST paycheck on or before the occurrence", so a bill funded IN ADVANCE is inexpressible | both members fund on or AFTER: at a 90-day cadence today's `Monthly First` funds February's rent on 31 March, 59 days late. Zero impact at a cadence <= 28 days, where every month holds a payday | OPEN | R8 (the add-ons step, which already owns the placement-adjacent business-day shift) |
| recurrence | D21 | -- | the PERIOD unit's phase is read from the `offset_periods` COLUMN, which R7c drops | deriving it from the anchor instead diverges exactly where `_phased_period_anchor` falls back to the raw bound (fewer than `interval_n` periods remain past the bound): today's engine generates nothing there, an anchor-derived phase would generate from the first out-of-phase period | OPEN | R7c (the authored anchor must carry the phase by construction) |
| recurrence | D10 | -- | a `Monthly First` anchor is horizon-dependent: extending the schedule can move it a month earlier | **none, MEASURED at R4b-2 and that is the finding's new content.** `_first_of_month_anchor`'s fallback -- the only horizon-dependent branch -- always answers a date strictly AFTER the schedule's last payday, so under `PERIOD_STARTING_ON_OR_AFTER` no occurrence derived from it can be placed on any period and no generated row can differ. Proven by argument, by a 3,390,012-pair sweep (1,935,097 of which reached the fallback, none an exception), and by the two `horizon_bound.*` baseline shapes plus `test_recurrence_resolution.TestTheHorizonDependentFirstOfMonthAnchor`. **No surface says so because no surface can see it** -- "say it on a surface" was ruled unwarranted 2026-08-08 on that measurement | OPEN | R7c (the backfill freezes the anchor into a NOT NULL column, which is where a horizon-dependent derivation stops being recomputed and starts being stored) |
| recurrence | D11 | -- | two branches of `_first_of_month_anchor` are unreachable, and one comments a case that cannot execute | none -- both are dead; proven by argument and by a 243,018-case sweep in which neither was ever taken | OPEN | R-F7 |
| recurrence | D12 | -- | `ref_cache.recurrence_unit_id` / `period_placement_id` / `business_day_shift_id` have ZERO callers in `app/` since R2d made `resolve()` return enum members | none -- they are correct and tested; the risk is a dead-code sweep DELETING them before their first consumer exists | OPEN | R7c (its backfill maps enums back to ids) |
| recurrence | D23 | -- | the recurrence write door refuses 3 of the table's 7 CHECK constraints; the other four reach the flush as an unhandled `IntegrityError` naming neither field nor value | none today -- every live writer is schema-validated. R4a added `ck_recurrence_rules_dom` and `ck_recurrence_rules_moy` to the door (they were forced: deleting `_match_annual` removed the only thing refusing month 13); `due_dom`, `valid_offset`, `positive_max_occurrences` and `single_end_bound` are not mirrored | OPEN | R7b (the form rewrite that gives `max_occurrences` its first writer, so it owns both bound CHECKs) |
| recurrence | D24 | -- | an `Every N Periods` rule's phase is READ from the start period when the schedule handed in contains it, and from the stored `offset_periods` column when it does not | none -- the write door has DERIVED the column from the start period on every write since R2c-1 (defect D1's fix), so the two can disagree only on a row written before that and never re-authored: zero on production, and all 46 live rules carry `interval_n = 1` where the phase is inert (measured 2026-08-08). Found by adversarial review of R4a, which is where the READ started agreeing with the write; the path-dependence is real -- the extend path hands over only the new periods, so it takes the column while create / regenerate take the derivation | OPEN | R7c (drops the column, so the authored anchor must carry the phase by construction -- the same requirement D21 states from the anchor's side) |
| recurrence | D27 | -- | `loan_loaders.loan_payment_due_date:608-616` documents a PRECONDITION nothing enforces: a loan payment's rule must carry a `day_of_month`, or `compute_due_date` falls back to `period.start_date` and writes a PAY-PERIOD START into the column the posting walk reads as the installment. The loan payment-transfer flow sets it (`routes/loan/payment_transfer.py:175`), but a loan payment set up as a plain every-paycheck transfer would not, and nothing refuses that | `$0.00` today, MEASURED 2026-08-08: both live loan rules are Monthly carrying `day_of_month` equal to their `payment_day` (Mortgage 1, Van 22). The exposure is the NEXT loan set up the other way, and the symptom is a silently wrong POSTED balance rather than a wrong label -- migration `c4e91a7b2d38` exists because this already happened once | OPEN, found 2026-08-08 | R5 (the installment gets its own column, `due_on`, so the precondition becomes structural rather than documented) |
| recurrence | D26 | -- | a generated row's DATE has TWO producers: the forward engine computes the occurrence and `resolve_generation_plan` carries it to the write loop, which discards it and re-derives a date from the PERIOD instead (`recurrence_engine.py:342`, `transfer_recurrence.py:127`). `PlannedOccurrence.occurrence` is read in exactly two places in `app/`, neither of which dates a row. The second producer is also POLYMORPHIC: `transactions.due_date` / `transfers.due_date` holds a pay-period start for a day-less rule, an in-period calendar day for the ordinary bill, a NEXT-MONTH day when `due_day_of_month < day_of_month` (a live form field), and the CONTRACTUAL INSTALLMENT for a loan payment, which the ledger reads as a POSTING INPUT | at the developer's 14-day cadence the two agree for every calendar rule, so the exposure today is the shapes where they cannot: `Monthly First` (measured on production, occurrence 2026-08-01 against a stored 2026-08-13, 11 rows) and every loan payment (112 shadows; 2 carry a date OUTSIDE their own pay period, which `attribution_date` clamps for the calendar while the ledger reads it unclamped). D18 is this defect's long-cadence face | OPEN, ruled R-R12 2026-08-08 | R5 |
| recurrence | D28 | -- | ruling R-R13's `starts_on` cannot carry a calendar rule's cycle PHASE. It is specified as "the opening validity bound, ONE meaning for every unit" while section 3 maps `month_of_year` -> `starts_on.month`, and those cannot both hold: the phase is a month RESIDUE class and the bound is not in it | **18 of the 24 live multi-month rules**, measured against production 2026-08-08 by driving `_effective_start` and `resolve` over all 46. Christmas (annual, November 1) has `starts_on = 2026-03-26` and no column left holding November, so the forward walk fires it 1 March every year -- generated rows and the projected balance, not a label. The 6 that survive are the ones whose authored month happens to be March. `res(anchor) == res(moy)` in every row: the ANCHOR carries the phase exactly, the bound never does | OPEN, found 2026-08-08 at R7a-1 | R7c (rule it BEFORE specifying the cutover; the recorded direction is that `starts_on` stays the bound, the phase keeps its own columns -- `day_of_month` / `month_of_year` RENAMED to `nominal_day` / `nominal_month` and constrained against `unit_id`, not deleted -- and the first occurrence stays DERIVED, so there is no second representation to disagree with. Open sub-forks: how DDL states which phase columns each unit requires, and whether the YEAR unit survives as `(12N, MONTH)`) |
| recurrence | F-13 | -- | three holes in the arc's OWN gate: `OccurrencePlacement(` is constructed in exactly ONE place in the repository and never in `tests/`, so R4b-2's `__post_init__` invariant has no negative control; `.outcome` is asserted ONCE in the whole suite (`test_recurrence_engine.py:1708`, a well-built control for the healthy-schedule false alarm, but `SCHEDULE_GAP` is never asserted as a member and `test_recurrence_occurrence.py` asserts only `period is None`); and `SHEKEL_UPDATE_RECURRENCE_BASELINE` turns the 430-shape gate into a `pytest.skip` (`test_recurrence_baseline.py:56`) with nothing asserting the variable is unset | `$0.00` today. R8 adds a second `OccurrencePlacement` producer, which is when the missing control starts mattering | OPEN, found 2026-08-08 | R-F13 |
| recurrence | D29 | -- | `describe`'s coordinate dispatches ``WEEK -> weekday, else day-of-month``, so an R8 nth-weekday rule -- unit MONTH, no `nominal_day`, its phase in `recurrence_weekday_anchors` -- would render "Monthly (day 17)", the anchor's incidental day, for a rule meaning "the third Tuesday" | `$0.00` and unconstructible today: no writer of that subtype exists and `ResolvedRecurrence` carries no weekday field, so the shape cannot be built. Found by adversarial review of R7a-1, which also measured that the module's own `RecurrenceDescriptionError` does NOT fire on it -- the day is present, just wrong, which is the one failure mode a totality guard over the UNIT cannot catch | OPEN, found 2026-08-09 | R8 (the step that gives the subtype its first writer: the coordinate must dispatch on a COORDINATE KIND, not on "WEEK or else") |
| recurrence | D30 | -- | `RecurrenceRule.start_period` is `lazy="joined"` with ZERO readers -- the identical defect D17 closed on the line above it. The only mention in `app/` or `tests/` is a comment claiming `recurrence_engine` dereferences `rule.start_period.start_date` (`routes/_recurrence_form_helpers.py:257`), which is FALSE: `resolve` reads `calendar.period_by_id(spec.start_period_id)` | none -- one unnecessary join per rule load, same as D17's. Left out of R7a-1 deliberately (CLAUDE.md rule 6: D17 named the `pattern` relationship, not this one) | OPEN, found 2026-08-09 by adversarial review | R7b (the step that retires the "First paycheck" affordance and `start_period_id` with it, so the relationship and the false comment go with the column) |
| recurrence | F-15 | -- | the application has FOUR month-name producers, not one: `utils/dates._MONTH_NAMES_*` (the `month_name` filter and `describe`), plus `routes/analytics.py:506,553` and `routes/analytics_view.py:135,138` reading locale-dependent `calendar.month_name`, plus `ledger_report_service/_income_statement.py:129` using `strftime("%B")` | none in the developer's own container, which runs an English locale. A container started under a different `LC_TIME` renders those three surfaces' months in another language while every other surface and every test stays English | OPEN, found 2026-08-09 by adversarial review of R7a-1, whose own "single source of truth" claim was the thing measured false | operator (convert the three to `utils.dates.month_name` as its own commit, or rule that locale-dependent month names are acceptable there?) |
| recurrence | F-9 | -- | a transfer create with no baseline scenario reports success having materialised nothing -- the outcome the adjacent missing-period branch was just made to refuse | none: every owner gets a baseline at registration, so it is unreachable; both create paths (`_materialize_one_time_transfer` and `generate_transfers_for_all_periods`) share the shape | OPEN | operator (refuse both paths, or leave the broken-setup state to the baseline repair handler?) |
| recurrence | F-11 | -- | the grid's inline amount editor pre-fills from the STORED `estimated_amount` while the cell renders the LIVE recomputed one, and saving sets `is_override` -- the flag that excludes the row from that recompute | none today (all 61 salary rows agree after R4b-1's migration), and re-arms whenever a profile / calibration / tax / code change invalidates the cache without firing a regeneration, which is the exact situation `income_service.live_projected_net` exists for. The user is then shown one number in the cell and a different one on clicking it, and accepting the editor's value freezes the stale figure into the projection permanently. Verified by reading `_transaction_cell.html:39`, `_transaction_quick_edit.html:25`, `transactions/forms.py:48` and `transactions/mutations.py:295`; NOT driven through the UI. Surfaced while measuring D25, and not part of it -- the mismatch predates this arc | OPEN | operator (pre-fill the editor with the live figure so what you see is what you edit, or stop setting `is_override` when the submitted amount is unchanged?) |
| recurrence | F-4 | -- | `pay_periods` stores NOMINAL paydays; a holiday/weekend shift for the pay SCHEDULE is unmodelled | Josh's 1 Jan 2026 payday was really paid 31 Dec 2025 | OPEN | operator (scope it as its own task, or rule it out?) |
| recurrence | F-5 | -- | a 27-paycheck year is a real budgeting event and no surface names one | one extra $500 Groceries + one extra $2,473.38 paycheck | OPEN | operator (build the surfacing, or leave it?) |
| pay_calendar | P1 | -- | `budget.pay_periods` stores two DERIVED columns (`end_date`, `period_index`) beside the fact they derive from, and nothing reconciles them -- the normalization failure every other row here is a face of | `-$140.63` via P2, the only one of its faces that has been priced | OPEN | C4 |
| pay_calendar | P2 | = recurrence:F-10 / ~ balance:N-128 | the writer accepted a batch that leaves a calendar hole: `_reject_overlapping_batch` refused `min(new_starts) <= latest_end` and nothing refused `latest_end + 5 days`. **The WRITER half CLOSED at C3-b** (`7e3fb33b`): `pay_period_write` materialises `derive_periods` over the whole payday list, in which a period ends the day before the next payday, so no door can leave a hole and the next write through any of them REPAIRS one. What survives is the MACHINERY the closure makes unconstructible -- `PlacementOutcome.SCHEDULE_GAP`, `GenerationPlan.gaps`, `report_schedule_gaps` -- which is C5's deletion, and the pre-C3-b data that can still carry a hole until its owner's next schedule write | `-$140.63` on the gapped clone (balance N-128): `_cash_sums` and `_assertion_sums` drop a fact whose day no period places while `_period_balances` keeps it, so the reconciliation identity breaks. None on production (all 61 periods contiguous, re-measured 2026-08-10). **The STATE goes, not the LOSS: NARROWED 2026-08-09** by row P16 -- an absorbed hole leaves a 28-day period and `should_skip_period` then bills one monthly where two are owed | OPEN on the machinery only. RULED 2026-08-08 -- neither "refuse" nor "bridge": the hole is two derived columns disagreeing, so this arc drops them and the state stops existing. MERGED 2026-08-09 with the recurrence arc's F-10 | C5 (the machinery; P16's ruling comes first) |
| pay_calendar | P4 | -- | `integrity_check` has no DATE-gap check at all (BA-03 checks `period_index` gaps), and BA-04's overlap predicate is `p2.start_date < p1.end_date` (`scripts/integrity_check.py:375`), so two periods sharing exactly one boundary day are invisible to it -- `[Jan 2, Jan 15]` beside `[Jan 15, Jan 28]` both cover Jan 15 and the check passes | `$0.00` today -- production is clean. The check that would have caught P2 (weekly, 3:30 AM Sunday) does not exist, and the one that would have caught its mirror is off by one | OPEN, found 2026-08-08 | C4 |
| pay_calendar | P5 | -- | `_pp_assert_structure` asserts `cur.start_date > prev.end_date` (`tests/_test_helpers.py:3392`) and nothing about contiguity, while documenting itself as "the single source of truth for this user's period structure is sound", so the invariant helper called after EVERY pay-period mutation test carries the write door's exact blind spot | `$0.00` -- but it means no existing test could have failed on a gapped write, which is why P2 survived to be found by reading rather than by the suite | OPEN, found 2026-08-08 | C4 |
| pay_calendar | P6 | = recurrence:F-12 | **SIX implementations of "which pay period contains this date" remain of the SEVEN this row counted.** Plan step **C2-d** deleted the loan one on 2026-08-10 (`3e6cd4ec`); the count had been three until an AST census that day found six and an adversarial review a seventh. `recurrence/_calendar.py:287` (bisect, `None` outside; 1 `app/` call site), `balance_at/_cash_periods.py:320` (bisect, `None` outside; 3 sites, all in-module, scoped to the reported WINDOW), ~~`loan_ledger/_visible.py:150`~~ (linear scan + a fallback to the latest period ENDING before the target; a PUBLIC package export no other package called, `balance:N-210`'s shape) -- **DELETED at C2-d**, together with `resolve_anchor_pay_period` and `owner_pay_periods`, and replaced by `pay_calendar.PayCalendar.filing_period`. Plus three the planning documents named nowhere: `investment_dashboard_service/_chart.py:200` (linear scan over SYNTHETIC periods), `pay_period_service.get_current_period` (`:226`, SQL, `.first()` with NO `ORDER BY` -- row **P19**) and `get_overlapping_periods` (`:364`, SQL range). `entry_service.py:816` is EXCLUDED: it asks MEMBERSHIP ("does THIS period cover this day"), which is the primitive the searches are built on, not a search. **CORRECTED AGAIN 2026-08-10, and the number is now SEVEN**: an adversarial review of C2-a found `savings_dashboard_service/_horizon.py:246-271` `_period_id_at`, a linear containment scan with a FORWARD clamp ("a target past the axis end resolves to the final period") returning `.id` -- which is `period_starting_on_or_before` plus a clamp, the very rule the 2026-08-10 ruling names as the second question. Its own docstring says why it exists: the synthetic periods it scans "carry no `period_index` for the general `find_period_containing_date`". **An AST census keyed on the containment PREDICATE could not see it, and that is the lesson** -- it was found by reading the consumers of the producer C2-e retires. Do not treat this count as final either | `$0.00` today -- every caller is correct for the question it asks. The cost is that "which paycheck is this in" has six answers whose edge behaviour differs, and the six `pay_period_service` readers alone carry **66** `app/` call sites, which is what sized C2's decomposition | OPEN, found 2026-08-08, CORRECTED and widened 2026-08-10 by census. MERGED 2026-08-09 with the recurrence arc's F-12. **The ruling this row was waiting on was taken 2026-08-10**: the third implementation's fallback is a legitimate SECOND QUESTION, not a compensator -- proven equivalent over 1,800 (shape, day) pairs to "the latest period starting on or before the day, else the earliest", which is the exact mirror of the `period_starting_on_or_after` the calendar already carries. No single leaf closes this row; it closes when the last of the SIX still standing goes -- C2-b2, C2-c, C2-e and C2-f each take one or two | C2 |
| pay_calendar | P7 | ~ balance:N-82 | the pay calendar is a PARTIAL function: `get_all_periods` returns the materialized rows and nothing else, so past the last payday every consumer improvises and the improvisations disagree | Empower `+$2,501.92` and Property `+$5,427.07` at six months out, where the modelled replay's ACCRUAL tier keeps running past the horizon while its CONTRIBUTION tier stops -- a half model with nothing on screen saying so (balance N-82 / X-l) | OPEN (= balance X-l root). **Its remedy is RULED 2026-08-10**: the calendar ANSWERS past the last stored payday, projecting forward at the OWNER's cadence with `period_id = None`, so the improvisations have nothing left to improvise about. `C2-a` builds that; `C2-e` is the leaf that puts these consumers on it and so closes this row | C2-e |
| pay_calendar | P8 | -- | `resolve_cadence` infers the cadence from the LAST PERIOD'S LENGTH (`pay_schedule_service.py:169-177`) when a user has periods but no schedule row, which reads back the value it produces once that period's end is projected FROM the cadence -- and `auth_service.register_user` writes a bootstrap payday and NO schedule row, so a backfill alone is reopened by the next signup | `$0.00` -- the owner on production carries a schedule row (cadence 14), so the fallback is dead code today. It is unresolvable only for a ONE-payday user; with two, `last_start - prev_start` gives the same integer non-circularly. That is exactly the registration state, which is why the remedy is a write-door invariant rather than a data repair | OPEN, sharpened by adversarial review 2026-08-08 | C4 |
| pay_calendar | P9 | -- | `ck_pay_periods_date_order CHECK (start_date < end_date)` forbids a one-day pay period, which two paydays a day apart legitimately produce | `$0.00` -- no live schedule is affected. It is an artifact of `end_date` being authored, and it constrains what the model may express going forward | OPEN | C4 |
| pay_calendar | P10 | -- | the normalized model admits a payday inserted BETWEEN two existing ones, splitting a period. The draft called the hazard "a row dated outside its own paycheck"; that state is UNREACHABLE because `utils/dates.attribution_date:228-233` clamps, and the real damage is a row silently RENDERED on the wrong day. `classify_period_lock` has no reason keyed on a transaction's date, so an unlocked period splits freely; and the split-off payday is either empty (income understated for the whole horizon) or repopulated past `should_skip_period`, which bills a monthly twice in one fortnight | not constructible today (the writer is forward-only). Ruled 2026-08-08: refuse when the split period is locked, insert and re-derive otherwise -- but "re-derive" is a feature, so it is its own step | OPEN, ruled 2026-08-08, re-scoped by adversarial review the same day | C6 |
| pay_calendar | P11 | ~ recurrence:D21 / ~ recurrence:D24 | `recurrence_rules.offset_periods` is a STORED derivative of `period_index` (the phase `(period_index - offset) % interval_n == 0`), so inserting a payday before an existing one would shift every later index and silently re-phase every `Every N Periods` rule | `$0.00` today, MEASURED 2026-08-08 on `shekel-prod-db`: all 46 live rules carry `interval_n = 1` and `offset_periods = 0`, where the phase is inert. The exposure is the first rule authored with an interval, and recurrence R7c removes it permanently by deriving the phase from the authored anchor | OPEN, found 2026-08-08 | C6 (the only step that can renumber) |
| pay_calendar | P14 | -- | the derivation is window-dependent where the stored column was not: `PeriodCalendar.from_pay_periods` and `_cash_periods._PeriodSpans.of` both accept ANY list, and over a partial window the LAST row falls to the `start + cadence - 1` branch instead of `lead(start_date) - 1`, so one period reports two different ends depending on which window asked | `$150,000.00` for the sibling shape already measured in-repo: `pay_calendar/_derive.derive_periods` records that folding a $100,000 true-up against a window missing its period moves the balance by that much (it was on `loan_ledger/_visible.owner_pay_periods` until C2-d deleted that function), and names the grid's six-period window as the caller that reaches the balance seam with it | OPEN, found by adversarial review 2026-08-08. **Its stated MECHANISM was REFUTED 2026-08-10 and the row is corrected rather than closed**: neither named constructor DERIVES an end today -- `recurrence/_calendar.py:219` copies `period.end_date` off the ORM row and `_cash_periods.py:324` compares against the stored column -- so neither can produce a window-dependent end. Window-dependence is a hazard `derive_periods` INTRODUCES, and the `$150,000.00` cited from `loan_ledger` is a different mechanism -- and its home MOVED at C2-d, which deleted `owner_pay_periods`; the measurement is restated in `pay_calendar/_derive.derive_periods` (a missing containing period firing a `periods[0]` fallback). The design conclusion stands and `C2-a` has already delivered it structurally -- a slice is a `PeriodWindow`, a type that is not a calendar. **What changes is the work: `C2-b` has NO partial-list caller to move** (`routes/_recurrence_preview.py:225` and `generation_schedule.py:190` both take `get_all_periods`, the COMPLETE set), so this row closes on `C2-c` alone | C2-c |
| pay_calendar | P15 | -- | a derived end is NOT stable against a later write, which is the one way it differs from the column it replaces: a payday recorded INSIDE the last period's projected span retroactively shortens that end, and a row dated into the vacated days then RENDERS on a different day on both surfaces built to agree | not constructible: `_reject_overlapping_batch` refused it by comparing against the STORED end, and **C3-b's replacement refuses the same set** -- its floor is one full CADENCE after the latest payday, so a new payday cannot land inside an existing paycheck at all | OPEN. **The plan said C3-b would close the SHORTENING half and that was WRONG in both directions, corrected 2026-08-11.** C3-b's first cut bounded at `latest_payday + 2`, which is a paycheck too low and made this row CONSTRUCTIBLE for the first time -- an adversarial review measured the split, the moved render and P10's both damage arms -- so the floor became one cadence and the row went back to not-constructible. What is left is what C6 unlocks deliberately: once a payday MAY be inserted mid-schedule, a row rendering on a different day inside its own paycheck is P10's damage, and P10 is C6's | C6 |
| pay_calendar | P16 | ~ pay_calendar:P10 / ~ recurrence:D19 | an over-long pay period under-bills a monthly: `_recurrence_common.should_skip_period:196-232` returns True on the FIRST template-linked row in a period, on every branch, so the period gets ONE monthly bill where the user owes two. **The threshold is a 29-DAY period, measured 2026-08-09** -- monthly occurrences are >=28 days apart, so a 28-day period (a whole missed paycheck absorbed) is the LARGEST that still bills correctly. **And it is not only a hole artifact**: `cadence_days` is user-selectable 1..365 (`ck_pay_schedule_cadence_range`), so a monthly-paid owner drops 2 occurrences a year at cadence 30 and 7 at cadence 31 with no hole anywhere. **Its STORAGE half is recurrence `D19`**, the two partial UNIQUE indexes keyed on `(template, period, scenario)`, and the relation is `~` rather than a merge BECAUSE the two have different owners: D19 is the index, this row is the skip predicate, and neither ledger said so until 2026-08-09 | not priced here. The MIRROR is: P10's split period bills a monthly TWICE in a fortnight. **This row's own claim that the days are 'at least REPORTED as a gap' today is FALSE, corrected 2026-08-09 by reading the code**: `report_schedule_gaps` (`_recurrence_common.py:387-443`) logs an occurrence no period CONTAINS, and at cadence 30/31 there is no hole -- both occurrences land INSIDE one period, so nothing reports it and deleting that logger removes no visibility it ever had. What actually fires on an EMPTY period is `refuse_unstorable_repeats` (`:235-300`, called at `:483` before any row is written), which RAISES `RecurrenceCadenceUnsupported` naming the template, the paycheck and every occurrence date -- loud, and the owner gets NO rows for that template. The SILENT under-bill needs a period that already holds one | OPEN, found by adversarial review 2026-08-08, recorded 2026-08-09. RULED 2026-08-09: make `should_skip_period` OCCURRENCE-AWARE. Refusing an over-long period at the writer was rejected -- it would refuse every legitimate monthly schedule the CHECK allows and would not repair owners already on cadence 30/31. **C5 SPLIT 2026-08-09** (developer): the deletion half is `C5a` and this is `C5b`. The index re-key LEAVES this row and stays with `R5`, which creates the `occurs_on` the only correct key needs -- writing it here too would be the same defect twice, one arc apart | C5b |
| pay_calendar | P17 | -- | **a FOURTH period producer fabricates ids in the SAME integer namespace as real `budget.pay_periods.id`.** `growth_engine.py:677` is `SyntheticPeriod = namedtuple(..., ['id', 'start_date', 'end_date'])` and `generate_projection_periods` (`:680-706`) numbers them from 1; `investment_dashboard_service/_context.py:48` declares `_PeriodList = list[PayPeriod \| growth_engine.SyntheticPeriod]`, asserting the two are interchangeable; and `balance_at/_kind_correct.py:45,99` takes `periods: list` UNTYPED, reading only `.id` / `.start_date` / `.end_date`, so a synthetic list is accepted silently. `growth_engine.py:698` is also a third copy of `start + cadence_days - 1`, beside `pay_period_service.py:190` and `pay_calendar/_derive.py` | `$0.00`, and prevented by ACCIDENT rather than by design -- which is the finding. Every `.id`-keyed map is built and read inside one function from one list, so the two families never share a container today. A ready-made collision site already exists: `balance_at/_cash_periods.py:334` builds `{period.id: _ZERO_MONEY for period in self.periods}`, then `:376-379` does `if fact.pay_period_id not in income: continue` / `income[fact.pay_period_id] += fact.delta`. Feed it a synthetic axis and REAL transactions in id range 1..N are attributed to synthetic periods while anything out of range is silently DROPPED. **That is N-128's shape (`-$140.63`, facts dropped by a period key) on a second axis.** All three lines verified by hand | OPEN, found 2026-08-09 while assessing this arc's ordering; verified against the tree rather than inferred. C1 already built the discriminator -- `DerivedPeriod.period_id: int \| None`, where `None` marks a span no FK can point at, which is exactly what a projected period is. C2's "one producer" claim must absorb this producer or rule it out explicitly, and **it ABSORBS it: ruled 2026-08-10**, `generate_projection_periods` and `SyntheticPeriod` DELETE into the calendar's forward projection at leaf `C2-e`. `tests/oracles/recurrence_baseline.py` holds a fourth copy of the arithmetic and is LEGITIMATE: an oracle must not share code with what it grades | C2-e |
| pay_calendar | P18 | -- | **`journal_entries.pay_period_id` is P1's defect on the ledger's header table**: a NOT NULL FK (`models/journal_entry.py:158-166`) stored beside the `entry_date` (`:178`) it is derivable from, with nothing reconciling them. Ruling **R-EA** already made the WRITE side derive it -- a correction books in the period CONTAINING the day it asserts, never read from a source row's stored id -- so the column is a materialization of a derivation the app now performs anyway. **And the NOT NULL is what makes the third period-containing implementation TOTAL**: `pay_calendar.PayCalendar.filing_period` CLAMPS rather than answering `None`, and its own docstring gives the FK as the reason. *It was `loan_ledger/_visible.resolve_anchor_pay_period` -- containing, else the latest period ENDING before, else the earliest -- until plan step **C2-d** (`3e6cd4ec`) replaced that three-branch chain with one clamp and deleted it; the TOTALITY the FK forces is unchanged, only where it is written.* `_cash_periods.py:271-278` states the split plainly -- the fallback is "right for the question it answers ... and wrong for this one" -- so the second QUESTION is legitimate and it is the chain's TOTALITY that is the storage artifact | `$0.00` today and not a defect to patch: the app is correct on both surfaces. The cost is that "which paycheck is this in" keeps a second definition, and that the compensating fallback cannot be deleted while the FK stands -- so row P6's three implementations cannot become one. 11 references in 7 `app/` modules, 2 of them docstrings: `_posting_reconcile.py:186`, `posting_service.py:145,153`, `pay_period_admin.py:876-879`, `ledger_report_service/_income_statement.py:165`, `account_posting_service/_walk.py:322`, `loan_posting_service/_payments.py:163`. The income statement GROUPS by it, so this is a real query key and not dead weight | OPEN, found 2026-08-09. Developer ruled it IN SCOPE the same day, as its own step **C7** sequenced after C4 -- not folded into C2, because C2's job is to build the total calendar and this is a schema decision about the ledger. C7 rules between three options: DROP and derive, make it NULLABLE, or KEEP it as a deliberate materialization with the second definition's cost stated. **"DERIVABLE FROM `entry_date`" IS FALSE AND WAS MEASURED SO 2026-08-10, while ruling C2's first fork** -- do not carry it into C7. On `shekel-prod-db`, **14 days carry TWO different `pay_period_id` values for ONE `entry_date`** (one owner), so the date does not determine the paycheck; **35 of 327 entries (10.7%) are dated outside their own paycheck** across five of the seven source kinds, which is the budget clock and the cash clock legitimately disagreeing. The column holds **two different relationships**: for transaction-sourced entries it EQUALS its source row's `pay_period_id` on **174 of 174**, a copy of stored data; for assertions it is derived from the day, by ruling R-EA. So C7's "DROP and derive" is available only for the assertion half, and the transaction half is a join-vs-copy question about a column the SOURCE row owns | C7 |
| pay_calendar | P19 (C2's fork-1 census 2026-08-10, read off the query) | -- | **`get_current_period` answers "which paycheck covers this day" in SQL with `.first()` and NO `ORDER BY`** (`pay_period_service.py:226-234`), so over two periods covering one day it returns whichever row PostgreSQL happens to yield first -- a plan-dependent answer to the app's single most-asked period question, at **21** `app/` call sites. The five other implementations in row P6 all order deterministically or bisect | `$0.00` today and not reachable through the app: `_reject_overlapping_batch` refuses an overlapping batch at the write door, so no owner can hold two periods covering one day. That guard is exactly what plan step C3 DELETES, on the correct ground that the payday model makes an overlap unexpressible -- so this row must close BEFORE or WITH the state that defends it | OPEN, found 2026-08-10 by the census that corrected P6. Not a query to patch: under the one calendar this reader stops issuing its own SQL, and the nondeterminism has no subject. **C2-f must preserve the `None`, and an adversarial review had to point that out**: `routes/grid.py:183-184` renders `no_periods.html` on it, so replacing this reader with the TOTAL `span_containing` rather than the saved-only `period_containing` would delete the grid's own recovery page and build a window at a projected `period_index` with no rows behind it. Only that one call site was verified; the other 20 are unaudited | C2-f |
| pay_calendar | P21 (C2-a's adversarial design review 2026-08-10, measured against a production-shaped calendar) | ~ pay_calendar:P17 | **Every PROJECTED period carries the same key, and four consumers use that key as a map index.** `DerivedPeriod` exposes `period_id`, NOT the `.id` the synthetic axis it replaces exposed -- so C2-e is an attribute rename at every site, which no document said. Worse, a projected period's `period_id` is `None` BY DESIGN, and it is `None` for ALL of them: over a 61-payday calendar an `axis(2026-08-10, 2046-08-10)` returns **523 periods with 53 distinct ids** -- 52 real, plus one `None` shared by 471 projections. The four sites that key on it: `investment_dashboard_service/_chart.py:89` (`{p.id: p}`, read at `:94`), `savings_dashboard_service/_horizon.py:275` (`pid_to_balance`, read through `_period_id_at` at `:310-313`), `growth_engine.ProjectedBalance.period_id: int` (assigned at `:445`, `:654`, and a `None` violates its own annotation) and `investment_dashboard_service/_context.py:48`'s `_PeriodList` alias asserting the two period families are interchangeable | not priced, and the shape is a LABEL collapse rather than a wrong figure: every projected row resolves to the same map entry, so a chart's month captions all become the last projected month. `$0.00` today because nothing calls the new value | **OPEN, and a REQUIREMENT on C2-e rather than a defect to fix afterwards.** `period_index` IS unique across a mixed saved/projected window (`_projected_after` continues the sequence), so the remedy is a re-key, not a new id namespace -- but row **P17** argued only that fabricated ids must leave the real namespace and never recorded that UNIQUENESS was the property those four sites relied on. Read this row before starting C2-e | C2-e |
| pay_calendar | P22 (C2-a's adversarial design review 2026-08-10, both producers driven side by side) | -- | **`axis()` anchors on the owner's paydays; five of its six consumers anchor on an arbitrary day, and the difference is a contribution credited early.** `generate_projection_periods(2026-08-10, 2046-08-10)` returns 522 periods whose first STARTS 2026-08-10; `PayCalendar.axis` over the same range returns 523 whose first starts **2026-07-30**, eleven days earlier, because a calendar period begins on a payday. `growth_engine._project_one_period:404` then applies a full `periodic_contribution` and a full-span return to that period against a balance seeded as of TODAY -- crediting a contribution that has already happened. Five call sites pass a mid-period day: `retirement_projection.py:388`, `retirement_levers.py:260,450`, `retirement_readiness.py:433`, `savings_dashboard_service/_horizon.py:848`. Only `investment_dashboard_service/_chart.py:139` is safe, because ruling R-AF already makes its `projection_start` the next payday (`_context.py:211-234`) | not priced. One extra contribution plus one period of compounding at the head of every retirement and savings projection for five of six surfaces, growing with the horizon. `$0.00` today: nothing calls the new value | **OPEN, a REQUIREMENT on C2-e.** The four `retirement_*` sites also read `date.today()` -- the PROCESS clock -- so the cutover must thread an `as_of` as well, which the leaf did not budget for. Decide explicitly whether the axis head is CLIPPED to the requested start or whether the consumers move to a payday-aligned start as `_chart.py` already did | C2-e |
| pay_calendar | P23 (C2-a's two adversarial reviews 2026-08-10, found independently by both) | -- | **`axis()` silently covers nothing below the owner's first payday, and its docstring does not say so.** The summary reads "the spans covering `[first_day, last_day]`"; for a range that STRADDLES `opening_bound` it returns periods covering only the part above it -- `axis(2025-12-20, 2026-03-01)` on a calendar opening 2026-01-02 leaves 13 days covered by nothing. The `Returns` block states only the wholly-before case. The producer it replaces, `growth_engine.generate_projection_periods:694-706`, walks from its `start_date` unconditionally and covers the whole range, so this is a BEHAVIOUR CHANGE at the cutover, not a preserved property. Reachable: the Generate form asks for "your next (or first) payday", a FUTURE date, so `as_of < opening_bound` is an ordinary state for a new owner. **A bounded arithmetic edge belongs with it**: `_projected_after` computes `start + (cadence - 1)`, which raises `OverflowError` within `cadence` days of `date.max` -- unreachable from `app/`, recorded so the word TOTAL is not read as unconditional | `$0.00` today (nothing calls it). At the cutover, a projection that silently begins late for any owner whose first payday is in the future | **OPEN, a REQUIREMENT on C2-e.** Three dispositions and the trace decides: refuse a `first_day` below `opening_bound` as `overlapping()` already refuses a crossed range, state the truncation in the `Returns` block, or project backwards -- which the 2026-08-10 ruling declined for `span_containing` and would have to be re-opened to allow here | C2-e |
| pay_calendar | P24 (C2-a's adversarial code review 2026-08-10) | -- | **`PeriodWindow`'s constructor is public and neither sorts nor validates, so "only ever produced by the calendar" is convention rather than structure.** The class is exported in `__all__`, its `periods` field is public, and `containing()` BISECTS -- so a hand-built out-of-order window answers containment wrong and silently: given two periods supplied newest-first, `containing()` misses the day the first one covers and answers correctly for the second by accident. This is a step BACKWARDS from the thing C2-c retires: `_cash_periods._PeriodSpans.of:305` sorts its input precisely because "the caller's order is not assumed". A second, narrower correction belongs here: the module docstring says a window "cannot be derived from", and it can -- `PayCalendar.from_paydays([(p.period_id, p.start_date) for p in window], ...)` is one line over the public `__iter__`. What the type actually guarantees is that no CONSTRUCTOR accepts a window, which is worth stating and is not the same claim | `$0.00` and unreachable today: every window in existence comes from `window()`, `overlapping()` or `axis()`, all of which slice an ordered calendar | **OPEN.** The fix is a `__post_init__` that sorts (cheap, and it makes the ordering rule structural rather than a precondition) or that refuses an unordered tuple. Owned by C2-c because that is the first leaf whose correctness depends on window containment being right | C2-c |
| pay_calendar | P25 (C2-a's adversarial design review 2026-08-10, verified against the value's own tiling test) | -- | **`PlacementOutcome.SCHEDULE_GAP` becomes UNSATISFIABLE at C2-b, three leaves before the step that deletes it.** `recurrence/_occurrence.py:711-716` derives that outcome as `opening <= occurrence <= horizon and calendar.period_containing(occurrence) is None`. Under a DERIVED calendar the periods tile `[opening_bound(), horizon()]` by construction, so the conjunction can never hold -- which `test_every_day_in_the_saved_span_has_exactly_one_period` asserts directly. **A second correction rides with it**: the reason both `period_containing` and `horizon()` give in their docstrings for staying saved-only is "so the recurrence engine can tell a HOLE from not-yet", and that reason expires at C2-b. The methods still deserve to exist, on the ground the docstrings do NOT lead with -- they refuse to seat a generated row past the horizon, where no `pay_periods.id` exists for `transactions.pay_period_id` to point at | `$0.00`: an unsatisfiable branch renders no wrong figure. The cost is that C5a's premise ("delete what is now unconstructible") becomes true THREE LEAVES EARLIER than the plan sequences it, and that two live docstrings will state a reason that has expired | **OPEN, found 2026-08-10.** Not a defect to patch at C2-b: deleting the outcome there would move recurrence `R-F10`'s work out of `C5a`, which is gated on C4 for reasons of its own. What C2-b owes is the docstring correction and a note that the branch is dead; C5a still performs the deletion | C5a (the deletion) / C2-b2 (re-pointed from the decomposed parent 2026-08-10; **the two expired docstring reasons were CORRECTED at C2-b1**, which is where the review that found them ran -- `period_containing` now names two ways to answer `None` and not three, and `horizon()` now rests on the foreign key rather than on holes) |
| pay_calendar | P20 (C2's fork-2 trace 2026-08-10, priced against the real growth engine) | ~ pay_calendar:P17 | **The synthetic projection axis is hardcoded to a 14-day cadence, and it carries CONTRIBUTIONS.** `growth_engine.generate_projection_periods(start, end, cadence_days=14)` takes its default at all **six** `app/` call sites (`retirement_projection.py:387`, `retirement_levers.py:259,449`, `retirement_readiness.py:432`, `savings_dashboard_service/_horizon.py:848`, `investment_dashboard_service/_chart.py:139`), while `growth_engine._project_one_period:404` applies `inputs.periodic_contribution` -- a per-PAYCHECK figure built from the owner's real pay periods -- once per axis period. So an owner is credited `365/14` contributions a year whatever their `budget.pay_schedule.cadence_days` actually is. Growth itself is cadence-neutral (`period_return_rate` prorates by span); only the contribution count is wrong | **`$588,959.22`**, measured through the real engine: 20 years, `$50,000` seed, 7%, `$1,000` per paycheck, no annual limit -- a monthly-paid owner (cadence 30) is shown `$1,300,344.92` against a true `$711,385.70`, **+83%**, because 522 axis periods are used where 244 are owed. A weekly-paid owner (cadence 7) is UNDERSTATED by `$1,103,888.62`. An annual contribution limit caps the yearly total and so bounds the error for a 401(k) or IRA, but not for a taxable or savings account, which carry none | **OPEN.** `$0.00` on production, which is cadence 14 -- and `ck_pay_schedule_cadence_range` permits **1..365**, so a monthly-paid owner is an ordinary user rather than a contrived one. This is the retirement-readiness figure. Closed structurally rather than by passing the cadence down: the calendar knows the owner's cadence, so once the axis IS the calendar there is no second value to get wrong | C2-e |
| pay_calendar | P26 (C2-b's instrumented full-suite run 2026-08-10; the wrapper compared every `PeriodCalendar.from_pay_periods` call in 8,535 tests against `derive_periods` over the same paydays) | ~ pay_calendar:P14 | **A PARTIAL payday set is silently RE-INDEXED from 0, where the stored ordinal used to survive a slice -- so P14's window-dependence has a second half, on the column nobody was watching.** `PeriodCalendar.from_pay_periods` COPIES `period.period_index` off each ORM row, so a slice of a schedule keeps its true ordinals; `derive_periods` computes the ordinal as `row_number()` over the payday set it is HANDED, so the same slice comes back numbered 0..n-1. The value cannot detect partiality -- `PayCalendar`'s own docstring says a subset is indistinguishable from a short schedule -- and `PeriodWindow` closes the door for a consumer holding a calendar but NOT for a caller of `from_paydays`. **Its headline mechanism was REFUTED by adversarial review 2026-08-10 and the row is corrected rather than closed.** A UNIFORM re-index is phase-NEUTRAL: `_derive_offset_periods` computes the offset as `start_period.period_index % interval_n` in the SAME frame the match predicate `(period.period_index - offset) % interval_n == 0` reads (`recurrence/_resolution.py:934`, `:660`), so subtracting a constant from both leaves `(p - s) % n` unchanged. What DOES re-phase is the fallback `if start_period is None: return spec.offset_periods` (`:932-933`) -- a rule naming no start period, or one the slice excludes, which takes the STORED column computed in the unshifted frame. Three ways the row understated it, all found by the same review: the hazard is also on the WRITE side, because `_authoring.py:97` PERSISTS `resolved.offset_periods`, so authoring against a partial calendar stores a shifted phase that stays masked until the start-period FK goes `ON DELETE SET NULL`; a NON-CONTIGUOUS slice compresses rather than shifts, so even the derived case breaks; and `test_recurrence_engine.py:738-740` justifies itself with "Reachable on the extend path, which hands the engine only the NEW periods", which is FALSE -- `generation_schedule.py:189-194` reads `get_all_periods` for the calendar and windows only `write_periods` | `$0.00` on production and `$0.00` in `app/`: all three constructors take `pay_period_service.get_all_periods`, the COMPLETE set, which is what P14's 2026-08-10 correction established. Two tests hand a partial list on purpose and **exactly ONE of them actually fails** under a simulated cutover (measured 2026-08-10 by running the full suite against a derived build): `TestTheEveryNPeriodsPhase::test_the_stored_column_is_used_when_the_calendar_lacks_the_period` (stored indices 6..25, re-indexed to 0..19), which is the fallback branch above. `TestGenerateForTemplate::test_a_bill_repeating_inside_one_paycheck_is_refused` (10..13 -> 0..3) is partial too and passes, because nothing in it reads a phase. The remedy is therefore ONE test, not two: a calendar that LACKS a rule's start period is a stale `start_period_id`, which is constructible against a COMPLETE calendar and is the state production can actually reach | OPEN, found 2026-08-10 by measurement rather than by reading -- the AST census that sized C2 keyed on the containment predicate and could not see an ordinal. Not a defect to patch with a partiality check the value provably cannot make: the remedy is that the two tests build a complete calendar and a stale id, and that `calendar_for` is the only door `app/` uses | C2-b2 |
| pay_calendar | P27 (C2-b's instrumented full-suite run 2026-08-10, and re-measured against `shekel-prod-db` the same day) | ~ pay_calendar:P2 / ~ balance:N-128 | **A calendar hole is ABSORBED by the derivation rather than reported, so between C2-b2 and C3 the app converts N-128's balance gap into P16's under-bill.** Derived periods TILE by construction, so the period before a hole ends the day before the NEXT payday instead of where its stored `end_date` says: the hole stops being a hole and becomes part of the preceding paycheck. Today `period_containing` answers `None` there, generation reports `PlacementOutcome.SCHEDULE_GAP`, and the obligation is dropped **with an operator alert** -- `_recurrence_common.py:435-444` logs `EVT_RECURRENCE_OCCURRENCE_UNPLACED` at WARNING naming the dates. Afterwards the row is generated into the preceding paycheck against a real `pay_periods.id`, `attribution_date` still clamps its RENDER against the stored end, **and nothing is logged at all**, because the `SCHEDULE_GAP` conjunction goes unsatisfiable (row **P25**). **An earlier draft of this row said "neither is quieter than the other"; adversarial review 2026-08-10 refuted that** -- the new answer is strictly quieter, and the absorbed hole then leaves an over-long period where `should_skip_period` bills one monthly of the several owed (row **P16**). It also means the plan's C5a entry ("It deletes no visibility, and that correction is why the split is safe") is true only because C2-b2 removed that visibility three leaves earlier, which no document said until now | `$0.00` on production: `shekel-prod-db` holds 61 contiguous paydays for its one payday-owning owner, 0 non-contiguous pairs, and derived index and end reproduce both stored columns on 61 of 61 rows (re-verified 2026-08-10). In the SUITE it is live rather than hypothetical -- **55 diverging shapes across 53 tests**, 53 of them the `seed_user` bootstrap period at 2024-01-05 whose stored end 2024-01-18 becomes a derived end up to **1,474 days** later. **DIVERGENCE is not FAILURE, and the second number is the one that sizes the work**: a simulated cutover run over the whole suite 2026-08-10 produced **5** real failures, not 53 -- P26's one phase test, plus FOUR gap tests no row named until now (`test_recurrence_engine.py:1394` `TestAnOccurrenceInAScheduleGap`, all three, and `test_transfer_recurrence.py:360`). Those four are built by the REAL writer to prove the gap state is constructible, so C2-b2 must delete or rewrite them; nobody has measured the MONEY consequence, only the test one. The production generator is real too: finding **P3** / `balance:N-123` records that every next-payday date past the registration placeholder is accepted WITH the gap left behind | OPEN, found 2026-08-10. The absorption is the RULED model working (2026-08-10: the periods tile, and C4 makes a hole unexpressible), so nothing here reverses it. What this row owns is that the window is real and its two ends are named: `balance:X-ad` deletes the bootstrap payday that generates most of it, and **C3** deletes the free-date door that generates the rest | C2-b2 (state it and pin it with a test; the free-date door CLOSED at C3-b, `7e3fb33b`, and the registration door at `balance:X-ad-a`) |
| pay_calendar | P28 (C2-b1's adversarial code review 2026-08-10, reproduced against the live suite and pinned by its own test) | -- | **The derived horizon comes from the STORED CADENCE and the stored `end_date` came from the cadence at GENERATION time, so a schedule row edited afterwards moves the horizon and only the horizon -- a second divergence class the C2-b cutover carries beside row **P27**'s hole absorption, and one no oracle built on generated fixtures can see.** (P27 is named here rather than in the `also` column because that column takes a bare id and P27's carries its provenance annotation.) `resolve_cadence`'s fallback is `(end - start).days + 1` (`pay_schedule_service.py:169-177`), the exact inverse of `generate_pay_periods`' `end = start + cadence - 1` (`pay_period_service.py:190`), so on any generated schedule the derived last end reproduces the stored one BY ARITHMETIC -- `new.horizon() == old.horizon()` cannot fail, whatever the loader does with the cadence. The door that broke the identity had a row of its own, **P12**, CLOSED at C3-b (`7e3fb33b`) and so no longer in the `also` column: `routes/pay_periods.py:96-98` reaches `upsert_schedule` even when the batch created nothing, so a user re-posting Generate with an existing payday rewrites `budget.pay_schedule.cadence_days` without touching a single period. After C2-b2 the recurrence horizon then moves by `stored - generated` days | `$0.00` today: nothing reads the derived horizon yet (C2-b1 has no consumers), and production's stored cadence 14 equals its generated one on 61 of 61 rows. Measured on the suite's own 10-period schedule: `upsert_schedule(21)` moves the horizon **+7 days** past the last stored `end_date`, and `upsert_schedule(3)` moves it **-11 days**, at which point 2026-05-21 -- a day inside a real, id-bearing pay period -- is covered by NO period, so every obligation the recurrence engine would have seated there is silently dropped | OPEN, found 2026-08-10 by adversarial review of C2-b1's own oracle, which reported agreement on an axis it held fixed. **The control now exists** (`test_they_diverge_when_the_stored_cadence_outlives_the_stored_end`) and asserts both directions, so C2-b2 inherits a failing-visible test rather than a silent change. The row closes when the stored `end_date` is gone: **C4** drops the column the derived horizon can disagree with, and **C3** stops `upsert_schedule` firing on a batch that created nothing | C2-b2 (state it) / C4 (the column; the no-op post CLOSED at C3-b, `7e3fb33b`) |
| pay_calendar | P30 (C3-a's design pass 2026-08-10; the developer ruled to RECORD rather than build) | -- | **Recording paydays and setting the FORWARD cadence are two operations welded onto one form, three times.** `cadence_days` is an input on generate, regenerate and reset, while `/pay-periods/schedule` -- the form that owns the rolling window -- deliberately refuses to touch it (`pay_schedule_service.set_rolling`: "Cadence is NOT touched here: it is owned by generate / regenerate"). After this arc the column has ONE job, projecting past the last recorded payday, so it is a forecast setting rather than a property of any batch; the normalized shape is one control that sets it and payday forms that only record paydays | `$0.00` -- no wrong number, and that is why it was recorded rather than built. What it costs is that P12 and **P29** are the SAME defect at two doors and had to be found twice | OPEN, found 2026-08-10. **The developer ruled C3-a does NOT build it.** It becomes load-bearing at C3-b, not before: applying the cadence rule to the extend door makes that door PERSIST the value that sets the derived horizon, through a form rendering no control for it -- so C3-b is where the question must be answered rather than deferred again | C8 |
| pay_calendar | P31 (C3-b's two neutral adversarial reviews 2026-08-11) | -- | **The one writer answered pylint's 1000-line module ceiling by trimming prose rather than by having a shape** -- `balance:N-152`'s form a third time, after `transfer_service` (987) and the split that made `pay_period_locks`. It stood at 985 when this row was written, 15 lines of headroom, which is not a design. **Two of its parts still leave on a schedule**: C4 deletes `MIN_MATERIALISABLE_CADENCE_DAYS` and the cadence floor it guards, and C6 deletes `_reject_backward_payday` outright | `$0.00` -- a size gate writes no wrong figure. The cost is that the next correction to this module has to buy its own room, which is how a docstring gets trimmed instead of a function extracted | **OPEN, NARROWED 2026-08-11 and re-measured: 836 lines, 164 of headroom.** Deleting the coverage rule (developer ruling, same day) took 149 lines out -- `_reject_coverage_withdrawal`, `_rows_dated_outside` and `StrandedRow` with their arguments -- so the pressure this row was written under is gone and the question it asks is not. N-152's answer still applies -- a PACKAGE, one private leaf per verb, the shape `pay_calendar/` already has -- and waiting is still cheaper: C4 and C6 remove a refusal and a constant from this file, and splitting a module about to shrink twice more is work done against a moving target | C4 |
| pay_calendar | P33 (C3-b's deletion review 2026-08-11, REPRODUCED by running it) | -- | **A stored cadence of 1 is legal in the column and fatal to the writer, and the rolling top-up meets it on a READ path.** `ck_pay_schedule_cadence_range` admits `1` (`models/pay_schedule.py:52`, `CADENCE_DAYS_MIN`) while `pay_period_write` refuses anything below `MIN_MATERIALISABLE_CADENCE_DAYS = 2`, because a stored `end_date` must satisfy `start < end` and a one-day cycle's derived end IS its own start. `top_up_rolling_window` is called by `/grid` and `/dashboard` with no handler, so for an owner holding that value both screens raise `ValidationError` and 500 -- permanently, on every load. The truncate route already handles the identical class at its own door (`routes/pay_periods.py:187-193`) and names the same cause, so the codebase knows this state exists and covers it only where a 500 costs least | `$0.00` on production, which stores cadence 14, and **no door can create the value**: every write door bounds the cadence at the writer's floor before storing it, so only pre-C3-b or hand-edited data holds one. Not a regression -- the 500 predates C3-b's coverage-rule deletion, which merely removed the `except` clause that happened to sit above it | OPEN, found 2026-08-11 by an adversarial review of the deletion, which reached the traceback rather than arguing for it. **Deliberately NOT swallowed at the read path**: a schedule this app cannot materialise is a broken schedule, not a refusal to shrug off, and the ruling that deleted the coverage rule the same day turned on an opportunistic writer needing a swallow being evidence of a bad rule. C4 closes it structurally -- it drops the stored `end_date` this floor exists to protect, deletes `MIN_MATERIALISABLE_CADENCE_DAYS` with it, and legalises the one-day cycle (row **P9**) | C4 |
| pay_calendar | P32 (C3-b's coverage-rule deletion, 2026-08-11) | ~ balance:N-128 | **`cash_period_view`'s CONTRACT permits the window that breaks R-K's identity, and only its callers keep it out.** The docstring states the reported periods "need not be contiguous" (`_cash_periods.py:212`), and `_PeriodSpans.containing` answers `None` for a day in a gap, silently. Each column still reconciles against its OWN opening boundary -- `test_the_identity_holds_over_a_non_contiguous_window` asserts exactly that -- but the BALANCE COLUMN a reader scans down does not telescope across the gap: in that test's own figures the balance falls `$1,000.00` to `$750.00` between the two reported periods while every explanatory row on the second reads `$0.00`. That is `balance:N-128`'s shape reached through a READER, and after C3-b no WRITER can produce a hole, so the reader is the only place it can still come from | `$0.00` and not reachable through the app: `routes/grid.py:303` passes `all_periods`, the COMPLETE set, and the derivation TILES, so every rendered window is contiguous by construction. The exposure is the contract -- the next caller to slice a window inherits a documented licence to build a gapped one | OPEN, born 2026-08-11 with the deletion that made it visible. The deleted coverage rule was the last thing in the arc claiming to defend this identity and it defended the wrong half: a settle day off the TOP of the window cancels on both sides of R-K and reports as `period_timing`, while a day in a GAP does not. Two dispositions and the trace decides: the window type refuses a non-contiguous slice, or `containing` stops being able to answer `None` inside its own span | C2-c (which retires `_PeriodSpans` onto `PeriodWindow.containing` and is where the windowing contract is restated) |
| balance | N-234 (found while restructuring `steps.md` 2026-08-11, REPRODUCED: the three-column table it added was parsed as a fork and turned three fork controls red) | -- | **`tools/plan_gate` separates `steps.md`'s tables by COLUMN COUNT, so any three-column table in that file is silently read as a cross-arc fork.** `_registry.forks()` calls `_rows(text, 3)` over the WHOLE document and the forks table is located by its width alone, never by its heading; `step_rows()` does the same at width 7. A table added for any other purpose joins the registry it happens to match, with no error | `$0.00` of money. The cost is that the arm rule 11 rests on -- an unruled fork refuses a tick on either remedy -- silently grades rows that are not forks, and a real fork added at the wrong width would not be graded at all. It fired on the first edit: a `\| arc \| document \| section \|` reference table read as four unruled forks | **OPEN, born with an owner.** The repair is to locate each table by its HEADING rather than by its width, which is the same asymmetry **N-217** and **N-220** need for the RULINGS tables -- no table in this corpus is parsed by anything but its shape. Carried by the step that already owes a heading-anchored parser | X-ao |
