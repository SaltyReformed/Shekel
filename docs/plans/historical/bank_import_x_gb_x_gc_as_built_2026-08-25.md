> **ARCHIVED. Historical record only -- this document governs nothing and
> may be out of date.** The live plan is `docs/plans/steps.md`; the code as
> committed is the source of truth for what the app does.

# The Phase 0 paper cuts, as built: the X-gb - X-gc span (2026-08-25)

**Two shipped steps, condensed out of `implementation_plan_bank_import.md`
under `conventions.md` rule 5** when that document reached 193 lines against
the 180 its cap leaves after headroom -- which is where the `X-gd`
decomposition and a fourth ruling met.

Rule 5's three conditions hold. **No finding is carried here**: `X-gb` closed
**N-344** and opened **N-348**, which is live in `ledger.md`; `X-gc` closed
**N-345**'s race half and re-pointed its disclosure half at `operator`, also
live there. **No live sentence depends on one archived here**: the two
forward-looking clauses `X-gb`'s entry carried moved into the live
specifications that own them before this file was written -- the double-count
residue to `X-gh`, which is the step that unifies the two balance records.
**Nothing is carried without re-verification**, because nothing is carried:
both entries are reproduced whole below and neither is relied on.

## X-gb -- `ec346c46`

The delete door the route already served, ruling **R-GM**, and the P-6 repair
through it: 14 rows and 11 matches gone, `$7,769.58` of double-booked spending
reversed. Closed **N-344**; opened **N-348**.

What a later step was told to obey, recorded here as it was written: the
INVARIANT is `_candidates.act_still_names_a_row`, not the five call sites; a
PARTIAL loss stays `AcceptedGroup.agrees`' case; and the balance did NOT move,
because 60 hand true-ups were absorbing the double count -- the residue
`balance:X-f3c` records rather than plugs.

## X-gc -- `0452eef3`

The screens stop stating what is false: **R-GN**, **R-GO**, **R-GP**. Dead
corroboration advice gone from four surfaces (migration `a1f4c7e0b839`); a
GUESSED placement stops reading like a proven one; the never-showed panel marks
the 18 rows it can prove and keeps every one TICKABLE, because that list is the
hand-build picker and **R-GJ** leaves the group match as a parked card
payment's only act; the dialog binds in `<head>`.

**N-345 is NOT closed**: its disclosure half is dev DATA (**R-GQ**), at
`operator`.
