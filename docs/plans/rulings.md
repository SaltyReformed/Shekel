# The rulings

**Every developer ruling of every arc that has moved here, one line each: the RULE, not the
deliberation.** The deliberation belongs in the commit that shipped it, and a ruling whose work has
fully shipped is in the as-built record of the step that shipped it.

**The key is `(arc, id)` and the arc is a COLUMN, never a prefix** (conventions.md rule 10). Two
arcs may hold the same bare id: `balance:R-GU` and `bank_import:R-GU` are two different rulings and
both are LEGAL here. What that costs is a BARE citation of such an id resolving to two rules, so
**a citation must name its arc wherever the id is ambiguous**. No gate grades that; the writer does.

**Why the tables moved.** Ruling ids came from ONE global sequence spelled across five arc documents
in THREE grammars, and `tools/plan_gate` parsed none of them, so a collision was invisible to every
gate: `R-EX` named two rulings in 2026-08-10, and on 2026-08-27 `R-GW` was minted in both `balance`
and `bank_import` on two unmerged branches by sessions that had each checked --
**a `docs/` grep cannot see an unmerged branch.** Holding the pair here makes such a collision legal
and gradeable rather than silent. The lift itself is as-built at `b8f1c862` and `91f95f43`.

**The ruling registry stands at 202 rows.**

**Arcs whose rulings live here: `balance` 84, `bank_import` 36, `recurrence` 37, `pay_calendar` 33,
`credit_card` 12.** That is every arc, and **51 of the ids here were MINTED by the lift** --
`R-R39`-`R-R45`, `R-PC2`-`R-PC33`, `R-CC1`-`R-CC12` -- because three of the five arcs spelled a
ruling with no id at all.

| arc | id | also | date | what was ruled |
|---|---|---|---|---|
| balance | R-G | -- | 2026-07-25 | A still-Projected item whose date has passed is CLAMPED FORWARD, never absorbed by an anchor: its instant is `max(attribution date, as_of + 1 day)` |
| balance | R-I | -- | 2026-07-25 | Before an account's first assertion the fold BACK-PROJECTS over the records, holding flat before the earliest one |
| balance | R-K | -- | 2026-07-25 | The grid's balance row and its subtotal rows are ONE step list grouped two ways, plus a named remainder |
| balance | R-N | -- | 2026-07-25 | The cutover ships FIRST; recording when money actually moved is the follow-up (X-f) |
| balance | R-O | -- | 2026-07-25 | The Reconciliation row is "Timing & true-ups", it sits in the sticky footer above the balance, and it renders whenever ANY visible column is non-zero |
| balance | R-L | R-Y | 2026-07-25/26 | Modeled interest and ACCRUAL begin at the account's LATEST assertion's own day, for all modelled kinds |
| balance | R-S | -- | 2026-07-26 | An ASSERTION always wins, and before the FIRST one the balance holds FLAT, for all five kinds |
| balance | R-T | R-X | 2026-07-26 | ACCRUAL events are DAILY, resolved in ONE sequential pass, computed at full precision and credited in whole cents carrying the remainder |
| balance | R-U | -- | 2026-07-26 | The replay owns the SEED and the history; the forward WHAT-IF surfaces keep `growth_engine` |
| balance | R-Z | -- | 2026-07-26 | A modelled CONTRIBUTION lands on its pay period's `start_date` and exists only when that payday is STRICTLY AFTER the latest assertion |
| balance | R-AF | -- | 2026-07-27 | The `/investment` chart's projection axis starts the day AFTER the history line's last valued date, so the two lines join BY CONSTRUCTION |
| balance | R-AG | -- | 2026-07-27 | Past the pay-period horizon, let the fold answer and RECORD the half-model rather than capping it |
| balance | R-AK | -- | 2026-07-27 | The dashboard, the pulse and the analytics calendar STAY on the kind-blind cash view; the divergence is RECORDED, and X-j is the step that rules it |
| balance | R-AO | -- | 2026-07-27 | The homeless half of the findings ledger becomes FOUR named steps, and two existing steps widen to absorb the rest |
| balance | R-AZ | -- | 2026-07-27 | A producer publishes only what the presentation boundary reads |
| balance | R-AP | -- | 2026-07-27, NOT as recommended | The write-side recurrence cluster STAYS in this arc, as plan step X-k |
| balance | R-BU | -- | 2026-07-28 | A residual double load is SEQUENCED behind the memo that fixes it, not deferred |
| balance | R-CH | -- | 2026-07-30 | The archived drawer's figure is NAMED for what it is |
| balance | R-CU | -- | 2026-07-30 | The ledger-class rule is finding N-122 with its OWN step (X-ab), and the false claim is corrected NOW |
| balance | R-CX | -- | 2026-07-31 | X-x is scoped to what X-l cannot subsume; the degraded-vocabulary question is SEQUENCED into X-l |
| balance | R-CY | -- | 2026-07-31 | The no-current-period answer is X-v's rule EXACTLY: raise a named error, handle it once, offer a repair |
| balance | R-CZ | -- | 2026-07-31 | A requested WINDOW that is empty is navigation, not absence, and stops answering with the absence card |
| balance | R-DA | -- | 2026-07-31 | `onboarding.has_periods` means "a period covers today", the same question every other surface asks |
| balance | R-DE | -- | 2026-07-31 | X-x is HELD behind X-ad: a refusal that points at a repair which does not work converts wrong numbers into a dead end |
| balance | R-DH | -- | 2026-07-31, amended at S1-c | An assertion is the CLOSING BALANCE for its civil day, and the day is the user's, not UTC. Six parts (a)-(f); (d) restated on an OBSERVED `settled_on`, so a NULL is "not seen on a statement" and is NOT reconciled |
| balance | R-DK | -- | 2026-08-02 | The posting self-heal stops being delta-keyed, which dissolves the reason `_reconcile_postings_after_update` gives for existing (N-153) |
| balance | R-DM | -- | 2026-08-02 | The checked-projection assert grades a FINISHED operation, never a half-finished one, and the commit boundary is the end state (X-ai) |
| balance | R-DN | -- | 2026-08-02 | ONE status seam, and the transition context stops being a caller's opinion |
| balance | R-DO | -- | 2026-08-02 | An illegally-drifted shadow status is REFUSED, not silently repaired |
| balance | R-DP | -- | 2026-08-02 | W9907 is DELETED, not shrunk -- the write door becomes structural |
| balance | R-DQ | -- | 2026-08-02 | The two remaining allowlist-bearing fences become their own phase (G), scheduled rather than remembered |
| balance | R-DR | -- | 2026-08-02 | `restore_transfer`'s four preconditions move to `_transfer_validation`, and X-aj1 ships as three commits |
| balance | R-DT | -- | 2026-08-03 | Reversing a documented deliberate decision is its own change and is not bundled into a writer swap (N-153 stays out of X-d) |
| balance | R-DU | -- | 2026-08-03 | The posted ledger gets ONE VERB and ONE TRIGGER, on BOTH ledgers, and the row-level posting writer stops being the interface |
| balance | R-DV | -- | 2026-08-03 | A journal entry is the projection of exactly ONE SOURCE EVENT, and the EVENT owns it. An account is the SCOPE of a re-derive, never an owner |
| balance | R-DW | -- | 2026-08-03 | A transfer's entry has ONE valuation; a broken Transfer Invariant 3 becomes an ASSERT failure rather than a write oscillation |
| balance | R-DY | -- | 2026-08-03 | The source identity is an EXCLUSIVE ARC of typed FKs with an AT-MOST-ONE check, never an exactly-one check |
| balance | R-EA | -- | 2026-08-03 | An anchor correction books in the pay period CONTAINING the day it asserts, DERIVED through the one function both ledgers share -- never read from the source row's stored `pay_period_id` |
| balance | R-EB | -- | 2026-08-03 | The anchor half is redesigned from scratch: the cash ledger is SUM-OF-POSTINGS, an assertion is a RECONCILIATION not a reset, the residual posts to Uncategorized Expense / Income, and bank import (X-f6) is the sequenced follow-on rather than an alternative |
| balance | R-EE | -- | 2026-08-03 | The settle door stays ONE CLICK, and the true-up form gets its own statement date |
| balance | R-EH | -- | 2026-08-03 | `accounts.current_anchor_balance` and `current_anchor_period_id` are DELETED |
| balance | R-EK | -- | 2026-08-04 | The resolver's history/projection cut is a PROXY for the day the money moved; replacing it is X-an, not X-f1 |
| balance | R-EM | -- | 2026-08-04 | The four "no current period" fallbacks ASK THE SEAM; they do not read a stored balance |
| balance | R-EN | -- | 2026-08-04 | The C-17 optimistic lock leaves the cash true-up, because the true-up stops writing the row it locked. **The ruling STANDS and its RATIONALE was corrected (N-190)**: "a second tab overwrites nothing" is true of one table in a transaction that mutates three, and the deleted column had been serialising the reconcile by accident. Replaced by one per-user advisory lock inside the reconcile, both ledgers, all four entry points |
| balance | R-EO | -- | 2026-08-04 | `account_anchor_history.pay_period_id` is DELETED. An assertion is (account, day, balance) and nothing else |
| balance | R-ER | -- | 2026-08-04 | The rule that a civil day is ASSERTABLE belongs to the module that owns what an assertion IS (`anchor_service`), now that a second writer asks it -- and **"the user has a pay schedule at all" SPLITS OUT of it** as account creation's own precondition, because ruling R-EO falsified the reason it gave and the live one is the opening's posting reconcile (**N-192**) |
| balance | R-EQ | -- | 2026-08-04, DESIGNED FROM SCRATCH rather than re-keyed; **horizon SHARPENED at the build** | **An assertion is refused only when it changes nothing.** The duplicate-submit rule is a comparison against the assertion GOVERNING THE DAY THE SUBMISSION ASSERTS, made in the write door under the per-owner lock that door already takes, on BOTH anchor tables; the content-keyed unique indexes are DELETED. Idempotency is a property of a REQUEST, not of the row's contents, so a content key must mis-classify either a retry or a deliberate re-assertion -- and the two errors are not the same size. **The horizon is the rule, not a detail of it**: this ruling first read "the account's CURRENT latest assertion", which has the index's fault mirrored -- a submission for an EARLIER day can never equal the latest, so every back-dated retry appends a permanent row |
| balance | R-ES | -- | 2026-08-05 | **`account_anchor_history.notes` is DELETED and the origination assertion goes through the SAME write door as every later one**, so the table has ONE writer. An assertion is (account, day, balance) and nothing else -- the sentence ruling R-EO already wrote into the model. Measured: no code in `app/` READS the column (AST census), 76 of 78 production rows are NULL, and it labels the origination on 2 of 9 accounts. It is a SECOND answer to a question the app already decides positionally (`CashAnchorFact.is_opening` -> `account_opening` / `account_trueup`), and the forensic trail it was nominally for is `system.audit_log`, which records every INSERT with the full row and the acting user. The loan twin's typed `source_id` STAYS: that one is read |
| balance | R-ET | -- | 2026-08-05 | **Feedback about a WRITE mounts where no refresh region owns it; a caption about STATE stays with its surface.** The two had shared one element, so the transient fact inherited the durable one's per-surface mounting and was destroyed by the durable one's own refresh. Corollary ruled at the same build: **an affordance that cannot succeed is DELETED, not given a nicer refusal** -- a loan's balance cell renders read-only rather than answering a designed error |
| balance | R-EU | -- | 2026-08-09 | **The true-up form compares the LEDGER's balance for the day it names against what the user typed, live.** Not the last asserted balance -- that figure is already the box's prefill, and the difference that matters is the reconciliation gap the grid's Period timing / Book vs bank rows diagnose. Rejected: a static book figure with the subtraction left to the reader, and a JS-computed difference (money math in the browser) |
| balance | R-EV | -- | 2026-08-09 | **An assertion gets a DURABLE home before its acknowledgement is re-keyed.** The loan page has listed every anchor with its drift since Commit 16 and the cash side lists none, so the fix for "nothing records that this landed" is the missing card, not a longer-lived toast. The 8s autohide STAYS as ruled at X-f1e3; what changes is that the toast stops being the only evidence |
| balance | R-EW | -- | 2026-08-09 | **The reconcile panel offers EVERYTHING the statement can settle, grouped by the thing it belongs to, and the two acts stay independent.** A purchase nests under its own envelope and ticking it settles that purchase ALONE; closing the envelope is a separate tick in the same block. Rejected: grouping by act-type (which separates a grocery purchase from the grocery envelope), a flat undifferentiated list, and an editable close amount (a second writer of `actual_amount` beside the entries that derive it) |
| balance | R-EX | -- | 2026-08-10 | **Registration ASKS rather than inventing**: the most recent payday, the cadence and the horizon, on the sign-up form. The bound is that the paycheck the stated payday opens must still be running, which is what makes sign-up day fall inside period 0 with no second step. The seed script REFUSES to run without a payday rather than defaulting one -- there is no honest default for the day somebody was paid, and defaulting it would put the fabrication back on the one path that provisions the production owner |
| balance | R-EY | -- | 2026-08-10 | **The interior hole's repair is pay-calendar `C3`'s DERIVED index, not a relaxed predicate in X-ad, so N-127 MOVES to that arc and `X-x` gains `C3` as a blocker.** R-DE had asked for "an overlap bound that permits a batch which fills a hole"; relaxing that bound alone is unsafe, because `generate_pay_periods` seats new periods at `max_index + 1` and a hole-filling period would take the LAST ordinal while carrying an interior date. `filing_period`'s own docstring names that as its precondition -- the two filing rules part company on **800 of 872 probed days** once `period_index` order stops matching `start_date` order -- and 21 `app/` files read the ordinal as chronological. C3 makes the writer DERIVE both, which fills a hole by construction and deletes the predicate instead of widening it. Rejected: building an interim renumbering pass in X-ad that C3 then deletes, and folding C3 into X-ad (two money-touching changes and a schedule rewrite in one PR) |
| balance | R-EZ | -- | 2026-08-10 | **An AUTOMATIC writer never creates a pay period that has already ended** (N-124), and **X-ad DECOMPOSES** into the door that CREATES a calendar and the door that GROWS it. Rejected for N-124: filling the whole lapse in one pass rather than converging over page loads (still manufactures history the owner never saw), and resuming at the next payday on or after today (writes no false history but opens exactly the calendar hole this arc exists to make impossible) |
| balance | R-FA | -- | 2026-08-10 | **What settling a row MEANS is a service verb, and the reconcile tick calls the same one the grid's Mark Paid does.** The rule lives today in two route branches -- `mutations.py:552` (envelope-with-entries settles at sum(entries), everything else keeps its stored amount) and `_shadow_mutations.py:209` (a loan-payment shadow freezes the live payment-date amount first) -- and BOTH are load-bearing for the panel: production's `Kayla's Spending Money` carries no entries, so an unconditional `settle_from_entries` books `$0.00` against a `$100.00` estimate, and 2 of the 4 transfer shadows the replay offers are loan payments (`$1,910.95`, `$531.94`) where skipping the freeze restores the creation-time escrow. Rejected: the writer re-stating both branches (this arc's own root cause 1, on a money rule), and the tick calling the mark-done HTTP endpoints (one request per row, no atomicity, and no channel for the statement date) **Recorded as R-EX** (recorded as **R-EX** in commit `daa9c402`, which cannot be edited; renamed 2026-08-10 on discovering that X-ad's own ruling had taken that id the same day, from another session. The id namespace is NOT graded -- see **N-217**) |
| balance | R-FB | -- | 2026-08-10 | **A BILL's tick MAY correct its amount; an ENVELOPE's close still may not.** The panel renders a bill's figure as a prefilled input: ticking it untouched settles at the stored figure, so the one-click habit **R-EE** protects is unchanged, and a different figure is written through the optional actual R-FA's verb ALREADY carries -- a second DOOR onto one rule, not a second rule. **R-EW's reason for refusing an editable close does not transfer**: an envelope's `actual_amount` is DERIVED from its entries, so a box there would be a second WRITER of a derived value, and a bill has no entries. Measured read-only on production 2026-08-10: **11 of 93 settled bills carry a hand-typed correction, `$252.79` gross** (Electricity `$300.00` -> `$245.32`, T-Mobile Fiber `$70.00` -> `$50.00`, Geico `$178.32` -> `$165.22`), and **4 of the 30 bills the panel would have offered** over Checking's 53 assertion days, `$40.16`. Rejected: settling at the estimate and leaving the difference to X-f3's residual, which books KNOWN money as Uncategorized Income and overstates the real category by the same amount -- feeding the very bucket X-f3 exists to shrink, at the one moment the user is holding the statement that says the true figure; and linking each row out to the full-edit popover, which is a second request and loses the reader's place mid-statement |
| balance | R-FC | -- | 2026-08-10 | **The offer set is ONE collection of blocks; a block with no children RENDERS as one row.** The developer chose the flat-bills layout on sight, and measuring why turned the fork into a non-fork: the whole readability difference was RENDERING, so three presentational rules -- a childless block prints inline instead of as a heading above a one-item list, the sort key gains a kind term, and a section label is emitted when the kind changes -- reproduce the preferred panel byte-for-byte over one collection (checked by normalising the two mockups' markup, not by eye). So X-f2-c2 and X-f2-c3 each ADD an arm and the assembler's ordering, heading, count and empty rules are never rewritten inside a money commit (**R-EY**). Rejected: two collections held side by side, which buys an identical screen for two ordering rules, two heading rules and a two-collection empty test. **One argument for this ruling was WITHDRAWN before it was made**: that a shared section label breaks once an offer set spans two pay periods -- true in principle, but no assertion day in production has ever had one, and across 93 settled bills the worst overdue is 12 days against a 14-day period with ZERO past a whole period |
| balance | R-FD | -- | 2026-08-11 | **The panel offers INCOME rows too: a deposit you are still waiting on is exactly what a statement settles.** The settle verb already books Received for income (`transaction_service.settled_status_id`), so the cost is the panel's VOICE -- "tick everything your statement shows" rather than "has your bank taken these" -- and a summary that counts deposits and payments separately. Measured read-only on production 2026-08-11, replaying all 53 Checking assertion days: **3 income rows would have been offered, `$2,304.27`**, the largest an FSA reimbursement of `$1,958.87`. The paycheck has never been in the offer set: it is marked Received the day it lands, so no assertion has caught it still Projected. Rejected: expenses only, which keeps one voice and leaves `$2,304.27` of real reconcilable money tickable only from the grid |
| balance | R-FE | -- | 2026-08-11 | **A settle books the FRESHEST derivation of the row's own amount, and the rule lives in the VERB so every door books the same figure.** Absent a caller-supplied actual: `sum(entries)` for an envelope carrying entries (unchanged), the projection's own live derivation where one exists, the stored `effective_amount` otherwise. This is its own step **X-aq** and it MOVES MONEY at the grid's Mark Paid, so it runs BEFORE X-f2-c2's money commit -- otherwise the new panel ships displaying a figure the grid contradicts. **The ROOT cause it does not fix is a denormalization**: `transactions.estimated_amount` is a CACHE of a derivation, `income_service.live_projected_net` repairs it at READ time and writes nothing back, so readers and writers hold two answers with no reconciler. Dissolving that -- one stored amount kept true by a reconciler on input change and at deploy, deleting `live_amount_overrides`, `ProjectedBasis.amount_overrides` and the whole override thread through `sum_projected` -- is step **X-ar** and closes **N-40** and **N-224**. Rejected: deciding the question inside the reconcile panel, which makes one row book two figures depending on which control was used -- the exact defect this arc exists to remove |
| balance | R-FF | -- | 2026-08-11 | **A tick's amount is CORRECTABLE exactly when the settle verb takes its MANUAL branch**, i.e. when the row is not (envelope-tracked AND carrying entries). One predicate, read off the verb's own branch, rather than a second classification kept in sync by hand. It makes **R-FB** true by construction -- an envelope with entries is the derived case R-EW protects -- and answers the case neither ruling names: production's `Kayla's Spending Money` is envelope-tracked, budgeted `$100.00` and carries ZERO entries, so the verb already treats it as manual and there is no derived value to protect. Rejected: correctable iff not envelope-tracked, which books that `$100.00` blind and sends the only correction path back to the full-edit popover R-FB rejected |
| balance | R-FG | -- | 2026-08-11 | **A row is offerable when EVERYTHING it would book had moved by the statement day, and that is the whole bound.** Ruling N-227: the attribution bound STAYS. The bound was challenged because an envelope has no meaningful landing day of its own, so its close is offerable for the whole period it is being spent in; the proposed remedy `period_end <= observed_on` is **REFUTED by the owner's own behaviour** -- measured on production, they close envelopes DURING the period **21 of 33 times (64%)**, as early as 9 days before period end, mean 2.7 days before, so it would have refused two thirds of the closes they really made and empties today's panel. Two of N-227's own claims were also WRONG and are withdrawn: its stated mechanism ("an envelope carries no `due_date` so `attribution_date` falls back to the period start") describes **4 of 231** envelope rows, while **185** carry an explicit `due_date` EQUAL to the period start -- a value the recurrence engine writes, not a fallback; and its hazard ("closing early makes every later purchase on that row inert") is FALSE for the `Paid` status the close actually writes -- `entry_service._update_actual_if_paid` recomputes `actual_amount` and `_resync_postings_if_settled` re-posts, which an existing green test and 24 of 24 production rows both confirm. What the challenge correctly identified is a DIFFERENT defect, and it is fixed structurally rather than by a calendar bound: an envelope's value is an AGGREGATE, so it could book a purchase the statement never saw. `_wholly_spent_by` is that fix. Rejected: narrowing the post-true-up modal to statement-observable rows, measured to move the prompt from 46 of 48 assertion days to 42 -- a UI palliative for a bound that is now correct |
| balance | R-FH | -- | 2026-08-11 | **A settle's refresh of a derived amount writes the CACHE (`estimated_amount`); `actual_amount` receives a HUMAN's figure and nothing else.** Amends **R-FE**, whose first implementation wrote the live derivation to `actual_amount`. That column's NULL-ness is read as "a human entered this fact" by three subsystems -- `income_service` (a settled income row's actual is never a recomputable projection), `spending_analysis` (only an explicitly entered actual is a surprise, so a machine write manufactures one) and the grid cell (which strikes through the estimate whenever the two differ, rendering a figure the owner never saw). The write was also PERMANENT and unlabelled: the row leaves `live_projected_net`'s Projected-only candidate set at that same flip, so the stale estimate could never be repaired afterwards and **X-ar's reconciler could not have told a machine write from a real correction**. Writing the cache instead makes X-aq X-ar's FIRST INCREMENT rather than a compensating layer -- the same column, one trigger instead of several -- and leaves the booked figure, `effective_amount` and R-DH (c)'s invariant unchanged. `is_override` is deliberately not set; the recurrence engine's `resolve_conflicts` rewrites `estimated_amount` with it False for the same reason. Rejected: keeping the single column and teaching the three readers to tolerate it, which spreads one denormalization into three |
| balance | R-FI | -- | 2026-08-11 (amended the same day by the two adversarial reviews it was written against) | **A row's amount is either its OWN -- a human authored the figure, or the money moved -- or it is DERIVED, and a DERIVED AMOUNT IS NOT STORED.** Two rules, the second following the first: every financial quantity that VARIES OVER TIME is a dated series resolved as-of; every quantity DERIVED from those is not stored at all. **The discriminator is an EXPLICIT `amount_source`, and the first draft's link-derived one was REFUTED by tracing**: the rules are not a partition over `template_id` / `transfer_id`, because salary is a SUBSET of template, loan payment is a SUBSET of transfer, and a CC payback row carries NEITHER link while its amount is derived -- so the proposed `CHECK (estimated_amount IS NOT NULL OR template_id IS NOT NULL OR transfer_id IS NOT NULL)` would have made storing THAT derived value structurally mandatory, which is the ruling inverted. A declared source cannot go stale against the links because the links never decided the rule. The CHECK becomes `(amount_source = OWN) = (estimated_amount IS NOT NULL)`, so a derived row is structurally incapable of holding a figure that can go stale and a reader that skips the resolver gets `None` rather than a plausible wrong number. The kinds this arc moves are salary, template, transfer, loan payment and the card payment `credit_card:CC4b` plans as another override-map entry; **they are not the whole census** -- about twelve stored-derived monetary values exist, which is **N-243**, and the arc SEQUENCES the rest rather than claiming to have found them all. **This SUPERSEDES R-FE's remedy and step X-ar**, whose "one stored amount kept true by a reconciler" preserves the denormalization, rests forever on trigger completeness, and moves salary in the OPPOSITE direction from the other four; **X-aq (`9cabc206`) survives as the FREEZE** -- the moment a derived amount becomes a fact. Rejected: reconciler-plus-detector, which needs a detector precisely because staleness stays representable; and deriving a template row from `template.default_amount` directly, a scalar with no time dimension, so a June price change retroactively rewrites March. The evidence is that the codebase already obeys rule 1 for loan rates (`RateHistory`), escrow (`EscrowComponentVersion`), tax years and raises (`apply_raises`), and obeys rule 2 nowhere |
| balance | R-FJ | -- | 2026-08-12 (developer) | **An envelope with NO purchases settles at its BUDGET through a door and at `$0.00` through carry-forward, and the discriminator is the CALLER's ACT, not the row.** Finding **N-230**: `transaction_service.settle_transaction` gates its entries branch on `tracks_purchases AND entries`, so an entry-less envelope takes the MANUAL branch and books `effective_amount`; `settle_from_entries`, which `carry_forward_service` calls directly, books `Decimal("0")` for the identical shape. Both are right, because the two callers have done different things to the money. **Carry-forward has already RELOCATED it** -- it rolls `estimated - sum(entries)` into the next period's row and then settles the source at what was spent, so booking the estimate there would count the same dollars twice. **A DOOR has relocated nothing**: pressing Paid says the budget is finished, and booking `$0.00` would record that no money left the account while marking the row Paid. Production carries BOTH signatures, which is how the split was found. What was not defensible, and is what this ruling fixes, is that the difference was undocumented and fell out of which function a caller happened to reach for; both functions now state it. **AMENDED 2026-08-12, the same day, by the adversarial review it was written against: the rule holds at the SETTLE MOMENT and nothing holds it afterwards.** An entry-less envelope settled either way is left `Paid`, not archived, so a purchase logged later re-derives `actual_amount` from the entries and re-posts -- reversing the door's "the budget is finished" reading, and spending money the carry-forward branch has already relocated. The ruling stands for the act it governs; reconciling the rollover it divided is **N-249** / step **X-ax**. Rejected: an unconditional `sum(entries)` for every caller, which hands already-spent money back to the projection (the reading **R-FA** already refused); an unconditional estimate, which makes every rollover count its envelope twice; and refusing an entry-less envelope settle outright, which breaks the Paid button for every entry-less envelope row sitting Projected on the grid |
| balance | R-FK | -- | 2026-08-12 (developer) | **The amount discriminator names the RELATION that prices a row -- its recurring DEFINITION, or its PARENT TRANSFER -- and never the RULE that computes the figure.** Amends **R-FI**'s enumeration, not its rule: the column is still an EXPLICIT declaration paired with the presence of a figure by a CHECK, and a CC payback still declares a relation while storing nothing. What changes is that SALARY-vs-TEMPLATE and LOAN_PAYMENT-vs-TRANSFER are properties of the DEFINITION rather than facts about the row -- a template is salary-linked when an ACTIVE profile names it, a transfer template is a loan payment when it holds a settings row -- so storing them on every generated instance is a denormalization with an update anomaly that TWO LIVE routes trigger (`loan.payment_transfer.track_payment` creates the settings row a legacy manual payment never had; `salary.profiles.delete_profile` archives a profile, the moment a template stops being salary-linked). Both would need a writer that rewrites every affected row, which is the shape this arc exists to delete. **The OWN state is the ABSENCE of a source rather than a value**, which is what lets the CHECK be `(amount_source_id IS NULL) = (<amount> IS NOT NULL)`: a constraint over two NULL-nesses with no `ref.amount_sources` id frozen into the schema, where the ref-table convention otherwise makes an id-free pairing inexpressible. Rejected: the five RULE values as R-FI first enumerated them, for the update anomaly; a plain boolean, which the CHECK makes logically equivalent to the amount column's own NULL-ness; and dropping the column so NULL alone means derived, which leaves a writer that sets a figure on a derived row indistinguishable from a hand edit. **AMENDED the same day by the adversarial review it was written against**: under a RELATION the salary-linked rows re-price to `default_amount`, a scalar `template_amount_service` itself calls vestigial. The ruling stands and the remedy moves to the write DOOR (**N-261**): an archive states the price its rows were generated at, or refuses. **What this design does NOT police, stated because an unstated limit reads as stronger than it is**: a declaration naming a relation the row does not carry is refused by the resolver rather than by the schema (all three link FKs are `ON DELETE SET NULL`, so the DATABASE can strip one); ownership INFERRED from `is_override` or from leaving Projected is invisible to the CHECK (**N-262**); a second derived amount on one row is inexpressible (**N-265**); and a producer reached from neither link nor definition needs a relation this set does not hold (**N-263**, **N-264**). **Two costs are accepted rather than denied**: a definition-level flip re-prices N rows with no row-level `system.audit_log` entry; and the mode booleans the refinement reads (`is_salary_linked_template`, `derive_from_loan`) are UNDATED, so archiving a profile today changes what a March row is worth -- R-FI rule 1 applied to the discriminator itself is a dated mode series on the definition, the deeper fix neither enumeration is. Also rejected: a composite FK from `(amount_source_id, <amount> IS NOT NULL)` to a `(id, is_own)` unique key, which keeps NOT NULL and freezes no id but closes none of the four gaps |
| balance | R-FM | -- | 2026-08-13 | **A purchase that has CLEARED is a cash posting; an envelope's close books only what its cleared purchases did not.** Forced rather than chosen: R-DH (c)'s invariant -- recording a purchase and truing up by the same amount must not move the projected end balance -- holds today because the anchor RESET drops the balance by exactly what the entry reservation releases (`_entry_aware_amount`'s `max(estimated - settled_debit - credit, outstanding_debit)`). Delete the reset without this and ticking a `$150.27` purchase releases `$150.27` of reservation against a book that never moved, so the projected end balance rises by it. It is also the grain the bank speaks in: of 110 movements matched to the developer's bank lines, **58 are individual purchases and 52 are transaction rows**, and an envelope row has no bank counterpart at all |
| balance | R-FL | -- | 2026-08-13 (developer) | **Whether a line is INSIDE a declared balance is a RECORDED FACT, not a comparison of two dates.** A transaction and a purchase each name the `account_anchor_history` row whose statement showed it; `ReconciledThrough.covers` and its date rule are DELETED from the cash side. This REVISITS **R-DH (a)** and **R-DH (d)**, and the developer's own bank exports are why: most Checking assertions do NOT equal the bank's closing balance for their day, several are that day's closing plus a subset of its own postings (a balance read mid-morning), and the rest are neither -- so an assertion is NOT the closing balance for its civil day, and no rule comparing two dates can decide what a statement showed. R-DH (d) deleted a stored `is_cleared` BOOLEAN as "a denormalized copy of a derivable fact", and that premise is what falls: clearing is not derivable. The replacement is not that boolean returning -- it is a LINK, which carries provenance (WHICH statement), has no date of its own to drift from the assertion's, and answers "what did this statement clear" as one indexed read. **The MODELLED side keeps a date rule and that is not an exception**: a payroll contribution (**R-Z**) and a modelled accrual (**R-L** / **R-Y**) are not lines anyone can tick, so there the assertion legitimately outranks the model (**R-S**) and the question is genuinely "is this payday after the latest assertion". Rejected: sharpening the date rule with the bank's posted day, which still cannot tell a mid-day reading from a late-posting item -- the two carry the same civil day. **AMENDED 2026-08-13, the same day, by the adversarial review it was written against: the DIAGNOSIS stands and the IMPLEMENTATION does not.** A link cannot simply replace `covers`, for three measured reasons. (1) `dated_deltas` emits EVERY source fact and only the correction's delta cancels the absorbed ones, so a settled row dated at or before an assertion that carries NO link is counted TWICE -- the `prefix = anchor + X` class the day partition exists to make unspellable. The link must therefore SHARPEN a default that still clears everything dated on or before, not replace it. (2) A link partition is not a PREFIX, and both absorb loops are monotonic-pointer prefix scans whose own docstrings state that precondition; assertion B legitimately clears a line dated before one of A's, which halts the pointer and silently shorts the ledger. (3) The POSTED walk has no row to read a link from: `_source_net_days` returns `(day, net)` aggregates read off the LEDGER, one of whose three partitions is residue with both source FKs NULL by construction. **X-f3a is under-specified until all three are ruled** |
| balance | R-FN | -- | 2026-08-13 (developer) | **An assertion is a CHECK, and an unexplained difference is a transaction the user ACCEPTS -- never an automatic plug.** It is an ordinary row with NO category, so it books to the per-owner Uncategorized Expense / Income ledger account through the category-leg rule that already exists (`ledger_account_service.get_or_create_category_ledger_account`), needs no new ledger-account kind, and is CATEGORIZABLE later -- which is the mechanism that shrinks the bucket. **This amends R-EB's remedy, and the measurement is why -- restated 2026-08-13 by the adversarial review it was written against, which REFUTED the first two forms of it.** *"92% of it cancels"* is an algebraic identity rather than a finding (the plug is DEFINED as what forces the ledger to the asserted balance, so it telescopes and the net is a function of the LAST assertion alone), and the *"two independent instruments"* are one and the same subtraction. **What survives is the real evidence**: adjacent assertions systematically undo one another (centred lag-1 autocorrelation `-0.306`, the 30-day bucket collapsing at the 1.8th percentile of a random-sign null), which is the signature of date misplacement rather than of spending; and the BANK-anchored untracked spend is several times the figure this ruling was first sized against. Booking the gross automatically would record over `$1,500` of "spending" on one day and a comparable "income" the next, either side of the mortgage payment that caused both. **The manual path is PRESERVED and that is the developer's own bound**: typing a balance away from a computer must still move the projected end balance, and it does -- in one accepted act that leaves a row you can find, instead of a plug you cannot. Rejected: automatic recording (books the churn), and leaving the difference underived (the projection then silently lags the bank) |
| balance | R-FO | -- | 2026-08-14 (developer) | **What a balance assertion's difference MEANS is a property of the ACCOUNT, and the counter leg is a TOTAL DISPATCH over `classify_account` -- not a carve-out.** `PLAIN` books it to Uncategorized once X-f3c makes it a recorded, user-accepted transaction (**R-FN**) and stays on `anchor_equity` until then, because classifying it automatically is what R-FN refuses; `INTEREST` books per-account **Interest Income**; `INVESTMENT` and `APPRECIATING` book per-account **Change in Value** in a SIXTH reporting class of their own, NAMED for what it measures rather than for a gain (developer 2026-08-14; **N-277**); `AMORTIZING` is untouched, a loan having its own correction family and its own chart. Five kinds, five answers, one function. **This is N-171 on a different account class, and it is LARGER**: `ref.posting_sources` holds no accrual, so nothing posts modelled interest or modelled return anywhere -- the true-up correction is the ledger's ONLY trace of them, and it lands in equity. Measured 2026-08-13, no interest, dividend or investment account or category existed at all, and thousands of dollars of return earned over 4.5 months were invisible on the income statement. **The RESET stays on the modelled kinds, and R-EB's own reasoning is why**: it deletes the reset because *"the reset discards what the records say"*, and an IRA has no record of a price movement to discard -- the assertion is the only fact, so the reset IS mark-to-market and re-deriving the correction each read is CORRECT (a contribution back-dated under an assertion rightly shrinks the gain). **The sixth class is not decoration**: `net_income = income - expense`, so a house revaluation booked to Income would read as money earned. Unrealized gain is other comprehensive income and is reported below the line. Rejected: one combined return account, which tells the owner the bank paid them the gain; and removing the reset on the modelled kinds, which breaks **R-S** for two account kinds with nothing to replace it and makes the app's IRA balance stop equalling the statement |
| balance | R-FQ | -- | 2026-08-14 (developer) | **Clearing is RECORDED now and only becomes AUTHORITATIVE at the cutover, so X-f3a splits in two.** The three answers R-FL's own amendment demanded, plus what measurement then forced on top of them. (1) A line naming no statement is UNKNOWN and the DATE rule answers it, so the link SHARPENS rather than replaces -- which is what keeps `dated_deltas` from counting an unabsorbed source twice. (2) Both walks stop being monotonic PREFIX scans: the rule answers per line and the walks GROUP by the answer, because a pointer meeting an out-of-order link halts and shorts every later assertion. (3) The posted walk reads a link where its source rows carry one and falls to the date rule where they cannot -- RESIDUE, whose entries carry both source FKs NULL by construction. **And the fourth answer is a THEOREM the first implementation got wrong.** While an assertion RESETS the ledger a link may NOT disagree with the day: a balance is the prefix sum of `dated_deltas`, which emits a source on its settle day and a correction on its statement's, so `prefix(statement day) = asserted balance` holds only where every line's clearing statement is the one the date rule picks. One source linked to a later assertion renders a balance below the asserted one on the assertion's own day, breaking **R-S**; the mirror direction reads `anchor + X`. So a link may only choose between assertions SHARING a civil day -- which is exactly what the panel writes -- every door that moves a settle day RELEASES the link, and `StatementCoverage` REFUSES what is left rather than rendering a balance that contradicts the user's own statement. **X-f3c is what lifts it**, because an assertion stops resetting anything there. The link is scoped by a COMPOSITE foreign key over the account rather than by a write-door convention, and NOTHING is backfilled: backfilling from the date rule would launder a measured-wrong guess into an observation nobody made, and X-f6a fills history from the bank. Rejected: letting the link outrank the date now, refuted by the measurement above; and refusing the day EDIT instead of releasing the link, which traps a user against the panel's own copy |
| balance | R-FR | -- | 2026-08-15 (developer) | **A purchase is a POSTING SOURCE of its own, and what makes it one is the DAY THE BANK TOOK IT.** The four answers **R-FM** left open, taken together because each decides the next. (1) The TRIGGER is `settled_on IS NOT NULL` on a debit purchase -- the same fact that makes a TRANSACTION an actual event, asked of the row in front of it -- and NOT the clearing link nor `StatementCoverage.is_cleared`. That is what *"a purchase that has CLEARED"* means: cleared the BANK, not cleared by a statement. It also DISSOLVES **N-286** rather than building it: no consumer needs to tell a record-cleared line from a date-cleared one, because which statement absorbed the movement is the walk's question about that movement, asked identically of both kinds. Rejected: the link, which carries 0 rows today so the step would move no money and leave **N-274** open until the bank import; and `is_cleared`, which would leave a purchase the bank took after the last assertion held as reserved BUDGET rather than booked on the day the money left. (2) The COUNTER leg is the ENVELOPE'S OWN category, so the expense is recognised in the right category on the day it happens and the close books the remainder to the same account. Rejected: Uncategorized until the close, which shows an open envelope's real spend as uncategorised on the income statement and makes every close write a reclassification pair. (3) A purchase gets its OWN journal entry -- `journal_entries.transaction_entry_id` plus a `purchase` source kind -- because the posted walk reads a source's DAY and its CLEARING LINK off the source row, and both are per-purchase; legs grouped under the parent's `transaction_id` would be dated at a `settled_on` a still-projected envelope does not have. It is also the grain the bank speaks in (**R-FM**). (4) A FUTURE `settled_on` on a purchase is REFUSED at the write door, which INVERTS the reason it had no upper bound: a forward day was conservative while a purchase was not a cash movement, and now releases the reservation today while booking the cash later -- already-spent money back in today's projection, the exact shape `status_seam.reject_future_settle_day` refuses on a transaction. Nothing expressible is lost, because an unobserved purchase leaves the day NULL |
| balance | R-GU | -- | 2026-08-26 (developer) | **A request is a QUERY or a COMMAND, and its TRANSACTION says which** -- the snapshot `X-i3` is about is taken at the REQUEST, not at the read pass. GET and HEAD run at `REPEATABLE READ, READ ONLY`; every other method keeps `READ COMMITTED`, writable; a GET that must write says so in one `write_transaction()` block that commits before the render resumes. **Two things the trace refuted, and each rules out the obvious design.** (1) The PASS cannot decide: `apply_statement_review` and `loan_recurrence_sync` both build one before they write, so a pass has no way to tell a render from a write's own decision basis and would need a second constructor -- "an argument a caller can get wrong is a defect, not a contract", which is `X-i4`'s own sentence reproduced in the step that removes `X-i3`'s. (2) A snapshot in front of a WRITE is a money bug, not a stricter setting: `user_write_lock`'s lock-then-reread requires the waiter to see the winner's just-committed postings, which only `READ COMMITTED` shows, so one snapshot re-arms the `$4,000.00`-asserted-against-`$1,000.00` divergence ruling **R-EN** measured -- silently, because the anchor-equity leg mirrors the error and the trial balance still closes at `$0.00`. **Measured before ruling**: the mechanism proven against this application's own session (two counts across one concurrent committed append answer `(1, 2)` today and `(1, 1)` under the query mode), 2,302 of 2,978 suite GETs arriving on an inherited transaction, and 36 arriving on one that had already written. **Rejected**: two constructors on the pass (the argument above); and "the render stops writing first", which is a product change across three arcs and still leaves every POST-then-render pass unsnapshotted |
| balance | R-GV | -- | 2026-08-26 (developer) | **A read pass BINDS the account it values, and the binding is a PRECONDITION of creating per-account pass state rather than a guard at each funnel.** `_context._memoize_once` -- the one primitive filling every `account.id`-keyed cache a `BalanceContext` holds -- takes the `Account` and refuses one whose owner is not the pass's, BEFORE the membership test, so a warm cache cannot serve a foreign hit and a funnel added later cannot forget a rule it never had to remember. **It is NOT a second ownership gate, and that distinction is the ruling's substance**: whether a REQUESTER may see an account is decided upstream where an untrusted id becomes a row, and a copy of that here would be the band-aid the developer refused. What this answers is whether an account and a pass describe ONE READ -- which no route can ask, because no route knows a context exists and a service-tier caller has no `current_user` at all. **What a mis-pairing would have published**: one owner's pass handed another owner's Checking answered that owner's real asserted balance across every column of the first, because transactions are scenario-scoped and `cash_anchor_facts(account_id)` is not. **The cash fold is MEMOIZED with it**, and its cache is PRIVATE because an `AssembledCashFold` IS a running total -- W9909 refuses a public one outright. **Two bindings the adversarial reviews forced on top**, each N-354's sentence one field over: `__post_init__` refuses a foreign SCENARIO, and `AssembledCashFold.account_id` binds the record `_asset_fold.resolve` folds an account onto, which memoizes nothing and so was never covered. **Rejected, on cost measured rather than asserted**: a `BoundAccount` value taking the pair out of all 42 seam signatures (~40 `app/` and ~420 test call sites, and it would not have closed the `resolve` case, which is account-vs-FOLD); and `ctx.account(account_id)` resolving through an owner-scoped query so a foreign account is UNREPRESENTABLE, which is the truer root but inverts the route layer and supersedes three resolvers. Both remain available and compose: `ctx.account(id)` would return a `BoundAccount` |
| balance | R-GW | -- | 2026-08-27 (developer) | **THE CUTOVER'S ORDER REVERSES: the residual is RECORDED before the reset is DELETED.** A measurement against the developer's own bank export refuted the first decomposition's sequence -- deleting the reset first lands the flip six assertions further from the bank than recording the residual first does. The flip is additionally ship-gated on the account's own statement history being imported (**N-368**), which no `blocked by` cell can hold because it is a DATA act. **Report the evidence SPLIT, never pooled**: pooled it read 17-of-75 for today against 0-of-75 for the cutover, and 15 of those 17 are days carrying an assertion, where the RESET forces the answer. On the 29 days no assertion touches: `$529.48` against `$1,956.64`. Rejected: swapping in `bank_agreement`'s residue metric, which is a MOVEMENT comparison and blind to a reset by construction |
| balance | R-GX | -- | 2026-08-27 (developer) | **Opening equity is the account's OWN stored fact, in a SATELLITE table, seeded at today's derived value so the migration moves `$0.00`.** What an account held before its records begin was inferred per read -- the earliest assertion less the movements it already contained (ruling **R-I**) -- which made it move when an assertion was back-dated, differ between scenarios, impossible to correct, and derived a second time by the posted ledger. Rejected: a column on `budget.accounts`, which would have to be NULLABLE for a LOAN, a rule the schema cannot state because the kind lives on `ref.account_types`, and which would sit where **R-EH** deleted a cached balance; and putting the figure on the opening ASSERTION row, which **R-EO** and **R-ES** reduced to `(account, day, balance)` -- a declared balance and capital brought on are two quantities |
| balance | R-GY | -- | 2026-08-27 (developer) | **An accepted residual is DERIVED while uncategorised, becomes the owner's own figure once categorised, and is offered only over a span an imported statement reconciles.** Three bounds on the acceptance act **R-FN** requires. The offer gate reads SHIPPED facts -- a `statement_imports` row at a zero `bank_agreement` residue -- because a first draft gated it on X-f3a-2's walked flag, which is a rank-3 step depending on a rank-5 one and could not be expressed in the `blocked by` column at all, so the dependency graph looked acyclic while the prose held a cycle |
| balance | R-HE | -- | 2026-08-27 (developer) | **The opening record is APPEND-ONLY, latest recorded row governs, and each row carries the DAY the books opened, the equity and a TYPED provenance** over `ref.account_opening_sources` (`user_declared` / `migration_derived`). Rejected: one mutable row per account -- `system.audit_log` would hold the restatement but nothing in the app could render it, the gap **N-205** built the balance-history card to close, and silently overwriting the level every balance rests on is not a state this app may enter. "Latest governs" is NOT the positional read X-f3c-2a deletes: `is_opening` inferred a TYPE from position in a list ordered by `observed_on`, a business date any owner may back-date, where this orders by `created_at`, which no door lets a user move. The DAY is stored because fixing only the amount leaves half the position-dependence alive. **EVERY account carries a row, loans included**, revising R-GX's first draft: `balance_at` falls through to the cash fold for an amortizing account with no `LoanParams`, so absence is reachable -- and with every account carrying one it is not, which lets the loader REFUSE rather than fabricate a `$0.00` level |
| balance | R-HF | -- | 2026-08-27 (developer) | **A movement dated before its account's books opened is repaired by moving the OPENING back to where the bank's own history starts, not by re-dating the movement.** The repair direction for `balance:X-f3c-2b` over **N-378**'s eight rows. Import what the records are missing so the recorded history is TRUE rather than merely self-consistent: account 10's books open at `$1,345.74` on 2026-01-30 by the owner's own Fidelity export, not at the derived `$4,879.26` (**N-379**). Rejected: re-dating the eight offending rows onto the opening day, which moves no rendered balance and is fully reversible, but makes the app's dates disagree with the bank's ON PURPOSE -- the same class of thing that made those dates wrong to begin with. Every changed figure goes to the developer before a production row moves |
| balance | R-GZ | -- | 2026-08-27 (developer) | **Every arc's RULINGS move OUT of its arc document into ONE gate-graded registry keyed `(arc, id)`.** Taken because rule 5's supply was measurably spent, not as a tidy-up: four of the five arc documents stood at or within 3 lines of their effective caps (balance 980 of 980, bank_import 179 of 180, recurrence 830 of 830 on `feat/r16-b`, pay_calendar 477 of 480), and of the balance README's nine ticked entries exactly ONE was not cited as a satisfied blocker by a live step -- so archiving bought one line, once. The growth engine is two-sided and rule 5 reclaims from one side only: a ruling is appended forever and never becomes archivable. **Also ruled: the LINE cap goes and the per-ROW cap comes with it**, which is the 2026-08-25 precedent applied whole rather than halved -- a first draft took only the removal and an adversarial review measured a 16,087-character row against a 529-character median. **And `(arc, id)` is kept over globally-unique ids**, re-put with the evidence for renaming (N-217 renamed `R-EX` to `R-FA`; ~35 citation sites today) and decided again: a collision becomes legal and gradeable rather than silent, and the citation arm that makes it safe is pulled forward to `X-ao-3` rather than the key being changed. Rejected: raising the caps (rule 4 forbids it where one binds), splitting section 5 by phase (helps one arc and not the growth engine), and archiving `X-i3-b` for its single line (the last one available). |
| balance | R-HA | -- | 2026-08-27 (developer) | **The `Settled` status -- the terminal ARCHIVE -- is DELETED, and whether a row is FINISHED is answered by reconciliation PROVENANCE, never by a member inside the settled band.** Provenance is three shipped columns answering three questions, each correctable without destroying the row: `settled_basis_id` (how the FIGURE is known), `settled_day_basis_id` (how the DAY is known), `reconciled_by_id` (which statement was seen to show it). **What decided it was not the zero row count.** The archive was ABSORBING and REACHABLE: entered from one dropdown, no door out, so it refused every act that CORRECTS a row -- revert, re-price, add or remove a purchase, match a bank line -- while `deletion_refusal` names no status, so the DELETE control on the same card removed it and reversed its postings. A lock whose only permitted mutation is destruction protects nothing, and `is_immutable` already gave Paid and Received the field lock, so its only distinct content was *and you may never revert*. **The law is a CONJUNCTION, not a ban on terminal states**: an absorbing state NOTHING can enter is a retired vocabulary item, which `credit_card` locked ruling 5 already builds at `CC3b`. **CC3b owes** a `deletion_refusal` arm for a terminal `Credit` or a stated reason the asymmetry is acceptable there -- X-am removed this instance and not the mechanism. **Supersedes** the archive half of `bank_import:R-GG` (d) by making it vacuous; its stored-figure half stands. **Rejected**: keeping it terminal with a confirmation, a badge and a delete refusal (the owner still gets a one-way trapdoor and the bank-import exclusion stays permanent); and keeping the name while dropping terminality (a second, cruder spelling of what `reconciled_by_id` already records). Measured: 1,591 audit rows over the two status-bearing tables since 2026-05-07, 229 of them DELETEs, name it nowhere |
| balance | R-HB | -- | 2026-08-27 (developer) | **`X-ao-2`'s shape: DECOMPOSED into TWO, the LIFT and the OVERFLOW.** The step's own sentence carried two nouns and only the first is a lift: `X-ao-2a` moves the last three arcs' rulings into the registry, mints an id for every row that has none and DELETES the map of arcs that had not moved; `X-ao-2b` re-homes the argument of every row over the 2,000-character cap, which is **N-370**. Measured before the split rather than after -- 24 over-cap rows holding 43,734 characters of overflow at the moment of the ruling, 25 and 43,855 once `recurrence:R-R38` arrived on the merge, none of the delta `bank_import`'s: `bank_import` owns 14 of them at 36,901, and that document stands at 151 lines against a 200-line cap -- so rule 4's literal destination could not hold it, and the second noun needed a ruling of its own (**balance:R-HD**) before it could be built at all. Rejected: one commit for both, whose second half cannot be reviewed in focus and spans sessions; and re-pointing N-370 at some later step, which rule 1 refuses because every other spelling of that means nobody |
| balance | R-HC | -- | 2026-08-27 (developer) | **A registry row states a RULE: a row that only POINTS at another decision does not become one, and a dated developer decision does become one wherever it was written.** Both halves were measured on the three arcs `X-ao-2a` lifted. Four `recurrence` rows stated no rule of their own and are recorded where their content belongs rather than given a minted id: a supersession fact that is `R-R13`'s own, a pointer at the archived `R-R7` (which becomes a row under the id it already had, since rule 5 forbids a live sentence depending on an archived one), a second copy of `pay_calendar`'s `R-PC2`, and a section LABEL rendered as a table row, which is **N-372**. The other half runs the other way: `credit_card`'s four `Grid / companion hard requirements`, taken in the same commit as its eight "locked rulings" and counted as rulings by nothing, are `R-CC9`-`R-CC12`. Rejected: minting an id for all four pointer rows, which puts a second copy of one ruling under two arcs and gives a section label a registry row |
| balance | R-HD | -- | 2026-08-27 (developer) | **An over-cap registry row's overflow goes to the step that OWNS the ruling: its AS-BUILT record where that step has SHIPPED, its live specification where it has not.** Rule 4 stated only the second and the second alone is unsatisfiable. Measured 2026-08-27 over the 24 rows the registry then held -- 25 since `recurrence:R-R38` arrived on the merge, which changes none of what follows: `bank_import` owns 14 at 36,901 characters of overflow against a document with 49 lines of headroom, and NINE of those 14 -- `R-FV`, `R-FW`, `R-FX`, `R-FY`, `R-GB`, `R-GC`, `R-GF`, `R-GJ`, `R-GG`, 14,949 characters -- are cited by no step entry in that document at all, live or shipped, because the spans that owned them were archived under rule 5. Every one of the nine is cited by an as-built record that already exists. This states no new policy: `rulings.md`'s own first sentence already said the deliberation belongs in the commit that shipped it and in the as-built record of the step that shipped it, and nothing graded that. **What it does NOT settle, measured after it was taken:** 17 of the 25 rows name no step of their own arc at all and only TWO name a SHIPPED one, so the OWNER this keys on is not a fact the corpus carries -- determining it per row is `X-ao-2b`'s first task rather than an input it has; and an owner that IS known and shipped may still have no as-built record, as `recurrence:R7d-c-1` has none. Owned by `X-ao-2b`. Rejected: raising the `bank_import` line cap by ~390 lines, which rule 4 forbids as the answer when a cap binds; and letting the argument live only in the commit, which deletes measured evidence from every document a reader can grep |
| bank_import | R-FP | -- | 2026-08-13 (developer) | **A statement importer is a SOURCE ADAPTER over one normalized line shape**; matching, review and fact-writing are source-independent, so the file path ships first and an automated source is additive. `FITID` is the idempotency key -- 342 of 342 distinct in the developer's own OFX -- so re-importing a file cannot duplicate. **A match is a PROPOSAL, never a silent apply**: it is reviewed before commit, every corrected `settled_on` goes through `system.audit_log`, and unmatched lines on BOTH sides are shown rather than guessed at. Rejected for the automated source: OFX Direct Connect, which needs no third party but stores the credentials that grant FULL account access, against SimpleFIN / Plaid, which store a revocable READ-ONLY token -- the strict option is the revocable one even though it adds a vendor. Also recorded, because it is what makes the file path first: the developer's exports already cover both accounts, so the matcher can be GRADED before an adapter exists **Amended 2026-08-24 by R-GH**: the a-match-is-a-proposal half now holds only for acts that MODIFY a hand-made row; a standing rule is consent for a NEW swipe's creation. The source-adapter, idempotency and security grounds stand. |
| bank_import | R-FS | -- | 2026-08-16 (developer) | **A MATCH has three shapes, because the grain mismatch runs in BOTH directions.** Measured on the developer's own accounts 2026-08-16: the bank shows 156 individual card-swipe lines where the app holds 34 envelope rows, and shows ONE payroll deposit where the app holds three rows (`$2,473.38` + `$100.00` + `$39.54`). So a match is (1) one line to one row, (2) a GROUP -- N app rows summing to one line, or N lines summing to one row -- with the bank's day and the clearing link written to every member, or (3) a bank line the app has no row for BECOMING a purchase against an envelope the owner picks. The third is what makes the app's records reach the bank's grain permanently, and `balance:X-f3b` is what makes it work: a purchase carrying a posting day is already a cash movement of its own whose envelope books only the remainder. Rejected: one-to-one only, which explains 119 of 227 lines and leaves half the account with no clearing fact at all |
| bank_import | R-FT | -- | 2026-08-16 (developer) | **An import PERSISTS a normalized line table**, not nothing and not an id ledger. `budget.bank_statement_lines` under a `budget.statement_imports` batch, with accepted matches recorded separately and PROPOSALS never stored. Uniqueness on the line's identity makes re-import idempotent STRUCTURALLY rather than by the importer remembering to look -- and the same table is the fact `balance:X-f3a-2` needs (a statement walked line by line, so a line it did not show is NOT CLEARED) and the provenance `balance:X-f3c` needs to re-open a recorded difference. One table serves three steps; storing nothing would have each invent its own |
| bank_import | R-FU | -- | 2026-08-16 (developer, on measurement) | **The source is SECU's CSV with running balances, and a line's identity is POSITIONAL rather than the bank's own id.** QBO and QFX are the same file as the OFX, so the real choice was CSV or OFX: the OFX truncates most descriptions to 32 characters and carries no per-line balance, while the CSV carries the merchant, the bank's category and a running balance, and its description starts with the OFX name on every shared line. What the CSV lacks is `FITID`, and **R-FP's idempotency key is superseded on measurement**: the positional key `(account, posted_on, amount, sequence within that group)` reproduced the `FITID` key exactly across two exports twelve days apart, so identity is one rule for every adapter and an external id is stored as corroboration. The ordinal is what makes it total: two real charges may share a day and an amount. **AMENDED 2026-08-16 by the step's own adversarial review, because the measurement is blind on the key's only novel component**: no group exceeded one member in either export, so the ordinal was 0 throughout and the comparison tested `(day, amount)` against `FITID` and never the ordinal -- the one term that is derived rather than observed, and the one that can be unstable. The honest statement is therefore: identity is positional, and the positional key is proven equivalent to the bank's own id ONLY on data where no group exceeded one member. **AMENDED AGAIN 2026-08-20 (developer), and this is the amendment the first one asked for**: the positional key STAYS the stored identity and the ordinal STOPS being a term any re-import compares against. Two same-day same-amount lines re-exported in the other order refused the WHOLE file, thirty days of genuinely new lines with it, and told the owner the bank had restated a line it had not; a genuinely NEW line the bank INSERTED ahead of a recorded one was refused the same way. Both are one defect: an app-assigned number compared as though the bank had supplied it, a derived value stored beside its source with nothing reconciling the two. A re-import now reconciles a `(posted_on, amount)` GROUP as a MULTISET on the wording the bank wrote, and mints an ordinal only for a line it has decided is new -- so the description compare stops being what holds the key total and becomes only what **R-FL** asks of it. **The refusal is unchanged** where BOTH halves of a group hold a member the other cannot account for, the only shape that is a contradiction rather than a change in what the export covers; neither half ALONE is one. The event stays unobserved, and **R-GB**'s repair door is what it costs when it happens |
| bank_import | R-FV | -- | 2026-08-17 (developer) | **A match is stored as IDENTITY, and the CLEARING LINK is not written from one.** The two answer different questions: a match says *which real movement this app row is*, and `transactions.reconciled_by_id` (**R-FL**) says *which declared balance already contains it*. The second is DERIVABLE from the first once a statement carries the line -- anything matched to a line on a statement covering day D is inside any balance declared for D or later -- while the first is not derivable from the second, so the app stores the fact and derives the rest. `reconciled_by_id` names an `account_anchor_history` row, a balance the owner typed, and a bank line names none. What an accepted match writes is the bank's posted day, which makes the date rule's INPUT an observation instead of a guess. **It does not make that rule correct, and a first draft of this ruling said it did** -- R-FL rejects *"sharpening the date rule with the bank's posted day"* by name, because a civil day still cannot tell a mid-day reading from a late-posting item. The honest claim is narrower: the same rule now runs on a true day, which is strictly better and is still a guess until `X-f3c`. **The settle doors RELEASE any prior link as they move the day**, inherited from `X-f3a-1`: the bank contradicts the DAY, not the STATEMENT, and an assertion observed weeks later still contains the row. The release is safe (`_recorded_anchor_id` falls back rather than raising) and it is a LOSS -- a row the owner had ticked goes CLEARED back to UNKNOWN; **N-307** owns the class. **Also ruled, on the same principle:** an unbalanced group is REFUSED and its difference NAMED, never apportioned (which needs a rule about which member is wrong -- a decision about a paycheck, not a matcher's) and never absorbed by a tolerance (which would silence **N-239**); **that refusal is SUPERSEDED 2026-08-22 by R-GD**, which RECORDS the variance instead, on the measurement that the refusal is what produced the duplicate Geico. And a match MAY settle a still-Projected row, because a statement is evidence that money moved -- which the GROUP path must reach too, or a split payroll deposit with one unsettled member is unproposable. **Rejected:** writing the link to whatever assertion the date rule already picks, which stores a copy of a derivation; and recording the BANK's own daily closing balances as assertions, which is the best data available but would move Checking's OPENING assertion back and redefine the account's opening equity -- the cutover's decision, recorded as **N-304** and owned by `balance:X-f3c` |
| bank_import | R-FW | -- | 2026-08-18 (developer, on measurement) | **A match corrects a purchase's PURCHASE day as well as its posting day, and it corrects only the day the bank CONTRADICTS.** A purchase carries two clocks -- `purchased_on`, the day it was made, and `settled_on`, the day the bank took the money -- and R-FV ruled only the second. **What forced the first is that the step below it was measured to be building a duplicate machine**: lines the matcher leaves unexplained include ones at an exact amount and merchant of an app purchase the screen ALSO lists as unexplained, not offered because `entry_service` correctly refuses a posting day earlier than the purchase day -- so the create-a-purchase door would have invited the owner to record each of them a SECOND time. The app HAS these rows, and what is wrong is the day. **The day it writes is the one the bank STATED, not the one it cleared on**: SECU states the swipe day inside a card line's description (`DATE 08-13`) and the adapter had been copying `posted_on` into `transaction_on` on every line, so the column could not distinguish an observation from a restatement. That column is now NULLABLE and the NULL means *this source states none*. **Rejected: taking the bank's day unconditionally**, the symmetric-sounding rule, which is measurably worse -- it replaces dates the owner got right to fix the few they got wrong, many of them onto a CLEARING day because the source states no swipe day at all. Correcting only what is REFUTED moves only the impossibilities. **Also rejected: relaxing `ck_transaction_entries_settled_not_before_purchase`** -- money cannot leave an account before it is spent, and the constraint is what makes the correction necessary rather than optional. **AMENDED the same day by three adversarial reviews.** (1) Making the pairing LEGAL removed the only bound an undated purchase had: `DAY_WINDOW` is measured from `settled_on` and such a row has none, so the app offered to re-date a purchase by weeks on an exact-amount coincidence, overwriting the one fact that would have exposed the mis-pairing. A purchase is never truly undated -- it is anchored on the day it was MADE. (2) What REFUTES a purchase day is the EARLIEST line of a match, not the latest: money cannot leave before it is spent, so testing against the latest leaves an impossibility standing that `update_entry` could not catch either. (3) The day is read from the bank's DESCRIPTION cell rather than the `Description \| Memo` text the row stores, so a user's own memo cannot state it |
| bank_import | R-FX | -- | 2026-08-19 (developer) | **A bank line the app has no row for becomes a PURCHASE, never a bare transaction, and the app may CREATE the budget line that holds it.** R-FS's third shape said "against an envelope the owner picks", and measurement made that insufficient: lines fall in periods whose every envelope closed at a stored figure, and several merchants have no envelope in any period. A purchase is the app's record of one payment and a transaction is a budget line that reserves, so recording a payment as a budget line would collapse two facts a purchase keeps apart and leave the next statement line for that merchant nowhere to go -- which is why the created container is always an ENVELOPE, born Projected budgeting `0.00` and closed from its own entries. **Also ruled, and it is the money half:** a settled row admits a NEW purchase exactly when its recorded figure IS its purchases AND that purchase states the day the bank took it. On a stored-figure row the gross cannot rise and `settled_cash_leg` subtracts money it never held, an expense row publishing an inflow; on ANY settled row an undated purchase moves the row's own leg on the day the row CLOSED, a past day with no evidence. `EntryDetails` therefore carries `settled_on`, which the create door had deliberately excluded on a premise true only of the hand-typed form. **It does NOT answer the carry-forward double count**: every carried-forward source settles on the admitted basis, and **N-249** already records that with the developer's 2026-08-12 remedy (reconcile the rollover), which explicitly REJECTED refusing late purchases -- so this ruling applies a decision already taken rather than making a new one. **Rejected:** refusing the add and sending the owner through revert / add / re-close, which rewrites the envelope's settle day and its statement link to record the bank's own evidence. **Amended 2026-08-24 by R-GJ**: a line matching a card-payment signature has NO create arm at all |
| bank_import | R-FZ | -- | 2026-08-19 (developer) | **A whole reviewed pass is ONE request, and four decisions are what keep it a REVIEW rather than a sweep.** **(a) A refused item leaves nothing behind and the rest still land**, each refusal quoted in the service's own sentence -- so one `$0.05` payroll gap (**N-239**) cannot cost 123 good corrections; measured, 5 of the developer's own 124 proposals refuse today on a settled credit-card payback whose figure has drifted (**N-323**). Each item is its own SAVEPOINT; the request is still the transaction, and a `PostingError` still fails the whole thing loud. **(b) The destination select IS the tick**, defaulting to *leave this line alone*: it used to default to the first envelope in the line's pay period, which has already CLOSED at a fixed figure on 78 of the 91 lines, and its category select to whichever category sorts first ("Auto: Property Tax"). One press per line hid that; one press for forty would not. **(c) Proposals are swept per CLASS, never as one "tick all"** -- they partition into 27 that only confirm a day the app already had, 46 that move one it got wrong and 51 that mark a row as having happened, and the riskiest class may not ride the same click as the safest. **(d) Where two ticked items collide over one envelope the PROPOSAL wins** and the recorded line is refused, because a proposal explains money the records already hold against a line the bank showed where the line can be re-aimed next pass -- 4 envelopes and 15 of the 91 lines, on the developer's own statement. **The outcome is rendered on the screen rather than flashed**: a flash rides in the signed session cookie, the longest sentence a batch item can carry is 497 bytes, and a browser stores 4 KB. **Amended 2026-08-24 by R-GH**: the four decisions survive on the exception queue only; a rule-covered NEW swipe no longer rides a reviewed pass. |
| bank_import | R-GA | -- | 2026-08-19 (developer) | **Where a merchant's spending goes is a DECISION the owner STATES, stored, and the app reads only to SUGGEST.** The unexplained outflows on a statement are a few times fewer MERCHANTS than lines, so the screen was asking the same question many times over. **Four things were decided.** **(a) The merchant is a NULLABLE COLUMN the adapter records**, not a token a reader parses: a rule that MATCHES on it is a stronger claim than a reader can carry, because a reader has to be TOTAL and SECU's own OFX truncates most descriptions to the identical 32 characters, so one policy would fire on every merchant behind them. NULL means *this source names none* and keys nothing, which is the direction a missing fact has to fail in; it is R-FW's shape for `transaction_on` on a second column. **(b) A policy is STORED, not derived from where that merchant's lines went before**, on three grounds: past placement is EVIDENCE and the next one is a DECISION; a derivation moves when history is edited; and a derivation cannot express *never a purchase* at all, because a line left alone leaves no trace. **(c) An answer names a TEMPLATE, a NEW ENVELOPE (name + category), or NEVER**, and those three are the complete set rather than a menu: a budget line either has a period-independent identity or it does not. It may not name a `transactions` row -- an envelope belongs to ONE pay period and a merchant's lines fall in many -- and it may not name the NAME, because one template generated rows under two different names. **A policy is restated or withdrawn freely and is expected to be**: when the credit-card arc gives a card its own account the Checking-side answer stops being right, which is why the key carries the account. **(d) A policy SUGGESTS and never TICKS.** Ruling **R-FZ** made the destination select the tick and its default *leave this line alone*; a remembered destination arriving already selected is still a default nobody chose. So the placement is rendered beside the control, one sweep sets them (the shape R-FZ(c)'s per-class sweep already has), and the policy section is its OWN form posting to its OWN door because it moves no money. **R-FZ's undo-based half of that reason is spent as of 2026-08-24 (R-GG) and the decision is not.** **Also ruled, and it is finding N-325**: a line the bank dates MADE after it POSTED is not offered as a purchase and is COUNTED on `ReviewBounds` like every other bound this screen states. Rejected: clamping the purchase day to the earlier of the two, which decides which day the app believes when the bank contradicts itself -- the substitution **R-FW** refused one clock over. **Amended 2026-08-24 by R-GI**: a stated answer is a standing RULE that auto-applies to NEW swipes under R-GH; suggest-only was measured unable to deliver the developer's stated goal |
| bank_import | R-FY | -- | 2026-08-19 (developer, on measurement) | **A row the app has never marked as paid is bounded by its OWN PAY PERIOD, widened by `DAY_WINDOW` at each end -- not by the statement's covered span, and not by nothing.** Such a row carries no observation of when its money moved, and that was read as *no bound at all*: any bank line sharing its amount could claim it, from any date whatever, so a projection budgeted months later would settle against this month's line and file real money under the wrong paycheck. The pay period is what the app itself asserts about when that money moves, and it is the WHOLE of what it asserts, so both ends travel. **Rejected: the statement's covered span** (**N-312**'s proposal) -- a property of how much history the owner happened to export rather than of the money, so the same two rows match or do not depending on the file. **Also rejected: the earlier reasoning that a bill must stay unbounded**, which held that bounding it would refuse the arm that settles a row nobody has marked as having happened: every row that arm settles is a PURCHASE, already bounded by the day it was made. **Also ruled: the clause that hides an already-matched envelope from the create door's destination list STAYS WHOLE.** **N-317** was right that it is wider than the money needs, and what this decides is only that a `$0.00` benefit does not buy a change to a money guard. **What must NOT be narrowed with it** is `_accept._reject_parent_and_its_own_purchase`: stamping an EXISTING unposted purchase whose parent is matched DOES move that parent's leg, a different act. **AMENDED 2026-08-20 (developer): N-317 is RETIRED and the clause still stays whole.** What it records is a DECISION -- a guard deliberately wider than the money needs, with `destinations_for`'s docstring carrying the argument -- rather than work owed. Keeping open a row nobody will ever act on weakens conventions rule 1 for every row beside it, and the honest way to retire one a ruling holds open is to amend the RULING rather than let a step close it around the ruling's back. No code changes with this. **And the bound is on what the matcher OFFERS, never on what the accept door WRITES**: the hand-build form exists so an owner may assert a grouping the proposer would not guess, and a date bound there would refuse the one act **R-FP** reserves to them |
| bank_import | R-GB | -- | 2026-08-20 (developer) | **An import can be UNDONE, and the undo is BALANCE-NEUTRAL by construction.** Finding **N-302** measured the dead end: no door in `app/` deleted an import, a line or a recorded account pairing, so a single refusal ended that account's ability to import for good while the message promised a human repair the app could not perform. Deleting removes what the BANK said; a settle day an accepted match wrote is the APP's own record and stays, which is `release_match`'s own rule and the reason the act moves no money. **Three decisions, each with its rejected alternative.** (1) A match naming a deleted line is RELEASED, through the one door that releases a match -- rejected: refusing the delete until the owner undoes them by hand, which reaches the same end state through a chore, and a match spanning two imports would block deleting either. (2) The source-account pairing is FORGOTTEN with the LAST import from that source, because it is a fact LEARNED from an import and should not outlive every import that taught it -- which is what makes a first import that named the wrong Shekel account repairable at all. Rejected: a separate *forget this pairing* control, which is stricter but adds a second door and a state nothing else produces, a pairing with no import behind it. (3) `fk_statement_match_members_line_account` becomes NO ACTION, so the DATABASE refuses to orphan a match. **That asymmetry is the point**: losing app ROWS is visible on the review screen and those keys still CASCADE, where losing bank LINES was invisible and permanent -- an act left standing with no line members claims its transactions forever, with no screen able to render it and no button able to free it. NO ACTION rather than RESTRICT because RESTRICT is checked per row where NO ACTION defers to the end of the statement, and a whole-account delete cascades matches, members, imports and lines inside ONE statement. **AMENDED 2026-08-24 by R-GG, and the amended term is the headline**: the undo is balance-neutral EXCEPT for the rows the REVIEW created from these lines, which go with them. A settle day a match wrote is still the app's own record and still stays; a purchase that exists only because a bank line did is not, and destroying the line while keeping it leaves a movement in the books that nothing accounts for. The receipt and the confirmation both name the count and the figure, from the release door's own derivation. **Also ruled: the review-screen divergence resolver is REJECTED**, because with **R-FU** amended the only conflict that survives is a real restatement, and a screen for negotiating one is machinery for an event measured 0 times |
| bank_import | R-GC | -- | 2026-08-20 (developer) | **A NEW-ENVELOPE merchant answer converges on ONE envelope per pay period, and the convergence is a SUGGESTION.** Finding **N-327**: the answer minted one envelope PER LINE, so a policy fragmented the very budget line it names -- one press could make several envelopes with two in the same period, and the next statement made more beside them. No figure was wrong; what fragmented was the budget. **Two halves, because they are two problems.** ACROSS statements the SUGGESTION converges: the placement resolves to a `RECORD_IN` against an envelope of that name already in the line's own period, and TWO of that name is a guess so it is REPORTED -- the same rule, and the same sentence shape, a template answer already takes for a template that generated two rows in one period. WITHIN one press the WRITE converges, through a per-REQUEST registry, because at render time the envelope the press is about to create does not exist for any select to name. **The per-line select is untouched and still opens on *leave this line alone*** (**R-FZ**, **R-GA**), so two purchases from one merchant may still go to two different envelopes, and the screen prints which line CREATES and which JOINS before the press rather than the receipt saying it after. **It moves no money**: each purchase carries its own posting day and is its own cash movement, leaving an envelope's own leg at `0.00` either way. Rejected: converging silently inside the create door, which would take away a deliberate second envelope of one name and is the substitution every other arm of this screen refuses; and converging on the NAME alone, which would file a March swipe into an April budget line. **What it does NOT fix is finding N-328**, the reason the convergence has to compare a string at all: an ad-hoc envelope carries no identity across periods, and `X-f6c` is where the answer names a TEMPLATE instead |
| bank_import | R-GE | -- | 2026-08-22 (developer) | **A bank statement's evidence justifies CORRECTING a settled purchase's amount, and it reaches that row from a MATCH and from nowhere else.** `entry_service._reject_settled_parent` refuses `amount` on a settled parent (finding **N-229**, widened to the settled BAND at `balance:X-au-c3`) because re-pricing a row whose money has moved would move it again -- and that reason holds for the act it was written about, a human typing a different number on their own second thoughts. **A bank line is not that act**, which is the argument **R-FX** already accepted one door over when it ruled that the same evidence justifies ADDING a purchase to a settled row: *adding one on a row whose figure IS its purchases raises that cost by exactly the figure a bank statement just showed*. Correcting one lowers or raises it by exactly the same kind of figure, from the same source. **The bound is the DOOR, not the row**: the ordinary edit form still refuses, so N-229 is unchanged everywhere a human supplies the number, and the permission exists only where a statement does. Measured: 2 of the developer's 10 near misses are purchases under a settled envelope (`Groceries: Walmart`, `$121.12` against the bank's `$121.16`, and `$192.24` against `$192.39`). **Rejected: recording the match and leaving the amount**, which leaves the row disagreeing with the bank about a fact the bank STATES and makes `X-f6e`'s identity fail by exactly that residual -- a variance nobody can attribute, invented where the bank has already said which row is wrong. **Also rejected: refusing outright**, which forces the proposer to withhold those two, and a line the screen will not explain is the line the merchant policy offers to RECORD -- the duplicate **N-335** measures |
| bank_import | R-GD | -- | 2026-08-22 (developer, on measurement) | **The bank's figure is the RECORD, so a match may DISAGREE with the app and the variance is the PRODUCT rather than a refusal.** **(a) A match RECORDS the variance**: the bank's figure becomes the settled figure, the difference is stored and reported, and the door never sends the owner away to retype a number the statement already carries. **R-FV's refusal is INVERTED** -- the payroll residue it refused to silence (**N-239**) becomes named rows on a variance report, and the refusal was never neutral: it is what made recording a duplicate the cheaper act. **(b) The predicate is a SCORE, never a TOLERANCE**, and a blanket tolerance is REJECTED. **The merchant is a REQUIREMENT for a near miss rather than a tie-break** (amended 2026-08-22), matched as a WHOLE WORD, and it stays corroboration for a WEAK claim only -- the exact tier may not require it. **The bound is 0.50% of the line**, RELATIVE, because three cents is 0.017% of a `$178.29` line and 15% of an `$18.64` one. **The RANK is WITHDRAWN** (amended again the same day): amount and day are both incapable of choosing, so a near miss is offered ONLY where it is the sole candidate on BOTH sides, and a contest is COUNTED and named on screen. **A REVIEWED line gets no candidate list either** (amended a third time, 2026-08-23) -- there is no bound at which one is anything but noise. **Nothing scores a GROUP.** Rejected: a separation threshold between best and runner-up, which is the tolerance this ruling rejects under another name. An accepted repricing needs its own count, or the receipt says *"Nothing moved."* over a rewritten figure. **(a)'s variance gets its own SWEEP CLASS**: repricing is the only act on that card that changes what money was SPENT, and **R-FZ(c)** refuses to let the riskiest class ride the safest click. **(c) is SUPERSEDED 2026-08-23 by R-GF**, both halves of it measured false. **(d) An account with statement coverage reconciles from STATEMENTS ONLY**: the panel and the review screen answered one question with two grades of evidence, the panel stays for an account nobody imports, and it owes its own specification pass. **THE GROUP** (amended a fourth time, 2026-08-23). **(i) The difference is an ordinary row the owner ACCEPTS** -- **R-FN** applied here rather than a new rule: no category, so it books to the per-owner `Uncategorized Income` / `Uncategorized Expense` through the existing category-leg rule, settled on the bank's day, categorisable later, and it becomes a MEMBER so `Sigma(lines) = Sigma(members)` holds BY CONSTRUCTION. **The FIGURE travels and the door reconciles it**: the consent box carries what the screen showed, because the SUM is the one number no per-row guard can see. Rejected: a bare consent checkbox carrying no figure, and recording it automatically, which R-FN itself refuses. **(ii) One ROW is determinate however many LINES explain it**, so `bank_cash_for`'s test is on the row rather than the lines. **(iii) The row is named `Statement difference: <merchant>`**, what it is before who paid it. **(iv) The running total is SERVER-computed**, one derivation shared by the screen and the door -- no JavaScript sum, and no implicit `ROUND_HALF_EVEN` on the one field the design says is exact. **(v) UNDO removes what the act CREATED**, so a row the owner has since edited refuses it; its mechanism is SUPERSEDED 2026-08-24 by **R-GG(b)**. **Rejected: widening the residual to the other refusals** -- a 1:1 whose row's figure is not its own stays refused. **The sign check applies to a group** and mixed signs INSIDE one stay legal; the not-its-own-figure and transfer-shadow census is asked of EVERY member, a sum past `Numeric(12, 2)` is a refusal, and a ZERO side opposes nothing |
| bank_import | R-GF | -- | 2026-08-23 (developer, on measurement) | **A statement's stated balance is an OBSERVATION whose EFFECTIVE DAY the lines solve, and the app records the day and how it was pinned rather than storing two balances derived from it.** SUPERSEDES **R-GD(c)**'s source and its refusal, both measured false. **The source**: no OFX adapter is built -- the two formats state the identical figure, and the CSV already carries the balance, the merchant, the bank's category and the `Totals:` self-check. **The day**: given a known opening, the stated figure is effective at day `d` iff `stated - Sigma(lines posted on or before d) == opening`, over the candidates {the day before the first line} + {every day the file covers}. Two hits differ only by lines netting zero, so take the latest, and a refusal is reserved for a file where NO day solves. **The known opening is a TOTAL DISPATCH naming three BASES**: `line_chain` (the file states a balance beside every line), `reconciled` (the recorded history determines it and exactly one day fits), `assumed_last_day` (nothing constrains it -- a first import -- so the figure is the balance after the last line, and the receipt SAYS SO, because an assumed anchor propagates to every later import and only a constant offset betrays it). **Two derived columns are DROPPED**: `opening_balance` and `closing_balance` are `stated - Sigma` and `opening + Sigma`, stored beside their source with nothing reconciling them. **The app-vs-bank comparison is an INSTRUMENT, never a gate** -- a refusal deadlocks, because the import is what fixes the disagreement and a first import has matched nothing; the disagreement itself is **N-314**, owned by `balance:X-f3c`. Rejected: gating on the closing day alone, which still refuses every first import. **AMENDED the same day by three adversarial reviews.** **(i) The basis is the WEAKEST LINK**: a solved day is only as good as the opening it was solved against, so there is ONE fact rather than two and the column records the minimum over the chain -- `file_chain` / `corroborated` / `uncorroborated` -- and no re-upload can strengthen anything. **(ii) The doors that change LINES release the anchors they undercut**, keyed on the days whose lines actually moved, so a delete removing 0 lines releases nothing. **(iii) The ONLY refusal is a file contradicting ITSELF**: only the file's own running-balance chain can convict the file, never a mismatch against recorded history. **(iv) Both the solve and the assumed day are BOUNDED by the day the header names**, or the door writes a row `ck_statement_imports_effective_day_within_file` rejects. **(v) The anchor to walk from is the STRONGEST, not the most recent**, and the ladder is read from the enum rather than from ref-table row ids. Also: the first anchor an account gets is still an assumption every later import inherits (**N-342**) |
| bank_import | R-GH | -- | 2026-08-24 (developer) | **Consent splits by ACT CLASS, and a standing rule is consent** (amends **R-FP**'s a-match-is-a-proposal half, **R-FZ**'s four decisions and **R-GA**). An act that CREATES a row from a NEW bank swipe under a rule the owner stated AUTO-APPLIES at import, itemised on a receipt whose every item carries the one-click undo R-GG built; an act that MODIFIES a row the owner made by hand -- re-date, re-price, settle, group-match -- remains a proposal needing its tick. Rejected: full auto over modifications, because a re-price of a hand-entered figure is a correction of the OWNER and review is the point; and keeping consent-per-line, measured unable to deliver the developer's stated goal (Food Lion's fortieth swipe needed the same human act as its first). Argument and worked example: `docs/audits/bank_import_redesign/README.md` P-1. |
| bank_import | R-GI | -- | 2026-08-24 (developer) | **A stated merchant answer is a standing RULE, scoped and revocable** (amends **R-GA**; the identity half is `X-f6c`'s template ruling unchanged). The answer set is {existing template, new template first time, always ask, never a purchase}; a rule applies only to NEW swipe lines, is revocable where it was stated, and every application is receipted. Stating one still moves no money -- the money moves at IMPORT time, under R-GH. Argument: README P-2. |
| bank_import | R-GJ | -- | 2026-08-24 (developer) | **A line matching a card-payment signature can NEVER become a purchase** (amends **R-FX**). Its only arms are a group match to payback rows with the difference NAMED, or PARK as a card payment awaiting its home -- which is the card arc's ruled transfer-shadow model (**N-337**, owner `CC3b`), so this is an interim GUARD that closes nothing of that row. Argument: README P-3. **Amended 2026-08-24 (developer, on measurement), and the amendment is what the SIGNATURE turned out to be.** It is the OWNER's own answer plus an OBSERVATION, and neither is a question. *Never a purchase* -- `merchant_destinations`' third answer -- stops being a CAPTION and becomes a structural bar: it had withheld a sweep value while the line's own destination select went on offering every envelope in the period and `create_purchase_from_line` read no policy at all, so the screen printed *nothing here records it* directly above a control that did. And a line a SOURCE files as a payment to an ACCOUNT THE OWNER HOLDS has no create arm at all -- **no answer lifts it**, and `state_policies` refuses a template or new-envelope answer for such a merchant outright so a contradicting answer cannot sit stored and inert. **A first build made that second bar an UNANSWERED state any answer lifted, and two adversarial reviews measured the hole**: the answer that lifts it is *a new envelope*, which is the answer the developer had saved for the card and the one that booked every card payment through a single sweep click. The permissive arm's reasoning -- that the source sometimes files a LOAN payment under this signature -- was imprecision about WHICH account rather than error: the app acts on *is this spending* and the answer is NO either way, so only the WORDING was wrong and the wording is what was fixed. **The known cost, stated:** genuine spending a source ever files under one of these categories has no create arm and no answer opens one, which is the deliberate trade against a measured one-click double count. **From scratch there is no card-payment code in this layer at all** -- paying a card is a transfer, the checking line matches the transfer's shadow 1:1 on the exact tier, and the second bar deletes whole when `CC3b` ships; the first is permanent. Shipped at `X-ga` (`73a36cf4`) |
| bank_import | R-GK | -- | 2026-08-24 (developer) | **An envelope FILLS from the bank.** It stays open through its period, rule-assigned purchases flow in dated by the bank, and it closes when the period's statement coverage is complete (`balance:X-f3a-2`'s walked statements) or when the owner closes it by hand; hand entries stay for cash and judgment envelopes. The owner's settle-early habit moves -- his own decision, 2026-08-24. Details are OWED to the `X-gg` design loop: carry-forward, the settle machine and the grid's chip language all touch this. Argument and worked example: README P-4. |
| bank_import | R-GL | -- | 2026-08-24 (developer) | **A confirmed import's anchor may ASSERT the opening when the day's residue is zero**, and a nonzero residue surfaces as the exception it is; the hand true-up door stays, demoted from habit to correction. Sequenced AFTER `balance:X-f3c`, because the ruling must be written against the post-cutover assertion rather than the one `X-f4` deletes. Measured ground: 60 hand true-ups on Checking, the last equal to the bank's own closing to the cent. Argument: README P-5. |
| bank_import | R-GG | -- | 2026-08-24 (developer, on measurement) | **An act that CREATES a row owes an inverse that REMOVES it, and what an act made is a different relation from what it names.** Findings **N-333** and **N-340**. **Four decisions.** **(a) The undo removes what the act CREATED, and a row the owner has EDITED since REFUSES it** -- the rule `X-f6d-4` applies to a group's residual, widened to the arm left out of it. The predicate is the subject's own `version_id`, because *still uncategorised and still at the figure we wrote and still holding no purchases* is three guesses about which edits matter where a counter that moves on every ORM update is the fact itself. **(b) What an act CREATED is its OWN relation**, `budget.statement_match_creations`, and `statement_match_members.created_version_id` is DROPPED: a column on the membership can only mark a subject the act also NAMES, and the create-a-purchase arm makes two things while the CONTAINER may not be a member -- naming an envelope beside its own purchase counts the same money twice (**R-FM**) and breaks `Sigma(lines) = Sigma(members)`. **A SUBJECT and a CONTAINER are told apart by MEMBERSHIP rather than by a second column**, which cannot drift from the members that define it. **(c) A container is removed only when it is now EMPTY and untouched, and it NEVER refuses.** A purchase is what the act is about, so an edited one stops the undo; a budget line is not, and one left standing budgets `0.00`, holds nothing and books nothing. A multi-line envelope survives every undo, empty, because each later line re-closes it -- accepted, because the alternative is enumerating which writes are the pass's own, the guess a revision counter exists to replace. **(d) N-229's REMOVAL half is corrected at the ROOT rather than bypassed**, in `entry_service._reject_settled_removal`, `_reject_settled_addition`'s mirror: a purchase that is CREDIT or POSTED sits in the settled figure AND in a subtracted term, so removing it moves both by the same amount. An UNPOSTED purchase's removal re-prices the close on a past day with no evidence and stays refused, as do the archive and a stored-figure settlement (**R-FX**'s reasons). **A door that can only go one way is the defect**: the ADD was already admitted by R-FX and the REMOVE was not. **(e) Deleting an IMPORT takes the rows the review created from its lines, which AMENDS R-GB**: that undo is balance-neutral EXCEPT for what the review created, because a row that exists only because a line did states nothing once the line is destroyed. Both the confirmation and the receipt name the count and the figure, from the release door's own derivation (`planned_removals`) rather than a cheaper estimate -- a confirmation that counted differently from the act it confirms would be a confirmation that lies |
| bank_import | R-GM | -- | 2026-08-25 (developer) | **A row LEAVING the books withdraws the accepted matches it was the last app row of**, so those bank lines are unexplained again in the same request rather than waiting for the owner to notice an amber act on the review screen. Every door an owner presses asks one rule (`app/services/match_withdrawal.py`): the transaction delete verb, the purchase door, both CC-payback teardowns and the transfer delete. **Narrowed on measurement**: withdrawing on the loss of ANY member destroyed a group act that was still two-thirds true and silently un-matched its survivors, so a PARTIAL loss stays `AcceptedGroup.agrees`' case -- the two mechanisms split on a predicate instead of shadowing each other. A SOFT delete withdraws nothing: the member survives a flag change and `restore_transfer` puts the rows back. Rows a withdrawn act CREATED are LEFT standing and COUNTED, because deleting one row is not withdrawing a decision -- `release_match` stays the owner's undo and the only door that destroys what an act made. **The INVARIANT is not the call sites**: `matched_subjects` and `_spoken_for` stopped counting a line as explained while its act names no app row (`_candidates.act_still_names_a_row`), because four more doors reach that state in bulk SQL and a rule enforced by enumeration is one the next door forgets. **Ruled on a CORRECTED premise**: the recommendation put to the developer said the review screen did not catch this at all, which was measured FALSE against `_still_holds` and `TestAnAcceptedMatchStopsAgreeingWhenItStopsHolding`; the correction was put back to him and the automatic withdrawal was chosen anyway, on the ground that a deliberate delete leaves nothing to re-review. Shipped at `X-gb` (`ec346c46`). |
| bank_import | R-GN | -- | 2026-08-25 (developer) | **An import's receipt warns when the app GUESSED which day a stated balance belongs to, and not when the corroboration LEVEL is merely weak.** The two were one signal and the evidence ladder cannot separate them: `uncorroborated` is minted BOTH by a solve against an unconfirmed opening -- where the day is proven -- and by the arm that has nothing to solve against and takes the file's last line. So the receipt warned on every import the owner could perform, which is what teaches a reader to dismiss the warnings that matter, while a guessed placement and a proven one read identically. `ImportedBalance.day_is_solved` carries the missing fact, stated by each arm of `resolve_anchor` rather than derived (an assumed day is `min(period_end, stated_balance_on)`, and a solved day can land there by coincidence, so no reader can recover it from the stored columns -- which is why the imports TABLE still cannot say it and a column for it is not taken here). **Ruled on a CORRECTED premise, and the correction is the point.** The recommendation first put to the developer said `uncorroborated` was the only rung his account could ever reach; that was measured FALSE the same day -- his 2026-07-19 export carries `Running Balance`, reaches `file_chain` when imported (306 lines, `$2,229.73` at 07-17), and a statement opening 07-18 then reads `corroborated`. Measured cost of the ruled arm on his real books: **zero** -- one contiguous recorded run 2026-01-02..2026-08-21, so his next import's day is SOLVED and reports green; it fires only on a genuine gap, clears by importing the missing span, and never depends on the dropped column returning. Shipped at `X-gc`. |
| bank_import | R-GO | -- | 2026-08-25 (developer) | **The review panel MARKS only what the app can prove the bank never shows alone, and never guesses.** A row whose figure is not its own to state -- a CC payback, an envelope whose figure is its purchases -- is one the bank accounts for through some other row, and the caption's *a payment your records claim happened and your bank did not make* is false of it. Those rows are ANNOTATED and never withheld: the same card is the hand-build form's row-picker, and **R-GJ** leaves the group match as a parked card payment's only ACT, so dropping the 18 CC Paybacks would have closed the one path that ruling kept open -- which is what this step's own plan sentence, read literally, said to do. **The audit's two named classes are deliberately NOT delivered and this records why**: the 7 envelope-tracked containers holding no entries (`Groceries` `$163.95` .. `Mint Mobile` `$132.69`) and the 10 payroll allowance rows carry no fact saying the bank showed them inside something else, and the only predicates that would claim them -- a row-name list, or `tracks_purchases`, which would silence the alarm on a `Mint Mobile` that is plainly one bank line -- fail in the one direction this caveat may not. They stay under a caption that no longer over-claims of them; being buried among 67 rows is `X-gf`'s. Shipped at `X-gc`. |
| bank_import | R-GP | -- | 2026-08-25 (developer) | **The destructive-action dialog binds before the DOM it guards.** The listener turning `data-confirm` / `hx-confirm` into the shared modal sat at the bottom of `app.js`, loaded after the whole document, so all 22 armed controls across 17 templates were clickable before their guard existed -- and finding **N-345** records an Undo submitted in that window, which ran with no dialog shown on a 537 KB page. It moves to `app/static/js/confirm.js`, loaded as a classic script in `<head>` and ABOVE the stylesheets (a script following pending `<link rel=stylesheet>` waits on all of them), so no element carrying a confirm attribute can be parsed before the listener exists. Two consequences stated: the htmx half binds on `document` rather than `document.body`, which does not exist yet and which htmx 2.0.4 reaches anyway (`bubbles: true`); and `ask()` falls back to `window.confirm` when Bootstrap has not loaded, which is fail-closed -- the act still cannot happen without an answer. Shipped at `X-gc`. |
| bank_import | R-GQ | -- | 2026-08-25 (developer) | **The undo's legacy disclosure gap is DEV DATA and is repaired by re-cloning, never by a schema flag.** `planned_removals` short-circuits on an act carrying no creation record, so such an act renders an Undo with no `data-confirm` at all -- measured on the rendered page, **221 Undo forms, 0 with a dialog**. The CODE is correct: `_accept._record` is the one writer of `StatementMatchCreation` and both creating callers pass what they made, so where every act post-dates that relation an empty list means *this act created nothing*, exactly. What is not correct is the dev database: **218 of 221 acts pre-date it, and 91 purchases worth `$4,013.69` were created inside them** (each entry born within 5 seconds of the act naming it, several with their envelopes minted at the same instant). A `creations_recorded` column would read TRUE on every row production ever writes -- production holds 0 acts -- so it would exist only to describe those 218. **Ruled: no column, no interim copy; clone dev from production and re-import, as its own coordinated session.** Until then those acts can orphan their purchase silently, which is the exposure this ruling accepts with its size measured. Owner of the residue is `operator`, in `ledger.md` under **N-345**. |
| bank_import | R-GR | -- | 2026-08-25 (developer) | **A merchant is a ROW, not a string two tables each keep a copy of.** `budget.merchants` is account-scoped and both referrers name it through composite `(merchant_id, account_id)` keys, so three things stop being maintained: the scope check that was the ONLY thing between a crafted body and an answer keyed on a merchant this account never saw becomes a SENTENCE (`fk_merchant_destinations_merchant_account` is the guard); *which merchants may be asked about* stops being a UNION of two derivations, because an ANSWERED merchant outlives its lines; and one width is stated once instead of twice. The wire posts the row, so the field is a `RowId` rather than unvalidated bank text. **Ruled as the deeper arm of a fork put with a narrower one**: the recommendation was to leave the merchant a string and keep the fence as a courtesy, and the developer took the table. **A merchant is NOT immortal** -- `delete_import` sweeps this account's merchants with no surviving line AND no stated answer, because without that the table has no ceiling at all (upload N unseen merchants, delete the import, keep them), which is the retired check's own hazard moved one table over; an adversarial security review measured it. Whether two SOURCES ever name ONE merchant is `X-f6b`'s and opens when a second source exists. Shipped at `X-gd-1` (`395b14f7`). |
| bank_import | R-GS | -- | 2026-08-25 (developer) | **The rule store's four answers are told apart by its COLUMNS plus one boolean, and *always ask* replaces withdrawal.** A stored discriminator was rejected on two grounds: it is a second statement of what the columns already say (which is `PolicyAnswer.of`'s own argument), and a CHECK cannot reference a `ref` row's id -- so a ref-keyed answer could sit on a row whose columns contradict it, on the sole-writer trust contract `ledger_accounts.kind_id` accepts and documents. The boolean shape is also the one that SURVIVES `X-f6c` collapsing the two container columns into a template id, where a ref table would need a data migration. **A stated answer is never UN-stated**: the control's *ask me every time* is a decision that stops the screen prompting for a rule, and there is no delete -- so a rule row, once made, is only ever restated. Shipped at `X-gd-2` (`d1910c95`), which also carried the RENAME: `budget.merchant_destinations` is `budget.merchant_rules` (`e7b597da`), so every `merchant_destinations` name an earlier ruling here cites reads as its `merchant_rules` counterpart. **Measured across the migration, row by row on a clone of the developer's own database**: all 29 stored answers read back identical, `Capital One Credit Card` included, which is the one whose meaning the backfill had to carry. |
| bank_import | R-GT | -- | 2026-08-25 (developer) | **A match act records WHETHER a standing rule performed it, as a flag on the ACT.** Which rule stays DERIVABLE -- the matched line carries the account and the merchant, which is exactly the rule's key -- so a foreign key to the rule row would store a pointer the line already determines, and would force a choice none of whose arms is right: cascade deletes money records, set-null claims the owner ticked it, restrict refuses to withdraw a rule that ever fired (the **N-302** dead end). What is NOT derivable, and is therefore the whole of what is stored, is that a rule acted rather than a tick: a person may tick a line whose merchant has a rule. Shipped at `X-gd-2` (`d1910c95`) as `budget.statement_matches.applied_by_rule`, NOT NULL with no default so the writer states it or the flush refuses. **Every one of the 221 acts on the developer's dev database is `false`, and nothing can write `true` until `X-ge`** -- the column ships one leaf ahead of that door so the step which MOVES MONEY carries no schema change. |
| bank_import | R-GU | -- | 2026-08-26 (developer, on measurement) | **WHAT a standing rule may file, and what it must refuse.** Three decisions. **(a) A rule FILES INTO AN ENVELOPE THAT HAS ALREADY CLOSED**, because the owner settles ahead of the bank -- a door refusing this would file nothing for the recurring envelopes the step exists for. Ruling **R-FX** already admits it on exactly the terms `destinations_for` enforces: the row's recorded figure IS its purchases, so a new one raises that cost by exactly the figure the bank showed and carries the bank's own posting day. **(b) A *new envelope* answer FILES TOO**, minting the container, rather than waiting for `X-f6c` to give that answer a template identity. A rule reaches NEW swipe lines only (**R-GI**), so a fortnightly import touches one period and `MintedEnvelopes` mints one per answer in it. **(c) A rule files only where the pass POSITIVELY finished looking.** Membership of the creatable set is defined by SUBTRACTION -- *no proposal claimed this line* -- which under a human tick is checked by the person reading the screen and under auto-apply is checked by nobody. So the bounds a pass publishes about its own search WITHHOLD the line, and the receipt says how many and why (`ReviewSet.search_gap_for`): a near miss the tier admitted and declined to choose between (**N-335**), a day the group search skipped as too crowded, and a row the amount model could not price at all. **A pass must report EVERY bound it applies, not every bound it published**, which is the correction two adversarial reviews forced: the first build read the published bounds and called that enumeration complete, and it was measured false. Each tier reports its own refusals now, in the module that owns the predicate (`X-ge-1`); nothing re-derives `NEAR_MISS_BOUND` or `DAY_WINDOW` outside its own module (**N-322**), and a tier added later must report or it is not a bound. **Also ruled, and it is R-FZ(d) from the other side**: a destination this statement PROPOSES as a whole-row match is not open to a rule, because auto-apply files BEFORE the proposal is ticked, so *the proposal wins* has to be reached by withholding rather than by ordering. Rejected for (a): refusing a closed envelope. Rejected for (c): filing whatever the review screen would offer a create control for, which makes *nothing matched* mean two things the app cannot tell apart. Shipped at `X-ge` |
| bank_import | R-GW | -- | 2026-08-27 (developer) | **`balance:R-GW` EXISTS TOO and both are kept** (rule 10): the cutover's order-reversal ruling was minted the same day on an unmerged branch, which a `docs/` grep cannot see. Every citation this ruling carries from `app/` and `tests/` names its arc for that reason. **A bank line the app has no row for becomes a row IN ITS OWN DIRECTION: an outflow a PURCHASE (**R-FX**), an inflow an uncategorized INCOME row.** Only the outflow half had ever existed, and the two refusals that bound it POINTED AT EACH OTHER -- `_accept._reject_empty_side` sent a line with no app row to the purchase door, `_create._load_line` sent an inflow back to matching -- so an inflow the books hold no row for sat inside that loop with no act at all, and the review badge could never reach zero. **The WRITER already exists and is not rebuilt**: `_variance.mint` mints exactly this row -- `INCOME` type, NULL category so `posting_service._settled_target` books it to the per-owner Uncategorized fallback, settled through the status seam on the bank's day at the `observed` basis, owning its amount beside a NULL `amount_source_id` -- so what ships is the DOOR, and the two callers share one writer rather than two spellings of one money rule. The period is the one holding the POSTING day. **Rejected: a per-line *set aside* act, and a merchant answer meaning *not tracked*** -- both dispose of the line without recording the money, so the books stay under the bank for good and **R-GL**'s residue surfaces those days as an exception forever, which makes one counter zero by making another non-zero. **Rejected: filtering them out of the count**, the figure-disagreeing-with-its-caption shape this arc has closed twice. **R-FP is unchanged**: the control opens on *leave this line alone*, and NO rule files an inflow, because a merchant answer says where SPENDING goes and a deposit is not spending. **Scope**: what these lines have no act for is the REVIEW SCREEN; an owner could always hand-build the row on the grid and match to it, and this door makes that one tick. **THREE things the step's adversarial reviews changed.** (i) The three lists the screen splits into are NOT total -- an outflow the bank dates MADE after it POSTED reaches none of them (**N-325**, ruled *reported rather than repaired*) and is still counted, named in `_leftovers`' header and pinned by a test rather than repaired here. (ii) A line the bank dates in the FUTURE resolved a pay period, rendered a tick and was refused by **R-EJ** only AFTER the row was written and settled; both halves now ask ONE published predicate, `status_seam.day_is_in_the_future`, and the door asks it before it writes. (iii) The card's per-line safeguard is *what this pay period's books already hold* -- a FACT about the period rather than a candidate for the line -- suppressed where the deposit is smaller than the smallest such row, because no subset of positive rows can come to less than its smallest |
| bank_import | R-GX | -- | 2026-08-27 (developer) | **THE REGISTER IS NOT THE QUEUE.** The review screen shows only what is still being DECIDED; a decision already made moves to a surface of its own. Three parts, each measured on the developer's own dev data 2026-08-27 after `X-gf-1` landed: (a) the merchant control SPLITS ON WHETHER AN ANSWER EXISTS -- the queue keeps a row only for a merchant with an unexplained line and NO answer (1 today, `Public Library`, `-$5.99`), and all 29 answered merchants move, including `Capital One`, whose *never a purchase* answer parks 9 lines worth `-$7,412.94`. **R-GJ still holds where it bites**: a merchant a SOURCE files as an account payment has no answer by definition, so its row is on the QUEUE, which is the only surface that can give it one. (b) The accepted acts move WHOLE, 221 of them and 216,637 bytes. (c) The register renders every act that NO LONGER HOLDS whatever its age, plus the newest `REGISTER_LIMIT` (50) of the rest, with the remainder behind a link -- the agreement test is a VALUATION over member rows and not a query, so the fold reads every act either way and what is bounded is what is RENDERED. **Measured before**: the review body was 578,523 bytes of which those two panels were 442,109, or 76%, and the work a routine import leaves was 136,414. **After**: 149,103, and `review_set` fell from 146 SQL statements to 7 -- a cost the IMPORT path paid too, `file_new_swipes` calling the same reader. **The earlier 554,122 / 79% figure in this document's `X-gf` entry is not contradicted**: it was measured before `X-gf-1` added the deposit card. |
| bank_import | R-GY | -- | 2026-08-27 (developer) | **A PRESS THAT DESTROYS A RECORD CONFIRMS, WHETHER OR NOT IT ALSO DESTROYS A ROW.** This REVERSES the rationale `_statement_undo_macros.html` carried, which attached `data-confirm` only where an undo removed rows the act had CREATED and argued that *a confirmation on a reversible act trains the owner to click through the one that is not*. Measured 2026-08-27: **0 of the developer's 221 accepted matches are in that state**, so all 221 Undo buttons fired on one click with no dialog at all. Undoing moves no money and leaves settle days alone, and what it destroys is still a record -- which bank lines ARE which of the owner's rows, and whether a standing rule filed the act rather than a person (**R-GT**) -- recoverable only by matching them again by hand. The dialog is therefore always there and its WORDING is what varies, naming the destruction that press actually performs. **The one press with no dialog is the one that performs nothing**: an act whose undo would be REFUSED destroys nothing, and a confirmation before a refusal is the dialog-for-nothing the overturned argument was about. |
| bank_import | R-HB | -- | 2026-08-27 (developer) | **THE EXCEPTION QUEUE IS ONE LIST, GROUPED BY THE DECISION EACH LINE POSES.** The review screen partitions its unexplained bank lines by MECHANISM -- an outflow that may be recorded (`creatable`), an outflow ruling **R-GJ** bars (`parked`), an inflow that may be recorded (`recordable_inflows`, ruling **bank_import:R-GW**) -- across three cards with three headings and three bodies of explanation. That is the SERVICE's partition, and it is the right one: the three reach different doors. It is not the READER's. The question all three pose is the same -- *is this money my books already hold, or is it new?* -- and the owner answers it the same way each time. So the queue becomes one list ordered by what the decision IS, each row carrying whatever acts are open to it, and the mechanism partition stops being visible on the screen while staying load-bearing underneath it. **Measured on the developer's own data 2026-08-27**, after `X-gf-1`, `X-gf-2` and `X-gf-3a`: the three cards hold 2, 9 and 16 lines against 1 unanswered merchant, and `2 + 9 + 16 = 27` is exactly `unmatched` -- the same 27 lines the hand-build form lists again as one flat set, which is what the reader is already being asked to reconcile by eye. |
| bank_import | R-HC | -- | 2026-08-27 (developer) | **THE WORKBENCH IS NOT THE QUEUE EITHER.** The same argument ruling **R-GX** made about the register, applied to the other thing that is not a decision: the hand-build match form is the TOOL that three separate exceptions send the owner to -- a parked card payment meeting its payback rows, a line a tier declined, a deposit the books may already hold -- and it is not itself an exception. It moves to a surface of its own, and each exception links to it with its own line already ticked. **Measured through the real route on the developer's own data 2026-08-27**: the review body renders 146,907 bytes of which that form's two pick lists are **89,247, or 61%** -- 22,830 for 27 bank lines and 66,417 for 67 rows -- and BOTH are unbounded, the right one growing with the whole span the statements cover, for ever (**N-374**). **A cap is refused as the remedy**: the row that explains a line may be number 51, and a scored shortlist is what ruling **R-GD**'s third amendment already withdrew on this screen after measuring 0 of 18 inspected correct. What is bounded is the QUEUE, by the tool leaving it. |
| bank_import | R-HD | -- | 2026-08-28 (developer) | **A SWEEP MAY REACH ONLY THE GROUP THAT OFFERS IT.** Ruling **R-FZ(c)** partitioned the one-click record by where the money LANDS -- into a budget line still open, into one that has closed, into an envelope it would create -- on the ground that the riskiest class may not ride the same click as the safest. That partition says nothing about whether the money is ALREADY RECORDED SOMEWHERE ELSE, so a line carrying a warning that the app found a row it might be was still counted in the sweep and still ticked by that one click: the warning printed beside the control and the control ignored it, which is the *warning paragraph above a working select* shape ruling **R-GJ** cost `$7,412.94` to learn, at the grain of one LINE rather than one merchant. **So the sweep belongs to the evidence group** ruling **R-HB** puts the line in, and only the group whose own heading says the app searched and found nothing offers one. A line in either other group keeps its own destination select and is hand-picked, so no act is taken away from an owner who looks -- what is removed is the BULK act over lines the same screen has just questioned. **Its reach is structural rather than nominal**: the sweep's root is the DOM subtree it lives in, so it cannot select a row outside its own card even if a later edit gives one a placement class. Measured 2026-08-28 on the developer's own data through the real producer: 0 of his 2 creatable lines carry both a sweep class and a warning, so this was LATENT and not live -- and grouping the queue by evidence is what would have made it a contradiction on the page. The developer's answer to the fork, asked with the alternatives of keeping the sweep global and of deleting it outright: build what you would build from scratch |
| recurrence | R-R39 | -- | 2026-08-05 (developer) | **Scope.** Full two-axis redesign (not additive patterns, not staged dual-write) |
| recurrence | R-R40 | -- | 2026-08-05 (developer) | **Add-ons.** ALL FOUR: weekly-by-date, nth-weekday, count-bounded end, business-day shift |
| recurrence | R-R41 | -- | 2026-08-05 (developer) | **`loan_params.payment_day`.** DELETE; every reader goes through one accessor |
| recurrence | R-R42 | -- | 2026-08-05 (developer) | **The three defects.** Folded into the redesign, not fixed as separate PRs |
| recurrence | R-R43 | -- | 2026-08-05 (developer) | **Sequencing vs the balance arc.** The arc SPLITS: the engine core is disjoint from the balance arc's live work, the date work is not. "Half B folded into X-an" is SUPERSEDED -- R6 reads a column R5 creates, and R5 cannot precede the balance step deleting from `cash_ledger/_events.py`. A developer ruling is owed; see section 0. The resulting ORDER is `steps.md`'s |
| recurrence | R-R44 | -- | 2026-08-05 (developer) | **`PAY_PERIODS_PER_YEAR`.** SHIPPED at R7a-2a (`003e3657`) as `pay_calendar.PayCadence`; derivation = `round(365.2425 / cadence_days)`, whose own section 4a left the arc document with the R7a-2 span and is in `historical/recurrence_findings_as_built_2026-08-15.md`. It surfaced a SECOND stored paycheck count -- finding **F-16**, ruled into this arc as plan step R-F16 |
| recurrence | R-R12 | -- | 2026-08-08 | **A generated row's dates: THREE facts, three homes: `occurs_on` (the occurrence), `pay_period_id` (the funding), `due_on` (the installment). `compute_due_date` is DELETED.** |
| recurrence | R-R13 | -- | 2026-08-08 | **`anchor_date` itself: SPLITS before R7c freezes it: `starts_on` (one meaning, every unit) + `nominal_day`.** **R-R3**'s month-end-clamp subtype is SUPERSEDED by this split -- the anchor day and the clamp were one question until it, and the table carried that supersession as a row of its own ("Anchor day vs month-end clamp") until `balance:X-ao-2a` folded it into the ruling it is about |
| recurrence | R-R7 | -- | 2026-08-05 | **Orphaned rules: NOT deleted in R2b; they ship with the fix for the leak that makes them**, so the cleanup and its cause are reviewed together and R2b stays purely additive. Shipped as plan step R-F6. Two claims the earlier drafts rested on were measured FALSE first: the 5 orphaned rules were said to carry "neither a start period nor a template" when ids 4, 41, 43 and 47 all carry a `start_period_id` (1, 1, 3, 2); and taking `transaction_templates.recurrence_rule_id` off `ON DELETE SET NULL` was said to close the leak, when that FK fires on a RULE deletion and the leak runs the other way, out of `templates.hard_delete_template`. The argument is in `historical/recurrence_evidence_2026-08-11.md`, which is where this ruling's own text sat until `balance:X-ao-2a` |
| recurrence | R-R14 | -- | 2026-08-08 | **A failed migration-bearing deploy: Back up unconditionally, PRE-FLIGHT whether rollback can work, and REFUSE the rollback that cannot. The app keeps failing loud rather than booting against a schema it cannot describe.** |
| recurrence | R-R45 | -- | 2026-08-13 | **What "this commitment has ended" means: The rule OWES no occurrence on or after the day asked about -- one reading for BOTH closing bounds.** A date bound used to wait for its bound date, so the same schedule written as a count stopped counting sooner. Shipped as plan step R-D33, $0.00 on live data |
| recurrence | R-R46 | -- | 2026-08-27 | **`R5` DECOMPOSES and `R17` is its first leaf: a generated row RECORDS which occurrence it answers (`occurs_on`), and the interim due-date match `R17` was specified as is not built.** `pay_calendar:C5b` is the step that makes `should_skip_period` occurrence-aware and it waits on `R5` for this column, so the interim would have been written to be deleted. Two consequences the specification did not have: `occurs_on` is NULLABLE, because `carry_forward_service` and the one-time transfer branch both create template-linked rows no cadence named, and the BACKFILL is `scripts/stamp_occurrences.py` run by `entrypoint.sh` rather than the migration -- a row's `due_date` is not its occurrence for 30 of 780 rows, so the walk is required, and no migration here imports app code because `build_test_template.py` replays the chain from zero |
| recurrence | R-R10 | -- | 2026-08-07 | **Where the two-axis columns live: Computed until R7c, stored from R7c.** It binds R7c's backfill. ARCHIVED: the argument is `historical/recurrence_as_built_2026-08-05.md`, "a derivation is not stored beside its own inputs" |
| recurrence | R-R15 | -- | 2026-08-08 | **What a Recurrence cell says: The UNIFORM shape: every calendar cadence names its cycle the same way, month and day, which is what a yearly rule already did and a quarterly one did not. The month named is the FIRST OCCURRENCE's, not the authored `month_of_year` -- the same residue class either way, and the only one R7c can still express.** |
| recurrence | R-R16 | -- | 2026-08-14 | **What `starts_on` MEANS: The rule's FIRST OCCURRENCE, ONE meaning for every unit: the first date a calendar cadence fires on, and the payday of the first paycheck a pay-period cadence bills in. Its position in the cycle IS the phase, so `day_of_month` and `month_of_year` are DROPPED rather than renamed. Supersedes R-R13's "opening validity bound" reading and closes row D28** |
| recurrence | R-R17 | -- | 2026-08-14 | **Whether the YEAR unit survives: YES, and `(12k, MONTH)` is CANONICALISED to `(k, YEAR)` at the write door once the interval is free, so one cadence has one spelling. Dropping the unit would move the times-twelve encoding into the form, which is the second vocabulary R7b-1 removed.** |
| recurrence | R-R18 | -- | 2026-08-14 | **How R7c ships: THREE leaves, expand / migrate / contract: `R7c-a` adds the columns and dual-writes them while nothing reads them, `R7c-b` moves every reader and the form onto them, `R7c-c` drops the closed set. The destructive DDL is last and no translation shim is written in any leaf.** |
| recurrence | R-R20 | -- | 2026-08-16 | **Whether `day_of_month` may be dropped before its reader: YES, because the column is a derived ENCODING and its reader reads the derivation instead. `recurrence_engine.compute_due_date` dates every generated row from it and plan step R5 deletes that function four ranks later; ledger row D37 named two ways out -- hold the column, or swap the ranks -- and this is a third. `recurrence.scheduling_day_of_month` answers the same value from `(unit_id, placement_id, starts_on, nominal_day)`, and the migration grades the equality in SQL before dropping: 0 of 46 live rules disagree.** |
| recurrence | R-R21 | -- | 2026-08-16 | **What the form does when a cadence admits ONE funding rule: RENDER the "Funded from" row anyway, with help text saying there is nothing to choose. Hiding it is how a bill's funding rule came to change with nothing on screen saying so (ledger row D32), and freeing the interval removes the measured instance outright -- the MONTH unit offers both placements at every interval. The two that remain are PERIOD, where the placement is provably inert, and YEAR, whose first-paycheck anchor is R8's.** Its second example expired the next day**: R-R24 admitted the YEAR unit's deferring reading, so PERIOD is the only cadence left admitting one placement. The RULING is unchanged -- the row renders either way -- and only the count of cadences it applies to moved** |
| recurrence | R-R22 | -- | 2026-08-16 | **Whether the AUTHORED month needs surfacing before it is dropped: NO: ruling R-R15 already decided the display and the two affected rules fire on the same cycle either way -- `3 = 6 (mod 3)` and `3 = 9 (mod 6)` -- so no generated row and no projected balance moves. The typed month was a second spelling of the residue class the first occurrence carries, not a second fact. Closes ledger row D38 with nothing built.** |
| recurrence | R-R23 | -- | 2026-08-16 | **What GATES the offer set, and how R8 ships: The gate is what the app can HONOUR, derived from two live facts, not from an anchor derivation that was deleted. `anchor_family` and its three `FAMILY_*` constants go: ruling R-R16 made the first occurrence AUTHORED and R7c-b deleted all three derivations the router selected between, leaving it refusing two cadences by citing them. Measured over all eight `(unit, placement)` pairs, its one live projection equals `has_day_of_month_coordinate(unit) and placement is CONTAINING_DATE`. And R8 SPLITS on that measurement: only the gate ships now, as `R8-a`, because the other three add-ons each need a generated row's own `occurs_on` and therefore wait on R5. Shipped as R8-a** |
| recurrence | R-R24 | -- | 2026-08-16 | **Whether a year-scale cadence may defer onto a later paycheck: YES. `(k, YEAR, PERIOD_STARTING_ON_OR_AFTER)` fires on its own authored date every `k` years and defers onto the next paycheck, exactly as its MONTH twin already does; the refusal cited `_first_of_month_anchor`, deleted at R7c-b. 0 of 46 live rules read differently, and a yearly cadence cannot repeat a paycheck at any `cadence_days` in 1-365. It also restores `(12, MONTH, deferred)` -> `(1, YEAR, deferred)`, so one rhythm keeps one spelling. Shipped as R8-a** |
| recurrence | R-R25 | -- | 2026-08-16 | **Where the nth-weekday coordinate LIVES: On `budget.recurrence_rules`, as an EXCLUSIVE ARC of columns under one CHECK, and `budget.recurrence_weekday_anchors` is DROPPED unwritten. "A rule fires on a day-of-month OR an nth-weekday" cannot be a CHECK while the two live in different tables -- PostgreSQL CHECKs are single-table -- so the satellite form would need a trigger or a door-only fence for an invariant the column form makes structural. The same move ruling R-R16 made for `nominal_day` and R7c-c made for the unwritten `recurrence_month_anchors`. Owned by R8-c** |
| recurrence | R-R26 | -- | 2026-08-16 | **What "non-business day" MEANS: Weekends plus the eleven US federal holidays, DERIVED as rules rather than seeded as rows -- they are nth-weekday-of-month rules and fixed dates with weekend observation, so there is no per-year seed to keep current and the derivation composes with R8-c's own machinery. Owned by R8-d. Finding F-4, the PAY SCHEDULE's own holiday shift, is a different question and is not settled by this** |
| recurrence | R-R27 | -- | 2026-08-17 | **Whether the closed set's TABLE and its ENUM may go in ONE release: YES, and R-R11's hazard does not generalise to a dropped TABLE at all -- which an adversarial review of R9 established after the step had argued the opposite. R-R11 held the `Once` ROW because `ref_cache.init` raises for an enum member with no row in a table that EXISTS; `_load_rows` CATCHES the `ProgrammingError` a missing table raises, and `init` records it unavailable and completes, pinned by `test_ref_cache.py::test_init_records_unavailable_table_and_keeps_others_usable`. Three independent reasons the auto-rollback image is safe, in the order they fire: it never reaches `ref_cache`, because `entrypoint.sh` step 3 runs `init_database.py` with `init_ref_cache=False` and its Alembic tree cannot resolve the new revision, so `set -eEuo pipefail` aborts (finding F-8); `shekel-deploy:repin_is_safe` refuses the re-pin for the same reason, leaving the pre-deploy dump as recovery; and booted anyway it would degrade rather than die. Precedent measured the same day: R7c-a, R7c-b and R7c-c all reached production in ONE release (PR #102, `41e09dad`), and R7c-c dropped a column the previous image's ORM mapped. Shipped as R9** |
| recurrence | R-R31 | -- | 2026-08-19 | **What a forward window named in MONTHS resolves to: The LAST WHOLE PAYCHECK that ARRIVES within the span, counted against the CADENCE itself: `floor(months x DAYS_PER_YEAR / (MONTHS_PER_YEAR x cadence_days))`. So the LABEL is the fixed thing and the pay-period count derives from the owner's stated rhythm. Flooring rather than rounding was measured: over all 365 legal cadences crossed with the 3 / 6 / 12 / 24-month spans the two disagree on 388 of 1,460 cases, and by mean absolute distance between the resolved period's END and the day the label names the floored answer is closer in 387 and equal in the last. Deriving it through the ROUNDED `periods_per_year` instead was the first implementation and an adversarial review measured it wrong -- double rounding disagrees with the arrival test on 384 cases, always overshooting, and in 74 of them OFFERS a horizon the owner has no paycheck inside at all. A span no paycheck reaches resolves to ZERO and is NOT OFFERED; a surface that must render something anyway (the Plan tab, the pulse chart) shows the paycheck the owner is in, which is a POLICY and not an invariant. Shipped as R-F17** |
| recurrence | R-R19 | -- | 2026-08-15 | **What a REGENERATION does to the rows it already generated: It MAINTAINS them; it does not destroy and rebuild them. A row the rule still names is UPDATED in place, a period the rule names with no row gets one, and a row the rule no longer names is removed only when it carries nothing of the owner's. A row holding the owner's own records -- purchases, a note, a hand-entered actual -- is RETAINED and reported, in two shapes: the rule stopped naming its period, or the template's ACCOUNT moved, which drags every purchase onto the new account and invalidates the statement link that cleared it. The delete-and-recreate this replaces was safe only while a generated row was a pure projection of `(template, period)`; `transaction_entries` CASCADE from their parent, so it destroyed `$499.82` of recorded purchases on live data. Shipped as R10-a (`5fc13cdb`), closing row N-292** |
| recurrence | R-R29 | -- | 2026-08-19 | **Where a loan payment's DERIVED validity window lives: SPLIT BY KIND: only the CLOSING bound stops being stored. `starts_on` is the cadence ANCHOR since R-R16 -- `_resolution.cadence_day_of_month` is its one reader and the cycle phase derives from it -- so a MONTH-unit loan payment without it has no day to fire on, and it stays written. `end_date` goes NULL for a loan payment and `ck_recurrence_rules_valid_window` lands on that. D35 half closes: a derived `starts_on` still shares a column with authored ones. Owned by R7d** |
| recurrence | R-R30 | -- | 2026-08-19 | **What generation does for an owner with no baseline scenario: REFUSES -- `BaselineMissingError` to the single application-level handler (R-BW), as every other producer needing a scenario does. The early return it replaces left the last-written bound standing, and with nothing stored there is nothing to stand. The create path and `period_population` already behave this way, so what it decides is the FORM READ path. Owned by R7d** |
| recurrence | R-R33 | -- | 2026-08-25 (developer) | **What an installment NO ROW covers is priced at: What the loan's own recurring payment SAYS it costs, resolved AS OF that installment, and the contract only where the definition states no price.** A payment that states none -- DERIVE mode, where the stored figure is a snapshot of the contract -- costs the contractual P&I plus that installment's escrow plus the standing extra, which is what the mode MEANS. One that states a price costs what its VERSION SERIES says on the installment's own due date, plus the extra. **Never `default_amount`**: an adversarial review measured that scalar reaching backwards, because `_resync_scalar` puts it on the NEWEST price stated rather than the price on a date, so a `$700.00` Van payment effective 2028-01-01 priced every 2026 installment at `$700.00` and moved the derived payoff six installments EARLY once the future rows were absent -- the under-generating direction this arc exists to close, re-entered by the step closing it. The ESTIMATED tier read the CONTRACT for every uncovered slot whatever the definition said, so "what will this loan be paid in month M" had two answers and which one the fold used depended on whether the row had been WRITTEN yet: measured on a production clone, a Van payment standing at `$300.00` against a `$531.94` contractual installment resolved its recurrence bound to `2029-02-22` at the moment `regenerate_pay_periods` generates against `2030-02-22` before and after, twelve payments the owner owes. The same two answers switched the projection at the materialised HORIZON -- the owner's figure inside it, the servicer's past it. One rule closes both, and the payoff stops depending on materialisation at all, which is the loop R7d must break before it can stop storing the bound. `$0.00` on the developer's data, where both templates state exactly P&I + escrow. Shipped as R7d-a |
| recurrence | R-R34 | -- | 2026-08-25 (developer) | **How far a step that stops STORING a derived value reaches: To every reader that inherits the column, one leaf each.** R7d's specification censused the ten WRITERS of a loan payment's closing bound and no readers; measured by setting `end_date` NULL on both live rules, SIX surfaces read it -- generation, `recurring_view` / `describe`, `obligations_aggregator.has_ended`, the form's locked "Ends" control, `_recurrence_form_refusals`' inverted-window check and `_recurrence_preview` -- and with the column NULL every one of them reads "never ends". For `has_ended` that means a RETIRED loan's payment never leaves `/obligations` or the `/savings` emergency-fund baseline, `$2,442.89` a month across the two live loans. Shipping the resolver behind generation alone would delete a cache while leaving five surfaces reading its corpse. R7d therefore DECOMPOSES into seven leaves. |
| recurrence | R-R36 | -- | 2026-08-26 (developer) | **What a contractual month owes a definition whose cadence is not monthly: Nothing, because a month is not what a definition owes anything TO.** Interest is charged because TIME passed and escrow because a month began; a payment moves cash on whatever date its definition names. They are separate EVENTS, and once they are the cadence question is deleted rather than answered: a second payment inside one accrual period clears no fresh charge and pays pure principal. Fusing them made the payment COUNT the clock -- measured on a production clone, 30 payments of `$531.94` fourteen days apart and 30 a month apart charge the identical `$1,096.34`, split for split, and that fact needs no accrual convention to state. **The alternatives were weighed over ONE stream, named so the figures re-derive**: a `$531.94` definition firing every 14 days from `2026-09-04` against the Van's `$14,745.51` seed at 5.668%, accruing from `2026-08-26`. Per-occurrence answers `2027-10-01` / `$493.34` and the actual/365 truth over the same stream is `2027-10-01` / `$467.51` -- the DATE exactly right. Aggregating the month's occurrences into one payment charges the SAME `$493.34` (this fold charges per period either way) and dates the payoff `2027-10-22`, **21 days late**, because it moves the month's whole cash to the contractual day. The cadence's monthly EQUIVALENT can never show a three-payment month, and elapsed-day accrual is a different CONVENTION (D52) rather than a different fold. Owned by R16-a |
| recurrence | R-R37 | -- | 2026-08-27 (developer) | **Whether the ESTIMATED tier may read a definition's stored closing bound: It must HONOUR it, so the COLUMN must first hold one fact, and the ORDER changes rather than the rule.** Ignoring a field an owner entered was refused outright (developer): a closing bound is a real input to "what will this loan be paid", and a rule that skips it models money the owner has said will not move. But `budget.recurrence_rules.end_date` holds TWO facts today -- what an owner authored, and a snapshot of the derived payoff that `sync_recurring_payment_bounds` writes from ten call sites. **What tells them apart is `owns_validity_window`, and reading it here is the remedy R-R35 already refused**: it ends `active.id == template.id`, so asking it puts `active_recurring_transfer_template`'s `.order_by(id).first()` back on the PRICING path -- the tie-break four remedies were rejected to delete -- and it answers "owned" for a no-baseline owner whose bound nothing wrote (its own docstring, and finding **D49**). The column is written at MUTATION time while the fold moves with the CLOCK, so the two agree only between events. So the `R7d` reader leaves and the writer deletion move AHEAD of the sum -- `steps.md` holds the order and this does not restate it -- putting every reader of the derived bound on `loan_payment_window` and then NULLing the column, after which the bound is honoured unconditionally and there is no rule left to state. **What that migration must not do is erase an AUTHORED bound**, which "NULL for every loan payment" as specified would on a loan holding a second definition: finding **D56**. Owned by R16-b-2 |
| recurrence | R-R35 | -- | 2026-08-25 (developer) | **WHICH recurring transfer into a loan the derived bound is FOR: Every one of them, so the question is deleted rather than answered.** `loan_payment_window` takes the DEFINITION, not the loan, and the step's originally specified `(account, ctx)` is superseded: asking "when does this LOAN's payment stop" forces a prior question -- which of the recurring transfers into the loan IS its payment -- that nothing in the schema answers, so `active_recurring_transfer_template` tie-breaks it on `id`. Four remedies were put to the developer (a pointer on the loan, the settings row as the identity, refusing a second transfer, and writing the tie-break down) and ALL FOUR WERE REFUSED as band-aids on a search that should not exist. The measurement behind the refusal: the SETTLED fold (`loan_loaders.query_shadow_income`) and the PLANNED tier (`balance_at._plan.loan_plan` via `projected_income_shadows`) both already sum EVERY transfer into the loan account with no template filter, and `loan_ledger.split_payment_cash` already routes anything above interest + escrow to principal -- so "every transfer into a loan is a payment against it" is the app's own rule at two tiers of three, and the ESTIMATED tier is the single outlier that picks one. Each definition stops when the loan does, because past payoff that same split routes the whole cash to `excess` (a Refund). What this does NOT settle is the outlier: pricing an uncovered installment from ONE picked definition where its siblings sum is the half of D47 that survives, re-pointed to R16. Measured on a production clone: a `$200.00`/mo transfer into the Mortgage created before the real `$1,910.95` one wins the tie-break and drives the derived payoff `2048-12-01` -> `None`, against a `$616.99` monthly escrow it does not cover; with the resolver, BOTH definitions answer the same window. Shipped as R7d-b |
| recurrence | R-R32 | -- | 2026-08-20 (developer) | **How a definition's ACCOUNT change reaches the transfers it already generated: IN PLACE, through the one write door, which learns to move a transfer's endpoints.** A generated transfer derives SIX columns from its template and `transfer_service.update_transfer` could write FOUR, so the two account columns reached their rows only by the sweep DELETING and re-creating every one of them -- measured on a production clone at 51 of 62 rows re-pointed as a side effect of a rebuild -- while a NON-repeating template's identical edit was REFUSED outright, because nothing could carry it. That is a limit of the door, not a rule about transfers, so the door moves the parent and both shadow rows now, re-deriving each leg's display name and reconciling the ledger on both sides; `NON_REPEATING_ACCOUNTS_ARE_FIXED` is DELETED and one edit stops meaning two things. A row holding the owner's own records is still RETAINED rather than moved, which is R-R19 unchanged: its settled figure is what moved between the OLD accounts and its statement link is scoped BY account. Shipped as R10-b |
| pay_calendar | R-PC2 | -- | 2026-08-08 (developer) | **The pay-period model: NORMALIZE, do not check. `budget.pay_periods` stores the PAYDAY; `end_date` and `period_index` are derived by one producer and dropped from the table.** Option "store paydays only". It answers the recurrence arc's **F-10** fork as well -- a pay-period HOLE -- which that arc's own table carried as a second copy of this ruling until `balance:X-ao-2a` recorded it once; a hole and an overlap are both inexpressible once the derived columns are gone, which is **R-PC3** |
| pay_calendar | R-PC3 | -- | 2026-08-08 (developer) | **A gapped or overlapping batch: Neither is refused, because neither is expressible once the derived columns are gone. The "refuse it" and "bridge it with a filler period" options were both weighed and rejected -- see `historical/pay_calendar_evidence_2026-08-11.md`** |
| pay_calendar | R-PC4 | -- | 2026-08-08 (developer) | **A payday inserted BETWEEN two existing ones: REFUSE when the period it splits is locked by the existing `pay_period_admin.classify_period_lock` (historical, settled, posted, or a recurrence anchor); otherwise insert and re-derive. No row may ever be left dated outside its own paycheck** |
| pay_calendar | R-PC5 | -- | 2026-08-08 (developer) | **The last payday's period end.** Projected as `start_date + cadence_days - 1` from `budget.pay_schedule`. It is the ONLY derived end that is not `lead(paid_on) - 1`, and it is a projection stated as one -- `DerivedPeriod.end_is_projected`, because a consumer holding one period out of its calendar cannot recompute it and a window VIEW must keep it |
| pay_calendar | R-PC6 | -- | 2026-08-08 (developer) | **What a derived period carries: `period_id`, `int \| None`.** `None` IS the marker for a period no foreign key can point at, which is the distinction `C2` must draw between "which paycheck does this row live in" and "which span does this day fall in". One value type for the arc, so C2 MOVES `SchedulePeriod` rather than merging two |
| pay_calendar | R-PC7 | -- | 2026-08-08 (developer) | **Table and column names.** `budget.pay_periods.start_date` KEEPS both names. A period is identified one-to-one by the payday that opens it, `transactions.pay_period_id` reads correctly against it, and a rename is a four-FK migration that buys nothing. See `historical/pay_calendar_evidence_2026-08-11.md` |
| pay_calendar | R-PC8 | -- | 2026-08-10 (developer) | **An entry dated outside the schedule: A legitimate SECOND QUESTION, named on the one value -- not a compensator to delete.** The calendar gains `period_starting_on_or_before`, the missing mirror of the `period_starting_on_or_after` it already carries, and the FILING rule is DERIVED from it rather than scanning again. The alternative (drop the ledger's stored paycheck and derive it from `entry_date`) was measured against `shekel-prod-db` and REFUTED: 14 days carry TWO paychecks for one date, so `entry_date` does not determine it; 35 of 327 entries are dated outside their own paycheck by design; and 4 loan-opening entries predate the first payday by up to seven years, so the clamp is live rather than hypothetical. See `historical/pay_calendar_evidence_2026-08-11.md` |
| pay_calendar | R-PC9 | -- | 2026-08-10 (developer) | **Past the last stored payday: The calendar ANSWERS, projecting forward at the OWNER's cadence with `period_id = None`**, which is what makes it the TOTAL function `balance:X-l` asks for. `growth_engine.generate_projection_periods` and `SyntheticPeriod` retire into it (rows **P17**, **P20**). Containment over SAVED periods stays its own named method, because the recurrence engine needs to tell a schedule HOLE from "the schedule has not reached there yet" |
| pay_calendar | R-PC1 | -- | 2026-08-10 (developer) | **The forward-only rule (R-PC1): REPLACE `_reject_overlapping_batch`, do not delete it. SPLIT IN TWO, corrected twice by C3-b's neutral reviews, then HALVED 2026-08-11 (developer).** The rule the plan stated -- "the last paycheck must hold no row dated on or after the new payday" -- was measured wrong in both directions. What survives is a forward-only FLOOR of one full cadence after the latest payday, whose only job is keeping C6 closed and which C6 deletes. **The COVERAGE half is DELETED**: it refused a write taking a day out of every paycheck while a SETTLED row's `settled_on` fell on it, on the claim that stranding such a day reproduces `balance:N-128` -- and the claim was false. `_cash_periods` values each column at its OWN `end_date`, so a day off the top of the window is absent from both sides of R-K and reports as `period_timing`. See C3-b |
| pay_calendar | R-PC10 | -- | 2026-08-11 (developer) | **When the gap machinery dies: With `C2-b2`, the leaf that makes its subject unreachable -- not with `C4`.** `C5a` was gated on C4 and the gate had no code behind it: `PlacementOutcome.SCHEDULE_GAP`, `GenerationPlan.gaps` and `report_schedule_gaps` read no stored column, so all three went dead when the recurrence engine took the derived calendar, and shipping a branch that cannot fire is what rule 1 forbids. The visibility it took is replaced by `integrity_check` **BA-07**, which asks the stored question as a query and dies with the column at C4. Rejected: leaving it dead with a docstring note (ledger row **P25**'s original disposition), which ships code that lies about a state the app cannot hold |
| pay_calendar | R-PC11 | -- | 2026-08-13 (developer) | **How the seam learns WHICH periods to report: It READS them; the argument is DELETED.** All eight callers filled it with one value -- the owner's whole saved set -- so the only thing it could express was a mistake, which is the shape ruling R-Q already removed the override map for. `BalanceContext` carries the calendar, taking the first of `balance:X-i1`'s five inputs early. Rejected: callers slicing the calendar themselves (eight extra loads, and it pushes calendar-loading into routes ahead of `C2-f`), and threading a `PayCalendar` beside the context (a second per-request bundle, which is what the context exists to end) |
| pay_calendar | R-PC12 | -- | 2026-08-13 (developer) | **A window's two invariants: ORDER is DERIVED and CONTIGUITY is CHECKED.** A window's identity is its period SET, so `__post_init__` sorts and a caller cannot state an order wrongly; contiguity is a property of the input no constructor can compute its way out of, so it is refused. That refusal is P32's first disposition, taken because the second ("`containing` stops answering `None` inside its own span") is what a tiling gives for free and so decides nothing |
| pay_calendar | R-PC13 | -- | 2026-08-13 (developer) | **`PayCalendarError` reaching a balance page: RECORD it, do not build the handler.** `C2-c` widens the raise from the recurrence and savings surfaces to `/grid` and `/accounts/<id>`, where `app/error_handlers.py` leaves it on the bare 500. The raise is right (a defaulted cadence misreports every horizon); what is missing is a recovery page, which is a ruling of `BaselineMissingError`'s size. Row **P35** carries it and `C4` deletes the fallback that causes it |
| pay_calendar | R-PC14 | -- | 2026-08-14 (developer) | **The axis below the first payday: REFUSE, and clamp in ONE named companion**, ledger row **P23**. `axis()` answered a range opening below `opening_bound` with the part above it -- silently, and a short axis is indistinguishable from a complete one, which is the argument `overlapping()` already makes for refusing a CROSSED range. Nothing is projected backwards (the 2026-08-10 ruling), so covering such a range was never an option and refusing is the only answer that is not a half-truth. The owner whose first payday has not happened yet is an ordinary state -- the Generate form asks for "your next (or first) payday" -- so `projection_axis()` sits beside `axis()` as the TOTAL companion every projecting surface calls, exactly as `filing_period` sits beside `period_starting_on_or_before`. Rejected: stating the truncation in the `Returns` block (a value that answers a different question than it was asked, however honestly documented), and projecting backwards (it would attribute money to paychecks that never happened) |
| pay_calendar | R-PC15 | -- | 2026-08-14 (developer) | **What the projection axis HEAD is: Each surface keeps its own, and the SEED follows it**, ledger row **P22**. The axis covers the range it is given, so a caller passing the read pass's clock gets the period CONTAINING it -- opening up to a cadence in the past. That is correct wherever the seed is read at `axis[0].start_date - 1`, which `retirement_projection._resolve_seed_balances` already did and which `investment_dashboard_service` was corrected to do. The one surface whose seed CANNOT follow -- the Horizon's asset band, seeded from the figure the net-worth hero shows -- left the axis instead: it carries no contributions, so an axis was only chopping the horizon into pieces the compound formula is indifferent to. Rejected: clipping the axis head to the requested day (a partial period is not a period, and the window type exists to say so), and re-seeding the asset band at the axis head (one balance-seam read per account, and index 0 would stop equalling the hero) |
| pay_calendar | R-PC16 | -- | 2026-08-10 (developer) | **C2's shape: DECOMPOSED into `C2-a`..`C2-f`, the value first with nothing calling it.** A single commit over 60 call sites cannot be proven against production BEFORE its consumers depend on it, which is exactly the technique that made C1 safe, cannot be reviewed in focus, and cannot be reverted precisely -- and two of the cutovers move money |
| pay_calendar | R-PC17 | -- | 2026-08-14 (developer) | **`C2-f`'s shape: DECOMPOSED again into three leaves, split by READER**, on a measurement the first decomposition did not have: 13 functions read two or more of the six readers over 27 of their 60 sites, and 11 pair `get_current_period` with `get_all_periods`. Splitting THAT pair would leave a dozen context objects holding an ORM row in one field and a `DerivedPeriod` in another, so the two travel together in `C2-f3`. Rejected: by SURFACE (no leaf can then delete a reader, so no leaf has a checkable end state) and one commit (the shape **R-PC16**, this ruling's parent, already refused) |
| pay_calendar | R-PC18 | -- | 2026-08-14 (developer) | **`C2-f2`'s shape: DECOMPOSED into FIVE leaves, split by PACKAGE**, on a measurement the parent decomposition did not have: the **20** call sites at a surface holding a `BalanceContext` (AST census over the seven packages) feed **23** `app/` modules -- those calling one of the three readers or carrying its rows onward -- and **12** templates, being those reading a period's `id`, `start_date`, `end_date`, `period_index` or `label`; every one of the 12 is the grid's. One commit over that is the shape ruling **R-PC16** (`C2's shape`) already refused. **The by-READER axis its parent used is UNAVAILABLE here, which is why the split is by package** (adversarial design review, 2026-08-14): six of the seven surfaces read `get_current_period` and `get_all_periods` inside ONE function, so a reader split yields exactly two leaves -- `get_periods_in_range` (3 sites, all grid) and everything else (17 sites, 7 packages) -- and the second is the leaf this ruling exists to refuse. Each leaf's end state is a GRADED predicate rather than a grep: `a` closes **P37**, `b` DELETES `get_periods_in_range` whole, `c` closes **P48**, `d` closes **P43**, `e` closes **P36**. The SEAM goes first: it is the only leaf touching money-shaped logic (the annual-limit accumulation order) and the smallest diff, so it gets a review of its own. Rejected: three leaves (the middle one is 17 modules, the same objection one size down) and one commit |
| pay_calendar | R-PC19 | -- | 2026-08-14 (developer) | **How the CONTRIBUTION tier learns its periods: A REQUIRED `PayCalendar` parameter on `_asset_fold.resolve`, which derives the window itself**, ledger row **P37**; the type was `PeriodWindow` until an adversarial design review of `C2-f2a` refuted it the same day. That function deliberately takes no `BalanceContext` -- a fold assembled at one scenario beside a context carrying another loads the modelled tier against rows the cash tiers never saw -- and a calendar carries neither a scenario nor a clock, so passing one reintroduces nothing. **The CALENDAR and not the window, on three counts the window loses.** The tier's precondition is the owner's WHOLE schedule (the annual limit is a calendar-year accumulation, so a slice restarts the year-to-date total mid-year and uncaps it) and `PeriodWindow` cannot say whether `saved()`, `window()` or `axis()` produced it, so a window put that precondition in a docstring one call from the grid's six-period slice; the calendar keeps the OWNER on the value where a period list drops it; and **C9**, which projects this tier past the horizon on `projection_axis`, becomes a one-line change inside `resolve` rather than a sixth parameter, which `max-args` and `max-positional-arguments` (both 5, and this signature is at 5) would refuse. *The superseded window ruling claimed its two callers held two DIFFERENT windows; they did not -- both were `reported_periods()`, which is the argument ruling **R-PC11** ("How the seam learns WHICH periods to report") deleted, at a smaller scale.* Rejected: a field on `ContributionInputs` (its `absent()` token would carry an empty window, the silent failure made reachable) and a field on `AssembledCashFold` (an OWNER value copied onto a per-ACCOUNT record, and an argument every cash reader pays for that only the modelled tier reads -- two of its four callers discard everything but `.walk`) |
| pay_calendar | R-PC20 | -- | 2026-08-14 (developer) | **The modelled fold past the horizon: PROJECT the contributions; ruling `balance:R-AG` is SUPERSEDED**, and the evidence is what changed rather than the argument: R-AG (2026-07-27) let the fold run a half model because no total calendar existed, `C2-e` built one, and three surfaces already project on it -- so the seam is now the only one that does not and it disagrees with the pages built on it. `C9` is the remedy and it MOVES MONEY (row **P7**, `+$5,427.07` at six months out) |
| pay_calendar | R-PC21 | -- | 2026-08-16 (developer) | **How a producer gets its READ PASS: The ROUTE builds one per request; every producer below takes `balance_ctx` as a REQUIRED parameter and DROPS its `user_id`**, row **P43**. A producer holding an id can manufacture a `BalanceContext`, and three did, so `/retirement` held two passes and `/savings` two plus a third calendar derivation -- two clock reads behind two cards on one screen, `$4.18` of after-tax projected savings and one paycheck of countdown apart across a midnight-into-payday render. Dropping the id makes a mismatched (owner, pass) pair unrepresentable rather than discouraged, and a required parameter fails at the call instead of silently opening a pass. It follows the seam's own convention (`balance_at(acct, ctx, day)`) and the required-`PayCalendar` ruling **R-PC19**. Rejected: `user_id` BESIDE the pass (two spellings, nothing reconciling them); an optional `balance_ctx=None` that builds one when absent, which is the pattern that CAUSED this; and fixing only the one leaf P43 named, which leaves the measured contradiction on screen. **It does not make a second pass unconstructible** -- eight service modules still open their own, and the layer predicate that would (row **P56**) waits on the leaves closing those doors |
| pay_calendar | R-PC22 | -- | 2026-08-16 (developer) | **How a TEST builds a pass over hand-written paydays: ONE shared helper, `tests/_test_helpers.read_pass_over_paydays`**, row **P54**. The pass derives its calendar into a field its own module declares private; a unit case with no database must seed it, and three sites wanted to. Rejected: a `BalanceContext.for_test(calendar=...)` constructor (a production entry point whose only caller is the suite, `CLAUDE.md` rule 13) and an optional `calendar=` on the real `build` (it hands every production caller a way to supply a calendar the module did not derive, unchecked against the owner). Also rejected, on measurement: making the calendar EAGER like the scenario -- deriving one can RAISE for an owner with no pay schedule, so every render would begin failing over a fact most never read |
| pay_calendar | R-PC23 | -- | 2026-08-16 (developer) | **How the PAYCHECK ENGINE takes a period: A `DerivedPeriod`, and every caller supplies one**, on a fork `C2-f2d-3` could not cross without it: the engine reads a period's `start_date` and its ID, `/savings` needed to hand it derived periods, and no adapter is honest. Measured first: an AST census found those two attributes and nothing else, so the move is a retyping over 12 call sites in 10 modules rather than a rewrite, and all 13 `app/` sites trace to a MATERIALISED producer -- which is what makes `PeriodInfo.period_id`'s non-nullability structural where three consumers KEY on it. Rejected: one shared `current_breakdown(balance_ctx)` producer for the three surfaces that each build one (it merges what two OTHER pages publish, so it wants its own ruling -- row **P62**); a narrow ORM load kept inside `savings_dashboard_service` (a second pay-period read on a render whose whole point is one); and leaving the engine alone (`/savings` then keeps an ORM reader the arc exists to delete) |
| pay_calendar | R-PC24 | -- | 2026-08-16 (developer) | **`retirement_projection`'s two period parameters: DELETED; it reads both off the pass it already takes.** Both callers filled them from that same pass, so the pair could express only a mismatch -- the shape ruling **R-PC11** ("How the seam learns WHICH periods to report") removed at `C2-c`. 6 arguments to 4, which retires the `too-many-arguments` / `too-many-positional-arguments` suppression at its root rather than by raising a threshold, and left `_CurrentPay`'s two period fields with ZERO readers, so they went too. Rejected: converting at the `/savings` boundary (a second pay-period read), and keeping the parameters with a new type (the pass and the arguments can still disagree, and the suppression stays) |
| pay_calendar | R-PC25 | -- | 2026-08-16 (developer) | **`project_balance_horizons`'s two callers: Take `accounts/detail.py` onto the pass EARLY, out of `C2-f2e`.** That helper reads a period's ordinal and its id, and a `PayPeriod` spells the id `.id` where a `DerivedPeriod` spells it `.period_id` -- so its two callers could not hold two types between them for even one commit. Rejected: one module briefly holding both spellings (`accounts/detail.py` would carry a derived `current_period` for this call and an ORM one for its other fifteen reads), and splitting the rule from the lookup so each caller resolves index -> period -> balance itself (the loop written twice) |
| pay_calendar | R-PC26 | -- | 2026-08-18 (developer) | **What `/` renders for an owner with no PROJECTABLE account: The setup card AND the position tracks, and the card names the KIND that is missing**, ledger row **P65**. `has_account` is `resolve_grid_account`, which refuses an amortizing account by design (ruling D4) because this page renders a cash-flow balance -- so an owner who had entered a mortgage and nothing else was told to "set up an account" while `compute_tracks_section` computed a `$200,000.00` `DebtSummary` the template discarded. The tracks tier reads no grid account and no pay period, which is what lets it render beside the card rather than instead of it. Measured on both sides of `C2-f2e`, so it was this page's defect rather than that step's. Rejected: changing only the copy (the owner still cannot see a position they have entered), and leaving it (rule 8 -- a `$0.00` finding is a defect waiting for the data to change) |
| pay_calendar | R-PC27 | -- | 2026-08-19 (developer) | **The Spending producer's unexposed window arms: KEEP them, and retire the ordinal WALK inside them** at `C2-f3d`, against deleting the pay-period arm outright. It is unreachable -- `/analytics/spending` builds only month windows and `analytics_view.serialize_spending_chart` has only a month branch -- which `CLAUDE.md` rule 13 would ordinarily refuse. Two things outweigh it: the S-P1 gate ruling DEFERRED the pay-period and year pickers rather than cancelling them, so the arm is unshipped UI and not dead logic; and `C4` drops the `period_index` column every "which paycheck precedes this one" answer reads, so an arm that reads it owes that conversion whichever surface reaches it -- deleting it moves the same work to `C4` under another name. Recorded at `compute_spending_report` so the next reader meets it. Rejected: deleting the arm (it would take `query_settled_expenses`, ten unit cases and the harness's 62-window probe, which is this step's best regression coverage) |
| pay_calendar | R-PC28 | -- | 2026-08-19 (developer) | **The day the destructive doors decide on: The OWNER's civil day (`utils.dates.display_today`), and the lock classifier's `as_of` becomes REQUIRED** at `C2-f3b`, on finding `balance:N-191` -- which named `classify_periods_bulk` and `top_up_rolling_window` as the two sites deciding something against the user's CALENDAR on the PROCESS clock, and said each owed its own ruling. "Has this paycheck ended" and "has it started yet" are questions about the owner's schedule, so the owner's day is what answers them; `date.today()` is the container's, and both compose files pin `TZ: America/New_York` so the two agree THERE and nowhere else -- CI, a script and a bare `flask run` are where a truncate refusal turns on the deployment. The REQUIRED half is the same ruling one level down: `regenerate_pay_periods` read the clock THREE times for one decision, benign only because a period cannot become historical between two statements of one transaction, which is an argument from timing rather than from construction. Rejected: keeping `date.today()` (it leaves a destructive door's refusal resting on a deployment setting, which is N-191's own sentence), and resolving the day in the ROUTE and threading it (the same value, at the cost of a parameter on three public service doors, one of which is already at `max-args`) |
| pay_calendar | R-PC29 | -- | 2026-08-25 (developer) | **`C4`'s shape: DECOMPOSED into SEVEN leaves**, on the census re-measured that day: five reader leaves split by PACKAGE (`C4-a-1` .. `C4-a-5`), a schedule-row foreign key (`C4-b`), and the drop (`C4-c`). Eighteen-to-nineteen signature changes across five packages, plus a destructive migration, plus 63 `PayPeriod(...)` constructions in 35 test files that pass a dropped column, is the shape ruling **R-PC18** ("`C2-f2`'s shape") refused as one commit at a smaller size. The readers come off the columns first with NO schema change, each leaf ending at a package that names neither column; then the schedule row becomes guaranteed and the inferred cadence dies; then the columns drop. Rejected: three commits (`C4-a` whole is the five-package diff this ruling exists to refuse, and it cannot be reverted precisely), and folding `C4-b` into `C4-c` (an additive guarantee and a destructive drop would revert together, and `resolve_cadence`'s fallback death would land inside a schema migration rather than in front of one) |
| pay_calendar | R-PC30 | -- | 2026-08-25 (developer) | **How an owner's schedule row is guaranteed: A FOREIGN KEY, not a write-door discipline**, row **P8**. `budget.pay_periods.user_id` gains a second FK to `budget.pay_schedule (user_id)` -- already UNIQUE as `uq_pay_schedule_user` -- `ON DELETE CASCADE`, beside the CASCADE to `auth.users` it already carries. A payday for an owner whose cadence is not recorded becomes UNCONSTRUCTIBLE rather than merely unwritten, which is what lets `resolve_cadence`'s inferring fallback be DELETED rather than left as a branch no state can reach. Measured 2026-08-25 on both databases: 0 owners hold paydays without a schedule row, so the backfill writes nothing and the constraint takes clean. Rejected: leaving the invariant on `pay_period_write` and `auth_service.register_user`, where a violation surfaces as a `PayCalendarError` on a bare 500 (row **P35**) -- a symptom, at read time, on whichever money screen the owner opened first |
| pay_calendar | R-PC31 | -- | 2026-08-25 (developer) | **Where the attribution CLAMP lives: On the value: `DerivedPeriod.attribution_day`, and `utils.dates.attribution_date` is DELETED**, landing at `C4-a-2`. Its three live callers each pass one period's two bounds as separate arguments, which is a pairing a caller can get wrong -- "an argument a caller can get wrong is a defect, not a contract", which `_cash_fold`'s own docstring already cites -- and after `C4-a-1` and `C4-a-2` every one of them holds a `DerivedPeriod`, so the free function's signature outlives its last reason to exist. `DerivedPeriod.covers` lands with it, for the three sites that open-code `start_date <= day <= end_date`. **The rule's BODY moves rather than being wrapped**: `app.utils.dates` cannot import `pay_calendar` (`_derive` imports `app.utils.dates`, so a delegating free function would close a cycle), so a method beside a surviving function would be two independent implementations of one clamp. Rejected: keeping the free function and changing only where its bounds come from (the mis-pairable signature survives with no caller that needs it), and adding the method BESIDE it (two spellings of one rule, which is the denormalization this arc exists to remove) |
| pay_calendar | R-PC32 | -- | 2026-08-27 (developer) | **How a transaction's OWNER is proved: A COLUMN with two COMPOSITE FOREIGN KEYS, which is neither remedy row P75's fork listed**, landing at `C13`. `budget.transactions` carries no `user_id`, so its owner IS its pay period's -- and nothing requires that owner to be its ACCOUNT's, which makes a row filed in a stranger's paycheck EXPRESSIBLE. The fork offered the owner's CALENDAR against a `user_id`-filtered JOIN, and both are ways of ASKING; this makes the question unanswerable by making the state unconstructible. The row gains `user_id` and two composite FKs -- `(account_id, user_id)` against `budget.accounts (id, user_id)`, whose `uq_accounts_id_user` superkey already exists and which `fk_account_external_identities_owner` and `fk_statement_matches_owner` already target that way, and `(pay_period_id, user_id)` against `budget.pay_periods (id, user_id)`, which needs one added. `fk_transactions_reconciled_by` is already a composite FK on this very table, so the shape is in the house. It also retires the NINETEEN hand-written ownership comparisons P75 counts: an owner becomes a column to filter on rather than a relationship each door walks. Measured 2026-08-27: **0 mismatched rows on production and on both dev clones**, so the constraint takes clean. Rejected: the calendar and the JOIN, which leave the state writable and disagree with each other on adjacent doors -- the denormalisation this arc exists to remove |
| pay_calendar | R-PC33 | -- | 2026-08-18 (developer) | **What holds a `tests/manual/` harness to the code it measures: The lint gate, at `E,F` only**, ledger row **P66**. These are the proof instruments every cutover here is verified with, and nothing checked them: pytest does not collect the directory, CI linted only `app/`, and each probe's `_guard` records a raise AS DATA -- so a signature change makes a harness dump `{"RAISED": ...}` where a figure belongs, and a before/after diff of two such runs reads BYTE-IDENTICAL over a region never measured. FOUR sites were already dead when this was found, one since `C2-f2d-3`. `E,F` and NOT the 10.00/10 floor the other three trees hold: these scripts legitimately duplicate one another and branch wide, and what is gated is the class that rotted them. A both-sides harness naming a HEAD-only API carries a scoped disable with a `Pylint:` rationale. Rejected: a pytest smoke test that runs each harness (stronger, but it needs a seeded owner rich enough to reach every probe and the both-sides files would need skips), and leaving it recorded |
| credit_card | R-CC1 | -- | 2026-07-19 (developer) | **Re-account model.** Marking a purchase "Credit" MOVES the transaction to the card account and settles it there: real category, real date/period, real liability. The phantom payback dies. Paying the card is a transfer checking -> card. Undo moves it back, reverts to Projected. |
| credit_card | R-CC2 | -- | 2026-07-19 (developer) | **Full statement cycle.** Card params carry statement-close day + payment-due day + min-payment rule. A statement is a DERIVED snapshot, never stored. Grace: prior statement paid in full by its due date -> zero interest; otherwise a finance charge from the event stream. |
| credit_card | R-CC3 | -- | 2026-07-19 (developer) | **Derived statement payment.** ONE auto-maintained projected transfer checking -> card per statement on the due date; amount live-derived = statement balance minus reward redemptions posted since close (floor 0). Mirrors `derive_from_loan`. Underpayment fires a C7-style warning plus a projected finance charge. |
| credit_card | R-CC4 | -- | 2026-07-19 (developer) | **Cash back: flat rate; redemptions are events.** `cashback_rate` on card params; rewards ACCRUE derived; REDEMPTIONS are real recorded credit events. Manual at any amount, or auto-redeem at `auto_redeem_threshold` (nullable, e.g. $25). |
| credit_card | R-CC5 | -- | 2026-07-19 (developer) | **Migration: freeze history, migrate live rows.** Settled historical Credit pairs untouched. LIVE pairs converted inside the Alembic migration to settled card purchases (real category, original period) + payback deleted. Credit status becomes historical-only (terminal `credit: {credit}`) -- **which `balance:R-HA` makes LEGAL and gives an obligation**: no state may be both REACHABLE and ABSORBING, and this one is absorbing while `projected` loses its edge to it in the same commit, so it is a retired vocabulary item rather than a trapdoor. **What CC3b owes**: `transaction_service.deletion_refusal` names no status, so a terminal `Credit` re-arms the destroy-but-not-correct asymmetry that killed the archive -- either an arm naming it, or a stated reason it is acceptable on frozen card history; old workflow code deleted. Entry-level `is_credit` follows the same rule (split tender). |
| credit_card | R-CC6 | -- | 2026-07-19 (developer) | **Zero-card owners at migration time:** the migration creates a $0-anchor "Credit Card" account inline (account row + origination anchor history + linked/anchor-equity ledger rows, the account-factory invariant satisfied by hand; params-less = dormant plain liability until configured). Deploy never bricks. |
| credit_card | R-CC7 | -- | 2026-07-19 (developer) | **Renames ship in-arc at CC3c** (approval granted; add `Review:` lines): `credit_payback_for_id -> card_charge_for_id`, `TransactionEntry.credit_payback_id -> card_charge_id`, `is_credit -> is_card_tender` (+ index/constraint renames), in the same commit that changes their semantics. |
| credit_card | R-CC8 | -- | 2026-07-19 (developer) | **Finance-charge math: APR/365 daily periodic rate x average daily balance;** when grace is lost, new purchases enter the ADB from their settle date. All Decimal; `round_money` at the boundary only. |
| credit_card | R-CC9 | -- | 2026-07-19 (developer) | **A charged expense stays visible in place** on the source account's grid for its period: a display-only ghost row derived from `charged_from_account_id` provenance (CC badge, $0 balance effect, uncharge affordance) -- never a second stored row. |
| credit_card | R-CC10 | -- | 2026-07-19 (developer) | **The derived statement payment renders on the CHECKING grid as a payable** and deducts from the projected balance in its due-date period (structural: it is a real projected transfer with an expense shadow on checking). |
| credit_card | R-CC11 | -- | 2026-07-19 (developer) | **The companion flow survives intact:** a companion user recording a card-tender entry on an envelope row (`app/routes/companion.py` + the entries blueprint's accessible-transaction path) feeds the card-side transaction end to end while the envelope row + entries stay on the owner's checking grid. Pinned by a companion-access test at CC3c. |
| credit_card | R-CC12 | -- | 2026-07-19 (developer) | Timing shift to expect (accepted): the payment deducts in the DUE-DATE period, which can be later than the old next-period payback; the current period's end balance excluded card activity in BOTH models. A conservative deduct-at-close placement option is a possible later addition; correct timing ships first. |
| recurrence | R-R28 | -- | 2026-08-19 | **Semi-monthly pay keeps the NOMINAL 15-day walk.** No fixed `cadence_days` expresses the 1st and the 15th, `round(365.2425 / 15)` gives the right COUNT and drifting paydays, and a monthly cadence already carries exactly that limitation; a day-of-month schedule KIND is scheduled as **R13** rather than blocking R-F16 on it. Chosen from four options. **What does NOT transfer from the monthly case is the excuse**: a biweekly owner's 27-paycheck year is real, so `round(365.2425 / 14)`'s 0.34% approximates a real phenomenon, where a genuinely semi-monthly owner is paid exactly 24 times every year with zero variance -- so the 1.46% a 15-day walk carries (`24.3495 / 24`) is an artifact the model manufactures, about `$1,334` a year of over-modelled gross on a `$91,675` salary, on top of paydays drifting ~20 days off the 1st and the 15th. R13 is what removes it |
| recurrence | R-R38 | -- | 2026-08-27 (developer) | **WHO opens the read pass a generate pass runs in: The ROUTE does, and the DOOR SPLITS so that it can.** `GenerationSchedule` carries the `BalanceContext` and DERIVES its calendar, so the schedule a rule is resolved against and the pass its bound is resolved in cannot be two values that disagree; each of the eight construction sites was already holding a `calendar_for` beside a `get_baseline_scenario`, which is the pair a pass pins. **The first build had `period_population` open its own, and that was refused as a band-aid** (developer): the root cause is that `extend_pay_periods` performs a write and then a READ-DEPENDENT write in one call, so no caller can get between them, and a pass built before the write holds the pre-write calendar -- which `__post_init__` refuses for `for_period_ids` and silently accepts for `for_pass` -- and the pre-write LOAN, which nothing catches. Splitting the doors lets the route build the pass AFTER the periods exist and BEFORE the rows do, so the ordering is the shape of the code rather than a paragraph, and `pay_calendar:C11`'s predicate needs no carve-out for the GENERATE path -- which is the whole of what this settles: `C11` still carves out `loan_recurrence_sync` and four other service modules still call `BalanceContext.build`, so "nothing under `app/services/` builds a pass" is C11's end state and not this ruling's claim. Safe for all three doors, and order-independent by construction -- every read either posting re-sync makes of `budget.transactions` or `budget.transfers` is keyed on a set of ids taken from the POSTED ledger, and a freshly generated row is `Projected` and posts nothing. Owned by R7d-c-1 |
