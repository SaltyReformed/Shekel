# The rulings

**Every developer ruling of every arc that has moved here, one line each: the RULE, not the
deliberation.** The deliberation belongs in the commit that shipped it, and a ruling whose work has
fully shipped is in the as-built record of the step that shipped it.

**The key is `(arc, id)` and the arc is a COLUMN, never a prefix** (conventions.md rule 10). Two
arcs may hold the same bare id: `balance:R-GU` and `bank_import:R-GU` are two different rulings and
both are LEGAL here. What that costs is a BARE citation of such an id resolving to two rules, so
**a citation must name its arc wherever the id is ambiguous**. No gate grades that; the writer does.

**A NEW ruling id CARRIES ITS ARC'S PREFIX** (developer, 2026-09-05), so the paragraph above governs
the LEGACY ids alone and nothing minted from here can join them. `pay_calendar`, `recurrence` and
`salary` already spelled ids this way and CONTINUE their runs: `R-PC61`, `R-R62`, `R-SAL12`.
`balance` and `bank_import` drew from the shared LETTER sequence, which is no numeric run to
continue, so theirs START at `R-BAL1` and `R-BI1` rather than at their row counts -- `BAL-462`
continues `N-461` because balance's findings WERE numeric, while `R-BAL130` would claim 129 prior
`R-BAL` ids that never existed. **No gate grades the ruling prefix yet**:
`tools/plan_gate/_ident_prefix.py` reads `ledger_rows` only, so until that step ships this is a rule
the writer keeps, which is the fence the ruling exists to delete.

**Why the tables moved.** Ruling ids came from ONE global sequence spelled across five arc documents
in THREE grammars, and `tools/plan_gate` parsed none of them, so a collision was invisible to every
gate: `R-EX` named two rulings in 2026-08-10, and on 2026-08-27 `R-GW` was minted in both `balance`
and `bank_import` on two unmerged branches by sessions that had each checked --
**a `docs/` grep cannot see an unmerged branch.** Holding the pair here makes such a collision legal
and gradeable rather than silent. The lift itself is as-built at `b8f1c862` and `91f95f43`.

**The ruling registry stands at 491 rows.**

**Arcs whose rulings live here: `balance` 196, `bank_import` 79, `recurrence` 78, `pay_calendar` 84,
`credit_card` 15, `salary` 39.** That is every arc, and
**51 of the ids here were MINTED by the lift** -- `R-R39`-`R-R45`, `R-PC2`-`R-PC33`,
`R-CC1`-`R-CC12` -- because three of the five arcs spelled a ruling with no id at all.

| arc | id | also | date | what was ruled |
|---|---|---|---|---|
| balance | R-G | -- | 2026-07-25 | A still-Projected item whose date has passed is CLAMPED FORWARD, never absorbed by an anchor: its instant is `max(attribution date, as_of + 1 day)` |
| balance | R-I | -- | 2026-07-25 | Before an account's first assertion the fold BACK-PROJECTS over the records, holding flat before the earliest one |
| balance | R-K | -- | 2026-07-25 | The grid's balance row and its subtotal rows are ONE step list grouped two ways, plus a named remainder |
| balance | R-N | -- | 2026-07-25 | The cutover ships FIRST; recording when money actually moved is the follow-up (X-f) |
| balance | R-O | -- | 2026-07-25 | The Reconciliation row is "Timing & true-ups", it sits in the sticky footer above the balance, and it renders whenever ANY visible column is non-zero |
| balance | R-L | R-Y | 2026-07-25/26 | Modeled interest and ACCRUAL begin at the account's LATEST assertion's own day, for all modelled kinds |
| balance | R-S | -- | 2026-07-26 | An ASSERTION always wins, and before the FIRST one the balance holds FLAT, for all five kinds |
| balance | R-T | R-X | 2026-07-26 | ACCRUAL events are DAILY, resolved in ONE sequential pass, computed at full precision and credited in whole cents carrying the remainder |
| balance | R-U | -- | 2026-07-26 | The replay owns the SEED and the history; the forward WHAT-IF surfaces keep `growth_engine` |
| balance | R-Z | -- | 2026-07-26 | A modelled CONTRIBUTION lands on its pay period's `start_date` and exists only when that payday is STRICTLY AFTER the latest assertion |
| balance | R-AF | -- | 2026-07-27 | The `/investment` chart's projection axis starts the day AFTER the history line's last valued date, so the two lines join BY CONSTRUCTION |
| balance | R-AG | -- | 2026-07-27 | Past the pay-period horizon, let the fold answer and RECORD the half-model rather than capping it |
| balance | R-AK | -- | 2026-07-27 | The dashboard, the pulse and the analytics calendar STAY on the kind-blind cash view; the divergence is RECORDED, and X-j is the step that rules it |
| balance | R-AO | -- | 2026-07-27 | The homeless half of the findings ledger becomes FOUR named steps, and two existing steps widen to absorb the rest |
| balance | R-AZ | -- | 2026-07-27 | A producer publishes only what the presentation boundary reads |
| balance | R-AP | -- | 2026-07-27, NOT as recommended | The write-side recurrence cluster STAYS in this arc, as plan step X-k |
| balance | R-BU | -- | 2026-07-28 | A residual double load is SEQUENCED behind the memo that fixes it, not deferred |
| balance | R-CH | -- | 2026-07-30 | The archived drawer's figure is NAMED for what it is |
| balance | R-CU | -- | 2026-07-30 | The ledger-class rule is finding N-122 with its OWN step (X-ab), and the false claim is corrected NOW |
| balance | R-CX | -- | 2026-07-31 | X-x is scoped to what X-l cannot subsume; the degraded-vocabulary question is SEQUENCED into X-l |
| balance | R-CY | -- | 2026-07-31 | The no-current-period answer is X-v's rule EXACTLY: raise a named error, handle it once, offer a repair |
| balance | R-CZ | -- | 2026-07-31 | A requested WINDOW that is empty is navigation, not absence, and stops answering with the absence card |
| balance | R-DA | -- | 2026-07-31 | `onboarding.has_periods` means "a period covers today", the same question every other surface asks |
| balance | R-DE | -- | 2026-07-31 | X-x is HELD behind X-ad: a refusal that points at a repair which does not work converts wrong numbers into a dead end |
| balance | R-DH | -- | 2026-07-31, amended at S1-c | An assertion is the CLOSING BALANCE for its civil day, and the day is the user's, not UTC. Six parts (a)-(f); (d) restated on an OBSERVED `settled_on`, so a NULL is "not seen on a statement" and is NOT reconciled |
| balance | R-DK | -- | 2026-08-02 | The posting self-heal stops being delta-keyed, which dissolves the reason `_reconcile_postings_after_update` gives for existing (N-153) |
| balance | R-DM | -- | 2026-08-02 | The checked-projection assert grades a FINISHED operation, never a half-finished one, and the commit boundary is the end state (X-ai) |
| balance | R-DN | -- | 2026-08-02 | ONE status seam, and the transition context stops being a caller's opinion |
| balance | R-DO | -- | 2026-08-02 | An illegally-drifted shadow status is REFUSED, not silently repaired |
| balance | R-DP | -- | 2026-08-02 | W9907 is DELETED, not shrunk -- the write door becomes structural |
| balance | R-DQ | -- | 2026-08-02 | The two remaining allowlist-bearing fences become their own phase (G), scheduled rather than remembered |
| balance | R-DR | -- | 2026-08-02 | `restore_transfer`'s four preconditions move to `_transfer_validation`, and X-aj1 ships as three commits |
| balance | R-DT | -- | 2026-08-03 | Reversing a documented deliberate decision is its own change and is not bundled into a writer swap (N-153 stays out of X-d) |
| balance | R-DU | -- | 2026-08-03 | The posted ledger gets ONE VERB and ONE TRIGGER, on BOTH ledgers, and the row-level posting writer stops being the interface |
| balance | R-DV | -- | 2026-08-03 | A journal entry is the projection of exactly ONE SOURCE EVENT, and the EVENT owns it. An account is the SCOPE of a re-derive, never an owner |
| balance | R-DW | -- | 2026-08-03 | A transfer's entry has ONE valuation; a broken Transfer Invariant 3 becomes an ASSERT failure rather than a write oscillation |
| balance | R-DY | -- | 2026-08-03 | The source identity is an EXCLUSIVE ARC of typed FKs with an AT-MOST-ONE check, never an exactly-one check |
| balance | R-EA | -- | 2026-08-03 | An anchor correction books in the pay period CONTAINING the day it asserts, DERIVED through the one function both ledgers share -- never read from the source row's stored `pay_period_id` |
| balance | R-EB | -- | 2026-08-03 | The anchor half is redesigned from scratch: the cash ledger is SUM-OF-POSTINGS, an assertion is a RECONCILIATION not a reset, the residual posts to Uncategorized Expense / Income, and bank import (X-f6) is the sequenced follow-on rather than an alternative |
| balance | R-EE | -- | 2026-08-03 | The settle door stays ONE CLICK, and the true-up form gets its own statement date |
| balance | R-EH | -- | 2026-08-03 | `accounts.current_anchor_balance` and `current_anchor_period_id` are DELETED |
| balance | R-EK | -- | 2026-08-04 | The resolver's history/projection cut is a PROXY for the day the money moved; replacing it is X-an, not X-f1 |
| balance | R-EM | -- | 2026-08-04 | The four "no current period" fallbacks ASK THE SEAM; they do not read a stored balance |
| balance | R-EN | -- | 2026-08-04 | The C-17 optimistic lock leaves the cash true-up, because the true-up stops writing the row it locked. **The ruling STANDS and its RATIONALE was corrected (N-190)**: "a second tab overwrites nothing" is true of one table in a transaction that mutates three, and the deleted column had been serialising the reconcile by accident. Replaced by one per-user advisory lock inside the reconcile, both ledgers, all four entry points |
| balance | R-EO | -- | 2026-08-04 | `account_anchor_history.pay_period_id` is DELETED. An assertion is (account, day, balance) and nothing else |
| balance | R-ER | -- | 2026-08-04 | The rule that a civil day is ASSERTABLE belongs to the module that owns what an assertion IS (`anchor_service`), now that a second writer asks it -- and **"the user has a pay schedule at all" SPLITS OUT of it** as account creation's own precondition, because ruling R-EO falsified the reason it gave and the live one is the opening's posting reconcile (**N-192**) |
| balance | R-EQ | -- | 2026-08-04, DESIGNED FROM SCRATCH rather than re-keyed; **horizon SHARPENED at the build** | **An assertion is refused only when it changes nothing.** The duplicate-submit rule is a comparison against the assertion GOVERNING THE DAY THE SUBMISSION ASSERTS, made in the write door under the per-owner lock that door already takes, on BOTH anchor tables; the content-keyed unique indexes are DELETED. Idempotency is a property of a REQUEST, not of the row's contents, so a content key must mis-classify either a retry or a deliberate re-assertion -- and the two errors are not the same size. **The horizon is the rule, not a detail of it**: this ruling first read "the account's CURRENT latest assertion", which has the index's fault mirrored -- a submission for an EARLIER day can never equal the latest, so every back-dated retry appends a permanent row |
| balance | R-ES | -- | 2026-08-05 | **`account_anchor_history.notes` is DELETED and the origination assertion goes through the SAME write door as every later one**, so the table has ONE writer. An assertion is (account, day, balance) and nothing else -- the sentence ruling R-EO already wrote into the model. Measured: no code in `app/` READS the column (AST census), 76 of 78 production rows are NULL, and it labels the origination on 2 of 9 accounts. It is a SECOND answer to a question the app already decides positionally (`CashAnchorFact.is_opening` -> `account_opening` / `account_trueup`), and the forensic trail it was nominally for is `system.audit_log`, which records every INSERT with the full row and the acting user. The loan twin's typed `source_id` STAYS: that one is read |
| balance | R-ET | -- | 2026-08-05 | **Feedback about a WRITE mounts where no refresh region owns it; a caption about STATE stays with its surface.** The two had shared one element, so the transient fact inherited the durable one's per-surface mounting and was destroyed by the durable one's own refresh. Corollary ruled at the same build: **an affordance that cannot succeed is DELETED, not given a nicer refusal** -- a loan's balance cell renders read-only rather than answering a designed error |
| balance | R-EU | -- | 2026-08-09 | **The true-up form compares the LEDGER's balance for the day it names against what the user typed, live.** Not the last asserted balance -- that figure is already the box's prefill, and the difference that matters is the reconciliation gap the grid's Period timing / Book vs bank rows diagnose. Rejected: a static book figure with the subtraction left to the reader, and a JS-computed difference (money math in the browser) |
| balance | R-EV | -- | 2026-08-09 | **An assertion gets a DURABLE home before its acknowledgement is re-keyed.** The loan page has listed every anchor with its drift since Commit 16 and the cash side lists none, so the fix for "nothing records that this landed" is the missing card, not a longer-lived toast. The 8s autohide STAYS as ruled at X-f1e3; what changes is that the toast stops being the only evidence |
| balance | R-EW | -- | 2026-08-09 | **The reconcile panel offers EVERYTHING the statement can settle, grouped by the thing it belongs to, and the two acts stay independent.** A purchase nests under its own envelope and ticking it settles that purchase ALONE; closing the envelope is a separate tick in the same block. Rejected: grouping by act-type (which separates a grocery purchase from the grocery envelope), a flat undifferentiated list, and an editable close amount (a second writer of `actual_amount` beside the entries that derive it) |
| balance | R-EX | -- | 2026-08-10 | **Registration ASKS rather than inventing**: the most recent payday, the cadence and the horizon, on the sign-up form. The bound is that the paycheck the stated payday opens must still be running, which is what makes sign-up day fall inside period 0 with no second step. The seed script REFUSES to run without a payday rather than defaulting one -- there is no honest default for the day somebody was paid, and defaulting it would put the fabrication back on the one path that provisions the production owner |
| balance | R-EY | -- | 2026-08-10 | **The interior hole's repair is pay-calendar `C3`'s DERIVED index, not a relaxed predicate in X-ad, so N-127 MOVES to that arc and `X-x` gains `C3` as a blocker.** R-DE had asked for "an overlap bound that permits a batch which fills a hole"; relaxing that bound alone is unsafe, because `generate_pay_periods` seats new periods at `max_index + 1` and a hole-filling period would take the LAST ordinal while carrying an interior date. `filing_period`'s own docstring names that as its precondition -- the two filing rules part company on **800 of 872 probed days** once `period_index` order stops matching `start_date` order -- and 21 `app/` files read the ordinal as chronological. C3 makes the writer DERIVE both, which fills a hole by construction and deletes the predicate instead of widening it. Rejected: building an interim renumbering pass in X-ad that C3 then deletes, and folding C3 into X-ad (two money-touching changes and a schedule rewrite in one PR) |
| balance | R-EZ | -- | 2026-08-10 | **An AUTOMATIC writer never creates a pay period that has already ended** (N-124), and **X-ad DECOMPOSES** into the door that CREATES a calendar and the door that GROWS it. Rejected for N-124: filling the whole lapse in one pass rather than converging over page loads (still manufactures history the owner never saw), and resuming at the next payday on or after today (writes no false history but opens exactly the calendar hole this arc exists to make impossible) |
| balance | R-FA | -- | 2026-08-10 | **What settling a row MEANS is a service verb, and the reconcile tick calls the same one the grid's Mark Paid does.** The rule lives today in two route branches -- `mutations.py:552` (envelope-with-entries settles at sum(entries), everything else keeps its stored amount) and `_shadow_mutations.py:209` (a loan-payment shadow freezes the live payment-date amount first) -- and BOTH are load-bearing for the panel: production's `Kayla's Spending Money` carries no entries, so an unconditional `settle_from_entries` books `$0.00` against a `$100.00` estimate, and 2 of the 4 transfer shadows the replay offers are loan payments (`$1,910.95`, `$531.94`) where skipping the freeze restores the creation-time escrow. Rejected: the writer re-stating both branches (this arc's own root cause 1, on a money rule), and the tick calling the mark-done HTTP endpoints (one request per row, no atomicity, and no channel for the statement date) **Recorded as R-EX** (recorded as **R-EX** in commit `daa9c402`, which cannot be edited; renamed 2026-08-10 on discovering that X-ad's own ruling had taken that id the same day, from another session. The id namespace is NOT graded -- see **N-217**) |
| balance | R-FB | -- | 2026-08-10 | **A BILL's tick MAY correct its amount; an ENVELOPE's close still may not.** The panel renders a bill's figure as a prefilled input: ticking it untouched settles at the stored figure, so the one-click habit **R-EE** protects is unchanged, and a different figure is written through the optional actual R-FA's verb ALREADY carries -- a second DOOR onto one rule, not a second rule. **R-EW's reason for refusing an editable close does not transfer**: an envelope's `actual_amount` is DERIVED from its entries, so a box there would be a second WRITER of a derived value, and a bill has no entries. Measured read-only on production 2026-08-10: **11 of 93 settled bills carry a hand-typed correction, `$252.79` gross** (Electricity `$300.00` -> `$245.32`, T-Mobile Fiber `$70.00` -> `$50.00`, Geico `$178.32` -> `$165.22`), and **4 of the 30 bills the panel would have offered** over Checking's 53 assertion days, `$40.16`. Rejected: settling at the estimate and leaving the difference to X-f3's residual, which books KNOWN money as Uncategorized Income and overstates the real category by the same amount -- feeding the very bucket X-f3 exists to shrink, at the one moment the user is holding the statement that says the true figure; and linking each row out to the full-edit popover, which is a second request and loses the reader's place mid-statement |
| balance | R-FC | -- | 2026-08-10 | **The offer set is ONE collection of blocks; a block with no children RENDERS as one row.** The developer chose the flat-bills layout on sight, and measuring why turned the fork into a non-fork: the whole readability difference was RENDERING, so three presentational rules -- a childless block prints inline instead of as a heading above a one-item list, the sort key gains a kind term, and a section label is emitted when the kind changes -- reproduce the preferred panel byte-for-byte over one collection (checked by normalising the two mockups' markup, not by eye). So X-f2-c2 and X-f2-c3 each ADD an arm and the assembler's ordering, heading, count and empty rules are never rewritten inside a money commit (**R-EY**). Rejected: two collections held side by side, which buys an identical screen for two ordering rules, two heading rules and a two-collection empty test. **One argument for this ruling was WITHDRAWN before it was made**: that a shared section label breaks once an offer set spans two pay periods -- true in principle, but no assertion day in production has ever had one, and across 93 settled bills the worst overdue is 12 days against a 14-day period with ZERO past a whole period |
| balance | R-FD | -- | 2026-08-11 | **The panel offers INCOME rows too: a deposit you are still waiting on is exactly what a statement settles.** The settle verb already books Received for income (`transaction_service.settled_status_id`), so the cost is the panel's VOICE -- "tick everything your statement shows" rather than "has your bank taken these" -- and a summary that counts deposits and payments separately. Measured read-only on production 2026-08-11, replaying all 53 Checking assertion days: **3 income rows would have been offered, `$2,304.27`**, the largest an FSA reimbursement of `$1,958.87`. The paycheck has never been in the offer set: it is marked Received the day it lands, so no assertion has caught it still Projected. Rejected: expenses only, which keeps one voice and leaves `$2,304.27` of real reconcilable money tickable only from the grid |
| balance | R-FE | -- | 2026-08-11 | **A settle books the FRESHEST derivation of the row's own amount, and the rule lives in the VERB so every door books the same figure.** Absent a caller-supplied actual: `sum(entries)` for an envelope carrying entries (unchanged), the projection's own live derivation where one exists, the stored `effective_amount` otherwise. This is its own step **X-aq** and it MOVES MONEY at the grid's Mark Paid, so it runs BEFORE X-f2-c2's money commit -- otherwise the new panel ships displaying a figure the grid contradicts. **The ROOT cause it does not fix is a denormalization**: `transactions.estimated_amount` is a CACHE of a derivation, `income_service.live_projected_net` repairs it at READ time and writes nothing back, so readers and writers hold two answers with no reconciler. Dissolving that -- one stored amount kept true by a reconciler on input change and at deploy, deleting `live_amount_overrides`, `ProjectedBasis.amount_overrides` and the whole override thread through `sum_projected` -- is step **X-ar** and closes **N-40** and **N-224**. Rejected: deciding the question inside the reconcile panel, which makes one row book two figures depending on which control was used -- the exact defect this arc exists to remove |
| balance | R-FF | -- | 2026-08-11 | **A tick's amount is CORRECTABLE exactly when the settle verb takes its MANUAL branch**, i.e. when the row is not (envelope-tracked AND carrying entries). One predicate, read off the verb's own branch, rather than a second classification kept in sync by hand. It makes **R-FB** true by construction -- an envelope with entries is the derived case R-EW protects -- and answers the case neither ruling names: production's `Kayla's Spending Money` is envelope-tracked, budgeted `$100.00` and carries ZERO entries, so the verb already treats it as manual and there is no derived value to protect. Rejected: correctable iff not envelope-tracked, which books that `$100.00` blind and sends the only correction path back to the full-edit popover R-FB rejected |
| balance | R-FG | -- | 2026-08-11 | **A row is offerable when EVERYTHING it would book had moved by the statement day, and that is the whole bound.** Ruling N-227: the attribution bound STAYS. The bound was challenged because an envelope has no meaningful landing day of its own, so its close is offerable for the whole period it is being spent in; the proposed remedy `period_end <= observed_on` is **REFUTED by the owner's own behaviour** -- measured on production, they close envelopes DURING the period **21 of 33 times (64%)**, as early as 9 days before period end, mean 2.7 days before, so it would have refused two thirds of the closes they really made and empties today's panel. Two of N-227's own claims were also WRONG and are withdrawn: its stated mechanism ("an envelope carries no `due_date` so `attribution_date` falls back to the period start") describes **4 of 231** envelope rows, while **185** carry an explicit `due_date` EQUAL to the period start -- a value the recurrence engine writes, not a fallback; and its hazard ("closing early makes every later purchase on that row inert") is FALSE for the `Paid` status the close actually writes -- `entry_service._update_actual_if_paid` recomputes `actual_amount` and `_resync_postings_if_settled` re-posts, which an existing green test and 24 of 24 production rows both confirm. What the challenge correctly identified is a DIFFERENT defect, and it is fixed structurally rather than by a calendar bound: an envelope's value is an AGGREGATE, so it could book a purchase the statement never saw. `_wholly_spent_by` is that fix. Rejected: narrowing the post-true-up modal to statement-observable rows, measured to move the prompt from 46 of 48 assertion days to 42 -- a UI palliative for a bound that is now correct |
| balance | R-FH | -- | 2026-08-11 | **A settle's refresh of a derived amount writes the CACHE (`estimated_amount`); `actual_amount` receives a HUMAN's figure and nothing else.** Amends **R-FE**, whose first implementation wrote the live derivation to `actual_amount`. That column's NULL-ness is read as "a human entered this fact" by three subsystems -- `income_service` (a settled income row's actual is never a recomputable projection), `spending_analysis` (only an explicitly entered actual is a surprise, so a machine write manufactures one) and the grid cell (which strikes through the estimate whenever the two differ, rendering a figure the owner never saw). The write was also PERMANENT and unlabelled: the row leaves `live_projected_net`'s Projected-only candidate set at that same flip, so the stale estimate could never be repaired afterwards and **X-ar's reconciler could not have told a machine write from a real correction**. Writing the cache instead makes X-aq X-ar's FIRST INCREMENT rather than a compensating layer -- the same column, one trigger instead of several -- and leaves the booked figure, `effective_amount` and R-DH (c)'s invariant unchanged. `is_override` is deliberately not set; the recurrence engine's `resolve_conflicts` rewrites `estimated_amount` with it False for the same reason. Rejected: keeping the single column and teaching the three readers to tolerate it, which spreads one denormalization into three |
| balance | R-FI | -- | 2026-08-11 (amended the same day by the two adversarial reviews it was written against) | **A row's amount is either its OWN -- a human authored the figure, or the money moved -- or it is DERIVED, and a DERIVED AMOUNT IS NOT STORED.** Two rules, the second following the first: every financial quantity that VARIES OVER TIME is a dated series resolved as-of; every quantity DERIVED from those is not stored at all. **The discriminator is an EXPLICIT `amount_source`, and the first draft's link-derived one was REFUTED by tracing**: the rules are not a partition over `template_id` / `transfer_id`, because salary is a SUBSET of template, loan payment is a SUBSET of transfer, and a CC payback row carries NEITHER link while its amount is derived -- so the proposed `CHECK (estimated_amount IS NOT NULL OR template_id IS NOT NULL OR transfer_id IS NOT NULL)` would have made storing THAT derived value structurally mandatory, which is the ruling inverted. A declared source cannot go stale against the links because the links never decided the rule. The CHECK becomes `(amount_source = OWN) = (estimated_amount IS NOT NULL)`, so a derived row is structurally incapable of holding a figure that can go stale and a reader that skips the resolver gets `None` rather than a plausible wrong number. The kinds this arc moves are salary, template, transfer, loan payment and the card payment `credit_card:CC4b` plans as another override-map entry; **they are not the whole census** -- about twelve stored-derived monetary values exist, which is **N-243**, and the arc SEQUENCES the rest rather than claiming to have found them all. **This SUPERSEDES R-FE's remedy and step X-ar**, whose "one stored amount kept true by a reconciler" preserves the denormalization, rests forever on trigger completeness, and moves salary in the OPPOSITE direction from the other four; **X-aq (`9cabc206`) survives as the FREEZE** -- the moment a derived amount becomes a fact. Rejected: reconciler-plus-detector, which needs a detector precisely because staleness stays representable; and deriving a template row from `template.default_amount` directly, a scalar with no time dimension, so a June price change retroactively rewrites March. The evidence is that the codebase already obeys rule 1 for loan rates (`RateHistory`), escrow (`EscrowComponentVersion`), tax years and raises (`apply_raises`), and obeys rule 2 nowhere |
| balance | R-FJ | -- | 2026-08-12 (developer) | **An envelope with NO purchases settles at its BUDGET through a door and at `$0.00` through carry-forward, and the discriminator is the CALLER's ACT, not the row.** Finding **N-230**: `transaction_service.settle_transaction` gates its entries branch on `tracks_purchases AND entries`, so an entry-less envelope takes the MANUAL branch and books `effective_amount`; `settle_from_entries`, which `carry_forward_service` calls directly, books `Decimal("0")` for the identical shape. Both are right, because the two callers have done different things to the money. **Carry-forward has already RELOCATED it** -- it rolls `estimated - sum(entries)` into the next period's row and then settles the source at what was spent, so booking the estimate there would count the same dollars twice. **A DOOR has relocated nothing**: pressing Paid says the budget is finished, and booking `$0.00` would record that no money left the account while marking the row Paid. Production carries BOTH signatures, which is how the split was found. What was not defensible, and is what this ruling fixes, is that the difference was undocumented and fell out of which function a caller happened to reach for; both functions now state it. **AMENDED 2026-08-12, the same day, by the adversarial review it was written against: the rule holds at the SETTLE MOMENT and nothing holds it afterwards.** An entry-less envelope settled either way is left `Paid`, not archived, so a purchase logged later re-derives `actual_amount` from the entries and re-posts -- reversing the door's "the budget is finished" reading, and spending money the carry-forward branch has already relocated. The ruling stands for the act it governs; reconciling the rollover it divided is **N-249** / step **X-ax**. Rejected: an unconditional `sum(entries)` for every caller, which hands already-spent money back to the projection (the reading **R-FA** already refused); an unconditional estimate, which makes every rollover count its envelope twice; and refusing an entry-less envelope settle outright, which breaks the Paid button for every entry-less envelope row sitting Projected on the grid |
| balance | R-FK | -- | 2026-08-12 (developer) | **The amount discriminator names the RELATION that prices a row -- its recurring DEFINITION, or its PARENT TRANSFER -- and never the RULE that computes the figure.** Amends **R-FI**'s enumeration, not its rule: the column is still an EXPLICIT declaration paired with the presence of a figure by a CHECK, and a CC payback still declares a relation while storing nothing. What changes is that SALARY-vs-TEMPLATE and LOAN_PAYMENT-vs-TRANSFER are properties of the DEFINITION rather than facts about the row -- a template is salary-linked when an ACTIVE profile names it, a transfer template is a loan payment when it holds a settings row -- so storing them on every generated instance is a denormalization with an update anomaly that TWO LIVE routes trigger (`loan.payment_transfer.track_payment` creates the settings row a legacy manual payment never had; `salary.profiles.delete_profile` archives a profile, the moment a template stops being salary-linked). Both would need a writer that rewrites every affected row, which is the shape this arc exists to delete. **The OWN state is the ABSENCE of a source rather than a value**, which is what lets the CHECK be `(amount_source_id IS NULL) = (<amount> IS NOT NULL)`: a constraint over two NULL-nesses with no `ref.amount_sources` id frozen into the schema, where the ref-table convention otherwise makes an id-free pairing inexpressible. Rejected: the five RULE values as R-FI first enumerated them, for the update anomaly; a plain boolean, which the CHECK makes logically equivalent to the amount column's own NULL-ness; and dropping the column so NULL alone means derived, which leaves a writer that sets a figure on a derived row indistinguishable from a hand edit. **AMENDED the same day by the adversarial review it was written against**: under a RELATION the salary-linked rows re-price to `default_amount`, a scalar `template_amount_service` itself calls vestigial. The ruling stands and the remedy moves to the write DOOR (**N-261**): an archive states the price its rows were generated at, or refuses. **What this design does NOT police, stated because an unstated limit reads as stronger than it is**: a declaration naming a relation the row does not carry is refused by the resolver rather than by the schema (all three link FKs are `ON DELETE SET NULL`, so the DATABASE can strip one); ownership INFERRED from `is_override` or from leaving Projected is invisible to the CHECK (**N-262**); a second derived amount on one row is inexpressible (**N-265**); and a producer reached from neither link nor definition needs a relation this set does not hold (**N-263**, **N-264**). **Two costs are accepted rather than denied**: a definition-level flip re-prices N rows with no row-level `system.audit_log` entry; and the mode booleans the refinement reads (`is_salary_linked_template`, `derive_from_loan`) are UNDATED, so archiving a profile today changes what a March row is worth -- R-FI rule 1 applied to the discriminator itself is a dated mode series on the definition, the deeper fix neither enumeration is. Also rejected: a composite FK from `(amount_source_id, <amount> IS NOT NULL)` to a `(id, is_own)` unique key, which keeps NOT NULL and freezes no id but closes none of the four gaps |
| balance | R-FM | -- | 2026-08-13 | **A purchase that has CLEARED is a cash posting; an envelope's close books only what its cleared purchases did not.** Forced rather than chosen: R-DH (c)'s invariant -- recording a purchase and truing up by the same amount must not move the projected end balance -- holds today because the anchor RESET drops the balance by exactly what the entry reservation releases (`_entry_aware_amount`'s `max(estimated - settled_debit - credit, outstanding_debit)`). Delete the reset without this and ticking a `$150.27` purchase releases `$150.27` of reservation against a book that never moved, so the projected end balance rises by it. It is also the grain the bank speaks in: of 110 movements matched to the developer's bank lines, **58 are individual purchases and 52 are transaction rows**, and an envelope row has no bank counterpart at all |
| balance | R-FL | -- | 2026-08-13 (developer) | **Whether a line is INSIDE a declared balance is a RECORDED FACT, not a comparison of two dates.** A transaction and a purchase each name the `account_anchor_history` row whose statement showed it; `ReconciledThrough.covers` and its date rule are DELETED from the cash side. This REVISITS **R-DH (a)** and **R-DH (d)**, and the developer's own bank exports are why: most Checking assertions do NOT equal the bank's closing balance for their day, several are that day's closing plus a subset of its own postings (a balance read mid-morning), and the rest are neither -- so an assertion is NOT the closing balance for its civil day, and no rule comparing two dates can decide what a statement showed. R-DH (d) deleted a stored `is_cleared` BOOLEAN as "a denormalized copy of a derivable fact", and that premise is what falls: clearing is not derivable. The replacement is not that boolean returning -- it is a LINK, which carries provenance (WHICH statement), has no date of its own to drift from the assertion's, and answers "what did this statement clear" as one indexed read. **The MODELLED side keeps a date rule and that is not an exception**: a payroll contribution (**R-Z**) and a modelled accrual (**R-L** / **R-Y**) are not lines anyone can tick, so there the assertion legitimately outranks the model (**R-S**) and the question is genuinely "is this payday after the latest assertion". Rejected: sharpening the date rule with the bank's posted day, which still cannot tell a mid-day reading from a late-posting item -- the two carry the same civil day. **AMENDED 2026-08-13, the same day, by the adversarial review it was written against: the DIAGNOSIS stands and the IMPLEMENTATION does not.** A link cannot simply replace `covers`, for three measured reasons. (1) `dated_deltas` emits EVERY source fact and only the correction's delta cancels the absorbed ones, so a settled row dated at or before an assertion that carries NO link is counted TWICE -- the `prefix = anchor + X` class the day partition exists to make unspellable. The link must therefore SHARPEN a default that still clears everything dated on or before, not replace it. (2) A link partition is not a PREFIX, and both absorb loops are monotonic-pointer prefix scans whose own docstrings state that precondition; assertion B legitimately clears a line dated before one of A's, which halts the pointer and silently shorts the ledger. (3) The POSTED walk has no row to read a link from: `_source_net_days` returns `(day, net)` aggregates read off the LEDGER, one of whose three partitions is residue with both source FKs NULL by construction. **X-f3a is under-specified until all three are ruled** |
| balance | R-FN | -- | 2026-08-13 (developer) | **An assertion is a CHECK, and an unexplained difference is a transaction the user ACCEPTS -- never an automatic plug.** It is an ordinary row with NO category, so it books to the per-owner Uncategorized Expense / Income ledger account through the category-leg rule that already exists (`ledger_account_service.get_or_create_category_ledger_account`), needs no new ledger-account kind, and is CATEGORIZABLE later -- which is the mechanism that shrinks the bucket. **This amends R-EB's remedy, and the measurement is why -- restated 2026-08-13 by the adversarial review it was written against, which REFUTED the first two forms of it.** *"92% of it cancels"* is an algebraic identity rather than a finding (the plug is DEFINED as what forces the ledger to the asserted balance, so it telescopes and the net is a function of the LAST assertion alone), and the *"two independent instruments"* are one and the same subtraction. **What survives is the real evidence**: adjacent assertions systematically undo one another (centred lag-1 autocorrelation `-0.306`, the 30-day bucket collapsing at the 1.8th percentile of a random-sign null), which is the signature of date misplacement rather than of spending; and the BANK-anchored untracked spend is several times the figure this ruling was first sized against. Booking the gross automatically would record over `$1,500` of "spending" on one day and a comparable "income" the next, either side of the mortgage payment that caused both. **The manual path is PRESERVED and that is the developer's own bound**: typing a balance away from a computer must still move the projected end balance, and it does -- in one accepted act that leaves a row you can find, instead of a plug you cannot. Rejected: automatic recording (books the churn), and leaving the difference underived (the projection then silently lags the bank) |
| balance | R-FO | -- | 2026-08-14 (developer) | **What a balance assertion's difference MEANS is a property of the ACCOUNT, and the counter leg is a TOTAL DISPATCH over `classify_account` -- not a carve-out.** `PLAIN` books it to Uncategorized once X-f3c makes it a recorded, user-accepted transaction (**R-FN**) and stays on `anchor_equity` until then, because classifying it automatically is what R-FN refuses; `INTEREST` books per-account **Interest Income**; `INVESTMENT` and `APPRECIATING` book per-account **Change in Value** in a SIXTH reporting class of their own, NAMED for what it measures rather than for a gain (developer 2026-08-14; **N-277**); `AMORTIZING` is untouched, a loan having its own correction family and its own chart. Five kinds, five answers, one function. **This is N-171 on a different account class, and it is LARGER**: `ref.posting_sources` holds no accrual, so nothing posts modelled interest or modelled return anywhere -- the true-up correction is the ledger's ONLY trace of them, and it lands in equity. Measured 2026-08-13, no interest, dividend or investment account or category existed at all, and thousands of dollars of return earned over 4.5 months were invisible on the income statement. **The RESET stays on the modelled kinds, and R-EB's own reasoning is why**: it deletes the reset because *"the reset discards what the records say"*, and an IRA has no record of a price movement to discard -- the assertion is the only fact, so the reset IS mark-to-market and re-deriving the correction each read is CORRECT (a contribution back-dated under an assertion rightly shrinks the gain). **The sixth class is not decoration**: `net_income = income - expense`, so a house revaluation booked to Income would read as money earned. Unrealized gain is other comprehensive income and is reported below the line. Rejected: one combined return account, which tells the owner the bank paid them the gain; and removing the reset on the modelled kinds, which breaks **R-S** for two account kinds with nothing to replace it and makes the app's IRA balance stop equalling the statement |
| balance | R-FQ | -- | 2026-08-14 (developer) | **Clearing is RECORDED now and only becomes AUTHORITATIVE at the cutover, so X-f3a splits in two.** The three answers R-FL's own amendment demanded, plus what measurement then forced on top of them. (1) A line naming no statement is UNKNOWN and the DATE rule answers it, so the link SHARPENS rather than replaces -- which is what keeps `dated_deltas` from counting an unabsorbed source twice. (2) Both walks stop being monotonic PREFIX scans: the rule answers per line and the walks GROUP by the answer, because a pointer meeting an out-of-order link halts and shorts every later assertion. (3) The posted walk reads a link where its source rows carry one and falls to the date rule where they cannot -- RESIDUE, whose entries carry both source FKs NULL by construction. **And the fourth answer is a THEOREM the first implementation got wrong.** While an assertion RESETS the ledger a link may NOT disagree with the day: a balance is the prefix sum of `dated_deltas`, which emits a source on its settle day and a correction on its statement's, so `prefix(statement day) = asserted balance` holds only where every line's clearing statement is the one the date rule picks. One source linked to a later assertion renders a balance below the asserted one on the assertion's own day, breaking **R-S**; the mirror direction reads `anchor + X`. So a link may only choose between assertions SHARING a civil day -- which is exactly what the panel writes -- every door that moves a settle day RELEASES the link, and `StatementCoverage` REFUSES what is left rather than rendering a balance that contradicts the user's own statement. **X-f3c is what lifts it**, because an assertion stops resetting anything there. The link is scoped by a COMPOSITE foreign key over the account rather than by a write-door convention, and NOTHING is backfilled: backfilling from the date rule would launder a measured-wrong guess into an observation nobody made, and X-f6a fills history from the bank. Rejected: letting the link outrank the date now, refuted by the measurement above; and refusing the day EDIT instead of releasing the link, which traps a user against the panel's own copy |
| balance | R-FR | -- | 2026-08-15 (developer) | **A purchase is a POSTING SOURCE of its own, and what makes it one is the DAY THE BANK TOOK IT.** The four answers **R-FM** left open, taken together because each decides the next. (1) The TRIGGER is `settled_on IS NOT NULL` on a debit purchase -- the same fact that makes a TRANSACTION an actual event, asked of the row in front of it -- and NOT the clearing link nor `StatementCoverage.is_cleared`. That is what *"a purchase that has CLEARED"* means: cleared the BANK, not cleared by a statement. It also DISSOLVES **N-286** rather than building it: no consumer needs to tell a record-cleared line from a date-cleared one, because which statement absorbed the movement is the walk's question about that movement, asked identically of both kinds. Rejected: the link, which carries 0 rows today so the step would move no money and leave **N-274** open until the bank import; and `is_cleared`, which would leave a purchase the bank took after the last assertion held as reserved BUDGET rather than booked on the day the money left. (2) The COUNTER leg is the ENVELOPE'S OWN category, so the expense is recognised in the right category on the day it happens and the close books the remainder to the same account. Rejected: Uncategorized until the close, which shows an open envelope's real spend as uncategorised on the income statement and makes every close write a reclassification pair. (3) A purchase gets its OWN journal entry -- `journal_entries.transaction_entry_id` plus a `purchase` source kind -- because the posted walk reads a source's DAY and its CLEARING LINK off the source row, and both are per-purchase; legs grouped under the parent's `transaction_id` would be dated at a `settled_on` a still-projected envelope does not have. It is also the grain the bank speaks in (**R-FM**). (4) A FUTURE `settled_on` on a purchase is REFUSED at the write door, which INVERTS the reason it had no upper bound: a forward day was conservative while a purchase was not a cash movement, and now releases the reservation today while booking the cash later -- already-spent money back in today's projection, the exact shape `status_seam.reject_future_settle_day` refuses on a transaction. Nothing expressible is lost, because an unobserved purchase leaves the day NULL |
| balance | R-GU | -- | 2026-08-26 (developer) | **A request is a QUERY or a COMMAND, and its TRANSACTION says which** -- the snapshot `X-i3` is about is taken at the REQUEST, not at the read pass. GET and HEAD run at `REPEATABLE READ, READ ONLY`; every other method keeps `READ COMMITTED`, writable; a GET that must write says so in one `write_transaction()` block that commits before the render resumes. **Two things the trace refuted, and each rules out the obvious design.** (1) The PASS cannot decide: `apply_statement_review` and `loan_recurrence_sync` both build one before they write, so a pass has no way to tell a render from a write's own decision basis and would need a second constructor -- "an argument a caller can get wrong is a defect, not a contract", which is `X-i4`'s own sentence reproduced in the step that removes `X-i3`'s. (2) A snapshot in front of a WRITE is a money bug, not a stricter setting: `user_write_lock`'s lock-then-reread requires the waiter to see the winner's just-committed postings, which only `READ COMMITTED` shows, so one snapshot re-arms the `$4,000.00`-asserted-against-`$1,000.00` divergence ruling **R-EN** measured -- silently, because the anchor-equity leg mirrors the error and the trial balance still closes at `$0.00`. **Measured before ruling**: the mechanism proven against this application's own session (two counts across one concurrent committed append answer `(1, 2)` today and `(1, 1)` under the query mode), 2,302 of 2,978 suite GETs arriving on an inherited transaction, and 36 arriving on one that had already written. **Rejected**: two constructors on the pass (the argument above); and "the render stops writing first", which is a product change across three arcs and still leaves every POST-then-render pass unsnapshotted |
| balance | R-GV | -- | 2026-08-26 (developer) | **A read pass BINDS the account it values, and the binding is a PRECONDITION of creating per-account pass state rather than a guard at each funnel.** `_context._memoize_once` -- the one primitive filling every `account.id`-keyed cache a `BalanceContext` holds -- takes the `Account` and refuses one whose owner is not the pass's, BEFORE the membership test, so a warm cache cannot serve a foreign hit and a funnel added later cannot forget a rule it never had to remember. **It is NOT a second ownership gate, and that distinction is the ruling's substance**: whether a REQUESTER may see an account is decided upstream where an untrusted id becomes a row, and a copy of that here would be the band-aid the developer refused. What this answers is whether an account and a pass describe ONE READ -- which no route can ask, because no route knows a context exists and a service-tier caller has no `current_user` at all. **What a mis-pairing would have published**: one owner's pass handed another owner's Checking answered that owner's real asserted balance across every column of the first, because transactions are scenario-scoped and `cash_anchor_facts(account_id)` is not. **The cash fold is MEMOIZED with it**, and its cache is PRIVATE because an `AssembledCashFold` IS a running total -- W9909 refuses a public one outright. **Two bindings the adversarial reviews forced on top**, each N-354's sentence one field over: `__post_init__` refuses a foreign SCENARIO, and `AssembledCashFold.account_id` binds the record `_asset_fold.resolve` folds an account onto, which memoizes nothing and so was never covered. **Rejected, on cost measured rather than asserted**: a `BoundAccount` value taking the pair out of all 42 seam signatures (~40 `app/` and ~420 test call sites, and it would not have closed the `resolve` case, which is account-vs-FOLD); and `ctx.account(account_id)` resolving through an owner-scoped query so a foreign account is UNREPRESENTABLE, which is the truer root but inverts the route layer and supersedes three resolvers. Both remain available and compose: `ctx.account(id)` would return a `BoundAccount` |
| balance | R-GW | -- | 2026-08-27 (developer) | **THE CUTOVER'S ORDER REVERSES: the residual is RECORDED before the reset is DELETED.** A measurement against the developer's own bank export refuted the first decomposition's sequence -- deleting the reset first lands the flip six assertions further from the bank than recording the residual first does. The flip is additionally ship-gated on the account's own statement history being imported (**N-368**), which no `blocked by` cell can hold because it is a DATA act. **Report the evidence SPLIT, never pooled**: pooled it read 17-of-75 for today against 0-of-75 for the cutover, and 15 of those 17 are days carrying an assertion, where the RESET forces the answer. On the 29 days no assertion touches: `$529.48` against `$1,956.64`. Rejected: swapping in `bank_agreement`'s residue metric, which is a MOVEMENT comparison and blind to a reset by construction |
| balance | R-GX | -- | 2026-08-27 (developer) | **Opening equity is the account's OWN stored fact, in a SATELLITE table, seeded at today's derived value so the migration moves `$0.00`.** What an account held before its records begin was inferred per read -- the earliest assertion less the movements it already contained (ruling **R-I**) -- which made it move when an assertion was back-dated, differ between scenarios, impossible to correct, and derived a second time by the posted ledger. Rejected: a column on `budget.accounts`, which would have to be NULLABLE for a LOAN, a rule the schema cannot state because the kind lives on `ref.account_types`, and which would sit where **R-EH** deleted a cached balance; and putting the figure on the opening ASSERTION row, which **R-EO** and **R-ES** reduced to `(account, day, balance)` -- a declared balance and capital brought on are two quantities |
| balance | R-GY | -- | 2026-08-27 (developer) | **An accepted residual is DERIVED while uncategorised, becomes the owner's own figure once categorised, and is offered only over a span an imported statement reconciles.** Three bounds on the acceptance act **R-FN** requires. The offer gate reads SHIPPED facts -- a `statement_imports` row at a zero `bank_agreement` residue -- because a first draft gated it on X-f3a-2's walked flag, which is a rank-3 step depending on a rank-5 one and could not be expressed in the `blocked by` column at all, so the dependency graph looked acyclic while the prose held a cycle |
| balance | R-HE | -- | 2026-08-27 (developer) | **The opening record is APPEND-ONLY, latest recorded row governs, and each row carries the DAY the books opened, the equity and a TYPED provenance** over `ref.account_opening_sources` (`user_declared` / `migration_derived`). Rejected: one mutable row per account -- `system.audit_log` would hold the restatement but nothing in the app could render it, the gap **N-205** built the balance-history card to close, and silently overwriting the level every balance rests on is not a state this app may enter. "Latest governs" is NOT the positional read X-f3c-2a deletes: `is_opening` inferred a TYPE from position in a list ordered by `observed_on`, a business date any owner may back-date, where this orders by `created_at`, which no door lets a user move. The DAY is stored because fixing only the amount leaves half the position-dependence alive. **EVERY account carries a row, loans included**, revising R-GX's first draft: `balance_at` falls through to the cash fold for an amortizing account with no `LoanParams`, so absence is reachable -- and with every account carrying one it is not, which lets the loader REFUSE rather than fabricate a `$0.00` level |
| balance | R-HF | -- | 2026-08-27 (developer) | **A movement dated before its account's books opened is repaired by moving the OPENING back to where the bank's own history starts, not by re-dating the movement.** The repair direction for `balance:X-f3c-2b` over **N-378**'s eight rows. Import what the records are missing so the recorded history is TRUE rather than merely self-consistent. **Its two FIGURES are superseded and must not be acted on**: the boundary is `<=` rather than `<`, so the census is 12 rows over five accounts and not eight over four (**R-HG**), and account 10 opens 2026-04-08 at `$5,363.56` rather than 2026-01-30 at `$1,345.74`, because no transaction can be dated before the pay calendar's own first day (**R-HH**, **N-379**). Rejected: re-dating the eight offending rows onto the opening day, which moves no rendered balance and is fully reversible, but makes the app's dates disagree with the bank's ON PURPOSE -- the same class of thing that made those dates wrong to begin with. Every changed figure goes to the developer before a production row moves |
| balance | R-GZ | -- | 2026-08-27 (developer) | **Every arc's RULINGS move OUT of its arc document into ONE gate-graded registry keyed `(arc, id)`.** Taken because rule 5's supply was measurably spent, not as a tidy-up: four of the five arc documents stood at or within 3 lines of their effective caps (balance 980 of 980, bank_import 179 of 180, recurrence 830 of 830 on `feat/r16-b`, pay_calendar 477 of 480), and of the balance README's nine ticked entries exactly ONE was not cited as a satisfied blocker by a live step -- so archiving bought one line, once. The growth engine is two-sided and rule 5 reclaims from one side only: a ruling is appended forever and never becomes archivable. **Also ruled: the LINE cap goes and the per-ROW cap comes with it**, which is the 2026-08-25 precedent applied whole rather than halved -- a first draft took only the removal and an adversarial review measured a 16,087-character row against a 529-character median. **And `(arc, id)` is kept over globally-unique ids**, re-put with the evidence for renaming (N-217 renamed `R-EX` to `R-FA`; cited by ONE live document as of 2026-09-11, re-run by `grep -rl 'R-FA\b' docs/`; it read "~35 citation sites") and decided again: a collision becomes legal and gradeable rather than silent, and the citation arm that makes it safe is pulled forward to `X-ao-3` rather than the key being changed. Rejected: raising the caps (rule 4 forbids it where one binds), splitting section 5 by phase (helps one arc and not the growth engine), and archiving `X-i3-b` for its single line (the last one available). |
| balance | R-HA | -- | 2026-08-27 (developer) | **The `Settled` status -- the terminal ARCHIVE -- is DELETED, and whether a row is FINISHED is answered by reconciliation PROVENANCE, never by a member inside the settled band.** Provenance is three shipped columns answering three questions, each correctable without destroying the row: `settled_basis_id` (how the FIGURE is known), `settled_day_basis_id` (how the DAY is known), `reconciled_by_id` (which statement was seen to show it). **What decided it was not the zero row count.** The archive was ABSORBING and REACHABLE: entered from one dropdown, no door out, so it refused every act that CORRECTS a row -- revert, re-price, add or remove a purchase, match a bank line -- while `deletion_refusal` names no status, so the DELETE control on the same card removed it and reversed its postings. A lock whose only permitted mutation is destruction protects nothing, and `is_immutable` already gave Paid and Received the field lock, so its only distinct content was *and you may never revert*. **The law is a CONJUNCTION, not a ban on terminal states**: an absorbing state NOTHING can enter is a retired vocabulary item, which `credit_card` locked ruling 5 already builds at `CC3b`. **CC3b owes** a `deletion_refusal` arm for a terminal `Credit` or a stated reason the asymmetry is acceptable there -- X-am removed this instance and not the mechanism. **Supersedes** the archive half of `bank_import:R-GG` (d) by making it vacuous; its stored-figure half stands. **Rejected**: keeping it terminal with a confirmation, a badge and a delete refusal (the owner still gets a one-way trapdoor and the bank-import exclusion stays permanent); and keeping the name while dropping terminality (a second, cruder spelling of what `reconciled_by_id` already records). Measured: 1,591 audit rows over the two status-bearing tables since 2026-05-07, 229 of them DELETEs, name it nowhere |
| balance | R-HB | -- | 2026-08-27 (developer) | **`X-ao-2`'s shape: DECOMPOSED into TWO, the LIFT and the OVERFLOW.** The step's own sentence carried two nouns and only the first is a lift: `X-ao-2a` moves the last three arcs' rulings into the registry, mints an id for every row that has none and DELETES the map of arcs that had not moved; `X-ao-2b` re-homes the argument of every row over the 2,000-character cap, which is **N-370**. Measured before the split rather than after -- 24 over-cap rows holding 43,734 characters of overflow at the moment of the ruling, 25 and 43,855 once `recurrence:R-R38` arrived on the merge, none of the delta `bank_import`'s: `bank_import` owns 14 of them at 36,901, and that document stands at 151 lines against a 200-line cap -- so rule 4's literal destination could not hold it, and the second noun needed a ruling of its own (**balance:R-HD**) before it could be built at all. Rejected: one commit for both, whose second half cannot be reviewed in focus and spans sessions; and re-pointing N-370 at some later step, which rule 1 refuses because every other spelling of that means nobody |
| balance | R-HC | -- | 2026-08-27 (developer) | **A registry row states a RULE: a row that only POINTS at another decision does not become one, and a dated developer decision does become one wherever it was written.** Both halves were measured on the three arcs `X-ao-2a` lifted. Four `recurrence` rows stated no rule of their own and are recorded where their content belongs rather than given a minted id: a supersession fact that is `R-R13`'s own, a pointer at the archived `R-R7` (which becomes a row under the id it already had, since rule 5 forbids a live sentence depending on an archived one), a second copy of `pay_calendar`'s `R-PC2`, and a section LABEL rendered as a table row, which is **N-372**. The other half runs the other way: `credit_card`'s four `Grid / companion hard requirements`, taken in the same commit as its eight "locked rulings" and counted as rulings by nothing, are `R-CC9`-`R-CC12`. Rejected: minting an id for all four pointer rows, which puts a second copy of one ruling under two arcs and gives a section label a registry row |
| balance | R-HD | -- | 2026-08-27 (developer) | **An over-cap registry row's overflow goes to the step that OWNS the ruling: its AS-BUILT record where that step has SHIPPED, its live specification where it has not.** Rule 4 stated only the second and the second alone is unsatisfiable. Measured 2026-08-27 over the 24 rows the registry then held -- 25 since `recurrence:R-R38` arrived on the merge, which changes none of what follows: `bank_import` owns 14 at 36,901 characters of overflow against a document with 49 lines of headroom, and NINE of those 14 -- `R-FV`, `R-FW`, `R-FX`, `R-FY`, `R-GB`, `R-GC`, `R-GF`, `R-GJ`, `R-GG`, 14,949 characters -- are cited by no step entry in that document at all, live or shipped, because the spans that owned them were archived under rule 5. Every one of the nine is cited by an as-built record that already exists. This states no new policy: `rulings.md`'s own first sentence already said the deliberation belongs in the commit that shipped it and in the as-built record of the step that shipped it, and nothing graded that. **What it does NOT settle, measured after it was taken:** 17 of the 25 rows name no step of their own arc at all and only TWO name a SHIPPED one, so the OWNER this keys on is not a fact the corpus carries -- determining it per row is `X-ao-2b`'s first task rather than an input it has; and an owner that IS known and shipped may still have no as-built record, as `recurrence:R7d-c-1` has none. Owned by `X-ao-2b`. Rejected: raising the `bank_import` line cap by ~390 lines, which rule 4 forbids as the answer when a cap binds; and letting the argument live only in the commit, which deletes measured evidence from every document a reader can grep |
| balance | R-HG | -- | 2026-08-28 (developer) | **EXTENDED 2026-09-05 by `pay_calendar:R-PC62`, which carries this bound from settled MOVEMENTS to GENERATED rows.** **An opening equity is the CLOSING balance for its own day, so no cash movement may be dated ON OR BEFORE `budget.account_openings.opened_on`.** The same rule `account_anchor_history.observed_on` states for an assertion (**R-DH** (a)), and adopting it is what makes the two writers of an opening MEAN the same thing: `account_service.create_account` stores the balance a human typed "as of" a day, which is that day's close, while X-f3c-2a's migration derived a level from BEFORE the earliest recorded movement -- one figure, two semantics, differing by whatever moved that day. Rejected: the START-of-day reading (refuse only a STRICTLY earlier movement), which leaves `create_account` storing a close-of-day figure against a start-of-day meaning and leaves the harm alive for a same-day row -- the correction the assertion books is then `-m` of phantom `unrealized_change` on a MODELLED account, which is **N-378**'s own mechanism one day over. **It moves the census from 8 rows to 12**: Checking's four 2026-03-27 rows, netting the `$2,057.42` that is exactly the gap between its `$689.16` opening and its `$2,746.58` first assertion, are inside the opening too. Its cost is stated rather than hidden: an owner may no longer record a movement on the day an account's books open, so the ordinary create-then-mark-paid-today path is refused until the opening is restated (`X-f3c-2b-2`'s door) or the movement dated later |
| balance | R-HH | -- | 2026-08-28 (developer) | **AMENDS R-HF's figure: account 10's books open 2026-04-08 at `$5,363.56`, not 2026-01-30 at `$1,345.74`, because the app cannot hold a transaction before 2026-03-26.** `budget.transactions.pay_period_id` is NOT NULL, the owner's earliest pay period starts 2026-03-26, and `PayPeriod(...)` is constructed in exactly ONE place (`pay_period_write.py:891`) behind `_reject_backward_payday`, which refuses any payday earlier than `latest + cadence`; plan step `pay_calendar:C6` and ledger row `P10` park the backward case deliberately. R-HF's own date would therefore render `$1,345.74` FLAT from 30 January to 25 March -- two months of real activity with no column to record it in -- where today those days read `$4,879.26`: further from the bank, not closer. 2026-04-08 is the bank's own closing balance for every day in its 03-31..04-09 gap, needs no pre-calendar row, and from it the recorded history reconciles to Fidelity TO THE CENT, taking all three of the owner's typed balances to a `$0.00` correction. **SUPERSEDED 2026-08-28 and must not be acted on: R-HK replaces the DATE and the FIGURE with 2026-03-26 at `$5,350.21`.** The pay-calendar premise below is SOUND and is what R-HK respects; what it got wrong is the inference from it. The books may OPEN on the calendar's first day and ABSORB that day's bank line (**R-HG**), so no pre-calendar row is needed to record March -- 2026-04-08 skips two real lines for a constraint that never bound. R-HM then records the 07-31 dividend behind an assertion of its own, so the dividends imported are five and not three. **And a real dividend is recorded only BEFORE the modelled accrual window opens**: 04-30 `$15.70`, 05-29 `$15.01` and 06-30 `$15.24` are imported, 07-31 `$14.39` is left to the 3.29% model, because the window opens at the latest assertion (2026-07-16, rulings **R-L** / **R-Y**) and a row inside it is counted twice -- **N-277**'s shape |
| balance | R-HI | -- | 2026-08-28 (developer) | **A `ledger.md` row's subject is the CODE. A finding about a PLAN DOCUMENT is neither a row nor a paragraph -- it is a QUESTION for the developer, asked when it binds.** Taken when `balance:X-f3c-2b-1` hit the balance README's 1,000-line cap and found `N-369` already recording that pressure as a finding with an owner. "Ledger rows are for the code not the documents. If a document reaches its cap that's just a notification to me and question about how to handle it if you don't know what to do. There is no need to waste lines in the documents about it." `N-369` was DELETED rather than answered (264 rows, balance 178). **Two rows are the same class and are NOT deleted here**, because each is cited by live work rather than by itself alone: `N-370` (rulings rows that are the arc document's argument) is owned by step `X-ao-2b` and cited by conventions.md rule 4, and `N-376` (a ruling cited from `app/` whose text lives only in an archive) is cited by three arc plans and by `tools/plan_gate/_rulings.py`. **A plan-GATE arm is code and stays**: `N-355`, `N-357`, `N-366`, `N-367`, `N-371`, `N-375`, `N-377` and `N-217` are all fixed by editing `tools/plan_gate/`, not a document |
| balance | R-HJ | -- | 2026-08-28 (developer) | **AMENDED 2026-09-15 BY R-BAL40.** **A DATA REPAIR IS PERFORMED THROUGH THE APP'S OWN DOORS, NEVER BY A MIGRATION WRITING MONEY ROWS.** Taken on the account-10 repair (**N-379**, **N-382**), whose four acts are a duplicate transfer deleted, five transfers re-dated, missing dividends recorded and one opening restated. Three of the four already have working doors, and TWO of the three questions a migration would have forced are already answered by their code: `transfer_service.delete_transfer` reverses the posted effect before the rows go, leaving the net-zero pair as history, and `transfer_service._update._reconcile_postings_after_update` re-dates a transfer's entries AND resyncs both endpoints' anchor corrections on a settle-day edit. **A question that only exists because you are writing raw SQL is evidence you are writing it in the wrong place.** Rejected: a data migration in `X-f3c-2b-1`'s second commit, which would make the chain a SECOND writer of `settled_on` and of the posted ledger beside `settle_day.record_settle_day` and `posting_service` -- the class this arc exists to delete -- and would hard-code one owner's account id and six dates into every future deploy and every test-template build; and a one-off `scripts/` routine driving the services, which buys a reviewed diff for three acts the UI already performs and is dead code the moment it runs. **The repair therefore RIDES `X-f3c-2b-2`'s door**, and `X-f3c-2b-1` ships as the invariant and the legalising restatement alone. **AMENDED the same day on measurement: one act is NOT performable and the limitation is ACCEPTED.** Transfer 102 is template-linked and `mutations.py:378` derives soft-vs-hard from that link alone, so only a SOFT delete is reachable; the money leaves correctly -- the fold excludes `is_deleted` and the posted effect is reversed first -- but the row stays restorable through `transfer_recurrence.resolve_conflicts`. That is **N-386** and its own step |
| balance | R-HK | -- | 2026-08-28 (developer) | **AMENDED 2026-09-05 BY R-BAL3; FIGURES SUPERSEDED.** The consolidation survives. **ACCOUNT 10'S BOOKS OPEN 2026-03-26 AT `$5,350.21` -- the bank's own close for that day -- and accounts 2 and 10 are ONE real Fidelity account whose history is consolidated onto 10.** AMENDS **R-HH**'s date and figure. Under **R-HG** every bank line dated on or before `opened_on` is already inside the equity, so the six lines to 2026-03-26 are ABSORBED rather than recorded and NO transfer endpoint has to move -- which matters, because no door moves one: `TransferUpdateSchema` carries neither endpoint, and the template door RETAINS a reattributed row holding a settlement record. **MEASURED by replay against the export rather than argued: zero mismatches on all 17 days it states a balance, and a `$0.00` correction at all FOUR of the owner's typed balances** -- `$5,363.56` on 04-06 (account 2's, which is bank-exact and is the strongest single evidence the two records are one account), then `$4,879.26`, `$5,644.27`, `$5,659.51` -- and at R-HM's new one. Rejected: 2026-03-25 at `$4,850.21`, one day below `pay_period_service.earliest_recordable_day`, the floor `anchor_service.resolve_observation_day`, `status_seam` and `account_service.create_account`'s own `opened_on` all take, which reads `$500` under the bank on 2026-03-26 while 03-25 has no grid column at all; and R-HH's 2026-04-08, which records neither March line. **Transfer 1 becomes a plain `$500` Checking EXPENSE dated 03-27**: its arrival leg would double-count money the opening now holds, Checking's own leg must survive (it nets into the `$2,057.42` reconciling Checking's opening to its first assertion, and Checking's own repair is **N-275**), and a transfer INTO an opening is not a transfer this app can hold. Account 2 is then zeroed by restating its opening to `$0.00` and asserting `$0.00` on `observed_on` 2026-04-06, which SUPERSEDES that day's figure rather than conflicting with it |
| balance | R-HL | -- | 2026-08-28 (developer) | **REAL INVESTMENT INCOME TAKES ITS OWN CATEGORY, `Income: Interest & Dividends`.** The five Fidelity dividends the account-10 repair records are income the app has never held as income: today they are absorbed by balance TRUE-UPS whose counter leg books to the per-account `interest_income` chart row (ruling **R-FO**'s dispatch), and once the dividends are real rows those corrections go to `$0.00` and that row empties. A category costs no code -- it is owner data -- and it keeps observed investment income distinct from Salary and from the Other Income catch-all on the income statement. Rejected: the existing `Income: Other Income`, which cannot tell investment income from any other miscellaneous receipt; and posting to the account's OWN `interest_income` chart row so observed and modelled interest share one line, which is the most coherent statement and needs a path that does not exist -- a transaction's counter leg resolves to a CATEGORY row or the Uncategorized fallback, never to an account-linked one. **THE ACTS ARE ORDER-DEPENDENT**, found by adversarial review: recording a dividend before the duplicate goes and the opening is restated leaves the 05-01 assertion booking a `-$15.70` correction, and on this account kind that counter leg is `interest_income` -- NEGATIVE interest income on the income statement until the rest of the repair lands. The order is delete, re-date, restate, then record |
| balance | R-HM | -- | 2026-08-28 (developer) | **THE 2026-07-31 `$14.39` DIVIDEND IS RECORDED, AND A 2026-07-31 ASSERTION AT `$3,673.90` MOVES THE ACCRUAL WINDOW PAST IT.** AMENDS **R-HH**, which left that dividend to the 3.29% model because the window opens at the latest assertion (2026-07-16, rulings **R-L** / **R-Y**, verified in `balance_at/_asset_fold`) and a row inside it is counted twice. Asserting the bank's own closing for the last day its export covers moves the window to 07-31, so the modelled accrual stops standing in for a figure the bank states, and the recorded history runs bank-true to the end of the export with a `$0.00` correction at EVERY assertion rather than at three of four. **The row sits on the window's INCLUSIVE first day, not past it** (adversarial review, 2026-08-28, correcting this ruling's first reason): the app still credits one day of accrual on top of the bank's close and reads `$3,674.22` against `$3,673.90`, which is a `$0.32` property of **R-L** that today's 07-16 assertion already has and not something this ruling introduces. **It also strips every modelled day from 07-16..07-30 and raises today's figure by `$8.13`**, which is the app becoming bank-true rather than drifting. The figure it replaces is the model's `$6.42` of accrual over 07-16..07-31 against a real `$14.39`; the `$7.65` first quoted priced the window at the PRE-withdrawal balance and ignored the `$2,000` leaving inside it. Rejected: leaving it to the model, which under-reports the account by about `$7` until the next real balance entry; and recording it with no new assertion, which puts a real row inside the open accrual window and is **N-277**'s shape deliberately reintroduced |
| balance | R-HN | -- | 2026-08-28 (developer) | **APPENDING TO AN APPEND-ONLY TABLE HAS NO INVERSE, so a migration's downgrade removes the CONSTRAINT and deletes NO DATA.** Taken on a reproduction rather than an argument: `d3b6f1c8a274`'s downgrade re-ran the upgrade's own join against the CURRENT state and deleted whatever matched, which identifies its rows by SHAPE and not by identity. Planting the hand restatement `cash_ledger.reject_movement_before_books_open`'s own message tells an owner to write, then running upgrade then downgrade on a production clone: the upgrade correctly restated FOUR accounts and never touched account 10, and the downgrade then DELETED the human's append-only row while printing that it had withdrawn a restatement nobody made -- leaving that account holding a movement inside its opening (**N-378**, re-opened) with the constraint gone. The `expected != actual` guard is structurally blind to it: deleting the row is what returns the account to the violating set, so the two sides agree. A second class rides the same cause -- three shipped doors hard-delete rows carrying `settled_on`, so `MIN(settled_on)` moves and the join then misses a row the upgrade really did write. **What the migration leaves is a CORRECTION, not a residue**: it moved a stored DAY to meet a figure that already meant that day, so reverting would re-introduce a day known wrong, and the older schema reads the restated row correctly because the fold seeds at the equity. A re-upgrade is then a measured no-op -- `0 account(s) restated`. Rejected: persisting the inserted ids, which needs a schema artifact whose only reader is one migration's undo; refusing to downgrade at all, which costs the deploy's rollback; and refusing only on ambiguity, which is undecidable here because the whole chain runs in ONE transaction so a human's row and the migration's share every column value including `created_at` |
| balance | R-HO | -- | 2026-08-29 (developer) | **A DEFERRED CONSTRAINT TRIGGER STATES ITS PRECONDITION IN THE `WHEN` CLAUSE, NEVER AS AN EARLY RETURN IN ITS BODY.** A deferred trigger QUEUES its event at STATEMENT time and runs the body at COMMIT, so a body-level guard cannot stop the queueing -- and PostgreSQL refuses `ALTER TABLE` on any table carrying pending trigger events. `ck_movement_after_books_open` opened with `IF NEW.settled_on IS NULL THEN RETURN NULL`, so **every Projected row this app writes reserved `budget.transactions` against DDL for the rest of its transaction** -- which broke `recurrence:R17`'s index re-key on one merge and two audit-trigger benchmarks in CI on the next. Moving the SAME condition into `WHEN (NEW.settled_on IS NOT NULL)` puts it where the queueing decision is made and leaves the rule stated ONCE. Measured both directions rather than argued: an unsettled INSERT then leaves the DDL legal and a SETTLED one still queues and still refuses it, so enforcement is unchanged; mutation-tested, removing the clause fails exactly the one new case. Rejected: `SET CONSTRAINTS ALL IMMEDIATE` at the call sites, which is the documented remedy two test files already carry and which three files have now independently tripped over -- a rule maintained by remembering, added at a fourth site rather than removed. **The reasoning that hid it twice is worth naming: "this write cannot violate the rule, so the trigger is harmless" is true of the CHECK and false of the QUEUE.** What remains needing a drain is a transaction that writes a genuinely SETTLED row and then performs DDL, which is the only case the rule was ever going to examine |
| balance | R-HW | -- | 2026-08-29 (developer) | **A PAYCHECK'S GROSS IS A RATE, NOT A SHARE OF A YEAR, AND THE PER-PAYCHECK FIGURE IS THE FACT THE APP SHOULD STORE.** **(a) NOW (`X-aw`)**: the gross is `round_money(annual_salary / periods_per_year)` -- ONE figure for every paycheck in a salary segment, a function of the salary and the cadence alone. MED-05 / PA-07's residue distribution is DELETED with `_residue_cents`, the reconciliation group and the partial-context fallback, so **N-239** dies by construction: the residue had to be APPORTIONED, apportioning it needed an ordinal, and the only ordinal available was a count of the `budget.pay_periods` rows that happened to exist. The cost is accepted and stated -- a year's grosses miss the annual salary by up to half a cent per paycheck (`$0.13` biweekly, `$1.83` at the daily cadence the schedule admits). **(b) NEXT (`X-av`, re-scoped)**: the STORED fact becomes a dated PER-PAYCHECK gross and the annual salary becomes `gross x periods_per_year`, shown at entry so a disagreement is visible rather than resolved silently. **The flat `$3,526.00` stub is the argument for (b) and NOT for (a)**, which an adversarial review corrected: at an annual of `$91,676` the superseded rule is flat too. What (a) rests on is N-239 itself, plus a biweekly year holding 27 paydays about one year in eleven -- 2026 is one on this owner's phase -- where MED-05's identity is false whatever the rule does. Rejected: deriving the year's paydays from the cadence and KEEPING the distribution, which closes N-239 and leaves the machinery standing |
| balance | R-HY | -- | 2026-08-30 (developer) | **How the three append-only ACCOUNT tables are made append-only: a DATABASE TRIGGER plus the object-layer refusal**, at `X-f3c-2c`, on finding **N-287**. There is no column type meaning "never updated", so every candidate is a REFUSAL and they differ only in whom they can refuse. The SQLAlchemy listener the siblings carried is blind to a bulk `query.update()` -- a spelling this app already uses in production at `reconcile_service.record_settled_days`, which holds no ORM instance for a listener to fire on -- and blind to a raw statement. `REVOKE UPDATE, DELETE FROM shekel_app`, the posture `journal_entries` and `account_postings` already carry, refuses every spelling the APP can produce and is **invisible to the test suite**, which connects as the owner: a door written to edit an assertion would pass locally and fail in production, which is this arc's own "a green gate can be measuring nothing". The trigger refuses everyone identically -- app, suite, psql, migration -- so the suite proves the rule that ships. Its cost is accepted rather than argued away: a future migration that must rewrite one of these tables calls `remove_append_only_infrastructure` and re-applies -- two lines, visible in the diff, and the RIGHT behaviour, because rewriting a stored assertion is the act ruling **R-HJ** already forbids a migration. **The DELETE arm asks whether the owning ACCOUNT still exists**, which is what separates disposal from vandalism: a blanket arm makes accounts undeletable, measured on four cases and on a live psql probe. Written ONCE and applied to `account_anchor_history`, `account_openings` and `loan_anchor_events`, replacing three hand-written listener pairs. Rejected: the listener alone (leaves the surface the finding's own harm sentence names), and all three tiers together (one rule in three places is the drift this arc calls its own root cause) |
| balance | R-HZ | -- | 2026-08-30 (developer) | **What the fixtures do instead of re-stamping: the FULL root-cause reshape, in its own commit on the same branch**, at `X-f3c-2c`. Three helpers UPDATEd a stored assertion to move its business day and recording instant onto a day a test had chosen, and `tests/conftest._drop_seed_user_bootstrap` bulk-UPDATEd the seeded account's origination onto whatever calendar a periods fixture built. Measured before the fork was put: the guard alone fails **539** tests; `passive_deletes` and an INSERT-only `append_balance_assertion` take it to 501; giving three test FACTORIES their day at `create_account` takes it to 150; the rest take it to 40, and the database trigger then adds 22 more the listener could not see. Rejected: re-spelling the re-stamp as a bulk `UPDATE` the listener cannot see (zero churn, and a fixture writing by a route the app is forbidden to use), and asserting the balance AGAIN on the new calendar's first day (measured worse rather than judged worse -- ~15 fold and walk cases place their own assertion one day BEFORE the calendar, so a fixture row nobody wrote would govern instead of the row the test author placed). **The reshape and the refusal ship in ONE commit**, so there is no tree anywhere in which the fixtures have stopped editing an assertion and nothing stops them.  The split was going to be two commits for reviewable diff size until `shekel-a1` named the third option -- do not create the window -- and observed that the ordering is forced in only one direction: guard-then-removal is impossible, removal-and-guard together is not.  Two step ids, one hash |
| balance | R-IA | -- | 2026-08-30 (developer) | **The payday RHYTHM projects BACKWARD as well as forward, bounded by a STORED fact rather than by a guess**: `budget.pay_schedule.history_opens_on`, nullable, asked at registration beside the payday and cadence it already asks for, with `NULL` meaning the rhythm runs back to `CALENDAR_DATE_MIN` -- the exact mirror of the `CALENDAR_DATE_MAX` that already bounds the forward projection. The app knows the CADENCE and cannot derive when the job began, and both owners are ordinary: the Generate form asks for "your most recent payday" (**R-EX**), so a first recorded payday is a RECORD boundary, and it also accepts "your next (or first) payday", whose owner has no history to project. **`pay_calendar:R-PC14` is NARROWED, not repealed** -- its stated reason is attributing money to paychecks that never happened, and the rhythm producers return `date` values no row can be filed into, where `filing_period` files only into `materialised_periods` and the axis takes explicit bounds. Rejected: counting only recorded paydays (knowingly wrong for a continuing job, and the app's own registration wording says the job continues), and projecting backward with no stored bound (right for this owner, and it fabricates an employment history for one whose first payday has not happened yet) |
| balance | R-IB | -- | 2026-08-30 (developer) | **ONE span search answers "how many paydays does this month hold" for the paycheck engine and the analytics card alike, asked of TWO named sets rather than one.** The engine reads the TOTAL rhythm, projected forward past the schedule's horizon, because a payday it cannot place would take ordinal 0 -- a silently wrong answer that drops a 12-per-year deduction and takes a 24-per-year one on a third paycheck -- and how often somebody is paid is a fact about them rather than about which rows are stored (`pay_calendar:R-PC9`). The CARD reads the SAVED half, because everything else on it folds from saved periods. **RULED TWICE, and the first answer was made on an incomplete cost**: the unbounded count shipped to review and was measured putting paydays on **29 month cards** reading *3 paychecks, income `$0.00`, balance `$9,539.92`* -- stated to the developer as "markers beside a frozen balance", which understated it. The bound is ledger row **N-394** and comes off when the cash tier projects. Rejected: shipping the unbounded count (a card that contradicts itself on a surface that had been consistently empty), and landing N-394 here (it moves every projected balance on every surface, which is its own PR) |
| balance | R-IC | -- | 2026-08-30 (developer) | **How a refusal that must PERMIT disposal is timed: three arms, three timings**, at `X-f3c-2d`. `X-f3c-2c` shipped one `BEFORE UPDATE OR DELETE` row trigger and argued its timing in a sentence that only ever reasons about UPDATE; the DELETE arm inherited the mechanism unargued, a refutation pass then broke it twice, each reproduced with a control that fired. **TRUNCATE never reaches a row trigger** -- with every account standing it took `account_openings` to zero, and `system.audit_log` is written by a row trigger too, so the log was byte-identical -- the ONE spelling that destroyed history both unrefused and unrecorded. **DELETE is a question about the transaction's END STATE**, not the statement: `DELETE FROM budget.accounts WHERE id=N` then `INSERT` of the same id committed clean, leaving the account standing with its assertions gone, because the predicate asked whether the account existed at the INSTANT the cascade ran. So UPDATE stays a `BEFORE UPDATE` row trigger, DELETE becomes a `DEFERRABLE INITIALLY DEFERRED` constraint trigger, and TRUNCATE gets a `BEFORE TRUNCATE` statement trigger. **No archive tables**: the outcome asked for was "history is never destroyed at all", and all three tables are already in `AUDITED_TABLES` with `to_jsonb(OLD)` on DELETE -- measured, every column of every destroyed row -- so closing TRUNCATE delivers it and an archive would duplicate `system.audit_log`. Rejected: `ON DELETE RESTRICT` plus archive-instead-of-delete, which was authorised and is VOID on a fact in the code neither party had checked -- `account_service` writes an opening AND an origination assertion for every account ever created, so "no hard-delete once history exists" removes `hard_delete_account` entirely rather than hardening it. Also rejected: documenting both holes as limits, leaving the guard claiming more in its name than it refuses |
| balance | R-ID | -- | 2026-08-31 (developer) | **`X-f3c-2b-2` is DECOMPOSED into THREE: the DOOR, the WITHHELD BANK LINE and the REPAIR.** The step's own sentence carried three acts and only the first is code an owner can use on its own. `X-f3c-2b-2a` builds the restatement door; `X-f3c-2b-2b` is **N-383**, the import doors that OFFER a line whose day the books cannot hold; `X-f3c-2b-2c` is the account-10 repair. **Two of the three reasons are scheduling facts measured rather than argued.** N-383's remedy has to RENDER on the reconcile cards `bank_import:X-gj-1b` is rebuilding in a concurrent session -- `_cards.py`, `_stating.py`, `_verbs.py` and four templates are in that session's diff -- so writing it now is either a merge conflict or a rendering thrown away and rewritten, and the leaf is SEQUENCED behind it (`conventions.md` rule 8: a schedule, not a deferral). The repair is an OPERATOR act through doors that do not exist in production yet: prod is at `a4c6f1d92b73` and carries no `budget.account_openings` table at all, so it cannot run until `2a` has DEPLOYED. Rejected: one commit for all three, whose review surface spans a service, a screen another session is replacing, and a data act nobody can perform -- the shape **R-HB** and **R-HZ** each split for; and leaving N-383 with the door, which buys nothing and pays for it twice |
| balance | R-IE | -- | 2026-08-31 (developer) | **The restatement door is ONE door with TWO entrances: the form on the shared account EDIT page, and a link to it from the balance-history card's opening row.** Taken on a coverage measurement, which is what makes the edit page the home: the opening is DISPLAYED in ONE place app-wide, the `<tfoot>` of `accounts/_balance_history.html`, rendered only by the cash detail page -- **3 of the developer's 9 accounts, while 4 of the other 6 carry a `migration_derived` opening the balance fold READS**, the class **N-275** and **N-379** measure wrong; the census is that step's own specification. **A deliberate page rather than an inline editor**, inverting the true-up's one-click grammar on purpose: asserting a balance is a habit five surfaces open an editor for; restating the books is rare and moves the level every balance rests on. It keeps the history card a RECORD, not a write door. **An AMORTIZING account is refused at the service**, the twin of `AmortizingAccountAnchorError`: nothing reads its row while the loan is configured, so a door there would report success and move no figure. Accepted limitation: an amortizing account with no `LoanParams` DOES have its opening read and cannot restate it -- that figure is `user_declared` from creation, and a half-built loan is repaired by finishing the loan-params form. **Rejected, and the first was NOT on the question's own menu** -- the other two are placement variants of one idea and it is not: taking the opening into the ANCHOR EDITOR so one door writes both facts, which is the shape plan step X-f3c-2a spent itself separating, and whose editor PRE-FILLS -- so a restatement would re-assert a balance the owner never read, **N-195**'s defect one fact over. Also rejected: the cash card alone, leaving four read figures uncorrectable; and adding the history card to the investment and property pages, an information-architecture job on two more screens |
| balance | R-IF | -- | 2026-08-31 (developer) | **`budget.pay_schedule.history_opens_on` NULL means NOT STATED, and an owner who has stated nothing is counted from the RECORD.** `balance:R-IA` had NULL mean "the rhythm runs back to `CALENDAR_DATE_MIN`, the exact mirror of the `CALENDAR_DATE_MAX` that bounds the forward projection"; asked whether that was how it would be built from scratch, it was not. **The two ends are not symmetric in NEED** -- the forward one genuinely runs unbounded (the balance axis, `paychecks_from`), while the backward one has exactly two readers, a calendar MONTH and a calendar YEAR, so it never reaches twelve months below the record and the mirror was aesthetic. **And NULL is what every owner nobody asked holds**, so the absence of a question stood in for an answer -- the guessing R-IA set out to stop, relocated into the default. DIRECTION decided it: over-counting a year-to-date retires the FICA wage base and exhausts an `annual_cap` early, understating the deduction and the tax and so OVERSTATING net, where under-counting does the reverse, and an application that budgets should guess poor. Priced by adversarial review at **`$1,437.91`** of Social Security tax not withheld on a `$200,000` salary whose record opens 2026-07-02. The flip also makes migration `a7e3c95d2f18` inert in BEHAVIOUR as well as in DDL, and leaves `CALENDAR_DATE_MIN` as the column's lower bound rather than a meaning. Rejected: keeping R-IA's default with better form wording (the engine does not read wording), and backfilling `min(pay_periods.start_date)` (it states as fact the guess **N-390** measured wrong, and leaves the developer's own defect live after deploy) |
| balance | R-IG | -- | 2026-08-31 (developer) | **A migration installs the books-boundary arms it DECLARED and censused, never whatever the shared builder has since grown into.** `apply_opening_infrastructure` takes a required `arms=` and states the boundary TOTALLY, so an arm a revision does not name is DROPPED rather than left standing -- which is what makes a downgrade one call naming one fewer arm instead of a frozen copy of the previous function bodies. Ruled after a clone stopped at `c9f4b1e78d02` came up carrying the matched-line triggers LIVE, five revisions before the census that validates them. |
| balance | R-IH | -- | 2026-08-31 (developer) | **A matched BANK LINE bounds its account's opening exactly as a settled movement does, and as a SEPARATE term rather than a widening of the movement row set.** A match settles every member on the LATEST of its bank days, so a group's earliest line posts strictly BEFORE the row explaining it settles, and every day in that window passes the movement bound while the line's money sits inside the opening equity and a settled row at once. R-HG's closing-balance boundary, applied to the second row set. |
| balance | R-IJ | -- | 2026-08-31 (developer) | **At what date a loan's contractual TERMS resolve: on the INSTALLMENT they govern, never on a read date -- as ruling `D5` already put a payment's escrow, so a resolver reads no wall clock.** Three producers read a clock instead: amount rule 4's P&I, pinned at `date.today()` when `amount_basis` builds `LoanPricing` while the escrow beside it in the same sum resolves on the shadow's due date (**N-40**); the escrow-subtraction threshold `contractual_pi`, one wall-clock P&I against a per-installment escrow (**N-409**); and the amortization schedule, adding ONE `monthly_escrow` resolved today to every projected row (**N-410**). Every neighbouring producer already keys on the installment -- the genesis split (`loan_ledger._split:330`), the forward charge (`_plan._charges_for:552`), and the tier pricing an installment NO ROW covers (`standing_installment_cash`, ruling **R-R33**, whose docstring already calls that figure the correct one). So ONE fold priced two adjacent installments two ways -- the materialised row at today's P&I, the next past the horizon at its own, the split R-R33 deletes. Measured with the app's own producers: `$0.00` on production (both Mortgage recasts past, the Van fixed-rate), `$216.37`/month too LOW from a recast recorded effective 2027-12-01, `$425.36`/month on a 5/1 ARM from its adjustment. **The consequence accepted with the rule**: `contractual_pi` is the loan CARD's figure today, so per-installment keying makes card and threshold differ for a historic payment, correctly. `LoanPricing` then takes no `as_of` at all, so `amount_basis` stops reading the clock and N-40 closes structurally rather than by threading a different one; `X-i2` loses this derivation as a subject. Rejected: the read pass's `as_of` (one clock per pass, but still ONE figure for every installment); the PAY-PERIOD start, already refused for escrow by `D5`/`N-34`. `D5` is a loan-arc ruling, archived, with no row here. Owned by `X-au-g-2b` |
| bank_import | R-FP | -- | 2026-08-13 (developer) | **A statement importer is a SOURCE ADAPTER over one normalized line shape**; matching, review and fact-writing are source-independent, so the file path ships first and an automated source is additive. `FITID` is the idempotency key -- 342 of 342 distinct in the developer's own OFX -- so re-importing a file cannot duplicate. **A match is a PROPOSAL, never a silent apply**: it is reviewed before commit, every corrected `settled_on` goes through `system.audit_log`, and unmatched lines on BOTH sides are shown rather than guessed at. Rejected for the automated source: OFX Direct Connect, which needs no third party but stores the credentials that grant FULL account access, against SimpleFIN / Plaid, which store a revocable READ-ONLY token -- the strict option is the revocable one even though it adds a vendor. Also recorded, because it is what makes the file path first: the developer's exports already cover both accounts, so the matcher can be GRADED before an adapter exists **Amended 2026-08-24 by R-GH**: the a-match-is-a-proposal half now holds only for acts that MODIFY a hand-made row; a standing rule is consent for a NEW swipe's creation. The source-adapter, idempotency and security grounds stand. |
| bank_import | R-FS | -- | 2026-08-16 (developer) | **A MATCH has three shapes, because the grain mismatch runs in BOTH directions.** Measured on the developer's own accounts 2026-08-16: the bank shows 156 individual card-swipe lines where the app holds 34 envelope rows, and shows ONE payroll deposit where the app holds three rows (`$2,473.38` + `$100.00` + `$39.54`). So a match is (1) one line to one row, (2) a GROUP -- N app rows summing to one line, or N lines summing to one row -- with the bank's day and the clearing link written to every member, or (3) a bank line the app has no row for BECOMING a purchase against an envelope the owner picks. The third is what makes the app's records reach the bank's grain permanently, and `balance:X-f3b` is what makes it work: a purchase carrying a posting day is already a cash movement of its own whose envelope books only the remainder. Rejected: one-to-one only, which explains 119 of 227 lines and leaves half the account with no clearing fact at all |
| bank_import | R-FT | -- | 2026-08-16 (developer) | **An import PERSISTS a normalized line table**, not nothing and not an id ledger. `budget.bank_statement_lines` under a `budget.statement_imports` batch, with accepted matches recorded separately and PROPOSALS never stored. Uniqueness on the line's identity makes re-import idempotent STRUCTURALLY rather than by the importer remembering to look -- and the same table is the fact `balance:X-f3a-2` needs (a statement walked line by line, so a line it did not show is NOT CLEARED) and the provenance `balance:X-f3c` needs to re-open a recorded difference. One table serves three steps; storing nothing would have each invent its own |
| bank_import | R-FU | -- | 2026-08-16 (developer, on measurement) | **The source is SECU's CSV with running balances, and a line's identity is POSITIONAL rather than the bank's own id.** QBO and QFX are the same file as the OFX, so the real choice was CSV or OFX: the OFX truncates most descriptions to 32 characters and carries no per-line balance, while the CSV carries the merchant, the bank's category and a running balance, and its description starts with the OFX name on every shared line. What the CSV lacks is `FITID`, and **R-FP's idempotency key is superseded on measurement**: the positional key `(account, posted_on, amount, sequence within that group)` reproduced the `FITID` key exactly across two exports twelve days apart, so identity is one rule for every adapter and an external id is stored as corroboration. The ordinal is what makes it total: two real charges may share a day and an amount. **AMENDED 2026-08-16 by the step's own adversarial review, because the measurement is blind on the key's only novel component**: no group exceeded one member in either export, so the ordinal was 0 throughout and the comparison tested `(day, amount)` against `FITID` and never the ordinal -- the one term that is derived rather than observed, and the one that can be unstable. The honest statement is therefore: identity is positional, and the positional key is proven equivalent to the bank's own id ONLY on data where no group exceeded one member. **AMENDED AGAIN 2026-08-20 (developer), and this is the amendment the first one asked for**: the positional key STAYS the stored identity and the ordinal STOPS being a term any re-import compares against. Two same-day same-amount lines re-exported in the other order refused the WHOLE file, thirty days of genuinely new lines with it, and told the owner the bank had restated a line it had not; a genuinely NEW line the bank INSERTED ahead of a recorded one was refused the same way. Both are one defect: an app-assigned number compared as though the bank had supplied it, a derived value stored beside its source with nothing reconciling the two. A re-import now reconciles a `(posted_on, amount)` GROUP as a MULTISET on the wording the bank wrote, and mints an ordinal only for a line it has decided is new -- so the description compare stops being what holds the key total and becomes only what **R-FL** asks of it. **The refusal is unchanged** where BOTH halves of a group hold a member the other cannot account for, the only shape that is a contradiction rather than a change in what the export covers; neither half ALONE is one. The event stays unobserved, and **R-GB**'s repair door is what it costs when it happens |
| bank_import | R-FV | -- | 2026-08-17 (developer) | **A match is stored as IDENTITY, and the CLEARING LINK is not written from one.** The two answer different questions: a match says *which real movement this app row is*, and `transactions.reconciled_by_id` (**R-FL**) says *which declared balance already contains it*. The second is DERIVABLE from the first once a statement carries the line -- anything matched to a line on a statement covering day D is inside any balance declared for D or later -- while the first is not derivable from the second, so the app stores the fact and derives the rest. `reconciled_by_id` names an `account_anchor_history` row, a balance the owner typed, and a bank line names none. What an accepted match writes is the bank's posted day, which makes the date rule's INPUT an observation instead of a guess. **It does not make that rule correct, and a first draft of this ruling said it did** -- R-FL rejects *"sharpening the date rule with the bank's posted day"* by name, because a civil day still cannot tell a mid-day reading from a late-posting item. The honest claim is narrower: the same rule now runs on a true day, which is strictly better and is still a guess until `X-f3c`. **The settle doors RELEASE any prior link as they move the day**, inherited from `X-f3a-1`: the bank contradicts the DAY, not the STATEMENT, and an assertion observed weeks later still contains the row. The release is safe (`_recorded_anchor_id` falls back rather than raising) and it is a LOSS -- a row the owner had ticked goes CLEARED back to UNKNOWN; **N-307** owns the class. **Also ruled, on the same principle:** an unbalanced group is REFUSED and its difference NAMED, never apportioned (which needs a rule about which member is wrong -- a decision about a paycheck, not a matcher's) and never absorbed by a tolerance (which would silence **N-239**); **that refusal is SUPERSEDED 2026-08-22 by R-GD**, which RECORDS the variance instead, on the measurement that the refusal is what produced the duplicate Geico. And a match MAY settle a still-Projected row, because a statement is evidence that money moved -- which the GROUP path must reach too, or a split payroll deposit with one unsettled member is unproposable. **Rejected:** writing the link to whatever assertion the date rule already picks, which stores a copy of a derivation; and recording the BANK's own daily closing balances as assertions, which is the best data available but would move Checking's OPENING assertion back and redefine the account's opening equity -- the cutover's decision, recorded as **N-304** and owned by `balance:X-f3c` |
| bank_import | R-FW | -- | 2026-08-18 (developer, on measurement) | **A match corrects a purchase's PURCHASE day as well as its posting day, and it corrects only the day the bank CONTRADICTS.** A purchase carries two clocks -- `purchased_on`, the day it was made, and `settled_on`, the day the bank took the money -- and R-FV ruled only the second. **What forced the first is that the step below it was measured to be building a duplicate machine**: lines the matcher leaves unexplained include ones at an exact amount and merchant of an app purchase the screen ALSO lists as unexplained, not offered because `entry_service` correctly refuses a posting day earlier than the purchase day -- so the create-a-purchase door would have invited the owner to record each of them a SECOND time. The app HAS these rows, and what is wrong is the day. **The day it writes is the one the bank STATED, not the one it cleared on**: SECU states the swipe day inside a card line's description (`DATE 08-13`) and the adapter had been copying `posted_on` into `transaction_on` on every line, so the column could not distinguish an observation from a restatement. That column is now NULLABLE and the NULL means *this source states none*. **Rejected: taking the bank's day unconditionally**, the symmetric-sounding rule, which is measurably worse -- it replaces dates the owner got right to fix the few they got wrong, many of them onto a CLEARING day because the source states no swipe day at all. Correcting only what is REFUTED moves only the impossibilities. **Also rejected: relaxing `ck_transaction_entries_settled_not_before_purchase`** -- money cannot leave an account before it is spent, and the constraint is what makes the correction necessary rather than optional. **AMENDED the same day by three adversarial reviews.** (1) Making the pairing LEGAL removed the only bound an undated purchase had: `DAY_WINDOW` is measured from `settled_on` and such a row has none, so the app offered to re-date a purchase by weeks on an exact-amount coincidence, overwriting the one fact that would have exposed the mis-pairing. A purchase is never truly undated -- it is anchored on the day it was MADE. (2) What REFUTES a purchase day is the EARLIEST line of a match, not the latest: money cannot leave before it is spent, so testing against the latest leaves an impossibility standing that `update_entry` could not catch either. (3) The day is read from the bank's DESCRIPTION cell rather than the `Description \| Memo` text the row stores, so a user's own memo cannot state it |
| bank_import | R-FX | -- | 2026-08-19 (developer) | **A bank line the app has no row for becomes a PURCHASE, never a bare transaction, and the app may CREATE the budget line that holds it.** R-FS's third shape said "against an envelope the owner picks", and measurement made that insufficient: lines fall in periods whose every envelope closed at a stored figure, and several merchants have no envelope in any period. A purchase is the app's record of one payment and a transaction is a budget line that reserves, so recording a payment as a budget line would collapse two facts a purchase keeps apart and leave the next statement line for that merchant nowhere to go -- which is why the created container is always an ENVELOPE, born Projected budgeting `0.00` and closed from its own entries. **Also ruled, and it is the money half:** a settled row admits a NEW purchase exactly when its recorded figure IS its purchases AND that purchase states the day the bank took it. On a stored-figure row the gross cannot rise and `settled_cash_leg` subtracts money it never held, an expense row publishing an inflow; on ANY settled row an undated purchase moves the row's own leg on the day the row CLOSED, a past day with no evidence. `EntryDetails` therefore carries `settled_on`, which the create door had deliberately excluded on a premise true only of the hand-typed form. **It does NOT answer the carry-forward double count**: every carried-forward source settles on the admitted basis, and **N-249** already records that with the developer's 2026-08-12 remedy (reconcile the rollover), which explicitly REJECTED refusing late purchases -- so this ruling applies a decision already taken rather than making a new one. **Rejected:** refusing the add and sending the owner through revert / add / re-close, which rewrites the envelope's settle day and its statement link to record the bank's own evidence. **Amended 2026-08-24 by R-GJ**: a line matching a card-payment signature has NO create arm at all |
| bank_import | R-FZ | -- | 2026-08-19 (developer) | **A whole reviewed pass is ONE request, and four decisions are what keep it a REVIEW rather than a sweep.** **(a) A refused item leaves nothing behind and the rest still land**, each refusal quoted in the service's own sentence -- so one `$0.05` payroll gap (**N-239**) cannot cost 123 good corrections; measured, 5 of the developer's own 124 proposals refuse today on a settled credit-card payback whose figure has drifted (**N-323**). Each item is its own SAVEPOINT; the request is still the transaction, and a `PostingError` still fails the whole thing loud. **(b) The destination select IS the tick**, defaulting to *leave this line alone*: it used to default to the first envelope in the line's pay period, which has already CLOSED at a fixed figure on 78 of the 91 lines, and its category select to whichever category sorts first ("Auto: Property Tax"). One press per line hid that; one press for forty would not. **(c) Proposals are swept per CLASS, never as one "tick all"** -- they partition into 27 that only confirm a day the app already had, 46 that move one it got wrong and 51 that mark a row as having happened, and the riskiest class may not ride the same click as the safest. **(d) Where two ticked items collide over one envelope the PROPOSAL wins** and the recorded line is refused, because a proposal explains money the records already hold against a line the bank showed where the line can be re-aimed next pass -- 4 envelopes and 15 of the 91 lines, on the developer's own statement. **The outcome is rendered on the screen rather than flashed**: a flash rides in the signed session cookie, the longest sentence a batch item can carry is 497 bytes, and a browser stores 4 KB. **Amended 2026-08-24 by R-GH**: the four decisions survive on the exception queue only; a rule-covered NEW swipe no longer rides a reviewed pass. |
| bank_import | R-GA | -- | 2026-08-19 (developer) | **Where a merchant's spending goes is a DECISION the owner STATES, stored, and the app reads only to SUGGEST.** The unexplained outflows on a statement are a few times fewer MERCHANTS than lines, so the screen was asking the same question many times over. **Four things were decided.** **(a) The merchant is a NULLABLE COLUMN the adapter records**, not a token a reader parses: a rule that MATCHES on it is a stronger claim than a reader can carry, because a reader has to be TOTAL and SECU's own OFX truncates most descriptions to the identical 32 characters, so one policy would fire on every merchant behind them. NULL means *this source names none* and keys nothing, which is the direction a missing fact has to fail in; it is R-FW's shape for `transaction_on` on a second column. **(b) A policy is STORED, not derived from where that merchant's lines went before**, on three grounds: past placement is EVIDENCE and the next one is a DECISION; a derivation moves when history is edited; and a derivation cannot express *never a purchase* at all, because a line left alone leaves no trace. **(c) An answer names a TEMPLATE, a NEW ENVELOPE (name + category), or NEVER**, and those three are the complete set rather than a menu: a budget line either has a period-independent identity or it does not. It may not name a `transactions` row -- an envelope belongs to ONE pay period and a merchant's lines fall in many -- and it may not name the NAME, because one template generated rows under two different names. **A policy is restated or withdrawn freely and is expected to be**: when the credit-card arc gives a card its own account the Checking-side answer stops being right, which is why the key carries the account. **(d) A policy SUGGESTS and never TICKS.** Ruling **R-FZ** made the destination select the tick and its default *leave this line alone*; a remembered destination arriving already selected is still a default nobody chose. So the placement is rendered beside the control, one sweep sets them (the shape R-FZ(c)'s per-class sweep already has), and the policy section is its OWN form posting to its OWN door because it moves no money. **R-FZ's undo-based half of that reason is spent as of 2026-08-24 (R-GG) and the decision is not.** **Also ruled, and it is finding N-325**: a line the bank dates MADE after it POSTED is not offered as a purchase and is COUNTED on `ReviewBounds` like every other bound this screen states. Rejected: clamping the purchase day to the earlier of the two, which decides which day the app believes when the bank contradicts itself -- the substitution **R-FW** refused one clock over. **Amended 2026-08-24 by R-GI**: a stated answer is a standing RULE that auto-applies to NEW swipes under R-GH; suggest-only was measured unable to deliver the developer's stated goal |
| bank_import | R-FY | -- | 2026-08-19 (developer, on measurement) | **A row the app has never marked as paid is bounded by its OWN PAY PERIOD, widened by `DAY_WINDOW` at each end -- not by the statement's covered span, and not by nothing.** Such a row carries no observation of when its money moved, and that was read as *no bound at all*: any bank line sharing its amount could claim it, from any date whatever, so a projection budgeted months later would settle against this month's line and file real money under the wrong paycheck. The pay period is what the app itself asserts about when that money moves, and it is the WHOLE of what it asserts, so both ends travel. **Rejected: the statement's covered span** (**N-312**'s proposal) -- a property of how much history the owner happened to export rather than of the money, so the same two rows match or do not depending on the file. **Also rejected: the earlier reasoning that a bill must stay unbounded**, which held that bounding it would refuse the arm that settles a row nobody has marked as having happened: every row that arm settles is a PURCHASE, already bounded by the day it was made. **Also ruled: the clause that hides an already-matched envelope from the create door's destination list STAYS WHOLE.** **N-317** was right that it is wider than the money needs, and what this decides is only that a `$0.00` benefit does not buy a change to a money guard. **What must NOT be narrowed with it** is `_accept._reject_parent_and_its_own_purchase`: stamping an EXISTING unposted purchase whose parent is matched DOES move that parent's leg, a different act. **AMENDED 2026-08-20 (developer): N-317 is RETIRED and the clause still stays whole.** What it records is a DECISION -- a guard deliberately wider than the money needs, with `destinations_for`'s docstring carrying the argument -- rather than work owed. Keeping open a row nobody will ever act on weakens conventions rule 1 for every row beside it, and the honest way to retire one a ruling holds open is to amend the RULING rather than let a step close it around the ruling's back. No code changes with this. **And the bound is on what the matcher OFFERS, never on what the accept door WRITES**: the hand-build form exists so an owner may assert a grouping the proposer would not guess, and a date bound there would refuse the one act **R-FP** reserves to them |
| bank_import | R-GB | -- | 2026-08-20 (developer) | **An import can be UNDONE, and the undo is BALANCE-NEUTRAL by construction.** Finding **N-302** measured the dead end: no door in `app/` deleted an import, a line or a recorded account pairing, so a single refusal ended that account's ability to import for good while the message promised a human repair the app could not perform. Deleting removes what the BANK said; a settle day an accepted match wrote is the APP's own record and stays, which is `release_match`'s own rule and the reason the act moves no money. **Three decisions, each with its rejected alternative.** (1) A match naming a deleted line is RELEASED, through the one door that releases a match -- rejected: refusing the delete until the owner undoes them by hand, which reaches the same end state through a chore, and a match spanning two imports would block deleting either. (2) The source-account pairing is FORGOTTEN with the LAST import from that source, because it is a fact LEARNED from an import and should not outlive every import that taught it -- which is what makes a first import that named the wrong Shekel account repairable at all. Rejected: a separate *forget this pairing* control, which is stricter but adds a second door and a state nothing else produces, a pairing with no import behind it. (3) `fk_statement_match_members_line_account` becomes NO ACTION, so the DATABASE refuses to orphan a match. **That asymmetry is the point**: losing app ROWS is visible on the review screen and those keys still CASCADE, where losing bank LINES was invisible and permanent -- an act left standing with no line members claims its transactions forever, with no screen able to render it and no button able to free it. NO ACTION rather than RESTRICT because RESTRICT is checked per row where NO ACTION defers to the end of the statement, and a whole-account delete cascades matches, members, imports and lines inside ONE statement. **AMENDED 2026-08-24 by R-GG, and the amended term is the headline**: the undo is balance-neutral EXCEPT for the rows the REVIEW created from these lines, which go with them. A settle day a match wrote is still the app's own record and still stays; a purchase that exists only because a bank line did is not, and destroying the line while keeping it leaves a movement in the books that nothing accounts for. The receipt and the confirmation both name the count and the figure, from the release door's own derivation. **Also ruled: the review-screen divergence resolver is REJECTED**, because with **R-FU** amended the only conflict that survives is a real restatement, and a screen for negotiating one is machinery for an event measured 0 times |
| bank_import | R-GC | -- | 2026-08-20 (developer) | **A NEW-ENVELOPE merchant answer converges on ONE envelope per pay period, and the convergence is a SUGGESTION.** Finding **N-327**: the answer minted one envelope PER LINE, so a policy fragmented the very budget line it names -- one press could make several envelopes with two in the same period, and the next statement made more beside them. No figure was wrong; what fragmented was the budget. **Two halves, because they are two problems.** ACROSS statements the SUGGESTION converges: the placement resolves to a `RECORD_IN` against an envelope of that name already in the line's own period, and TWO of that name is a guess so it is REPORTED -- the same rule, and the same sentence shape, a template answer already takes for a template that generated two rows in one period. WITHIN one press the WRITE converges, through a per-REQUEST registry, because at render time the envelope the press is about to create does not exist for any select to name. **The per-line select is untouched and still opens on *leave this line alone*** (**R-FZ**, **R-GA**), so two purchases from one merchant may still go to two different envelopes, and the screen prints which line CREATES and which JOINS before the press rather than the receipt saying it after. **It moves no money**: each purchase carries its own posting day and is its own cash movement, leaving an envelope's own leg at `0.00` either way. Rejected: converging silently inside the create door, which would take away a deliberate second envelope of one name and is the substitution every other arm of this screen refuses; and converging on the NAME alone, which would file a March swipe into an April budget line. **What it does NOT fix is finding N-328**, the reason the convergence has to compare a string at all: an ad-hoc envelope carries no identity across periods, and `X-f6c` is where the answer names a TEMPLATE instead |
| bank_import | R-GE | -- | 2026-08-22 (developer) | **A bank statement's evidence justifies CORRECTING a settled purchase's amount, and it reaches that row from a MATCH and from nowhere else.** `entry_service._reject_settled_parent` refuses `amount` on a settled parent (finding **N-229**, widened to the settled BAND at `balance:X-au-c3`) because re-pricing a row whose money has moved would move it again -- and that reason holds for the act it was written about, a human typing a different number on their own second thoughts. **A bank line is not that act**, which is the argument **R-FX** already accepted one door over when it ruled that the same evidence justifies ADDING a purchase to a settled row: *adding one on a row whose figure IS its purchases raises that cost by exactly the figure a bank statement just showed*. Correcting one lowers or raises it by exactly the same kind of figure, from the same source. **The bound is the DOOR, not the row**: the ordinary edit form still refuses, so N-229 is unchanged everywhere a human supplies the number, and the permission exists only where a statement does. Measured: 2 of the developer's 10 near misses are purchases under a settled envelope (`Groceries: Walmart`, `$121.12` against the bank's `$121.16`, and `$192.24` against `$192.39`). **Rejected: recording the match and leaving the amount**, which leaves the row disagreeing with the bank about a fact the bank STATES and makes `X-f6e`'s identity fail by exactly that residual -- a variance nobody can attribute, invented where the bank has already said which row is wrong. **Also rejected: refusing outright**, which forces the proposer to withhold those two, and a line the screen will not explain is the line the merchant policy offers to RECORD -- the duplicate **N-335** measures |
| bank_import | R-GD | -- | 2026-08-22 (developer, on measurement) | **The bank's figure is the RECORD, so a match may DISAGREE with the app and the variance is the PRODUCT rather than a refusal.** **(a) A match RECORDS the variance**: the bank's figure becomes the settled figure, the difference is stored and reported, and the door never sends the owner away to retype a number the statement already carries. **R-FV's refusal is INVERTED** -- the payroll residue it refused to silence (**N-239**) becomes named rows on a variance report, and the refusal was never neutral: it is what made recording a duplicate the cheaper act. **(b) The predicate is a SCORE, never a TOLERANCE**, and a blanket tolerance is REJECTED. **The merchant is a REQUIREMENT for a near miss rather than a tie-break** (amended 2026-08-22), matched as a WHOLE WORD, and it stays corroboration for a WEAK claim only -- the exact tier may not require it. **The bound is 0.50% of the line**, RELATIVE, because three cents is 0.017% of a `$178.29` line and 15% of an `$18.64` one. **The RANK is WITHDRAWN** (amended again the same day): amount and day are both incapable of choosing, so a near miss is offered ONLY where it is the sole candidate on BOTH sides, and a contest is COUNTED and named on screen. **A REVIEWED line gets no candidate list either** (amended a third time, 2026-08-23) -- there is no bound at which one is anything but noise. **Nothing scores a GROUP.** Rejected: a separation threshold between best and runner-up, which is the tolerance this ruling rejects under another name. An accepted repricing needs its own count, or the receipt says *"Nothing moved."* over a rewritten figure. **(a)'s variance gets its own SWEEP CLASS**: repricing is the only act on that card that changes what money was SPENT, and **R-FZ(c)** refuses to let the riskiest class ride the safest click. **(c) is SUPERSEDED 2026-08-23 by R-GF**, both halves of it measured false. **(d) An account with statement coverage reconciles from STATEMENTS ONLY**: the panel and the review screen answered one question with two grades of evidence, the panel stays for an account nobody imports, and it owes its own specification pass. **THE GROUP** (amended a fourth time, 2026-08-23). **(i) The difference is an ordinary row the owner ACCEPTS** -- **R-FN** applied here rather than a new rule: no category, so it books to the per-owner `Uncategorized Income` / `Uncategorized Expense` through the existing category-leg rule, settled on the bank's day, categorisable later, and it becomes a MEMBER so `Sigma(lines) = Sigma(members)` holds BY CONSTRUCTION. **The FIGURE travels and the door reconciles it**: the consent box carries what the screen showed, because the SUM is the one number no per-row guard can see. Rejected: a bare consent checkbox carrying no figure, and recording it automatically, which R-FN itself refuses. **(ii) One ROW is determinate however many LINES explain it**, so `bank_cash_for`'s test is on the row rather than the lines. **(iii) The row is named `Statement difference: <merchant>`**, what it is before who paid it. **(iv) The running total is SERVER-computed**, one derivation shared by the screen and the door -- no JavaScript sum, and no implicit `ROUND_HALF_EVEN` on the one field the design says is exact. **(v) UNDO removes what the act CREATED**, so a row the owner has since edited refuses it; its mechanism is SUPERSEDED 2026-08-24 by **R-GG(b)**. **Rejected: widening the residual to the other refusals** -- a 1:1 whose row's figure is not its own stays refused. **The sign check applies to a group** and mixed signs INSIDE one stay legal; the not-its-own-figure and transfer-shadow census is asked of EVERY member, a sum past `Numeric(12, 2)` is a refusal, and a ZERO side opposes nothing |
| bank_import | R-GF | -- | 2026-08-23 (developer, on measurement) | **A statement's stated balance is an OBSERVATION whose EFFECTIVE DAY the lines solve, and the app records the day and how it was pinned rather than storing two balances derived from it.** SUPERSEDES **R-GD(c)**'s source and its refusal, both measured false. **The source**: no OFX adapter is built -- the two formats state the identical figure, and the CSV already carries the balance, the merchant, the bank's category and the `Totals:` self-check. **The day**: given a known opening, the stated figure is effective at day `d` iff `stated - Sigma(lines posted on or before d) == opening`, over the candidates {the day before the first line} + {every day the file covers}. Two hits differ only by lines netting zero, so take the latest, and a refusal is reserved for a file where NO day solves. **The known opening is a TOTAL DISPATCH naming three BASES**: `line_chain` (the file states a balance beside every line), `reconciled` (the recorded history determines it and exactly one day fits), `assumed_last_day` (nothing constrains it -- a first import -- so the figure is the balance after the last line, and the receipt SAYS SO, because an assumed anchor propagates to every later import and only a constant offset betrays it). **Two derived columns are DROPPED**: `opening_balance` and `closing_balance` are `stated - Sigma` and `opening + Sigma`, stored beside their source with nothing reconciling them. **The app-vs-bank comparison is an INSTRUMENT, never a gate** -- a refusal deadlocks, because the import is what fixes the disagreement and a first import has matched nothing; the disagreement itself is **N-314**, owned by `balance:X-f3c`. Rejected: gating on the closing day alone, which still refuses every first import. **AMENDED the same day by three adversarial reviews.** **(i) The basis is the WEAKEST LINK**: a solved day is only as good as the opening it was solved against, so there is ONE fact rather than two and the column records the minimum over the chain -- `file_chain` / `corroborated` / `uncorroborated` -- and no re-upload can strengthen anything. **(ii) The doors that change LINES release the anchors they undercut**, keyed on the days whose lines actually moved, so a delete removing 0 lines releases nothing. **(iii) The ONLY refusal is a file contradicting ITSELF**: only the file's own running-balance chain can convict the file, never a mismatch against recorded history. **(iv) Both the solve and the assumed day are BOUNDED by the day the header names**, or the door writes a row `ck_statement_imports_effective_day_within_file` rejects. **(v) The anchor to walk from is the STRONGEST, not the most recent**, and the ladder is read from the enum rather than from ref-table row ids. Also: the first anchor an account gets is still an assumption every later import inherits (**N-342**) |
| bank_import | R-GH | -- | 2026-08-24 (developer) | **Consent splits by ACT CLASS, and a standing rule is consent** (amends **R-FP**'s a-match-is-a-proposal half, **R-FZ**'s four decisions and **R-GA**). An act that CREATES a row from a NEW bank swipe under a rule the owner stated AUTO-APPLIES at import, itemised on a receipt whose every item carries the one-click undo R-GG built; an act that MODIFIES a row the owner made by hand -- re-date, re-price, settle, group-match -- remains a proposal needing its tick. Rejected: full auto over modifications, because a re-price of a hand-entered figure is a correction of the OWNER and review is the point; and keeping consent-per-line, measured unable to deliver the developer's stated goal (Food Lion's fortieth swipe needed the same human act as its first). Argument and worked example: `docs/audits/bank_import_redesign/README.md` P-1. |
| bank_import | R-GI | -- | 2026-08-24 (developer) | **A stated merchant answer is a standing RULE, scoped and revocable** (amends **R-GA**; the identity half is `X-f6c`'s template ruling unchanged). The answer set is {existing template, new template first time, always ask, never a purchase}; a rule applies only to NEW swipe lines, is revocable where it was stated, and every application is receipted. Stating one still moves no money -- the money moves at IMPORT time, under R-GH. Argument: README P-2. |
| bank_import | R-GJ | -- | 2026-08-24 (developer) | **A line matching a card-payment signature can NEVER become a purchase** (amends **R-FX**). Its only arms are a group match to payback rows with the difference NAMED, or PARK as a card payment awaiting its home -- which is the card arc's ruled transfer-shadow model (**N-337**, owner `CC3b`), so this is an interim GUARD that closes nothing of that row. Argument: README P-3. **Amended 2026-08-24 (developer, on measurement), and the amendment is what the SIGNATURE turned out to be.** It is the OWNER's own answer plus an OBSERVATION, and neither is a question. *Never a purchase* -- `merchant_destinations`' third answer -- stops being a CAPTION and becomes a structural bar: it had withheld a sweep value while the line's own destination select went on offering every envelope in the period and `create_purchase_from_line` read no policy at all, so the screen printed *nothing here records it* directly above a control that did. And a line a SOURCE files as a payment to an ACCOUNT THE OWNER HOLDS has no create arm at all -- **no answer lifts it**, and `state_policies` refuses a template or new-envelope answer for such a merchant outright so a contradicting answer cannot sit stored and inert. **A first build made that second bar an UNANSWERED state any answer lifted, and two adversarial reviews measured the hole**: the answer that lifts it is *a new envelope*, which is the answer the developer had saved for the card and the one that booked every card payment through a single sweep click. The permissive arm's reasoning -- that the source sometimes files a LOAN payment under this signature -- was imprecision about WHICH account rather than error: the app acts on *is this spending* and the answer is NO either way, so only the WORDING was wrong and the wording is what was fixed. **The known cost, stated:** genuine spending a source ever files under one of these categories has no create arm and no answer opens one, which is the deliberate trade against a measured one-click double count. **From scratch there is no card-payment code in this layer at all** -- paying a card is a transfer, the checking line matches the transfer's shadow 1:1 on the exact tier, and the second bar deletes whole when `CC3b` ships; the first is permanent. Shipped at `X-ga` (`73a36cf4`) |
| bank_import | R-GK | -- | 2026-08-24 (developer) | **An envelope FILLS from the bank.** It stays open through its period, rule-assigned purchases flow in dated by the bank, and it closes when the period's statement coverage is complete (`balance:X-f3a-2`'s walked statements) or when the owner closes it by hand; hand entries stay for cash and judgment envelopes. The owner's settle-early habit moves -- his own decision, 2026-08-24. Details are OWED to the `X-gg` design loop: carry-forward, the settle machine and the grid's chip language all touch this. Argument and worked example: README P-4. |
| bank_import | R-GL | -- | 2026-08-24 (developer); SUPERSEDED 2026-09-03 | **SUPERSEDED by `balance:R-IS` and `R-JN`, and its step `X-gh` WITHDRAWN** (2026-09-03): under the level relation the bank's closing is an OBSERVATION and asserts nothing. As ruled: **A confirmed import's anchor may ASSERT the opening when the day's residue is zero**, and a nonzero residue surfaces as the exception it is; the hand true-up door stays, demoted from habit to correction. Sequenced AFTER `balance:X-f3c`, because the ruling must be written against the post-cutover assertion rather than the one `X-f4` deletes. Measured ground: 60 hand true-ups on Checking, the last equal to the bank's own closing to the cent. Argument: README P-5. |
| bank_import | R-GG | -- | 2026-08-24 (developer, on measurement) | **An act that CREATES a row owes an inverse that REMOVES it, and what an act made is a different relation from what it names.** Findings **N-333** and **N-340**. **Four decisions.** **(a) The undo removes what the act CREATED, and a row the owner has EDITED since REFUSES it** -- the rule `X-f6d-4` applies to a group's residual, widened to the arm left out of it. The predicate is the subject's own `version_id`, because *still uncategorised and still at the figure we wrote and still holding no purchases* is three guesses about which edits matter where a counter that moves on every ORM update is the fact itself. **(b) What an act CREATED is its OWN relation**, `budget.statement_match_creations`, and `statement_match_members.created_version_id` is DROPPED: a column on the membership can only mark a subject the act also NAMES, and the create-a-purchase arm makes two things while the CONTAINER may not be a member -- naming an envelope beside its own purchase counts the same money twice (**R-FM**) and breaks `Sigma(lines) = Sigma(members)`. **A SUBJECT and a CONTAINER are told apart by MEMBERSHIP rather than by a second column**, which cannot drift from the members that define it. **(c) A container is removed only when it is now EMPTY and untouched, and it NEVER refuses.** A purchase is what the act is about, so an edited one stops the undo; a budget line is not, and one left standing budgets `0.00`, holds nothing and books nothing. A multi-line envelope survives every undo, empty, because each later line re-closes it -- accepted, because the alternative is enumerating which writes are the pass's own, the guess a revision counter exists to replace. **(d) N-229's REMOVAL half is corrected at the ROOT rather than bypassed**, in `entry_service._reject_settled_removal`, `_reject_settled_addition`'s mirror: a purchase that is CREDIT or POSTED sits in the settled figure AND in a subtracted term, so removing it moves both by the same amount. An UNPOSTED purchase's removal re-prices the close on a past day with no evidence and stays refused, as do the archive and a stored-figure settlement (**R-FX**'s reasons). **A door that can only go one way is the defect**: the ADD was already admitted by R-FX and the REMOVE was not. **(e) Deleting an IMPORT takes the rows the review created from its lines, which AMENDS R-GB**: that undo is balance-neutral EXCEPT for what the review created, because a row that exists only because a line did states nothing once the line is destroyed. Both the confirmation and the receipt name the count and the figure, from the release door's own derivation (`planned_removals`) rather than a cheaper estimate -- a confirmation that counted differently from the act it confirms would be a confirmation that lies |
| bank_import | R-GM | -- | 2026-08-25 (developer) | **A row LEAVING the books withdraws the accepted matches it was the last app row of**, so those bank lines are unexplained again in the same request rather than waiting for the owner to notice an amber act on the review screen. Every door an owner presses asks one rule (`app/services/match_withdrawal.py`): the transaction delete verb, the purchase door, both CC-payback teardowns and the transfer delete. **Narrowed on measurement**: withdrawing on the loss of ANY member destroyed a group act that was still two-thirds true and silently un-matched its survivors, so a PARTIAL loss stays `AcceptedGroup.agrees`' case -- the two mechanisms split on a predicate instead of shadowing each other. A SOFT delete withdraws nothing: the member survives a flag change and `restore_transfer` puts the rows back. Rows a withdrawn act CREATED are LEFT standing and COUNTED, because deleting one row is not withdrawing a decision -- `release_match` stays the owner's undo and the only door that destroys what an act made. **The INVARIANT is not the call sites**: `matched_subjects` and `_spoken_for` stopped counting a line as explained while its act names no app row (`_candidates.act_still_names_a_row`), because four more doors reach that state in bulk SQL and a rule enforced by enumeration is one the next door forgets. **Ruled on a CORRECTED premise**: the recommendation put to the developer said the review screen did not catch this at all, which was measured FALSE against `_still_holds` and `TestAnAcceptedMatchStopsAgreeingWhenItStopsHolding`; the correction was put back to him and the automatic withdrawal was chosen anyway, on the ground that a deliberate delete leaves nothing to re-review. Shipped at `X-gb` (`ec346c46`). |
| bank_import | R-GN | -- | 2026-08-25 (developer) | **An import's receipt warns when the app GUESSED which day a stated balance belongs to, and not when the corroboration LEVEL is merely weak.** The two were one signal and the evidence ladder cannot separate them: `uncorroborated` is minted BOTH by a solve against an unconfirmed opening -- where the day is proven -- and by the arm that has nothing to solve against and takes the file's last line. So the receipt warned on every import the owner could perform, which is what teaches a reader to dismiss the warnings that matter, while a guessed placement and a proven one read identically. `ImportedBalance.day_is_solved` carries the missing fact, stated by each arm of `resolve_anchor` rather than derived (an assumed day is `min(period_end, stated_balance_on)`, and a solved day can land there by coincidence, so no reader can recover it from the stored columns -- which is why the imports TABLE still cannot say it and a column for it is not taken here). **Ruled on a CORRECTED premise, and the correction is the point.** The recommendation first put to the developer said `uncorroborated` was the only rung his account could ever reach; that was measured FALSE the same day -- his 2026-07-19 export carries `Running Balance`, reaches `file_chain` when imported (306 lines, `$2,229.73` at 07-17), and a statement opening 07-18 then reads `corroborated`. Measured cost of the ruled arm on his real books: **zero** -- one contiguous recorded run 2026-01-02..2026-08-21, so his next import's day is SOLVED and reports green; it fires only on a genuine gap, clears by importing the missing span, and never depends on the dropped column returning. Shipped at `X-gc`. |
| bank_import | R-GO | -- | 2026-08-25 (developer) | **The review panel MARKS only what the app can prove the bank never shows alone, and never guesses.** A row whose figure is not its own to state -- a CC payback, an envelope whose figure is its purchases -- is one the bank accounts for through some other row, and the caption's *a payment your records claim happened and your bank did not make* is false of it. Those rows are ANNOTATED and never withheld: the same card is the hand-build form's row-picker, and **R-GJ** leaves the group match as a parked card payment's only ACT, so dropping the 18 CC Paybacks would have closed the one path that ruling kept open -- which is what this step's own plan sentence, read literally, said to do. **The audit's two named classes are deliberately NOT delivered and this records why**: the 7 envelope-tracked containers holding no entries (`Groceries` `$163.95` .. `Mint Mobile` `$132.69`) and the 10 payroll allowance rows carry no fact saying the bank showed them inside something else, and the only predicates that would claim them -- a row-name list, or `tracks_purchases`, which would silence the alarm on a `Mint Mobile` that is plainly one bank line -- fail in the one direction this caveat may not. They stay under a caption that no longer over-claims of them; being buried among 67 rows is `X-gf`'s. Shipped at `X-gc`. |
| bank_import | R-GP | -- | 2026-08-25 (developer) | **The destructive-action dialog binds before the DOM it guards.** The listener turning `data-confirm` / `hx-confirm` into the shared modal sat at the bottom of `app.js`, loaded after the whole document, so all 22 armed controls across 17 templates AS MEASURED 2026-08-25; the tree now holds 33 over 20, which does not change what was ruled were clickable before their guard existed -- and finding **N-345** records an Undo submitted in that window, which ran with no dialog shown on a 537 KB page. It moves to `app/static/js/confirm.js`, loaded as a classic script in `<head>` and ABOVE the stylesheets (a script following pending `<link rel=stylesheet>` waits on all of them), so no element carrying a confirm attribute can be parsed before the listener exists. Two consequences stated: the htmx half binds on `document` rather than `document.body`, which does not exist yet and which htmx 2.0.4 reaches anyway (`bubbles: true`); and `ask()` falls back to `window.confirm` when Bootstrap has not loaded, which is fail-closed -- the act still cannot happen without an answer. Shipped at `X-gc`. |
| bank_import | R-GQ | -- | 2026-08-25 (developer) | **The undo's legacy disclosure gap is DEV DATA and is repaired by re-cloning, never by a schema flag.** `planned_removals` short-circuits on an act carrying no creation record, so such an act renders an Undo with no `data-confirm` at all -- measured on the rendered page, **221 Undo forms, 0 with a dialog**. The CODE is correct: `_accept._record` is the one writer of `StatementMatchCreation` and both creating callers pass what they made, so where every act post-dates that relation an empty list means *this act created nothing*, exactly. What is not correct is the dev database: **218 of 221 acts pre-date it, and 91 purchases worth `$4,013.69` were created inside them** (each entry born within 5 seconds of the act naming it, several with their envelopes minted at the same instant). A `creations_recorded` column would read TRUE on every row production ever writes -- production holds 0 acts -- so it would exist only to describe those 218. **Ruled: no column, no interim copy; clone dev from production and re-import, as its own coordinated session.** Until then those acts can orphan their purchase silently, which is the exposure this ruling accepts with its size measured. Owner of the residue is `operator`, in `ledger.md` under **N-345**. |
| bank_import | R-GR | -- | 2026-08-25 (developer) | **A merchant is a ROW, not a string two tables each keep a copy of.** `budget.merchants` is account-scoped and both referrers name it through composite `(merchant_id, account_id)` keys, so three things stop being maintained: the scope check that was the ONLY thing between a crafted body and an answer keyed on a merchant this account never saw becomes a SENTENCE (`fk_merchant_destinations_merchant_account` is the guard); *which merchants may be asked about* stops being a UNION of two derivations, because an ANSWERED merchant outlives its lines; and one width is stated once instead of twice. The wire posts the row, so the field is a `RowId` rather than unvalidated bank text. **Ruled as the deeper arm of a fork put with a narrower one**: the recommendation was to leave the merchant a string and keep the fence as a courtesy, and the developer took the table. **A merchant is NOT immortal** -- `delete_import` sweeps this account's merchants with no surviving line AND no stated answer, because without that the table has no ceiling at all (upload N unseen merchants, delete the import, keep them), which is the retired check's own hazard moved one table over; an adversarial security review measured it. Whether two SOURCES ever name ONE merchant is `X-f6b`'s and opens when a second source exists. Shipped at `X-gd-1` (`395b14f7`). |
| bank_import | R-GS | -- | 2026-08-25 (developer) | **The rule store's four answers are told apart by its COLUMNS plus one boolean, and *always ask* replaces withdrawal.** A stored discriminator was rejected on two grounds: it is a second statement of what the columns already say (which is `PolicyAnswer.of`'s own argument), and a CHECK cannot reference a `ref` row's id -- so a ref-keyed answer could sit on a row whose columns contradict it, on the sole-writer trust contract `ledger_accounts.kind_id` accepts and documents. The boolean shape is also the one that SURVIVES `X-f6c` collapsing the two container columns into a template id, where a ref table would need a data migration. **A stated answer is never UN-stated**: the control's *ask me every time* is a decision that stops the screen prompting for a rule, and there is no delete -- so a rule row, once made, is only ever restated. Shipped at `X-gd-2` (`d1910c95`), which also carried the RENAME: `budget.merchant_destinations` is `budget.merchant_rules` (`e7b597da`), so every `merchant_destinations` name an earlier ruling here cites reads as its `merchant_rules` counterpart. **Measured across the migration, row by row on a clone of the developer's own database**: all 29 stored answers read back identical, `Capital One Credit Card` included, which is the one whose meaning the backfill had to carry. |
| bank_import | R-GT | -- | 2026-08-25 (developer) | **A match act records WHETHER a standing rule performed it, as a flag on the ACT.** Which rule stays DERIVABLE -- the matched line carries the account and the merchant, which is exactly the rule's key -- so a foreign key to the rule row would store a pointer the line already determines, and would force a choice none of whose arms is right: cascade deletes money records, set-null claims the owner ticked it, restrict refuses to withdraw a rule that ever fired (the **N-302** dead end). What is NOT derivable, and is therefore the whole of what is stored, is that a rule acted rather than a tick: a person may tick a line whose merchant has a rule. Shipped at `X-gd-2` (`d1910c95`) as `budget.statement_matches.applied_by_rule`, NOT NULL with no default so the writer states it or the flush refuses. **Every one of the 221 acts on the developer's dev database is `false`, and nothing can write `true` until `X-ge`** -- the column ships one leaf ahead of that door so the step which MOVES MONEY carries no schema change. |
| bank_import | R-GU | -- | 2026-08-26 (developer, on measurement) | **WHAT a standing rule may file, and what it must refuse.** Three decisions. **(a) A rule FILES INTO AN ENVELOPE THAT HAS ALREADY CLOSED**, because the owner settles ahead of the bank -- a door refusing this would file nothing for the recurring envelopes the step exists for. Ruling **R-FX** already admits it on exactly the terms `destinations_for` enforces: the row's recorded figure IS its purchases, so a new one raises that cost by exactly the figure the bank showed and carries the bank's own posting day. **(b) A *new envelope* answer FILES TOO**, minting the container, rather than waiting for `X-f6c` to give that answer a template identity. A rule reaches NEW swipe lines only (**R-GI**), so a fortnightly import touches one period and `MintedEnvelopes` mints one per answer in it. **(c) A rule files only where the pass POSITIVELY finished looking.** Membership of the creatable set is defined by SUBTRACTION -- *no proposal claimed this line* -- which under a human tick is checked by the person reading the screen and under auto-apply is checked by nobody. So the bounds a pass publishes about its own search WITHHOLD the line, and the receipt says how many and why (`ReviewSet.search_gap_for`): a near miss the tier admitted and declined to choose between (**N-335**), a day the group search skipped as too crowded, and a row the amount model could not price at all. **A pass must report EVERY bound it applies, not every bound it published**, which is the correction two adversarial reviews forced: the first build read the published bounds and called that enumeration complete, and it was measured false. Each tier reports its own refusals now, in the module that owns the predicate (`X-ge-1`); nothing re-derives `NEAR_MISS_BOUND` or `DAY_WINDOW` outside its own module (**N-322**), and a tier added later must report or it is not a bound. **Also ruled, and it is R-FZ(d) from the other side**: a destination this statement PROPOSES as a whole-row match is not open to a rule, because auto-apply files BEFORE the proposal is ticked, so *the proposal wins* has to be reached by withholding rather than by ordering. Rejected for (a): refusing a closed envelope. Rejected for (c): filing whatever the review screen would offer a create control for, which makes *nothing matched* mean two things the app cannot tell apart. Shipped at `X-ge` |
| bank_import | R-GW | -- | 2026-08-27 (developer) | **`balance:R-GW` EXISTS TOO and both are kept** (rule 10): the cutover's order-reversal ruling was minted the same day on an unmerged branch, which a `docs/` grep cannot see. Every citation this ruling carries from `app/` and `tests/` names its arc for that reason. **A bank line the app has no row for becomes a row IN ITS OWN DIRECTION: an outflow a PURCHASE (**R-FX**), an inflow an uncategorized INCOME row.** Only the outflow half had ever existed, and the two refusals that bound it POINTED AT EACH OTHER -- `_accept._reject_empty_side` sent a line with no app row to the purchase door, `_create._load_line` sent an inflow back to matching -- so an inflow the books hold no row for sat inside that loop with no act at all, and the review badge could never reach zero. **The WRITER already exists and is not rebuilt**: `_variance.mint` mints exactly this row -- `INCOME` type, NULL category so `posting_service._settled_target` books it to the per-owner Uncategorized fallback, settled through the status seam on the bank's day at the `observed` basis, owning its amount beside a NULL `amount_source_id` -- so what ships is the DOOR, and the two callers share one writer rather than two spellings of one money rule. The period is the one holding the POSTING day. **Rejected: a per-line *set aside* act, and a merchant answer meaning *not tracked*** -- both dispose of the line without recording the money, so the books stay under the bank for good and **R-GL**'s residue surfaces those days as an exception forever, which makes one counter zero by making another non-zero. **Rejected: filtering them out of the count**, the figure-disagreeing-with-its-caption shape this arc has closed twice. **R-FP is unchanged**: the control opens on *leave this line alone*, and NO rule files an inflow, because a merchant answer says where SPENDING goes and a deposit is not spending. **Scope**: what these lines have no act for is the REVIEW SCREEN; an owner could always hand-build the row on the grid and match to it, and this door makes that one tick. **THREE things the step's adversarial reviews changed.** (i) The three lists the screen splits into are NOT total -- an outflow the bank dates MADE after it POSTED reaches none of them (**N-325**, ruled *reported rather than repaired*) and is still counted, named in `_leftovers`' header and pinned by a test rather than repaired here. (ii) A line the bank dates in the FUTURE resolved a pay period, rendered a tick and was refused by **R-EJ** only AFTER the row was written and settled; both halves now ask ONE published predicate, `status_seam.day_is_in_the_future`, and the door asks it before it writes. (iii) The card's per-line safeguard is *what this pay period's books already hold* -- a FACT about the period rather than a candidate for the line -- suppressed where the deposit is smaller than the smallest such row, because no subset of positive rows can come to less than its smallest |
| bank_import | R-GX | -- | 2026-08-27 (developer) | **THE REGISTER IS NOT THE QUEUE.** The review screen shows only what is still being DECIDED; a decision already made moves to a surface of its own. Three parts, each measured on the developer's own dev data 2026-08-27 after `X-gf-1` landed: (a) the merchant control SPLITS ON WHETHER AN ANSWER EXISTS -- the queue keeps a row only for a merchant with an unexplained line and NO answer (1 today, `Public Library`, `-$5.99`), and all 29 answered merchants move, including `Capital One`, whose *never a purchase* answer parks 9 lines worth `-$7,412.94`. **R-GJ still holds where it bites**: a merchant a SOURCE files as an account payment has no answer by definition, so its row is on the QUEUE, which is the only surface that can give it one. (b) The accepted acts move WHOLE, 221 of them and 216,637 bytes. (c) The register renders every act that NO LONGER HOLDS whatever its age, plus the newest `REGISTER_LIMIT` (50) of the rest, with the remainder behind a link -- the agreement test is a VALUATION over member rows and not a query, so the fold reads every act either way and what is bounded is what is RENDERED. **Measured before**: the review body was 578,523 bytes of which those two panels were 442,109, or 76%, and the work a routine import leaves was 136,414. **After**: 149,103, and `review_set` fell from 146 SQL statements to 7 -- a cost the IMPORT path paid too, `file_new_swipes` calling the same reader. **The earlier 554,122 / 79% figure in this document's `X-gf` entry is not contradicted**: it was measured before `X-gf-1` added the deposit card. |
| bank_import | R-GY | -- | 2026-08-27 (developer) | **A PRESS THAT DESTROYS A RECORD CONFIRMS, WHETHER OR NOT IT ALSO DESTROYS A ROW.** This REVERSES the rationale `_statement_undo_macros.html` carried, which attached `data-confirm` only where an undo removed rows the act had CREATED and argued that *a confirmation on a reversible act trains the owner to click through the one that is not*. Measured 2026-08-27: **0 of the developer's 221 accepted matches are in that state**, so all 221 Undo buttons fired on one click with no dialog at all. Undoing moves no money and leaves settle days alone, and what it destroys is still a record -- which bank lines ARE which of the owner's rows, and whether a standing rule filed the act rather than a person (**R-GT**) -- recoverable only by matching them again by hand. The dialog is therefore always there and its WORDING is what varies, naming the destruction that press actually performs. **The one press with no dialog is the one that performs nothing**: an act whose undo would be REFUSED destroys nothing, and a confirmation before a refusal is the dialog-for-nothing the overturned argument was about. |
| bank_import | R-HB | -- | 2026-08-27 (developer) | **THE EXCEPTION QUEUE IS ONE LIST, GROUPED BY THE DECISION EACH LINE POSES.** The review screen partitions its unexplained bank lines by MECHANISM -- an outflow that may be recorded (`creatable`), an outflow ruling **R-GJ** bars (`parked`), an inflow that may be recorded (`recordable_inflows`, ruling **bank_import:R-GW**) -- across three cards with three headings and three bodies of explanation. That is the SERVICE's partition, and it is the right one: the three reach different doors. It is not the READER's. The question all three pose is the same -- *is this money my books already hold, or is it new?* -- and the owner answers it the same way each time. So the queue becomes one list ordered by what the decision IS, each row carrying whatever acts are open to it, and the mechanism partition stops being visible on the screen while staying load-bearing underneath it. **Measured on the developer's own data 2026-08-27**, after `X-gf-1`, `X-gf-2` and `X-gf-3a`: the three cards hold 2, 9 and 16 lines against 1 unanswered merchant, and `2 + 9 + 16 = 27` is exactly `unmatched` -- the same 27 lines the hand-build form lists again as one flat set, which is what the reader is already being asked to reconcile by eye. |
| bank_import | R-HC | -- | 2026-08-27 (developer) | **THE WORKBENCH IS NOT THE QUEUE EITHER.** The same argument ruling **R-GX** made about the register, applied to the other thing that is not a decision: the hand-build match form is the TOOL that three separate exceptions send the owner to -- a parked card payment meeting its payback rows, a line a tier declined, a deposit the books may already hold -- and it is not itself an exception. It moves to a surface of its own, and each exception links to it with its own line already ticked. **Measured through the real route on the developer's own data 2026-08-27**: the review body renders 146,907 bytes of which that form's two pick lists are **89,247, or 61%** -- 22,830 for 27 bank lines and 66,417 for 67 rows -- and BOTH are unbounded, the right one growing with the whole span the statements cover, for ever (**N-374**). **A cap is refused as the remedy**: the row that explains a line may be number 51, and a scored shortlist is what ruling **R-GD**'s third amendment already withdrew on this screen after measuring 0 of 18 inspected correct. What is bounded is the QUEUE, by the tool leaving it. |
| bank_import | R-HD | -- | 2026-08-28 (developer) | **A SWEEP MAY REACH ONLY THE GROUP THAT OFFERS IT.** Ruling **R-FZ(c)** partitioned the one-click record by where the money LANDS -- into a budget line still open, into one that has closed, into an envelope it would create -- on the ground that the riskiest class may not ride the same click as the safest. That partition says nothing about whether the money is ALREADY RECORDED SOMEWHERE ELSE, so a line carrying a warning that the app found a row it might be was still counted in the sweep and still ticked by that one click: the warning printed beside the control and the control ignored it, which is the *warning paragraph above a working select* shape ruling **R-GJ** cost `$7,412.94` to learn, at the grain of one LINE rather than one merchant. **So the sweep belongs to the evidence group** ruling **R-HB** puts the line in, and only the group whose own heading says the app searched and found nothing offers one. A line in either other group keeps its own destination select and is hand-picked, so no act is taken away from an owner who looks -- what is removed is the BULK act over lines the same screen has just questioned. **Its reach is structural rather than nominal**: the sweep's root is the DOM subtree it lives in, so it cannot select a row outside its own card even if a later edit gives one a placement class. Measured 2026-08-28 on the developer's own data through the real producer: 0 of his 2 creatable lines carry both a sweep class and a warning, so this was LATENT and not live -- and grouping the queue by evidence is what would have made it a contradiction on the page. The developer's answer to the fork, asked with the alternatives of keeping the sweep global and of deleting it outright: build what you would build from scratch |
| bank_import | R-HP | -- | 2026-08-29 (developer) | **THE RECONCILE SCREEN'S VOCABULARY IS FOUR VERBS.** Every bank line ends with exactly one of MATCH (it is a row the books already hold), ADD (new spending or income the budget did not have), TRANSFER (money between the owner's own accounts, never spending) or SKIP (deliberately explained by nothing), and the inbox is the lines with none yet. The evidence grouping **R-HB** made visible (already held / unfinished / nothing found) returns to the service; what the screen groups by is whether a standing RULE, a TIER, or NOTHING suggested the verb. Amends R-HB's visible grouping only: the mechanism partition stays load-bearing underneath. Argument and the locked direction: `docs/design/bank_import_audit.md` |
| bank_import | R-HQ | -- | 2026-08-29 (developer) | **A LINE WITH NO AVAILABLE ACT IS A HOLDING STATE, never an inbox row.** It is a count in the hero's holding chips and a row on the tab that owns it: a card payment **R-GJ** parks sits on Transfers reading *waiting for the card account*, a line before the pay calendar opens is a chip with its count and the day. R-GJ's bar and its sentence are unchanged; only where they render moves. Measured 2026-08-29: 16 of the 27 lines on the review screen had no correct act anywhere in the app, and a queue that cannot empty is not a queue |
| bank_import | R-HR | -- | 2026-08-29 (developer) | **THE CARD SHOWS THE DECISION; THE DISCLOSURE IS ONE CLICK AWAY.** A card carries the bank's facts, the amount, ONE sentence whose first word is the verb, and OK. Why a rule withheld, what a tier declined, what accepting writes and every bound the pass applied live in the opened card's panel and in the receipt, which keeps **R-FZ**'s every-refusal-in-the-service's-own-words. Amber appears on a card only where money is at risk. This retires the practice of printing each bound and refusal beside the line -- the grain that put two sentences on the review page sixteen times -- and adds one line of prose to the page: a dismissable four-verb legend |
| bank_import | R-HS | -- | 2026-08-29 (developer) | **A JUSTIFIED SUGGESTION IS PRE-FILLED** (amends **R-FZ(b)**). The sentence and the opened panel open on the destination a standing rule names, or the rows an exact or group tier found, and an untouched card is not submitted: OK per card and Apply are the consent, undo is the safety. R-FZ(b) banned the ARBITRARY default -- the first envelope in the period, the first category alphabetically, 78 of 91 lines onto a closed envelope -- and stands for exactly that; it never meant a suggestion the app can justify, which every peer product pre-fills |
| bank_import | R-HT | -- | 2026-08-29 (developer) | **A STANDING RULE MAY COVER AN INFLOW, AND MAY NAME A ROW SET** (extends **R-GI**). **(a)** An INFLOW rule answers a deposit signature with an income category, or a merchant credit with a NEGATIVE purchase in that merchant's envelope (a refund is the merchant rule's inverse); it CREATES, so it auto-applies at import under **R-GH**. **(b)** A GROUP rule answers a signature with the row set it pays (`TOWN OF CLAYTON PAYROLL` = this period's payroll rows) and where the residue lands -- onto a named row, re-pricing it (**R-GD**), or its own uncategorized row (**R-FN**); it MODIFIES the owner's rows, so it PRE-BUILDS the match as a solid suggestion and applies only on the owner's OK, which is R-GH's protective half kept. On 2026-08-29's queue, nine of 27 lines dissolve under (a) and the seven payroll deposits become one OK each under (b), until `balance:X-aw` closes **N-239** and the residue is zero |
| bank_import | R-HU | -- | 2026-08-29 (developer) | **THE REGISTER AND THE WORKBENCH RETIRE AS PAGES.** **R-GX** and **R-HC** stand -- neither is the queue -- and the answer becomes a TAB and an EXPANSION rather than a page: the Explained tab with Undo on the card, and the opened card's MATCH panel (candidate rows, a search over the period's rows, the running difference and where it lands), on ONE Reconcile page that replaces review, register and workbench. Rules stay a page of their own. The old routes stay alive beside the new page until `X-gi`'s census deletes them |
| bank_import | R-HV | -- | 2026-08-29 (developer) | **SEQUENCING, AND THE PROCESS RULING.** `balance:X-aw` (**N-239**, the 4-6 cent payroll residue) is ranked beside the rebuild, because the group rule is its interim and the calculator is the root cause; `X-gg` stays behind `credit_card:CC3c` (envelope filling is two-source); the rebuild is `bank_import:X-gj`, decomposed in `steps.md`, with `X-gi` ranked after its last leaf. And the owner's process moves: he stops hand-entering DEBIT swipes for merchants that carry a rule and keeps hand-entering card purchases until the card arc, so **R-GU(a)**'s filing into a closed envelope raises it by exactly what the bank showed and never double counts |
| bank_import | R-HW | -- | 2026-08-29 (developer) | **THE OPENED CARD RENDERS ALL FOUR VERB TABS, AND A VERB WITH NO DOOR IS DISABLED AND SAYS WHAT IT WAITS FOR.** `MATCH * ADD * TRANSFER * SKIP` appear on every line whatever this build can act on, because the vocabulary **R-HP** names is taught by the panel itself and a verb absent from three cards in four teaches nothing. A verb whose door does not exist renders its explanation, one muted sentence naming what it waits for, and NO submitting control: `TRANSFER` until a bank line can BECOME a transfer, which is the card arc's ruled model (**N-337**, owner `credit_card:CC3b`) and not the card ACCOUNT TYPE, which `app/enums.py` and `app/ref_seeds.py` already hold; `SKIP` until `X-gj-4` records a disposition. **Which verbs are OPEN is the service's fact stated per line, never a Jinja condition** -- a control whose submission can never succeed is the shape this package has closed repeatedly, and a disabled tab carrying its reason is a disclosure rather than one. **It BOUNDS `balance:R-ET`'s corollary** (*an affordance that cannot succeed is DELETED, not given a nicer refusal*), which stands for a CONTROL and not for a panel whose whole content is the explanation. |
| bank_import | R-HX | -- | 2026-08-29 (developer) | **AN UNMATCHED INFLOW IS NOT PRE-FILLED UNTIL A RULE CAN JUSTIFY THE DESTINATION** (bounds **R-HS**). Recording a deposit as uncategorized income is the only inflow door that exists, and being the ONLY act is not what R-HS means by justified: a merchant credit is a refund, and filing one as income is the wrong act this arc's audit measured. So every unmatched inflow's card reads *Choose what this is* and opens the panel, until `X-gj-2` ships **R-HT(a)**'s inflow rule and the destination becomes one the app can defend. Measured 2026-08-29 on the developer's own account 1: 16 of the 18 lines the inbox would hold are deposits -- 7 payroll, 5 dividends, 3 merchant refunds, 1 member deposit -- and 8 of the 16 already carry the books-already-hold-income warning. |
| bank_import | R-HY | -- | 2026-08-30 (developer) | **THE OPENED CARD'S MATCH TAB OPENS ON THE LINE'S OWN PAY PERIOD, AND THE SEARCH REACHES EVERY UNEXPLAINED ROW ON THE ACCOUNT.** Both halves are load-bearing and both were measured on the developer's own account before the fork was put: the PERIOD is what a card renders with the page, so the payroll group a card is usually about needs no round trip and no JavaScript -- 6 to 15 rows per card, 311 row renders across all 27 cards, and the `2026-03-26` deposit of `$2,573.42` finds `Health Insurance Allowance` `$100.00` and `Data Manager` `$2,473.38` there, a difference of `$0.04` (**balance:N-391**). The SEARCH is over every row the pass holds unexplained, because a bound that cannot be widened is the cap finding **N-374** refused -- *the row that explains a line may be number 51* -- and all 9 of the 9 card payments have payback rows their own period does NOT hold (`2026-06-01`'s `-$1,000.44` against period 5's `$943.41` of paybacks; `2026-04-10`'s `-$627.39` against a period holding none). **Rejected: rendering every candidate in every card**, measured at 67 rows in 18 cards, ~1.2 MB at the workbench's own 991 bytes a row, which is N-374 rebuilt one surface later; and **rejected: the period alone**, which would leave no card payment groupable once `X-gi` deletes the workbench. The list a card renders is a starting view and never a bound |
| bank_import | R-HZ | -- | 2026-08-30 (developer) | **THE RECONCILE PAGE POSTS ITS OWN FORM, KEYED BY BANK LINE, AND THE OK CHECKBOX IS THE CONSENT.** `reconcile_payload` is a SECOND READER for the schema and the door `batch_payload` already feeds, which is what `hand_match_payload` already is -- one grader, one applier, one receipt. What differs is the FORM: one card submits `ok=<line>` (a checkbox, so presence IS the consent), `verb-<line>` (the panel's four verb tabs as a RADIO GROUP, so the tab a card is left on is the verb it is OK'd with and the panel needs no script), and that verb's own parameters. **It is what makes R-HS structural rather than a default**: R-HS pre-fills the destination a standing rule names AND says an untouched card is not submitted, and those two cannot both hold on the review queue's form, where the destination select IS the tick -- a pre-filled select on a card nobody touched would write a purchase. **No rendered POSITION anywhere**, which is R-HC's own lesson taken one surface further: a position is a property of the DOCUMENT, and two acts sharing a namespace are one `hx-include` away from being unioned into an act nobody assembled. **A card OK'd on nothing is REPORTED, never dropped and never a pass-level refusal** -- it is reachable from a browser, and R-FZ(a) says one bad item may not cost the other 123. Rejected: keeping the index-keyed form and holding the pre-fill safe with a `disabled` attribute JavaScript keeps in step, which is a money control whose safety rests on a script |
| bank_import | R-IA | -- | 2026-08-30 (developer) | **EVERY MATCH STATES THE DIFFERENCE IT WAS REVIEWED AGAINST, AND THE ACCEPT DOOR EXEMPTS NO SHAPE.** `_variance._reject_unaccepted_difference` exempted *one bank line against one row* until this ruling, on the premise that the near tier offers exactly that shape and its card states the correction -- so a SHAPE was read as a proxy for *the screen already disclosed this figure*. **A proxy for a fact the wire could carry is a guess**, and the Reconcile page's tickable proposal rows manufacture the shape: untick one row of a proposed group and what reaches the door is one line against one row, byte-identical to the tier's own offer. Measured on a restored production clone over 137 proposals: the exemption was load-bearing for **2** of them, writing `-$0.15` and `-$0.04`, and **3** carried more than one row whose untick wrote up to **`$2,572.36`** with nothing asked. An allowlist that existed for four cents permitted twenty-five hundred dollars. The rule is now *a difference this door would WRITE must have been reviewed, and the figure reviewed must be the figure derived* -- what **R-FP** and finding **N-336** already require of each ROW, raised to the SUM, which is the one number no per-row guard can see. All three submitting surfaces carry it. **A ZERO derived difference carries nothing and that is not the exemption returning**: the old test asked what the SUBMISSION looked like, which its sender chooses, and this asks what the DOOR derived inside the transaction that will write -- at zero both writers are provably idle, so requiring a figure would refuse every exact match built with scripting off. **Rejected: three narrower options** -- hidden read-only proposal rows, a *change these* escape hatch, and teaching the door which submissions ARE the proposal -- all refused by the developer as band-aids. Per-proposal figures and the reproduction are in the shipping commit (conventions rule 4) |
| bank_import | R-IB | -- | 2026-08-30 (developer) | **A STANDING MERCHANT RULE IS OFFERED ONCE PER MERCHANT, ON THE RECEIPT, ABOUT WHAT THE DOOR ACTUALLY APPLIED.** The Reconcile card's *always, for this merchant* checkbox is DELETED. **The cause is that a rule is ONE fact per merchant and the card rendered it once per LINE**: measured on a restored production clone, the developer's pass offers 86 creatable lines over 21 merchants, so the page asked one question 86 times -- Amazon **26**, Walmart **13**, Food Lion **12**, ten merchants over-asked. Two defects follow from that single cause and both become unconstructible. **(a)** The rule was derived from what was OK'd, BEFORE `apply_reviewed` ran, so a per-item refusal rolled back in its savepoint while the rule was still written -- auto-filing that merchant on the next import with no press (reproduced; two docstrings asserted the opposite and were false). Deriving the offer from `BatchOutcome.applied` gives a refused creation no form to take. **(b)** N controls for one fact can state N different answers; one offer per merchant leaves a contradiction no form to take either. **The measured contradiction rate today is ZERO** -- a rule keys on `template_id`, so all 13 Walmart lines and all 26 Amazon lines collapse to one answer each and the app's own suggestions never disagree -- so this removes the SHAPE that permits one rather than fixing a live bug. It reuses the SHIPPED rule door (`_statement_rules.record_submitted_rules`, `MerchantRuleBatchSchema`, and the `rule_merchant-<i>` / `rule-<i>` field names the review queue and register already post), so three surfaces share one grader and one writer. It revises the SURFACE of **R-GI** and not its substance: the answer is still read back off the destination and never typed. **Rejected: four narrower options** -- store neither, store the last, store the first, refuse the whole rules half -- all refused by the developer as laziness |
| bank_import | R-IC | -- | 2026-08-30 (developer) | **THE DURABLE HOME FOR A MERCHANT'S STANDING ANSWER IS A MERCHANTS LIST, AND IT SHIPS BEFORE THE REGISTER IS RETIRED.** `docs/design/bank_import_audit.md`'s own fix column named it -- *a Rules settings list, one row per rule, edit on click; a rule is minted INLINE from the row that prompted it* -- and only the second half was ever built (**R-IB**, the receipt's offer). Measured on a restored production clone: **62 merchants on the account, 29 answered and 33 not**, spread over three partial surfaces -- the review queue's section (unanswered AND still carrying an unexplained line), the register (answered only) and the receipt's offer (just-filed only) -- with no surface showing all 62 and merchants falling through every one of them. **The sequencing is part of the rule rather than a preference**: `X-gj-1c` retires the register as a page, so shipping it first would delete the answered merchants' only home while its replacement did not exist. **Rejected: widening the review queue's merchant section** (it patches a surface that is being retired), **persisting the receipt's offer across a press** (it puts a page's display state on the wire to stand in for a missing page), and **one form per merchant on the receipt** (round trips, and it does not help a merchant the owner never reached). What the list makes true is that *Not now* costs nothing, because the question is always one click away -- so **R-IB**'s offer is a shortcut rather than the only chance |
| recurrence | R-R39 | -- | 2026-08-05 (developer) | **Scope.** Full two-axis redesign (not additive patterns, not staged dual-write) |
| recurrence | R-R40 | -- | 2026-08-05 (developer) | **Add-ons.** ALL FOUR: weekly-by-date, nth-weekday, count-bounded end, business-day shift |
| recurrence | R-R41 | -- | 2026-08-05 (developer) | **`loan_params.payment_day`.** DELETE; every reader goes through one accessor |
| recurrence | R-R42 | -- | 2026-08-05 (developer) | **The three defects.** Folded into the redesign, not fixed as separate PRs |
| recurrence | R-R43 | -- | 2026-08-05 (developer) | **Sequencing vs the balance arc.** The arc SPLITS: the engine core is disjoint from the balance arc's live work, the date work is not. "Half B folded into X-an" is SUPERSEDED -- R6 reads a column R5 creates, and R5 cannot precede the balance step deleting from `cash_ledger/_events.py`. A developer ruling is owed; see section 0. The resulting ORDER is `steps.md`'s |
| recurrence | R-R44 | -- | 2026-08-05 (developer) | **`PAY_PERIODS_PER_YEAR`.** SHIPPED at R7a-2a (`003e3657`) as `pay_calendar.PayCadence`; derivation = `round(365.2425 / cadence_days)`, whose own section 4a left the arc document with the R7a-2 span and is in `historical/recurrence_findings_as_built_2026-08-15.md`. It surfaced a SECOND stored paycheck count -- finding **F-16**, ruled into this arc as plan step R-F16 |
| recurrence | R-R12 | -- | 2026-08-08 | **A generated row's dates: THREE facts, three homes: `occurs_on` (the occurrence), `pay_period_id` (the funding), `due_on` (the installment). `compute_due_date` is DELETED.** |
| recurrence | R-R13 | -- | 2026-08-08 | **`anchor_date` itself: SPLITS before R7c freezes it: `starts_on` (one meaning, every unit) + `nominal_day`.** **R-R3**'s month-end-clamp subtype is SUPERSEDED by this split -- the anchor day and the clamp were one question until it, and the table carried that supersession as a row of its own ("Anchor day vs month-end clamp") until `balance:X-ao-2a` folded it into the ruling it is about |
| recurrence | R-R7 | -- | 2026-08-05 | **Orphaned rules: NOT deleted in R2b; they ship with the fix for the leak that makes them**, so the cleanup and its cause are reviewed together and R2b stays purely additive. Shipped as plan step R-F6. Two claims the earlier drafts rested on were measured FALSE first: the 5 orphaned rules were said to carry "neither a start period nor a template" when ids 4, 41, 43 and 47 all carry a `start_period_id` (1, 1, 3, 2); and taking `transaction_templates.recurrence_rule_id` off `ON DELETE SET NULL` was said to close the leak, when that FK fires on a RULE deletion and the leak runs the other way, out of `templates.hard_delete_template`. The argument is in `historical/recurrence_evidence_2026-08-11.md`, which is where this ruling's own text sat until `balance:X-ao-2a` |
| recurrence | R-R14 | -- | 2026-08-08 | **A failed migration-bearing deploy: Back up unconditionally, PRE-FLIGHT whether rollback can work, and REFUSE the rollback that cannot. The app keeps failing loud rather than booting against a schema it cannot describe.** |
| recurrence | R-R45 | -- | 2026-08-13 | **What "this commitment has ended" means: The rule OWES no occurrence on or after the day asked about -- one reading for BOTH closing bounds.** A date bound used to wait for its bound date, so the same schedule written as a count stopped counting sooner. Shipped as plan step R-D33, $0.00 on live data |
| recurrence | R-R46 | -- | 2026-08-27 | **`R5` DECOMPOSES and `R17` is its first leaf: a generated row RECORDS which occurrence it answers (`occurs_on`), and the interim due-date match `R17` was specified as is not built.** `pay_calendar:C5b` is the step that makes `should_skip_period` occurrence-aware and it waits on `R5` for this column, so the interim would have been written to be deleted. Two consequences the specification did not have: `occurs_on` is NULLABLE, because `carry_forward_service` and the one-time transfer branch both create template-linked rows no cadence named, and the BACKFILL is `scripts/stamp_occurrences.py` run by `entrypoint.sh` rather than the migration -- a row's `due_date` is not its occurrence for 30 of 780 rows, so the walk is required, and no migration here imports app code because `build_test_template.py` replays the chain from zero |
| recurrence | R-R47 | -- | 2026-08-28 | **The unique index RE-KEYS onto the occurrence in `R17`, not in `R5`.** A generated row's identity is the occurrence it answers; the pay period is where that occurrence's money lands, and the owner may move it. Keyed on the paycheck the index was a second statement of the wrong rule beside a predicate being corrected, so both moved together: `(template, scenario, occurs_on)` where dated, `(template, scenario, pay_period_id)` where NULL. The alternative was a guard clause in the predicate against a second non-override row in one paycheck -- a fence, and one that puts back the period-thinking `D57` exists to remove |
| recurrence | R-R48 | -- | 2026-08-28 | **The `D19` refusal RETIRES with the re-key, and carry-forward learns the difference in the same step.** `refuse_unstorable_repeats` existed only because the paycheck-keyed index could not store a cadence naming one paycheck twice; once it can, the refusal guards nothing. But a SECOND fence stood over the same fact -- `carry_forward_service` refused any target paycheck holding more than one open row, citing that index -- so deleting one and leaving the other would have installed a latent defect with two detonators: a pay cadence of 30+ days (2 of the developer's templates at 30, 11 at 31) and `R5` making the WEEK unit authorable |
| recurrence | R-R49 | -- | 2026-08-28 | **A row whose occurrence a rule EDIT dropped is RETIRED, never re-pointed** -- deleted where it carries nothing, held back as a conflict where it carries the owner's records. Re-pointing it at whatever occurrence is left in its paycheck is a deduction only if every row answers some occurrence, which a NULL `occurs_on` denies; it is the inference an adversarial review cut from `stamp_occurrences.py`, where it paired a `$12.34` envelope with a car payment nine paychecks away. A wrongly adopted row SUPPRESSES the real bill, which is a payment vanishing with nothing on screen |
| recurrence | R-R10 | -- | 2026-08-07 | **Where the two-axis columns live: Computed until R7c, stored from R7c.** It binds R7c's backfill. ARCHIVED: the argument is `historical/recurrence_as_built_2026-08-05.md`, "a derivation is not stored beside its own inputs" |
| recurrence | R-R15 | -- | 2026-08-08 | **What a Recurrence cell says: The UNIFORM shape: every calendar cadence names its cycle the same way, month and day, which is what a yearly rule already did and a quarterly one did not. The month named is the FIRST OCCURRENCE's, not the authored `month_of_year` -- the same residue class either way, and the only one R7c can still express.** |
| recurrence | R-R16 | -- | 2026-08-14 | **What `starts_on` MEANS: The rule's FIRST OCCURRENCE, ONE meaning for every unit: the first date a calendar cadence fires on, and the payday of the first paycheck a pay-period cadence bills in. Its position in the cycle IS the phase, so `day_of_month` and `month_of_year` are DROPPED rather than renamed. Supersedes R-R13's "opening validity bound" reading and closes row D28** |
| recurrence | R-R17 | -- | 2026-08-14 | **Whether the YEAR unit survives: YES, and `(12k, MONTH)` is CANONICALISED to `(k, YEAR)` at the write door once the interval is free, so one cadence has one spelling. Dropping the unit would move the times-twelve encoding into the form, which is the second vocabulary R7b-1 removed.** |
| recurrence | R-R18 | -- | 2026-08-14 | **How R7c ships: THREE leaves, expand / migrate / contract: `R7c-a` adds the columns and dual-writes them while nothing reads them, `R7c-b` moves every reader and the form onto them, `R7c-c` drops the closed set. The destructive DDL is last and no translation shim is written in any leaf.** |
| recurrence | R-R20 | -- | 2026-08-16 | **Whether `day_of_month` may be dropped before its reader: YES, because the column is a derived ENCODING and its reader reads the derivation instead. `recurrence_engine.compute_due_date` dates every generated row from it and plan step R5 deletes that function four ranks later; ledger row D37 named two ways out -- hold the column, or swap the ranks -- and this is a third. `recurrence.scheduling_day_of_month` answers the same value from `(unit_id, placement_id, starts_on, nominal_day)`, and the migration grades the equality in SQL before dropping: 0 of 46 live rules disagree.** |
| recurrence | R-R21 | -- | 2026-08-16 | **What the form does when a cadence admits ONE funding rule: RENDER the "Funded from" row anyway, with help text saying there is nothing to choose. Hiding it is how a bill's funding rule came to change with nothing on screen saying so (ledger row D32), and freeing the interval removes the measured instance outright -- the MONTH unit offers both placements at every interval. The two that remain are PERIOD, where the placement is provably inert, and YEAR, whose first-paycheck anchor is R8's.** Its second example expired the next day**: R-R24 admitted the YEAR unit's deferring reading, so PERIOD is the only cadence left admitting one placement. The RULING is unchanged -- the row renders either way -- and only the count of cadences it applies to moved** |
| recurrence | R-R22 | -- | 2026-08-16 | **Whether the AUTHORED month needs surfacing before it is dropped: NO: ruling R-R15 already decided the display and the two affected rules fire on the same cycle either way -- `3 = 6 (mod 3)` and `3 = 9 (mod 6)` -- so no generated row and no projected balance moves. The typed month was a second spelling of the residue class the first occurrence carries, not a second fact. Closes ledger row D38 with nothing built.** |
| recurrence | R-R23 | -- | 2026-08-16 | **What GATES the offer set, and how R8 ships: The gate is what the app can HONOUR, derived from two live facts, not from an anchor derivation that was deleted. `anchor_family` and its three `FAMILY_*` constants go: ruling R-R16 made the first occurrence AUTHORED and R7c-b deleted all three derivations the router selected between, leaving it refusing two cadences by citing them. Measured over all eight `(unit, placement)` pairs, its one live projection equals `has_day_of_month_coordinate(unit) and placement is CONTAINING_DATE`. And R8 SPLITS on that measurement: only the gate ships now, as `R8-a`, because the other three add-ons each need a generated row's own `occurs_on` and therefore wait on R5. Shipped as R8-a** |
| recurrence | R-R24 | -- | 2026-08-16 | **Whether a year-scale cadence may defer onto a later paycheck: YES. `(k, YEAR, PERIOD_STARTING_ON_OR_AFTER)` fires on its own authored date every `k` years and defers onto the next paycheck, exactly as its MONTH twin already does; the refusal cited `_first_of_month_anchor`, deleted at R7c-b. 0 of 46 live rules read differently, and a yearly cadence cannot repeat a paycheck at any `cadence_days` in 1-365. It also restores `(12, MONTH, deferred)` -> `(1, YEAR, deferred)`, so one rhythm keeps one spelling. Shipped as R8-a** |
| recurrence | R-R25 | -- | 2026-08-16 | **Where the nth-weekday coordinate LIVES: On `budget.recurrence_rules`, as an EXCLUSIVE ARC of columns under one CHECK, and `budget.recurrence_weekday_anchors` is DROPPED unwritten. "A rule fires on a day-of-month OR an nth-weekday" cannot be a CHECK while the two live in different tables -- PostgreSQL CHECKs are single-table -- so the satellite form would need a trigger or a door-only fence for an invariant the column form makes structural. The same move ruling R-R16 made for `nominal_day` and R7c-c made for the unwritten `recurrence_month_anchors`. Owned by R8-c** |
| recurrence | R-R26 | -- | 2026-08-16 | **What "non-business day" MEANS: Weekends plus the eleven US federal holidays, DERIVED as rules rather than seeded as rows -- they are nth-weekday-of-month rules and fixed dates with weekend observation, so there is no per-year seed to keep current and the derivation composes with R8-c's own machinery. Owned by R8-d. Finding F-4, the PAY SCHEDULE's own holiday shift, is a different question and is not settled by this** |
| recurrence | R-R27 | -- | 2026-08-17 | **Whether the closed set's TABLE and its ENUM may go in ONE release: YES, and R-R11's hazard does not generalise to a dropped TABLE at all -- which an adversarial review of R9 established after the step had argued the opposite. R-R11 held the `Once` ROW because `ref_cache.init` raises for an enum member with no row in a table that EXISTS; `_load_rows` CATCHES the `ProgrammingError` a missing table raises, and `init` records it unavailable and completes, pinned by `test_ref_cache.py::test_init_records_unavailable_table_and_keeps_others_usable`. Three independent reasons the auto-rollback image is safe, in the order they fire: it never reaches `ref_cache`, because `entrypoint.sh` step 3 runs `init_database.py` with `init_ref_cache=False` and its Alembic tree cannot resolve the new revision, so `set -eEuo pipefail` aborts (finding F-8); `shekel-deploy:repin_is_safe` refuses the re-pin for the same reason, leaving the pre-deploy dump as recovery; and booted anyway it would degrade rather than die. Precedent measured the same day: R7c-a, R7c-b and R7c-c all reached production in ONE release (PR #102, `41e09dad`), and R7c-c dropped a column the previous image's ORM mapped. Shipped as R9** |
| recurrence | R-R31 | -- | 2026-08-19 | **What a forward window named in MONTHS resolves to: The LAST WHOLE PAYCHECK that ARRIVES within the span, counted against the CADENCE itself: `floor(months x DAYS_PER_YEAR / (MONTHS_PER_YEAR x cadence_days))`. So the LABEL is the fixed thing and the pay-period count derives from the owner's stated rhythm. Flooring rather than rounding was measured: over all 365 legal cadences crossed with the 3 / 6 / 12 / 24-month spans the two disagree on 388 of 1,460 cases, and by mean absolute distance between the resolved period's END and the day the label names the floored answer is closer in 387 and equal in the last. Deriving it through the ROUNDED `periods_per_year` instead was the first implementation and an adversarial review measured it wrong -- double rounding disagrees with the arrival test on 384 cases, always overshooting, and in 74 of them OFFERS a horizon the owner has no paycheck inside at all. A span no paycheck reaches resolves to ZERO and is NOT OFFERED; a surface that must render something anyway (the Plan tab, the pulse chart) shows the paycheck the owner is in, which is a POLICY and not an invariant. Shipped as R-F17** |
| recurrence | R-R19 | -- | 2026-08-15 | **What a REGENERATION does to the rows it already generated: It MAINTAINS them; it does not destroy and rebuild them. A row the rule still names is UPDATED in place, a period the rule names with no row gets one, and a row the rule no longer names is removed only when it carries nothing of the owner's. A row holding the owner's own records -- purchases, a note, a hand-entered actual -- is RETAINED and reported, in two shapes: the rule stopped naming its period, or the template's ACCOUNT moved, which drags every purchase onto the new account and invalidates the statement link that cleared it. The delete-and-recreate this replaces was safe only while a generated row was a pure projection of `(template, period)`; `transaction_entries` CASCADE from their parent, so it destroyed `$499.82` of recorded purchases on live data. Shipped as R10-a (`5fc13cdb`), closing row N-292** |
| recurrence | R-R29 | -- | 2026-08-19 | **Where a loan payment's DERIVED validity window lives: SPLIT BY KIND: only the CLOSING bound stops being stored. `starts_on` is the cadence ANCHOR since R-R16 -- `_resolution.cadence_day_of_month` is its one reader and the cycle phase derives from it -- so a MONTH-unit loan payment without it has no day to fire on, and it stays written. `end_date` goes NULL for a loan payment and `ck_recurrence_rules_valid_window` lands on that. D35 half closes: a derived `starts_on` still shares a column with authored ones. Owned by R7d** |
| recurrence | R-R30 | -- | 2026-08-19 | **What generation does for an owner with no baseline scenario: REFUSES -- `BaselineMissingError` to the single application-level handler (R-BW), as every other producer needing a scenario does. The early return it replaces left the last-written bound standing, and with nothing stored there is nothing to stand. The create path and `period_population` already behave this way, so what it decides is the FORM READ path. Owned by R7d** |
| recurrence | R-R33 | -- | 2026-08-25 (developer) | **What an installment NO ROW covers is priced at: What the loan's own recurring payment SAYS it costs, resolved AS OF that installment, and the contract only where the definition states no price.** A payment that states none -- DERIVE mode, where the stored figure is a snapshot of the contract -- costs the contractual P&I plus that installment's escrow plus the standing extra, which is what the mode MEANS. One that states a price costs what its VERSION SERIES says on the installment's own due date, plus the extra. **Never `default_amount`**: an adversarial review measured that scalar reaching backwards, because `_resync_scalar` puts it on the NEWEST price stated rather than the price on a date, so a `$700.00` Van payment effective 2028-01-01 priced every 2026 installment at `$700.00` and moved the derived payoff six installments EARLY once the future rows were absent -- the under-generating direction this arc exists to close, re-entered by the step closing it. The ESTIMATED tier read the CONTRACT for every uncovered slot whatever the definition said, so "what will this loan be paid in month M" had two answers and which one the fold used depended on whether the row had been WRITTEN yet: measured on a production clone, a Van payment standing at `$300.00` against a `$531.94` contractual installment resolved its recurrence bound to `2029-02-22` at the moment `regenerate_pay_periods` generates against `2030-02-22` before and after, twelve payments the owner owes. The same two answers switched the projection at the materialised HORIZON -- the owner's figure inside it, the servicer's past it. One rule closes both, and the payoff stops depending on materialisation at all, which is the loop R7d must break before it can stop storing the bound. `$0.00` on the developer's data, where both templates state exactly P&I + escrow. Shipped as R7d-a |
| recurrence | R-R34 | -- | 2026-08-25 (developer) | **How far a step that stops STORING a derived value reaches: To every reader that inherits the column, one leaf each.** R7d's specification censused the ten WRITERS of a loan payment's closing bound and no readers; measured by setting `end_date` NULL on both live rules, SIX surfaces read it -- generation, `recurring_view` / `describe`, `obligations_aggregator.has_ended`, the form's locked "Ends" control, `_recurrence_form_refusals`' inverted-window check and `_recurrence_preview` -- and with the column NULL every one of them reads "never ends". For `has_ended` that means a RETIRED loan's payment never leaves `/obligations` or the `/savings` emergency-fund baseline, `$2,442.89` a month across the two live loans. Shipping the resolver behind generation alone would delete a cache while leaving five surfaces reading its corpse. R7d therefore DECOMPOSES into seven leaves. |
| recurrence | R-R36 | -- | 2026-08-26 (developer) | **What a contractual month owes a definition whose cadence is not monthly: Nothing, because a month is not what a definition owes anything TO.** Interest is charged because TIME passed and escrow because a month began; a payment moves cash on whatever date its definition names. They are separate EVENTS, and once they are the cadence question is deleted rather than answered: a second payment inside one accrual period clears no fresh charge and pays pure principal. Fusing them made the payment COUNT the clock -- measured on a production clone, 30 payments of `$531.94` fourteen days apart and 30 a month apart charge the identical `$1,096.34`, split for split, and that fact needs no accrual convention to state. **The alternatives were weighed over ONE stream, named so the figures re-derive**: a `$531.94` definition firing every 14 days from `2026-09-04` against the Van's `$14,745.51` seed at 5.668%, accruing from `2026-08-26`. Per-occurrence answers `2027-10-01` / `$493.34` and the actual/365 truth over the same stream is `2027-10-01` / `$467.51` -- the DATE exactly right. Aggregating the month's occurrences into one payment charges the SAME `$493.34` (this fold charges per period either way) and dates the payoff `2027-10-22`, **21 days late**, because it moves the month's whole cash to the contractual day. The cadence's monthly EQUIVALENT can never show a three-payment month, and elapsed-day accrual is a different CONVENTION (D52) rather than a different fold. Owned by R16-a |
| recurrence | R-R37 | -- | 2026-08-27 (developer) | **Whether the ESTIMATED tier may read a definition's stored closing bound: It must HONOUR it, so the COLUMN must first hold one fact, and the ORDER changes rather than the rule.** Ignoring a field an owner entered was refused outright (developer): a closing bound is a real input to "what will this loan be paid", and a rule that skips it models money the owner has said will not move. But `budget.recurrence_rules.end_date` holds TWO facts today -- what an owner authored, and a snapshot of the derived payoff that `sync_recurring_payment_bounds` writes from ten call sites. **What tells them apart is `owns_validity_window`, and reading it here is the remedy R-R35 already refused**: it ends `active.id == template.id`, so asking it puts `active_recurring_transfer_template`'s `.order_by(id).first()` back on the PRICING path -- the tie-break four remedies were rejected to delete -- and it answers "owned" for a no-baseline owner whose bound nothing wrote (its own docstring, and finding **D49**). The column is written at MUTATION time while the fold moves with the CLOCK, so the two agree only between events. So the `R7d` reader leaves and the writer deletion move AHEAD of the sum -- `steps.md` holds the order and this does not restate it -- putting every reader of the derived bound on `loan_payment_window` and then NULLing the column, after which the bound is honoured unconditionally and there is no rule left to state. **What that migration must not do is erase an AUTHORED bound**, which "NULL for every loan payment" as specified would on a loan holding a second definition: finding **D56**. Owned by R16-b-2 |
| recurrence | R-R35 | -- | 2026-08-25 (developer) | **WHICH recurring transfer into a loan the derived bound is FOR: Every one of them, so the question is deleted rather than answered.** `loan_payment_window` takes the DEFINITION, not the loan, and the step's originally specified `(account, ctx)` is superseded: asking "when does this LOAN's payment stop" forces a prior question -- which of the recurring transfers into the loan IS its payment -- that nothing in the schema answers, so `active_recurring_transfer_template` tie-breaks it on `id`. Four remedies were put to the developer (a pointer on the loan, the settings row as the identity, refusing a second transfer, and writing the tie-break down) and ALL FOUR WERE REFUSED as band-aids on a search that should not exist. The measurement behind the refusal: the SETTLED fold (`loan_loaders.query_shadow_income`) and the PLANNED tier (`balance_at._plan.loan_plan` via `projected_income_shadows`) both already sum EVERY transfer into the loan account with no template filter, and `loan_ledger.split_payment_cash` already routes anything above interest + escrow to principal -- so "every transfer into a loan is a payment against it" is the app's own rule at two tiers of three, and the ESTIMATED tier is the single outlier that picks one. Each definition stops when the loan does, because past payoff that same split routes the whole cash to `excess` (a Refund). What this does NOT settle is the outlier: pricing an uncovered installment from ONE picked definition where its siblings sum is the half of D47 that survives, re-pointed to R16. Measured on a production clone: a `$200.00`/mo transfer into the Mortgage created before the real `$1,910.95` one wins the tie-break and drives the derived payoff `2048-12-01` -> `None`, against a `$616.99` monthly escrow it does not cover; with the resolver, BOTH definitions answer the same window. Shipped as R7d-b |
| recurrence | R-R32 | -- | 2026-08-20 (developer) | **How a definition's ACCOUNT change reaches the transfers it already generated: IN PLACE, through the one write door, which learns to move a transfer's endpoints.** A generated transfer derives SIX columns from its template and `transfer_service.update_transfer` could write FOUR, so the two account columns reached their rows only by the sweep DELETING and re-creating every one of them -- measured on a production clone at 51 of 62 rows re-pointed as a side effect of a rebuild -- while a NON-repeating template's identical edit was REFUSED outright, because nothing could carry it. That is a limit of the door, not a rule about transfers, so the door moves the parent and both shadow rows now, re-deriving each leg's display name and reconciling the ledger on both sides; `NON_REPEATING_ACCOUNTS_ARE_FIXED` is DELETED and one edit stops meaning two things. A row holding the owner's own records is still RETAINED rather than moved, which is R-R19 unchanged: its settled figure is what moved between the OLD accounts and its statement link is scoped BY account. Shipped as R10-b |
| pay_calendar | R-PC2 | -- | 2026-08-08 (developer) | **The pay-period model: NORMALIZE, do not check. `budget.pay_periods` stores the PAYDAY; `end_date` and `period_index` are derived by one producer and dropped from the table.** Option "store paydays only". It answers the recurrence arc's **F-10** fork as well -- a pay-period HOLE -- which that arc's own table carried as a second copy of this ruling until `balance:X-ao-2a` recorded it once; a hole and an overlap are both inexpressible once the derived columns are gone, which is **R-PC3** |
| pay_calendar | R-PC3 | -- | 2026-08-08 (developer) | **A gapped or overlapping batch: Neither is refused, because neither is expressible once the derived columns are gone. The "refuse it" and "bridge it with a filler period" options were both weighed and rejected -- see `historical/pay_calendar_evidence_2026-08-11.md`** |
| pay_calendar | R-PC4 | -- | 2026-08-08 (developer) | **A payday inserted BETWEEN two existing ones: REFUSE when the period it splits is locked by the existing `pay_period_admin.classify_period_lock` (historical, settled, posted, or a recurrence anchor); otherwise insert and re-derive. No row may ever be left dated outside its own paycheck** |
| pay_calendar | R-PC5 | -- | 2026-08-08 (developer) | **The last payday's period end.** Projected as `start_date + cadence_days - 1` from `budget.pay_schedule`. It is the ONLY derived end that is not `lead(paid_on) - 1`, and it is a projection stated as one -- `DerivedPeriod.end_is_projected`, because a consumer holding one period out of its calendar cannot recompute it and a window VIEW must keep it |
| pay_calendar | R-PC6 | -- | 2026-08-08 (developer) | **What a derived period carries: `period_id`, `int \| None`.** `None` IS the marker for a period no foreign key can point at, which is the distinction `C2` must draw between "which paycheck does this row live in" and "which span does this day fall in". One value type for the arc, so C2 MOVES `SchedulePeriod` rather than merging two |
| pay_calendar | R-PC7 | -- | 2026-08-08 (developer) | **Table and column names.** `budget.pay_periods.start_date` KEEPS both names. A period is identified one-to-one by the payday that opens it, `transactions.pay_period_id` reads correctly against it, and a rename is a four-FK migration that buys nothing. See `historical/pay_calendar_evidence_2026-08-11.md` |
| pay_calendar | R-PC8 | -- | 2026-08-10 (developer) | **An entry dated outside the schedule: A legitimate SECOND QUESTION, named on the one value -- not a compensator to delete.** The calendar gains `period_starting_on_or_before`, the missing mirror of the `period_starting_on_or_after` it already carries, and the FILING rule is DERIVED from it rather than scanning again. The alternative (drop the ledger's stored paycheck and derive it from `entry_date`) was measured against `shekel-prod-db` and REFUTED: 14 days carry TWO paychecks for one date, so `entry_date` does not determine it; 35 of 327 entries are dated outside their own paycheck by design; and 4 loan-opening entries predate the first payday by up to seven years, so the clamp is live rather than hypothetical. See `historical/pay_calendar_evidence_2026-08-11.md` |
| pay_calendar | R-PC9 | -- | 2026-08-10 (developer) | **Past the last stored payday: The calendar ANSWERS, projecting forward at the OWNER's cadence with `period_id = None`**, which is what makes it the TOTAL function `balance:X-l` asks for. `growth_engine.generate_projection_periods` and `SyntheticPeriod` retire into it (rows **P17**, **P20**). Containment over SAVED periods stays its own named method, because the recurrence engine needs to tell a schedule HOLE from "the schedule has not reached there yet" |
| pay_calendar | R-PC1 | -- | 2026-08-10 (developer) | **The forward-only rule (R-PC1): REPLACE `_reject_overlapping_batch`, do not delete it. SPLIT IN TWO, corrected twice by C3-b's neutral reviews, then HALVED 2026-08-11 (developer).** The rule the plan stated -- "the last paycheck must hold no row dated on or after the new payday" -- was measured wrong in both directions. What survives is a forward-only FLOOR of one full cadence after the latest payday, whose only job is keeping C6 closed and which C6 deletes. **The COVERAGE half is DELETED**: it refused a write taking a day out of every paycheck while a SETTLED row's `settled_on` fell on it, on the claim that stranding such a day reproduces `balance:N-128` -- and the claim was false. `_cash_periods` values each column at its OWN `end_date`, so a day off the top of the window is absent from both sides of R-K and reports as `period_timing`. See C3-b |
| pay_calendar | R-PC10 | -- | 2026-08-11 (developer) | **When the gap machinery dies: With `C2-b2`, the leaf that makes its subject unreachable -- not with `C4`.** `C5a` was gated on C4 and the gate had no code behind it: `PlacementOutcome.SCHEDULE_GAP`, `GenerationPlan.gaps` and `report_schedule_gaps` read no stored column, so all three went dead when the recurrence engine took the derived calendar, and shipping a branch that cannot fire is what rule 1 forbids. The visibility it took is replaced by `integrity_check` **BA-07**, which asks the stored question as a query and dies with the column at C4. Rejected: leaving it dead with a docstring note (ledger row **P25**'s original disposition), which ships code that lies about a state the app cannot hold |
| pay_calendar | R-PC11 | -- | 2026-08-13 (developer) | **How the seam learns WHICH periods to report: It READS them; the argument is DELETED.** All eight callers filled it with one value -- the owner's whole saved set -- so the only thing it could express was a mistake, which is the shape ruling R-Q already removed the override map for. `BalanceContext` carries the calendar, taking the first of `balance:X-i1`'s five inputs early. Rejected: callers slicing the calendar themselves (eight extra loads, and it pushes calendar-loading into routes ahead of `C2-f`), and threading a `PayCalendar` beside the context (a second per-request bundle, which is what the context exists to end) |
| pay_calendar | R-PC12 | -- | 2026-08-13 (developer) | **A window's two invariants: ORDER is DERIVED and CONTIGUITY is CHECKED.** A window's identity is its period SET, so `__post_init__` sorts and a caller cannot state an order wrongly; contiguity is a property of the input no constructor can compute its way out of, so it is refused. That refusal is P32's first disposition, taken because the second ("`containing` stops answering `None` inside its own span") is what a tiling gives for free and so decides nothing |
| pay_calendar | R-PC13 | -- | 2026-08-13 (developer) | **`PayCalendarError` reaching a balance page: RECORD it, do not build the handler.** `C2-c` widens the raise from the recurrence and savings surfaces to `/grid` and `/accounts/<id>`, where `app/error_handlers.py` leaves it on the bare 500. The raise is right (a defaulted cadence misreports every horizon); what is missing is a recovery page, which is a ruling of `BaselineMissingError`'s size. Row **P35** carries it and `C4` deletes the fallback that causes it |
| pay_calendar | R-PC14 | -- | 2026-08-14 (developer) | **The axis below the first payday: REFUSE, and clamp in ONE named companion**, ledger row **P23**. `axis()` answered a range opening below `opening_bound` with the part above it -- silently, and a short axis is indistinguishable from a complete one, which is the argument `overlapping()` already makes for refusing a CROSSED range. Nothing is projected backwards (the 2026-08-10 ruling), so covering such a range was never an option and refusing is the only answer that is not a half-truth. The owner whose first payday has not happened yet is an ordinary state -- the Generate form asks for "your next (or first) payday" -- so `projection_axis()` sits beside `axis()` as the TOTAL companion every projecting surface calls, exactly as `filing_period` sits beside `period_starting_on_or_before`. Rejected: stating the truncation in the `Returns` block (a value that answers a different question than it was asked, however honestly documented), and projecting backwards (it would attribute money to paychecks that never happened) |
| pay_calendar | R-PC15 | -- | 2026-08-14 (developer) | **What the projection axis HEAD is: Each surface keeps its own, and the SEED follows it**, ledger row **P22**. The axis covers the range it is given, so a caller passing the read pass's clock gets the period CONTAINING it -- opening up to a cadence in the past. That is correct wherever the seed is read at `axis[0].start_date - 1`, which `retirement_projection._resolve_seed_balances` already did and which `investment_dashboard_service` was corrected to do. The one surface whose seed CANNOT follow -- the Horizon's asset band, seeded from the figure the net-worth hero shows -- left the axis instead: it carries no contributions, so an axis was only chopping the horizon into pieces the compound formula is indifferent to. Rejected: clipping the axis head to the requested day (a partial period is not a period, and the window type exists to say so), and re-seeding the asset band at the axis head (one balance-seam read per account, and index 0 would stop equalling the hero) |
| pay_calendar | R-PC16 | -- | 2026-08-10 (developer) | **C2's shape: DECOMPOSED into `C2-a`..`C2-f`, the value first with nothing calling it.** A single commit over 60 call sites cannot be proven against production BEFORE its consumers depend on it, which is exactly the technique that made C1 safe, cannot be reviewed in focus, and cannot be reverted precisely -- and two of the cutovers move money |
| pay_calendar | R-PC17 | -- | 2026-08-14 (developer) | **`C2-f`'s shape: DECOMPOSED again into three leaves, split by READER**, on a measurement the first decomposition did not have: 13 functions read two or more of the six readers over 27 of their 60 sites, and 11 pair `get_current_period` with `get_all_periods`. Splitting THAT pair would leave a dozen context objects holding an ORM row in one field and a `DerivedPeriod` in another, so the two travel together in `C2-f3`. Rejected: by SURFACE (no leaf can then delete a reader, so no leaf has a checkable end state) and one commit (the shape **R-PC16**, this ruling's parent, already refused) |
| pay_calendar | R-PC18 | -- | 2026-08-14 (developer) | **`C2-f2`'s shape: DECOMPOSED into FIVE leaves, split by PACKAGE**, on a measurement the parent decomposition did not have: the **20** call sites at a surface holding a `BalanceContext` (AST census over the seven packages) feed **23** `app/` modules -- those calling one of the three readers or carrying its rows onward -- and **12** templates, being those reading a period's `id`, `start_date`, `end_date`, `period_index` or `label`; every one of the 12 is the grid's. One commit over that is the shape ruling **R-PC16** (`C2's shape`) already refused. **The by-READER axis its parent used is UNAVAILABLE here, which is why the split is by package** (adversarial design review, 2026-08-14): six of the seven surfaces read `get_current_period` and `get_all_periods` inside ONE function, so a reader split yields exactly two leaves -- `get_periods_in_range` (3 sites, all grid) and everything else (17 sites, 7 packages) -- and the second is the leaf this ruling exists to refuse. Each leaf's end state is a GRADED predicate rather than a grep: `a` closes **P37**, `b` DELETES `get_periods_in_range` whole, `c` closes **P48**, `d` closes **P43**, `e` closes **P36**. The SEAM goes first: it is the only leaf touching money-shaped logic (the annual-limit accumulation order) and the smallest diff, so it gets a review of its own. Rejected: three leaves (the middle one is 17 modules, the same objection one size down) and one commit |
| pay_calendar | R-PC19 | -- | 2026-08-14 (developer) | **How the CONTRIBUTION tier learns its periods: A REQUIRED `PayCalendar` parameter on `_asset_fold.resolve`, which derives the window itself**, ledger row **P37**; the type was `PeriodWindow` until an adversarial design review of `C2-f2a` refuted it the same day. That function deliberately takes no `BalanceContext` -- a fold assembled at one scenario beside a context carrying another loads the modelled tier against rows the cash tiers never saw -- and a calendar carries neither a scenario nor a clock, so passing one reintroduces nothing. **The CALENDAR and not the window, on three counts the window loses.** The tier's precondition is the owner's WHOLE schedule (the annual limit is a calendar-year accumulation, so a slice restarts the year-to-date total mid-year and uncaps it) and `PeriodWindow` cannot say whether `saved()`, `window()` or `axis()` produced it, so a window put that precondition in a docstring one call from the grid's six-period slice; the calendar keeps the OWNER on the value where a period list drops it; and **C9**, which projects this tier past the horizon on `projection_axis`, becomes a one-line change inside `resolve` rather than a sixth parameter, which `max-args` and `max-positional-arguments` (both 5, and this signature is at 5) would refuse. *The superseded window ruling claimed its two callers held two DIFFERENT windows; they did not -- both were `reported_periods()`, which is the argument ruling **R-PC11** ("How the seam learns WHICH periods to report") deleted, at a smaller scale.* Rejected: a field on `ContributionInputs` (its `absent()` token would carry an empty window, the silent failure made reachable) and a field on `AssembledCashFold` (an OWNER value copied onto a per-ACCOUNT record, and an argument every cash reader pays for that only the modelled tier reads -- two of its four callers discard everything but `.walk`) |
| pay_calendar | R-PC20 | -- | 2026-08-14 (developer) | **The modelled fold past the horizon: PROJECT the contributions; ruling `balance:R-AG` is SUPERSEDED**, and the evidence is what changed rather than the argument: R-AG (2026-07-27) let the fold run a half model because no total calendar existed, `C2-e` built one, and three surfaces already project on it -- so the seam is now the only one that does not and it disagrees with the pages built on it. `C9` is the remedy and it MOVES MONEY (row **P7**, `+$5,427.07` at six months out) |
| pay_calendar | R-PC21 | -- | 2026-08-16 (developer) | **How a producer gets its READ PASS: The ROUTE builds one per request; every producer below takes `balance_ctx` as a REQUIRED parameter and DROPS its `user_id`**, row **P43**. A producer holding an id can manufacture a `BalanceContext`, and three did, so `/retirement` held two passes and `/savings` two plus a third calendar derivation -- two clock reads behind two cards on one screen, `$4.18` of after-tax projected savings and one paycheck of countdown apart across a midnight-into-payday render. Dropping the id makes a mismatched (owner, pass) pair unrepresentable rather than discouraged, and a required parameter fails at the call instead of silently opening a pass. It follows the seam's own convention (`balance_at(acct, ctx, day)`) and the required-`PayCalendar` ruling **R-PC19**. Rejected: `user_id` BESIDE the pass (two spellings, nothing reconciling them); an optional `balance_ctx=None` that builds one when absent, which is the pattern that CAUSED this; and fixing only the one leaf P43 named, which leaves the measured contradiction on screen. **It does not make a second pass unconstructible** -- eight service modules still open their own, and the layer predicate that would (row **P56**) waits on the leaves closing those doors |
| pay_calendar | R-PC22 | -- | 2026-08-16 (developer) | **How a TEST builds a pass over hand-written paydays: ONE shared helper, `tests/_test_helpers.read_pass_over_paydays`**, row **P54**. The pass derives its calendar into a field its own module declares private; a unit case with no database must seed it, and three sites wanted to. Rejected: a `BalanceContext.for_test(calendar=...)` constructor (a production entry point whose only caller is the suite, `CLAUDE.md` rule 13) and an optional `calendar=` on the real `build` (it hands every production caller a way to supply a calendar the module did not derive, unchecked against the owner). Also rejected, on measurement: making the calendar EAGER like the scenario -- deriving one can RAISE for an owner with no pay schedule, so every render would begin failing over a fact most never read |
| pay_calendar | R-PC23 | -- | 2026-08-16 (developer) | **How the PAYCHECK ENGINE takes a period: A `DerivedPeriod`, and every caller supplies one**, on a fork `C2-f2d-3` could not cross without it: the engine reads a period's `start_date` and its ID, `/savings` needed to hand it derived periods, and no adapter is honest. Measured first: an AST census found those two attributes and nothing else, so the move is a retyping over 12 call sites in 10 modules rather than a rewrite, and all 13 `app/` sites trace to a MATERIALISED producer -- which is what makes `PeriodInfo.period_id`'s non-nullability structural where three consumers KEY on it. Rejected: one shared `current_breakdown(balance_ctx)` producer for the three surfaces that each build one (it merges what two OTHER pages publish, so it wants its own ruling -- row **P62**); a narrow ORM load kept inside `savings_dashboard_service` (a second pay-period read on a render whose whole point is one); and leaving the engine alone (`/savings` then keeps an ORM reader the arc exists to delete) |
| pay_calendar | R-PC24 | -- | 2026-08-16 (developer) | **`retirement_projection`'s two period parameters: DELETED; it reads both off the pass it already takes.** Both callers filled them from that same pass, so the pair could express only a mismatch -- the shape ruling **R-PC11** ("How the seam learns WHICH periods to report") removed at `C2-c`. 6 arguments to 4, which retires the `too-many-arguments` / `too-many-positional-arguments` suppression at its root rather than by raising a threshold, and left `_CurrentPay`'s two period fields with ZERO readers, so they went too. Rejected: converting at the `/savings` boundary (a second pay-period read), and keeping the parameters with a new type (the pass and the arguments can still disagree, and the suppression stays) |
| pay_calendar | R-PC25 | -- | 2026-08-16 (developer) | **`project_balance_horizons`'s two callers: Take `accounts/detail.py` onto the pass EARLY, out of `C2-f2e`.** That helper reads a period's ordinal and its id, and a `PayPeriod` spells the id `.id` where a `DerivedPeriod` spells it `.period_id` -- so its two callers could not hold two types between them for even one commit. Rejected: one module briefly holding both spellings (`accounts/detail.py` would carry a derived `current_period` for this call and an ORM one for its other fifteen reads), and splitting the rule from the lookup so each caller resolves index -> period -> balance itself (the loop written twice) |
| pay_calendar | R-PC26 | -- | 2026-08-18 (developer) | **What `/` renders for an owner with no PROJECTABLE account: The setup card AND the position tracks, and the card names the KIND that is missing**, ledger row **P65**. `has_account` is `resolve_grid_account`, which refuses an amortizing account by design (ruling D4) because this page renders a cash-flow balance -- so an owner who had entered a mortgage and nothing else was told to "set up an account" while `compute_tracks_section` computed a `$200,000.00` `DebtSummary` the template discarded. The tracks tier reads no grid account and no pay period, which is what lets it render beside the card rather than instead of it. Measured on both sides of `C2-f2e`, so it was this page's defect rather than that step's. Rejected: changing only the copy (the owner still cannot see a position they have entered), and leaving it (rule 8 -- a `$0.00` finding is a defect waiting for the data to change) |
| pay_calendar | R-PC27 | -- | 2026-08-19 (developer) | **The Spending producer's unexposed window arms: KEEP them, and retire the ordinal WALK inside them** at `C2-f3d`, against deleting the pay-period arm outright. It is unreachable -- `/analytics/spending` builds only month windows and `analytics_view.serialize_spending_chart` has only a month branch -- which `CLAUDE.md` rule 13 would ordinarily refuse. Two things outweigh it: the S-P1 gate ruling DEFERRED the pay-period and year pickers rather than cancelling them, so the arm is unshipped UI and not dead logic; and `C4` drops the `period_index` column every "which paycheck precedes this one" answer reads, so an arm that reads it owes that conversion whichever surface reaches it -- deleting it moves the same work to `C4` under another name. Recorded at `compute_spending_report` so the next reader meets it. Rejected: deleting the arm (it would take `query_settled_expenses`, ten unit cases and the harness's 62-window probe, which is this step's best regression coverage) |
| pay_calendar | R-PC28 | -- | 2026-08-19 (developer) | **The day the destructive doors decide on: The OWNER's civil day (`utils.dates.display_today`), and the lock classifier's `as_of` becomes REQUIRED** at `C2-f3b`, on finding `balance:N-191` -- which named `classify_periods_bulk` and `top_up_rolling_window` as the two sites deciding something against the user's CALENDAR on the PROCESS clock, and said each owed its own ruling. "Has this paycheck ended" and "has it started yet" are questions about the owner's schedule, so the owner's day is what answers them; `date.today()` is the container's, and both compose files pin `TZ: America/New_York` so the two agree THERE and nowhere else -- CI, a script and a bare `flask run` are where a truncate refusal turns on the deployment. The REQUIRED half is the same ruling one level down: `regenerate_pay_periods` read the clock THREE times for one decision, benign only because a period cannot become historical between two statements of one transaction, which is an argument from timing rather than from construction. Rejected: keeping `date.today()` (it leaves a destructive door's refusal resting on a deployment setting, which is N-191's own sentence), and resolving the day in the ROUTE and threading it (the same value, at the cost of a parameter on three public service doors, one of which is already at `max-args`) |
| pay_calendar | R-PC29 | -- | 2026-08-25 (developer) | **`C4`'s shape: DECOMPOSED into SEVEN leaves**, on the census re-measured that day: five reader leaves split by PACKAGE (`C4-a-1` .. `C4-a-5`), a schedule-row foreign key (`C4-b`), and the drop (`C4-c`). Eighteen-to-nineteen signature changes across five packages, plus a destructive migration, plus 63 `PayPeriod(...)` constructions in 35 test files that pass a dropped column, is the shape ruling **R-PC18** ("`C2-f2`'s shape") refused as one commit at a smaller size. The readers come off the columns first with NO schema change, each leaf ending at a package that names neither column; then the schedule row becomes guaranteed and the inferred cadence dies; then the columns drop. Rejected: three commits (`C4-a` whole is the five-package diff this ruling exists to refuse, and it cannot be reverted precisely), and folding `C4-b` into `C4-c` (an additive guarantee and a destructive drop would revert together, and `resolve_cadence`'s fallback death would land inside a schema migration rather than in front of one) |
| pay_calendar | R-PC30 | -- | 2026-08-25 (developer) | **How an owner's schedule row is guaranteed: A FOREIGN KEY, not a write-door discipline**, row **P8**. `budget.pay_periods.user_id` gains a second FK to `budget.pay_schedule (user_id)` -- already UNIQUE as `uq_pay_schedule_user` -- `ON DELETE CASCADE`, beside the CASCADE to `auth.users` it already carries. A payday for an owner whose cadence is not recorded becomes UNCONSTRUCTIBLE rather than merely unwritten, which is what lets `resolve_cadence`'s inferring fallback be DELETED rather than left as a branch no state can reach. Measured 2026-08-25 on both databases: 0 owners hold paydays without a schedule row, so the backfill writes nothing and the constraint takes clean. Rejected: leaving the invariant on `pay_period_write` and `auth_service.register_user`, where a violation surfaces as a `PayCalendarError` on a bare 500 (row **P35**) -- a symptom, at read time, on whichever money screen the owner opened first |
| pay_calendar | R-PC31 | -- | 2026-08-25 (developer) | **Where the attribution CLAMP lives: On the value: `DerivedPeriod.attribution_day`, and `utils.dates.attribution_date` is DELETED**, landing at `C4-a-2`. Its three live callers each pass one period's two bounds as separate arguments, which is a pairing a caller can get wrong -- "an argument a caller can get wrong is a defect, not a contract", which `_cash_fold`'s own docstring already cites -- and after `C4-a-1` and `C4-a-2` every one of them holds a `DerivedPeriod`, so the free function's signature outlives its last reason to exist. `DerivedPeriod.covers` lands with it, for the three sites that open-code `start_date <= day <= end_date`. **The rule's BODY moves rather than being wrapped**: `app.utils.dates` cannot import `pay_calendar` (`_derive` imports `app.utils.dates`, so a delegating free function would close a cycle), so a method beside a surviving function would be two independent implementations of one clamp. Rejected: keeping the free function and changing only where its bounds come from (the mis-pairable signature survives with no caller that needs it), and adding the method BESIDE it (two spellings of one rule, which is the denormalization this arc exists to remove) |
| pay_calendar | R-PC32 | -- | 2026-08-27 (developer) | **How a transaction's OWNER is proved: A COLUMN with two COMPOSITE FOREIGN KEYS, which is neither remedy row P75's fork listed**, landing at `C13`. `budget.transactions` carries no `user_id`, so its owner IS its pay period's -- and nothing requires that owner to be its ACCOUNT's, which makes a row filed in a stranger's paycheck EXPRESSIBLE. The fork offered the owner's CALENDAR against a `user_id`-filtered JOIN, and both are ways of ASKING; this makes the question unanswerable by making the state unconstructible. The row gains `user_id` and two composite FKs -- `(account_id, user_id)` against `budget.accounts (id, user_id)`, whose `uq_accounts_id_user` superkey already exists and which `fk_account_external_identities_owner` and `fk_statement_matches_owner` already target that way, and `(pay_period_id, user_id)` against `budget.pay_periods (id, user_id)`, which needs one added. `fk_transactions_reconciled_by` is already a composite FK on this very table, so the shape is in the house. It also retires the NINETEEN hand-written ownership comparisons P75 counts: an owner becomes a column to filter on rather than a relationship each door walks. Measured 2026-08-27: **0 mismatched rows on production and on both dev clones**, so the constraint takes clean. Rejected: the calendar and the JOIN, which leave the state writable and disagree with each other on adjacent doors -- the denormalisation this arc exists to remove |
| pay_calendar | R-PC33 | -- | 2026-08-18 (developer) | **What holds a `tests/manual/` harness to the code it measures: The lint gate, at `E,F` only**, ledger row **P66**. These are the proof instruments every cutover here is verified with, and nothing checked them: pytest does not collect the directory, CI linted only `app/`, and each probe's `_guard` records a raise AS DATA -- so a signature change makes a harness dump `{"RAISED": ...}` where a figure belongs, and a before/after diff of two such runs reads BYTE-IDENTICAL over a region never measured. FOUR sites were already dead when this was found, one since `C2-f2d-3`. `E,F` and NOT the 10.00/10 floor the other three trees hold: these scripts legitimately duplicate one another and branch wide, and what is gated is the class that rotted them. A both-sides harness naming a HEAD-only API carries a scoped disable with a `Pylint:` rationale. Rejected: a pytest smoke test that runs each harness (stronger, but it needs a seeded owner rich enough to reach every probe and the both-sides files would need skips), and leaving it recorded |
| pay_calendar | R-PC34 | -- | 2026-08-31 (developer) | **How the entry-list producer learns a paycheck's SPAN: The CALLER hands it the periods it queried by, `{pay_period_id: DerivedPeriod}`**, landing at `C4-a-3`. It is `due_captions_by_id`'s shape from `C4-a-1` and it costs no caller a read: both BATCH callers' maps ARE the period scope their own query filtered on -- `/grid`'s `pay_period_id IN (ctx.all_periods)` and the companion's `== period.period_id` -- so a row outside the map is unconstructible there rather than guarded against, which is `require_period`'s own "where the precondition is carried by the QUERY, the total form is honest". `entry_list_view` takes ONE `DerivedPeriod` and stopped taking the transaction at all, the row having been read for its pay period and nothing else. Rejected: the whole `PayCalendar` asked through `require_period` -- the arc's idiom at `_cash_fold` and the reconcile panel, and the better REFUSAL -- because the companion page holds no calendar by the design review of `C2-f2b` and would have to derive a SECOND one to look up the single period it is already holding, which is the redundant-producer read this arc exists to remove; and a map keyed by TRANSACTION id, parallel to `budgets`, which makes every caller write the join. **The package's own figure supports the shape rather than merely permitting it**: `_searches.period_by_id` records a dict built ONCE and then indexed at **0.019 ms** against **0.194 ms** for the scan and **0.692 ms** for a naive per-call index (measured 2026-08-27), and says a caller holding the whole period set and needing it many times is the pattern that decides this way |
| pay_calendar | R-PC35 | -- | 2026-08-31 (developer) | **`grid/page._build_grid_row_data`'s five-argument ceiling: HOIST the two entry-map builds into `index`, which DELETES a pass rather than moving one**, landing at `C4-a-3`, whose specification had offered only a rationale-carrying disable or the hoist. That helper runs TWICE per grid render and `_build_plan_view` DISCARDS both maps it returns -- its docstring had claimed since the Plan tab was built that no entry data was computed for it, describing the OUTPUT while the work was done anyway. So nothing gained an argument: `_GridRowData` lost two fields and both helpers lost `budgets`, leaving four. Rejected: the scoped disable, which keeps the wasted pass and adds a suppression to a codebase whose stated goal is to delete them; and narrowing `entry_lists` to the VISIBLE window, which would be cheaper still and is out of this leaf's scope (`CLAUDE.md` rule 6) |
| pay_calendar | R-PC36 | -- | 2026-08-31 (developer) | **How the merchant-destination picker learns each row's paycheck: the CALENDAR'S OWN SAVED INDEX, whose KEYS are also the query's scope** -- `PayCalendar.saved_by_id()`, landing at `C4-a-4`. **R-PC34** ruled the shape for a caller HANDED a map; this rules it for the caller that BUILDS one, and adds the half that makes it structural rather than merely faster: the filter and the lookup are ONE value, so a row the query returns and the mapping cannot place is unconstructible rather than skipped -- `require_period`'s own *where the precondition is carried by the QUERY, the total form is honest*. The producer therefore takes the CALENDAR and no `owner_id`, which is `candidates_for`'s stated rule one signature over. The accessor is the ONE spelling of `period_id is not None`, over `materialised_periods`; `statement_match._candidates` and both of `reconcile_service._rows`' scope properties each wrote their own until then. **Rejected: `calendar.period_by_id()` per row**, which this step's own sentence named -- it needs an unreachable-`None` branch that either silently drops an envelope off a money screen or raises, and `_searches.period_by_id`'s measurement puts the scan an order of magnitude above the index for this caller shape. **Also rejected: building the map locally**, which leaves one module with two spellings of one derivation. **AMENDED the same day by the adversarial design review**: the index is MEMOIZED behind a read-only proxy, because a pass asks it twice and an apply request four times -- the DRY rule the comment beside the call quotes -- and a shared MUTABLE scope lets one producer narrow the other's |
| pay_calendar | R-PC37 | -- | 2026-08-31 (developer) | **`PurchaseDestination` carries its paycheck as the `DerivedPeriod`, not as three copies of its facts** (`period_start`, `period_end`, `pay_period_id`), landing at `C4-a-4`. It is this arc's own defect one tier up -- one period's facts copied into a record -- and collapsing it takes the record from eight fields to six, which DELETED the class's `too-many-instance-attributes` disable rather than moving it: a fence retired by removing its subject. The order key moved with it, from `pay_period_id` to the paycheck's own PAYDAY, which is what the docstring already promised by *oldest pay period first*: the two agree on every schedule written forward, and `C6` inserts a payday MID-SCHEDULE by design. Blast radius measured BEFORE the ruling: four app read sites, the `label` property, four test constructors, no template. **Rejected: leaving the three fields** on the ground that a value built and consumed inside one request has no writer to drift from -- true, and it does not buy back the disable. **Also rejected: its own follow-up step**, declined because this leaf already rewrites the producer and traces every read |
| pay_calendar | R-PC38 | -- | 2026-08-31 (developer) | **`_calendar.py`'s 1,000-line ceiling is answered by CUTTING the identity section's arguments down to `_searches.py`, never by a split or a disable** -- landing at `C4-a-4`, which found the module at 998 with two lines of headroom, so ANY method broke it. The module's views section already states the rule (the sibling module holds the RULE and the argument; the call site keeps the contract) and a `C2-f3b` review measured that section's first draft restating them at 103 lines of one claim written twice. The identity section never got that cut: 162 lines over 3 methods against the views section's 148 over 7. `require_period`'s body moved with its argument so the method delegates exactly as `period_by_id` does, and `period_by_id`'s caller COUNT was deleted rather than updated -- it had claimed five values in turn. 1013 -> 935. **Rejected: a mixin**, which splits one class's methods across two files so a reader looking for `period_by_id` on the class does not find it there, and which this project has never used for this ceiling. **Also rejected: a module-level `too-many-lines` disable**, which `docs/coding-standards.md` permits and which all 11+ modules that met this ceiling declined. **Also rejected: a separate step first**, a rebase for no change in the answer |
| pay_calendar | R-PC39 | -- | 2026-08-31 (developer) | **How a caller names the stored row whose pay period it is looking up: ONE `FiledRow` value, never two loose ids** -- landing at `C4-a-5`. `require_period(period_id, transaction_id)` took two `int`s in a fixed order with nothing holding them to one row; four of its five call sites read both off the same object on the same line and were right by inspection, and the fifth (`reconcile_service._assemble._block_headings`) passed `row[2], row[0]` -- positional indexes into a three-column query tuple. **A crossed pair does not raise; it returns a DIFFERENT period.** The fields are KEYWORD-ONLY, so `FiledRow(1, 2)` is a `TypeError` and the only wordless construction is `FiledRow.for_row(row)`, which reads both ids off ONE object. **The STAKES are a mislabelled money screen, not a wrong figure**, and the first statement of this ruling said otherwise: it claimed the crossing "moves money to another day" by citing `_cash_fold._cash_plan`'s `attribution_day`, which is a DIFFERENT call site that reads both ids off one object and was never crossable. `_block_headings`' answer becomes `OutstandingGroup.period`, whose only consumer is the reconcile panel's `period_span` macro -- no Python reads it. It earns the value anyway: that heading names which paycheck's envelope a block of settle checkboxes belongs to, and the crossing becomes unconstructible for the next id-holding caller, which `C7` opens on `budget.journal_entries`. **Rejected: widening the refusal MESSAGE alone** (a `row_table` argument), which fixes a string and leaves the pairing while editing the same call sites. **Also rejected: passing the mapped ROW**, impossible at `_block_headings`, which selects three COLUMNS because hydrating the parent costs 9 LEFT OUTER JOINs and 89 columns against 0 and 3 (compiled against the postgres dialect, no database, 2026-08-31). **Also rejected: a second refusal inside the chooser**, two spellings of one rule |
| pay_calendar | R-PC40 | -- | 2026-09-01 (developer) | **A test fixture reaches its resting state through the application DOOR that produces it, and an account outliving the pay period it was opened against is a REAL state rather than a fixture artefact.** Ruling on `N-392`'s fork, whose two options were "defer `seed_user`'s account until a calendar exists" and "~40 cases stating their own asserted day is the end state". Neither: the row's premise was measured FALSE -- `pay_period_admin.reset_pay_periods`, reached from `POST /pay-periods/reset`, deletes every pay period an owner has including the one an account was opened against. So nothing is deferred, the ~40 cases are CORRECT rather than tolerated, and what was wrong is that the fixture hand-rolled that reset (delete, renumber `period_index` in raw SQL, re-post the corrections) instead of calling the door, which also re-syncs the loan genesis postings the hand-rolled version skipped. Built at `pay_calendar:C4-b-1`. |
| pay_calendar | R-PC41 | -- | 2026-09-01 (developer) | **An owner's pay periods HOLD their cadence row in place: `fk_pay_periods_schedule` is `ON DELETE RESTRICT`.** Ruling on the fork `C4-b-2`'s specification recorded as a testing note rather than a decision -- which is a decision nobody took. The full option space was put with measurements: `CASCADE`, `RESTRICT`, `NO ACTION`, `NO ACTION DEFERRABLE INITIALLY DEFERRED`. **The plan's own reason for `CASCADE` was measured FALSE first**: it said the `auth.users` double cascade needs an ordering `CASCADE` supplies, and a driven `DELETE FROM auth.users` against a clone of the developer's database SUCCEEDED under all four actions in BOTH referential-trigger orderings -- eight of eight -- so it was never an argument for either. The event is unreachable in production regardless: `transactions_account_id_fkey` is itself `RESTRICT` and dies first. **What decided it is what the action MEANS.** Nothing in `app/` deletes a `budget.pay_schedule` row, so the event has no live source and is reachable only by a bug, a hand-run statement, or a future door whose author has not thought about it -- and each wants a loud refusal. `CASCADE` states the opposite, that a pay period is a detail of the cadence with no independent existence: measured on that clone, deleting one schedule row took **63 pay periods, 1,057 transactions and every journal entry**. The cadence is a SETTING and the paydays are the RECORD; a record is not destroyed because a setting went away. **Rejected `NO ACTION`**: it refuses identically today and differs only in permitting a later `ALTER` to defer the check. **Rejected `DEFERRABLE`**: it legalises a wrong delete ordering instead of fixing it and opens a window in which the forbidden state exists. `pay_periods_user_id_fkey` KEEPS its `CASCADE` to `auth.users`: without it this key would refuse a user delete. Built at `pay_calendar:C4-b-2`, migration `f1c8b3d5e920`. |
| pay_calendar | R-PC42 | -- | 2026-09-01 (developer) | **`PayCalendarError` gets an application-level answer, and it SHIPS with `C4-b-2` rather than being deferred a fourth time.** Ruling on the deferral ledger row **P35** recorded: that row weighed a `BaselineMissingError`-shaped handler and deferred it to "this row's own owner", which is `C4-b` -- the container ticking with `C4-b-2`. Closing P35 by removing one CAUSE while leaving the deferral unanswered would have retired a question nobody took. **What decided it is that the gap is a SILENT-failure gap rather than an error-handling one.** `app/error_handlers.py` had no arm at all, so all raise sites reached the browser through the bare 500 -- and inside an htmx fragment reached NOTHING, because htmx does not swap a 500: a click that did nothing and said nothing, which is the failure a user cannot even report. Census by AST rather than grep (a grep over-counts: the class's own docstrings name it): **14 raise sites** in `calendar_infrequency` 1, `pay_calendar/_calendar` 2, `pay_calendar/_derive` 6, `pay_calendar/_loader` 1, `pay_calendar/_views` 2, `pay_calendar/_window` 1, `paycheck_calculator` 1. **Two states reach the handler and the PAGE deliberately does not distinguish them**: an owner with no `budget.pay_schedule` row, which since `fk_pay_periods_schedule` means no paydays -- ordinary, and repaired by generating a schedule -- and a payday set that cannot define a calendar, which is a broken invariant the owner can do nothing about. The page offers the one repair that exists; the `ERROR` event carries which state fired. Quiet on screen, loud in the log, which is the no-baseline handler's own split. `_recovery_response()` was extracted rather than the htmx branch copied: the rule belongs to the CLIENT (htmx swaps only 2xx) and not to either error. Built at `pay_calendar:C4-b-2`. |
| pay_calendar | R-PC43 | -- | 2026-09-01 (developer) | **`budget.pay_schedule.id` STAYS the primary key; the promote-`user_id` finding is REJECTED.** `C4-b-2`'s adversarial design review proposed dropping the surrogate `id` and promoting `user_id` -- already UNIQUE as `uq_pay_schedule_user` and the target of the new `fk_pay_periods_schedule` -- so that "exactly one schedule per owner" would be the table's identity rather than a constraint beside it. **Two measurements taken AFTER the review weakened it, and the review's own premise was the thing that failed.** (a) The finding's basis was "nothing reads `id`", true within `app/` and FALSE of the suite: `test_pay_schedule_service.py:63,90,95` read it, and `assert updated.id == schedule.id` is load-bearing -- it is how the suite proves the `ON CONFLICT DO UPDATE` upsert UPDATED the existing row rather than inserting a second, which `user_id` structurally cannot assert because it is the conflict target. (b) **All FIVE** 1:1 satellite tables in this schema key on `id` -- `auth.user_settings`, `auth.mfa_configs`, `budget.loan_params`, `budget.investment_params`, `budget.pay_schedule` -- so changing one would CREATE the special case rather than remove one. The gain was one integer column and a conceptual tidiness; the cost was making this table the only one in the schema shaped differently. **The new key is indifferent either way**: it targets `user_id` because `uq_pay_schedule_user` makes that legal, not because `user_id` is or is not the primary key. Recorded as the shape of the lesson: a subagent finding that reads as decisive is the one to re-measure. |
| pay_calendar | R-PC44 | -- | 2026-09-01 (developer) | **`ScheduleFacts`' optional cadence becomes its OWN leaf (`C4-d`), and `calendar_for`'s shape is decided there rather than inside `C4-b-2`.** `ScheduleFacts` carries `cadence_days` typed as an OPTIONAL int and `history_opens_on` as an OPTIONAL date, both copied off one `budget.pay_schedule` row -- but the columns are `NOT NULL` and nullable respectively, so the pair `(cadence=None, history=<a date>)` is constructible in Python and **no row can produce it**. That is the same defect `C4-b-2` removes from the SCHEMA, surviving one tier up in the TYPE. The remedy is `resolve_schedule` returning an OPTIONAL `ScheduleFacts` with a non-optional `cadence_days`, collapsing four representable states to the two real ones. **What made it a leaf rather than part of `C4-b-2` is the tier above it**, measured before the ruling: the direct blast radius is **5 call sites** in `app/`, only two of which branch on `None` -- but `pay_calendar.calendar_for` is the sole consumer of `resolve_schedule` and today builds an EMPTY calendar for a schedule-less owner. Making it return `None` instead is the honest version and reaches **48 call sites**, which is a design question about what a page does for an owner with no pay calendar and deserves its own review rather than riding in on a migration. **Rejected: doing the 5-site half now**, which moves the impossible pair from `ScheduleFacts` to `PayCalendar` rather than deleting it. **Rejected: recording without building**, on the developer's standing preference for structure over a fence. `C4-d` is BLOCKED BY `C4-b-2` (not `C4-c`): the new type is only true once `None` means "no schedule row" and nothing else, which is what deleting the inferring arm establishes; `C4-c` drops `end_date`/`period_index`, a different tier and different columns. |
| credit_card | R-CC1 | -- | 2026-07-19 (developer) | **RE-OPENED 2026-09-18 BY R-CC15: a charge is the plan row's covering MOVEMENT on the card and the row never moves.** **Re-account model.** Marking a purchase "Credit" MOVES the transaction to the card account and settles it there: real category, real date/period, real liability. The phantom payback dies. Paying the card is a transfer checking -> card. Undo moves it back, reverts to Projected. |
| credit_card | R-CC2 | -- | 2026-07-19 (developer) | **Full statement cycle.** Card params carry statement-close day + payment-due day + min-payment rule. A statement is a DERIVED snapshot, never stored. Grace: prior statement paid in full by its due date -> zero interest; otherwise a finance charge from the event stream. |
| credit_card | R-CC3 | -- | 2026-07-19 (developer) | **Derived statement payment.** ONE auto-maintained projected transfer checking -> card per statement on the due date; amount live-derived = statement balance minus reward redemptions posted since close (floor 0). Mirrors `derive_from_loan`. Underpayment fires a C7-style warning plus a projected finance charge. |
| credit_card | R-CC4 | -- | 2026-07-19 (developer) | **Cash back: flat rate; redemptions are events.** `cashback_rate` on card params; rewards ACCRUE derived; REDEMPTIONS are real recorded credit events. Manual at any amount, or auto-redeem at `auto_redeem_threshold` (nullable, e.g. $25). |
| credit_card | R-CC5 | -- | 2026-07-19 (developer) | **Migration: freeze history, migrate live rows.** Settled historical Credit pairs untouched. LIVE pairs converted inside the Alembic migration to settled card purchases (real category, original period) + payback deleted. Credit status becomes historical-only (terminal `credit: {credit}`) -- **which `balance:R-HA` makes LEGAL and gives an obligation**: no state may be both REACHABLE and ABSORBING, and this one is absorbing while `projected` loses its edge to it in the same commit, so it is a retired vocabulary item rather than a trapdoor. **What CC3b owes**: `transaction_service.deletion_refusal` names no status, so a terminal `Credit` re-arms the destroy-but-not-correct asymmetry that killed the archive -- either an arm naming it, or a stated reason it is acceptable on frozen card history; old workflow code deleted. Entry-level `is_credit` follows the same rule (split tender). |
| credit_card | R-CC6 | -- | 2026-07-19 (developer) | **Zero-card owners at migration time:** the migration creates a $0-anchor "Credit Card" account inline (account row + origination anchor history + linked/anchor-equity ledger rows, the account-factory invariant satisfied by hand; params-less = dormant plain liability until configured). Deploy never bricks. |
| credit_card | R-CC7 | -- | 2026-07-19 (developer) | **Renames ship in-arc at CC3c** (approval granted; add `Review:` lines): `credit_payback_for_id -> card_charge_for_id`, `TransactionEntry.credit_payback_id -> card_charge_id`, `is_credit -> is_card_tender` (+ index/constraint renames), in the same commit that changes their semantics. |
| credit_card | R-CC8 | -- | 2026-07-19 (developer) | **Finance-charge math: APR/365 daily periodic rate x average daily balance;** when grace is lost, new purchases enter the ADB from their settle date. All Decimal; `round_money` at the boundary only. |
| credit_card | R-CC9 | -- | 2026-07-19 (developer) | **RE-OPENED 2026-09-18 BY R-CC15 (the row never moves); what the source grid shows is `CC3a`'s design loop's question.** **A charged expense stays visible in place** on the source account's grid for its period: a display-only ghost row derived from `charged_from_account_id` provenance (CC badge, $0 balance effect, uncharge affordance) -- never a second stored row. |
| credit_card | R-CC10 | -- | 2026-07-19 (developer) | **The derived statement payment renders on the CHECKING grid as a payable** and deducts from the projected balance in its due-date period (structural: it is a real projected transfer with an expense shadow on checking). |
| credit_card | R-CC11 | -- | 2026-07-19 (developer) | **The companion flow survives intact:** a companion user recording a card-tender entry on an envelope row (`app/routes/companion.py` + the entries blueprint's accessible-transaction path) feeds the card-side transaction end to end while the envelope row + entries stay on the owner's checking grid. Pinned by a companion-access test at CC3c. |
| credit_card | R-CC12 | -- | 2026-07-19 (developer) | Timing shift to expect (accepted): the payment deducts in the DUE-DATE period, which can be later than the old next-period payback; the current period's end balance excluded card activity in BOTH models. A conservative deduct-at-close placement option is a possible later addition; correct timing ships first. |
| recurrence | R-R28 | -- | 2026-08-19 | **Semi-monthly pay keeps the NOMINAL 15-day walk.** No fixed `cadence_days` expresses the 1st and the 15th, `round(365.2425 / 15)` gives the right COUNT and drifting paydays, and a monthly cadence already carries exactly that limitation; a day-of-month schedule KIND is scheduled as **R13** rather than blocking R-F16 on it. Chosen from four options. **What does NOT transfer from the monthly case is the excuse**: a biweekly owner's 27-paycheck year is real, so `round(365.2425 / 14)`'s 0.34% approximates a real phenomenon, where a genuinely semi-monthly owner is paid exactly 24 times every year with zero variance -- so the 1.46% a 15-day walk carries (`24.3495 / 24`) is an artifact the model manufactures, about `$1,334` a year of over-modelled gross on a `$91,675` salary, on top of paydays drifting ~20 days off the 1st and the 15th. R13 is what removes it |
| recurrence | R-R38 | -- | 2026-08-27 (developer) | **WHO opens the read pass a generate pass runs in: The ROUTE does, and the DOOR SPLITS so that it can.** `GenerationSchedule` carries the `BalanceContext` and DERIVES its calendar, so the schedule a rule is resolved against and the pass its bound is resolved in cannot be two values that disagree; each of the eight construction sites was already holding a `calendar_for` beside a `get_baseline_scenario`, which is the pair a pass pins. **The first build had `period_population` open its own, and that was refused as a band-aid** (developer): the root cause is that `extend_pay_periods` performs a write and then a READ-DEPENDENT write in one call, so no caller can get between them, and a pass built before the write holds the pre-write calendar -- which `__post_init__` refuses for `for_period_ids` and silently accepts for `for_pass` -- and the pre-write LOAN, which nothing catches. Splitting the doors lets the route build the pass AFTER the periods exist and BEFORE the rows do, so the ordering is the shape of the code rather than a paragraph, and `pay_calendar:C11`'s predicate needs no carve-out for the GENERATE path -- which is the whole of what this settles: `C11` still carves out `loan_recurrence_sync` and four other service modules still call `BalanceContext.build`, so "nothing under `app/services/` builds a pass" is C11's end state and not this ruling's claim. Safe for all three doors, and order-independent by construction -- every read either posting re-sync makes of `budget.transactions` or `budget.transfers` is keyed on a set of ids taken from the POSTED ledger, and a freshly generated row is `Projected` and posts nothing. Owned by R7d-c-1 |
| bank_import | R-II | -- | 2026-08-31 (developer) | **A REFUND IS A NEGATIVE PURCHASE AGAINST THE ENVELOPE, AND THE POSITIVITY RULE MOVES FROM THE TABLE TO THE HAND-ENTRY DOOR.** `ck_transaction_entries_positive_amount` becomes `amount <> 0`. **Four grounds.** A refund is a CONTRA to the expense rather than revenue -- booking it as income grosses up both sides of the income statement, and `posting_service._settled_target` takes its class from the transaction TYPE, so an income row under a spending category mints an INCOME-class ledger account for it. The app's arithmetic is ALREADY SIGN-GENERAL: `_purchase_target` at `-28.29` emits `{cash: +28.29, category: -28.29}` with no branch, and `settled_cash_leg`'s three terms are all sums that net -- which is the tell that the ledger never needed the restriction. The envelope's recorded cost IS the sum of its entries, so it must fall or *did I stay in budget* answers wrong. And `amount > 0` is a fence at the wrong TIER: the real invariant is `<> 0` (a purchase worth nothing is not a purchase), while positivity is a statement about what a HAND-ENTRY FORM should accept -- where a typed negative is a typo -- and belongs on that door. **It rules the MODEL and promises nothing about feasibility**: the census over every reader that assumes a positive entry is `X-gj-2b`'s opening task and may send this back. A ruling that says *this is the shape* is not a prediction that the corpus agrees. Ruled after `bank_import:X-gj-2a` shipped the deposit half of **R-HT(a)**, which reports a merchant credit rather than filing it precisely because this act did not exist |
| bank_import | R-IK | -- | 2026-09-01 (developer) | **AN ENVELOPE'S BUDGET IS A NET CASH TARGET, SO ITS REMAINING IS UNBOUNDED ABOVE AND A REFUND CREATES ROOM IN IT.** `entry_service.compute_remaining` stays `budget - Sigma(entries)`: a `$100.00` envelope holding one `-$50.00` refund answers `$150.00`, the dashboard's still-due reads `$150.00`, and carry-forward rolls `$150.00` into the next period while the source settles at `-$50.00` -- the two conserving the `$100.00` of plan the owner declared. **Two alternatives were put with those numbers and refused.** Capping at the budget breaks `Sigma(entries) + remaining == budget`, which every surface rendering the figure depends on, and makes the dashboard disagree with the reservation `cash_ledger._amounts._entry_checking_impact` already holds for the same row. Splitting the envelope into gross spend and refunds shown apart needs that reservation changed too, and that is the BALANCE. The ruled basis is the one the balance already reads: its own derivation says treating a refund as spending nothing further *is the opposite of what a reservation is for*. **WHAT IT COSTS, ruled known and accepted:** a refund of a purchase made in an EARLIER pay period lands in the period the bank POSTED it (the same day's cash-basis ruling, taken because the alternative moves a figure the balance projection depends on), so it enlarges an envelope whose own purchases it did not reverse and enlarges what that envelope rolls forward. Ruled during `bank_import:X-gj-2b-3`'s reader census, which is what surfaced it |
| bank_import | R-IL | -- | 2026-09-01 (developer) | **THE SPENDING REPORT'S SHARE IS A SLICE OF WHAT MOVED, NOT OF THE NET.** `_breakdown._share_base` is `sum(abs(category total))` over the window. It was the NET total, guarded below and UNBOUNDED ABOVE, and ruling **R-II** let a category's refunds carry it below zero: `Groceries 600.00` beside `Electronics -500.00` nets `100.00`, so `analytics/_spending.html` printed **600%** and **-500%** beside `$600.00` and `-$500.00` -- over bars that looked right, because `analytics_view._bar_pct` scales to the largest row and floors at zero. Magnitudes bound every share in `[-1, 1]` by construction, and **the two denominators are the SAME figure on any window holding no refund**, which is what makes it safe to merge: an ordinary month renders exactly what it always did. Rejected at the gate: the positive categories only (unbounded for the same reason the net is, at `$10.00` of purchases beside a `-$500.00` refund), printing no percent at all where any category went negative, and leaving `600%` standing with only its docstring corrected |
| bank_import | R-IM | -- | 2026-09-01 (developer) | **THE DESKTOP GRID CELL'S FIGURES GO THROUGH `money()`, AND THE CELL GAINS A CURRENCY MARK.** `grid/_transaction_cell.html` formatted four figures with a raw `"{:,.0f}".format` -- a second spelling of the ONE template currency formatter gate B7 names, in the file whose own `detail` tooltip already used the macro, and for two figures (`es.total`, and `recorded` on a settled envelope) that `grid/_grid_row_macros.html` renders through `money(..., cents=false)` for the same row. The raw spelling could not show a sign while every purchase was positive; under ruling **R-II** it printed `-87 / 100` where the mobile twin printed `-$87 / $100`. The visual change was stated before the ruling and accepted: the desktop cell reads `$50 / $500`, and three test assertions move with it under CLAUDE.md rule 5's stated exception |
| balance | R-IN | -- | 2026-09-01 (developer) | **EVERY transfer SHADOW is declared DERIVED, not only a loan payment's.** A loan payment's shadow IS a transfer shadow and both declare the same relation (`parent_transfer`), so a cutover scoped to loan payments needs a conditional at the shadow constructor, two more in `update_transfer` and `restore_transfer`, and a WRITER on the two loan-settings routes -- because flipping an existing payment to auto-track would have to declare rows already generated. Declaring every shadow needs none of them: the constructor stamps unconditionally, the amount copy and the Invariant-3 drift corrector are DELETED, and WHICH rule prices a leg stays a live read off the parent's template (**R-FK**), so a mode flip rewrites no row. It absorbs `X-au-f`'s SHADOW half; what remains of that step is the parent transfer's own amount deriving from its series. Measured before it was taken, on production at stamp `a4c6f1d92b73`: 350 shadows, **0** whose figure differed from their parent's, so it moves `$0.00`. Rejected: the loan-only scope, for the four conditionals and the route writer -- a second maintainer of a derived value is the shape this arc exists to delete |
| balance | R-IO | -- | 2026-09-01 (developer) | **The figure the OWNER types always wins, and it reaches the loan's split AS the cash.** A leg owns a figure exactly when the caller states one, says a human authored it, AND that figure MOVED. Without the first two a typed figure is discarded on the one shape where a leg's derivation is not its parent's amount -- a DERIVE-mode loan payment resolves to the loan's installment, so retyping `$1,325.00` over a `$1,499.10` PITI and marking it Paid books the contract and loses the `$174.10` really paid. Because the typed figure is the CASH, the genesis split allocates against it: interest `$1,000.00` and escrow `$300.00` in full, principal absorbing `$25.00` rather than the `$199.10` a full installment pays down. **The MOVED test is the third condition and omitting it was a money defect**: `routes/transfers/mutations.py` raises `is_override` when the amount changed OR THE PERIOD DID, and that form posts every input it renders, so a period-only save carries an ECHOED amount beside the flag -- taking ownership on it would freeze a derive-mode payment's legs at the stale snapshot, which is finding **N-238**'s exposure added by the step removing it. **The allocation RULE -- escrow once per INSTALLMENT in both producers -- is `X-au-g-2c-3`'s (**N-409**), not this ruling's.** A bare flag with no amount changes no ownership in either direction |
| balance | R-IR | -- | 2026-09-01 (developer) | **PYLINT'S 1,000-LINE MODULE CEILING STAYS, AND THE SESSION THAT BREAKS A MODULE IS THE ONE THAT SPLITS IT.** It is not a number this project chose: `max-module-lines` is set in NO config file (`.pylintrc`, `pyproject.toml`, `setup.cfg` and `tox.ini` are all silent), so 1,000 is pylint's DEFAULT and a later reader asking who picked it is looking for a decision that was never taken. The split falls to the breaker at the moment C0302 fires, which makes the refusal itself the notice -- **`N-435` asked for a third thing, a breach visible in the RATING, and did not get it**: one C0302 across ~500 modules still rounds to 10.00/10, so a commit message quoting the score is silent about the ceiling and pylint's exit code (bit 16) is what carries it. **It also WITHDRAWS the escape hatch `docs/coding-standards.md:96` reads as permitting**, a module-level `too-many-lines` disable carrying a `Pylint:` rationale. Measured 2026-09-01 on `dev@720b9855`: **zero** modules in `app/` carry one, so the permission was never exercised and withdrawing it grandfathers nothing. **This row deliberately names no module.** `N-435`'s census went stale twice while its own question was open -- `_candidates.py` 996 -> 999, and `calendar_service.py` left the top eight when `bank_import:X-gj-2b-3` split `calendar_day_flows.py` out of it -- which is the argument for a rule rather than a worklist. Closes **N-435** |
| balance | R-IP | -- | 2026-09-01 (developer) | **AN ACCEPTED RESIDUAL OWNS ITS FIGURE, AND THE DERIVATION R-GY REQUIRES IS AN OFFER RATHER THAN A READ.** Amends **R-GY**'s *"DERIVED while uncategorised"*. The row STORES its amount; when the outstanding difference reopens because the owner records something that was part of the lump, the card says *"you already accounted for this once"* and ONE press reprices that row. R-GY's EFFECT is kept -- the bucket still shrinks as things are recorded, which **R-FN** calls the mechanism that shrinks it -- and only its LETTER changes. **The literal reading is INFEASIBLE and was measured so in three independent places** (the SQL tier the posted-ledger oracle reads, R-FI's own definition of a settled figure, and three shipped guards a silent reprice would break); the measurements are X-f3c-4b's specification. **The two arms are ONE door and the GOVERNING assertion decides between them**: where its accepted row exists and still carries the app's own derivation the act reprices it, else it mints one. **A reprice is REVERT, RESTATE, RE-SETTLE, never a write underneath the owner** -- the house rule of 2026-08-17 that `entry_credit_workflow.sync_entry_payback` already enforces in those words, after which `Settlement.from_settle` re-resolves because a `derived` record is the app's own inference about a moment that has passed. **The freeze is therefore CATEGORISED, OR carrying a retained `corrected` record**, both meaning the owner has stated the figure themselves: `honoured_correction` makes the second authoritative, so a fold-in offered there would book the old figure under a screen showing the new one. Rejected: a read-time derivation, a reconciler maintaining the stored figure (**R-FI**'s named shape), and one new row per reopening, which overstates BOTH gross figures on the income statement -- the churn R-FN refuses, one step removed |
| balance | R-IQ | -- | 2026-09-01 (developer), amended 2026-09-02 | **THE COMPARISON BETWEEN AN ACCOUNT'S STORED OPENING AND THE BANK'S OWN CLOSING FOR THAT DAY IS A STANDING CHECK, NOT A GATE.** Ruled first as a third bound on **R-GY** -- the acceptance act refused unless the opening was bank-corroborated -- and **AMENDED 2026-09-02 when its own step's adversarial review refuted the rationale**: the bound removes `e_o` and then books `e_a`, so it argues against the act it was written to license. **The MECHANISM survives and is the most valuable single check in the arc.** It reads `statement_import.recorded_opening_before` at the books' opening day, and on the developer's own Checking it measured a stored opening of `$689.16` for 2026-03-26 where the bank closed at `$3,182.63` -- **`e_o = -$2,493.47`**, the largest single error in the books and one no other instrument reports. So it stops REFUSING an act and starts REPORTING a state, naming the restatement door **X-f3c-2b-2a** as the remedy. EQUALITY, never a tolerance: **R-GD** rejects a blanket tolerance and one here would admit exactly the small permanent level error this exists to surface. **The ROOT CAUSE it defers to rather than duplicates is N-314**, closed structurally by **R-IS**. The measurement, the refutation and the eleven other claims those reviews broke are `docs/design/from_scratch_architecture.md`; the superseded wording is at `6c995201` |
| balance | R-IS | -- | 2026-09-01 (developer) | **THE OWNER'S TRUE-UPS AND THE BANK'S STATEMENT CLOSINGS BECOME ONE EVIDENCE-RANKED LEVEL RELATION.** They are observations of the same quantity -- an account's balance on a day -- held in unrelated places with no rule saying which governs, which is finding **N-314**. One append-only relation of `(account, day, amount, source, evidence)` closes it STRUCTURALLY rather than by ruling: there is no "which authority wins" branch to write, because there is one relation with a declared rank, and `StatementBalanceEvidenceEnum` with `statement_import.weaker_of` already ship and already rank exactly this. **A level observation NEVER moves a balance**; it produces `discrepancy = observed - computed`, and zero is the healthy state. **The OPENING stays a separate constitutive fact** (**R-GX**, **R-HE**): it is the seed the fold starts from, not an observation of it. **The bank half cannot move in as it stands** -- an assertion is append-only at the DATABASE tier (**R-HY**, **R-IC**) while `statement_import.release_anchors_from` releases an anchor by UPDATE, so a release becomes an APPENDED superseding row, which also keeps the audit trail an update destroys. Rejected: ruling the precedence case by case, which is a rule a reader must remember rather than a structure that cannot be got wrong |
| balance | R-IT | -- | 2026-09-02 (developer) | **THE AMOUNT-OWNERSHIP CUTOVER RUNS BEFORE THE MOVEMENT UNIFICATION, AND THE FLIP RUNS AFTER BOTH; the order itself is `steps.md`'s and is not restated here.** **An earlier order was ruled the same day and WITHDRAWN by its author**: the plan gate REFUSED it on three arms, because it ranked the flip ahead of the unshipped step blocking it, and an approved order no gate accepts is not a plan. **The reason the cutover goes first is measured, and it is the part worth keeping**: cutting template rows over NULLs `estimated_amount` on 511 of them, so a movement table built first copies figures later steps are about to delete; cutting transfers over makes **Transfer Invariant 3 STRUCTURAL rather than maintained**, which removes the movement work's largest named risk instead of managing it; and the salary cutover deletes from `transaction_service/_settle.py`, where the movement work's own hardest leaf lives. **What it costs is stated rather than hidden**: the credit-card arc still waits, and this is not the faster route to the flip. **R-GW was RE-MEASURED rather than overruled** -- its own figures reproduce, and its premise dissolves only under a COMPLETE reconcile, after which what remains is the owner's typing rather than a residual; its ordering requirement is replaced by a measurable precondition, `outstanding_difference.SpanAgreement.reconciles`. Rejected: keeping the withdrawn order |
| balance | R-IX | -- | 2026-09-02 (developer) | **AN ACCRUAL PERIOD'S RATE IS RESOLVED ONCE, ON THE CHARGE, AND EVERY PAYMENT IN THAT PERIOD CARRIES IT.** `AccrualCharge` carries the whole governing `RatePeriod` rather than a bare `annual_rate`, and `LoanPaymentSplit.period` is that period rather than one re-resolved on the payment's own due date. The split's `period` is what the CONFIRMED schedule row displays as its `interest_rate` and sizes its `extra_payment` against (`period_pi`), so the two readings must be one: while the payment re-resolved, a rate change effective INSIDE an accrual period would tag a second payment's row with the NEW rate while its interest -- `$0.00`, since the period's charge was already cleared -- belonged to the OLD one. **Identical for the one-payment-a-month shape every live loan is in, and reachable only in the shape `X-au-g-2c-3b-2` exists to serve**, which is why it is ruled with that step rather than discovered by the first owner to make two payments in a month. Rejected: re-resolving `period_for_date` on the charge's date inside the walk, which gives the same answer and states one lookup twice -- the duplication this arc keeps deleting; and leaving the payment to re-resolve on its own due date, which is the smallest diff and leaves the docstring's *"the row's rate is the rate its interest accrued at"* false in exactly the new shape |
| bank_import | R-IU | -- | 2026-09-01 (developer) | **A GROUP'S DIFFERENCE MAY LAND ON A MEMBER THE OWNER NAMES, AND NO MEMBER IS PRE-SELECTED.** The MATCH pane gains one control -- *where the difference goes* -- offered only where there is a choice to make, and its first option is **R-FN**'s ordinary row, which is what every group did before. **The DEFAULT was ruled with the control, and the ruling that settles it is R-HX rather than R-FZ(b)**: R-HS says in its own words that R-FZ(b) banned the ARBITRARY default and *never meant a suggestion the app can justify*, so citing it for a DERIVED pick cites the half it carved away; R-HX is on point -- *being the ONLY act is not what R-HS means by justified*. **The app is not without a CANDIDATE**: `salary_profiles.template_id` names the member whose figure is COMPUTED, so *which member rounds* is a stored fact. What it lacks is a JUSTIFICATION, because `balance:N-391` reads **NOT yet attributed** -- a `$0.04` error in any of 12 hand-entered deductions reproduces the same net, and the gross is n=1 -- and supplying one is what `bank_import:X-gj-3b`'s standing rule is for (**R-HT(b)**). **Measured, on two restores of one dump of the developer's staged data, applying all seven payroll deposits through the real door**: the residue on the salary row moves those rows `+$0.35` and mints 0 uncategorized rows; on nothing it moves `$0.00` and mints 7; trial balance `0.00` both ways. That is the two remedies being DISJOINT, and it is NOT a per-member attribution -- the bank shows ONE deposit, so *the allowances match exactly* is `bank less allowances = salary` restated. **Rejected: pre-selecting the computed member** (a justification the app cannot defend while N-391 stands), and **leaving the door as it was** (the salary row stays under what the employer paid, and seven 4-6 cent rows a year are minted) |
| bank_import | R-JJ | -- | 2026-09-02 (developer) | **A STANDING RULE DOES NOT NAME A ROW SET, AND `X-gj-3b` IS WITHDRAWN.** **R-HT(b) had TWO clauses and only the ROW-SET one is repealed.** Its *where the residue lands* clause SHIPPED at `X-gj-3a`, is amended by **R-IU**, and is cited for live behaviour by `_variance.py` and `_accept.py` -- repealing the ruling whole would strand them. **R-HT(a)** stands, shipped at `X-gj-2a` / `X-gj-2b`. **THE DELTA IS SMALLER THAN THE STEP.** R-HT(b) says the deposits become one OK each under it -- they are one OK each WITHOUT it, through the door `X-gj-3a` shipped. The rule adds PRE-TICKING 2-3 checkboxes ~26 times a year, bought with a child table, a migration, a module split and a new arm on a money door. **THE CENSUS IS WHY**: on a clone of the developer's staged data, **3 of 221** acts name more than one app row, and each population is removed by a ROOT-CAUSE step elsewhere -- payroll by `recurrence:R18`, Capital One by `credit_card:CC3b` (**N-337**), and a lone Amazon order split no rule can express. **ITS SURFACE IS THE DEFECT, AND R-IB DELETED THAT SURFACE ONCE ALREADY**: storing the larger template set is right on 7 of 7, but WHICH set is stored depends on which line the owner pressed, and stored small then met large it proposes `$118.78` of re-pricing on the salary row. A rule is ONE fact and the control renders once per LINE. **AND THE FACT IT WOULD STORE HAS NO HOME YET**: nothing records which templates are part of a paycheck, so a merchant rule keyed on a bank string would be that fact's FIRST home, and `R18` then gives it its proper one on the salary profile -- at which point the bank_import copy is `CLAUDE.md` rule 14's stale second home. Build it in the right place once. **The chain is pulled UP**: the recurrence work ending in `R18` is re-ranked directly after bank_import's production scope, which `steps.md` carries. The measurements, the rejected options and the open question are in `X-gj-3b`'s withdrawal record. |
| bank_import | R-JG | -- | 2026-09-02 (developer) | **A SKIPPED LINE'S DISPOSITION IS ITS OWN ACT ROW, DELETED TO UNDO.** `budget.statement_line_skips` holds one row per skipped line, and undoing a skip DELETES it. **Not a nullable column on `budget.bank_statement_lines`**: every column there is a fact the SOURCE stated, and the re-import path rests on that premise -- `statement_import._record._absorb_gained_facts` fills a NULL column from what a later export states, which is how a re-exported running balance is gained rather than discarded -- so a *the owner skipped this* column would be the first there a source can never state. *An earlier draft called it a loop a new column would need excluding from; it is five explicit `if recorded.X is None` arms, so such a column would simply have no arm -- the argument stands on its other two legs.* The line table also carries no `user_id`, so who decided would be unrecordable. **And not append-only**, which was the step's own stated recommendation and is refused on a measurement: the forensic record it was for already exists, because `budget.statement_line_skips` joins `app.audit_infrastructure.AUDITED_TABLES` and `system.audit_trigger_func`'s DELETE arm writes `to_jsonb(OLD)` and the acting `user_id`. An *unskipped* row would add only a history the APP could display, which nothing asks for, at the price of a "latest row per line" read on the review pass and the grid badge -- and of a row meaning *no disposition*, the shape `RuleAnswer` refuses in as many words. **It cannot be a `statement_matches` row at all**: `_candidates.act_still_names_a_row` deliberately treats an act with no app-side member as NOT A CLAIM, so a match holding a line and nothing else would leave that line reading unexplained forever. Rule 14's stored half is satisfied by construction -- the disposition has ONE home and nothing else stores it. Shipped at `X-gj-4a`. |
| bank_import | R-JH | -- | 2026-09-02 (developer) | **THE SKIPPED TAB HOLDS ONLY LINES THE OWNER SKIPPED, AND A STANDING *NEVER A PURCHASE* ANSWER CLAIMS NOTHING ABOUT WHAT A LINE IS.** That answer bars the create door and stops there: *not a purchase* is not *explained by nothing*, since a paycheck is neither and a transfer to savings is neither. So a line it parks is not a disposition, it is an unexplained line whose ADD verb is shut -- and it returns to the inbox under *Nothing suggested* with the reason in its panel, keeping MATCH and gaining SKIP, which is why it never has "no available act" and **R-HQ** still holds for it. **This ruling is the developer's confirmation that the expected behaviour CHANGED**, which is the only thing that makes `CLAUDE.md` rule 5's exception apply: `X-gj-4c` DELETES `_reconcile._parked_tab`'s SKIPPED arm, `_sentence.for_parked_never` and `test_reconcile.TestAStandingNeverAnswerIsAlreadyASkip`. **The deletion rests on the SEMANTIC argument above and on nothing else, because the arm is NOT unreachable.** A first draft called it structurally memberless and adversarial review refuted that with a PASSING TEST in this repository; the refutation is recorded in `X-gj-4c`'s specification, and what matters here is that the arm IS reachable, so the deletion rests on the semantic argument alone. **So `X-gj-4c` must REMOVE those lines from `parked` altogether and not merely drop the tab arm**: dropping the arm alone makes `_parked_tab` total on TRANSFERS, and such a line then joins the *waiting for the account they paid* chip, whose label carries a COUNT and a MAGNITUDE -- a rendered money figure covering a line the bank never filed as an account payment, which is the harm **R-JI** refuses. |
| bank_import | R-JI | -- | 2026-09-02 (developer) | **SKIP IS SHUT FOR A LINE THE SOURCE FILES AS PAYING AN ACCOUNT THE OWNER HOLDS, AND IT IS SHUT AT THE DOOR.** That money moved between two accounts they hold and the card arc will pair the two sides (**N-337**, owner `credit_card:CC3b`), so recording *explained by nothing* for it would store a decision the app already knows to be false. **The refusal lives in `skip_line` and not only in the panel**, which is ruling **R-GJ**'s lesson applied to a second verb: that ruling cost `$7,412.94` to learn that a warning paragraph over a working control is not a refusal, so a SKIP tab rendered shut over an open door would be the same shape. **And it is not only doctrine**: such a line is a `ParkedLine` counted on the hero's *waiting for the account they paid* chip, whose label carries a COUNT and a MAGNITUDE, so admitting the skip would drop a rendered MONEY figure -- which is the one thing this act is supposed never to do. Named by adversarial review 2026-09-02; enforced at the door from `X-gj-4a`, rendered shut at `X-gj-4b`. |
| bank_import | R-IV | -- | 2026-09-01 (developer); SUPERSEDED 2026-09-11 | **SUPERSEDED by `R-BI2` at `X-gp`** (`40354328`, 2026-09-11): the bound below rested on a DOM event, and `X-gi-2a` renders the pane on a page where nothing swaps; the consent is ONE value carrying the figure AND the member (`ReviewedDifference`), compared whole, so there is no second field left to pair -- the widening this ruling refused is what shipped. As ruled: **A MATCH'S CONSENT BINDS THE FIGURE AND NOT THE REMEDY, AND THAT BOUND IS KNOWN AND ACCEPTED.** Since `X-gj-3a` a submission carried how MUCH (`residual-<line>`) and WHERE it lands (`difference_on-<line>`), and `_variance._reject_unaccepted_difference` compared only the first, so a body could pair the consent rendered under *record it as a row with no category* with an attribution that re-priced a budget row instead. A browser could not produce it, VERIFIED: the select sat inside `.rec-match-picks`, whose change swapped the consent box unticked. The only party able to craft it was the owner, on their own rows. Rejected then: widening the consent value to carry the remedy beside the figure -- **R-IA**'s rule one field wider, refused only because it changed a wire format two surfaces submit to close a path whose sole traveller was the person consenting. Found by adversarial code review of `X-gj-3a`; the deliberation is that step's commit and **R-BI2** |
| pay_calendar | R-PC45 | -- | 2026-09-02 (developer) | **A PAY CALENDAR HAS A CADENCE, AND AN OWNER WITH NO `budget.pay_schedule` ROW HAS NO PAY CALENDAR: `calendar_for` REFUSES THEM.** Ruling on the fork `R-PC44` deferred to `C4-d`. `ScheduleFacts.cadence_days` was optional beside a nullable `history_opens_on`, so four pairs were constructible where a row can produce two -- and the impossible one, an absent cadence beside a STATED opening, cannot come from a `NOT NULL` column. That absence travelled into `PayCalendar`, `derive_periods` and three `_views` producers: six optional declarations policed by two runtime raises and five prose preconditions, one conceding outright that it *"cannot check for itself"*. Making the cadence plain deletes all of it, and forces this question, because the empty cadence-less calendar was its last construction site. **CHOSEN: refuse.** `calendar_for` keeps its signature and raises `PayCalendarError` -- what `cadence_for` has done for this owner since `R7a-2a`, answered by the handler `R-PC42` shipped, whose docstring already names the state. **None of the 40 call sites in `app/` changes** (AST, 2026-09-02; `R-PC44` said 48). **Rejected: an optional return** -- ~38 of the 40 sit behind `@require_owner` where the state cannot occur, so each would write a branch saying so: one refusal across 40 files, a fence whether spelled as a comment or as an `if`. **Rejected: a second soft door** -- its only job would be keeping `/grid` and the account detail page off the refusal, and neither preserves anything real: that card names TWO states in one sentence (`balance:X-x4`'s subject) and the grid's safety was ORDERING, conceded in its own test class name. **Rejected: the `ScheduleFacts`-only half**, already refused at `R-PC44`. **Cost**: those two pages answer the recovery card for a row-less owner, and two `cadence_days is not None` guards are DELETED rather than satisfied. Unreachable in production. Detail at `C4-d`. |
| balance | R-IW | -- | 2026-09-02 (developer) | **A ROW'S AMOUNT OWNERSHIP IS ONE ATTRIBUTE OF A TYPE THAT CANNOT HOLD ANY OTHER SHAPE.** `X-au-k`'s gate fork, REAFFIRMED the same day after an adversarial review refuted part of the argument for it. The figure column and `amount_source_id` become ONE `composite()` over a frozen `AmountOwnership`, columns double-underscored, read-only `hybrid_property` shims keeping every read and query expression unchanged. **The type is TOTAL over the two legal states** -- it refuses a figure beside a relation AND the empty pair -- and "no ownership stated" is `None` on the attribute, absorbed by a `from_columns` factory mapped as the `composite_class`. That factory is load-bearing: SQLAlchemy builds a composite from raw column values in `get_history` and `Session.is_modified`, so mapping the validating class directly raises from a path no caller enters. **A first draft weakened the TYPE to survive that and called the split forced; the review measured it a CHOICE.** Rejected: private columns behind read-only properties (the same call sites, measured); a `@validates` that CLEARS the partner (a machine splat then silently un-derives a row); **a `@validates` that REFUSES, which was NOT put at the gate and is the review's central charge** -- zero call sites and N-437 would not exist, but the pair stays two columns, the per-table figure registry stays, and every legitimate transition becomes an ORDERED two-statement write; and deleting `amount_source_id`, whose SET-NULL ground was refuted (`ck_transfers_adhoc_owns_amount` already ships it), leaving **N-264** alone as an OPEN question this ruling does not settle |
| balance | R-IY | -- | 2026-09-02 (developer) | **A DERIVABLE COLUMN IS DELETED, NOT MAINTAINED, AND `amount_source_id` IS THE FIRST TO GO.** Where a column's value can be computed from other stored facts the column goes and the computation stays: a stored copy of a derivable fact is a stale cache with no reconciler, *"which is what started this cash balance arc"*. **R-FI** rules exactly this for AMOUNTS; this generalises it to every column. **R-JA** is the rule this and **R-IZ** are the two halves of. **Applied**: `amount_source_id` is a stored discriminator over an EXCLUSIVE ARC whose legs the row already carries and `ck_transactions_one_pricing_link` already makes exclusive, so it is DELETED -- and **N-264 is answered by its SECOND reading**, CC4c's finance charge carrying its OWN pricing link, which fits that CHECK unchanged, rather than justifying the column's survival. Deletion is the END STATE; the migration, the resolver re-point and the CC4c link are steps behind it. **THE BOUNDARY, because a principle with none is cited against everything**: a CONSTITUTIVE fact is not a derivation. Opening equity (**R-GX**), an asserted balance and `pay_schedule.history_opens_on` (**R-IA**) are stored because nothing else can compute them -- they are INPUTS, and this rule bites on COPIES. **IT COLLIDES WITH A RANKED STEP AND THAT IS NOT RESOLVED HERE**: `X-bi-1` stores `tracks_purchases`, which DERIVES -- the property defers to the TEMPLATE's `is_envelope` for a generated row -- so a template edit stales every stored row. Its specification says *backfilled*, which is a cache and not a freeze. Flagged for the developer, not decided |
| balance | R-IZ | -- | 2026-09-02 (developer) | **EVERY DERIVED VALUE HAS EXACTLY ONE PRODUCER: ONE WALK, ONE ANSWER THAT CAN NEVER DISAGREE WITH ITSELF.** Stated for ANY derivation, not only one replacing a deleted column. It is the DERIVED half of **R-JA**; **R-IY** is the crossing case, not the parent. A second walk is a CACHE WITH NO COLUMN: nothing reconciles the two, and the cost is not the wasted pass but that they can PART. **Agreement is not the test** -- two spellings that agree today are still two spellings, and `X-au-g-2c-3c` is why that is not pedantry: a fifth spelling of `(b*r)/12` against `b*(r/12)` agreed over 200,000 randomised draws and was REFUTED at 500,000, parting at `$565.37` against `$565.36`. **Where a layer puts the shared leaf out of reach, MOVE THE LEAF** -- `X-au-g-2c-3a` deleted three restatements that placement had FORCED, because two walks sat BELOW the allocation and reaching it was an import cycle; the placement is the root cause and the duplication is its symptom -- and the MOVE has to be pinned or it reverts: `tests/test_services/test_loan_allocation_is_one_rule.py` asserts the moved leaf reaches NOTHING in `app.services`, which is the property that keeps it callable from every walk. **Five OPEN findings are instances**, which is what makes this a rule rather than a preference: **N-409** (an escrow floor that is a SECOND allocation rule for a question the fold already answers, and the two DISAGREE -- the dangerous shape), **N-56**, **N-14**, **N-115** and **N-121** (the same view, schedule, loader set and total each computed twice per pass). It generalises the resolve-once-and-thread rule already ruled for a single request |
| balance | R-JA | -- | 2026-09-02 (developer) | **A VALUE HAS EXACTLY ONE SOURCE OF TRUTH, AND WHICH KIND IT IS DECIDES WHAT THAT MEANS.** *"If the value is a derivation then one walk; if it is a stored value then it is only stored in one place."* **R-IZ** (one walk) and **R-IY** (a derivable column is deleted) are its two halves. The crossing case is what started this arc: a value that is DERIVED and also STORED has two sources -- the walk and the copy -- and the copy is a stale cache with no reconciler. **THE STORED ARM IS NOT HYPOTHETICAL AND THIS ARC IS ALREADY PAYING IT OFF.** Transfer Invariant 3 -- *shadow amounts, statuses and periods always equal the parent transfer's* -- is a maintained DUPLICATE written into `CLAUDE.md` as an invariant; `X-au-g-2c-2` made the shadow half structural (0 of 248 live shadows store a figure) and `X-au-f` takes the parent half. `X-bi-5` deletes `is_envelope`, the same fact stored on a row and on its template. `X-bj` (**R-IS**) unifies the two balance records into one evidence-ranked relation. **THE TELL IS AN INVARIANT**: where a rule says two places must ALWAYS AGREE, they are one value with two homes and a maintenance contract, and the remedy is to delete a home rather than to keep them in step. An invariant that cannot be violated because there is nothing to violate is worth more than one a reconciler enforces |
| balance | R-JB | -- | 2026-09-02 (developer) | **A SETTLED ROW'S PLAN IS DERIVED LIKE ANY OTHER, so a per-kind cutover's population is every NON-OVERRIDE row of its kind and not the projected ones alone.** Asked at `X-au-d` and ruled for the class. **The ground is R-JA's stored arm**: the figure a row was PLANNED at when its money moved is already stored once, in `settled_amount` on the `derived` settlement basis -- which is what that basis MEANS (`X-au-c3`: *the app's resolution at the moment of settle is a point in time*) -- so keeping `estimated_amount` beside it is one fact in two columns. Measured on the 2026-09-02 production clone: all 8 settled non-override salary rows hold the two figures EQUAL TO THE CENT, so the cutover deletes a copy rather than a fact. **THE REJECTED ALTERNATIVE IS RECORDED WITH THE DEFECT IT WOULD HAVE REINTRODUCED**: declaring only the PROJECTED rows needs a writer at the settle AND a second at the revert, and without the second a paycheck reverted in order to be edited keeps a frozen figure nothing ever recomputes -- **N-224 reborn on the revert path**, with the status back inside a pricing rule **N-262** took it out of. **What it costs**: `$0.00` through every balance (measured row by row over all 63 salary rows -- a settled row is worth what it RECORDED) and `+$68.67` of DISPLAYED estimate on 7, in the grid's `(est: ...)` caption and the edit popover alone. **That drift is not the cutover's and has more than one source** -- `+$29.09` from an undated calibration (**N-441**) against `-$19.28` unattributed (**N-442**) -- which is itself the ground: a figure that cannot be reproduced is a RECORD |
| balance | R-JC | -- | 2026-09-02 (developer) | **A PER-KIND CUTOVER'S DOWNGRADE RESTORES A SETTLED ROW EXACTLY FROM ITS RECORD AND EVERY OTHER ROW FROM ITS DEFINITION'S SCALAR.** Stated for the CLASS -- `X-au-d`, `X-au-e`, `X-au-g` -- so the three cite it rather than each wording it, which is R-JA applied to the rulings themselves. The problem it answers: a migration may not import `app` (that is what makes a schema change depend on code which may not exist at the revision being replayed), and a paycheck or a P&I split is a COMPUTATION, so the figure the upgrade emptied cannot be reproduced by the downgrade that has to put one back. Two arms: a row whose settlement basis is `derived` restores from `settled_amount`, which IS the plan the upgrade emptied and is therefore exact; every other row restores from `template.default_amount`, which is the app's own documented fallback for exactly this quantity -- generation wrote that scalar whenever its pricing engine could not answer. **The second arm is VALUE-lossy and BEHAVIOUR-lossless, and the difference is the claim worth making**: the code a downgrade returns to recomputes such a row at read time and publishes the recomputation rather than the column, on every surface, so the restored scalar is a placeholder the next `regenerate_for_template` rewrites exactly. **REFUSING the downgrade instead was rejected**: the only readers of a SETTLED row's plan are the grid's `(est: ...)` caption and the edit popover's Estimated box -- every money reader answers from the settlement record -- so blocking a rollback over a display caption is worse than taking it. The migration NAMES the rows whose plan-at-settle it cannot restore instead of leaving them to be discovered (0 on production at `X-au-d`) |
| balance | R-JD | -- | 2026-09-03 (developer) | **A PER-KIND CUTOVER DELETES THE FIGURE OUT OF A DECISION, NOT THE DECISION.** Asked at `X-au-e`, whose specification had the conflict chooser's keep-vs-use decision DELETED on the ground that a hand-edited month owns its figure, so the collision the chooser mediates cannot occur. Both halves of that ground are true and neither reaches the OFFER. **What refuted the deletion is a census, not an opinion**: `recurrence_engine._conflicts:170`'s `txn.is_override = False` is the only writer in `app/` that clears that flag on a TEMPLATE-LINKED transaction, and likewise its only per-row un-delete outside archiving the whole template. **The qualifier is the ruling's own correction**: a grep for the literal `is_override = False` found six writers; an AST census found NINE, because `transfer_service._update` and `._restore` clear the flag on a shadow through a variable. Those reach shadows only and `ck_transactions_one_pricing_link` makes `transfer_id` exclusive with `template_id`, so the conclusion holds by the SCHEMA rather than by the count -- which is the stronger ground, and is not where the first draft rested it. Deleting it would have stranded every overridden row permanently OWN with no route back to its definition until `X-au-h` reworks the flag: **40 such rows on the 2026-09-03 production clone**. So "use" survives and means *hand this row back to its definition*: it clears both flags and calls `declare_derived(TEMPLATE)`, writing no amount at all, which is what closes **N-244** (the old arm wrote the template's CURRENT `default_amount` onto a row whose due date could precede the edit). **The per-kind half**: a generated TRANSFER still stores its amount until `X-au-f`, so `RecurrenceConflictKind.use_states_a_figure` asks the KIND rather than assuming both, and that field, its branch in `apply_conflict_decisions` and the chooser page's figure arm are all deleted together at `X-au-f`. |
| balance | R-JE | -- | 2026-09-03 (developer) | **A SOFT-DELETED SETTLED ROW IS PAYMENT HISTORY FOR THE PURPOSE OF REFUSING A HARD DELETE.** The question ledger row **N-444** was filed with, and the answer closes it. `archive_helpers.template_has_paid_history` filtered `is_deleted = False` while `routes/templates/crud.hard_delete_template`'s bulk delete did not, so such a row was invisible to the gate AND excluded from the delete: the template went, `fk_transactions_template` is ON DELETE SET NULL, and the row was left carrying its money and no link to the definition that made it. Harmless while such a row OWNED its figure. `X-au-e` declares every non-override template row DERIVED, at which point the same survivor carries `amount_source_id = template` with no template to read -- unpriceable by `_stated_amount` on any revert to Projected, and unrestorable by the cutover's own downgrade, whose restore joins on the `template_id` that is now NULL (**N-440**). **The remedy is the GATE and not a CHECK**: ruling **R-IY** already condemns `amount_source_id`, so a constraint pairing it with the link would guard a column that is going, and `X-au-l` sits at rank #83 behind `credit_card:CC4d`. Dropping the filter makes the two halves of one door ask the same question, so the state is unrepresentable through it rather than guarded after it. **What it costs, taken deliberately**: a template whose only settled rows are soft-deleted can no longer be permanently deleted and is archived instead. ZERO such rows exist on the 2026-09-03 production clone, so nothing deletable today stops being so. The transfer twin `transfer_template_has_paid_history` KEEPS its filter and says why: a generated transfer still stores its own amount, so its survivor is priced by rule 1 off the column it holds -- `X-au-f` is the step that owes it this same edit. |
| recurrence | R-R50 | -- | 2026-09-02 (developer), AMENDED 2026-09-03 | **What a definition paying a FINISHED loan says: it says it is OVER and names no date -- and the PREMISE that forced that answer was refuted the same night, so this stands as the record of a question rather than as guidance.** As ruled: a retired loan has no forward crossing, `loan_payoff_date` answers `None`, and `recurrence_end_date` substitutes the READ PASS's own now -- so spelling it `ClosesOn` states a fact about when the page was loaded as a fact about the loan. Measured on a production clone, Van Loan trued to `$0.00`: the resolver answered `2026-09-02` today, `2026-09-03` tomorrow, `2026-12-25` at Christmas, and once `R7d-g` NULLs the column nothing pins it. The remedy taken was a fourth `DerivedStop` shape, `AlreadyOver`, whose `admits` was byte-for-byte `ClosesOn(as_of)`'s. **What was wrong was the premise I supplied -- that the app cannot name the day a retired loan closed.** It can, today: `walk_loan_ledger` -> `dated_deltas` already produces the dated recorded balance and `balance:X-au-g-2c-3b-2` (`3b7716f8`) put both tiers on `replay_loan_events`, so the backward crossing is `plan_payoff_date`'s forward rule read the other way, and the same clone answers **`2026-09-01`** over nine dated movements. The shape was an epicycle around a producer nobody had written, and it PRESERVED a defect it did not cause: a retired loan's stop tracked `ctx.as_of`, so the admitted set GREW one occurrence per cadence period where the stored column froze. **Superseded by `recurrence:R7d-h`** (developer, 2026-09-03: build the producer BEFORE the display step rather than ship the shape and delete it later). `AlreadyOver` was removed at `ea61116d`, recoverable at `bbf90b29`. Kept rather than deleted -- a deleted row orphans every citation. Closed by R7d-h (`83dd4b8a`) |
| recurrence | R-R51 | -- | 2026-09-03 (developer) | **A loan that closed, was trued back UP, and closed again has TWO zero-crossings, and its closing date is the LATER one -- the day it LAST became closed.** Asked while building `recurrence:R7d-h`, whose specification proposed the rule and marked it `unconfirmed`. The worked case, measured on the shape `insert_trueup_event` builds: a loan clears 2026-09-01, an operator trues it back up to `$1,200.00` on 2026-10-15, and a second true-up clears it again on 2026-11-20, read as of 2026-12-25. Taking the FIRST crossing reports the loan finished on 2026-09-01 and stops its recurrence there, so the two months it genuinely owed `$1,200.00` admit no occurrences and a payment settling in that span has no projected row behind it -- silent UNDER-generation across the reopened span, which is the direction `lessons.md` records as worse than the duplicate it would be avoiding. The later crossing is also what the phrase means literally, and it is what the backward scan already computes: `_fold.last_closed_on` overwrites its answer at every open-to-closed transition, so the last wins by construction rather than by a filter. A re-opening is REPRESENTABLE because `loan_ledger.dated_deltas` books an anchor as `anchor_balance - owed_before`, so a true-up from `$0.00` upward is a positive step. The rule applies only where the loan is closed AT `as_of`; a loan still owing takes the forward crossing and never reaches this question. |
| pay_calendar | R-PC46 | -- | 2026-09-03 (developer) | **A ROW'S OWNER IS A COLUMN; A SUBMITTED PAYCHECK'S OWNER IS THE CALENDAR'S ANSWER.** How `C13-b` retires finding **P75**'s nineteen, which are not one question: ELEVEN walk `X.pay_period.user_id` on a row that EXISTS, EIGHT refetch `budget.pay_periods` by an id a USER SUBMITTED. **`C13-a`'s key answers only the first** -- it decides what may be STORED, and deleting an input check does not refuse the request, it changes the ANSWER (measured: `routes/transactions/create.py` catches the `IntegrityError` and returns `400 "Invalid reference..."` where the response rule asks for the 404 "not found" gets). **CHOSEN:** the eleven become `txn.user_id`; the eight become `calendar_for(owner).period_by_id(id)`, where a foreign id is ABSENT rather than present-and-rejected, so no comparison is left to drop -- the shape `C2-f3e` ruled for the READ doors, extended to the WRITE doors. **And ONE walk**: the four route-boundary duplicates of `transfer_service._ownership._get_owned_period` are DELETED, reversing commit C-27 / F-043's defence in depth. **Rejected: delete all eight** (on `budget.transfers` it refuses nothing until **P83** is ruled); **keep all eight** (eight producers); **collapse the duplicates and keep the comparison** (fewer copies of a second spelling is not one spelling). **The period-set SCOPES are untouched and that is part of the ruling**: they are not P75's, and `Transaction.user_id` cannot replace them because each is also what makes its span lookup TOTAL. Detail at `C13-b`. |
| balance | R-JF | -- | 2026-09-03 (developer) | **A CUTOVER'S CONTROL MUST BE ABLE TO FAIL BEFORE THE CUTOVER STARTS.** Ruling on **N-445**: `tests/manual/verify_amount_resolver.py`'s invariance control skipped every DERIVED row by construction (its loop continues on a `NULL` `estimated_amount`, and `ck_transactions_amount_ownership` makes that column `NULL` on every derived row), so the *409* and *934 derived rows invariant* two shipped as-built records quote measured nothing. The rebuilt control perturbs the SOURCE -- the resolved version's amount, the parent transfer's figure, the salary profile -- and asserts the derived rows move by exactly that while the OWN rows do not, the disjoint pair the old harness lacked; and it is its OWN step, `X-bl`, ranked before and named in the blockers of `X-au-f`, the next per-kind cutover, rather than shipped inside it. `X-bl` also takes **N-432**, the loan parallel-run oracle whose reference is assembled by 91 production modules: the same class, a control that cannot fail for the population it grades. `X-au-d`'s as-built takes the correction `X-au-e`'s already carries. |
| balance | R-JM | -- | 2026-09-03 (developer) | **A TRANSFER LEG READS ITS PARENT, AND THE PARENT IS WHERE OWNER-OR-CONTRACT IS DECIDED.** Ruling on `X-au-m`'s open question. An owner-priced pair wrote one figure on the parent AND each leg because a derive-mode loan payment's leg read the loan's INSTALLMENT directly, so handing the leg back to derivation reverted an owner's `$1,325.00` to the contract's `$1,499.10`, twice. One chain instead: the contract or the owner's figure prices the PARENT (**R-IO**: the figure the owner types always wins and reaches the loan's split as the cash), and a leg derives from the parent it already names. A typed figure on a derive-mode payment is therefore the pair's OWNED amount at the parent -- the composite `X-au-k` built (**R-IW**) -- so *derive-mode with a typed figure* is not a representable state and the exception dissolves. Worked: owner types `$1,325.00`, parent `$1,325.00`, both legs `$1,325.00`; nothing typed, parent reads `$1,499.10`, both legs `$1,499.10`. Rejected: the leg reading the installment directly, which keeps the two homes the step exists to remove. |
| balance | R-BAL10 | -- | 2026-09-09 (developer, DESIGNED FROM SCRATCH rather than picked) | **A RECURRING LOAN PAYMENT'S AMOUNT IS ONE VALUE WITH ONE PRODUCER, AND IT LIVES ON THE PARENT TRANSFER.** Ruled on `X-au-f`'s open question after the developer REFUSED its three-way pick and asked what the design would be from scratch. The occurrence is ONE economic event held as three rows only because Transfer Invariant 5 makes the fold read `budget.transactions`; the two legs are its PROJECTION, so the value's home is the parent and each leg reads it. The producer answers THE CASH THAT LEAVES THE BANK -- contractual P&I plus escrow on the installment's own due date (derive mode) or the definition's dated series price (manual), plus the standing `extra_principal` -- in ONE expression with ONE `round_money` boundary, where today that sum is spelled twice in two modules. WHICH mode applies stays a live read off `template.settings` (**R-FK**), so the parent declares only `TEMPLATE` and no ref member is minted. Consequence: amount rule 4 stops being a SHADOW rule and becomes a TRANSFER rule, `_rule_within_parent_transfer` is DELETED, and a shadow's answer is *= my parent* unconditionally, which is **R-JM**'s one chain. Worked (P&I `$1,400.00`, escrow `$110.95`, extra `$100.00`): parent and both legs `$1,610.95`, against today's stale `$1,910.95` on the parent. Rejected: the base on the parent with the extra at the leg, which after `X-au-m` gives an owner who types `$1,325.00` legs worth `$1,425.00` (manual) or `$1,610.95` (derive) -- contradicting **R-IO** and re-creating the `$174.10` state R-JM makes unrepresentable; the amount on the LEGS, a value living on its own projection with two candidates and no rule; a per-occurrence series entry, which prices everything after its date rather than one occurrence; and excluding loan payments from the cutover, which leaves **N-450** open and 500s the transfers page one *auto-track* click later (**N-263**'s 47 rows) |
| balance | R-BAL11 | -- | 2026-09-09 (developer) | **A TRANSFER WRITE STATES THE ROW'S AMOUNT OWNERSHIP, NOT A FIGURE PLUS TWO FLAGS.** `update_transfer` takes ONE `AmountOwnership` -- the composite `X-au-k` built (**R-IW**) -- so *the owner's, and here is the figure* and *its definition's* are one parameter, and ABSENT means this save says nothing about the amount. `_amount.apply_amount_ownership`'s `stated_amount`, `amount_authored` and `stated_override` all dissolve into it, and the PARENT gains the three answers only its legs had. Ruled at `X-au-f` after a trace measured the step's own claim -- that `stated_override` dissolves once `transfers.amount` empties -- FALSE as written: on its own that parameter becomes the ONLY spelling of the conflict resolver's hand-back, so what dissolves it is stating the ownership rather than emptying the column. Rejected: extending the three existing parameters to the parent, which keeps two spellings of one question; and expressing the hand-back as *an unauthored figure*, which needs a sentinel to tell it from *this save says nothing* -- the distinction that cost `$174.10` twice |
| balance | R-BAL12 | -- | 2026-09-09 (developer) | **`EVT_TRANSFER_AMOUNT_FROZEN` IS DELETED, NOT RE-POINTED.** Its predicate `booked != rows.transfer.amount` (`transfer_service/_settle.py`) is finding **N-451**: `X-au-f` empties that column, so it fires on EVERY settle including a plain checking-to-savings transfer. Re-pointing it at the RESOLVED figure makes it fire on NONE -- under **R-BAL10** the booked figure IS what the transfer states, so the two sides share one producer and the comparison is an identity. An event that cannot fire is a fence the design made unnecessary, so it goes with its predicate, its `log_events` registration and its integration test rather than being kept as a green check that measures nothing. Closes **N-451** by dissolution |
| balance | R-BAL14 | -- | 2026-09-10 (developer) | **THE PARENT-TRANSFER CUTOVER IS ONE LEAF, NOT TWO.** `X-au-f-3` is ABSORBED into `X-au-f-2` and never ships under its own name. Giving the parent its producer and emptying its column are two halves of ONE act -- moving a value's home -- and any boundary between them leaves a commit in which every reader asks the new home while the old one is still full. **Measured, not argued**: 27 suite cases across three integration files fail in that window, a derive-mode payment freezing at its creation-time P&I + escrow (`$1,499.10` against a re-escrowed `$1,599.10`) and a manual one silently dropping its standing extra (`$1,300.00` against `$1,450.00`). `$0.00` exposed on production, where `budget.loan_payment_settings` is empty, and ONE *auto-track* click from live -- `track_payment` flips the settings row and never touches `amount_source_id`. **It SUPERSEDES nothing: R-BAL10 stands untouched.** The DESIGN was right and the BOUNDARY was wrong, which are different errors with different remedies, and the question was put as *is the from-scratch design wrong, or are you re-cutting the leaves* -- the answer being the second, in service of the first. Rejected: shipping `X-au-f-2` alone and ruling the window acceptable, which also rules that 27 financial assertions be retargeted to the stale figure; and a narrower re-split covering only loan payments, which turned out to need every writer change the full cutover needs and to narrow only which ROWS the migration touches |
| balance | R-BAL15 | -- | 2026-09-11 (developer) | **"THE BANK MOVED `$0.00`" IS A LEGAL OBSERVATION.** `transactions.settled_amount` and `transfers.settled_amount` keep their INCLUSIVE minimum; Projected is not the only way to say the money did not move, and reverting is how you say it never was expected to. **The contradiction `N-301` reports DISSOLVES rather than resolving**: an `amount` is a PLAN and a `settled_amount` is an OBSERVATION, so a create door refusing a `$0.00` plan and a settle door accepting a `$0.00` observation are answering different questions, not the same question two ways. What `X-cd` owes is therefore the SEAM -- each bound states which question it answers -- and not a bound change. |
| balance | R-BAL16 | -- | 2026-09-11 (developer) | **ONE STEP DELETES BOTH `default_amount` COLUMNS.** `X-bp` covers `transaction_templates` and `transfer_templates` together, closing `N-446` and `N-450`: each is a stored figure beside an `amount_versions` series that already exists, so they are one design executed twice and splitting them means taking the decision again with one column live in between. **The two kinds `template_amount_service.owns_its_amount` REFUSES a series -- a salary-linked transaction template and a DERIVE-mode loan payment -- are in scope and take a different remedy**: their column is a snapshot of a live computation rather than a copy of a series, so it is deleted with no successor to read, the way `X-au-f-2` deleted the instance column. |
| balance | R-BAL17 | -- | 2026-09-11 (developer) | **A ROW OF A DEFINITION HAS ONE CONSTRUCTOR, AND THE SCHEMA SAYS WHAT IT IS.** Rather than hand-date 68 test sites so `X-bv-2`'s CHECK could bind, the suite gets ONE builder that obtains the row by CALLING the recurrence engine into one paycheck (`tests._test_helpers.generate_row_of`); a fixture wanting the OWNER's row takes ownership the way the re-price door does (`state_own_amount` + `is_override`), and a settled one settles the engine's row. The CHECK then binds on BOTH row tables (`budget.transactions` and its transfers twin); `_stated_amount`'s `on_date is None` arm and its test are DELETED rather than re-targeted; the two tests grading frozen migrations' `due_date IS NULL` arms are deleted, the CHECK's DDL being their successor; and `DerivedRowFields.due_date` tightens to `date`. Filed as the `X-cf` container and its leaves, ranked ahead of `X-bv-2`. |
| balance | R-JN | -- | 2026-09-03 (developer); clause (3) REVISED 2026-09-16 BY R-BAL53 | **THE LEVEL RELATION LANDS NOW AND ITS NEVER-MOVES-A-BALANCE CLAUSE LANDS WITH THE FLIP; `X-bj` SPLITS, SUBSUMES `bank_import:X-gh`, AND DEFINES COVERAGE.** Three rulings on one step. (1) As written, `X-bj` at rank #10 claimed **R-IS**'s *a level never moves a balance* eight ranks before `X-f3c-5` stops an assertion resetting a plain account, and building the relation beside the old reset would hold one observation in two homes for the interval. So `X-bj-1` builds the ONE evidence-ranked relation now -- the owner's true-ups and the bank's per-import closings move in, a release becomes an APPENDED superseding row (**R-HY**, **R-IC**), and the existing reset READS that relation until the flip deletes it -- and `X-bj-2` rides `X-f3c-5` with the clause and the `discrepancy = observed - computed` figure. (2) `bank_import:X-gh` (**R-GL**, *a confirmed import's anchor may ASSERT the opening*) is WITHDRAWN as superseded: under R-IS the bank's closing is an OBSERVATION, neither an assertion that resets nor an opening-equity restatement, which answers **N-314**'s question and closes it; **N-470** re-homes to `X-gi`'s census and **N-434**'s column half to `X-bj-1`, its parser half to `X-f6b`. (3) *Statement coverage* is defined ONCE, by the relation: a level row carries its source's span, and a day is covered when a statement's span reaches it -- which is the specification pass `bank_import:X-f6g` owed, so that pass folds into `X-bj-1`, **N-343** (choose within the RUN) re-keys to `balance` under it, and `X-f6g` keeps only the panel change. **Clause (3) REVISED by R-BAL53**: coverage is the STATEMENT's span (`covered_runs`, unchanged), a level reaches its span through its import, and N-434's column half rides `bank_import:X-f6b` with its parser (R-BAL55). |
| balance | R-JO | -- | 2026-09-03 (developer); premise corrected 2026-09-04 | **N-214 CLOSES: THE SHARED RATE-LIMIT BUCKET IT DESCRIBED DOES NOT EXIST.** Ruled acceptable by the developer on 2026-09-03 and found MOOT on review: Flask-Limiter's default limits are scoped per ENDPOINT and no application-wide limit is configured, so `accounts.anchor_difference` and `accounts.true_up` never drew from one per-IP bucket -- measured 2026-08-14, route A exhausted at hit 31 while route B answered 200. The row's premise was false and its remedy question (a per-route ceiling, an exemption, or acceptance) had nothing to decide. The query-string exposure the row also recorded is mitigated (nginx logs `$uri` only) and stays in the sweep record so it is not rediscovered. |
| balance | R-JP | -- | 2026-09-03 (developer); premise corrected 2026-09-04 | **ONE PRODUCER OF A PROPERTY'S VALUE: THE CHART TAKES THE SEAM'S FIGURE, COMPOUNDED FROM ITS ASSERTION; THE INCLUSIVE CONVENTION IS R-L'S AND STANDS.** Ruling on **P41**, re-keyed from `pay_calendar` to `balance` under `X-j`, and the row's own DIAGNOSIS was refuted when the ruling was reviewed against the 2026-08-14 measurement: the seam accrues from the latest assertion's `observed_on` INCLUSIVE (**R-L**, `balance_at/_asset_fold`) and the chart's `span_return_rate` counts the same inclusive span, so with an assertion dated today the two agree to the cent (`$401,377.78` both). The defect is the SEED, not the convention: `property_equity_chart` flat-carries the latest asserted `market_value` to today, so when `observed_on` precedes today it never compounds `observed_on..today` while the seam does -- `$2,630.49` at 60 days on that measurement. Dropping the `+ 1`, as the row proposed, would have put the chart one day BELOW the seam on a same-day assertion and left the seed untouched. The chart consumes the seam's value at today, which is `X-j`'s subject: which producer answers *what is this account worth* for a surface. It moves a figure on the property detail screen and says so. |
| balance | R-JQ | -- | 2026-09-03 (developer) | **`tracks_purchases` IS DERIVED THROUGH ONE ACCESSOR AND NEVER STORED; `X-bi-1` IS RE-SPECIFIED.** The step as written made the envelope predicate a STORED column *backfilled from each row's template* -- a value that DERIVES from the template's flag, so a template edit would stale every stored row, which is exactly the class **R-IY** deletes and the README flagged as unasked on 2026-09-02. Ruled: no column; the predicate derives from the template's flag through ONE accessor (**R-IZ**), every `transactions.is_envelope` reader moves onto it, and `X-bi-5` still deletes `is_envelope` and the branch sites. `docs/design/from_scratch_architecture.md`'s re-cut carried the stored design and is corrected with the step. |
| balance | R-JR | -- | 2026-09-03, second sitting 2026-09-04 (developer) | **`is_override` SAYS ONE THING -- *this row is the OWNER's, not the rule's* -- AND WHETHER A HUMAN AUTHORED A FIGURE IS A FACT THE PAYLOAD CARRIES.** Ruled over the plan's four-fact framing after a census of all 22 live readers: *survives the regeneration sweep* and *exempt from the unique index* are ONE fact with three readers, *the user re-priced it* already has a home in `amount_source_id` (**R-FI**), and *the user moved it* is an ACT rather than a stored fact. So every form rendering a money box posts the figure it RENDERED, and the door asks whether what came back differs from what the person was shown -- never reading a stored column, so emptying one at `X-au-f` cannot make it vacuously true (**N-436**, **N-448**), and never inferring authorship from a flag that also records a move (**N-248**). **SECOND SITTING, after a neutral adversarial review falsified BOTH premises of the first.** (a) A figure arriving with NO companion is REFUSED as malformed, not guessed at: the first ruling assumed it authored, on the argument that a wrong take is undone by the conflict resolver -- but `is_override` is in no schema and the chooser is SUPPRESSED for salary-linked templates, so N-248's own population has no in-app hand-back, while a discarded figure re-renders in the row's cell immediately. Refusing deletes the question instead of reversing it, and makes a door that forgets the companion fail loudly on its first save. (b) `is_override = FALSE` is DROPPED from the OCCURRENCE-keyed unique index on both row tables and KEPT on the paycheck-keyed undated one: the census that said those were one fact was wrong, and since the flag now rises on a RE-PRICE, keeping the exemption would drop a row that never moved out of a guarantee it never used to lose. Where it overlaps the transfer leg, **R-JM** governs and is cited rather than restated. |
| balance | R-JT | -- | 2026-09-04 (developer) | **AN ARCHIVED ACCOUNT'S CARD LINKS THE ACCOUNT EDIT PAGE PLAIN -- UNGATED, AND CARRYING NO `#books-opening` FRAGMENT.** Taken on `balance:X-f3c-2b-2d` after the option it replaces had been BUILT and was green, so it is recorded as an overturned implementation rather than a fork chosen on paper. Both options resolve to the same URL. The rejected one linked *Restate opening* at `/accounts/<id>/edit#books-opening`, and the fragment names an anchor a loan's edit page does not render -- so it needed a gate, and the gate needed a shared predicate (`opening_service.restatable_opening`), a new `ArchivedAccount.offers_restatement` field and a per-archived-row query. **Its gate solved a problem the gate itself created**, which is the shape rule 1 calls a fence around a defect: the design doctrine's goal state is that a fence be structurally unnecessary, and deleting the fragment deletes the fence outright. It also RESTORES something the gated version took: an archived LOAN reaches the only page that can rename or re-type it, which the predicate refused for every amortizing account. **R-IE is not amended and deliberately is not**: the door still has TWO entrances, and this gives the FIRST of them a handle from a state that had none rather than adding a third. Rejected: the fragment plus its gate (the built option); the fragment ungated, which dead-ends an archived loan on a page with no anchor; and linking the account NAME to its detail page, which presumes an answer to what a detail page means for an archived account -- **N-384**'s question, owned elsewhere. |
| bank_import | R-JK | -- | 2026-09-03 (developer) | **THE RELEASE IS CUT AFTER `X-gj-4b` MERGES AND BEFORE `X-gi`.** A frozen `release/<date>` branch from a known-green `dev` SHA, rehearsed as PR #111 was -- two throwaway Postgres instances, the old and the new entrypoint, the money oracle -- then a PR to `main`; `X-gi` rides the next release. The ground: production was PR #111 of 2026-08-18 with `dev` 562 commits ahead on 2026-09-03, so *bank_import to production* means a release of everything on `dev`, and the operator acts the balance arc waits on (**N-368**'s import, the account-10 repair, `X-bk`) need the restatement door deployed first. |
| bank_import | R-JL | -- | 2026-09-03 (developer); SUPERSEDED 2026-09-15 | **SUPERSEDED for `X-f6b` by `R-BI8` (an outcome of the 2026-09-15 tiers in `steps.md`) and for `X-f6c` by `balance:R-BAL24` (2026-09-13, `balance:X-bi-7b`'s alias); `X-f6g` keeps its wait on `balance:X-bj-1` and its reward rank below the card rows.** As ruled: **AN ENHANCEMENT STEP RANKS BELOW THE CARD ARC.** Applies the 2026-09-02 ruling that every bank_import step after `X-gj-4b`, `X-gj-4c` and `X-gi` *carries no precedence*: `X-f6c`, `X-f6b` and the narrowed `X-f6g` move below `credit_card:CC5b`, the last card step, instead of sitting at ranks #23-#25 ahead of money-correctness rows such as `balance:X-au-g-2c-3b-3`. The priority is bank_import to production, then the card arc; an enhancement to the importer is neither. |
| bank_import | R-JW | -- | 2026-09-04 (developer) | **THE SKIPPED TAB TAKES R-GX'S SHAPE: THE PAGE'S OWN BOUND, PLUS A LINK PAST IT.** Ruling on the fork `X-gj-4c-2` opened. The tab first shipped UNBOUNDED, defended on the claim that the two settled tabs bound because they VALUE every act they render -- which **R-GX** and `REGISTER_LIMIT`'s own docstring contradict in as many words: the agreement test is a valuation and not a query, *so the fold reads every act either way, and what is bounded is what is RENDERED*. Bytes are the criterion. Measured 2026-09-04 as the marginal cost of one card on the real rendered page: **1,427 bytes** a skip against 216,637 / 221 = about **980** a settled act, so the tab with no bound cost ~1.46x per card of the one that had it, and this owner's 378 recorded lines would render ~556 KB against a 17,162-byte empty tab. **It is the page's ONE bound and not a constant of its own**: a first version declared a `SKIPPED_LIMIT` the page never read, because `reconcile_page` threads one `limit` from the route into every bounded arm POSITIONALLY, so its default was shadowed on every render and it governed nothing but its own tests -- flexibility nobody asked for, which rule 13 refuses. Rejected: leaving it unbounded on expected size, which is a judgment about a rate that nothing would tell us had gone wrong. **The undo carries `all=` back**, so a press while the bound is lifted does not collapse the record under the reader. Shipped at `X-gj-4c-2` (`56f97b98`). |
| bank_import | R-JX | -- | 2026-09-04 (developer) | **WHICH KIND OF CARD A TAB HOLDS IS STATED BY WHAT BUILT THE CARDS, AND IS CARRIED ON THE PAGE.** Ruling on the second fork `X-gj-4c-2` opened. A recorded SKIP is an act with no `StatementMatch` behind it and a bank line to print, so it answers `Tab.holds_settled_acts` with NEITHER value -- a third arm cannot be added to a boolean, and widening what `True` means is how a tab comes to render the wrong partial with nothing raising. `Tab.holds` over a `_TAB_CARD_KIND` table was ONE FACT IN TWO HOMES, in two modules, agreeing only because a test said so: set the table to `ACT` for the Skipped tab and the dispatch still built `SkipCard`s, the template still picked the act partial, and the page 500'd on a field that is not there. `_tab_sections` returns the `CardKind` BESIDE the sections it just built and `ReconcilePage.kind` carries it, so both tables and the reconciling test are DELETED (rule 14: the remedy for two homes is to delete one). **It buys LOCALITY, not impossibility** -- an arm can still pair the wrong literal with a builder, but no longer in a different FILE from the cards it describes; the docstrings say so, an earlier draft having claimed "unrepresentable", which is the word the design doctrine reserves for a defect that cannot be written. **On the PAGE and not on a `CardSection`**, which was the reviewer's remedy and does not work: an empty tab has no sections, and the template still needs the kind to decide whether the Apply form renders. Shipped at `X-gj-4c-2` (`56f97b98`). |
| bank_import | R-JY | -- | 2026-09-04 (developer) | **A BANK LINE'S DISPOSITION IS ONE ROW, AND A STANDING RULE STATES A DISPOSITION PLUS A SEPARATE REFUSAL.** The from-scratch act model replaces the two act stores -- `budget.statement_matches` with its members for MATCH/ADD, `budget.statement_line_skips` for SKIP, and nothing at all for TRANSFER -- with ONE row per bank line naming the VERB it ended on and WHO decided, the argument staying with the verb that has one. **R-HP**'s *exactly one verb per line* becomes a UNIQUE key rather than an invariant two doors maintain under a row lock, and `applied_by_rule` (**R-GT**) becomes ONE column over all four verbs -- so the rule receipt itemises every kind it files and no act of the app's renders as the owner's own. `budget.merchant_rules` stops deriving five answers from which column is non-null: it carries a nullable DISPOSITION (the verb plus its argument, SKIP taking none) beside a separate REFUSAL flag, so *never a purchase* leaves the answer set and **R-JH** stops being a rule a reader must remember. The screen, the rule and the store then teach ONE vocabulary, the four verbs. **Its SHAPE is a decomposed container inside THIS arc and not a new arc**: `X-gl`, leaves `X-gl-1..n`, ranked beside the card arc after the release **R-JK** requires -- TRANSFER is the verb the disposition exists to admit and the card arc is what makes TRANSFER real -- with `X-gj-4b` shipping first on the store as it stands. The arc document carries the container and its leaf specifications; the argument, the fences it deletes, the limit it does NOT reach (a group holding no app row needs an `AFTER DELETE` trigger, or the key is not earned) and the forks still owed a ruling are `docs/design/statement_disposition_model.md`. |
| bank_import | R-JZ | -- | 2026-09-04 (developer) | **A SKIP RULE SHIPS, AND A RULE-AUTHORED SKIP IS SHOWN SEPARATELY FROM THE OWNER'S OWN.** Fork 1 of `docs/design/statement_disposition_model.md`, ruled in the shape its section 6.1 works through, so that example stops being an illustration and becomes the specification. A merchant rule may state SKIP as its disposition (**R-JY**'s nullable disposition column, the verb taking no argument); the rows it files carry `applied_by_rule = true`, render under *Skipped by your rule* and never under *Skipped*, itemise on the import receipt with the one-click undo **R-GG** built, and are EXCLUDED from `RuleFiling.filed_total`, because a skip names no created subject and that total answers what the bank moved on the lines a rule FILED. **The case against was recorded and lost on its merits, and the remedy is what the ruling turns on**: a skip rule is the only standing answer whose effect is INVISIBLE -- an ADD rule creates a budget row the owner can see, a skip rule creates nothing but an absence from the inbox, while the books-versus-bank difference the hero reports does not move -- and the answer taken is to make that absence VISIBLE on a surface of its own rather than to refuse the rule. So this ruling is only honoured where the separate surface ships with the rule: a skip rule whose filings render as the owner's own presses is the harm, not the rule. **Owners**: `X-gl-3` carries the SKIP disposition and the `filed_total` exclusion, `X-gl-5` the *Skipped by your rule* surface. It does NOT touch **R-JI**, which shuts SKIP at the door for a line a source files as paying an account the owner holds: a rule may not reach what the door refuses. |
| bank_import | R-KA | -- | 2026-09-05 (developer) | **THE MATCH PANE RENDERS ITS CANDIDATE ROWS SERVER-SIDE, ONE CARD AT A TIME.** The `<noscript>` links to the same Reconcile page with `?open=<line_id>`, which the page reads beside its existing `?tab=` and `?all=`, and that ONE card renders its rows instead of a spinner; the live re-pricing stays scripted and the door re-derives the difference anyway. **The ground is that `X-gi` is N-404's own remedy and may not mint a fail-closed instance of the same class**: after the workbench goes, a parked card payment has NO reachable act with scripting off -- ADD barred, TRANSFER doorless, SKIP shut by **R-JI**, MATCH a fragment -- which is 9 lines and `$7,412.94` on the developer's own Checking, and 0 of 18 inbox cards carry a proposal whose hidden rows would let it be OK'd unopened. **Rendering EVERY card's rows was REJECTED on the measurement the template already carries**: 143,298 bytes of a 238,645-byte page for rows nobody had opened, which is **N-374** re-opened; the assistant first recommended it having quoted the row count (199 across 18 cards) without that byte figure, and corrected the recommendation when the template's own note was read. One card is at most 15 rows. Also rejected: `<noscript>`-only rendering, which ships the bytes to every browser and shows them to almost none; and stating the pane needs JavaScript, which leaves the hole |
| bank_import | R-KB | -- | 2026-09-05 (developer) | **THE GRID BADGE AND THE RECONCILE INBOX ARE ONE PRODUCER, AND IT IS ITS OWN STEP (`X-gm`), RANKED BEFORE `X-gi-1`.** Measured 2026-09-05 at migration head on the developer's Checking: `awaiting_review_count` reads **27**, the `TO_EXPLAIN` tab reads **18**, the Transfers tab holds the 9 parked card payments worth `$7,412.94`, the impossible-day term is 0, and 18 + 9 = 27 exactly. **They AGREE today** -- the badge opens the review queue, which renders all 27 -- so `X-gi-1`'s repoint is what would MINT the disagreement. `_reconcile.py` had already recorded which of the two a badge should render as "a decision, not a defect this step may take unilaterally". The remedy moves the parked and impossible-day split OFF the proposer into `_undisposed`, which **R-HQ** already implies -- a holding state is not a task, so nothing should propose a match for one -- leaving `to_explain` a pure SQL predicate the grid's hottest render can afford. **No interim reconciling sentence is built**: an interim guard must not teach what a later step un-teaches, which is why the order is `X-gm` first rather than a sentence `X-gm` would delete. Rejected: the badge counting the inbox (a third, cheap, approximate spelling of one value, rule 14's own failure mode); two numbers on the app's densest control; and deleting the badge, which its own docstring says is how the feature is FOUND |
| bank_import | R-KC | -- | 2026-09-05 (developer) | **THE WORKBENCH PAGE IS DELETED, THE MULTI-LINE AXIS IS KEPT, AND A CARD-SIDE DOOR FOR IT IS ITS OWN STEP (`X-gn`).** **R-HU** stands. The census's headline number is VOID and no step may cite it: 287 matches ever recorded, none naming more than one bank line, measures an import feature the developer had abandoned in August and has been rebuilding since -- "the workbench hasn't been used because the bank import functionality was so bad when I first tried it" -- so it is evidence about neither need nor disuse. **The decision therefore rests on design alone, and three facts stand**: a bank really does split one payment across two lines; `uq_statement_match_members_line` forbids expressing that as two matches, so nothing works around it; and the axis is REAL rather than vestigial -- `MatchSides.of` sums over lines, `MatchDays.posted_first` exists only for the multi-line case, `test_accept.py` covers it end to end, and **R-JY**'s act model keeps `group_id`. So only the DOOR is at stake, the gap between `X-gi-2` and `X-gn` is accepted, and deleting the axis was rejected because `X-gl` would reinstate it |
| bank_import | R-KD | -- | 2026-09-05 (developer) | **THE BADGE AND THE INBOX BECOME ONE Python WALK, NOT ONE SQL PREDICATE**, superseding **R-KB**'s remedy while keeping its measurement. A SQL mirror must re-derive `RuleAnswer` from `merchant_rules`' columns and `RuleAnswer` is deliberately NOT stored (`_rules.py:60`), so the mirror would add three SQL restatements of Python predicates to repair a defect CAUSED by two of them; the walk instead DELETES the two already in `awaiting_review_count` (the books-open `>` and the calendar `>=` mirrors) and the arm in `test_books_bound` that graded them together. Measured on the SHIPPED implementation, 2026-09-05 at head `b3f7c2a91d4e` on the dev-preclone restore, 378 lines / 157 undisposed, medians of 15: the old SQL count 1.21 ms returning 27, the walk 3.05 ms returning 18, a delta of 1.84 ms on the grid render. The 1.64 ms quoted for the SQL mirror is a PROTOTYPE's and nothing in `app/` computes it, so it cannot be re-measured by running the code. **The impossible-day line is not rehomed**: `_creatable_lines` stops dropping it, it stays in `creatable` carrying `CreatableLine.withheld`, `_scope.reject_impossible_days` raises at the door, and `ReviewBounds.impossible_day_count` is DELETED. **Second clause, the worse half**: on the pre-`X-gm` tree a crafted or stale submission for an impossible-day line RESOLVED ITS DESTINATION AND MINTED THE ENVELOPE before `create_entry` refused, so `_create`'s stated promise that every refusal fires before anything is written was FALSE for this class -- graded by MUTATION, not by reading. **Third clause**: a card payment can no longer be PROPOSED, which falsifies `ReviewSet.account_payments`' stated reason for existing; the wide set is KEPT as the DOOR's own set, so a later card builder shuts SKIP by asking the question `skip_line` asks rather than by remembering a list. |
| balance | R-BAL1 | -- | 2026-09-05 (developer) | **A LOCK IS THE INSTRUMENT FOR A CORRECTNESS HAZARD; A RESOURCE FACT GETS INFORMATION INSTEAD.** Asked at `X-br-4`, whose own steps.md sentence -- "a private cluster per run has no shared state to coordinate, so each of them guards nothing rather than guards less" -- is TRUE of the live-backend probe, `RESTART_TEST_DB`, `TEST_DB_PREFIX` and `TEST_TEMPLATE_DATABASE` and **FALSE of the suite slot**, whose own header named a second and independent hazard: *"CONTENTION is real and measured: 859s contended vs 304s alone... This one needs NO restart to bite."* A cluster per run makes mutual corruption unrepresentable and does not multiply the cores. **The old number could not settle the new question and was not allowed to**: 859/304 was taken on ONE postmaster where the cluster-wide `pg_database` catalog lock was the serialised resource, and that serialiser is gone, so it was re-measured -- 2026-09-05, 24-core host, 13,238 passed in 349 s alone against two of three concurrent suites reaching ~38% in 13 minutes at a run-queue of 32, ~950k context switches/sec and 28% iowait, **with ZERO failing tests in either arm** and a slowest single test of 2.58 s against `pytest.ini`'s 30 s timeout. That is what separates *the slot protected a wall clock* from *the slot protected a result*. So the LOCK goes and a NOTE replaces it: the wrapper prints any other live pytest with its worktree and proceeds. **The identity is the CWD, never the argv** -- every worktree here shares one venv, so a peer's command line names the main checkout whatever tree it is testing, and an argv-based probe misattributed a peer's run during this very session. As-built `6a3eb135`. |
| balance | R-BAL2 | -- | 2026-09-05 (developer) | **A DEFECT IS FIXED WHERE A STEP ARMS IT, NOT ONLY WHERE A LEDGER ROW OWNS IT.** Asked at `X-br-4`. `build_test_db_image.py`'s `key_inputs()` hashes the WHOLE `app` package deliberately, so any `app/**/*.py` edit moves the cache key and forces a rebuild on the next test run, and `build()` was the last container publishing a docker-ASSIGNED port -- measured at `X-br-3` as a 25-40% failure on the rootless daemon, because dockerd allocates inside the container's netns and rootlesskit binds that number on the HOST. **Opt-in that was a rare annoyance; as the DEFAULT path it is roughly one in three post-edit invocations exiting 2 with no tests run**, on every lane on this box. The class is `N-459` and belongs to `X-bs`, and waiting for its owner would have shipped a broken harness, so the arming step fixed the site it armed: the bake port is now caller-chosen from the PID inside 20000-32767, entirely below `ip_local_port_range`, which `X-br-3`'s own table already measured at 0/12 against the assigned form's 5/12. `N-459`'s remaining site, the deploy fixtures' `-p 0:443`, stays with `X-bs`. As-built `6a3eb135`. |
| bank_import | R-BI1 | -- | 2026-09-05 (developer) | **THE SCRIPTLESS MATCH PANE OFFERS EVERY UNEXPLAINED ROW, NOT THE LINE'S OWN PAY PERIOD**, superseding the row-set half of **R-KA** and keeping the rest of it whole. **R-KA's ground did not reach its own remedy**: it exists because a parked card payment has no reachable act with scripting off, and `_panel.py` measures (2026-08-30) that all 9 of the 9 card payments on the developer's Checking have payback rows their own period does NOT hold -- so a period-bounded pane strands the class the ruling is for and offers rows that cannot explain the line, WORSE than the workbench link it replaces. The SEARCH is what widens the period and it is a keystroke-triggered htmx post, so a render with no script cannot widen anything: the cap **N-374** already refused, one surface later. The set is the pass's own `unmatched_rows`, bounded to the recorded lines' span and exactly what `_statement_workbench_body.html` renders today, so the deleted door and its replacement offer the same rows. **Measured 2026-09-05 on the pane's own row markup: 570 bytes a row, ~760 with the not-shown-alone badge**, so 67 rows on ONE card is ~40 KB against the 143,298 R-KA rejected for every card's period rows, paid only on a request the owner made. **Second clause, which makes the first SAFE**: the pane renders every TICKED row THE PASS STILL OFFERS, whatever list it shows -- a checkbox not in the document is not submitted, so a browser WITH scripting landing on `?open=` would otherwise tick a row and lose it on the first re-price. It closes a live scripted defect too: ticking a payback found by one search then searching for the second dropped the first silently. A row the pass no longer offers renders nowhere; `resolve_rows` refuses that body BY NAME. **Rejected**: the literal R-KA reading, which leaves the hole; and a scriptless SEARCH submit, whose only plain-HTML forms serialise the page into a URL or press the MONEY door to search |
| bank_import | R-BI2 | -- | 2026-09-06 (developer) | **THE X-gi-2a FORK, RULED IN THREE PARTS, OF WHICH `X-gp` SUPERSEDES `R-IV`.** Building `X-gi-2a` as specified ARMS two defects it does not cause, and the developer refused both first framings -- *"these all seem like different ways of hiding the problem, what is the root cause solution"* and *"why is the ceiling 100 members, that seems like the real problem"* -- so what follows is the re-derived answer rather than an option he picked. **ROOT CAUSE 1: the consent witnesses a PROJECTION of the act, not the act.** The MATCH pane draws TWO controls for ONE decision -- `difference_on-<line>`, and a consent box whose value is a figure and whose LABEL is composed from that select at draw time -- and `_reject_unaccepted_difference` compares only the FIGURE. Worked: bank `-$793.23`, rows `-$93.23` and `-$650.00`, difference `-$50.00`. Leave the select alone and a NEW uncategorised `-$50.00` row is minted; name the `-$650.00` row and nothing is minted and that row is re-priced to `-$700.00`. Both bodies submit `residual = -50.00`. **R-IV** accepted that bound on the VERIFIED ground that the htmx swap of `.rec-match-picks` re-renders the box unticked -- a DOM event holding a money consent together -- and `X-gi-2a` is what makes the select render on the scriptless `?open=` pane, where nothing swaps. So `X-gp` replaces both controls with ONE whose options ARE the acts, each labelled with the figures it writes, its value carrying BOTH the landing and the figure, the door comparing it whole. That is the widening R-IV explicitly REJECTED, on a premise `X-gi-2a` falsifies. **ROOT CAUSE 2: `_MAX_MATCH_MEMBERS` was a resource fence wearing a domain rule's clothes**, its stated reason held already by `resolve_rows`/`load_lines` and by `MAX_FORM_MEMORY_SIZE`, and contradicting a pane that offers every unexplained row as a tickbox (**R-HW**); `X-go` deletes it. Rejected: every interim wording for the stale label, as hiding the defect. |
| bank_import | R-BI3 | -- | 2026-09-11 (developer) | **THE RE-DRAWN MATCH PANE ECHOES EVERY SUBMITTED CONTROL WHOSE VALUE IS STILL ON OFFER, THE CONSENT BY WHOLE-VALUE EQUALITY.** Ruled at `X-gi-2a`, which prices the `?open=` pane from the SUBMITTED form so a refused Apply comes back with the owner's ticks (**BI-478**). The fork: once the rows are echoed, does the *what to do with the difference* option the owner picked come back PICKED when the press was refused for an unrelated reason? YES, on the rule every well-behaved form follows -- a refused form comes back filled in exactly as submitted, for every control whose value is still a valid choice: a row renders ticked where the pass still offers it, and a consent option renders picked where its WHOLE value (`ReviewedDifference`: the figure AND the member's reviewed row token) equals the one submitted, so equal value is equal act and a pick clears exactly when the act consented to is no longer offered. Not **R-IU**'s pre-selection: the app picks nothing, it echoes the owner's own tick, and the value echoed is one the server spelled. One producer and one template serve both surfaces, so the scripted pane keeps a pick across a search keystroke and clears it on a row tick that moves the difference. Worked: rows `-$93.23` and `-$650.00` against `-$793.23`, the owner picks *write `-$700.00` to the `-$650.00` row*, the press fails on a sibling card's malformed item -- the radio comes back picked; re-tick the rows to a `-$30.00` difference and no option carries `-50.00@...`, so nothing is picked. Rejected: rows only (re-tick the radio after every refusal, for no safety gain); echo by FIGURE alone (the projection **R-BI2** deleted). The first framing was refused as id-heavy (*you lost me*); ruled on the plain-language restatement |
| bank_import | R-BI4 | -- | 2026-09-11 (developer) | **AUTHENTICATION IS ONE APP-WIDE GATE, DEFAULT-DENY, AND AN UNMATCHED URL IS DENIED TOO.** Ruled at `X-gi-4` on **N-402**'s fork. The finding's remedy -- put the statements family into the auth-required sweep -- was the instance; the class was a hand-typed list of 124 `(method, path)` pairs graded complete while the app served 222, 213 of them decorated (censused 2026-09-11): a gate whose coverage is a list somebody remembers to update. Three options were put: (A) add the 12 rows; (B) derive the sweep from `url_map` and keep the decorators; (C) delete every `@login_required` and refuse anonymous requests in ONE `before_request` hook keyed on a central `PUBLIC_ENDPOINTS` frozenset of endpoint names. C was chosen over the recommended B-now-C-later, because the doctrine's goal state is a fence that is structurally unnecessary and under C a forgotten decorator is unrepresentable. Two sub-rulings the same night: an anonymous request for a URL matching NO route is bounced to `/login` like every other (fail-closed; Werkzeug's 404/405 stay for a session -- it costs a flash and a session cookie on such requests), and the public set is one central frozenset pinned by a test rather than a marker on each view. The gate is Flask-Login's `login_required` hoisted -- `OPTIONS` and `LOGIN_DISABLED` exempt, `login_manager.unauthorized()` the answer -- registered after CSRF and the limiter so an anonymous request meets both before it is bounced. Rejected: A, which leaves 77 pairs unswept; a pylint checker over `.route` without the decorator (a source-text sampler with its own allowlist); the hand list plus an equality test against `url_map` (two homes). **Every later lane obeys one sentence: write no decorator; a route is gated by existing, and a public one is named in `PUBLIC_ENDPOINTS`** (`tests/test_arch/test_the_login_gate_has_one_home.py` refuses the decorator) |
| bank_import | R-BI5 | -- | 2026-09-12 (developer) | **THE BANK-LINE ROW LOCK IS `FOR NO KEY UPDATE`, AND ITS MODE HAS ONE SPELLING.** Ruled at `X-gi-5`. `load_lines` and `_skipping._line_on` had passed `key_share=False` since `758bbe55` (`X-gj-4a`), which SQLAlchemy renders as the stronger `FOR UPDATE`, while both docstrings and the 2026-09-02 security review argued `FOR NO KEY UPDATE` (`FOR KEY SHARE` is what a foreign-key insert takes and two of those serialise nothing; `FOR UPDATE` blocks those checks for no benefit); no test read the emitted text. `$0.00` today: every writer referencing a bank line holds its lock first. Three options were put: fix it in `X-gi-5` through ONE helper the new ordered pre-read also uses, with a test grading the emitted SQL (chosen); keep `FOR UPDATE` and rewrite the prose; file a finding for a later step. Chosen because the pre-read is the lock statement's one spelling and a docstring the code contradicts ships no longer. `_resolve.locked_for_write` (`key_share=True`) is that spelling; `load_lines`, `lock_lines` and `_line_on` take the lock through it and nothing else in `app/` spells `with_for_update` on a bank line |
| bank_import | R-BI6 | -- | 2026-09-12 (developer) | **THE "NOT PLACED" BADGE AND THE UNPLACED RECEIPT ASSERT NO CAUSE, AND THE RELEASED-PLACEMENT FACT LIVES NOWHERE UNTIL THE LEVEL RELATION.** Ruled at `X-gr` on **BI-489**'s fork, three answers. (1) That a placement was RELEASED is stored nowhere now: a reason column on `statement_imports` would be a second home for exactly the fact `balance:X-bj-1`'s relation records as an appended superseding row (**R-JN**, rule 14), living only until that step lands and then needing a drop; leaving the false sentence until then was rejected because a false cause stays on the page the owner checks after a delete. (2) The badge's tooltip is worded for both cases, the developer's sentence verbatim: *No day holds this figure. Either no day this file covers reconciled it with what was already recorded before the file's first day, or a later import or a delete changed the lines it had been placed on and released the placement. A later import can place a balance again.* Label and colour unchanged. (3) The import RECEIPT's sentence for an unplaced figure carried the same defect -- *which its own lines do not reach* is the date-range cause, printed on files whose lines reach fine while the RECORDED opening disagrees -- and changes to *which no day it covers reconciles with what was already recorded before it*; its one quoting assertion updates under rule 5's developer-confirmed exception. The cause itself is `X-bj-1`'s (**BAL-485**). |
| bank_import | R-BI7 | -- | 2026-09-12 (developer) | **THE RECEIPT VALUE LEAVES `_batch.py` WHOLE, FOR `_outcome.py`.** Ruling on `X-gt`'s cut (**R-PC71**'s shape; **BI-491** named the seam without deciding it), taken with `app/services/statement_match/_batch.py` at 998 of pylint's 1,000 lines after `X-gi-5` and `balance:X-bn` still to put an advisory lock into it. What a pass HANDS BACK -- `AppliedItem`, `RefusedItem`, `BatchOutcome` and the accumulator `_Tally` -- moves to a NEW sibling `_outcome.py`; `_Tally` becomes `Tally`, the package's own rule for a name a sibling constructs (`_receipt_sentences`' three; **R-PC74**'s three); `_batch` re-exports nothing and both importers (`__init__`, `_filing`) re-point; the references a byte-pure move cannot keep true (**BI-484**) are repaired IN the move commit, purity graded by an AST comparison with docstrings stripped (`outcome_move_check.py`, re-runnable on any tree whose `_outcome.py` is `89e39a18`'s; `X-gx` edits that file next and the check fails there by design). What stays is the pass: the header's argument, `Consent`, `ReviewedBatch`, `_run`, the four arms, `apply_reviewed`. `$0.00` under every option: a placement and rule-14 question, as **R-PC60** and **R-PC69** were. Rejected: `BatchOutcome` and `_Tally` alone, the plan's literal sentence (circular: `BatchOutcome.applied` is typed on `AppliedItem`); the frozen value without `_Tally` (the 14-field `Tally`/`BatchOutcome` mirror, rule 14's tell, would span two modules); the input side, `Consent` + `ReviewedBatch` (every arm reads it, and `__post_init__` is **R-GH**'s consent boundary); the arms and `_run` (they ARE the pass the header argues); trimming prose (**R-PC60**, **R-PC69**, **P31**); `_filing` or `_receipt_sentences` as the home (a consumer, and the words). The `Tally` -> `BatchOutcome` mapping finding surfaced at the ruling is **BI-494**, filed and not built (owner `X-gw`). Shipped at `89e39a18`. |
| pay_calendar | R-PC47 | -- | 2026-09-03 (developer) | **A PAYDAY IS A RECORDED FACT THAT MAY FALL OFF THE CADENCE, AND THE SCHEDULE CARRIES ITS SHIFT CONVENTION.** Ruling on four rows that were one question: **F-4** (a payday really paid 31 December 2025), **N-398** (the rhythm models no shift), **P80** (no check sees an irregular payday set), **P78** (fixtures build a calendar no door can write). Real payroll is occasionally days off the cadence, and the shift is the employer's CONVENTION, not noise: `budget.pay_schedule` gains one (none, preceding business day, following business day), ONE business-day module holds the weekend rule and the computed federal holiday set (the pay calendar's projection and `recurrence:R8-d`'s cash dates both call it), and the forecast reproduces the shift from the rule alone, so no stored correction per payday is needed. **C14 moves NOTHING recorded** (developer, same night, when `pay_calendar:C7` was found to hang on it): the convention shapes only the PROJECTED half, a recorded payday that disagrees with the rule is warned about and never rewritten, and correcting one is an owner act through `C6`'s door, whose **P10** rulings govern what re-files -- so past entries stay where they landed and C7 is not blocked -- **the same principle `pay_calendar:C7`'s own ruling states one tier down** (its id `R-PC53` is reserved to that lane and lands with C7): a recorded fact is warned about, never rewritten. A recorded payday that differs from the prediction is an exception the integrity sweep WARNS on, never a refusal (**P80**); `F-4` merges into `N-398` and both are owned by `C14`; **P78**'s fixtures re-point at a schedule a door can write (calendar-monthly is `R13`'s cadence kind, not a shift). Worked: 1 January 2026 plus 26 fortnights is 31 December 2026, a nominal 27-payday year; payroll moved the first to 31 December 2025, so 2025 had 27 and 2026 has 26 -- one `$3,526.00` gross paycheck changes tax year, wrong in both years under nominal dates. |
| pay_calendar | R-PC48 | -- | 2026-09-03 (developer) | **EIGHT COMPOSITE OWNER KEYS OVER SIX TABLES LAND IN ONE MIGRATION, AS ONE STEP, `C13-c`.** Ruling on **P83**: `budget.transfers` (three column pairs), `transaction_templates`, `ledger_accounts`, `savings_goals`, `statement_imports` and `journal_entries` each carry a `user_id` beside a foreign id no key holds to one owner, which is **P75**'s defect on six more tables; `fk_statement_matches_owner` and `fk_account_external_identities_owner` are already this construction. One migration adds each superkey and composite key -- three on `transfers`, one on each of the other five, eight in all -- and it DELETES `integrity_check.py`'s DC-09 rather than joining it (`CLAUDE.md` rule 14). Filed under `pay_calendar` as `C13`'s third leaf though the tables span three arcs, because a migration does not care which arc a table's steps live in and P75's family is where the pattern was proven. |
| pay_calendar | R-PC49 | -- | 2026-09-03 (developer) | **A CLOSED PAYCHECK IS OFFERABLE ON THE PERIOD-MOVE CONTROL; THE LOCK CLASSIFIER ALONE REFUSES A MOVE.** Ruling on **P46**: the transaction's period-move `<select>` withheld every closed paycheck while the PATCH doors accepted any the owner holds -- one rule stated in the form and not in the door -- so a row could not be moved backwards into a closed paycheck through the UI at all, for an owner who back-fills records. The narrowing was a convenience, not the policy; the control widens to every paycheck the owner holds and `classify_period_lock` is the one thing that refuses. Owned by `C6`, which already consults that classifier for a mid-schedule edit. |
| pay_calendar | R-PC50 | -- | 2026-09-03 (developer) | **THE FOUR DATE-RANGE REGISTERS STAND; THE CROSS-FILE DUPLICATE COLLAPSES ONTO THE SHARED LABEL.** Ruling on **P47**: the four widths that genuinely differ (`utils.dates.pay_period_label`'s `02/21 - 03/06`, the analytics and spending-report `Feb 21 - Mar 06, 2026`, the companion's and the mobile plan's) are deliberate registers and stay. The SEVENTH spelling the census never named -- `accounts/_reconcile_panel.html` and `dashboard/_pulse.html` both hand-rendering `%b %-d - %b %-d` -- is a straight duplicate and both read `period.range_label`, which `C4-a-2` already made reachable on the panel's block. Owned by `C8`, the pay-calendar UX step. |
| pay_calendar | R-PC51 | -- | 2026-09-03 (developer) | **THE SAVINGS COCKPIT'S HORIZON GUARD IS DELETED: A LAPSED OWNER SEES HORIZONS AT THEIR STATED CADENCE.** Ruling on **P81**, re-keyed from `pay_calendar` to `balance` under `X-x1`. `savings_dashboard_service/_orchestrator._build_projection_context` published NO horizon tiles unless a current period existed, on a guard whose one stated reason `C4-d` deleted when it made `PayCalendar.cadence` total; the emptying then decided a product question its comment never argued. Rejected: keeping the emptying as a stated rule. The tiles render at the schedule's cadence with no dependence on a current period, and the LAPSE is surfaced by `X-x1`'s own state -- the one answer to *no pay period covers today* -- which is where **P40** (an owner with a schedule but no paydays told their retirement date has passed) went the same day. |
| pay_calendar | R-PC52 | -- | 2026-09-03 (developer) | **THE RETIRE-LATER SOLVE RE-RUNS ONLY WHEN AN ASSUMPTION IT READS HAS MOVED.** Ruling on **P60**: the readiness card re-solved the retire-later binary search -- about nine projection walks of pure compute -- on every refresh, `159 -> 426` ms on a slider-only change, as the PRICE of P59's fix. Rejected: accepting the cost, though it sits inside the 500 ms debounce. The card is split so the solve refreshes only when an assumption moved and a stepper move costs one probe; its own small step, `C15`. |
| pay_calendar | R-PC53 | -- | 2026-09-03 (developer; two-half wording ratified 2026-09-04) | **`journal_entries.pay_period_id` STAYS, AND `P18`'s PREMISE FITS NEITHER HALF OF THE COLUMN.** Ruling on **P18**, which called the column a materialisation of `entry_date`. It is not one uniformly: the 332 SOURCE-LINKED entries COPY the source row's own period -- the owner's budgeting choice, not a function of the settle date -- and 105 of them disagree with what `filing_period` would answer, so deriving them destroys a fact nothing else holds; the 218 ANCHOR corrections DO derive it from the entry's date (**balance:R-EA** mandates that; 216 of 218 equal it exactly), and rule 14 would normally delete such a copy, but the CALENDAR can move under a past entry -- `extend_pay_periods`, `top_up_rolling_window`, `reset_pay_periods` -- so the stored value is what records where the postings landed. **`filing_period` stays TOTAL because every per-period reader must be able to place every entry**, which the `NOT NULL` EXPRESSES rather than causes; stating that causation the other way round is what let P18 read a reader requirement as a storage artifact. **NULLABLE is REFUSED**: it buys the deletion of one clamp serving two call sites and costs a third state across six readers, one of which (`account_posting_service._walk._residue_source_days`, `PayPeriod.id.in_(nets)`) would drop a NULL group silently. Measured 2026-09-04 on `shekel-prod-db` at 550 entries: DROP-and-derive re-attributes 107 entries, `$9,700.48` net across period boundaries -- **of which 60 are purchases sharing one backfilled `settled_on`, so the durable figure is `$2,200.92`**. Twin of **R-PC47** one tier down: a recorded fact is warned about, never rewritten |
| pay_calendar | R-PC54 | -- | 2026-09-04 (developer; first form of the same night REPLACED after adversarial review) | **THE SHIFT IS APPLIED AT THE PRODUCER, AND NO PHASE IS STORED YET.** Ruling on how far the convention reaches. `_rhythm._paydays_between` takes its forward paydays by reading `.start_date` off the `DerivedPeriod` values `_views.projected_paychecks` yields, so the payday a COUNT uses and the payday a PERIOD opens on are ONE value with ONE producer already; displacing only the count applies the shift AFTER that read and splits it into two spellings, which is rule 14's tell. So the shift goes where paydays are generated. Three things in the derivation are wrong once a payday can move, and each is a `$0.00` REPAIR rather than a cost of the shift: the projected end `start + cadence - 1` leaves 2026-01-14 in NO period under this step's own worked example, which `PeriodWindow` refuses; `shift` is not injective, so cadence 1-3 produces duplicate paydays `derive_periods` refuses; and two writer paths feed a CASH date back into the rhythm. They land at `C14-c` and `C14-d` BEFORE the shift goes live, and the arc document carries the detail. **NO nominal anchor is stored on `budget.pay_schedule`** -- the first form added one, and review measured that a single (anchor, cadence) pair cannot describe a schedule whose cadence CHANGED, which `record_paydays` permits and a shipped test builds. A phase is an ERA's fact, so it lands at `C17` (**R-PC58**) rather than as a column this arc would delete; the rhythm keeps its phase from the recorded paydays meanwhile, whose one bounded gap is an owner whose FIRST recorded payday was itself shifted -- `$0.00` for the production owner, whose opening payday is an ordinary Thursday. Rejected: shifting only the count, and the single anchor column. *ITS SECOND PREMISE -- the "one bounded gap" -- WAS MEASURED FALSE ON 2026-09-05 and* **R-PC61** *reverses the clause it rests on; that row carries the measurement.* |
| pay_calendar | R-PC55 | -- | 2026-09-04 (developer; first form REPLACED the same night) | **THE OFF-CADENCE CHECK IS TWO CLAUSES, BECAUSE A PER-PAYDAY EQUALITY CANNOT SEE A MISSING PAYDAY.** Ruling on **P80**, correcting a first form that ruled the equality ALONE. Measured against P80's own worked example: paydays `[2026-01-02, 2026-01-16, 2026-07-31]` at cadence 14 sit 0, 14 and 210 days from the opening payday, so all three are EXACTLY on the grid and an equality reports GREEN on the very 196-day paycheck the row exists to detect -- its defect is thirteen MISSING paydays, and a per-row equality is blind to an absent row by construction. The gap predicate the first form REJECTED is the one that catches it (gaps of 14 and 196; the 196 fails). The two are complementary and were framed as substitutes: the sweep WARNS when a recorded payday is off its predicted day, AND when consecutive recorded paydays are not one cadence apart. Neither refuses, neither rewrites a recorded fact, and neither needs a tolerance number. Measured 2026-09-04 on `shekel-prod-db`: 63 recorded paydays and 62 gaps, every gap exactly 14 days, so both clauses are silent on the developer's data today -- and that silence grades only the identity case, since with the convention at `none` the first clause reduces to *is this set an arithmetic progression from its own opening*, which those rows satisfy trivially |
| pay_calendar | R-PC56 | -- | 2026-09-04 (developer) | **THE SHIFT CONVENTION DEFAULTS TO `none` AND IS ASKED WHEREVER A CADENCE IS.** Every `budget.pay_schedule` row is seeded `none`, so behaviour is OFF until somebody answers, and the question joins the FOUR templates that already ask for a cadence: `auth/register.html:104`, `settings/_pay_periods_form.html:35` (the `pay_periods.generate` door), and `settings/_pay_periods_manage.html:141` (regenerate) and `:291` (reset). *The first form of this ruling named two doors, one of which was not one -- `_pay_periods_manage.html` holds seven POST forms and `history_opens_on` sits in a third of them -- and it OMITTED the generate door, which is precisely the endpoint **P80**'s predicate names as the one that accepts an irregular payday.* The vocabulary is the EXISTING `ref.business_day_shifts` (`none` / `prior` / `next`) seeded at `recurrence:R2`, so nothing is minted. Rejected: defaulting to `prior` because real payroll usually pays early -- it would close **N-398** for everyone with nobody answering, and it states as fact what no owner was asked, the error **balance:R-IF** (2026-08-31) was written to correct. *The first form cited that amendment as `balance:R-IA`, which is the 2026-08-30 ruling R-IF AMENDED and states the opposite disposition* |
| pay_calendar | R-PC57 | -- | 2026-09-04 (developer; re-split after adversarial review) | **`C14` IS SIX LEAVES, AND THE FIXTURE REBUILD IS NOT ONE OF THEM.** `C14-a` is the shared business-day module, pure and consumed by nobody; `C14-b` lands the convention column, its cadence CHECK and the four doors, with behaviour off; `C14-c` corrects the derivation's boundary arithmetic (the projected end becomes the next payday minus one, and the containment probe tolerates a moved boundary); `C14-d` corrects the two writer paths that feed a cash date back into the rhythm; `C14-e` switches the shift ON at the producer and is the one that **MOVES MONEY**; `C14-f` adds **R-PC55**'s two-clause check. `C14-c` and `C14-d` are `$0.00` REPAIRS that must precede `C14-e`, because a live shift over the old end rule puts a day in no period. **`P78`'s eight fixtures move to `C17`**: their remedy is a door that can write a 28-31-day schedule, which **R-PC47** itself says is `R13`'s cadence KIND and not a shift, so sequencing a `$0.00` test-corpus fix behind a money step had no reason (conventions rule 8). *The first form of this ruling claimed the split lets `recurrence:R8-d` start earlier; it does not -- R8-d's binding blocker is `recurrence:R5`, and its `starts` cell reads the same rank before and after. What is true is narrower: R8-d now names the LEAF it reads rather than a money-moving parent.* *Its `C14-b` clause "its cadence CHECK" is SUPERSEDED by **R-PC59** (2026-09-05): that leaf lands the column, the pairing rule and the four doors as written, and the rule is refused at the write door because its floor is derived from a holiday set no CHECK expression can recompute* |
| pay_calendar | R-PC58 | -- | 2026-09-04 (developer) | **A PAY SCHEDULE IS A SEQUENCE OF ERAS, AND THAT IS `C17`.** Ruling on where the rhythm's phase and kind live, taken when **R-PC54**'s first form was found to store an era's fact on a schedule row. `budget.pay_schedule` holds ONE cadence per owner while the recorded paydays can be PIECEWISE -- `record_paydays` deliberately permits *correct my cadence going forward* (`app/services/pay_period_write.py`, and `pay_period_admin.regenerate_pay_periods` says the same in its own Args) -- so today a cadence change silently re-describes every PAST payday too, which is a live defect this ruling opens **N-492** for rather than a consequence of the shift. From scratch the schedule is `(user_id, effective_from, kind, cadence, nominal anchor, convention)`, one row per *how I have been paid since*, and `resolve_cadence` becomes *the era covering this day*. It is deliberately NOT part of `C14`: it changes what a stored cadence means, it touches the pay-period writer, and an era carries a KIND as easily as a length, so it is also the door `recurrence:R13`'s calendar-monthly cadence needs and the one **P78**'s eight fixtures have never had -- one relation serving three open rows instead of three migrations over one column |
| pay_calendar | R-PC59 | -- | 2026-09-05 (developer) | **THE CADENCE-CONVENTION RULE IS REFUSED AT THE WRITE DOOR, NOT BY A CHECK CONSTRAINT.** AMENDS **R-PC57**, whose `C14-b` clause reads "its cadence CHECK"; the RULE is unchanged and **R-PC54**'s substance stands. A displacing convention needs a cadence longer than the longest run of consecutive closed days, or two nominal paydays displace onto ONE day and `derive_periods` refuses the owner's whole calendar. Two reasons a CHECK cannot carry it. **The floor is DERIVED** -- the longest closed run PLUS ONE, a theorem rather than a census, since under `NEXT` two nominal days collide exactly when every day between them is closed, and `PRIOR` mirrors it -- and it moves with the federal holiday set, which `business_days.JUNETEENTH_FIRST_YEAR` records changing once inside this application's calendar window; a CHECK expression must be IMMUTABLE, so one could only freeze a copy nothing recomputes. **A constraint cannot name a FIELD**: it arrives as an `IntegrityError` carrying a constraint name where a form needs the message on the control the owner chose, as `X-ad-a` already found for the cadence BOUND. Nor could it have been the PRIMARY refusal -- the pair is written in ONE statement, because two leave the row in a state neither means and either order refuses a legal request. So `business_days.shortest_collision_free_cadence` derives the floor (**4** today, measured 2026-09-05), `pay_schedule_service.reject_shift_on_short_cadence` refuses the pair at the column's one writer, and `pay_periods.validate_derivable_rhythm` asks the same predicate at the schema. **What no placement buys**: a refusal asked on WRITE cannot see a STORED row a later holiday change made illegal, and nothing reconciles this table -- reported, not claimed away. Rejected: a pinned `ref` id behind a migration assertion; a validation trigger; a legal-pairs table behind a composite FK; and raising `CADENCE_DAYS_MIN` for everybody, which re-opens **P9** |
| pay_calendar | R-PC60 | -- | 2026-09-05 (developer) | **THE NOMINAL GRID IS A MODULE BELOW THE DERIVATION.** Ruling on where `C14-d`'s nominal-rhythm producer lives, taken because `_derive.py` stood at 996 of pylint's 1,000-line ceiling and a documented function would not fit. Four options were costed: a new package module; `_derive.py` with ~30 lines of `projected_payday`'s census prose moved to the arc document (the `C7` tier answer); `app/utils/dates.py`, which already holds `pay_period_label` and has room; and no producer at all, `extend_pay_periods` open-coding `last_recorded + cadence`. **The developer took the new module.** `app/services/pay_calendar/_grid.py` holds `nominal_payday` and `cadence_steps_to` (moved down, they are inverses), `_derive` imports both, and `projected_payday` returns `nominal_payday`'s answer unchanged until `C14-e` wraps it in the displacement. Money is identical under all four -- `$0.00` today and after `C14-e` -- so this is a placement and rule-14 question rather than a financial one. What decided it: the split is a DISTINCTION rather than a filing decision, since from `C14-e` a calendar displaying a paycheck and a writer continuing a rhythm want different days and both have callers; and it leaves `_derive.py` at 972 with room for `C14-e`, where moving prose spends the budget now and meets the same ceiling one step later. Rejected: `utils/dates`, which puts the pay rhythm outside the package that owns it; and the fourth option, which is the duplicate spelling `C14-d` exists to delete. **The chain head moves**: the package's one-way order is now `_grid` -> `_derive` -> `_searches` -> `_window` -> `_views` -> `_calendar` -> (`_walks`, `_rhythm`), and `_grid` imports nothing from `app` at all |
| pay_calendar | R-PC61 | -- | 2026-09-06 (developer, on the from-scratch design he asked for) | **THE NOMINAL GRID'S PHASE IS A STORED FACT, AND `C14-e` IS THREE LEAVES.** `budget.pay_schedule` gains `nominal_anchor`, a day the owner's nominal grid passes through, and `pay_period_admin.extend_pay_periods` steps from it rather than from its own last recorded payday. **It is a FACT and not a stored derivation**: today it equals `MIN(budget.pay_periods.start_date)`, which the backfill uses, and it stops being equal the moment a convention displaces -- a recorded payday is then what the BANK did, the anchor what payroll INTENDED, and **R-PC47** says a recorded payday may fall off the cadence, so neither determines the other. The root cause it answers is `budget.pay_periods` holding a RECORD and a PLAN in one table, with the projection re-reading its own stored output as its anchor. **It REVERSES R-PC54's "No phase is stored here"**, whose premise -- the rhythm keeps its phase from the recorded paydays, "whose one bounded gap is an owner whose FIRST recorded payday was itself shifted" -- holds only while no writer records a DISPLACED payday, which `C14-e-3`'s must (**PC-497** fault 1) and which makes the gap COMPOUND: **178 of 301** recorded paydays wrong under `prior` at a batch of ONE, the rolling top-up's steady state, against **10 of 301** at `none` and **0** anchored here. **RE-DERIVED** from `app/utils/business_days.py` and the developer's stated cadence and opening payday, not read from a database; the mechanism is in migration `a1c7e5d20f43`. **It is NOT scaffolding**: **R-PC58** already specifies this column as one of `C17`'s five era columns, so `C17` ABSORBS it as it does `cadence_days` and `shift_id`. The leaves are `C14-e-1` (the rhythm becomes ONE pure value), `C14-e-2` (this column) and `C14-e-3` (MOVES MONEY, its own PR). **R-PC54's substance stands**: the shift is still applied AT THE PRODUCER. **N-492** -- which cadence a PAST payday ran at -- stays `C17`'s |
| recurrence | R-R52 | -- | 2026-09-03 (developer) | **`R5` RE-POINTS BEHIND `balance:X-bi-4`; ITS `X-f4` GATE WAS MERGE HYGIENE, NOT A DEPENDENCY.** The blocker rested on one shared file, `cash_ledger/_events.py`, where R5's whole touch is a docstring line; that is a rebase question, not an ordering constraint, and it held six steps (`R5`, `R6`, `R8-b`, `R8-c`, `R8-d`, `balance:X-k`) behind a flip they do not read. The real dependency is the fold re-pointing to movements (`X-bi-4`), after which a generated row's dates are written against a settled shape that will not change under them. `R6`, `R8-b`, `R8-c`, `R8-d` and `X-k` follow R5 and inherit the move. |
| recurrence | R-R53 | -- | 2026-09-02 (developer), recorded 2026-09-03 | **A LOAN IS WALKED ONCE, WITH PLUGGABLE CASH -- ACTUAL, PLANNED OR CONTRACTUAL -- AND THE OTHER THREE WALKS GO.** Ruling on **N-439**, re-keyed from `balance` to `recurrence` as **D60** so its owner resolves inside its own arc (conventions rule 1). The developer's question, verbatim: *"Why are there four walks over a loan? One walk with one answer can't disagree with itself and doesn't need to be kept in sync."* Three recurrence siblings answer it, no sixth arc and no new document: `R16-c` merges the past and future streams, **`R16-e`** DELETES walk 3 (`rate_period_engine.replay_schedule`, whose balance has one consumption site every production read bypasses; what survives is two calendar scalars that need no walk), and **`R16-f`** re-expresses walk 4 (`amortization_engine.project_forward`) over the one replay. `R16-e` and `R16-f` were ruled on 2026-09-02 and existed in no document until this row. |
| recurrence | R-R54 | -- | 2026-09-03 (developer) | **A 27-PAYCHECK YEAR IS A FEATURE; THE LOCALE IS PINNED IN THE IMAGE; A STALE EDITOR PRE-FILL IS THE OVERRIDE FLAG'S TO FIX.** Three of this arc's oldest operator rows dispositioned. **F-5** (no surface names a 27-paycheck year) is not a defect in the code: once `pay_calendar:C14` makes the per-year count a derived fact, surfacing it is a roadmap item, so the row closes and the item moves to `docs/project_roadmap_v5.md`. **F-15** (month names would follow the process locale if anything ever called `setlocale`; nothing does, `$0.00`) is answered by ONE line -- `LC_ALL` pinned in the image with a startup assertion -- which covers all 109 sites and every future one, and `R12`, the deploy-predicates step, takes it. **F-11** (the grid's inline amount editor pre-fills from the STORED figure while the cell renders the live one, and saving sets `is_override`) is `balance:X-au-h`'s subject -- the flag says one thing -- so the row re-keys to `balance` under it, and *stop setting the flag on an unchanged amount* is that step's to build. |
| recurrence | R-R55 | -- | 2026-09-02 (developer) | **`R7d-d` REACHES AS FAR AS THE COMPOSED VALUE**, over two smaller options. Five surfaces need the conjunction of the authored bound and the derived stop -- generation, the Recurring surface's next date, its cadence sentence, the `/obligations` and `/savings` totals, and the form's preview -- so the conjunction is a VALUE the walk already reads (`ResolvedRecurrence.closing`) rather than a narrowing each performs. An optional `narrowed_by=` on the walk was REFUSED: a default meaning *no narrowing* leaves each caller one forgotten keyword from admitting occurrences against a debt that is gone, with nothing to raise on. The answer SHAPES move to `recurrence/_closing.py` because `_describe` cannot import `loan_recurrence_sync` without a cycle (rule 14's placement clause); the RESOLVER stays, since deciding which shape applies means folding a balance. Stated inline in the arc document until 2026-09-05, against the rule that no arc document declares a ruling; filed here at the R7d-h tick. |
| recurrence | R-R56 | -- | 2026-09-04 (developer) | **A CLOSING BOUND THE APP WRITES IS THE CACHE IT IS, NOT THE OWNER'S WORD.** Asked at `R7d-d`, whose adversarial reviews both found that the composed `Closing` ANDed the stored `end_date` with the derived stop while, for the loan payment the app itself bounds, that column holds the ten chokepoints' cached payoff (the edit form locks the control) -- so a cache EARLIER than the derived closing date still bound: ledger row **D35**'s measured shape, `2029-01-22` stored against `2029-02-22` derived. The developer first chose to RE-RANK `R7d-g` ahead of `R7d-d`; that was sent back and withdrawn because `R7d-g` is blocked by `R7d-c-2`, `R7d-e` and `R7d-f` for a money reason -- NULLing the column before generation takes the door leaves a loan payment's rule with no bound at all and projects installments past the payoff -- and the gate refuses a rank ahead of an unshipped blocker. RULED, in the option's words: for a definition whose window the app writes (`owns_validity_window`), the door composes `authored=NEVER_ENDS` so only the derived stop binds; the Recurring row reads `until Feb 22, 2029`; generation is untouched until `R7d-c-2` reads through the same door; `R7d-g` keeps its place, and its migration's one rendered move is the archived drawer named below; a second transfer into the same loan keeps its owner's bound. Measured after the ruling and stated in the code: the predicate names the account's ACTIVE payment, so an ARCHIVED loan payment's cached column is still read as authored in the drawer until `R7d-g` NULLs it -- at which point that drawer row flips from the cached date to the derived stop -- and `R7d-g` must DECIDE archived loan payments rather than sweep them (**D56**, an open fork); the arm leaves with the column, because a bound authored on the generic create form (which cannot lock the control) would otherwise be read as the cache forever once nothing overwrites it. Shipped at `4f40d6de`. |
| recurrence | R-R57 | -- | 2026-09-05 (developer) | **THE DERIVED STOP TAKES R-R45's READING: A DEFINITION HAS ENDED WHEN IT OWES NO OCCURRENCE ON OR AFTER THE DAY ASKED**, not when its closing date has passed. The two readings part at the tail of a loan: the Van payment trued to `$0.00` on 2026-09-01 after its 2026-08-22 installment, so the closing-date reading leaves the totals on 2026-09-02 while the owes-no-occurrence reading leaves them on 2026-08-23. `R7d-e` builds the derived half on this reading. |
| recurrence | R-R58 | -- | 2026-09-05 (developer) | **A LOCKED *Ends* CONTROL RENDERS THE DOOR'S DESCRIBED STOP PHRASE, IN A ONE-OPTION DISABLED SELECT.** Asked at `R7d-f-1`, where the control had to show SOMETHING for a bound the owner does not author. It renders what the door itself says the rule stops on rather than a date the form computes, so the form and the door cannot drift; a disabled select with one option posts nothing, which is why the preview never saw the column. As-built `6af50d53`. |
| recurrence | R-R59 | -- | 2026-09-05 (developer) | **A LOAN'S OWN PAYMENT KEEPS ITS *Ends* CONTROL LOCKED PERMANENTLY, and ARCHIVE is the door to stop it early.** Asked at `R7d-f-1`. The closing bound of a standing loan payment is DERIVED from the loan, so an owner editing it would be authoring a second answer to a question the loan already answers -- rule 14's shape on a form control. Stopping early is a different act with its own door. As-built `6af50d53`. |
| recurrence | R-R60 | -- | 2026-09-05 (developer) | **A STATED CLOSING BOUND IS REFUSED AT CREATE WHERE THE DESTINATION LOAN HOLDS NO ACTIVE PAYMENT**, which is how **N-512** closes. Asked at `R7d-f-1` and BUILT at `R7d-f-3`: the create form can name a loan whose payment does not exist yet, so the refusal belongs at the door rather than in the lock, and the JS lock is demoted to an affordance. It lands in `_transfer_creation_helpers` because `app/routes/transfers/templates.py` stands at 997 of pylint's 1000-line ceiling. |
| recurrence | R-R61 | -- | 2026-09-05 (developer) | **`R7d-f` DECOMPOSES INTO THREE LEAVES.** Taken 2026-09-05 when `R7d-f-1`'s trace showed the parent's own sentence inexact in two places -- `owns_validity_window` splits no SET, one renamed identity serving both rows, and the live preview never read the column at all. The leaves are the identity and the form's locks (`R7d-f-1`, shipped `6af50d53`), the pass's memo and the reading horizon (`R7d-f-2`), and the create-side refusal (`R7d-f-3`). |
| salary | R-SAL1 | -- | 2026-09-03 (developer), named 2026-09-03 | **THE PAYCHECK ENGINE IS ITS OWN ARC, NAMED `salary`.** It owns `paycheck_calculator`, `income_service`, `calibration_service`, `payroll_basis`, the `routes/salary/` package and the tax services -- a subsystem four arcs edited and none named, which is how `balance:X-au-d`'s three measured defects (**N-441**, **N-442**, **N-443**) came to have no owner. `recurrence:R14`, `R15`, `R18`, `balance:X-at`, `X-av` and `pay_calendar:C12` move under it with their bare ids unchanged (conventions rule 10; C12 moves because it IS N-443's remedy), their ledger rows with them, and two steps are minted here (`S1`, `S2`). Its document is `docs/plans/implementation_plan_salary.md`, capped at 260 lines with a 20-line signpost; its ruling ids are `R-SAL1` onward, three letters because the corpus-wide two-letter sequence would eventually reach `R-SA`, and a prefix that sequence cannot produce cannot collide with it. Rejected: leaving the engine unowned, and filing it under `pay_calendar`, whose subject is periods and not pay. |
| salary | R-SAL2 | -- | 2026-09-03 (developer) | **A PAYROLL DEDUCTION IS PRICED FROM ITS OWN PROFILE, AT THAT PERIOD'S GROSS, BY THE ONE PAYCHECK ENGINE; THE PERIOD IS THE CLOCK.** Ruling on `R14` (finding **D45**): `investment_projection._compute_deduction_per_period` divided a profile's stored `annual_salary` by the paycheck count, `$137.51` a year understated on the developer's own 5% employer match (2026-08-19), and `R-F16`'s reverted fix -- one owner-level current gross -- priced every deduction off ONE profile chosen by an unordered `.first()` (a 39% swing on a two-job owner) and off ZERO whenever no period covered today. The three questions are answered together: WHOSE salary is the deduction's own profile; WHICH gross is the one `project_salary` already computes for that period, raises applied as-of the period; WHICH clock is the period itself, never `date.today()`. Worked on the developer's own pair (r14 lane, 2026-09-03): `$3,525.96` at 5% is `$176.30`, and after the real 2026-07 raise to `$3,631.74` it is `$181.59`, history unchanged -- against `$181.59` on every period under a today-scalar and `$176.30` everywhere under the annual divide; over the 63 saved paydays, with both raises recurring, the flat feed understates `$1,646.84`. The contribution tier CONSUMES the per-period breakdown, the shared leaf `C12` collapses the other readers onto -- and the breakdown already carries the hook: `DeductionLine.target_account_id` was populated *so the two surfaces agree* and nothing ever consumed it, so this ruling finishes a seam rather than adding one; consuming it also retires the feed's two re-spellings of the annual cap and its silence on inflation escalation. Which PROFILE funds an employer contribution that no deduction names is **R-SAL5**'s, ruled the same night in the r14 lane. |
| salary | R-SAL3 | -- | 2026-09-03 (developer) | **A DEDUCTION'S OR AN EARNINGS LINE'S CADENCE IS A RECURRENCE RULE AGAINST THE PAY CALENDAR.** Ruling on `R15` (findings **F-21**, **N-395**): `deductions_per_year` stores 26 / 24 / 12 and is only ever COMPARED, a three-valued mode wearing a biweekly count that lies at any other cadence -- 52 is *every paycheck* weekly and *skip the 3rd* names nothing -- and 11 of the developer's 12 live deductions carry 24. A line names a `recurrence_rule`, `NULL` meaning every paycheck; the engine that already places recurring rows (every-N-periods, first payday of a month) decides which paychecks a line lands on, `_deduction_applies_at` is deleted, and `R18`'s earnings lines take the same rule. One cadence engine in the app (`CLAUDE.md` rule 14). Rejected: a `ref` table of cadence kinds beside the recurrence engine's vocabulary, and keeping the integer with 52 added. The step's first act is a trace of how the engine couples to a template, since a paycheck line has none. |
| salary | R-SAL4 | -- | 2026-09-02 (developer), recorded 2026-09-03 | **A CALIBRATION APPLIES FORWARD FROM ITS STUB'S OWN DATE, AS A RAISE DOES.** Ruling on **N-441**, recorded when its step `S1` was minted: `salary.calibration_overrides` holds effective tax rates derived from ONE real stub on ONE real date and stores no date, so a calibration entered 2026-08-28 moved each of seven already-RECEIVED March-June paychecks `+$29.09` (2026-09-02 clone) -- an input that describes a MOMENT stored without it. An effective date on the row, a migration backfilling the existing row at its stub's date, and the engine resolving the calibration per period. It can never move a balance, since a settled row is worth what it recorded; it moves what a past paycheck is EXPECTED to be. Explicitly separate work from `X-au-d`. |
| salary | R-SAL5 | -- | 2026-09-03 (developer) | **AN EMPLOYER CONTRIBUTION NAMES THE SALARY PROFILE THAT FUNDS IT.** `budget.investment_params` gains `salary_profile_id`, so a percentage-of-gross employer contribution is priced from THAT profile's period gross rather than from whichever profile a reader happened to resolve. Until now an account with an employer contribution and NO deduction naming it had no profile link anywhere, and the basis fell to `income_service.get_current_gross_biweekly`'s unordered `.first()` across the owner's active profiles -- the shape **R-F16**'s adversarial review measured at a **39% swing** on a two-job owner, flipping between renders with no data change. **LIVE rather than latent**: account 6 (Empower 401(k), 5% flat) carries no deduction naming it, so its modelled employer money is priced off an arbitrary profile today. **R-SAL2 removes the CLOCK from that call and does not remove the PROFILE ambiguity**, which is why this is a separate rule rather than a consequence of that one. Rejected: making the owner-level pick deterministic by ordering it, which is stable but still arbitrary and keeps a fence the per-period design otherwise deletes; and refusing to model an employer contribution with no linked deduction, which is structurally clean but deletes account 6's modelled employer money today and MOVES MONEY in the wrong direction. Shipped as the additive column at `salary:R14-a` (`9e81d9e7`). Its reader owns what a NULL does at the door |
| salary | R-SAL6 | -- | 2026-09-03 (developer) | **AN ADDITIVE EXPAND AND A MONEY CUTOVER ARE TWO PRs, NOT ONE.** `R14` decomposes on that rule. One leaf lands the funding-profile column, the shared projection leaf and the ownership guard, and moves no figure. The other re-points the contribution feed onto the engine's per-period breakdown and MOVES MONEY, with its own adversarial review and its own before/after harness; `steps.md` carries which is which and when. Taken because the two halves want different reviews: an additive migration is graded by its backfill's branches, a money cutover by what it moves against a named input set. **The split earned itself twice.** `R14-a`'s own review found two defects in its backfill -- it could write an ARCHIVED profile's id, because archiving a profile never deactivates its deductions, and it could write ANOTHER USER's `salary_profile_id` onto this owner's row, because `target_account_id` had no ownership door (**N-534**). A SECOND review, run because the first had covered a tree that no longer existed once its findings were fixed, then found that the firing control for the first fix COULD NOT FIRE. Neither would have been visible in a diff that also moved money. Rejected: one PR for both, whose money diff and refactor diff are reviewed in the same pass |
| salary | R-SAL7 | -- | 2026-09-04 (developer) | **AN ARC DOCUMENT'S LINE CAP RISES WHEN IT BINDS ON A NEWLY MINTED ARC, BECAUSE RULE 5'S ESCAPE DOES NOT EXIST THERE YET.** `salary`'s cap goes 260 -> 280. Rule 5's remedy for a bound cap is *archive a completed span*, and a document minted days ago HAS no completed span -- this one had shipped exactly one leaf, worth four lines as a pointer -- so the only remedy actually available was the one rule 4 forbids: trimming a live step's specification to fit. The fold created the document at **239 lines against a 240 headroom threshold**, so the FIRST decomposition in it was always going to breach, whatever that decomposition was. It bound on the cheapest possible growth: a preamble duplicating **R-SAL6** was deleted, the shipped entry condensed to a pointer, and a paragraph **D45**'s own row carries removed, leaving only the deletion census and the S2-ordering ruling -- both instruction. **Section 2's evidence table is NOT the archivable span it looks like**: all seven rows are dated measurements that OPEN findings still cite (**D45**, **N-441**, **N-442**, **N-391**, **F-21**, **D59**), so archiving it would take the evidence the live steps rest on. Rejected: trimming `R14-b`'s specification, which rule 4 forbids and which would have deleted a developer ruling; and archiving section 2. **The reason generalises**: the next arc minted at its cap meets this identically, and the remedy is this ruling rather than a rediscovery |
| salary | R-SAL8 | -- | 2026-09-04 (developer) | **THE `target_account_id` IDOR IS FIXED INSIDE `R14-a`, NOT DEFERRED**, mirroring the guard `retirement.py` already carries. `app/routes/salary/items.py` cited this decision as *"a developer ruling of 2026-09-04"* with no row to point at, which is the citation-without-a-row shape `bank_import` had just fixed in its own arc and which conventions rule 8 forbids: a claim stated as a ruling must name the ruling. Recorded here so the code can cite `R-SAL8` instead of a date. Rejected: deferring the guard to a later step, which leaves a door that writes another owner's `salary_profile_id` open for the length of the deferral |
| salary | R-SAL9 | -- | 2026-09-04 (developer) | **A CALIBRATION IS A DATED OBSERVATION AND IS NEVER DESTROYED**, amending **R-SAL4**. `S1` dates the surviving calibration at its stub's date, RESTORES the one deleted 2026-08-28 from `system.audit_log` id 4212's `old_data` as a second dated row, and changes the write door so a new stub ADDS a calibration instead of replacing one. All 12 of the owner's settled paychecks then re-derive to the figure they were generated at, `$0.00` each (measured at `salary:S2`, `08638f61`). **N-441 and N-535 become UNREPRESENTABLE rather than fixed**, because no calibration can be destroyed -- the doctrine's goal state, where the fence is structurally unnecessary. **FOUR arms rejected, each recorded because each was live**: (a) restore the deleted row but leave the door replacing, which recurs on the next stub and is recoverable only while the audit trail keeps it; (b) date the survivor ALONE, `S1` as previously specified, which resolves no calibration before 2026-08-27 and costs `-$224.69` against the figures the eleven earlier rows were generated at and `-$334.32` on the 2026 net total the projection page and the cockpit show; (c) freeze the expected figure onto settled rows, which is the stored copy `balance:X-au-d` / **R-JB** just deleted, a stale cache with no reconciler (rule 14); (d) drop the expected-vs-record caption on settled rows, which blinds the surface to **N-391**'s real `$0.04`-`$0.06` deposit differences in order to hide one wrong number |
| salary | R-SAL10 | -- | 2026-09-05 (developer) | **`R14-b` SHIPS ITS INTERIM TAIL RULE WITH THE DEFECT DOCUMENTED RATHER THAN FIXED, BECAUSE `S3` DELETES THE EXTRAPOLATION ALTOGETHER** -- *"get this step in dev even if it isn't correct... if `S3` won't fix the problems then we need to discuss that otherwise ship to dev"*. Six extrapolation rules were each measured wrong over the priced fold, and a SEVENTH was refused on the developer's own reasoning: each of the six looked correct until it met a shape nobody had thought of, so another guard over the same fold buys an unfound defect rather than safety. A rule that never over-reads does exist (**N-541**) and was DECLINED on its cost, not found impossible; the code and that row record the declining. |
| balance | R-BAL3 | -- | 2026-09-05 (developer) | **ACT 4b IS DELETED RATHER THAN ANSWERED: Checking and account 10 both open their books 2026-03-25 at their banks' own closes -- `$1,234.04` and `$4,850.21` -- and the four bank lines of 2026-03-26 are RECORDED on 2026-03-26.** AMENDS **R-HK**. The `$500` ACH left Checking and reached Fidelity on 2026-03-26, the day BOTH books opened, so under **R-HG** it was absorbed on both sides and had nowhere true to live; `$689.16` was never a fact but the PLUG absorbing that (`$2,746.58` asserted less `$2,057.42` of movements, re-derived off the rows). R-HK rejected a 03-25 opening for reading `$500` under the bank -- true only while the movement went unrecorded. **Transfer 102 is KEPT** and transfer 1 dropped, inverting **N-382**'s direction. Corroborated by TWO BANKS rather than by the app: SECU's 03/12 and 03/26 `FID BKG SVC` debits pair one-for-one with Fidelity's receipts on the same days, and template 1's other *Emergency Fund* occurrences are ALREADY soft-deleted, so transfer 1 is the last live one. *`occurs_on` is NOT evidence, and a draft offered it as such.* Rehearsed on a production clone: account 10 scores **15/15** exact on the cash fold where the previous design scored 14/14, Checking's opening corroboration moves `-$2,493.47` -> **`$0.00`**, and Expense moves **`+$108.87`** of real purchase rather than `+$500.00` of invented spending. Rejected: booking the `$500` as an expense (*"I don't like faking or hiding money"*); re-deriving the plug to `$189.16`, which takes **R-IQ**'s check to `-$2,993.47`; and recording nothing. **THE UNEXPLAINED-DIFFERENCE FIGURE DOES NOT DECIDE THIS AND A DRAFT USED IT TO KILL THAT ARM**: the ACCEPTED design lands at `-$2,803.37` against today's `-$1,867.36` and that arm's `-$2,867.36`. What separates them is the OPENING's corroboration (`-$2,493.47` -> `$0.00` here, unchanged there); the argument is the README's. Opened **BAL-467**, **BAL-468**; answers **N-275** |
| pay_calendar | R-PC62 | -- | 2026-09-05 (developer) | **THE BOOKS BOUNDARY BOUNDS GENERATED ROWS, NOT ONLY SETTLED MOVEMENTS.** **R-HG** refuses a MOVEMENT dated on or before its account's `opened_on`; nothing refuses a PROJECTED one, so a pay period recorded before the books generates occurrences into a span whose money is already inside the opening equity (**PC-500**). Taken over two narrower remedies -- skipping population for a backward period, and deleting the fabricated rows by hand in the runbook -- because both are fences around the same defect and the second leaves the next prepend to hit it again. **Blast radius measured on production 2026-09-05: ZERO settled rows violate the bound and TWO rows sit in a period starting before their account's opening**, one of them transfer 102, which **R-BAL3**'s repair re-dates anyway. That count is a PROXY predicate -- the row's pay period starting before the opening day -- and it is wrong in BOTH directions, which is why the exact one is owed by the step that builds the arm: it MISSES transaction **788**, whose own `occurs_on` is 2026-03-01, and of the two it names only **1720** is re-dated by **R-BAL3**, leaving account 3's **1456** unremedied. **The bound is PER ACCOUNT and not per period** -- production's nine openings are staggered 2026-03-26 to 2026-06-26, so a period-granular arm would suppress generation for every account in a period one of them opens after. It must reach `budget.transfers.occurs_on`, which the settled-movement arm does not cover |
| pay_calendar | R-PC63 | -- | 2026-09-07 (developer) | **`C14-f` IS THE DOOR FIX, NOT A CHECK, AND IT SUPERSEDES R-PC55.** An owner who already holds a rhythm is asked only how many more paychecks, and the days come from the producer Extend already uses, which makes **P80**'s worked example UNWRITABLE rather than merely detectable -- the move this arc already made at `C14-d` and `C14-e-2`. The door CONTINUES rather than refuses: a submitted `start_date`, `cadence_days` or `shift` is IGNORED, on `PayPeriodExtendSchema`'s own **P29** precedent that *an old client that still posts one is not refused; the value is simply ignored, which is now what it means*. `C8`'s door half folds in. The residue is `regenerate`, which is **R-PC64**'s. |
| pay_calendar | R-PC64 | -- | 2026-09-07 (developer) | **`regenerate` IS `C17`'s ERA-MINT IN DISGUISE; GUARD IT MEANWHILE.** A rebuild that skips a whole paycheck asks first. **P80 does NOT CLOSE and re-points at `C17`** -- measured after the generate door was fixed, `POST /pay-periods/regenerate` still wrote paydays 140 days apart at HTTP 200 on a clean owner, and a confirmed gap is still a gap. |
| pay_calendar | R-PC65 | -- | 2026-09-07 (developer) | **THE GATES SPLIT OUT OF `pay_period_admin`**, mirroring `C3-a` and discharging **PC-498**'s `pay_period_admin` half: `app/services/pay_period_gates.py` holds them, 993 lines to 669 with a 500-line sibling. The `_derive.py` half stands at 998 of 1,000 and remains open. |
| pay_calendar | R-PC66 | -- | 2026-09-11 (developer) | **THE ERA RELATION IS A NEW TABLE BESIDE THE SCHEDULE ROW, AND ITS `effective_from` IS THE GRID'S PHASE.** Ruling on `C17`'s shape, taken on the fork the lane presented with the full option space. `budget.pay_eras` holds `(user_id, effective_from, kind_id, cadence_days, shift_id)` under `UNIQUE (user_id, effective_from)` and `fk_pay_eras_schedule`; `budget.pay_schedule` KEEPS the owner's facts -- `rolling_enabled`, `rolling_target_periods`, `history_opens_on` -- and DROPS `cadence_days`, `shift_id` and `nominal_anchor`. **R-PC61's column is absorbed INTO `effective_from`, not carried beside it**: an era's first nominal payday is by definition a day its grid passes through, so a second column would have to satisfy *anchor equals effective_from modulo the cadence*, one value in two homes held equal by a maintenance contract (rule 14's tell). An era governs from its `effective_from` to the next era's; the EARLIEST era also runs backward below the record, bounded by `history_opens_on`, which is why that column stays the owner's. The record stays authoritative below the horizon (**R-PC47**). Rejected: a separate `nominal_anchor` column on the era (R-PC58's literal five); reshaping `pay_schedule` into the era table (retargets `fk_pay_periods_schedule` and every `get_schedule` caller for no structural gain); a current-era row plus a history table (the current era in two relations); eras as `recurrence_rules` rows (no `day` unit, semi-monthly inexpressible). `$0.00` on production under every option: one owner, one grid, convention `none` |
| pay_calendar | R-PC67 | -- | 2026-09-11 (developer) | **A HOLE IN THE SCHEDULE IS UNREPRESENTABLE, NOT CONFIRMED.** Ruling on the era question **R-PC64** left to `C17` -- may an owner record a span with no paycheck. The era-mint door's first payday must fall before the old era would pay a SECOND time (the predicate `C14-f` already computes in `pay_period_gates.reject_unconfirmed_gap`, turned from a confirmation into a refusal), so **P80**'s set is unwritable through any door and `reject_unconfirmed_gap`, `PayPeriodGapRequired`, `confirm_gap` and the banner are DELETED at `C17-c`. Worked on P80's own example -- paydays 2026-01-02, 01-16, 07-31 at cadence 14, `$3,526.00` gross per paycheck (R-PC47's figure): the 13 grid paydays between 01-16 and 07-31 carry `$45,838.00`, and under REFUSE the owner continues the old era (that income appears in the forecast) or states what happened, where CONFIRM leaves a 196-day paycheck at HTTP 200 with `$45,838.00` absent and six months of bills in one grid column. Rejected: keeping the C14-f confirmation (P80 stays open); an explicit `unpaid` era KIND now -- the honest model of unemployment, unrequested today, and one ref row plus one grid arm inside this same relation whenever it is asked for, which is R-PC58's "one relation, no migration over a column" argument working |
| pay_calendar | R-PC68 | -- | 2026-09-11 (developer) | **`C17-d` ABSORBS `recurrence:R13` AS AN IDENTITY CLASS.** The day-of-month cadence KIND -- `monthly` and `semi_monthly` members of `ref.pay_cadence_kinds`, `_grid.nominal_payday` / `cadence_steps_to` dispatching on the kind, `PayCadence` answering 24 / 12 without dividing -- is one commit under two names (conventions rule 11); **P78**'s eight fixtures go through the door there and **P78** closes with R13's tick; **N-399** (the capped-deduction cumulative's month walk per prior payday) is CARRIED by the class, since the kind answers a day-of-month owner's month ordinal structurally and whether that ends the per-payday walk for a fixed-days owner is the leaf's to measure, not this ruling's to assert. Re-derived cost of not having it: a semi-monthly owner on a 15-day walk is `365.2425 / 15 = 24.35` paychecks a year against a true 24, `0.3495 x $3,819.79 = ~$1,335` a year of over-modelled gross on a `$91,675` salary, with paydays drifting `365.2425 - 24 x 15 = 5.24` days a year off the 1st and 15th. `C14-d` already made `_grid` the ONE arithmetic body, so the branch lands in one module. Rejected: `C17` landing `kind_id` with one member and R13 staying a separate step at #131 (P78 would not close at C17); building the kind inside `C17-a` (a pure move and a money-moving producer in one PR) |
| pay_calendar | R-PC69 | -- | 2026-09-11 (developer) | **`C17` IS FOUR LEAVES, AND `_derive.py` SPLITS BEFORE THE MONEY LEAF.** `C17-a`: the relation, its backfill, the column drop, every reader taking the LATEST era's rhythm exactly where it took the row's (`$0.00` by construction; NARROWS **N-494** to the one top-up that restates an era, and closes N-492's write half). `C17-b-1`: `project_period_after` + `covering_projection` move from `_derive.py` (998 of 1,000, **PC-498**) to a new `_projection.py` between `_derive` and `_searches` -- a pure move, its own commit, both callers (`_views`, `_calendar`) above it and the one-way chain held. `C17-b-2`: readers ask the era covering their day and ANCHOR on its phase -- `derive_periods`' last end, `project_period_after`, `_backdated_paydays`, `_reject_backward_payday`'s floor -- **MOVES MONEY, OWN PR** (`$0.00` on production; worked: a last recorded payday of 2030-11-27, the nominal 11-28 Thanksgiving under `prior`, is projected a next payday of 12-11 today where the era's grid says 12-12, one paycheck and every boundary after it a day early). `C17-c`: the doors ask for the ERA -- registration and first-time generate the NOMINAL first payday + cadence + convention, `regenerate` the era mint (**R-PC64**), `reset` wiping eras -- closing **PC-504**, **P80**. `C17-d`: the day-of-month kind (**R-PC68**). Rejected: moving prose out of `_derive.py` instead of splitting (the C7-tier answer R-PC60 rejected once); folding `C17-a` into `C17-b` (a schema move and a money move in one PR; legal as a boundary, unlike **R-BAL14**'s, because `C17-a` leaves no half-moved home -- the old columns are dropped and every reader takes the latest era -- and the money move is then reviewed alone) |
| pay_calendar | R-PC70 | -- | 2026-09-11 (developer) | **A STORED ERA A MOVED HOLIDAY SET MAKES ILLEGAL IS REPAIRED AT THE ERA-MINT DOOR, AND NOTHING SWEEPS FOR IT.** Disposition of **N-493**, the row `C14-b`'s design review opened and no leaf of `C17` closed: if the federal holiday set ever gains a closed run of four or more days, an era whose cadence is that short under `prior` or `next` becomes one `derive_periods` refuses. The read path is already LOUD -- the calendar raises and the recovery page renders -- so what the owner lacks is a DOOR, and `C17-c`'s `regenerate` is it (the leaf `C17-c-2` since **R-PC71**): the owner states a legal pair and the calendar re-derives from the new era. The row re-points at that leaf and closes there once the recovery page offers the door. `$0.00` and unreachable today: the longest closed run over 2000-2100 is 3 days and production's one era is cadence 14 under `none`. Rejected: a sweep clause in `scripts/integrity_check.py` grading every `budget.pay_eras` pair against `shortest_collision_free_cadence` -- a reconciler over a fact that changes by act of Congress, buying nothing a loud read path does not (**R-PC59** already refused freezing the floor into a CHECK); leaving the row on the container, which ticks with `C17-d` and would orphan it |
| pay_calendar | R-PC71 | -- | 2026-09-11 (developer) | **`C17-c` IS TWO LEAVES: THE PURE-MOVE SPLIT OF `pay_period_write.py`, AND THE DOORS IT MAKES ROOM FOR.** `C17-c-1` moves part of `app/services/pay_period_write.py` -- at exactly pylint's 1,000-line ceiling after `C17-a`, ledger row **PC-507** -- into a sibling module, a `$0.00` pure move in its own commit and PR, ranked directly after `C17-b-2` because that leaf edits the same file; the CUT (which functions leave, and to where) is presented as a fork by the session that builds it and ruled then, as **R-PC60** placed `_grid` and **R-PC69** placed `_projection`. `C17-c-2` is what `C17-c` was: the doors ask for the era (**R-PC64**, **R-PC67**, **R-PC70**). Taken because the registry said `C17-c` "closes PC-507" while PC-507's own row said the split PRECEDES it with a placement owed, so a session picking up the rank could not tell whether to do the move inside the door rewrite or stop. Rejected: one leaf with the split as its first commit (the door PR carries a pure move in its diff, which R-PC69 refused for `C17-b`, and the cut is decided inside a money-adjacent step with no ruling); a standalone `C19` ranked before `C17-c` (the same work filed outside the container that needs it) |
| pay_calendar | R-PC72 | -- | 2026-09-11 (developer, from the full option space) | **THE PROJECTION IS PIECEWISE -- AN ERA PAYS FROM ITS FIRST PAYDAY TO THE NEXT ERA'S, SEAMS IN CASH DAYS -- AND A RECORDED PAYDAY STANDS FOR ITS NEAREST PLANNED ONE.** `C17-b-2`'s two forks. (1) When the latest era's first payday lies past every recorded payday (reachable: a truncate leaves every era standing), the days between the horizon and that day are projected on the era COVERING them (**R-PC66**'s letter), a projected period closes the day before the next era's first payday, and `period_index` counts across the seam. Worked: eras 14 days from 2026-01-02 and 7 days from 07-01, the record truncated to 03-13: the 03-13 paycheck ends 03-26, April-June project seven fortnightly paydays, July on is weekly. Seams are read in CASH days -- an era's span opens on its `effective_from` DISPLACED (`pay_calendar.era_index_at`) -- because an era minted on a nominal holiday under `prior` pays its first paycheck before its own `effective_from`. (2) A recorded payday is MATCHED to the planned payday nearest it (**R-PC47**'s own example: the 2025-12-31 that was the 2026-01-01 paycheck), the last period runs to the day before the following planned one, and a record exactly half a cadence from two planned paydays is read as the LATER one paid early, the poorer forecast. Worked on the catalogue's thirteen-day shape (01-02 and 01-15 at 14): 01-15 is the 01-16 paycheck paid early and the period ends 01-29; the literal "first grid payday after the record" would end it on 01-15 and project a 01-16 paycheck the owner never receives. Rejected: the latest era governing everything past the record (the last paycheck described by a rhythm that never paid it); a per-day lookup with no seam (overlapping projections); truncate retiring eras (deletes a stated fact; `C17-c-2`'s door question). `$0.00` on production under every option. **Seam clause REVISED at R-PC75** (`C17-c-2b`) |
| pay_calendar | R-PC73 | -- | 2026-09-11 (developer) | **`_eras.py` TAKES THE GRID LAYER BELOW THE DERIVATION, AS THE FIRST COMMIT OF `C17-b-2`'s OWN PR.** `app/services/pay_calendar/_derive.py` stood at 799 of pylint's 1,000 lines after **R-PC69**'s split, and `C17-b-2`'s era producers -- which `derive_periods` must close its last period through, and which need the displacement producer and the package's error type -- took it to 1,119. A new `_eras.py` between `_grid` and `_derive` takes `PayCalendarError`, `MIN`/`MAX_CADENCE_DAYS`, `validate_cadence` and `projected_payday` as a PURE move (identical bodies with docstrings stripped, by AST comparison, re-run independently by the adversarial review and again on the merged tree at the tick; `git diff -M` cannot show a partial extraction as a rename; commit `3369b2ab`), the era producers then landing beside them in the money commit `06fc0d33`, which also corrected `validate_cadence`'s refusal message from the `pay_schedule` names `C17-a` had made stale. The cut follows the SUBJECT: `_derive` keeps what derives periods from a record, `_eras` is the grid an era pays on, where `C17-d`'s cadence KIND branches. One branch, two commits, one PR -- **R-PC69**'s "its own commit" satisfied without a second PR. Rejected: moving only the error type and the producer (a weaker subject line); trimming `_derive.py`'s prose to fit (**R-PC60** / **R-PC69**'s refused shape) |
| pay_calendar | R-PC74 | -- | 2026-09-12 (developer) | **THE BATCH SHAPE LEAVES THE WRITER FOR `pay_period_batch.py`.** Ruling on `C17-c-1`'s cut (**R-PC71** owed it; **PC-507** named the seam without deciding it), taken with `pay_period_write.py` at 998 of pylint's 1,000 lines after `C17-b-2`. What a batch of paydays IS -- `PERIOD_BATCH_MIN` / `PERIOD_BATCH_MAX`, `reject_out_of_range_batch_size`, `_reject_undatable_payday`, `_requested_paydays`, `_reject_backward_payday` -- and the module docstring's record of ruling **R-PC1**'s two refusals move to a NEW sibling `app/services/pay_period_batch.py`; the three private names drop their underscore as the `C14-f` split of `pay_period_gates` did, since a function another module calls is that module's surface and a `protected-access` disable at every call site is a bare disable. What stays is what `TestThereIsOneWriter` pins to the writer -- the doors, `_PaydayChange`, `_apply`, the one `PayPeriod(...)` and the one `DELETE` -- so the one-writer census reads as before; the new module imports no model, issues no statement, and the dependency runs one way. `$0.00` under every option: a placement and rule-14 question, as **R-PC60** was. Rejected: the refusals alone, with `_requested_paydays` staying as the writer's producer (the batch's days and the floor that judges them split across two files); the floor alone (148 lines; `C17-c-2`'s mirror refusal under **R-PC67** and `C6` / `C18` all edit the floor, so the writer meets the ceiling a step later -- R-PC60's own test); `pay_period_gates.py` as the home (500 to ~800 lines, holding a batch producer that is not a gate beside gates that read rows); `pay_schedule_service.py` (its own text separates the two bounds by OWNER); the `pay_calendar` package (the read side, raising `PayCalendarError` that no route catches); moving the statements (the writer's identity); trimming prose (refused at **R-PC60**, **R-PC69** and ledger **P31**) |
| pay_calendar | R-PC75 | -- | 2026-09-12 (developer, from the full option space) | **THE SEAM BETWEEN TWO ERAS IS DRAWN WHERE THE DOOR THAT MINTED THE LATER ONE DREW IT: ITS FIRST PAYDAY REPLACES THE OLD ERA'S LAST PLANNED PAYDAY AT OR BEFORE IT.** Ruling on **PC-509**, REVISING **R-PC72**'s seam clause (an era pays to the day before the next era's first payday) and its 06-19 example. The writer's floor and **R-PC67** put a minted era's first payday F in `[P1, P2)` -- from the plan's next payday after the kept record up to, not including, the one after -- and the regenerate that mints it DELETES P1 and records F, so the last old paycheck runs from P0 to F-1. Read literally, R-PC72 re-invented P1 after any truncate below F (eras 14 days from 2030-01-03 and 7 from 02-22, record ending 01-17: 01-31, **02-14**, 02-22, where the regenerate wrote a 22-day 01-31 paycheck) and projected a ONE-day paycheck where F fell the day after a planned one (19 days from 01-19, 10 from 02-08: 02-07..02-07). Under this ruling `last_step_of` is the step before the last old planned payday at or before F, `validate_eras` refuses an era that would then pay nothing, and extend materialising the plan writes what the regenerate wrote. Rejected: NEAREST (F stands for the old planned payday nearest it, R-PC72's record rule reused) -- right only when F falls in the first half of `[P1, P2)`, re-inventing P1 in the second; the literal seam (every truncate re-invents a deleted paycheck, and a one-day phantom paycheck); truncate retiring eras (deletes a stated fact, refused at R-PC72). **MOVES MONEY** for a piecewise owner truncated below a later era; `$0.00` on production (one era). Built at `C17-c-2b` (`36c6b6af`) |
| pay_calendar | R-PC76 | -- | 2026-09-12 (developer) | **THE SKIPPED-PAYCHECK REFUSAL BINDS AT EVERY BATCH WITH A SURVIVOR, IN `pay_period_batch` BESIDE THE FLOOR.** Ruling on **R-PC67**'s placement. `reject_skipped_paycheck` refuses a batch whose earliest new payday falls at or past the SECOND payday the owner's plan projects after the surviving record (`pay_calendar.planned_paydays_after`, the sequence the floor's day is the first of), asked of every batch by the one writer whichever door sent it; `reject_unconfirmed_gap`, `Confirmations`, `PayPeriodGapRequired`, `confirm_gap` and the banner are deleted whole, and `SpanReplacement` with them. **It reads the eras the batch leaves STANDING** (its review's finding): a retired era takes its plan with it, and against the stored plan a rebuild retiring a 30-day era wrote a 35-day paycheck on a fortnightly plan; the floor keeps the stored eras, where the paycheck the owner HAS ends. Every live door is covered either way -- registration, first-time generate and reset have no survivor; regenerate states a start beside a kept record -- so the difference was the test corpus: 253 tests in 36 files built a hole (218 a 2026 block beside `seed_user`'s 2024-01-05 opening payday), and they move to ONE test-only helper, `tests._test_helpers.record_paydays_across_a_hole`, which writes the rows directly, mints the era the writer would, and REFUSES a batch the writer would accept. Rejected: keying on a supplied `retiring_ids` (spares the corpus; a future appending door inherits nothing -- the per-door shape **P80** was born from). `$0.00`: a refusal. **Transitional, stated**: until `C17-c-2b` the extend door restates the LATEST era (PC-509), so an owner truncated below a longer-cadence era meets this refusal on the rolling top-up from `/grid` with no handler -- loud where it wrote P80's hole silently; not in production's data, two posts away for any owner, so 2a and 2b DEPLOY TOGETHER |
| pay_calendar | R-PC77 | -- | 2026-09-12 (developer) | **N-493 IS DEFERRED TO ITS OWN CALENDAR LEAF, `C17-e`; R-PC70'S PREMISE MEASURED FALSE AND THE RECOVERY-PAGE CLAUSE IS DROPPED.** Ruled on a measurement (`shekel-handoffs/c17c2-probes/n493_probe.py`, re-run by the building session): with the federal holiday set given a four-day closed run (Fri 2030-03-08 .. Mon 03-11) and a stored era at cadence 4 under `prior`, NOTHING raises -- steps 1 and 2 both displace onto 2030-03-07, `derive_periods`, `payday_after`, `span_containing` and the axis all answer, one projected paycheck silently vanishes and `_ordinal` runs one high, which the recurrence reads (`period_index % interval_n`). **R-PC70**'s "the read path is loud, so what the owner lacks is a door" is therefore false: no state reaches the recovery page to offer a door from, and `C17-c-2` builds none. The from-scratch fix is the calendar's -- two nominal paydays a closed run displaces onto one cash day are ONE paycheck everywhere an era's grid is read -- after which `reject_shift_on_short_cadence` is a fence the calendar no longer needs; that is `C17-e`, ranked after `C17-d`, owning **N-493**. Rejected: building the coalescing inside `C17-c-2` (widens a doors step to the derivation); adding the door to the recovery page on the false premise. `$0.00` and unreachable today: the longest closed run over 2000-2100 is 3 days |
| pay_calendar | R-PC78 | -- | 2026-09-12 (developer) | **`C17-c-2` IS TWO LEAVES AND TWO PRs.** `C17-c-2a` (`$0.00`): the skipped-paycheck refusal and the gap confirmation's deletion (**R-PC67**, **R-PC76**) with the scheduled-payday wording of the four doors (**PC-504**), closing **P80** and **PC-504**. `C17-c-2b` (**MOVES MONEY**, own PR, **R-PC69**'s precedent): extend and the rolling top-up MATERIALISE the plan through a continuing writer entry that mints and retires nothing, deleting the top-up's restatement of a truncated era, and the seam rule **R-PC75**, closing **PC-509** and **N-494** and carrying **N-496**. `C17-c-2` is the decomposed parent and ticks with `2b`; `C17-c`, its own parent, ticks with it. Rejected: three PRs, one per job (two `$0.00` jobs need no separate review); one PR with three commits (the money commit reviewed inside a PR carrying two `$0.00` jobs, the shape R-PC69 refused for `C17-b`) |
| pay_calendar | R-PC79 | -- | 2026-09-13 (developer) | **SEMI-MONTHLY IS TWO OWNER-CHOSEN DAYS OF THE MONTH, MONTHLY IS ONE, AND THE FLOOR IS ASKED OF THE ERA'S SHORTEST GAP.** Ruled on `C17-d`'s design fork with worked dates. A day-of-month era pays on a stated day, 1..31, where 29-31 mean "or the last day of a shorter month"; a semi-monthly era pays on TWO such days the owner states (1st/15th, 15th/last, 15th/30th, 5th/20th, 10th/25th are all expressible), and a pair whose lower day is 28 or more is REFUSED because both clamp onto one day in February. The era's `effective_from` stays its first NOMINAL payday; where that month was too short to hold the meant day, the day the era MEANS is stored beside it exactly as `recurrence:R-R3` stores a rule's `nominal_day` (29..31, only when the date could not carry it). Every theorem the package leans on rests on "a displacement is shorter than the gap between two grid days", never on the gap being constant, so `reject_shift_on_short_cadence` compares the collision floor against the era's SHORTEST GAP: a fixed-days cadence's day count, 28 for monthly, and for a semi-monthly pair `(lo, hi)` the closed form `min(min(hi, 28) - lo, 28 - min(hi, 28) + lo)` (1st/15th 14, 15th/last 13; checked against a brute-force walk over 2024-2027, 0 disagreements). Re-derived cost of the 15-day walk it replaces: 25 paydays in 2026 from 2026-01-01 against a true 24, `$3,819.75` of over-modelled gross on `$91,675`, paydays on the 16th and 31st for an owner paid on the 1st and 15th. Rejected: two fixed conventions only (cannot express 5th/20th); a second day derived as the first plus 15 (cannot express both 1st/15th and 15th/last). |
| pay_calendar | R-PC80 | -- | 2026-09-13 (developer) | **ABSENCE IS THE DISCRIMINATOR: `budget.pay_eras.kind_id` AND `ref.pay_cadence_kinds` ARE DROPPED WITH THE COLUMNS THAT MAKE THEM DERIVABLE.** Revises the LETTER of **R-PC58** and **R-PC68** ("members of `ref.pay_cadence_kinds`"), whose template was `budget.recurrence_rules.unit_id` -- necessary THERE because week, period and month rules share one parameter shape. Once the month kinds' parameters exist the three kinds have DISJOINT stored shapes: every-N-days is `cadence_days` present, semi-monthly is the other day present, monthly is neither (`nominal_day` beside either month kind only when the first month was too short). The kind is then readable off the row, so a stored `kind_id` is a derived value beside its source (rule 14, **balance:R-IY**) that no CHECK can tie to its columns without pinning a seed id (**R-PC59**), and a row saying `fixed_days` with no cadence, or `monthly` beside `cadence_days = 14`, is storable and needs a reader-side refusal to hold it. Under this ruling CHECKs make every storable row a legal era and there is no fence; the kind is the value's Python type (`FixedDays` / `Monthly` / `SemiMonthly`), dispatched on in `pay_calendar._grid`; a form's kind control posts a plain wire value the schema maps to a type. The counter-case (**N-241**: an explicit discriminator survives a future kind sharing a shape) was weighed: a payroll kind's parameters are a day count or days of the month, no two share a shape, and a leaf that ever needed a discriminator would add it with the kind that needs it. `C17-d-1` deletes `kind` from the VALUE and maps the type to the stored member at the one writer; `C17-d-2`'s migration drops the column and the table. |
| pay_calendar | R-PC81 | -- | 2026-09-13 (developer) | **`C17-d` IS THREE LEAVES AND THREE PRs, AND THE CONTAINER TICKS WITH THE LAST.** `C17-d-1` (`$0.00`): the cadence VALUE's shape -- `Rhythm.cadence` a `FixedDays` value, `Era.kind` gone from the value, `_grid` dispatching on the value's type, `PayCadence` taking the value, every caller re-spelled -- a pure refactor reviewed as one (**R-PC73**'s precedent). `C17-d-2` (**MOVES MONEY** for a month-kind owner, `$0.00` on production, own PR): the `Monthly` and `SemiMonthly` kinds and their grid arithmetic, the migration (**R-PC80**: `cadence_days` nullable, `nominal_day` and the other day added, `kind_id` and `ref.pay_cadence_kinds` dropped), the era reader and writer, `PayCadence` answering 12 and 24 without dividing, **P78**'s eight fixtures through the door, **N-399** measured; `recurrence:R13` ticks with it. `C17-d-3` (`$0.00`, own PR): the four schedule forms and registration offer the kind. Rejected: two PRs (the money diff reviewed beside four template edits); one commit (the literal reading of **R-PC68**'s "one commit", reviewable only as a whole and spanning sessions). Shipped: `C17-d-1` at `7a6bbaf5`. |
| salary | R-SAL11 | -- | 2026-09-05 (developer) | **THE HORIZON A RECURRING RAISE DECAYS OVER IS A FACT ON THE RAISE, NOT A SETTING ON THE OWNER.** `salary.salary_raises` gains a nullable `terminal_year`, NULL meaning believed indefinitely, and `auth.user_settings.merit_raise_horizon_years` -- one number for the whole owner, applied by `/retirement` alone and keyed by raise TYPE -- is deleted. Taken via the option label the developer selected, *end year stored on each raise*. Three measurements decided it. **(1) THE CUTOFF SLIDES**: all three call sites pass `as_of.year` as `start_year`, so it moves every January -- the 2040 salary reads `$161,595.26` from a 2026 render, `$182,830.20` from 2031 and `$201,810.34` from 2036, **and that last figure is exactly what believing the raise forever gives**; the pension reads `$8,541.71`/mo today and `$11,936.54`/mo from a 2041 render. The rule is not a claim that merit raises stop, it is a tail discount that evaporates as the tail arrives. **(2) IT IS A SET DEFINED BY SUBTRACTION**: `_terminate_after_horizon` reads *None if recurring and cola else cutoff*, so a NON-RECURRING COLA is terminated by a rule naming only merit and custom. A second member was claimed and is FALSE -- `raise_type_id` is `nullable=False`, and that false member originated in the docstring at `S3-a` and propagated by being quoted. **(3) A RAISE DATED PAST THE CUTOFF NEVER HAPPENS**: a one-time `$8,000` promotion recorded for 2035 under a 2031 cutoff leaves the 2040 salary at the `$91,675.00` base. Rejected: believe forever (moves the pension `+$3,394.83`/mo and `$28,707.44` of employer contributions over 2026-2046); teaching today's rolling rule to the engine (dominated, keeps 1 and 3); re-anchoring the global setting on each raise's own effective year (keeps 2, adds an off-by-one). |
| balance | R-BAL4 | -- | 2026-09-05 (developer) | **A READER THAT ANSWERS "WHAT DID THIS ROW ACTUALLY COST" MAY NOT FALL THROUGH TO THE PLAN COLUMN.** The fall-through is the amount model's, so an unsettled row's actual and its estimate are one call on one row against one basis and their delta is zero BY CONSTRUCTION rather than by two producers agreeing. Supersedes **BAL-462**'s "the one external survivor is correct and stays" clause, which two neutral reviews of `X-bu` measured false: `settled_figure` returns `None` IFF the row is unsettled, so that arm served exactly one population, and for an UNSETTLED DERIVED row it read the emptied column and raised `AmountUnresolvable` while the estimate half beside it resolved -- BAL-462's own failure mode on the other population, held off only by `query_settled_expenses`' status filter |
| salary | R-SAL12 | -- | 2026-09-05 (developer) | **THE `S3-c` CUTOVER MIGRATION BACKFILLS THE STORED END YEAR RATHER THAN DROPPING THE SETTING.** Not backfilling deletes `merit_raise_horizon_years` without carrying its value, silently replacing a stated belief with one the owner never gave: pension `$8,541.71` -> `$11,936.54`/mo (**+$3,394.83**) and 2046 salary `$192,953.19` -> `$279,453.75` (**+$86,500.56**), measured through `apply_raises` and `calculate_benefit` on the developer's own data. Backfilled, the projection does not move at all. The backfill writes only where `terminal_year IS NULL`, so a re-upgrade cannot overwrite a year the owner set by hand |
| salary | R-SAL13 | -- | 2026-09-05 (developer) | **A NEW RECURRING RAISE IS ASKED HOW LONG IT IS BELIEVED, WITH NO DEFAULT.** A blank answer would silently mean *forever*, which is the stated-belief-replaced-by-an-unstated-one failure `R-SAL12` refuses one layer down. The mode is authoritative in `drop_end_year_mode`, so a year box disagreeing with the chosen mode is not a representable state and the validator arm that policed the disagreement is DELETED -- rule 14's "delete a home rather than keep two in step". Since `S3-f-2b` (`587c20d5`) the vocabulary and the ONE end-year rule live in `app.services.salary_raises` (`end_year_of`), where the salary form and the `/retirement` rail's per-raise probe both reach them. |
| salary | R-SAL14 | -- | 2026-09-06 (developer) | **A PROFILE'S PAYCHECKS ARE A FUNCTION OF THE PAYDAY, NOT A LIST WITH A HORIZON.** `S3-d` was specified as *"`income_service.project_profile` takes a HORIZON and prices to it"*; the developer asked what the from-scratch design was and took the answer. A horizon is only ever the answer to *how long should the list be*, and ONE RENDER ASKS TWO LENGTHS of it -- the balance seam wants `calendar.saved()`, the 40-year chart wants forty years, and they share one memo, so a memo keyed by profile alone is filled at one span and read at another. **Nothing has to decide, because a paycheck does not depend on any other paycheck**: `calculate_paycheck` rebuilds its own month position and both cumulatives off `basis.calendar`, whose rhythm runs forward past the horizon at the owner's cadence, so pricing N paydays costs the same asked one at a time as in one batch. The producer becomes a MAP from payday to paycheck and a caller asks for the paydays it reads. **Pinning ONE horizon on the read pass is UNBUILDABLE, not merely worse**: `/savings` derives its horizon from `loan_payoff_outlook(account_data)`, and that data is the projections the pass itself produces, so the route cannot know its horizon before opening the pass. Also rejected: a horizon per caller keyed `(profile, horizon)`, which runs the engine twice per profile and fails the one-per-render gate; and one app-wide horizon, which makes every non-projecting page pay ~1,040 paydays for a map it reads 63 entries of. **What it deletes rather than checks**: the horizon argument, any rule about who agrees on it, `payday_by_period_id`, and -- at `S3-e` -- `AccountPayrollFeed`'s hold, `salary_basis(beyond=)` and `build_employer_salary_basis`. It does NOT close **P63**: `SalaryPricing` still derives a second pricer, because `cash_ledger.amount_basis` takes an owner and a scenario and has nothing to hand it, which that row records as **C12**'s object and not a reader move. |
| salary | R-SAL15 | -- | 2026-09-06 (developer) | **A PAYROLL FEED PRICES A PAYDAY ON DEMAND; IT DOES NOT CARRY A WINDOW.** `S3-e` was specified as *widen the ask, then make `employee_at` a lookup that RAISES past the horizon* -- every `load_payroll_feeds` caller stating the periods it will read. **That reintroduces at the FEED tier the defect `R-SAL14` deleted one tier down**, and two callers demonstrate it rather than merely risking it. `/retirement` loads ONE `ProjectionBatch` at the base retirement date (a 516-payday axis on the developer's data) and RE-PROJECTS it against every retire-later probe axis, up to `_MAX_DELAY_MONTHS = 180` (907 paydays) -- so the feed is filled at one span and read at another, and a raise there is a 500 on a money page. Priming at the cap instead makes `retirement_plan.load_retirement_inputs` depend on a constant `retirement_levers` owns, which is the *who agrees on the horizon* rule `R-SAL14` deletes rather than checks. `/investment` builds the feed BEFORE the horizon slider is read, so it must prime at 40 years (1,040 paydays, measured 428 ms) to serve a 1-year slider. **The remedy is that the feed carries two RESOLVERS the loader builds over the pass's pricer**, so there is no window to outrun, no ask to widen and no raise: the fence is unrepresentable rather than deleted. Cost is NOT the argument either way -- `_retire_later_lever` probes `+180` on nearly every render unless the owner is already funded at month 0, so both designs price ~907 paydays there. Taken via the option label the developer selected, *price on demand, feed stays put*: the feed stays in `investment_projection`, whose *no database access* contract the loader preserves by supplying the callables, at the stated cost that a lazy ORM load could now fire mid-walk (none does: `_load_funding_profiles` eager-loads both relationships). Rejected: moving it to `projection_inputs`, the same design paid for with an import sweep; and the window-and-raise design above. |
| salary | R-SAL16 | -- | 2026-09-06 (developer) | **`S3-e` SPLITS AT THE MONEY LINE: THE WINDOW QUESTIONS MOVE FIRST, IN A COMMIT WHERE A MOVED FIGURE IS A DEFECT.** Two members of `AccountPayrollFeed` answer questions that only mean anything while the feed has a fixed window -- `prices(payday)` (*did the engine price this day*) and `models_employee` (*did any priced payday pay this account*) -- and `R-SAL15` takes the window away. `prices()` is the dangerous one: its ONE reader gates whether an account's average recurring TRANSFER is added to a period, so once the feed answers every payday it reads `True` everywhere, the term is never added again, and every account funded by BOTH a deduction and transfers silently loses its whole recurring-transfer stream from the forward walk for the entire horizon. **It was in NO census** -- zero mentions in `steps.md` or `implementation_plan_salary.md` when the step was written. `S3-e-1` re-homes both while the ask is still `calendar.saved()`, so the boundary can be graded against a state where the two disagree; `S3-e-2` then takes the window away and moves the money, in its own PR with its own harness. Rejected: one commit, which lands a provable no-op and a `+$193,855.37` repricing in one diff and lets neither be measured on its own. |
| salary | R-SAL17 | -- | 2026-09-06 (developer) | **`models_employee` IS DELETED FOR `is_payroll_linked`: THE GATE IS PRESENCE, NOT PRICE.** It asked *did payroll pay this account on any PRICED payday*, which under `R-SAL15` means *on any payday THIS CALLER ASKED FOR* -- so one account models money for one consumer and nothing for another, an answer that depends on the reader. The PRESENCE fact does not move, and `build_contribution_timeline` already gates on it one tier over, so the two consumers ask one question. **It moves no figure**, and the reason is structural rather than a coincidence of today's data: `models_employee` IMPLIES `is_payroll_linked` (an employee amount exists only where a deduction does), so the only new state is a linked deduction pricing `$0.00` on every payday with no employer contribution -- which builds a plan whose every event is zero, and `contribution_events` emits an event only for a NON-ZERO amount. Measured byte-identical across 8 accounts x 63 periods of `balance_map` and three `/retirement` plan points on a production clone, with a `$250` deduction and three `$400` transfers armed onto an investment account so the path was live. Cost: one recorded-contribution query for an account that models nothing. Rejected: keeping the price gate scoped explicitly to `calendar.saved()`, which gives the horizon a second home on the very value `R-SAL15` removes it from. |
| salary | R-SAL18 | -- | 2026-09-06 (developer) | **A PERIOD ANSWERS *AM I PROJECTED* ITSELF; NOTHING PASSES THAT BOUNDARY DOWN.** `S3-e-1` moved the transfer-average boundary off `AccountPayrollFeed.prices()` and onto a `saved_through` date the CALLER supplies (**R-SAL16**), which is correct as far as it goes and leaves the boundary, the feed and the axis as three inputs nothing ties together -- an unenforced pairing fenced by an AST census over the two call sites. **The value already carries the fact**: `DerivedPeriod.period_id is None` IS *projected past the horizon*, because `_views.axis_window` builds the saved half from materialised rows and the projected half from `projected_paychecks`, which opens at `horizon + 1` and stamps `period_id=None`. So `S3-e-2` adds `DerivedPeriod.is_projected`, a NAMED accessor over that, and the timeline asks a DOMAIN question: the `saved_through` parameter, both call sites' wiring and the census all go, and a mismatched pair stops being representable rather than being checked. **The accessor is what makes this legal, and reading `period_id` raw is not**: `pay_calendar:C2-f2c` deliberately stopped this function knowing how a period spells its primary key -- `id` on an ORM row, `period_id` on a `DerivedPeriod` -- and a named property preserves that, since `budget.pay_periods` grows the same member returning `False` (a saved row is by definition not projected) and both types keep serving. Taken via the option label the developer selected, *add DerivedPeriod.is_projected*, at the stated cost that it adds a member to a value the whole app reads. NO FIGURE MOVES either way: both spellings answer the same days. Rejected: reading `period_id is None` raw, the smallest diff, which breaks C2-f2c's contract and the one ORM-row caller with it; passing the `PayCalendar` itself, which narrows the pairing instead of deleting it and hands a pure module a larger value than it needs; and keeping `saved_through`, which leaves the fence standing. |
| salary | R-SAL19 | -- | 2026-09-11 (developer) | **A PAYROLL FEED'S RESOLVER TAKES THE PERIOD, NOT ITS PAYDAY, AND BOTH PRESENCE FACTS ARE THE RESOLVERS' PRESENCE.** `S3-e-2` builds the feed **R-SAL15** ruled -- two resolvers the loader closes over the read pass's `ProfilePaychecks` -- and the paycheck engine prices a `DerivedPeriod`, reading `start_date` AND `period_id`, so the feed's input is the engine's input: `employee_at(period)` / `gross_at(period)`, and `salary_basis()` dissolves because `gross_at` IS the growth engine's `period -> gross` hook. Keying by date was an artifact of the deleted dictionaries; keeping it re-derives the period from a date the caller just peeled off it, behind a fence refusing dates that are not paydays. **It REVISES one clause of R-SAL18**, *both period types keep serving `build_contribution_timeline`*: an ORM `budget.pay_periods` row spells its key `id` and cannot reach the engine. Measured first: all six production sites pass `DerivedPeriod`s (since `pay_calendar:C2-f2d-3`), so the clause protected nothing, and `PayPeriod.is_projected` is NOT added. R-SAL18's core stands. **The same sitting derived `is_payroll_linked` and `funds_employer` from the resolvers' presence** (rule 14, one home each): the loader builds each resolver exactly when its fact exists, so a constructor cannot hand the value a wrong answer, where `S3-e-1` had to make the carried flag required for the same reason. One state moves -- `funds_employer` reads True for an owner who NAMED a funding profile and has no saved payday, where `bool(gross_by_payday)` read False -- and nothing prices it; the adversarial review corrected a first claim that `is_payroll_linked` moved (it was carried off the rows and did not). Taken via the option labels *take the period (root cause)* and *derive from the resolver*. Rejected: keeping the date (a round trip plus a fence, honouring a clause with no caller); carrying the flag beside the resolver (two homes). |
| salary | R-SAL20 | -- | 2026-09-11 (developer) | **THE PAYCHECK ENGINE'S BASIS NAMES THE RAISE SET IT PRICES FROM.** Ruling on `S3-f`'s first fork, the STOP the step opened on: since `S3-e-2` the payroll feed prices every account projection through the engine, which read the raise rows off the ORM object at three sites, so a what-if over one raise's end year had no input to arrive through. `PayrollBasis` gains the raise set as an INPUT, the profile's rows unless a caller supplies terms, resolved on READ so no constructor changes; the three engine sites and `get_raise_event` read `basis.raises`; `PaycheckPricing.for_profile` keys its memo on the believed set so the stored render still builds ONE pricer and ten retire-later probes at one set share one; `load_payroll_feeds` splits into once-loaded WIRING and a per-raise-set BUILD. Worked on a `$91,675.00` owner probing *ends after 2031* on a 2.5% merit raise: the 2046 salary `$150,220.16 -> $103,721.85`, the pension `$7,005.59 -> $4,957.04`/mo, the employer 5%-of-gross over 2027-2046 `$120,017.82 -> $102,489.40` -- the last row is the engine's. Rejected: a forwarding profile proxy (implicit input, same memo-key change anyway); threading an end-year map through `apply_raises` (a parameter shadowing a field on the engine's own input); reaching the pension and the target but not the feed (two beliefs on one verdict, `$17,528.42` of employer contributions plus growth unmoved); mutating the session row without commit (an autoflush writes the probe). Shipped at `S3-f-1`, where two adversarial reviews turned the terms into a VALUE (`RaiseTerms`, the engine's whole contract with a raise, canonical as a memo key) and moved a raise type's name onto the FK through the ref cache, because a never-flushed row -- the X-bl control's own -- has no relationship to read. The probe that ASKS for a set shipped at `S3-f-2b` (`587c20d5`): `PlanPoint.terms_for` is the one producer of a believed set and every salary-path read on the picture takes it. |
| salary | R-SAL21 | -- | 2026-09-11 (developer), AMENDED 2026-09-12 on measurement | **THE CURRENT PAYCHECK IS DERIVED AT THE PLAN POINT -- THROUGH THE PASS'S PRICER, CALIBRATED, AS ITS OWN MONEY LEAF.** `_compute_current_pay` prices one paycheck at load under the STORED raises and supplies the take-home rate the income target scales by; a probe can move this year's paycheck when the probed end year precedes this year, which the column and the salary form both admit, so the current paycheck comes off the point's pricer and the picture at any point is consistent by construction. Rejected: leaving the snapshot load-time and documenting the inconsistency; refusing end years before this year on the rail (a fence the form and the CHECK do not share). **The option was taken on a sentence that read *nothing moves at the stored point*, and that sentence was FALSE**: measured 2026-09-12 on the developer's data, `/retirement`'s direct door prices the 2026-09-10 paycheck with NO calibration (net `$2,541.49`, taxes `$355.14`) while the pass's pricer prices it with one (net `$2,572.78`, taxes `$323.85`), and through `picture_at` that is required savings `$1,120,707.00 -> $1,162,269.00` (**+$41,562.00**), pre-retirement net `+$138.54`/mo, funded `0.7417 -> 0.7152` -- **P62**'s *they agree* measured wrong. The developer re-ruled the same day: `/retirement` prices its current paycheck as every other surface does, one calibrated producer, shipped as its OWN money leaf (`S3-f-2a`) with its own harness and PR, taking `C12`'s `/retirement` half; the probe (`S3-f-2b`) reads that pricer at the point. Rejected then: a money-neutral leaf re-running today's uncalibrated door per point and filing the gap for `C12`; keeping the load-time snapshot. Shipped at `S3-f-2a` (`f3032c87`): the harness moved exactly the figures above and nothing on `/savings` or `/investment`. The probe: `S3-f-2b` (`587c20d5`). |
| salary | R-SAL22 | -- | 2026-09-11 (developer) | **THE RAIL'S SAVE IS A WRITE-THROUGH ARM OF `retirement.update_settings`**, beside the pension-date one the date row already has: the raise row posts its id, the mode and the year; the raise must resolve to this owner's profile (404 otherwise); ONE end-year rule -- the mode authoritative, the year not before the raise's effective year and not past 2100, spelled once in `RaiseCreateSchema.validate_end_year` against the payload's effective year and shared with the rail against the ROW's -- validates it; a 422 re-renders the rail with the error on that row; the write goes through the ORM and the rail re-renders, then the existing post-save reload. Rejected: a new `POST /retirement/raises/<id>/end-year` (a second door and a second re-render for one rail); reusing `salary.update_raise` (it validates the whole raise payload and redirects to the salary page). Its leaf is `S3-f-3`. The ONE rule is `salary_raises.end_year_of(mode, year, effective_year)` since `S3-f-2b` -- all three clauses, the ceiling included -- called by the salary schema and the rail's probe; the Save calls it against the row. Shipped at `S3-f-3` (`a5ef1bdf`): the pair is resolved against the rows the rail LISTS, the refusal is a designed 422 on the row's control, a stale race a 409 on the row. The rail row carries NO version counter (developer, 2026-09-12): a one-column Save from a rail rendered before the same raise was edited elsewhere is last-write-wins on that column -- the owner typed the year while looking at the row and the rail re-renders fresh after every Save -- where the salary form's whole-payload submit refuses the race; the flush-time lock still catches a true race. |
| salary | R-SAL23 | -- | 2026-09-11 (developer) | **`S3-f` IS THREE LEAVES, THE ENGINE SEAM FIRST.** Nothing in the step moves a stored figure -- a probe is a what-if and the stored point is byte-identical throughout -- so the split is by what each leaf can be GRADED on: `S3-f-1` the engine seam, graded on byte-identical output at the stored set; `S3-f-2` the plan point, the probe and the current paycheck at the point; `S3-f-3` the Save and the shared end-year rule. Each its own review; PRs as the coordinator orders. Rejected: two leaves (seam and probe together, one larger review); one PR. Taken via the recommended option's label; the earlier reply to *how would you design this from scratch* (2026-09-06) was never recorded, which is why the four rulings above were re-derived and written down before anything was built. *The premise that nothing moves held for the seam and the probe and not for the current paycheck: `S3-f-2` split again on 2026-09-12 at the money line (**R-SAL21** as amended), on **R-SAL6**'s rule.* |
| balance | R-BAL5 | -- | 2026-09-06 (developer) | **A READER THAT ANSWERS "WHAT DID THIS ROW'S MONEY DO" REFUSES AN UNSETTLED ROW RATHER THAN PRICING ITS PLAN, IN EITHER SPELLING.** `row_valuation.owned_contribution`'s fall-through onto `estimated_amount` is DELETED and the accessor renamed `settled_contribution`; it is NOT re-pointed at the resolver. **Narrows R-BAL4 rather than contradicting it**: that ruling sends a fall-through to the amount model and governs a reader comparing a PLAN against an ACTUAL, where the plan IS the answer wanted; these seven readers want no plan at all -- `settled_cash_leg` means CONFIRMED cash effect, so pricing a projected row's forecast there publishes a plan as a fact, the substitution `X-au-c3` exists to remove and the one `settled_figure` already refuses. **Supersedes the X-bx specification** in `steps.md` and the balance README, both of which read *"put the SEVEN readers on the one producer"*. Worked on two rows: a Projected loan shadow whose plan is DERIVED raises today and raises after, where routing answers `$565.36` and books a payment that never happened; a Projected bill OWNING `$120.00` was priced silently before, is priced silently by routing, and refuses now -- so routing fixes the two-spellings defect and none of the money, while converting today's loud failure into a silent one. Rejected: routing to `resolve_transaction_amount` (an `AmountBasis` threaded through ~14 functions in 8 packages, provably unread on every one, and the loan writer must acquire an owner id it does not hold); a `SettledRow` wrapper type, which RELOCATES the check rather than deleting it -- `posting_service` settles one row and must mint one, and `settled_figure` still raises on a broken record; and seven inline refusals, one per reader. **What it deletes is a fence**: the seven-caller census the accessor's docstring carried, which had to be re-run at every per-kind cutover that widened the derived class |
| balance | R-BAL6 | -- | 2026-09-07 (developer, from the full option space) | **A CARRIED-FORWARD LEFTOVER TAKES THE DATE ITS DEFINITION'S OWN RULE GIVES FOR THAT PAYCHECK**, from `recurrence_engine._plan.compute_due_date` -- the app's single producer of that date -- and where the rule has been CLEARED it takes the paycheck's start date, which is that same producer's answer for a cadence naming no day of the month, so the two arms are ONE rule. The CHECK is `template_id IS NULL OR due_date IS NOT NULL`, invariant under the chooser's declare, needing NO guard because no writer sets `template_id` on an existing row. Rejected: the paycheck's start date unconditionally (`$180.00` against `$210.00` on the worked example), its last covered day, dropping the template link, and guarding the chooser. |
| recurrence | R-R63 | -- | 2026-09-08 (developer) | **AN UNDATED ROW IS RETAINED AS A CONFLICT, NEVER DELETED AND NEVER SKIPPED SILENTLY.** Finding **REC-516**: `classify_maintain_work` read *this row answers no occurrence* as *the rule dropped this occurrence* and hard-deleted a mutable, record-free row whose `occurs_on` was NULL -- and the create arm refilled the freed occurrence in the same pass, so no row count could see it. Those are different facts: answering no occurrence is a property of the ROW, the rule dropping one is a property of the RULE, and only the second licenses a deletion. **RETAIN was chosen over a silent SKIP on the figures**: the definition has moved past such a row and cannot be applied to it, so a template repriced `$100.00 -> $250.00` and renamed leaves the row at `$100.00` under its old name for good, while also suppressing the correctly-priced row its paycheck would otherwise receive. `retained_ids` is the word this pass already had for *the definition moved and I refused to apply it*, and the pre-fix code already raised it for the undated rows carrying the owner's records -- so this RESTORES that prompt rather than inventing one. The first draft argued for the silent skip on the ground that a conflict asks a question the root fix deletes; the adversarial review refuted it by finding that question already being asked. **The retain is scaffolding and `R19-b` deletes it**: when a template-linked non-override row always records an occurrence the branch is unreachable rather than merely quiet. Rejected: skip silently (the stale figure, unreported); skip but apply the template's derived fields, which needs a rule for which date prices a row that answers no occurrence; and deferring the whole question into `R19-b`, which leaves a silent-wrong-figure path open for its duration |
| balance | R-BAL7 | -- | 2026-09-09 (developer, from the full option space) | **A LOAN'S PAYMENT FEED HAS ONE DATE PRODUCER AND ONE SETTLED-HISTORY DERIVATION, AND THE STEP SPLITS AT THAT SEAM.** Ruling on **N-432**'s unruled remedy. The oracle's reference loaded 98 modules (at `e74f7d6d`; 101 after this step's package split) to read three dates per payment and no amount -- measured: `$176,719.77` with the amounts and `$176,719.77` without -- and the root cause was one table down: `get_payment_history` was the only producer of *which installment each payment satisfies*, and it also priced. Refused: a literal `LoanInputs` (a hand-rolled replay), a `prices=False` flag (an override parameter routing around a wrong shape), and splitting `PaymentRecord` by settled-ness -- the developer's own reason, that a payment settled AFTER the read date still needs its amount for `_build_monthly_override`. Built: `loan_ledger.payment_installments` answers the dates alone, `amortization_engine.schedule_dates` owns the biweekly-collision slot for BOTH feeds (a slot the two could disagree on is a month the replay consumes and the forward override plans separately -- measured: split the redistribution sets and one planned payment is silently dropped), and `loan_loaders.income_shadows` is the single settled/projected partition, TOTAL and refusing a status it cannot place. **TWO LEAVES**: `X-bl-2a` ships the producers, `X-bl-2b` moves the oracle and the five other suite copies onto them, because `PaymentRecord` composing its installment would change ~50 construction sites in the same diff. |
| balance | R-BAL8 | -- | 2026-09-09 (developer, refusing the question's premise) | **THE CALLER STATES EVERY LOAD THAT COSTS A ROUND TRIP; A ROW LOADER DOES NOT DECIDE WHAT ITS CALLERS WILL TRAVERSE.** The developer was asked to choose between three ways of paying for a second eager-load chain and REFUSED the premise -- *these seem to be working around a bigger root cause* -- which is the ruling. `loan_loaders.query_shadow_income` baked `pricing_load_options()` (the amount model's five chains) into itself, so every caller paid for one caller's reads: FOUR of its SEVEN consumers never touch that chain (`loan_posting_service._reader`, `confirmed_shadows_through`, `walk_loan_ledger`, `_settled_payment_due_dates`), and finding **N-296** is the same defect inverted -- seven batch callers reaching a loader WITHOUT it and paying a query per definition. `options` is now a required keyword, so a new caller decides rather than inheriting a guess; a `joinedload` of a ref row stays with the loader because it rides on the same SELECT and costs no trip. Measured on dev: the fold's walk 11 statements to 7, the posting reader 19 to 11, the DISPLAYED loan balance 25 to 21, and the priced feed unchanged at 9. The seam is graded by a DISJOINT pair (`TestTheCallerStatesItsOwnEagerLoad`) that COUNTS statements: a presence check passed a mutant that deleted the options entirely, because a lazy load reads the same table one row at a time. |
| balance | R-BAL9 | -- | 2026-09-09 (developer, refusing the fork's premise) | **A LOAN PAYMENT'S THREE DATES ARE ONE VALUE, AND THE COMPOSE REFACTOR IS THE ROOT CAUSE -- BUILD IT, WITH NO FENCE AND NO WORKAROUND.** Ruling on `X-bl-2b`'s shape, and it SUPERSEDES **R-BAL7**'s deferral of exactly this work (*"`PaymentRecord` composing its installment would change ~50 construction sites in the same diff"*). The developer was offered three ways to move six suite consumers off the pricing tier and REFUSED the premise of all three: *"I want root cause solutions only. I want the best from scratch design."* **The design that answered it is BIGGER than the question asked**: composing alone does not fix the defect, because `_replay_from_anchor` would still be typed on the priced record, so the replay must ALSO take the dates. Three types held one payment's three dates -- `PaymentRecord` (`payment_date`/`due_date`/`settled_on`), `PaymentInstallment` (`period_start`/...) and `ConfirmedPayment` -- with TWO NAMES for the funding basis. Built: `amortization_engine.PaymentDates` is the one home, both records COMPOSE it, `_replay_from_anchor` takes the five values it reads (keyword-only: three are adjacent lists), and `slotted_dates` is the one application of the collision rule. **`ConfirmedPayment` is DELETED on a measurement rather than a preference**: `is_confirmed_payment_eligible` already excludes an unsettled payment because `has_settled_by(None, as_of)` is `False` by contract, so the caller-side filter that type existed to justify was pure redundancy -- and the type carried no runtime check, so nothing enforced was lost. Rejected: a public app door returning the replay (the balance fence forbids a balance-at-T producer outside the seam), a test-only producer in `app/` (**P54**), and a suite-side restatement of it (rule 14). Shipped `0e93ec9f`; 13,217 passed, closure 101 -> 46; the residue is **BAL-472**, **BAL-473** |
| balance | R-BAL13 | -- | 2026-09-09 (developer, from the full option space) | **A TRANSFER'S TWO LEGS ARE NOT ROWS IN `budget.transactions`, AND FOUR OF THE FIVE TRANSFER INVARIANTS ARE SYMPTOMS OF STORING THEM THERE.** Invariant 5 is not a rule about transfers at all: it is the decision that forces the other four, because a fold that reads only `budget.transactions` cannot see an event that is not in it. **The endpoint has two halves and they are NOT the same shape.** The RECORD half is a per-account movement row, which is right rather than convenient -- `reconciled_by_id` genuinely differs per leg, since the checking statement clears the outgoing leg and the savings statement the incoming one -- and `X-bi-3` already writes it. The PLAN half is a pair DERIVED from `budget.transfers`, because `balance_at/_plan.py`'s PLANNED tier reads projected shadows and the movement unification replaces only the record half; delete the shadows without it and every projected transfer leaves the projection. **A database VIEW for the plan half is REFUTED by the fact R-JM turns on**: a derive-mode loan payment's leg cannot be priced without the amortization engine, so the producer is Python in the seam. **Rejected**: hardening each clause where it stands (two of its three triggers are deleted by the steps that follow); a `budget.transfer_legs` table (`X-bi` deletes the table it would create); and dropping `budget.transfers` to link the pair to each other, which re-creates **R-JA**'s violation with no source of truth between the two homes. **It REFUTES a live sentence**: `from_scratch_architecture.md` section 7 called these invariants structure rather than fences, true only of invariant 1's upper bound, invariant 2's hard-delete arm and invariant 3's derived-amount half. The measurement is **BAL-475**; the steps are `X-bi-6a` and `X-bi-6`, whose stored-column scope this SUPERSEDES -- stripping two columns off rows the next step deletes is work the plan would buy twice |
| recurrence | R-R64 | -- | 2026-09-11 (developer), BOUNDED the same day | **THE FOLD PRICES EVERY OCCURRENCE A DEFINITION NAMES THAT NO ROW IN ANY STATE ANSWERS, PAST OR FUTURE, FROM THE DEFINITION -- BOUNDED TO OCCURRENCES THE OWNER'S SCHEDULE PLACES; A DELETED OR CANCELLED ROW STILL ANSWERS ITS OCCURRENCE.** Re-draws finding B-9's boundary: "an overdue installment with no record pays nothing" keeps its meaning for a record the owner un-planned and loses it for "no record at all", which since R17 means "not generated yet". Asked at R7d-c-2, whose adversarial review measured door-bounded generation writing one installment past a loan's life through the reset door (`$1,200` at 3% over 3 months, `as_of` 2026-03-01, 4 rows against HEAD's 3). **The BOUND was ruled the same day at R16-b-2**, because the first statement priced every occurrence: the Mortgage's rule starts 2019-01-01 against a schedule opening 2026-03-26, and its 87 pre-schedule occurrences clamped to `as_of + 1` pay `$166,252.65` the day after the read (the Van's 37, `$19,681.78`). An occurrence the calendar cannot place -- before its first payday under `CONTAINING_DATE` -- is neither generated nor estimated. Refused: a two-phase fixed-point pass at the doors, D46's two former remedies, and bounding by the loan's last anchor instead of the schedule. Shipped at R16-b-2 (`7e2e6413`); narrows R7d's TRAP 1 to the answered case |
| recurrence | R-R65 | -- | 2026-09-11 (developer) | **R16-b-2 MOVES AHEAD OF R7d-c-2, BECAUSE R-R37'S ORDER PREMISE IS ALREADY ANSWERED BY THE COMPOSED DOOR.** R-R37 sequenced the sum behind R7d-g so the column would hold one fact before the sum honoured it; since R7d-d and R7d-f the door tells the two facts apart without the column (`authored_closing`: the standing payment's column is the chokepoints' cache, a second definition's is its owner's). The sum honours the DOOR's authored half and NEVER its derived half -- the derived stop is the fold's own zero crossing, so a fold that read it would be a fixed point -- and the `authored_closing` leaf moves to a layer the fold can import (R-R70). R7d-c-2, built and HELD at `de8d1a56` on `feat/r7d-c-2`, ships after R16-b-2 with no fixed-point scaffolding. Shipped at R16-b-2 (`7e2e6413`) |
| recurrence | R-R66 | -- | 2026-09-11 (developer) | **THE FOLD READS `budget.transfers` FOR OCCURRENCE IDENTITY ONLY, NEVER FOR A FIGURE.** A generated row's identity is the occurrence it answers -- `(transfer_template_id, scenario_id, occurs_on)`, the key both unique indexes carry since R17 -- and it is deliberately not mirrored onto the shadow transactions the fold reads money from, so "has this occurrence been answered" cannot be asked of `budget.transactions` at all. The seam asks it of the parent rows through the ONE claim rule generation applies (`_recurrence_common.OccurrenceClaims` over `rows_claiming`), every state counted, and reads no amount off any of them. Transfer Invariant 5's purpose is double-counting (`balance:R-BAL13` names it the symptom that forces the other four), and its text gains this clause. Refused: mirroring `occurs_on` onto the shadows (a second writer of a column only the engine writes, R-JA's two homes) and reading the parents through the projected shadows (blind to cancelled and deleted rows, so it cannot honour R-R64). Shipped at R16-b-2 (`7e2e6413`) |
| recurrence | R-R67 | -- | 2026-09-11 (developer) | **A ROW AND THE ESTIMATE OF A ROW NOT YET WRITTEN ARE PRICED BY ONE FUNCTION.** `recurring_transfer_query.standing_installment_cash` was a copy of the amount model's rule-3 / rule-4 arms, kept in step by intent and measured a cent apart on a loan's final contractual installment (**REC-517**: `$3,000` at 5% over 3 months, the copy billed the contract's `$1,008.35` residual row where the row bills the level `$1,008.34`, and the payoff flipped `2026-04-15` -> `2026-05-15` once the rows existed). Deleted; the arms are stated once over the four VALUES a generated transfer carries or would carry (`cash_ledger._definition_cash`: `DefinitionRow`, `definition_cash`), the written row's arms delegate down to them, and the estimate refuses an empty series exactly as the row would (the copy answered the contract; production has 0 transfer templates without a version). The cent that remains is the amount model's own: `derive_cash` bills the rate period's level payment where the contract bills the residual on the final installment. **REC-517 stays OPEN, re-owned to R16-f** (walk 4 over the one replay), rather than widened into this step. Shipped at R16-b-2 (`7e2e6413`) |
| recurrence | R-R68 | -- | 2026-09-11 (developer) | **THE FORWARD CHARGE CALENDAR IS THE CONTRACT'S INSTALLMENT SEQUENCE, SEED-AWARE.** One `AccrualCharge` per contractual installment -- origination through the last row and the post-contractual extension -- for every slot at or after `as_of` or occupied by a plan payment, MINUS every slot the settled seed already charged. Deriving the calendar from the payments (R16-a's rule, kept deliberately at X-au-g-2c-3b-1) was exact only while the ESTIMATED tier filled every month; summing definitions on their own cadence deletes that fill, so a quarterly definition would have collapsed the charge set with the payment set (D48's case: 40 charges where the contract owes 120). Closes **D53** (an empty forward period charges, so a cancelled row's period grows the balance -- R-R37's "the contract charges every forward period") and, as a consequence, **D54** (a seeded slot is never charged twice: on the clone, August charged once where it was charged twice, `$13,981.80` owed 2026-10-11 against `$14,051.78`), re-pointed from R16-c. Refused: keeping `charges_for_due_dates` over the payments and adding the empty forward periods (two producers, D54 kept); charging every period from origination (the past, R16-c's). **Its `as_of` boundary was amended the same day by R-R71.** Shipped at R16-b-2 (`7e2e6413`) |
| recurrence | R-R69 | -- | 2026-09-11 (developer) | **AN ESTIMATED OCCURRENCE IS DATED AS ITS ROW WOULD BE.** `compute_due_date(rule, period)` over the occurrence's placement on the paycheck its row would live in, saved or PROJECTED (`pay_calendar.span_starting_on_or_after` beside `span_containing`; `recurrence.projected_occurrence_placements`), so the plan is invariant under generation and schedule extension for every authorable shape -- a `Monthly First` definition's row is dated on its funding payday, a `due_day_of_month` override shifts it, and the occurrence date alone would have moved the payoff by up to a paycheck the moment the row was written. `compute_due_date` MOVED from `recurrence_engine._plan` into `app.services.recurrence` (the engine re-exports nothing; every caller re-pointed), because the seam cannot import the engine and the engine cannot be handed the seam's question; plan step R5 deletes it. Refused: the occurrence date itself; waiting for R5's `due_on` (rank #35, behind `balance:X-bi-4`). Shipped at R16-b-2 (`7e2e6413`) |
| recurrence | R-R70 | -- | 2026-09-11 (developer) | **`authored_closing` AND THE IDENTITY IT ASKS ARE THE SEAM'S.** R-R56's arm lived in `recurring_definition` and `is_standing_loan_payment` in `loan_recurrence_sync`, both of which import `balance_at`, so the forward plan could reach neither without a cycle and a private twin would have been a second spelling of one rule. Both live in `balance_at._resolution` now (below `_plan` in the seam's own DAG; `_loan_figures` sits above it), exported by the package and imported by the door, the recurrence form's refusals and locks, and the plan; `destination_account` -- the column-then-lookup read the sync kept private -- is `recurring_transfer_query`'s, a definition's fact in the definition leaf. Refused: a pure arm in `recurring_transfer_query` (it would import `app.services.recurrence`, and `cash_ledger` inherits the edge). R7d-g deletes the arm with the column. Shipped at R16-b-2 (`7e2e6413`) |
| recurrence | R-R71 | -- | 2026-09-11 (developer) | **THE FORWARD CHARGE CALENDAR IS EVERY CONTRACTUAL INSTALLMENT AFTER THE LOAN'S LAST BALANCE ASSERTION VISIBLE AT THE READ, PAST OR FUTURE, MINUS THE MONTHS THE SETTLED SEED CHARGED.** Amends R-R68's boundary ("at or after `as_of`, or occupied by a plan payment"): R16-b-2's adversarial review measured that bound making the payoff a function of the READ DAY -- a month the owner cancelled accrued while it lay ahead of the read and accrued nowhere once the read passed it (`$12,000` at 6% over six months, May cancelled: `2026-09-01` read in April, `2026-08-01` read in May). A skipped month owes its interest, and the escrow the servicer advanced, whichever side of today it is on; the assertion is the boundary because the owner's own statement of the balance supersedes every month before it; and a catch-up payment at the definition's level cash clears the arrears before it reaches principal, which is what the servicer's books say. Refused: the `as_of` bound; charging only the months a payment occupies (D48's collapse). **What this does to a loan whose only assertion is an old origination is R-R72's question.** Shipped at R16-b-2 (`7e2e6413`) |
| recurrence | R-R72 | -- | 2026-09-11 (developer) | **A LOAN'S BALANCE IS THE REPLAY FROM ITS LATEST ASSERTION OVER THE CONTRACT'S CALENDAR -- THE DESIGN OF RECORD, IN THREE PARTS.** (1) ONE charge calendar, every contractual installment after the latest assertion, in the settled walk and the forward plan alike: R-R71 is the forward half, and `R16-c` applies it to the walk and deletes the seed-slot partition (D54's guard with it). (2) An assertion CLEARS whatever charges stand before it -- "the balance on this date is X" supersedes every earlier month, interest owed included -- the ruling `loan_ledger._replay` says it owes the day a charge can precede a reset, which is R16-c's day. (3) The balance the owner states at setup IS an assertion and is recorded as one: a `tracking_start` dated by the owner (default the setup date, bounded `[origination, today]`); `LoanParams.current_principal` -- required by the setup form and read by NOTHING since E-18 -- is deleted with its CHECK, and the tracking-start door's "must precede the earliest payment" leftover goes with it (**REC-519**; step **R20**, ranked after R16-b-2 and before R16-c). Asked when seventeen fixtures failed under R-R71, every one a loan whose only assertion was years old (the worked example, the refusals and the clone evidence are in R16-b-2's as-built record and R20's specification in `implementation_plan_recurrence_redesign.md`, per rule 4). **Applied to the forward plan at R16-b-2**: the post-anchor boundary is spelled ONCE (`rate_period_engine.due_after_anchor`, strict `<`), read by the resolver's replay, the door's origination refusal (R-C) and the plan, which drops every planned row and estimated occurrence due at or before the latest assertion and charges only after it -- a second adversarial review measured the plan folding three projected rows a true-up had subsumed, `$5,955.13` that vanished the day they settled. Ruled at R16-b-2 (`7e2e6413`) |
| recurrence | R-R73 | -- | 2026-09-11 (developer) | **THE READ PASS MEMOISES A RULE'S RESOLUTION BY THE RULE'S SPEC, NOT ITS ID.** `BalanceContext.resolved_recurrence_of` (R16-b-2's memo, the one that collapses the door's and the fold's resolutions of one rule to one) was keyed by `rule.id`; `reauthor_rule` rewrites a rule's columns IN PLACE, so a rule edited and regenerated on ONE pass was served the pre-edit resolution once generation read the door (R7d-c-2's merge onto R16-b-2: two of R17's controls, a monthly rule moved from the 5th to the 19th regenerated `updated 5, deleted 0, created 0`). No live route reached it because every edit route builds its pass AFTER its write -- a convention with no gate, maintaining the invariant the id key carried. Asked "what is the root cause": the key is a PROXY for the derivation's input, and a memo of a pure function is keyed by that input -- `recurrence_spec(rule)`, frozen, hashable, `user_id` a field -- so a stale entry is unrepresentable, two rules stating one spec share one resolution, and the transient rule needs no special case. The pure package gains `resolved_spec(spec, calendar)` as the one producer of the empty-calendar `None` arm; `resolved_recurrence` delegates to it. **The same lens one memo over is LEFT AS FOUND**: the per-loan memos (`loans` / `plans` / `payoffs`) are keyed by account id and derive from rows as well as specs; they are invariant under generation's own write by R-R64's construction and stale under any other write inside a pass, guarded by the route convention, since their input key would be rows. Refused: keeping the id key with the two R17 controls re-cut to open a fresh pass (a silent, financial failure behind a convention); generation resolving fresh past the memo (N-511's double resolution, and the fold still reading the memo); eviction from `reauthor_rule` (the pure package cannot see the pass). Shipped at R16-b-2 (`7e2e6413`) |
| recurrence | R-R74 | -- | 2026-09-12 (developer) | **AT CREATE, A "STATED" CLOSING BOUND IS A REAL STOP -- A DATE OR A COUNT -- AND NEVER THE KEY'S PRESENCE; THE LOCKED ROW IS BLANK.** Asked at `R7d-f-3` while building **R-R60**. The edit door refuses on PRESENCE because its locked control is server-disabled, so any key it receives is crafted, and because a posted START could be a stale echo of a derived date; neither holds at create: the form's server render cannot know the destination, so it emits the "Ends" select ENABLED with "Never" preselected and an ordinary browser posts `never` unless the script's lock ran, and the derived value of the stop is the constant `NEVER_ENDS`, which an absent key authors too (`recurrence_spec_for_create`). Refusing on presence would make the script the control -- the thing R-R60 demotes -- and refuse an owner who chose nothing on a page rendered before the loan lost its payment. `_refuse_stop_on_a_new_loan_payment` refuses `on_date` and `after_occurrences` and accepts `never` and absence, both authoring the unbounded rule the loan's own payment carries. The affordance: the server emits the loans holding no active payment as `data-loan-account-ids-without-payment` beside `data-loan-account-ids` (one value, `LoanDestinationLocks`), and the script locks the "Ends" row on membership -- disabled, BLANK (the edit form's locked-row render with nothing derived), the locked help sentence, the prior selection restored on deselect. Rejected: presence symmetry with the edit door (makes the script the control); an HTMX-fetched server render of the bound rows on destination change (`recurrence_form.js` binds its controls and lock flags ONCE at load, and it changes nothing about the door's rule); dropping the posted stop silently (R-R37); a value-less "Never" option on the create form alone (moves the asymmetry into the template). Shipped at `e3661f6f`. |
| recurrence | R-R75 | -- | 2026-09-12 (developer) | **THE `_context.py` SPLIT BAL-483 ASKED FOR IS TAKEN ON THE TWO MODULE-LEVEL PRIMITIVES, NOT THE MEMO METHODS, AND IT LANDS AS R7d-f-2's PREREQUISITE.** Asked at `R7d-f-2`: the walk memo the step puts on the read pass (row N-513's remedy) had to land in `balance_at/_context.py`, which stood at exactly pylint's 1,000-line cap, and BAL-483 rules the remedy a SPLIT and never a trim. Ruled: `_memoize_once` (the account-binding store-once behind every per-account cache) and `require_scenario` (the no-baseline guard) move whole, with the `_Derived` TypeVar, to `balance_at/_memoize.py` -- a pure move graded by AST (both bodies, signatures and the `BalanceContext` class identical to HEAD with docstrings stripped; the one code change is the forward-reference annotation, because `_memoize` imports `_context` under TYPE_CHECKING only and so joins the seam's floor). The cut BAL-483 NAMED (the memo methods `calendar` / `amounts` / `paychecks` / `resolved_recurrence_of` into a `_memos` leaf) was rejected because a frozen dataclass's methods cannot leave it without inheritance -- a base dataclass carrying the pins and the memo dicts under the object every producer holds -- while these two were already free functions with six sibling importers. Rejected too: the recurrence memos leaving as PUBLIC pass caches filled by a seam sibling (reverses the class docstring's rule that public caches are for derivations from modules ABOVE `_context`, a placement chosen for line count); deferring the walk half of N-513 to after X-i1 (leaves ONE WALK half open while R7d-c-2 makes generation read the same door). The lane executing a balance-arc split is the developer's call, taken; the coordinator told the balance lane so nobody else started it. Closes BAL-483 ahead of X-i1, which now finds room for its three methods. Shipped at `f5e8b0e7` (the move) under `1d1f466f` (the step). |
| recurrence | R-R76 | -- | 2026-09-12 (developer) | **A LOAN PAYMENT CANNOT BE POINTED AT A DIFFERENT ACCOUNT -- THE LOAN'S STANDING PAYMENT, OR A DEFINITION CARRYING LOAN-PAYMENT SETTINGS, THE UNION ITS TWIN REFUSAL COVERS.** Asked at `R7d-f-4`: the transfer update door let the loan's standing payment (its oldest active recurring transfer, `is_standing_loan_payment`) change `to_account_id` -- the old loan was left with no payment projecting against it (the harm `LOAN_PAYMENT_CANNOT_BE_ONE_TIME` refuses one field over) and the moved row carried the old loan's cached payoff (R-R56) into a column its new destination reads as the owner's word. Ruled: refused, with the twin sentence `_transfer_creation_helpers.LOAN_PAYMENT_CANNOT_CHANGE_DESTINATION` ("archive it to stop paying this loan, or set up a new transfer to pay another one"), asked FIRST and of the stored definition, so that every destination move the door then settles is a move of a NON-standing definition: its stored closing bound is its owner's word and the start the door derives cannot trip the presence refusal the edit door keeps for the standing payment. WIDENED the same day to the twin's union (the 2026-08-14 ruling on `LOAN_PAYMENT_CANNOT_BE_ONE_TIME`) after the step's adversarial review: a second, DERIVE-mode payment the loan dashboard's own door writes carries a settings row and is not standing, and moved to savings its amount cannot be priced (`_loan_payment_cash` asks the loan for P&I and refuses on `None`) -- every read of its cash would raise. Rejected: allowing the move and deriving from the new loan (leaves the old loan unpaid; needs the stored bound read through `authored_closing` as the old cache and the presence refusal gated on "destination unchanged", two more fences); leaving it as it was with a ledger row; the identity alone. A rename that posts the destination unchanged is not a move; a second transfer WITHOUT a settings row into a paid loan may move. Shipped at `e0c67c0b`. |
| recurrence | R-R77 | -- | 2026-09-12 (developer) | **AN EDIT THAT MAKES A DEFINITION A PAYMENT-LESS LOAN'S PAYMENT READS ITS CLOSING BOUND THE CREATE DOOR'S WAY: A REAL STOP THE SUBMISSION STATES IS REFUSED, AND "NEVER" OR NOTHING WRITES THE UNBOUNDED RULE -- EVEN OVER A STORED STOP.** Asked at `R7d-f-4` for the two edits REC-521 names (a cadence added to a one-time transfer into such a loan; a repeating transfer moved onto one): the update door's ordinary reading of an absent bound is "leave the stored one alone", and a moved savings transfer may store a real stop, which would ride onto the loan's standing payment where the door reads it as the chokepoints' cache and the next loan edit overwrites it (N-512's shape). Ruled: R-R74's reading one door over. A stop THIS submission states -- a date or a count -- is refused with `LOAN_PAYMENT_BOUND_IS_DERIVED` (R-R37: an entered field is never ignored); where it states "Never" or NOTHING, the write states `NEVER_ENDS`, because the stored stop was the owner's word about a savings transfer and not about the loan, whose own payment carries no stop (R-R59), and because a locked "Ends" row posts nothing, so "nothing" must mean the unbounded rule for the affordance R7d-f-5 adds to the edit form to be lockable at all. The absent-means-keep reading stands for every other edit, a move onto a loan that already holds a payment included: a second transfer's stop is its owner's, graded against the derived start. Rejected: refusing until "Never" is posted explicitly (incompatible with a locked row the owner cannot unlock); refusing on presence as for the standing payment (the control is enabled on these forms and a browser always posts it). The start is DERIVED whatever the payload says, as at create. Shipped at `e0c67c0b`. |
| recurrence | R-R78 | -- | 2026-09-12 (developer) | **THE LOAN-DESTINATION HALF OF `app/routes/_transfer_creation_helpers.py` MOVES WHOLE TO `app/routes/_loan_destination.py`, AS R7d-f-5's PREREQUISITE.** Asked at `R7d-f-5`: the leaf's per-edit lock producer had to land beside the create form's (`loan_destination_locks`) in a module standing at 987 of pylint's 1,000 lines, which its own docstring described as two concerns -- the four transfer-creation helpers (~330 lines) and everything a LOAN destination decides at the transfer form's two doors, accumulated there since R7c-b (~660 lines). Ruled: the whole loan-destination half moves -- `LOAN_PAYMENT_CANNOT_CHANGE_DESTINATION`, `settle_first_occurrence`, `settle_loan_start`, `_loan_holds_no_active_payment`, the three stop refusals, `settle_destination_for_update` and its two helpers, `LoanDestinationLocks`, `loan_destination_locks` -- as a pure move graded by AST (every definition identical, code AND docstrings; nothing new; `__all__` partitioned), the shape ruling R-R75 took for `balance_at/_context.py`; the four creation helpers stay. Rejected: moving only the locks pair (leaves the file at ~930 with R7d-g still to land on it, and separates the affordance from the door whose producers it reads; needs `_loan_holds_no_active_payment` made public); housing the per-edit producer in `_recurrence_form_render.py` (a kind-agnostic module learning about transfer destinations, and the two producers of one value living apart). Graded by `~/projects/shekel-handoffs/recurrence-2026-09-12/move_check_r7df5.py` against `e0c67c0b` on the move commit's files (the step edits the moved module after, so the check reads the head as changed by design), both mutation arms fired. Shipped at `cd22baa6` (the move) under `48e78700` (the step). |
| recurrence | R-R79 | -- | 2026-09-12 (developer) | **A DEFINITION R-R76 PINS TO ITS LOAN SHIPS EMPTY LOCK SETS AND A DISABLED DESTINATION CONTROL CARRYING THE REFUSAL'S SENTENCE; THE SETS AN EDIT FORM SHIPS ARE COMPUTED THE WAY THE UPDATE DOOR DECIDES THE SAME EDIT; THE R-R76 UNION IS ONE PREDICATE.** Asked at `R7d-f-5`: the leaf's minted sentence had the edit form emit "every loan but the stored destination when it has one", which for a loan payment claims "choosing this loan derives the start" about a choice `settle_destination_for_update` REFUSES (R-R76). Ruled, from scratch: `LoanDestinationLocks` gains `pinned_reason` (the R-R76 sentence, else None) with two invariants enforced in `__post_init__` (pinned means both sets empty; the stop set within the start set); `loan_destination_locks_for_edit(template, pass_ctx)` mirrors the door's order filter by filter -- pinned: empty and the sentence; a definition that already repeats: its stored destination out (staying put derives nothing, D50's second transfer included); one that does not repeat yet: every loan in; the payment-less subset for the "Ends" row throughout -- and the transfer form renders the destination select DISABLED with that sentence as help, so every refusal the server can foresee on this form has a rendered lock. The R-R76 union (standing OR settings-carrying) is spelled once, `_recurrence_form_refusals.is_loan_payment_or_standing`, read by `refuse_recurrence_update`'s one-time refusal, by `settle_destination_for_update` and by the producer. `recurrence_form.js` is unchanged in code: it tests membership of the CHOSEN destination and never re-enables a server-locked control. Rejected: empty sets with the select left open (a refusal with no affordance); the minted sentence literally (lists false for their consumer). Financially identical under every option: the door is R7d-f-4's. Shipped at `48e78700`. |
| recurrence | R-R80 | -- | 2026-09-13 (developer) | **WHICH ROWS `R7d-g`'s MIGRATION NULLs: THE STANDING PAYMENT OF EVERY CONFIGURED LOAN AND EVERY ARCHIVED RECURRING TRANSFER INTO ONE; A SECOND ACTIVE TRANSFER'S STOP IS ITS OWNER'S AND STAYS; `max_occurrences` IS TOUCHED NOWHERE.** Asked at `R7d-g` for ledger row **D56**: the schema records who wrote a bound nowhere, so the step's literal NULL-every-loan-payment predicate cannot tell the chokepoints' cache from a stop an owner typed. Measured 2026-09-13 on a fresh production clone (`shekel_r7dg`, head `4d7123cd9803`): two recurring transfers into loans, rules 40 and 48, both active, both the standing payment, both storing exactly today's derived payoff; zero archived, zero second, zero count bounds -- every predicate NULLs the same two rows and erases nothing, so the choice is the rule. Worked on a `$50`/mo sweep beside the Van carrying an owner's stop of 2027-06-01: the literal sentence erases it (20 more sweeps, `$1,000.00` projected the owner never authored, the summed payoff moved on it) and contradicts R-R60/R-R77; the standing payment alone leaves an archived former payment's cache to be read as the owner's word once the R-R56 arm goes (unarchived beside a live payment, a cache three months stale under-projects `3 x $531.94 = $1,595.82` silently); the ruled set NULLs both classes the sync wrote, at the cost of an archived SECOND transfer's owner stop (0 on production), which re-enters through the doors `R7d-g-2` builds. Rejected: matching the value against today's derivation (leaves D35's stale shape in place); hardcoded row ids (a production-specific migration). Shipped at `a776b9df` (migration `bf50951a3599`). |
| recurrence | R-R81 | -- | 2026-09-13 (developer) | **A RECURRING TRANSFER INTO A LOAN CARRIES THE LOAN'S CONTRACT START WHERE IT IS THE LOAN'S STANDING PAYMENT, AND ITS OWNER'S START WHERE IT IS NOT.** Asked at `R7d-g` for ledger row **D50**, and re-asked with the from-scratch design first after the developer refused a sequencing-led recommendation. The standing payment's `starts_on` is the first contractual installment: stored (R-R29), locked on the form, derived at every door where a definition BECOMES the standing payment (create; the update door's two edits; unarchive; loan setup after the transfer exists; promotion by archiving the older payment) and re-derived where `payment_day` or its own cadence unit moves -- `origination_date` is immutable after setup, so no edit moves it below origination. A SECOND transfer's start is the OWNER's at create and after: required at the door, refused below the loan's origination, unlocked, never re-synced. Measured on the clone: the doors as built derive the start for EVERY loan destination, so a `$50`/mo sweep into the Van created 2026-09-13 is written with the Van's 2023-03-22 start and the create path generates five past sweeps (2026-04-22..08-22), `$250.00` that never happened, which the summed tier prices into the Van's payoff. Rejected: the same rule for the standing payment with the doors left deriving for seconds (keeps that fiction and the derive-then-discard shape); every definition app-owned (a sweep can never start when the owner says); deriving the start on read (the NOT NULL cadence anchor becomes a column nothing reads, R-R56's arm reborn); an immutable `payment_day` (refuses a real servicer event). The sync keeps the singular standing identity for the ONE definition it writes. Ruled at `R7d-g-1`; the doors are re-cut at `R7d-g-2`, which closes D50. |
| recurrence | R-R82 | -- | 2026-09-13 (developer) | **A CLOSING BOUND AN OWNER AUTHORED IS HONOURED WHEN ITS DEFINITION BECOMES THE LOAN'S STANDING PAYMENT WITH NO SUBMISSION TO REFUSE IT AT; R-R59 IS A RULE THE SUBMISSION DOORS ENFORCE, NEVER ONE A COLUMN IS REWRITTEN TO.** Asked at `R7d-g` for the doors where a definition becomes the standing payment with no form stating its bounds -- unarchive, loan setup after the transfer exists, promotion by archiving the older payment -- while it carries a stop its owner typed as a second transfer or into an account that was not yet a loan. Worked: the owner archives the Van payment and sweep S (`$50`/mo until 2027-06-01) is promoted. Honoured, S runs 9 sweeps, `$450.00`, then nothing -- the state the owner made. Replaced with `NEVER_ENDS` (R-R77's stored-stop clause one door over), S runs to payoff: 30 sweeps, `$1,500.00`, `$1,050.00` more than authored, written by the app with no submission. Ruled honoured, because `R7d-g`'s own invariant is that nothing but an owner's submission writes the closing-bound column; R-R59's "the loan's payment has no authored stop" is enforced at create (R-R60), at the update door's two edits (R-R77) and at the archived edit door `R7d-g-2` adds. The one hole this opens in `ck_recurrence_rules_valid_window` -- a later `payment_day` edit moving the derived start past the stop -- is closed by refusing THAT loan-params edit whole, naming the transfer (`loan_recurrence_sync._sync_loan_cadence`); the START is still re-derived at these doors (R-R81). Consequence, filed nowhere: a standing payment carrying an owner's stop renders a locked "Ends" row it cannot edit, and archive-and-recreate is the remedy. Rejected: refusing the event while a stop stands (a fence: the owner could not archive the real payment without editing the sweep first). Shipped at `a776b9df`. |
| recurrence | R-R83 | -- | 2026-09-13 (developer) | **A LOAN'S DASHBOARD OFFERS ITS PAYMENT CONTROLS PER DEFINITION: the card lists every recurring transfer into the loan with its own extra-principal and track-the-contractual-payment controls, and the tie-break `active_recurring_transfer_template` makes at those two doors and the prefill is DELETED rather than answered.** Asked at `R7d-g` for ledger row **D49**'s remaining half. Worked: with a `$50` sweep created before the Van payment, `.order_by(id).first()` hands the Track door the sweep and flips it to derive -- `$531.94 + $531.94` projected a month. Rejected: the oldest active, named on the form (keeps the tie-break R-R35 wants deleted); the settings-carrying definition, else the sole one, else refuse (no tie-break, but a rule about which ONE row a loan-level form writes, where the design is that no such form writes one). Reaches the seam: `resolved_loan` prices the contractual schedule with the STANDING payment's extra alone, so per-definition extras are summed there. Built as `R7d-g-3`, which closes D49. |
| recurrence | R-R84 | -- | 2026-09-13 (developer) | **THE THREE LIFECYCLE DOORS OF `transfers/templates.py` -- ARCHIVE, UNARCHIVE AND HARD-DELETE -- MOVE WHOLE TO `app/routes/transfers/lifecycle.py`, AS R7d-g-2's PREREQUISITE.** Asked at `R7d-g-2`: the leaf edits every one of them and the module stood at exactly 1,000 of pylint's 1,000 lines. Archive PROMOTES the next-oldest transfer into the loan to its standing payment, unarchive makes the restored one standing, and hard-delete does both (its history arm archives, its no-history arm deletes the standing payment), so the three are one concern, and one home lets the step's shared archive (`_archive`) and entry helper have one spelling. Ruled: all three, a pure move graded by AST (every definition identical, code AND docstrings; nothing new; `logger` identical in both), R-R78's shape, its own commit; the module registers against `transfers_bp` the way `forms.py` and `mutations.py` do, and no URL or endpoint name changes. Rejected: archive + unarchive alone (the arc doc's literal proposal: splits the lifecycle doors across two files and leaves the archive loop duplicated); moving the regenerate tail instead (moves code the step does not touch to make room for code it does, so the cut follows the line count rather than a concern). Graded by `~/projects/shekel-handoffs/recurrence-2026-09-13/move_check_r7dg2.py` against `b58df005`, all three mutation arms fired. Shipped at `4ead5334` (the move) under `547cc43d` (the step). |
| recurrence | R-R85 | -- | 2026-09-13 (developer) | **EVERY DOOR WHERE A DEFINITION CAN BECOME A LOAN'S STANDING PAYMENT CALLS THE ONE PRODUCER `sync_loan_payment_start(loan)` AFTER ITS WRITE IS FLUSHED, THROUGH ONE ROUTE HELPER THAT BRINGS THE MOVED DEFINITION'S ROWS ALONG AND REFUSES THE DOOR WHOLE WHERE THE RE-DERIVED START WOULD PASS AN OWNER'S STOP; THE CREATE PATH'S `bind_rule_to_loan` IS DELETED.** Asked at `R7d-g-2` for the entry doors D35 and D50 name; the runbook's emptiness test at unarchive misses an older transfer restored beside a younger live payment, which the seam names standing. Ruled: one producer, after the flush, idempotent, at loan setup, the params edit, archive, hard-delete (both arms), unarchive and the transfer update door (`_standing_payment.sync_loan_payment_start_or_refuse`); the one refusal is the window CHECK's (R-R82), `LOAN_START_WOULD_PASS_AN_AUTHORED_STOP` worded once for every door; the create path's bind went (a no-op for the standing payment, an overwrite of a second's owner start, R-R81). **RE-RULED the same evening after the adversarial review: the moved definition's ROWS FOLLOW its rule** -- the producer reports what it moved and the helper runs the transfer MAINTAIN pass for it from today (R-R19's; retained rows flashed; no amount moves, so no chooser): the unarchive fill-forward had written the contract's dates BESIDE the restored owner's dates, two debits a month, and promotion elsewhere left rows on the old dates until the next edit. Unarchive runs that pass in place of its fill-forward. Rejected: fixing the unarchive duplicate alone (a rule and its rows as two homes at three doors). Worked: a `$50`/mo sweep with an owner's stop of 2027-06-01, promoted by archiving the Van payment, starts 2023-03-22, keeps the stop, its rows on the 22nd. Each door's pinned write lands at its FIRST flush under `flush_or_handle_stale` (a 500 on `b58df005`). Shipped at `547cc43d`. |
| recurrence | R-R86 | -- | 2026-09-13 (developer) | **THE EDIT FORM'S LOCKS, THE PRESENCE REFUSAL AND THE R-R76 PIN READ ONE ROUTE-SIDE PREDICATE, `bounds_are_the_loans`: THE LOAN'S STANDING PAYMENT, OR A RECURRING TRANSFER ARCHIVED INTO A LOAN HOLDING NO ACTIVE PAYMENT OLDER THAN IT; EVERY DOOR-TIME READING OF THE IDENTITY IS THE SEAM'S OWN, `would_be_standing_payment`.** Asked at `R7d-g-2` for ledger row REC-522: the seam's identity reads the ACTIVE set, so an archived former payment's form unlocked both bound rows and an owner could author a start and a stop that rode onto the loan's standing payment when it was unarchived. Ruled from scratch: the archived form renders as the standing payment's does -- both rows server-locked, the destination pinned, an empty lock set -- and posts nothing; a crafted bound is refused with `LOAN_PAYMENT_BOUND_IS_DERIVED`; the stored bounds ride through every edit; the UNARCHIVE door derives the start (R-R85) and honours a stored stop (R-R82); `_loan_destination.py` gains no arm. Rejected: the runbook's shape (the update door runs its loan branch for the archived case: derive and write the start, refuse a real stop, read nothing as `NEVER_ENDS`), which erases a stored owner's stop on any edit. **RE-RULED the same evening after the adversarial review: the door-time reading is the SEAM's -- would this definition be the OLDEST active transfer into the loan -- not emptiness**, at the archived arm, the two doors' branch gate and the per-definition lock set: an archived or moved definition OLDER than the loan's live payment is the seam's standing payment the moment it is active there, and emptiness rendered its rows open and wrote the owner's start for the next sync to overwrite; a definition not saved yet is standing only where the loan holds none. Residue: a locked row shows an archived transfer's stored owner start until unarchive re-derives it. Shipped at `547cc43d`. |
| recurrence | R-R87 | -- | 2026-09-14 (developer, from the full option space) | **THE OCCURRENCE WALK'S PLACEMENT FOR PRICING RUNS BACKWARD FOR AN OWNER WHO HAS STATED `history_opens_on`.** Ruling on **REC-527**, found by `salary:R15-b`'s adversarial review: a payroll line's cadence rule starts on the recorded opening payday (**R-SAL30**) and the walk projects nothing backward, while the pay calendar runs in both directions for a stated owner since `balance:X-bh-2` and the engine prices those paydays -- so below the record a capped line's year-to-date counts no backdated paycheck (the cap binds later), and on the recorded opening payday a monthly line is taken where the ordinal rule, counting the backdated paydays before it, took it a paycheck earlier. The walk reads that calendar the same way: `paychecks_from` and `_first_occurrence` run below the record for a stated owner, so a rule from the calendar's true opening reaches backdated paydays and reproduces the retired reading in BOTH directions; generation still needs a saved period (the 2026-09-11 schedule bound, **R-R64**, revisited for stated owners' PRICING only). Its step is `R21`. Rejected: accepting the divergence as the rule's meaning (a documented `$0.00` defect awaiting the first stated owner with a capped or monthly line); the engine keeping the old every-paycheck reading below the record (a second semantics in one producer, repairing only the backdated direction); re-authoring from the stated opening (measured impossible: the walk clamps its first occurrence to the recorded opening). Worked under STATED assumptions (no owner has the shape): a `$100.00` 24-line capped at `$2,000.00`, five backdated 2026 paychecks -- year-to-date `$500.00` old / `$0.00` new, `+$500.00` that year; a `$50.00` monthly line, `+$50.00` on the recorded opening. `$0.00` on production 2026-09-13. Both directions are stated in migration `542c61e48ee8`'s docstring (`4ed9b5b3`). |
| recurrence | R-R88 | -- | 2026-09-14 (developer, from the full option space) | **THE LOAN RESOLVER TAKES NO LOAN-LEVEL EXTRA: `extra_principal` IS DELETED FROM `resolve_loan` AND `compute_payoff_scenarios`, AND THE MONTHS NO ROW COVERS ARE PRICED FROM EVERY DEFINITION'S OWN OCCURRENCES BY THE SEAM'S FORWARD PLAN, NOT BY A SUM.** Asked at `R7d-g-3`, where **R-R83** had said per-definition extras are SUMMED at `resolved_loan`. Measured 2026-09-14 with a throwaway probe: a derive-mode payment's projected rows reach the composer's feed at `$626.46` (P&I `$526.46` + its `$100` extra, amount rule 4) and `compute_payoff_scenarios` then applied `extra_principal` AGAIN on those months -- `extra 100.00` beside `payment 626.46`, the balance dropping `$676.46` -- while the contractual tail applied it once; born at `balance:X-au-g-2c-1`, `$0.00` live (no `loan_payment_settings` row on production). Worked, Van P&I `$531.94`, A the standing monthly payment with a `$100` extra, B a `$50` every-paycheck sweep given a `$25` extra: today the oldest's alone (`$100` or `$25` by creation order); a plain sum `$125`/month; a cadence-normalized sum (`Cadence.monthly_equivalent`) `$154.17`; the definitions' own occurrences `$262.50` a month, B's whole `$75` being principal once A covers the interest. Ruled the last: the parameter goes (the double count with it, and the tie-break's last figure, **D49**), and the band chart, pay-off-sooner lever, allocation bar and schedule page are re-cut off the composer's committed slice onto the forward plan, pulling **R16-f**'s outcome forward. **Rejected**: both sums (each an estimate blind to a second definition's base, and a new pricing spelling beside the plan's). Shipped at `R7d-g-3` in one PR: the deletion and the card (`a66496ec`), the re-cut (`a1c5082f`). |
| balance | R-BAL18 | -- | 2026-09-11 (developer) | **THE DEAD CELL IS SEALED, NOT PINNED: `X-bi-1` SHIPS NO FENCE.** Asked how the move of every `transactions.is_envelope` reader onto the one accessor should be kept from reverting until `X-bi-5` deletes the column, the lane offered a census test and a pylint checker; the developer refused both -- *"Why are you building fences and checkers that will be deleted? Is there a better way of doing this? What is the best from scratch design?"* -- and the answer is the structural one already in this model: X-au-k's `__estimated_amount`. The row's `is_envelope` column is mapped PRIVATELY (SQL name and DDL unchanged, autogenerate delta against `origin/dev` empty), the public name is a property whose READ is `tracks_purchases` and whose WRITE lands on the cell, and every derived flag's class-level name answers a sentinel that REFUSES to key a query -- because a plain `property` at class level compares `False` to everything, so `filter_by(is_envelope=True)` returned NO ROWS with NO ERROR (measured on the first cut). The misread that read 4 envelopes where there are 238 is unwritable in Python, in Jinja and in the ORM; raw SQL naming the column is what remains, which **R-HJ** forbids and `X-bi-5` makes unwritable. From scratch there is no flag at all (**R-IT**: every plan item takes zero or more movements), and that stays `X-bi-5`'s. `companion_visible` is NOT sealed: `companion_service` keys SQL on it, its own finding. Rejected: the census test, the checker, renaming the write door to `tracks_purchases=` (245 test kwargs and 40 field strings deleted again at `X-bi-5`), a SQL body on `tracks_purchases` (a second spelling, **R-IZ**), and a refusal of the inert write a crafted PATCH can make on a template-generated row (left for `X-bi-5`, which deletes the cell) |
| balance | R-BAL19 | -- | 2026-09-12 (developer) | **A SECURITY PREDICATE HAS ONE SPELLING, AND IT IS THE PROPERTY'S: `BAL-482` IS REMEDIED BY LOAD-THEN-FILTER.** `companion_service.py:238-245` states `visible_to_companion`'s rule a second time in SQL -- the template's flag, or the ad-hoc row's own -- on the predicate that decides what a companion may see, and it is why `companion_visible` could not take `is_envelope`'s seal at `X-bi-1`. Ruled, from four worked options: the query keeps its owner / period / baseline-scenario / not-deleted scope and DELETES the visibility clause; the rows are filtered in Python through the ONE property; `companion_visible` then takes the same seal (private cell, public name reads the accessor). The leaf is `X-bi-1b`, ranked right after `X-bi-1`. **Rejected, each with its reason**: a `column_property` carrying the rule (a stored copy INSIDE THE SESSION with no reconciler -- a row loaded before a flag flip keeps the old answer until refreshed, rule 14's cache one layer up); widening `X-bi-5` to the twin (the two spellings stay live until then, on a security predicate); a `hybrid_property` with a SQL body (two bodies of one rule in one file, **R-IZ**'s shape with a shorter distance to part). The before/after row set is an EQUALITY to grade: nothing but the filter's language moves |
| salary | R-SAL24 | -- | 2026-09-12 (developer, from the full option space) | **THE SALARY REGENERATION IS A SERVICE.** `routes/salary/_helpers._regenerate_salary_transactions` -- the walk every salary write is followed by (the template's per-paycheck amount re-stated at today's net, the projected rows regenerated from today) -- moves below the route layer to `app/services/salary_regeneration.py`, Flask-free: it takes the read pass the route builds (a producer below the route takes the `BalanceContext`; only a route builds one) and RETURNS the retained-row ids for the route to flash; the salary package keeps a thin adapter under the old name so its call sites and the tests that patch it are untouched; `retirement.update_settings`' raise arm (`S3-f-3`) reaches the same walk. Asked because the rail's Save had to run what the salary page runs or leave two doors with two outcomes (rule 14), and the walk was private to the salary routes package (the `shekel-private-module-import` gate), read `current_user` and called `flash`. Rejected: a shared route-tier private module `app/routes/_salary_regeneration.py` (the established home for cross-blueprint route helpers, but it leaves paycheck pricing and money-row writes in the route tier -- the reason `salary_profile_service` gives for being a service); a public `save_raise_end_year` door in the salary ROUTES package (measured: no route package imports another today, so it would be the first cross-blueprint import, a URL-less door, and its flash+redirect outcomes would leave the stale race to the caller anyway); writing the column without regenerating; posting to `salary.update_raise` (R-SAL22). The direct-engine census (`C12`'s) moved its entry with the walk. Shipped at `S3-f-3` (`a5ef1bdf`). |
| salary | R-SAL25 | -- | 2026-09-12 (developer) | **`/SAVINGS` PRICES ITS CURRENT PAYCHECK THROUGH THE READ PASS'S PRICER, CALIBRATED, AS EVERY OTHER SURFACE DOES.** Ruling on `C12`'s money question (ledger row **P62**'s last site): `_metrics._get_current_paycheck_breakdown` prices ONE period through a direct `calculate_paycheck` passing NO `calibration=`, the door **R-SAL21** measured on `/retirement`; both doors resolve tax configs through `_configs_from_series`, so the calibration is the whole gap. Measured 2026-09-12 on the developer's data (clone `shekel_s3f1`, payday 2026-09-10): net `$2,541.49` by the page's door against `$2,572.78` by the pricer (taxes `$355.14` against `$323.85`, gross `$3,631.74` and pre-tax `$713.29` identical); through the Emergency Fund goal (3.00 months of salary) the target `$16,519.69 -> $16,723.07` (+$203.38), the required contribution `$987.37 -> $1,003.01` a period, progress 22.30% -> 22.03%, months to goal 24 -> 25 and completion 2028-09-12 -> 2028-10-12; the DTI denominator is gross-based and does not move. Taken via the option label *Pricer, calibrated*. Shipped as its own money leaf `C12-b` (R-SAL6) at `945651c2`. Rejected: keeping the uncalibrated door and documenting the `$31.29` (rule 14's two homes); routing through the pricer with a `calibration=None` mode for this page (the override-parameter band-aid, two modes on one producer). |
| salary | R-SAL26 | -- | 2026-09-12 (developer) | **"THE OWNER'S CURRENT PAYCHECK" ON `/SAVINGS` IS THE SUM OVER THE OWNER'S ACTIVE SALARY PROFILES -- NET FOR AN INCOME-RELATIVE GOAL, GROSS FOR THE DTI DENOMINATOR.** Ruling on `C12`'s profile question: the page's door picked a profile with an unordered `.first()`, the shape **R-F16**'s adversarial review measured at a 39% swing between renders on a two-job owner. A goal stated in months of salary and a debt-to-income ratio are about total income, and it is what the grid already does, where both profiles' paycheck rows count. `$0.00` on the developer's data (one profile). Taken via the option label *Sum over active profiles*. Rejected: the first profile by id (deterministic, still arbitrary; **R-SAL5** rejected the same remedy for the contribution feed); answering nothing with two profiles (deletes a legitimate two-job owner's goal target and DTI). Shipped at `C12-b` (`945651c2`). |
| salary | R-SAL27 | -- | 2026-09-12 (developer) | **THE AMOUNT BASIS IS BUILT OVER THE READ PASS'S PRICER; A PRODUCER HOLDING NO PASS SAYS SO IN ITS CONSTRUCTOR'S NAME.** Ruling on ledger row **P63**, and the shape `balance:X-i1` was waiting on: `cash_ledger.amount_basis(user_id, scenario_id)` was built from two ids, so `SalaryPricing` derived a second pricer over a second calendar -- measured 2026-09-12 at 2 calendars, 2 `ProfilePaychecks` and 115 engine runs for one grid-shaped read, 1 / 1 / 63 after. `amount_basis(paychecks, scenario_id)` is THE constructor and the owner is read off the pricer, so `ctx.amounts()` hands `ctx.paychecks()` and a pass-holding producer cannot reach for the wrong door by habit (**R-BX**'s rule: the obvious spelling fails loud); the basis takes the PRICER, which carries the calendar and the per-payday memo. The twelve pass-less sites (ledger row **BAL-491**, owner balance:X-i6) build theirs through `derived_amount_basis(user_id, scenario_id)`, whose pricer derives the calendar LAZILY on the first paycheck priced, so a non-salary settle derives nothing and an owner with no schedule row is refused only when a paycheck is asked for -- the two properties the old derivation had; the name is that row's census and the constructor is deleted with its last caller. **The pass's own pricer is lazy the same way** (`PaycheckPricing` takes the calendar as a SOURCE), so building `ctx.amounts()` derives nothing and the loan arm and the bank-agreement fragment keep their *raises none* contracts. Taken via the option label *amount_basis takes the pricer; ctx-less sites get derived_amount_basis*. Rejected: keeping `amount_basis(user_id, scenario_id)` beside a pass-side constructor (the obvious spelling keeps deriving); an EAGER calendar at every pass-less site (`calendar_for` REFUSES a schedule-less owner, so a non-salary settle would 500); handing the whole `BalanceContext` to `cash_ledger` (inverts the import arrow). Shipped at `C12-a` (`26a7b816`). |
| salary | R-SAL28 | -- | 2026-09-12 (developer) | **THE PAYCHECK ENGINE IS A PACKAGE OF ONE PRIVATE LEAF PER VERB, AND `C12` SPLITS AT THE MONEY LINE.** Ruling on ledger row **P64**'s engine half and `C12`'s decomposition: `paycheck_calculator.py` stood at 997 of pylint's 1000 lines, and every edit shaved prose to add a line. It becomes `paycheck_calculator/` -- `__init__` (the contract every test reads, plus the public API), `_breakdown` (the six value types), `_calendar_questions` (`_month_ordinal`, `_is_third_paycheck`, `_get_cumulative_wages`), `_deductions`, `_withholding`, `_pricing` (`calculate_paycheck`, `project_salary`) -- mirroring `pay_calendar/`, `cash_ledger/` and `transaction_service/`; every public import path unchanged, the unit test's private imports moved to the leaves as `pay_calendar`'s did, the arch census excluding the package by path prefix. Under **R-SAL6** the step is two leaves: `C12-a` (the split, P63 and the four direct-engine sites a calibration cannot move, graded byte-identical on the harnesses) and `C12-b` (`/savings`, MOVES MONEY, own PR, own harness). Taken via the option label *Approve both*. Rejected: shaving prose to buy lines (`transaction_service` ruled against it); one PR for both leaves (a money diff and a refactor diff reviewed in one pass). Shipped at `C12-a` (`26a7b816`). |
| balance | R-BAL20 | -- | 2026-09-12 (developer) | **EVERY PLAN ITEM HAS EXACTLY ONE DEFINITION: `BAL-484` IS REMEDIED BY DELETING THE ROW'S FLAGS, NOT BY FENCING THEM.** The seal at `X-bi-1b` left a generated row's `companion_visible` cell dead, writable by a crafted PATCH, and -- unlike `is_envelope`, which `X-bi-5` deletes -- named for deletion by no step. Asked which remedy owns it, the lane led with a CHECK constraint and the developer refused the premise: *"Why do you recommend an option that isn't the from scratch design?"* Ruled, from four worked options: a one-off item becomes a RULE-LESS DEFINITION (the 0-or-1 cadence `recurrence:R-F6` made structural; a state no transaction definition on production holds, 40 of 40 carrying a rule on the 2026-09-12 restore, while the TRANSFER twin already holds it for 2 of its 6) plus one placed occurrence -- AMENDED 2026-09-13 by **R-BAL24** to *plus its placed rows*, a bank-born envelope definition holding one per paycheck; the row carries no flag; both dead cells are deleted; the one-off half of every branch on `template_id IS NULL` becomes unwritable -- the shadow and payback half stays until `X-bi-6` and the card arc's `CC3b` / `CC3c`, which the audit that followed measured rather than the *up to 53 sites* the ruling was taken over. A new decomposed X-bi family, `X-bi-7`, its SPEC written and reviewed before any code (`from_scratch_architecture.md` section 10), nothing interim; the six forks that spec raised are **R-BAL21** to **R-BAL26**, ruled the same day with the family's owner and rank (10.7), and 10.10 states the result. **Rejected**: the CHECK `template_id IS NULL OR NOT companion_visible` (a fence on a cell with no reason to exist), a setter and route refusal (guards one writer of several), ruling the dead half permanent (rule 14's maintenance contract) |
| balance | R-BAL21 | -- | 2026-09-12 (developer) | **A ONE-OFF'S PRICE LIVES ON ITS DEFINITION, AND A ONE-OFF'S SERIES HOLDS ONE VERSION.** Fork A of `X-bi-7`'s audit (`from_scratch_architecture.md` 10.6): the definition prices the row through amount rule 3, as `X-au-f` ruled for the one-time transfer, its series opened with ONE version dated on the row's due date; a restatement corrects the version the row's due date reads, IN PLACE, and never appends -- `set_amount` alone would append, leaving the grid and the balance on the old figure while the edit form reads the new. Where that arm lives, in `set_amount` beside its does-not-own-its-amount arm or in a `restate_one_off` door, is the doors leaf's placement question. A RECURRING row's typed figure still detaches the row (OWN, `is_override`), because that statement is about one occurrence among many; a one-off's only occurrence IS the definition. **Rejected**: the row owns its figure beside a `default_amount` copy (two homes, no reconciler, rule 14's crossing); a nullable `default_amount` meaning "ask the row" (`R-IY`'s deleted design one table over) |
| balance | R-BAL22 | -- | 2026-09-12 (developer) | **A ONE-OFF IS DUE ON ITS PLACED PAYCHECK'S START UNLESS THE OWNER STATES A DATE, AND THE CUTOVER WRITES THAT DATE ONTO EVERY UNDATED LINK-LESS ROW.** Fork B: the one rule the app already states twice (`_leftover_due_date`, `_materialize_one_time_transfer`), balance-neutral by construction since `DerivedPeriod.attribution_day` already budgets an undated item to its paycheck's start. The inline create defaults to it; the popover keeps the date editable and never clearable. **Cost accepted**: 7 settled expenses among the 26 undated rows on the 2026-09-12 restore then read 6 to 11 days late in `payment_timeliness_from_txns`, a day nobody observed (the shape of N-181, closed at `X-f1c2`, with the edit door left open as that step did), and that the cutover migration may write that day -- the question trace 8 asks of **R-HJ** -- is part of this ruling. **Rejected**: `settled_on` where settled (manufactures "on time", two rules for one column, and a revert withdraws the fact); leaving one-offs undated and exempt from `X-bv-2`'s CHECK (a fence in Python beside a constraint that no longer states the invariant); refusing the migration until the owner dates the 26 by hand |
| balance | R-BAL23 | -- | 2026-09-12 (developer) | **THE GRID POPOVER IS A ONE-OFF'S WHOLE LIFECYCLE; THE RECURRING LIST SHOWS DEFINITIONS WITH A RULE.** Fork C: a one-off is created at the grid, its name, category, flags and price are edited at the popover and land on the definition without one being shown, and it is deleted with its definition -- REFUSED while a standing merchant rule names it, since `fk_merchant_rules_template_account` is `ON DELETE CASCADE` and a bare delete would take an owner's stated answer with it -- while *make this repeat* opens `templates.edit_template`. The transaction template form's *Does not repeat* option is removed (it makes a definition with no occurrence); the transfer form keeps its period picker until the transfer's own step decides; the account-delete refusal counts definitions with a rule, so an account holding only a one-off archives as today. **Rejected**: a One-time section on the Recurring page, a second door for every act the popover already performs |
| balance | R-BAL24 | -- | 2026-09-12 (developer, against the lane's recommendation); RE-RULED 2026-09-13 (developer) | **BANK IMPORT'S ENVELOPES TAKE `bank_import:X-f6c`'S SHAPE INSIDE THIS FAMILY: A NEW-ENVELOPE MERCHANT ANSWER MINTS ONE RULE-LESS DEFINITION THE FIRST TIME AND NAMES IT THEREAFTER; `transaction_templates.category_id` BECOMES NULLABLE; THE MERCHANT-RULE PICKER IS NOT FILTERED.** Fork D. Re-ruled when the audit's premise fell: the cross-period identity is finding N-328 (N-327 is closed), RULED by bank_import 2026-08-20 and owned by `X-f6c`, which the audit never saw and called *bank_import's to revisit*. `X-bi-7b` builds it and `X-f6c` is its alias (conventions rule 11): `_create_envelope` mints one definition the first time a merchant answer needs it, the answer becomes TEMPLATE naming it, and every later paycheck's envelope is that definition's row placed in the line's period through the one-off producer -- so a rule-less definition holds its PLACED ROWS (a grid one-off one, a bank-born envelope one per paycheck), `R-BAL20`'s *one placed occurrence* is amended to say so, and the cutover's downgrade folds one-row definitions only. `mint_uncategorized` mints a definition for a category-less row, hence the nullable column (a schema-only migration whose downgrade refuses while a category-less definition exists); the cross-statement suggestion is re-keyed from `template_id is None` onto the definition. **Rejected**: the 2026-09-12 cut (a definition per envelope per paycheck, NEW-ENVELOPE kept for a rule-less destination -- it overturns bank_import's ruling and leaves N-328 open); X-f6c first and 7b re-routing it (the shape built twice); a real Uncategorized CATEGORY for a category-less row (the opposite of **R-FN**'s ledger-account mechanism); bank-minted rows staying link-less as a third meaning of `template_id IS NULL`; a `recurs` filter on the picker |
| balance | R-BAL25 | -- | 2026-09-12 (developer); RE-RULED 2026-09-13 (developer) | **A ONE-OFF'S ROW RECORDS ITS OWN DUE DATE AS THE OCCURRENCE IT ANSWERS (`occurs_on = due_date`), TRANSACTION AND TRANSFER ALIKE, SO NO ROW ANSWERS NO OCCURRENCE.** Fork E, first ruled NULL (the one-time transfer's practice) on the argument that such a row is RETAINED when its owner later adds a rule; re-ruled when the review found `recurrence:R19-b` (NOT NULL, deleting that retain branch, the NULL claim arm and the undated transfer index) waiting on exactly this rule, its own spec naming the row's date as the obvious answer. The cutover writes it onto every minted row; R19-b lands as written and dates the one-time transfers. A placed one-off later made to repeat is adopted when the new rule names its date, else treated like any projected row of a rule that changed: a record-free Projected one (1 of 34 on the 2026-09-12 restore) is deleted and the rule's own row written; settled and immutable rows are never touched. `recurrence:R-R63`'s protected class -- a row answering no occurrence -- is empty by construction, which is what makes `R19-a`'s branch deletable rather than quiet. **Rejected**: NULL with R19-b withdrawn (three fences kept for good); the row's own date plus an owner-placed marker on `is_override` (a second meaning on one column, and the reader BAL-493 is about re-keyed to carry it) |
| balance | R-BAL26 | -- | 2026-09-12 (developer) | **`X-bi-7` IS TRANSACTIONS ONLY; TRANSFERS TAKE THE ONE-DEFINITION SHAPE IN A SIBLING STEP OUTSIDE THE FAMILY.** Fork F (the sibling's place in the sequence is `steps.md`'s): the rule is stated for every plan item and `budget.transfers` already holds the shape in data (0 ad-hoc and 2 one-time transfers on the 2026-09-12 restore), so the ad-hoc transfer door and the twin's two defects, `BAL-492` and `BAL-493`, are the sibling's (`X-ci`), and the family's cutover stays one table wide; the twin's delete arm is already `X-bg`'s (N-386). A one-off created in a non-baseline scenario mints a definition carrying no scenario, noted rather than forked while one scenario exists and the selector is a stub |
| balance | R-BAL27 | -- | 2026-09-13 (developer) | **A RULE-LESS DEFINITION WITH NO ROW DEFINES NOTHING, SO THE ACCOUNT HARD-DELETE DISPOSES OF IT; A LIVE ROW UNDER A DEFINITION ON THE ACCOUNT IS THE ACCOUNT'S HISTORY WHEREVER IT SITS.** Ruled at `X-bi-7a` when re-keying `routes/accounts/crud.py`'s guard 3 onto recurring definitions (**R-BAL23**) exposed a state the spec never named: a definition with no rule and no live row (the template form's *Does not repeat* today; a one-off whose row was hard-deleted after 7a) passed every guard and the delete met `transaction_templates.account_id`'s RESTRICT key as a 500. Three consequences: guard 3 loads the account's definitions and asks each one `recurs`; `archive_helpers.account_has_history` reads a non-deleted row ON the account OR UNDER one of its definitions (an outer join), so a retained row that stayed behind when its definition's account moved still archives the account rather than letting `transactions.template_id`'s `SET NULL` leave a TEMPLATE-priced row nothing prices; and the permanent-delete cleanup deletes the surviving rule-less definitions through the session beside the ghost rows and ghost transfers, their series rows cascading and the merchant rules that could name one already cascading with the account (`fk_merchant_rules_owner`). **Rejected**: archive-instead whenever a rule-less definition exists (a fence on a state that defines nothing, unremovable by the owner once the Recurring list shows definitions with a rule); keeping today's refusal on ANY definition (contradicts R-BAL23 and names, for a one-off, a "recurring transaction" listed nowhere) |
| balance | R-BAL28 | -- | 2026-09-13 (developer) | **THE `is_override` FLIP'S MOVE HALF KEYS ON `recurs`; ITS TYPED-FIGURE HALF KEEPS FLIPPING ON EVERY LINKED ROW UNTIL `X-bi-7b` BUILDS R-BAL21'S IN-PLACE RESTATE.** `routes/transactions/mutations._apply_field_updates` flipped the flag on a template-linked row for a typed figure OR a period move. At `X-bi-7a` a period MOVE flips only when the row's definition recurs: the flag is what keeps the maintain and generate passes off a row the owner placed elsewhere, a rule-less definition runs no pass, and the flag would only exclude the row from `propagate_to_unruled_definition` for good -- the twin's defect **BAL-493** on this table. A TYPED FIGURE still flips on every linked row, rule or not: until the doors leaf builds the in-place restate **R-BAL21** names, the figure lands OWN on the row, and the new twin selects Projected, not-overridden rows exactly as `non_repeating_live_transfers` does -- so without the flag the definition's next edit would re-declare the row TEMPLATE-priced and silently discard it (worked: Kayla's Kindle `$162.25` on the definition, `$170.00` typed, a rename reverting it to `$162.25`), and the day a rule is added the maintain pass would do the same where the flag makes it a conflict the chooser asks about. **Rejected**: both halves on `recurs` (the literal reading of 10.5's *drops the :267 flip*, which ships that silent loss); the twin carrying the row's own ownership instead (an OWN row beside a definition's series with no marker any reader knows, R-BAL21's rejected two-homes shape) |
| balance | R-BAL29 | -- | 2026-09-13 (developer) | **A ONE-OFF'S IN-PLACE RESTATE (R-BAL21) IS A NEW FUNCTION IN `template_amount_service`, BESIDE `set_amount`, THAT CORRECTS THE VERSION THE ROW'S DUE DATE READS.** The placement question `R-BAL21` left to the doors leaf, walked on Kayla's Kindle (`$162.25`, one version dated on the row's due date 2026-05-07; `$170.00` typed on 2026-09-13). `set_amount(effective_on=today)` appends and leaves the grid on `$162.25` (R-BAL21's rejected crossing). `set_amount(effective_on=row.due_date)` corrects in place only while a version stands on that EXACT date: once the owner has moved the due date to 05-10 it APPENDS `{05-07: 162.25, 05-10: 170.00}`, and moving the date back silently re-reads `$162.25`. The ruled function finds the version `amount_as_of` answers for the row's due date and corrects THAT one, so the series stays `{05-07: 170.00}` whatever the date did, and one version stays one. **Rejected**: an arm inside `set_amount` (exact-date semantics with a flag, the override-parameter shape rule 1 forbids); a `restate_one_off` door in the one-off module (it needs the series' private helpers across modules, which `shekel-private-module-import` refuses, and gives the series a second home). Builds at leaf 7b-2 |
| balance | R-BAL30 | -- | 2026-09-13 (developer) | **ONE PRODUCER FOR A ONE-OFF: A PARAMETER OBJECT OF WHAT THE DEFINITION SAYS, PLACED BY THE SAME ARGUMENTS A LATER ROW OF IT TAKES.** `one_off.OneOffToPlace` carries the definition's eight own columns (owner, account, type, name, amount, category-or-None, the two flags); `place_one_off(spec, period, *, scenario_id, due_date=None)` mints the definition, opens its series with one version dated on the row's due date (R-BAL21) and calls `place_row_of(definition, period, *, scenario_id, due_date=None)` -- the placing half alone, dated at the paycheck's start unless told otherwise (R-BAL22), `occurs_on = due_date` (R-BAL25), priced `derived(TEMPLATE)`, which a bank-born envelope's later-paycheck rows reuse at `X-f6c` (R-BAL24). The five definition-derived columns come from `recurrence_engine.unruled_row_fields`, the maintain twin's own statement, so a row as born and a row as re-declared cannot differ. The four writers differ only in what they put in the object. **Rejected**: keyword-only arguments (crosses the argument bound; a signature unreadable at four call sites); a producer taking a period ID (re-resolves the paycheck's start it was already handed -- a second producer call in one request; `ReviewScope.period_holding` returns the derived period instead); two producers with no shared placing half (two spellings of the row's date and pricing rules) |
| balance | R-BAL31 | -- | 2026-09-13 (developer) | **`X-bi-7b` IS THREE LEAVES, EACH A SESSION AND EACH LEAVING THE APP CONSISTENT; `X-bi-7b` AND `bank_import:X-f6c` TICK WITH THE THIRD.** **7b-1** *born as a definition, dies with it*: the producer, the `category_id NULL` migration, both grid create doors, `mint_uncategorized`, the delete (a one-off's definition goes with its LAST row; refused while a standing merchant rule names it), *Does not repeat* removed, the Recurring list's active sections filtered to `recurs`, and two behaviour-preserving `recurs` keys in bank import (`rule_naming`, `_new_envelope_placement`). **7b-2** *edited on its definition*: the popover's flags land on the definition, R-BAL29's restate arm (the typed-figure flip of R-BAL28 goes), the due date not clearable, *make this repeat*, the delete dialog's sentence; the update schema's flag fields go, which is **BAL-484**'s writer. **7b-3 = X-f6c**: `_create_envelope` mints through the producer, a NEW-ENVELOPE answer becomes TEMPLATE naming the definition at first mint, a TEMPLATE answer on a rule-less definition PLACES its row where the paycheck holds none, the receipt offers TEMPLATE, `MintedEnvelopes` keys on the definition, and 7b-1's two keys are deleted. **The interim between 7b-1 and 7b-2 is STATED in code, not discovered**: a new one-off's popover shows no flag controls and a typed figure lands OWN + `is_override` (R-BAL28's own interim, now reaching a row the grid itself minted); a cleared due date meets 7a's sentence. **Rejected**: one session (three unrelated review surfaces in one diff, the charter's *pushing on degraded*); the handoff's two-way split (X-f6c's rule flip and placement arm inside an already-full first leaf) |
| balance | R-BAL32 | -- | 2026-09-13 (developer) | **THE TRANSACTION TEMPLATE FORM OFFERS NO *DOES NOT REPEAT* ON CREATE **OR EDIT**; THE TRANSFER FORM KEEPS IT.** R-BAL23 removed the option from *the transaction template form*, and one form serves both doors. Ruled to the literal reading: a transaction definition either recurs (the Recurring page) or is a one-off born at the grid, so a cadence can no longer be CLEARED from the form -- the closing bound and archive remain the ways to stop one -- and the *cleared cadence* state 7a made behave is unreachable from the UI. The shared partial `_recurrence_fields.html` takes `offers_no_cadence` (the transfer form leaves it true: that form IS the one-time transfer's door until the transfer's own step decides); an UNSET transaction picker renders a *Choose how it repeats* placeholder posting the same empty value, which `TemplateCreateSchema.validate_a_cadence_is_chosen` refuses outright and `TemplateUpdateSchema`'s override refuses when PRESENT (an omitted unit on a partial update leaves the cadence alone). The *no longer repeats* flash in `update_template` is deleted as unreachable. A one-off's own edit form (7b-2, via *make this repeat*) opens on that placeholder so nothing is authored by accident. **Rejected**: create-only removal (keeps a second door that manufactures rule-less definitions holding N scattered rows the filtered Recurring list then shows nowhere) |
| balance | R-BAL33 | -- | 2026-09-14 (developer) | **A PERIOD MOVE RE-PLACES A ONE-OFF: A RULE-LESS DEFINITION'S ROW MOVED TO ANOTHER PAYCHECK TAKES THAT PAYCHECK'S START AS ITS DUE DATE, UNLESS ITS DATE WAS NOT ITS OLD PAYCHECK'S START.** Found by 7b-1's adversarial review, measured: a one-off is dated at birth since that leaf (R-BAL22) and neither carry-forward's move-whole arm (`carry_forward_service/_execute.py`, `pay_period_id` only) nor the popover's period move (`_field_updates`) re-dated it, so a one-off rolled to the next paycheck read OVERDUE on the dashboard pulse and late in `payment_timeliness`, where the undated row it replaced sat on the *anytime this period* shelf. Worked: Kayla's Kindle `$162.25`, born in the paycheck starting 2026-09-04, carried forward to the 09-18 paycheck, paid 09-20 -- 14 days overdue and 16 late under the shipped behaviour, due 09-18 and on time under this ruling. R-BAL22's own words carry it: *due on its PLACED paycheck's start unless the owner states a date*, so the default follows the placement; an owner-stated day is read BY POSITION (any date other than the source paycheck's start), since **R-BAL25** rejected a stored marker -- the miss (an owner who typed exactly the start day is moved to the new start) is benign. Both doors take it. The balance is identical under every option (a one-off's series is flat, R-BAL21). Builds as the FIRST item of leaf 7b-2. **Rejected**: always re-dating (loses a typed day on every move); keeping the date (every rolled-forward one-off reads overdue); carry-forward re-dating while a popover move keeps (two rules for one act on two doors) |
| balance | R-BAL34 | -- | 2026-09-14 (developer) | **THE BUDGET GRID GROUPS A RULE-LESS DEFINITION'S ROWS BY `(category_id, name)`, AS IT GROUPED LINK-LESS ROWS; RECURRING DEFINITIONS' ROWS GROUP BY DEFINITION.** Found by 7b-1's review: every one-off carries a definition since that leaf, and `grid_view_service.build_row_keys` keyed a definition-linked row on `(category_id, template_id)`, so two same-named one-offs -- one grid row with two cells before -- became two rows both labelled by the name. The developer asked why they could not be combined and whether `bank_import:X-f6c` bore on it; the lane's answer that it did was WRONG and withdrawn: X-f6c is a merchant RULE needing a period-independent identity to file into, which how the grid groups rows for display does not touch. Ruled: display is cockpit grammar and identity is the correctness layer; **R-BAL20** rules identity and says nothing about grid grouping, so the grid keeps the grouping the owner had, with the predicate moved from *link-less* onto *does not recur* (7a's re-key at one more site: `build_row_keys` and `_match_row_in_period`). The six-cell double-display defect the spec measured (10.4 trace 5) stays fixed -- a recurring row matches by template and a one-off row excludes recurring rows -- and the cutover's grade (that diff, the row set otherwise byte-identical) is unchanged. A one-off renamed at the popover (7b-2) moves to a new row, as a link-less rename did. Builds as the SECOND item of leaf 7b-2 (the developer's option named 7b-2 or a follow-up commit; the coordinator had called hands-off on the branch, so 7b-2). **Rejected**: one grid row per one-off definition (the spec's trace 5 wrote it that way, and it made a repeated informal one-off a row per instance on the highest-stakes screen for no correctness gain) |
| balance | R-BAL35 | -- | 2026-09-15 (developer) | **A MOVEMENT'S SCOPE IS ITS PLAN ITEM'S, READ THROUGH THE LINK AND NEVER COPIED; `X-bi-2` DISSOLVES.** `X-bi-2` as minted (`db7898e9`) gave `budget.transaction_entries` a category, a type and `scenario_id` *backfilled from each entry's parent*, plus *the settle-day basis pair*. Measured on the tree: the pair and `reconciled_by_id` were on the entry before the step was minted (**X-az** `488e8dd2`; `d6d9692c`), and the other three are COPIES -- the cache **R-IY** deletes and the shape **R-JQ** re-specified `X-bi-1` over -- since **R-BAL20** / 7b-1 gave every entry a plan row one hop away, which `cash_ledger._events._posted_purchase_facts` already reads; two live doors would have staled them silently. Ruled, on the transfer legs' precedent (**R-BAL10**: *the two legs are its PROJECTION, so the value's home is the parent and each leg reads it*): an entry's category, type and scenario DERIVE through its parent -- the stronger case, since a shadow derives its MONEY and is deleted at `X-bi-6` while an entry OWNS its money and derives only whose plan it satisfies. Rejected: co-located keys (`account_id`'s own pattern on this table), right only if an entry must stand without its parent, a need 7b-1 deleted; and plain copies. `X-bi-2` DISSOLVES with no code and no migration. The one fact a movement lacks that its plan item cannot supply -- the provenance of its OWN figure, homeless once `X-bi-3` writes covering movements and `X-bi-4` makes `settled_amount` derivable -- is `X-bi-3`'s to state with its writer, in its own design loop (backfilled by predicate off the entry's day-basis, never from the parent; its catalogue is a fork). Carried forward: `X-bi-6` gives a transfer's movements `budget.transfers` as a second plan item, so *this movement's plan item* must be ONE total producer over both links (**R-IZ**). The measurements, the two doors and the option grid: `docs/design/from_scratch_architecture.md` 4.5, leaf 2. |
| balance | R-BAL39 | -- | 2026-09-15 (developer) | **A COVERING MOVEMENT STATES ITS OWN FACTS AND NOTHING ITS PLAN ITEM CAN SUPPLY; ITS FIGURE NAMES ITS SOURCE FROM A THREE-MEMBER CATALOGUE.** Ruled on `X-bi-3`'s design loop, the column **R-BAL35** left this step. A settle's covering movement -- the `budget.transaction_entries` row that records a bill's, paycheck's or transfer leg's money the way a purchase already records an envelope's -- carries where its FIGURE came from in a new ref catalogue of three members partitioned by the writer: `resolved` (the settle priced it from the plan), `typed` (a person stated it), `observed` (the bank's line stated it); the argument **X-az** made for the DAY's basis (three writers, nothing said which), and a reader needs it because a resolved figure is re-priced on a re-settle and a stated one is honoured (`Settlement.from_settle`). Today's purchase rows classify by their own `settled_day_basis_id` (observed -> `observed`, else `typed`), NOT NULL after. Rejected: reusing `ref.settlement_bases` (`purchases` is meaningless on a movement and needs a CHECK fence; a bank-stated figure would be filed as *a human typed it*); reusing `ref.settled_day_bases` plus `derived` (`asserted` is a day fact). The movement's `purchased_on` is the SETTLE DAY (the only day a bill's payment has; `ck_transaction_entries_settled_not_before_purchase` holds as equality) and its `description` is the plan's `name` as it reads at the settle, a constitutive fact like a purchase's merchant that a later rename does not rewrite; no column is made nullable (16 and 103 readers). The column lives on the MOVEMENT and is permanent; what a later leaf deletes under **R-IY** is the row's `settled_amount` / `settled_basis_id`, derivable once every settled figure is the sum and source of its movements. The write is ONE function both settle verbs call. |
| balance | R-BAL40 | -- | 2026-09-15 (developer) | **AMENDED 2026-09-18 BY R-BAL61 (the source arm) AND R-BAL62 (the downgrade clause).** **`X-bi-3`'S CUTOVER WRITES EACH SETTLED ROW'S COVERING MOVEMENT IN A MIGRATION BY A TOTAL RULE, FAIL-CLOSED; R-HJ IS SCOPED TO REPAIRS, AS R-BAL22 SCOPED IT FOR `X-bi-7d`.** The developer refused the door the re-cut implied (*a door and screen just for me to press a button once then remove all the code*), the 154 reverts and a command: *the data should already exist in the database*, and it does -- every column of a payment row is a function of the settled row's stored facts (`settled_amount`; source by predicate: `derived` -> `resolved`, else an `observed` day -> `observed`, else `typed`; `settled_on` + basis + `reconciled_by_id`; `name`; account; owner), a `$0.00` close writing none. **R-HJ**'s three objections do not hold: no judgment (a row with no settle day or a broken transfer pair REFUSES the migration with the count and the repair door, `9c1e4b7a2d3f`'s downgrade shape; the 2026-09-06 snapshot holds none); no second ledger writer (the posted ledger is reconcile-to-target and `posting_service.resync_all_cash_postings` re-derives it on every deploy, walking every row holding a posted purchase); no hard-coded owner data. Balance-neutral BY CONSTRUCTION on every account-day (**R-FM**'s identity: the parent's leg nets to zero and the movement books the figure), and the migration's docstring states the before/after fold equality it measured on a production restore. It is leaf `X-bi-3d`, AFTER 3b and 3c so income and transfer parents read correctly. Downgrade: delete exactly the entries whose parent does not track purchases (unwritable before this family), lossless because the row's `settled_amount` still stands. Rejected: a fold fallback treating an uncovered settled row as a virtual movement (two semantics live); a startup reconciler (a fence that never retires). |
| balance | R-BAL41 | -- | 2026-09-15 (developer) | **`X-bi-3` IS A CONTAINER OF FOUR LEAVES, ONLY THE LAST OF WHICH MOVES MONEY.** `X-bi-3a`: the figure-source catalogue and the ONE settle-owned writer, covering EXPENSE parents (bills, CC paybacks) on the manual branch -- balance-neutral by construction, graded by a before/after fold and posted-ledger equality over every account-day on a production restore. `X-bi-3b`: the INCOME arm -- `cash_ledger._events._posted_purchase_facts` (`is_income=False` hardcoded), `_posting_purchases.emit_purchase_deltas` (EXPENSE kind) and `posting_reads.settled_transaction_effect` derive a movement's direction from its parent's type (**R-BAL35**), then income parents are covered; the control is a paycheck reading `+figure` (uncorrected it reads `-figure`, wrong by 2x). `X-bi-3c`: both legs of a transfer through `transfer_service` (Invariant 4), one movement per shadow until `X-bi-6` re-parents them onto `budget.transfers`. `X-bi-3d`: the cutover migration (**R-BAL40**). Measured 2026-09-15 on the 2026-09-06 snapshot: 185 settled rows, 154 on the manual branch (38 shadows, 45 income, 22 CC paybacks, 9 empty envelopes), 0 without a settle day -- the row's *14* was the design doc's 2026-09-01 figure on another restore and is corrected here. Rejected: two leaves (the writer for every kind, then the cutover; the first no longer neutral by construction); one step as written. |
| salary | R-SAL29 | -- | 2026-09-13 (developer); AMENDED 2026-09-13 (developer) | **A RECURRENCE RULE'S CADENCE HAS A THIRD VALUE, THE PER-MONTH CEILING `max_per_month` (NULL = none), AND FOR A PAYCHECK CADENCE THE CEILING COUNTS THE MONTH'S PAYDAYS ON THE OWNER'S CALENDAR.** Asked at `R15` for ledger rows **F-21** and **SAL-549** (ex `recurrence:D43`): `deductions_per_year` stores 26 / 24 / 12 as a MODE, and the recurrence vocabulary could not say "every paycheck but the third of its month" (**D59**'s cadence half). The ruled vocabulary: `24` = every paycheck, at most 2 a month; `12` = every month, the first paycheck on or after the 1st; `26` = no rule (**R-SAL3**'s NULL). A SMALLINT with a positive CHECK, refused beside a MONTH or YEAR unit (`can_repeat_within_month`), placed by `_ceilinged` between the walk and the bound. AMENDED the same day after R15-a's adversarial review measured a start-month divergence: as first built, and as the option text worded it, the ceiling counted the RULE'S OWN occurrences in the month, so a `$100` allowance started 2026-07-16 (a month's SECOND payday) admitted 2026-07-30, its third -- D59's own defect; for a paycheck cadence it now counts the month's paydays on the owner's calendar (`pay_calendar.paydays_in_month_through`, the engine's month-ordinal producer), so a rule started on a month's second payday still skips its third, while a WEEK cadence keeps the rule's own first N. Rejected: the rule's own occurrences for every unit (above); a semi-monthly unit (skips the WRONG paycheck in July 2027 on the developer's phase); a set-valued placement; migrating 24 to every paycheck (`+$1,034.42`/yr); a bespoke column on the deduction (R-SAL3's own rejection). Shipped at `R15-a` (`bdd77055`, migration `ef32dfe4cd8e`); the code cites "R-SAL29 as amended 2026-09-13". |
| salary | R-SAL30 | -- | 2026-09-13 (developer) | **AMENDED 2026-09-15 BY R-SAL38 (2), BUILT AT `R18-c`: THE FORM OFFERS A START FIELD; WHAT A BLANK IS -- STORED AS BLANK, RESOLVED AT THE TRUE OPENING -- IS R-SAL39.** **A PAYROLL LINE'S RULE HAS ITS FIRST OCCURRENCE AT THE OWNER'S OPENING PAYDAY, AND THE FORM OFFERS NO START FIELD.** Asked at `R15` for the `starts_on` a deduction's rule carries, since a paycheck line has no template to anchor on and **R-SAL3** made its cadence a rule. Ruled: the opening payday of the owner's schedule, with no form field. The D39-shaped stale bound if a later `history_opens_on` is stated is to be recorded in R15-b's migration docstring (`$0.00` today: no live line is capped, no opening stated). Applied at `R15-b`. |
| salary | R-SAL31 | -- | 2026-09-13 (developer) | **AMENDED 2026-09-15 BY R-SAL38 (2), BUILT AT `R18-c`: THE END-BOUND ROW IS ON; ONLY THE DUE-DAY ROW STAYS OFF.** **THE DEDUCTION FORM REUSES `_recurrence_fields.html`: THE END-BOUND AND DUE-DAY ROWS OFF BY FLAG, THE PER-MONTH CEILING ON.** Asked at `R15` for how a deduction's cadence is authored once the 26 / 24 / 12 select goes. Ruled: the one recurrence partial every definition's form already renders, flagged for what a payroll line cannot carry (no stop, no due day), the ceiling control on. Built as `R15-c`. |
| salary | R-SAL32 | -- | 2026-09-13 (developer) | **`R15` IS A CONTAINER OF THREE LEAVES -- `R15-a` THE VOCABULARY AND ITS MIGRATION (`$0.00`), `R15-b` THE OWNING ARM, THE ENGINE AND THE MIGRATION DROPPING `deductions_per_year` (BYTE-IDENTICAL OVER THE 63 SAVED PAYCHECKS), `R15-c` THE FORM -- AND LEDGER `recurrence:D43` RE-HOMES TO SALARY AS `SAL-549`, OWNER `R15-b`.** Asked at `R15`'s decomposition (**R-SAL6**'s shape). D43 (`deductions_per_year` is F-16's closed 26/24/12 world-view one table over) named owner `R7e`, whose sentence closes only D36; its subject is the cadence R15 designs, so the row moves arcs with its remedy. **N-395** (its reader deleted at `R14-b`, `e0f0c05f`) leaves the ledger at R15-a's tick; **D59**'s cadence half is answered by R15-a and its text narrows to the earnings-lines half (`R18`). Applied at `R15-a` (`bdd77055`). |
| salary | R-SAL33 | -- | 2026-09-13 (developer) | **EVERY REFUSAL OF THE READINESS WHAT-IF IS THE ASSUMPTIONS RAIL, RE-RENDERED AT 422 WITH EACH MESSAGE ON ITS CONTROL.** Ruling on `S3-f-4`'s first fork (ledger **SAL-548**): `GET /retirement/readiness` answered a refused raise probe (and every other refused input) with a JSON the panel never swapped in, so a bad year left the card silently at its previous picture, in two 422 shapes nothing consumed. The GET now answers every refusal with the Save's exact 422 body -- `_assumptions.html` through the ONE renderer both routes share, `{control: [messages]}` through `errors_by_rail_control`, the query string echoed -- delivered to `#assumptions-region` by htmx's own `HX-Retarget` header, because the request's trigger targets the readiness card; the card keeps the picture it last drew, as on a Save's refusal. The raise rows and the SWR box render their refusals (the SWR what-if arrives as `swr` and lands in the box AND its mirror; the box's `max` fences only a form submit, so 150 reaches the schema); a refused assumed return, months or contribution -- controls outside the rail -- renders nowhere yet, a known opening. `RaiseProbeError.errors` carries WHICH HALF was refused, `{raise_id: (field, message)}`, keyed by the one `_refused_probes_by_control` the Save's refusals and failure arms use; the rail's three rows come through one loader, `load_retirement_rows`, which `load_gap_inputs` composes. Rejected: the rail for the rail's own controls and JSON for the rest (two answers on one route); the rail for the raise probe alone (keeps a JSON shape nobody consumes for a control inside the rail); an out-of-band rail beside `HX-Reswap: none` (two mechanisms and a wrapper for one body). Under rule 5's exception: `test_garbage_params_are_422`'s JSON assertion became the rail's; the three probe route tests read the rail. `$0.00`: display-only. Shipped at `S3-f-4` (`329b663d`). |
| salary | R-SAL34 | -- | 2026-09-13 (developer) | **A RAIL ROW'S REFUSAL RETIRES THE MOMENT ONE OF ITS WHAT-IF CONTROLS IS EDITED.** Ruling on `S3-f-4`'s second fork: the readiness GET's SUCCESS never re-renders the rail (a re-render on every debounced refresh would replace the input the owner is still typing in and reset unsaved date and tax edits), so a refusal rendered on a raise row by **R-SAL33** would otherwise outlive its correction -- "cannot end before it starts" in red beside a card drawn at the corrected year. `retirement_controls.js` drops `is-invalid` from the row's controls and removes its feedback line on the first `input` event in any `.js-whatif-input` of that row (a raise row's two controls share one feedback line, and a refusal of either half is about the pair); the server's next answer -- 200 or 422, GET or Save -- re-states whatever still holds. The message is bound to the value it refused (the ordinary form idiom; a refused Save clears the same way). Rejected: leaving the message until the next rail render (the Save's precedent is click-driven; the probe is keystroke-driven); re-rendering the raise rows out-of-band on success (clobbers keystrokes in flight). Two hazards of re-rendering the input under the caret, found by the step's review and measured: a superseded request's answer could land after a newer keystroke (the trigger carries `hx-sync="this:replace"` and the input handler aborts the request in flight on every keystroke), and Chromium's `focus()` on the swapped-in number input put the caret at the START ("02025"; the rail's afterSwap handler re-assigns the value on the refusal branch). Graded in a browser by `tests/manual/verify_retirement_rail_refusal.py` (28 checks: the retarget, the untouched card, no reload, the clear, the caret, the held-answer sequence, the re-stated refusal, the SWR box, nothing written; the designed 422 and abort console lines counted; green 2026-09-13 on a clone). `$0.00`. Shipped at `S3-f-4` (`329b663d`). |
| salary | R-SAL35 | -- | 2026-09-13 (developer) | **`R15-b` AND `R15-c` RIDE IN ONE PR.** Ruling on `R15-b`'s open question (its arc entry): `R15-b` drops `salary.paycheck_deductions.deductions_per_year` and makes a line's cadence a recurrence rule on the row, `R15-c` rebuilds the deduction form around the shared recurrence control (**R-SAL31**), and between the two the form has no way for a NEW deduction to state its cadence -- eleven of the developer's twelve live lines are 24s. The two stay two LEAVES -- two commits, two adversarial reviews, `R15-b` graded byte-identical over the 63 saved paychecks (and a 585-paycheck, 20-year projection), `R15-c` by a browser drive -- but ride in ONE PR, so the deduction form is never deployed without its control and the interim has no state to answer: on `R15-b`'s commit the 26 / 24 / 12 select, its `app.js` prefill, the schema field and the update-field entry are gone, the Frequency cell reads each line's rule through the recurrence package's one phrase producer (`app/services/deduction_cadence.py`; "Every paycheck" for no rule), and a line added through the form carries no rule until `R15-c`'s commit in the same PR. Rejected: `R15-b` keeping the select as a TRANSLATOR that authors the migration's three rule shapes from the route (the shapes spelled twice until `R15-c` deletes the second spelling); `R15-b` removing the select with new lines every-paycheck on a deployed tree (a 24 line unrecordable until `R15-c`). `$0.00`. Shipped at `R15-b` (`4ed9b5b3`). |
| salary | R-SAL36 | -- | 2026-09-14 (developer) | **A DEDUCTION RULE'S DERIVED FIRST OCCURRENCE IS THE UNIT-NATURAL ZERO AT THE OPENING.** Ruling on what **R-SAL30** ("a payroll line's rule starts on the owner's opening payday; no form field") means for a calendar cadence, where `starts_on` is also the cycle's DAY: `PERIOD` -> the owner's opening payday; `MONTH` -> the 1st of the opening payday's month; `YEAR` -> January 1st of the opening payday's year (`app/services/deduction_cadence.first_occurrence`). So "months, every 1, funded from the first paycheck on or after" authored on the deduction form is the month's FIRST paycheck -- byte-for-byte the shape migration `542c61e48ee8` wrote for a 12 (`date_trunc('month', opening)`), and the only monthly shape its downgrade reads back. Worked on a $100 line, biweekly paydays Jan 15, 29; Feb 12, 26; Mar 12, 26; Apr 9, 23 (opening Jan 15): the ruled reading lands Jan 15, Feb 12, Mar 12, Apr 9 ($1,200/yr, the month's first paycheck); R-SAL30 read literally would anchor on the 15th and land Jan 15, Feb 26, Mar 26, Apr 23 -- the same $1,200/yr on the month's SECOND paycheck most months, the 15th an accident of when the schedule was generated. Rejected: R-SAL30 literally for every unit ("monthly on the 15th", refused by the downgrade as a cadence 12 never meant); anchoring on TODAY's month/year (a quarterly line's phase would depend on the day it was saved, and saved paychecks before today would price unlike the migrated lines); a form field (R-SAL30). `$0.00`: no live line is authored by the form yet. Shipped at `R15-c` (`77901fe0`). |
| salary | R-SAL37 | -- | 2026-09-14 (developer) | **THE DEDUCTION FORM OFFERS THE PICKER'S WHOLE OFFER SET, `YEAR` INCLUDED.** Ruling on the runbook's open question whether the deduction form filters the recurrence picker: it renders `app.services.recurrence.picker_model()` unfiltered -- paychecks, months at both placements, years at both placements -- ONE producer for every recurrence form, and the R15-b engine walk (`payroll_basis._WalkedCadence`) is unit-agnostic. Worked example: a $500 annual line "years, every 1, first paycheck on or after" starts Jan 1 of the opening year (R-SAL36) and is taken on the first paycheck of each year, $500/yr. Rejected: filtering `YEAR` out for deductions -- a second offer set (a fence over the picker) plus a schema refusal for the pair, and it buys nothing, because "months, every 12" is canonicalised to `(1, YEAR)` at the write door by **R-R17** and the same rule is stored either way. `$0.00`. Shipped at `R15-c` (`77901fe0`). |
| pay_calendar | R-PC82 | -- | 2026-09-13 (developer) | **THE `pay_periods_generated` EVENT CARRIES THE RHYTHM AS ITS KIND'S PHRASE.** Ruled on `C17-d-2`'s fork, interactively. The event's `cadence_days=` field had nothing to say for a month-kind era, so it is REPLACED by `cadence=`, holding the phrase each cadence kind states about itself (`FixedDays.phrase` "every 14 days", `Monthly.phrase` "monthly on day 15", `SemiMonthly.phrase` "twice a month on days 1 and 15" -- `recurrence.describe`'s "day 15" grammar, no ordinal suffix), on both emitters (`record_paydays`, `continue_paydays`). One field every kind fills, human-readable in the log; `test_service_log_events` re-asserts `record.cadence == "every 14 days"`. Rejected: a structured `cadence_kind` + per-kind parameter fields (three field shapes for one fact); keeping `cadence_days=` beside a phrase (a field `None` for two of three kinds). |
| pay_calendar | R-PC83 | -- | 2026-09-13 (developer) | **REGISTRATION'S "ALREADY ENDED" REFUSAL NAMES THE PAYDAY ON THE OWNER'S STATED RHYTHM WHOSE PAYCHECK COVERS TODAY.** Ruled on `C17-d-2`'s fork, interactively. `registration_service._reject_impossible_first_payday` named `E - cadence_days` -- the earliest first payday whose paycheck covers today, "X or later" -- a spelling no month grid has. It names the grid's own day now: `nominal_payday(fp, c, cadence_steps_to(fp, c, E - 1))`, the last day on the stated grid strictly before `E` (the earliest nominal day paid after today), worded "on that rhythm the paycheck covering today opened on {day}; enter that payday". ONE spelling for all three kinds; for a day-of-month rhythm it is the unique legal answer (the grid is fixed by the stated day), for a fixed-days rhythm one legal answer among `[E - days, today]`. The refusal SET is unchanged: `fp + c >= E` iff the last grid day before `E` is `fp` itself. Worked: sign-up today with last payday `today - 20` at 14 days named `today - 13` and names `today - 6`; the MLK 2026 case (fp 01-05, prior, today 01-16) names 01-19 where it named 01-06. One assertion in one test moved (`test_auth_service`). Rejected: dispatching by kind (two spellings of one boundary, rule 14's tell); naming both days (the fixed-days message grows a second date). |
| pay_calendar | R-PC84 | -- | 2026-09-14 (developer, interactive) | **THE CADENCE KIND IS STATED ON THE FOUR DOORS AS RADIO ARMS, WITH NO SCRIPT, AND THE SCHEMA HANDS BACK THE VALUE.** Ruled on `C17-d-3`'s wire-shape fork with the full option space. Registration, generate, regenerate and reset render THREE RADIO ARMS -- every N days, monthly on one day, twice a month on two days -- each holding its own inputs, all visible and all submitted; no JavaScript. Wire keys: `cadence_kind` (`fixed_days` / `monthly` / `semi_monthly`, **R-PC80**'s tokens, mapped to the value's TYPE by `CadenceKindField`) beside `cadence_days`, `day_of_month`, `first_day_of_month`, `second_day_of_month` -- DISTINCT names per arm, since a duplicate form key loads its FIRST value (Werkzeug's `MultiDict` under marshmallow 4.3.0). `rhythm_from_wire` reads the chosen arm only; the mixin's `@post_load` puts the `Rhythm` on the payload as `rhythm` and CONSUMES the wire keys, so no route spells a constructor; `rhythm_to_wire` is the ONE inverse (the regenerate banner, the manage card's preselection of the STORED rhythm); one macro (`_pay_cadence_fields.html`) on all four doors. `validate_derivable_rhythm` asks the writer's three predicates in its order: range/pair, `reject_phase_off_grid`, the collision floor. Four assertions reading `data["cadence_days"]` off a LOADED payload re-pointed at `data["rhythm"]`. Rejected: a kind `<select>` with revealed inputs (a script on three pages); free text parsed server-side (a second grammar); a preset select (cannot state the 5th/20th; **R-PC79**); inferring the kind from the filled boxes (**R-PC80** names a control; two filled shapes are ambiguous); an encoded single value (a JS-composed mini-grammar). `$0.00`. Shipped at `C17-d-3` (`4420df87`). |
| credit_card | R-CC13 | -- | 2026-09-15 (developer) | **THE CARD ROWS' BLANKET WAIT ON `balance:X-f4` IS RE-DERIVED PER LEAF: each `CC` leaf is traced against the `X-bi` chain's files and the flip's semantics, a leaf with no real dependency becomes `NOW`, and the blanket `X-f4` wait becomes per-leaf waits, so the card arc can start in its own lane while the balance chain runs.** Eighteen of the nineteen `CC` rows read `after balance:X-f4` (`CC4d` reads `CC4c`), and the recorded reason (`docs/audits/balance_architecture/README.md` section 5.0, block 4) binds two leaves: `CC1b`'s fold is specified against the reset semantics the flip `balance:X-f3c-5` deletes, and `CC3b` derives a settle from `paid_at`, which `X-f1b` deleted -- a binding `CC3b`'s own cell already records as discharged (`balance:X-f1`, shipped), though its specification still names the deleted column. Ruled with the 2026-09-15 outcome order in `steps.md`, which names the card arc in production as outcome O2; until the trace lands, O2's path is the graph as it stands. |
| bank_import | R-BI8 | -- | 2026-09-15 (developer) | **THE SIMPLEFIN DAILY FEED `X-f6b` IS AN OUTCOME (O4 of the 2026-09-15 order in `steps.md`), SUPERSEDING `R-JL`'S PLACEMENT OF IT BELOW THE CARD ARC; AND `X-gl-1`'S WAIT ON `credit_card:CC5b` IS DELETED.** `R-JL` applied the bank_import-first priority to rank three importer enhancements below `credit_card:CC5b`; with that priority discharged the developer ruled the daily feed an outcome in its own right. Of R-JL's other two placements, `X-f6c` is now `balance:X-bi-7b`'s alias and ranks with it on O1's path, and `X-f6g` keeps its wait on `balance:X-bj-1`, whose coverage it reads. `X-gl-1`'s wait was the 2026-09-04 RANK ruling (*immediately after `credit_card:CC5b`*) spelled as a dependency, and nothing in `X-gl-1` reads a card row; under the tiers the reward rank carries that priority, so the wait goes and the row reads `NOW` (conventions.md rule 13: the column is the dependency graph). |
| bank_import | R-BI9 | -- | 2026-09-15 (developer, during the first production matching session) | **THE MATCH PANE SHOWS A CANDIDATE ROW'S BUDGETED PLACEMENT AND, FOR AN ENVELOPE ENTRY, ITS PURCHASE DAY, EVERY DATE LABELLED BY KIND (settled / purchased / budgeted), BESIDE THE BANK LINE'S OWN DATE; THE SETTLE STAMP IS NOT A DAY TO SHOW.** Ruled at 20:3x-20:4x EDT after the pane labelled a `$47.61` BJ's fuel entry purchased 2026-07-13 as "2026-08-18" -- the day a true-up settled 66 of Checking's 110 envelope purchases in one act -- against the 2026-07-14 bank line, and the developer, unable to verify proposals by date, stopped at 22 of 172 lines (**BI-498**). Printing the gap to the bank's date finishes the comparison. The step is `X-gz`, outcome O0 of `steps.md`'s order, ranked first the same night. **Two sub-rulings at the build (developer, 2026-09-16, the midnight boundary): the settle day is NEVER printed on the MATCH pane, whatever its basis (the literal reading); the pane's dates are in the paycheck register's MM/DD (`DerivedPeriod.label`, `utils.dates.day_label`) while the intro sentence and card header stay ISO.** Shipped at `X-gz` (`08a66901`): `CandidateRow` carries `period` and `purchased_on`, `_dating.py` presents, one macro for both row lists; the stamp's rule is now `accounts.css:1205-1211` and `.rec-row-day` names the per-fact span. |
| balance | R-BAL36 | -- | 2026-09-15 (developer) | **THE GRID POPOVER OFFERS A ONE-OFF'S NAME, CATEGORY AND BOTH FLAGS, ALL LANDING ON ITS DEFINITION.** R-BAL23 says a one-off's *name, category, flags and price are edited at the popover and land on the definition*; the `X-bi-7b-2` step sentence said *flag edits*, and the popover offered no name or category control at all (a crafted PATCH landed both on the ROW alone, leaf 7b-1's stated interim). Ruled to R-BAL23's letter from three options: two controls added for a PLACED row (`Transaction.is_placed`, the accessor minted for the predicate five doors spelled for themselves), the category select disabled on a finalised row as the period select is, a rename or re-category answered with `gridRefresh` because the row moves to another grid row (R-BAL34); the definition-edit act became ONE service both doors call (`definition_edit.apply_fields` + `propagate_to_unruled_rows`, a pure move out of `routes/templates`, commit `1e77e1e3`). A category-less placed row (the bank door's, R-FN) shows a placeholder posting the empty value the schema drops, so a one-off is never UN-categorised at the popover. **Rejected**: flags only (R-BAL23's name/category half unbuilt with no step named for it); name + flags without category (no principle picks it); name/category through *make this repeat* (R-BAL23's own rejected second door) |
| balance | R-BAL37 | -- | 2026-09-15 (developer) | **A TYPED FIGURE ON A ONE-OFF'S ROW THE 7b-1 INTERIM DETACHED RESTATES THE DEFINITION AND RE-ATTACHES THE ROW.** Between leaf 7b-1 (deployed 2026-09-14) and 7b-2 a typed figure landed OWN on a placed row with `is_override` beside it (R-BAL28's stated interim), the definition left at the old price -- rule 14's two homes. Worked: definition `$162.25`, row OWN `$170.00`, owner types `$180.00`. Ruled: `one_off.restate_price` corrects the version the row's date reads (R-BAL29) and then declares the row TEMPLATE-priced with the flag cleared -- idempotent on a row never detached, so it is the ONE act for every placed row's typed figure; the definition, the grid and the Recurring page all read `$180.00`. Residue rows nobody edits stay detached until the cutover migration (`X-bi-7d`) re-attaches them, a spec addition for that step; their count on production is UNMEASURED (the production read was refused; the newest clone predates 7b-1). A category edit on a residue row reaches the definition and the propagation skips the overridden row, as it skips any. **Rejected**: restate only (the definition takes `$180.00` while the row stays OWN `$170.00`, so the typed figure vanishes from the grid); keep detaching a detached row (OWN `$180.00` beside a definition at `$162.25`, the crossing R-BAL21 rejected) |
| balance | R-BAL38 | -- | 2026-09-15 (developer) | **A PROJECTED TRANSFER'S LEGS ARE READ OFF THE PARENT BY EVERY BALANCE READER, THE LOAN PARTITION INCLUDED, AND TRANSFER INVARIANT 5 SAYS SO.** Ruled on `X-bi-6a`'s two forks. (1) SCOPE: the README's "FOUR sites" undercounted by one -- `loan_loaders.income_shadows()`'s projected half had TWO consumers, `balance_at/_plan.py` and `loan_ledger.payment_installments` -> `get_payment_history` -- so the PARTITION is re-pointed once and both consumers read legs derived from `budget.transfers`; re-pointing `_plan.py` alone would have left two producers of a loan's projected payment set (rule 14's tell). The reconcile panel's offer list (`reconcile_service._transfers.outstanding_transfers`, a settle DOOR keyed by shadow id) stays on shadows for `X-bi-3` / `X-bi-6`. (2) TRANSFER INVARIANT 5 is rewritten in the same commit, in `CLAUDE.md` and `.claude/agents/code-reviewer.md`: the PLAN half derives from `budget.transfers` (each still-projected leg priced by `resolve_transfer_amount` on the parent), the RECORD half reads `budget.transactions` until `X-bi-4`, no amount is read off a transfer for a settled row, and **R-R66**'s occurrence-identity clause stands. As built: the one leg loader is the new leaf `app/services/transfer_legs.py` (`PlannedTransferLeg(transfer, account_id, is_income)`), below both `cash_ledger` and `loan_loaders` because the first imports the second; `$0.00` on the 2026-09-15 dev snapshot (175 transfers, 350 shadows, 0 drifted) and byte-identical across four manual harnesses with 196 legs live; where a pair HAS drifted on period, due date, deletion or scenario the plan follows the PARENT (**R-JA**'s direction), pinned by firing controls. Record: `docs/audits/balance_architecture/archive/x_bi_6a_as_built_2026-09-15.md`. |
| balance | R-BAL42 | -- | 2026-09-15 (developer) | **A STATUS DRIFT BETWEEN A TRANSFER AND ITS SHADOW IS DISCLOSED AND PINNED, AND NOTHING IS BUILT TO TEAR DOWN.** `X-bi-6a`'s neutral review refuted the step's own "double count is unrepresentable" sentence: a settled shadow beneath a still-Projected parent -- a drift Transfer Invariants 3 and 4 forbid and no door writes, 0 of 350 on the snapshot -- is counted by BOTH halves once the plan half reads the parent (`$250.00` checking -> savings folds checking at `-$500.00`), and the reverse drift by neither, where the shadow-reading fold counted either once. The developer ruled *disclose and pin, build nothing to tear down*: the false sentences are corrected in `CLAUDE.md`, the reviewer mirror and three docstrings; both outcomes are pinned in `tests/test_services/test_transfer_legs.py` with `X-bi-4` named; the structural end is `X-bi-4` + `X-bi-6`, after which a transfer's status lives in ONE row. Refused: a refusal in the record half (a fence `X-bi-6` deletes and `X-bi-4` may have to un-teach) and keying the record half off the parent's status now (`X-bi-3`'s live territory, a discriminator `X-bi-4` deletes). |
| salary | R-SAL38 | -- | 2026-09-15 (developer, six forks ruled) | **A PAYCHECK IS BASE PAY PLUS A LIST OF LINES, AND A LINE'S KIND IS ITS POSITION IN THE WATERFALL.** (1) `salary.paycheck_deductions` becomes `salary.paycheck_lines`; `ref.deduction_timings` (pre_tax / post_tax) becomes `ref.paycheck_line_kinds` with FOUR values in waterfall order: `taxable_earning` (joins gross: FICA and withholding), `pre_tax_deduction`, `post_tax_deduction`, `after_tax_earning` (joins net, untaxed); sign and stage derive from the kind; the 12 live rows backfill from their timing; one door, one form section, one engine pass, the third owning arm renamed. Rejected: a sibling earnings table with a parallel door; negative deductions (FICA differs); income templates flagged as riding in the deposit (**R-JJ**, **R-SAL3**); two columns side x timing. (2) EVERY line carries a start and an optional end on its own rule, amending **R-SAL30** and **R-SAL31**: blank start = the opening payday (**R-SAL36**'s unit-zero applies), the end-bound row on the shared partial turns ON -- the developer's `$100.00` Health Insurance Allowance ran 2026-03-26..2026-06-30 and is unrepresentable without an end. (3) A PERCENTAGE line is a percentage of BASE PAY (the salary rate), never of gross: existing lines byte-identical whatever earnings exist, no circularity for a percentage earning, the employer-match resolver reads the same base (worked example in the R18 specification). (4) The two live income templates are an OPERATOR ACT through the doors (**R-HJ**), never a migration: the Phone template ended as of August 2026, the phone line entered on the salary page (`$45.00` GROSS, taxable, the first paycheck of every month, start 2026-09-01); the ended health template and its 7 Received rows stay as history. Stub facts ruled: the Health Insurance Allowance ENDED June 2026; the Phone Allowance is TAXABLE at `$45.00` gross (the `$39.54` the rows carry was the hand-computed after-tax residue). |
| balance | R-BAL43 | -- | 2026-09-16 (developer) | **A TYPED BUDGET ON A ROW OF A RULE-LESS DEFINITION THAT HOLDS ANOTHER ROW IS THAT ROW'S OWN; A ONE-ROW ONE-OFF KEEPS THE IN-PLACE RESTATE.** Closes **BAL-499**, found by 7b-2's adversarial review: R-BAL21 gives a one-off's series ONE version and R-BAL29's restate corrected it in place, so once R-BAL24 / leaf 7b-3 gives a bank-born envelope one placed row per paycheck, `$100.00` typed on one paycheck's Amazon card re-budgeted every paycheck's (measured: two rows, `$120.00` typed at row 1, row 2 resolved `$120.00`). Worked: rows in the 09-04 and 09-18 paychecks both budgeting `$0`, `$100` typed on 09-18's card. Ruled: the popover's field step forks on `definition_delete.is_last_row_of_its_definition` -- the only row restates the definition through `one_off.restate_price` and re-attaches (R-BAL29 / R-BAL37); a row with a sibling takes the figure OWN with `is_override` through `amount_ownership.state_own_amount`, exactly as a recurring definition's row re-priced at the card (R-BAL21's *one occurrence among many*), so 09-04 and every future paycheck stay `$0` and the definition's standing price is edited on its own form with an effective date. The OWN write runs AFTER the definition's propagation so a category typed beside the figure reaches the row it was typed on (found by 7b-3's adversarial review); the propagation of a later definition edit skips such a row as it skips any overridden one. **Rejected**: one price for the definition (7b-2 as built: every paycheck's envelope takes the last budget typed on any of them); a version as of the row's date (the popover editing a definition's price series, which it does for no recurring definition, and R-BAL29's rejected append for many-row definitions only) |
| balance | R-BAL44 | -- | 2026-09-16 (developer) | **A RULE-LESS ENVELOPE'S ROW TAKES CARRY-FORWARD'S ROLLOVER LIKE A RECURRING ENVELOPE'S; A PLAIN ONE-OFF MOVES WHOLE.** Closes **BAL-496**: carried whole, a placed row re-dated onto the target paycheck's start (R-BAL33) collided on `idx_transactions_template_scenario_occurrence` with a sibling the same definition already held there, a 500 that rolled the batch back. Worked: Amazon rows in 09-04 (`$45` spent, `$0` budget) and 09-18; a "Camping" one-off envelope, `$200` budget, `$150` spent, no sibling. Ruled: `carry_forward_service._context` routes every envelope OF A DEFINITION to the envelope branch; the source settles at its purchases and the unspent budget rolls into the sibling (TOP_UP, OWN) or into a row of the definition PLACED in the target through `one_off.place_row_of`, started at `$0.00` OWN so the bump lands on the leftover alone (Camping: a new `$50.00` row, R-BAL43's shape) -- R-BAL20 trace 4's rejected shape (ii), taken now that R-BAL24 amended its one-occurrence premise. **The refusals** (corners the loop did not see, built as designed refusals): a rule-less envelope whose target row has FINALISED, or whose own row already answers the target's start (`_TargetKind.CLOSED`, two arms; `BLOCK_CLOSED_TARGET`), and a PLAIN placed row carried into a paycheck already holding its sibling (`one_off.holds_a_row_in`, the discrete branch) refuse rather than meet the index; a LEGACY link-less envelope moves whole until the cutover mints it a definition. **Rejected**: refuse and report every such row; keep its date on the move (two rows of one merchant in one column); merge the purchases into the sibling (re-files postings under another paycheck's budget line). |
| balance | R-BAL45 | -- | 2026-09-16 (developer) | **THE LEDGER'S SHAPE FOR A TRANSFER IS C -- TWO MOVEMENTS, EACH POSTED AS ITS OWN JOURNAL ENTRY AGAINST A TRANSFERS-IN-TRANSIT CLEARING ACCOUNT ON ITS OWN BANK DAY -- RULED AS THE ENDPOINT; `X-bi-3c` SHIPS THE MOVEMENT ROWS ONLY.** Forced by three ruled premises: the fold reads movements only (3.1, `X-bi-4`); the bank is the default source and each statement shows ITS side on ITS day (3.4); the ledger's claim is per account per DAY, and one entry carries one date. Worked on `$500.00` Checking -> Savings posting 9/14 and 9/15: one entry `{Chk -500, Sav +500}` is wrong on one account for a day whichever date it takes; `{Chk -500, Transit +500}` 9/14 and `{Sav +500, Transit -500}` 9/15 agree with both banks and Transit nets 0. The one-entry shape is what the shadow MIRROR makes sufficient (Transfer Invariant 3 forces one day per pair; the reconcile tick on Savings' statement re-dates Checking's leg), and `X-bi-6` deletes the mirror. 3c = C's first leaf: the seam's transfer gate deleted, the transfer's one entry kept off the income shadow's record, a shadow's movement posting NOWHERE through the interval (every purchase-posting door returns for a shadow); walk and ledger agree because the seam mirrors ONE record into both homes (**R-BAL40**'s interval). `X-bi-4` builds NO one-entry-off-the-movement interval; `X-bi-6` deletes Invariant 3's day clause, adds the clearing kind, re-keys the loan probes onto the loan-side movement and posts C. Rejected: (A) the purchase source -- its counter leg is the parent's CATEGORY account, so a transfer between the owner's own accounts reads `$500` expense AND `$500` income, and `_transfer_nets_by_date` reads a mortgage payment as unposted; (A') one entry sourced from the movement now -- refuses the 38 uncovered production shadows until 3d; building C inside 3c -- decides X-bi-6's forks in a `$0.00` leaf. Record: `HANDOFF-X-bi-3.md` s.8. |
| balance | R-BAL46 | -- | 2026-09-16 (developer) | **AN ENDPOINT MOVE OF A SETTLED TRANSFER CARRIES ITS COVERING MOVEMENTS: `fk_transaction_entries_parent_account` RECREATED `ON UPDATE CASCADE` BY A DDL-ONLY MIGRATION (`c4e8a2d7f1b3`), AND `_apply_endpoint_move` ASSIGNS THE MOVEMENTS' ACCOUNT TOO.** Measured, not predicted: the full suite on the gate-deleted tree flipped 8 of 14,325, five of them R10-b's endpoint-move cases refusing with `violates foreign key constraint "fk_transaction_entries_parent_account"` -- the shadow's `account_id` moves and a non-deferrable key checks the parent's UPDATE before the child's the same flush would issue. The key already says *the entry's account IS the parent's* on DELETE; saying it on UPDATE is `account_id`'s own co-located-key pattern (**R-BAL35**) and lets no writer move a parent without its movements. The Python assignment is for the SESSION alone (the ORM never learns a cascade; measured: cascade alone leaves the in-session object on the old account) and is read for a settled leg only. Trade stated: the `NO ACTION` key refused any parent-account move leaving an entry behind, an envelope's purchases included; no writer performs it (the maintain pass retains such a row, N-292) and `X-bi-6` restores `NO ACTION` with the shadow rows (**BAL-506**). A statement-linked or matched leg still refuses (**BAL-503**), as the shadow itself does. Rejected: `DEFERRABLE INITIALLY DEFERRED` (refuses at COMMIT, far from the door); release before / re-cover after (id churn; the endpoints leaf learns the seam's lifecycle); refusing settled moves (deletes R10-b's shipped contract). The census's third class -- the seam's lazy load of `entries` autoflushes and killed `_update._bump_parent_version_if_a_leg_moved`'s `is_modified` predicate -- was a DEFECT fixed at the root (version snapshots), not a fork. |
| balance | R-BAL47 | -- | 2026-09-16 (developer) | **THE LEVEL RELATION IS `budget.account_anchor_history` WIDENED IN PLACE.** Ruled at `X-bj-1a` on fork F1 of **R-IS**'s relation. It gains `evidence_id` (NOT NULL, `ref.statement_balance_evidence`) and `statement_import_id` (nullable, two composite keys onto the import: `(id, account_id)` so a level cannot name another account's statement, and `(id, stated_balance)` over the new superkey `uq_statement_imports_id_stated_balance` so a bank level's figure IS its file's claim and an import with no claim can own no level); it keeps its name, id sequence, the two clearing-link key targets, its append-only triggers and its audit history. Rejected: a new `budget.balance_levels` (the same columns and normal form; it would re-point two composite keys on money rows, re-mint ids and break 88 rows' `system.audit_log` continuity for a name), and a VIEW over both tables (two homes with a reconciler, and the release stays an UPDATE) |
| balance | R-BAL48 | -- | 2026-09-16 (developer) | **A LEVEL'S SOURCE IS ITS IMPORT KEY'S NULLABILITY: `statement_import_id IS NULL` MEANS THE OWNER DECLARED IT, AND THERE IS NO TYPED `source_id`.** Fork F8 at `X-bj-1a`. The key is not a proxy for the fact, it is the fact: `fk_anchor_history_statement_import_claim` locks a row's amount to its import's stated figure, so any row naming an import is a bank level by construction, and no third writer exists (`pay_calendar:R-PC80`'s shape: absence is the discriminator when the stored shapes are disjoint). A typed column would copy `statement_imports.source_id` on bank rows, be a constant on the owner's, and could not be CHECK-paired to a nullable key without freezing a ref id into the schema, so the writer would maintain the pairing. Class-table inheritance (a base `(account, day, amount)` plus a statement subtype table) was rejected on robustness: an import delete would cascade the subtype row and leave the base row standing as an owner-declared level that the cash reset then replays; SQL cannot say "the parent dies with this child". The badge and the history card derive the source label from the join |
| balance | R-BAL49 | -- | 2026-09-16 (developer) | **AN OWNER'S LEVEL CARRIES `evidence_id = uncorroborated`, BY THE COLUMN'S DEFAULT.** Fork F2 at `X-bj-1a`: the enum's own definition ("nothing confirms it; taken at face value, which is what a first import is"), so a typed figure and a first import's assumed-day anchor rank equal and a `file_chain` statement outranks both. The default is a model callable resolving the id through `ref_cache` (the `recorded_on = display_today` precedent), so the suite's direct constructions keep working and the one owner writer names no evidence; the import door sets a bank row's from what it solved. Rejected: a fourth ladder member `owner_declared` (a SOURCE smuggled into an evidence ladder that is exhaustive over evidence), and NULL evidence on owner rows (a `None` arm in every rank site) |
| balance | R-BAL50 | -- | 2026-09-16 (developer) | **A RELEASE IS A ROW IN THE APPEND-ONLY `budget.anchor_releases`, NEVER AN EDIT AND NEVER A ROW IN THE LEVEL RELATION.** Fork F3 at `X-bj-1a`, the shape **R-IS**'s "appended superseding row" takes. One row per withdrawn level: `anchor_id` (composite with the account onto `uq_anchor_history_account_id`, CASCADE), `released_by_import_id` (the import whose fresh lines undercut the level; composite with the account onto `uq_statement_imports_id_account`, `ON DELETE SET NULL (released_by_import_id)` -- the column-subset form, because a bare composite SET NULL nulls `account_id` and every import delete fails on NOT NULL), `lines_changed_from` (the earliest day whose lines changed, the one fact both doors pass). A level STANDS when no release names it; `uq_anchor_releases_anchor` holds withdrawn-at-most-once. Both doors write one: `record_statement` naming itself, `delete_import` naming the DOOMED import BEFORE deleting it so the cause reaches `system.audit_log` before the key's SET NULL takes it off the row; NULL therefore means exactly "the import whose lines changed no longer exists". Rejected: a superseding row inside the level relation (it observes nothing, so `anchor_balance` goes nullable and every reader of standing levels carries that branch for good). Closed **BAL-485**: the badge names the cause |
| balance | R-BAL51 | -- | 2026-09-16 (developer) | **A BANK LEVEL GOES WITH ITS IMPORT, AND THE APPEND-ONLY ARMS LEARN THE SECOND OWNER.** Fork F7 at `X-bj-1a`. Both level-to-import keys are `ON DELETE CASCADE` (a RESTRICT sibling could fire before the cascade and refuse the delete): a placement is the file's conclusion, and `bank_import:R-GG(e)` already says a row that exists only because a line did states nothing once the line is destroyed; `system.audit_log` keeps `to_jsonb(OLD)`. `budget.refuse_append_only_change` serves FOUR tables now and gains, behind `IF TG_TABLE_NAME` guards so a sibling never resolves an absent field, the same disposal-versus-vandalism end-state test **R-IC** wrote for the account: a level's DELETE is permitted at commit when its import is gone, a release's when its level is gone. UPDATE admits exactly ONE transition, on `anchor_releases`: the old cause present, every other column byte-equal (`to_jsonb(NEW)` against the old row with that field nulled) AND the named import already gone -- true inside the referential action, false for a hand-written UPDATE erasing a standing cause (the step's code review found the arm admitting that; a control fires). Rejected: RESTRICT (a placed import could never be deleted, and delete is the repair door) and keeping the level with a release saying its import was deleted (its key dangles; SET NULL is an UPDATE the trigger refuses). The two shipped migrations `f4a7c2d9e51b` / `b8e3d5a06c94` now name their three tables literally through `apply/remove_append_only_infrastructure(tables=)`, the `opening_infrastructure` `arms` precedent, because a replay from the start read a constant naming a table it had not created |
| balance | R-BAL52 | -- | 2026-09-16 (developer) | **THE BANK WALK ANCHORS ON STANDING BANK LEVELS ONLY, PERMANENTLY; THE CASH RESET READS THE OWNER'S ROWS THROUGH ONE PREDICATE THAT THE FLIP DELETES.** Fork F4 at `X-bj-1a`. `fold_bank_balances` is `anchor + sum(lines)`, exact only from the bank's own posted end-of-day figure; a number the owner typed may be an available balance carrying pending items, so walking the bank's lines from it is arithmetic on two bases. That restricts the DOMAIN of one derived figure ("what the bank's record says") to the rows it is about and ranks nothing across sources, so **R-IS**'s no-precedence-branch rule stands; the honest cost is that the relation keeps two source-filtered reader families after the flip, and the cross-source rank is consulted only by `X-bj-2`'s discrepancy. Until `X-f3c-5`, `_facts._governing_row`, `reconciled_through`, `_events.cash_anchor_facts` and `_books.earliest_assertion_day` compose ONE clause, `balance_predicates.owner_declared_clause()`, a fence with a named deleter; `integrity_check.py` BA-01 spells it once more in raw SQL, BA-05 reads every level on purpose. Rejected: every standing level ranked with owner rows at strength 0 (arithmetically wrong, not merely weaker; on production it prices the same days today because the `file_chain` statement outranks all 66 owner rows) |
| balance | R-BAL53 | -- | 2026-09-16 (developer) | **STATEMENT COVERAGE IS KEYED ON THE STATEMENT THAT OWNS LINES, NOT ON A LEVEL ROW; REVISES R-JN (3)'s WORDING.** Fork F5 at `X-bj-1a`. `covered_runs` (imports still owning a line, spans merged when overlapping or adjacent) stays the ONE definition, unchanged; a level reaches its span through its import. Read literally, R-JN's "a level row carries its source's span, and a day is covered when a span reaches it" would make the developer's own 2026-01-02..03-31 date-range export (header unplaceable, 139 lines recorded) cover nothing, and a released level's still-recorded lines likewise -- the bank walk could not cross those days and `SpanAgreement.imported` would drop. Coverage answers whether the bank's LINES are complete over a span, which is a property of a statement whether or not its header could be placed. `bank_import:X-f6g` therefore reads `statement_import.covered_runs` (shipped at `X-f6e-2`) and waits on no leaf of `X-bj-1`. N-343's "choose within the run" is unaffected and is `X-bj-1b`'s |
| balance | R-BAL54 | -- | 2026-09-16 (developer) | **THE WITHIN-FILE BOUND IS ONE SQL RULE WITH TWO ATTACHMENTS, IMMEDIATE.** Fork F9 at `X-bj-1a`. `ck_statement_imports_effective_day_within_file` (`period_start - 1 <= day <= min(period_end, stated day)`) was TOTAL while the placed day lived on the import row -- it refused a bad placement and an UPDATE of the file's span under one alike -- and the day is a level row's now, so the rule spans two tables. `app/level_infrastructure.py` states it once as `budget.level_lies_within_file(day, first, last, stated)` and attaches it `BEFORE INSERT` on the level and `BEFORE UPDATE OF period_start, period_end, stated_balance_on` on the import, installed by the migration, `scripts/init_database.py` and `scripts/build_test_template.py`, and counted by the image check from `LEVEL_TRIGGERS`. Immediate rather than deferred because nothing reorders: the import door flushes the file's span before it writes the level. Rejected: FK-locked copies of the three span columns on the level row plus a local CHECK (declarative, but three columns that exist only to serve a backstop no reader needs), and an insert-only trigger (weaker than today's CHECK; the design review showed `UPDATE statement_imports SET period_end` under a placement committing) |
| balance | R-BAL55 | -- | 2026-09-16 (developer) | **`X-bj-1` SPLITS INTO `X-bj-1a` AND `X-bj-1b`, AND N-434's COLUMN HALF IS DEFERRED TO `bank_import:X-f6b` WITH ITS PARSER HALF.** Forks F6 and F10 at `X-bj-1a`. 1a is the relation, the release relation, every reader and writer, the migration and BAL-485's badge, `$0.00` by construction and graded by a before/after dump (`tests/manual/verify_level_relation.py`); 1b is **N-343** -- `fold_bank_balances` chooses an anchor PER RUN among standing bank levels, every other level in the run becomes a checkpoint `walked - observed`, `BankAgreement` carries them per run and the agreement page lists them -- which can price a day unpriced today and is graded on its own dump. The declared-span pair (`declared_start` / `declared_end`) would be written by nothing until the SECU adapter learns to read a date range, the born-dead shape `lessons.md` names, behind a `COALESCE` fallback `X-f6b` would then delete; so the column and the parser land together there, revising R-JN's placement of the column half. The flip `X-f3c-5`'s wait on `X-bj-1` resolves to 1b |
| balance | R-BAL56 | -- | 2026-09-16 (developer) | **THE R-EQ GATE'S PREDICATE IS THE OBSERVATION COLUMNS: A UNIQUE KEY OVER A FOREIGN KEY TO THE ROW'S SOURCE IS IDENTITY, NOT CONTENT.** Ruled at `X-bj-1a` when `test_anchor_service.test_neither_anchor_table_carries_a_uniqueness_guard` fired on `uq_anchor_history_statement_import` (an import places its figure on at most one day; NULLs distinct, so the owner's rows are untouched). **R-EQ** forbids a key over values a transport retry and a deliberate re-assertion SHARE -- `(account, day, amount)`, the loan twin's three -- because such a key must mis-classify one of them; a re-import is a NEW import row with its own id, so no legitimate act can collide on the import key, and the reader that keys placements by import id gets a structural guarantee rather than an assumption. `_content_keys_on_anchor_tables` now reports a unique index whose columns are a subset of the observation columns; its planted-index control (the `(account_id, anchor_balance, observed_on)` key R-EQ deleted) still fires. Rejected: dropping the key (one-level-per-import becomes the door's promise only) |
| balance | R-BAL57 | -- | 2026-09-16 (developer) | **THE R-ES ONE-WRITER CENSUS NAMES ONE WRITER PER ROW KIND, AND ITS RAW-SQL ARM IS VERB-ADJACENT.** Ruled at `X-bj-1a` when `test_account_anchor_invariant.test_exactly_one_place_writes_an_assertion` fired twice. The relation holds two kinds of row, so `_WRITERS` names two doors: `anchor_service.stage_anchor_true_up` (the owner's; **R-ES**'s write lock, R-EQ compare and log line are that door's properties) and `statement_import._record.record_statement` (the bank's; it solves the day from the file's lines and writes the level keyed to the file's claim, which needs none of the three). A third writer still fails by name. The census's raw-SQL arm reported the append-only trigger body -- a `SELECT ... FROM budget.account_anchor_history WHERE id = OLD.anchor_id` in a string that elsewhere says "UPDATE rejected" -- as a writer, a false positive by the census's own docstring ("a SELECT naming the table is NOT a writer"); the arm now matches a write verb ADJACENT to the table name, and its planted raw-`INSERT INTO` control still fires |
| balance | R-BAL58 | -- | 2026-09-16 (developer) | **UNLOCK, EDIT, LOCK: A PATCH THAT REVERTS A SETTLED ROW APPLIES THE STATUS TRANSITION BEFORE ITS FIELD EDITS; ONE THAT SETTLES APPLIES THEM AFTER.** Found by leaf 7c-1 moving the suite's one-off fixtures onto the producer: the posting-lifecycle cases post revert + re-category in ONE PATCH, and on a placed row (every one-off since 7b, every row after 7d) act 4 landed the category on the DEFINITION while the propagation (`definition_edit.unruled_live_rows`, Projected rows only) skipped the still-settled row. Worked: `$50` Groceries Paid; one PATCH status=Projected + category=Rent; before -- definition Rent, row Groceries, the re-settle posted `$50` to GROCERIES; after -- the revert runs first, the propagation reaches the unlocked row, the re-settle posts `$50` to Rent. Ruled: `routes.transactions.mutations._apply_regular_update` runs the status arm (`_apply_status_or_postings`) BEFORE `_apply_field_updates` when the transition LIFTS the row's lock (`state_machine.lock_lifts`, the clause `finalised_edit_rejection` admits the "revert and correct" edit on: `is_immutable` to mutable, so un-cancelling and un-crediting unlock too), after it otherwise -- the untick-and-settle save still needs the definition's flag written before the lock -- and in the unlock order reconciles the ledger AGAIN after the edits where a posting-relevant field moved: the verb's own reconcile read the pre-edit row, and a moved period or category re-files an envelope's purchase legs only through one that reads the final row (a first build unlocked on the settled band alone and reconciled before the edits; both found by the leaf's adversarial review, each now a firing case). The card disables the selects on a locked row; any HTTP client posts the pair (crafted-only after 7b-2's review). **Rejected**: a ledger row and a later fix (7d would ship the disagreement live); refusing the pair with a designed 400 (deletes a lifecycle the suite holds as CRITICAL) |
| balance | R-BAL59 | -- | 2026-09-16 (developer) | **7c's RULE-5 BATCH: A FIXTURE WHOSE SUBJECT IS THE LEGACY LINK-LESS SHAPE MOVES ONTO ITS ONE TRANSITIONAL HOME; A PAYBACK FIXTURE GOES THROUGH THE PAYBACK'S PRODUCER; THREE ASSERTIONS ARE RE-AIMED.** The X-cf ruling's twin for the one-off builder. (1) `tests._test_helpers.legacy_link_less_row_of` is the ONE hand-built home of the shape the cutover (7d) deletes -- link-less, OWN figure, own flag cells -- and 7d deletes it with its callers; seven cases moved onto it (three that orphan a category by a raw delete a definition now RESTRICTs, `test_a_row_with_no_links_owns_its_amount`, two statement-match cases about a legacy envelope), each docstring naming 7d. (2) Four payback fixtures that built a row through a builder and then set `credit_payback_for_id` (a second pricing link once the row has a definition) go through `payback_row_of` on `entry_credit_workflow.sync_entry_payback`. (3) `test_no_paycheck_and_no_loan_payment_means_no_query_at_all` grades the two derivations' memos (`basis.salary._profiles is None`, `not basis.loans._loans`) instead of `statements == []`: a placed row prices through its definition (3 statements per bare row, measured), and the control's subject was always the derivations; two cross-owner cases expect the builder's `AssertionError` one tier above the composite key, which `test_c13a_transaction_owner_key.py` grades on a bare row. **Rejected**: leaving the three assertions and parking those tests on the legacy home until 7d; bringing each edit separately |
| salary | R-SAL39 | -- | 2026-09-18 (developer) | **A BLANK START ON A PAYROLL LINE MEANS "SINCE MY PAYCHECKS BEGAN" AND IS STORED AS BLANK; A TYPED START IS PINNED.** Asked at `R18-c`, whose review found that below a stated `history_opens_on` the engine answers "was this line taken on a replayed payday" two ways nobody chose: a line with NO rule answers yes (`line_applies_on`'s `cadence is None` arm), a rule FROM the recorded opening answers no; the FICA cumulative and a capped line's year-to-date feel it (**SAL-562**). Worked (biweekly, recorded opening 2026-03-26, history stated 2026-01-20, base `$7,500`, a `$100` every-paycheck deduction capped `$2,000`, a `$45` monthly taxable allowance): the ruled reading takes the `$100` 01-29..10-22, puts 12 allowances (`$540`) in the wage base, SS on 12-31 `$245.52`, and December keeps `$205.58` more than the recorded-opening reading; the full year is `S9`'s arc entry. Ruled: option 1b -- the blank stays NULL on the line's rule (payroll arm only, CHECK-enforced) and resolves at read time to the owner's true opening (`history_opens_on` when stated, else the recorded one; the unit's zero there, **R-SAL36**); a typed date is pinned; every line becomes a rule, and the no-rule shape, R-SAL29's rewrite and `line_applies_on`'s `cadence is None` arm are deleted, so stating a history later moves every blank line with no reconciler (rule 14). Needs `recurrence:R21`, re-ranked by the developer the same day; built at `S9`, its own step outside every outcome (S8's precedent), whose migration authors a BLANK rule; `$0.00` on production, where no owner has stated a history. **Rejected**: the recorded opening for every line (knowingly wrong for a continuing job); refusing the blank for a stated owner (re-types a fact the app holds; `_resolution.py:696`'s clamp turns a typed 2026-01-20 into 03-26 until R21); copying the derived date onto each line (the D39 shape); blank = today or `created_at`; a checkbox beside the date |
| balance | R-BAL60 | -- | 2026-09-16 (developer) | **7c-3's RULE-5 BATCH: THE RAW-COLUMN READERS ARE RE-AIMED AT `resolved_amount`, DOLLARS UNCHANGED; THE TWO C-18 LOCKING CASES BUILD AN OWNER-REPRICED ENGINE ROW.** R-BAL59's twin for the `test_routes` file group, asked as the lane's batch question and filed at leaf 7c-3 (`b9ee1495`). (1) Route tests that PATCH a row and then assert the RAW COLUMN -- `persisted.estimated_amount == Decimal(...)`: edit allowed (`123.45` -> `200.00`, `50.00` -> `75.00`, `181.58` -> `200.00`), refused on a Paid row, unchanged after a 422, unchanged after an IDOR block, unchanged after a due-date or a notes save -- read `resolved_amount(row)` instead, dollars and claims unchanged: a one-off's typed figure restates its DEFINITION (R-BAL29) and the row stays derived, so the column reads `None` on every one, and what each claims ("the figure changed" / "did not change") is what the app's resolver answers (amount rule 3; X-cf's 2026-09-11 precedent for the engine's rows). Asked as eight; the diff holds TEN, the two others reading the column after a due-date and a notes PATCH -- the same class, the same remedy. (2) The two C-18 optimistic-locking cases -- a PATCH bumps the ROW's `version_id`; a race on the row's UPDATE is a 409 -- build a recurring definition's row made the owner's (`generate_row_of` + `repriced_by_the_owner`, X-cf's owner-row idiom) rather than a one-off, whose figure edit goes to `one_off.restate_price` and never UPDATEs the row (measured: `version_id` unchanged after a 200); no assertion changes, and the version-starts-at-1 case takes a fresh one-off (one INSERT, no UPDATE). **Rejected**: probing the lock with a `notes` edit (keeps the one-off but changes what the case says it grades); parking the two locking cases on the legacy home until 7d (they would grade the legacy row's counter, which 7d deletes with the shape) |
| credit_card | R-CC14 | -- | 2026-09-18 (developer) | **PHASE 1 DISSOLVES: `CC1a` AND `CC1b` LEAVE THE PLAN, THE CARD KEEPS RIDING THE SHIPPED CASH FOLD, AND `CC1c` SHRINKS TO THE LIABILITY BAND.** Asked by the R-CC13 trace (lane 4, 2026-09-18): Phase 1 was specified against a seam that ended 2026-07-27 -- `CC1a` names `account_posting_service/_walk.py` as the shared core, which is the posted-ledger CORRECTION producer `X-f4` deletes, while the core the card needs (`cash_ledger/_walk.py` + `balance_at/_assertions.py` + `_fold.sample_cumulative`) is what the cash, asset and loan folds share since `X-c2b2` / `X-g2b` and the card (PLAIN today) already rides through `balance_at/_asset_fold.py`; `CC1b`'s event stream and its whole oracle matrix are `_cash_fold.py`'s three tiers as shipped, so built as written it is a SECOND WALK (rule 14) against the reset semantics the flip deletes. Ruled: the card keeps riding the shipped cash fold (no new producer). `CC1a` and `CC1b` dissolve the way `X-bi-2` did under R-BAL35 (no code). `CC1c` becomes one leaf: the liability band reads the fold's forward values instead of holding flat. The card then inherits the flip by construction and Phase 1 has no wait on the balance arc. **Rejected**: keeping `CC1b` behind the flip (`after balance:X-f3c-5`) as a second walk, the trace's one alternative; the three surfaces `CC1c` sheds are already the fold (`_account_balance_map` has one branch, the scalar and `grid_balance_view` are kind-blind, `stale_anchor_warning` no longer exists) -- measured, not chosen |
| credit_card | R-CC15 | -- | 2026-09-18 (developer) | **A CHARGE IS THE PLAN ROW'S COVERING MOVEMENT ON THE CARD; THE ROW NEVER MOVES; R-CC1 AND R-CC9 ARE RE-OPENED.** Asked by the R-CC13 trace (lane 4, 2026-09-18): under the movement model (`X-bi-3a`..`3c` shipped, `X-bi-4` re-points the fold onto movements) Phase 3's ruled shape -- retarget `account_id`, a `charged_from_account_id` provenance column, a display-only ghost row -- is a second way of saying where money moved, and its guard names `tracks_purchases`, a predicate `X-bi-5` deletes. Ruled (from scratch): re-open R-CC1 and R-CC9. A charge is the plan row's covering movement on the card; the row never moves; card-tender purchases are movements on the card and clear on the card's statement line by line. Requires relaxing `fk_transaction_entries_parent_account`, to be put to `X-bi-4` / `X-bi-6`'s design loop. `CC3a`, `CC3b`, `CC3c` become `after balance:X-bi-4`. The trace's finding for `CC3b`'s own loop, not ruled here: its settle writes what `status_seam.apply_status_change` writes as one act (`paid_at` is a column `X-f1b` deleted), and WHICH day a migrated pair settles on is `CC3b`'s ruling to ask (13 of the 14 link-less Credit rows were undated on the 2026-09-12 restore); its in-migration postings are redundant with the deploy resync. The 2026-07-19 entries of `CC3a`..`CC3c` stand as history until each leaf's loop restates them. **Rejected**: the 2026-07-19 shape kept (`CC3a` NOW, `CC3b` after `X-bi-3d`, `CC3c` after `CC3b`): the retarget-and-provenance design is a second way of saying where money moved once `X-bi-4` re-points the fold onto movements |
| balance | R-BAL61 | -- | 2026-09-18 (developer) | **A MOVEMENT'S `figure_source_id` RECORDS WHO WROTE THE FIGURE, STATED BY THE WRITER AND NEVER INFERRED FROM THE DAY; `X-bi-3d` CLASSIFIES `derived` -> `resolved` AND `corrected` -> `typed`, AMENDING R-BAL40'S "else an observed day -> observed" ARM.** Ruled from scratch after the developer re-opened the fork with the import's state (one import, 306 lines, 22 matched, 0 purchases matched; the first import is finalised after the next release). Measured on the 2026-09-18 production restore: every bank-observed day ever written onto a settled row was a day-only confirmation (the matcher has never re-priced a settled row), 5 settled rows carry a person-typed `corrected` figure beside such a day, and 0 `corrected` shadows exist -- so **BAL-508**'s class is every row kind, not the transfer arm: a figure typed over a standing observed day inherits the bank's label. The day's basis and the clearing link already record the bank's agreement; the figure's column answers the one question those do not, and only the writer knows it. The live change is a leaf of its own, `X-bi-3e`: `Settlement` carries the source as `SettleDay` carries the basis; `settle_day.figure_source_of` and `entry_service.update_entry`'s raise-on-confirmation arm are deleted; a revert UN-DATES the covering movement and keeps it, the retained record's home and `X-bi-4`'s end state once the row's columns go. Rejected: evidence semantics ("the bank has stated this exact figure": a third home for the bank's agreement, derivable from the match and the line while the match stands); a kind branch in `_source_of`; refusing the 5 (a deploy blocked over a label nothing reads); re-pricing transfer legs from lines (a matcher question that closes only the shadow case); building the seam change inside the money-moving leaf. `$0.00`. Record: `HANDOFF-X-bi-3.md` s.9. |
| balance | R-BAL62 | -- | 2026-09-18 (developer) | **`X-bi-3d`'S DOWNGRADE LEAVES THE COVERING MOVEMENTS IN PLACE, AMENDING R-BAL40'S "delete exactly the entries whose parent does not track purchases" CLAUSE.** Traced: the deploy's `resync_all_cash_postings` books each movement as a purchase-sourced journal entry linked `ON DELETE SET NULL`, so deleting the rows would orphan 124 entries no migration may reverse (R-BAL40's own "no second ledger writer") and the older image's next resync would re-post each row's own leg: `$48,991.00` double-booked on Checking over the 124 non-shadow rows (Electricity `$364.60` reading `$729.20` on its day). Every state 3d leaves is one the revision below produces for its own settles (3a's seam writes the identical rows) and the older tree reads a covered row by R-FM's identity; the row's `settled_amount` stands, so it is lossless; stepping past `b5c7e9a1d2f4` turns them into ordinary purchases with their postings intact, which that downgrade already states. Rejected: delete-only-unposted-then-refuse (refuses after every real deploy, a fence that never retires); deleting the ledger rows by SQL. Measured on the clone: downgrade then upgrade leaves the 162 rows, the fold byte-identical, the second upgrade writing 0. `$0.00`. |
| balance | R-BAL63 | -- | 2026-09-18 (developer) | **THE BANK WALK ANCHORS PER RUN, AND EVERY OTHER STANDING BANK LEVEL A RUN REACHES IS A CHECKPOINT THE AGREEMENT PAGE LISTS PER RUN, FILES NAMED.** Fork (a) at `X-bj-1b` (**N-343**). `fold_bank_balances` chooses one anchor per recorded run (`covered_runs`) among the standing bank levels the run reaches (`reaches_end_of`), strongest evidence then latest day then id -- the rank `usable_anchor` applied account-wide; every other standing bank level the run reaches is a CHECKPOINT `walked - observed`, `walked` read off the same prefix-sum walk every priced day is. `statement_agreement.html` renders ONE BLOCK PER RUN in date order: the anchor's figure, day, file name and evidence badge, then each checkpoint's figure, day, file name, badge and "agrees" or "off by $Z (the walk says $W)"; the `unpriced_days` badge keeps its count with copy naming both causes. File names ride on the anchor and checkpoint values because a re-import legitimately places a second figure on the same day (`uq_anchor_history_statement_import` is per import, R-BAL56) and the day alone cannot tell two apart. Worked on the planted production copy: the July `file_chain` level left all 20 days of a planted August run at `--`, that run's own placed day included; per run, August prices from its own `$3,000.00`. The constant-offset signature stays account-wide (**BAL-512**). Rejected: a per-run table (tables are for the grid and statements); a "bank stated" column in the day table (one column for four rows in 247); one global paragraph plus a checkpoints line (names one figure over days another anchor prices, the defect's own shape) |
| balance | R-BAL64 | -- | 2026-09-18 (developer) | **A RUN HOLDING LINES BUT NO STANDING BANK LEVEL ANCHORS ON NOTHING; R-BAL52 STANDS.** Fork (b) at `X-bj-1b`. Its days are unpriced (`--` in the bank column, counted in `unpriced_days` beside an anchored run) and its block reads "No statement here places a balance on a day"; the movement columns still answer, needing no anchor. Worked: a planted September run 09-01..09-05 beside an August anchor `$3,000.00` at 08-20 with 08-21..08-31 unimported. Rejected: walking across the gap from the nearest anchored run (`$3,050.00` for 09-01 over eleven unimported days; a gap is lines nobody imported, the failure the coverage test refuses); anchoring on an owner true-up inside the run (`$2,900.00` typed on 09-03 pricing 09-01 at `$2,850.00` from a figure that may hold pending items; refused by R-BAL52 permanently) |
| balance | R-BAL65 | -- | 2026-09-18 (developer) | **THE BANK FOLD IS TOTAL: `BankBalances.runs` LISTS EVERY RECORDED RUN WITH ITS ANCHOR OR NONE AND ITS CHECKPOINTS, AND `BankAgreement.runs` REPLACES BOTH `anchor` AND `imports`.** Fork (c) at `X-bj-1b`. `fold_bank_balances` never returns `None`: an account with no line has no run, one whose runs hold no standing bank level has runs anchored on nothing and no priced day. `BankAgreement.imports` and `anchored` are DERIVED from `runs`, so `outstanding_difference` reads what it read and `covered_runs` is queried once per page rather than twice; the `None` branches in `bank_agreement`, `recorded_opening_before` and `bank_balance_on` go. "Unconfirmed" (the weakest rung) is `BankAnchor.unconfirmed`, decided in Python on the enum member and read per run by the template; the route's `_anchor_is_assumed` helper and its `anchor_assumed` variable are deleted. **Rule 5 exercised by the developer**: four assertions `fold_bank_balances(...) is None` re-aim to the equivalent total-shape assertion (no run holds an anchor, no day priced), the hand-built hero agreement passes `runs=`, the harness dumps `runs`, and the test asserting N-343's defect ("the later run's own anchored day is among the unpriced") flips to priced; the four are named in the as-built record. Rejected: `anchors` beside `imports` with the fold still `None`-able (the run spans in two lists on one value, rule 14's invariant tell, and the duplicate query kept); keeping `anchor` and adding `anchors` (two homes for one value) |
| balance | R-BAL66 | -- | 2026-09-18 (developer) | **THE IMPORT DOOR'S OPENING WALKS FROM THE ANCHOR OF THE RUN THE DAY BEFORE THE FILE'S FIRST LINE SITS IN; A DISAGREEING CHECKPOINT IS AN INSTRUMENT AND MOVES NOTHING.** Fork (d) at `X-bj-1b`. `recorded_opening_before` reads the total fold at `day - 1`; its evidence is `weaker_of(corroborated, that anchor's evidence)`, the anchor being the one that priced the day (`BankBalances.anchor_for`, the fold's own membership, no second walk). A checkpoint between the anchor and the opening day that disagrees is REPORTED on the agreement page and changes no evidence and no pricing: **R-GF** makes the comparison an instrument and never a gate, and by rank the checkpoint is the WEAKER figure (most often an assumed day), so letting it weaken the anchor would invert the evidence ladder. Worked on the planted copy: a file starting 08-21 opens at `$3,000.00` uncorroborated with a SOLVED day where the account-wide walk gave it no opening and a guessed day; a file starting inside the 07-18..07-31 gap opens on nothing, as before. Rejected: capping the opening at `uncorroborated` when a disagreeing checkpoint lies between (the weaker weakens the stronger); returning no opening (the file's day is then guessed: a gate on a comparison) |
| balance | R-BAL67 | -- | 2026-09-18 (developer) | **`X-bi-7d`'S DOWNGRADE RESTORES THE SCHEMA AND FOLDS NOTHING: BOTH FLAG COLUMNS RETURN `false`, THE CHECK RETURNS TO `<= 1`, BOTH LINK KEYS RETURN TO `SET NULL`, AND EVERY ROW KEEPS ITS DEFINITION, ITS PRICE AND ITS DATE.** AMENDS `from_scratch_architecture.md` 10.8, whose fold (every rule-less definition with exactly one row folded back onto its row, the figure by R-JC's two arms, the definition and its version deleted) was written 2026-09-12 before `X-bi-7b-1` deployed; since 2026-09-14 the app mints exactly that shape for every new one-off (3 on the 2026-09-18 production restore: definitions 42, 43, 44), so the signature can no longer tell this migration's 34 from the owner's and would fold theirs too. Ruled from four worked options on DBCode 1509 (plan `$36.00`, settled `corrected` `$18.00`), Homeschool Curriculum 2581 (plan `$1,000.00`, `purchases` basis, 3 entries `$742.32`) and Vitamins (definition 42): under A all three read their definition's price after rollback and no balance moves; the pre-7d app (7b's code) reads and writes this shape, so a rollback loses no behaviour, and a re-upgrade mints 0 and dates 0. **Rejected**: B, 10.8 as written (over-folds every grid-made one-off, 37 definitions deleted on today's data, exact for the `corrected` row only by the data, its `default_amount` happening to equal the old plan); C, fold only the definitions this migration minted, recorded in a marker table (an exact inverse bought with a system table whose only reader is a rollback, scaffolding the goal state deletes); D, a downgrade that raises `NotImplementedError` (allowed only where a downgrade is unsafe, and A is safe). |
