# The step order

**This is the single source of truth for WHAT TO DO NEXT.** Every step in every arc appears here
exactly once, and the order table below is sorted into EXECUTION ORDER. The next step is its first
row. You do not need to read anything else to find it.

**How to read a row.**

| column | what it holds |
|---|---|
| `arc` + `id` | The step's key. An `id` alone is not unique across the corpus, so cite both |
| `also` | The other names this same step is filed under. `C2`, `X-l` and `R-F12` are ONE commit |
| `what this step does` | One sentence. If you need more, the arc's own document specifies it |
| `order` | `#N` is its place in the sequence; `container` is a grouping, never work; `SHIPPED` is done |
| `commit` | The hash for a shipped step. The code it shipped is the record; prose about it is not |
| `starts` | `NOW` means every blocker has shipped. Otherwise the rank it waits for, then the blockers |

**Where a step's detail lives**, by its `arc`:

| arc | document and section |
|---|---|
| `balance` | `../audits/balance_architecture/README.md`, section 5 |
| `recurrence` | `implementation_plan_recurrence_redesign.md`, section 4 |
| `pay_calendar` | `implementation_plan_pay_calendar.md`, section 4 |
| `credit_card` | `implementation_plan_credit_card.md`, "The steps" |
| `bank_import` | `implementation_plan_bank_import.md`, "The steps" |

*A table in this file is found by its own HEADER, so one added for any other purpose joins no
registry: `tools/plan_gate` separated them by COLUMN COUNT until 2026-08-14, and a three-column
reference table read as four unruled forks. That was finding **N-234**, closed by `d8aed644`.*

**Anything under an `archive/` or `historical/` directory is a HISTORICAL RECORD and governs
nothing.** It may be cited for how something came to be; it may never be read as a live plan or as a
statement of the current state. The code as committed is the source of truth for what the app does.

## Working two steps at once

**Any row whose `starts` reads `NOW` can be started today, whatever its rank.** The rank is the
recommended sequence; `starts` is the hard constraint. That is how `X-f2-c2` and `C3` ran side by
side: both were unblocked and they shared no file.

**Two `NOW` rows are safe together when they are in different arcs or different phases.** Before
pairing two inside one arc, check that neither names a module the other deletes. A row marked
**MOVES MONEY** takes its own PR either way, so it is never the second lane.

**The rank is a DECISION, not a derivation.** 59 of these steps are legal to start right now, so the
dependency graph alone cannot say which comes next; the sequence below follows each arc's own stated
sequencing -- the balance README's ten blocks, and each plan's section 0.
**The standing priority (developer, 2026-08-19; RE-RULED 2026-09-02).** bank_import reaches
production first, then the credit_card arc. **Its scope is `X-gj-4b`, `X-gj-4c` and `X-gi`** -- the
Reconcile screen rebuild finished and the review queue's orphans deleted -- and every bank_import
step after those three is enhancement carrying no precedence (developer, 2026-09-02). Where two
arcs' rows are both legal today, this is what "each arc's own stated sequencing" above resolves
against. **How this document APPLIES it is not part of the ruling**: an unblocked bank_import leaf
is ranked above an equally unblocked row of another arc. Written here 2026-09-02, on the ruling that
placed the recurrence earnings-lines chain directly after that scope: until then it lived only in
session memory, and when a placement finally turned on it neither the session working the step nor
the coordinator could cite it.
**The `starts` column is DERIVED from the blocker keys beside it and the gate reconciles the two**,
so a rank can never contradict a real dependency and a stale `NOW` cannot survive a commit.

**245 steps, 148 open.** The dependency graph holds 98 edges over 77 rows.

## The order

| arc | id | also | what this step does | order | commit | starts |
| --- | --- | --- | --- | --- | --- | --- |
| bank_import | X-gj-4c-2 | -- | Build the SKIPPED TAB itself (**R-JH**): the reader over `budget.statement_line_skips`, the card a skipped line renders as, its Undo route, and `Tab.holds_settled_acts` generalised to the card KIND a tab holds, since the tab now holds an ACT that is neither a bank line nor a match. | #1 | -- | NOW / bank_import:X-gj-4c-1 (shipped) |
| bank_import | X-gj-4b | -- | LIGHT the SKIP verb (**R-HW**, **R-JI**): `offers_for` stops shutting it, a `skips` list joins the batch schema beside `incomes`, `reconcile_payload` reads `verb=skip`, `apply_reviewed` grows the arm, and the SKIP pane renders its control plus the disclosure that a skip closes no difference between books and bank. Built AFTER the tab, because a lit verb needs somewhere for its result to land. | #2 | -- | after #1 / bank_import:X-gj-4c-2 |
| bank_import | X-gi | -- | Delete what the exception queue ORPHANS, which is a census before it is a deletion: the review-screen rebuild replaced the queue's model and nobody has measured what of the old one is now unreachable. Minted by **X-gf** shipping, as its arc document requires. | #3 | -- | after #2 / bank_import:X-gj |
| recurrence | R14 | -- | Rule, then apply, what a payroll deduction's gross is priced from -- whose salary, which period's figure, and against which clock -- so the employee contribution and the employer-match basis stop being blind to every raise the owner has taken without collapsing a multi-profile owner onto one arbitrary gross. **MOVES MONEY.** Closes **D45**. | #4 | -- | NOW |
| recurrence | R15 | -- | Rule, then apply, what a payroll deduction's own FREQUENCY means: `deductions_per_year` stores 26 / 24 / 12 as a MODE the engine only ever compares against, so a weekly owner's every-paycheck deduction is stored and shown as 26 while it is taken 52 times, and "skip the 3rd paycheck" names nothing at their cadence. Closes **F-21**. | #5 | -- | after #4 / recurrence:R14 |
| recurrence | R18 | -- | Give a paycheck EARNINGS LINES, as it already gives DEDUCTION lines: `Earnings` holds one figure while `DeductionBreakdown` holds many, each placed by `_deduction_applies_at` on a `deductions_per_year` of 26 / 24 / 12 -- so an employer-paid allowance with a cadence has nowhere to live and becomes a separate income template, which is why one payroll deposit needs a rule to find three app rows. | #6 | -- | after #5 / recurrence:R15 |
| balance | X-f3c-2b-2c | -- | Perform the account-10 repair through the app's own doors as a rehearsed OPERATOR runbook (**R-HJ**..**R-HM**) -- delete the duplicate transfer, re-date five, restate the books to 2026-03-26 at `$5,350.21`, zero the archived twin and record five dividends -- and re-measure the post-state to zero mismatches. **MOVES MONEY**, and it cannot run until the door has DEPLOYED. | #7 | -- | NOW / balance:X-f3c-2b-2a (shipped) |
| balance | X-au-f | -- | Cut the PARENT transfer over: `transfers.amount` resolves from its definition's price series for a generated transfer, and the amount arm's ownership question dissolves with it, because "the owner authored this figure" becomes "the parent owns its amount". Its SHADOW half shipped at `X-au-g-2c-2` (**R-IN**) -- the two repairs and Transfer Invariant 3 went there. | #8 | -- | NOW / balance:X-au-c3 (shipped) |
| balance | X-au-m | -- | Stop an owner-priced pair storing ONE figure twice -- Transfer Invariant 3's amount clause, the half `X-au-g-2c-2` did not reach (**R-JA**): the TAKE arm states the figure on the parent AND each leg. The leg should derive from the parent it names, EXCEPT on a derive-mode loan payment, where a leg's derivation is not its parent's amount -- `$174.10`, twice. That exception is UNRULED. | #9 | -- | after #8 / balance:X-au-f |
| balance | X-bj | -- | Make the owner's true-ups and the bank's statement closings ONE evidence-ranked level relation (**R-IS**), where a level observation never moves a balance and instead yields `discrepancy = observed - computed`; the bank half's release becomes an APPENDED superseding row, because an assertion is append-only at the database tier. Closes **N-314**. | #10 | -- | NOW |
| balance | X-bi-1 | -- | Make `tracks_purchases` a STORED column with one writer, backfilled from each row's template and retiring the lazy load, because it is the app's own envelope predicate and keying anything on `transactions.is_envelope` instead reads 4 envelopes where there are 238. | #11 | -- | after #8 / balance:X-au-k / balance:X-au-e / balance:X-au-f |
| balance | X-bi-2 | -- | Give `budget.transaction_entries` the full movement column set -- a category, a type, `scenario_id` (which the fold REFUSES a mismatch on) and the settle-day basis pair -- backfilled from each entry's parent, additively, so its downgrade is a column drop. | #12 | -- | after #11 / balance:X-bi-1 |
| balance | X-bi-3 | -- | Make `settle_from_entries` the ONLY settle path, its MANUAL branch writing one covering movement THROUGH THE SERVICE DOOR as **R-HJ** requires, for the 165 settled rows that hold no entries -- 14 of them carrying no settle day and 38 of them transfer shadows, each answered by a human at a door rather than by a `WHERE` clause; it **MOVES MONEY** and is this family's costly, irreversible act. | #13 | -- | after #12 / balance:X-bi-2 |
| balance | X-bi-4 | -- | Re-point the fold to movements in ONE commit for every account kind at once, because after the settle path is single every settled row has exactly one covering movement and `opening + SUM(movements)` becomes an identity provable against the pre-state instead of a second balance semantics running beside the first. | #14 | -- | after #13 / balance:X-bi-3 |
| balance | X-bi-5 | -- | Delete `is_envelope`, `tracks_purchases`'s branch sites and the template sites the two of them reach, which is the fence a unified movement concept makes structurally unnecessary rather than merely unused. | #15 | -- | after #14 / balance:X-bi-4 |
| balance | X-bi-6 | -- | Delete the shadow's stored `status_id` and `pay_period_id` -- Transfer Invariant 3's other two clauses (**R-JA**), which `transfer_service` keeps equal to the parent's BY HAND. It runs after the fold re-points and that ordering IS the substance: **invariant 5 is why the mirror exists**. Rewrites Transfer Invariant 3 in `CLAUDE.md` into a structural fact. | #16 | -- | after #15 / balance:X-bi-4 / balance:X-bi-5 |
| balance | X-bk | -- | Perform the ONE-TIME manual reconcile of the imported bank history against the app's own rows, through the app's doors and into the post-restructure shape so the whole history keeps its plan-versus-actual comparison; an OPERATOR act and a rehearsed runbook rather than a code change, and it **MOVES MONEY**. | #17 | -- | after #16 / balance:X-bi |
| balance | X-f3c-5 | -- | THE FLIP: the assertion stops resetting a PLAIN account, `balance(T)` becomes opening equity plus the sum of postings, and the reconcile reverses the 162 `account_trueup` entries that stand. **MOVES MONEY, OWN PR, NO BACKLOG**, and gated on the operator import **N-368** that no `blocked by` cell can hold. Closes **N-172**, **N-174**, **N-285**, **N-290**, **N-307**, **N-308**. | #18 | -- | after #17 / balance:X-bk |
| balance | X-f3c-4 | -- | Let an owner ACCEPT that difference as an uncategorized transaction dated on the latest assertion's own day, its amount DERIVED from the postings other than itself until they categorise it, offered only where an imported statement reconciles the span (**R-FN**, **R-GY**); it **MOVES MONEY**, taking out of equity a figure it must RE-DERIVE rather than quote. Closes **N-171**. | #19 | -- | after #18 / balance:X-f3c-5 |
| balance | X-f3c-2b-3 | -- | Bound the ASSERTION door at its account's books opening -- both write doors and a deferrable constraint trigger, the shape **X-f3c-2b-1** gave the movement side -- so a balance cannot be declared for a day the books did not exist on. It legalises NOTHING: the bound is STRICTLY before `opened_on` and no row sits below its books. Closes **N-400**. | #20 | -- | after #18 / balance:X-f3c-5 |
| balance | X-f3a-2 | -- | Record that a STATEMENT was walked line by line, so a line it did not show is NOT CLEARED rather than unknown, and widen the offer set to every uncleared line including the already-settled ones, which **MOVES MONEY**; it closes **N-273** and **N-289**. | #21 | -- | after #20 / balance:X-f3c |
| balance | X-f4 | -- | Delete what the cutover orphans: `ReconciledThrough`'s coverage rule, `_anchors.py`, the correction machinery and the R-I seed compensator, a set X-f3a has already narrowed. Closes **N-176**, **N-218**, **N-161**, **N-170**. | #22 | -- | after #20 / balance:X-f3c |
| bank_import | X-f6c | -- | Let a NEW-ENVELOPE merchant answer create a recurring TEMPLATE once and name that template thereafter, so the container a policy files into carries an identity across pay periods instead of a NAME the app has to converge on -- which waits on `X-au-e` because a row that has a template has a derivation to read, and that is the constraint `X-au-e` rebuilds. Closes **N-328**. | #23 | -- | NOW / balance:X-au-e |
| bank_import | X-gh | -- | Unify the two balance records (**R-GL**): a confirmed import's anchor ASSERTS the opening when the day's residue is zero and surfaces the residue as an exception when it is not, demoting the hand true-up from habit to correction -- designed against the post-cutover assertion, never the one X-f4 deletes. | #24 | -- | after #20 / balance:X-f3c |
| bank_import | X-f6b | -- | Add the automated SOURCE ADAPTER -- SimpleFIN on the security ground **R-FP** states -- re-scoped 2026-08-24: the daily fetch lands on standing rules rather than a review queue, its per-sync balance is the corroboration source the evidence ladder lost when SECU dropped running balances, and the scheduled fetch runs from host cron through a CLI door. | #25 | -- | NOW / bank_import:X-ge (shipped; a daily feed lands on rules, not a queue) |
| balance | X-au-g-2c-3b-3 | -- | Stop the ENGINE FEED allocating at all: `prepare_payments_for_engine` passes the CASH and `project_forward` charges the month's escrow, which DELETES the floor -- a SECOND allocation rule that reports a short payment as exactly on schedule -- and orphans `LoanContext.contractual_pi` and `compute_contractual_pi` with it. **MOVES MONEY, OWN PR.** Closes **N-409**. | #26 | -- | NOW / balance:X-au-g-2c-3b-2 (shipped) |
| pay_calendar | C13-a | -- | Make a transaction's OWNER a COLUMN held equal to BOTH its parents' by a composite foreign key each, so a row whose account and whose paycheck belong to different people becomes unconstructible rather than merely unwritten. Expand / backfill / contract in one revision. **OWN REVIEW PASS.** Ruling **R-PC32**. | #27 | -- | NOW / pay_calendar:C4-c (shipped) |
| balance | X-au-h | -- | Split the FOUR facts `is_override` carries, of which two were found by adversarial review: re-priced, moved, survives the regeneration sweep, and exempt from the partial unique index. Closes **N-238**. | #28 | -- | after #26 / balance:X-au-d / balance:X-au-e / balance:X-au-f / balance:X-au-g |
| pay_calendar | C13-b | -- | Retire the NINETEEN hand-written ownership comparisons the column makes unnecessary, deciding per site whether the check becomes one equality on `transactions.user_id` or goes entirely now that the state it refuses cannot be stored. Closes **P75**; carries **N-373**. | #29 | -- | after #27 / pay_calendar:C13-a |
| pay_calendar | C7 | -- | Rule and then fix `journal_entries.pay_period_id`, a NOT NULL FK stored beside the `entry_date` it derives from, which is P1's defect on the ledger's header table. Closes **P18**. | #30 | -- | NOW / pay_calendar:C4 (shipped) |
| pay_calendar | C8 | -- | Give the forward forecast cadence its own control, separating it from the payday forms it is currently welded onto. Closes **P30**. | #31 | -- | NOW / pay_calendar:C4 (shipped) |
| pay_calendar | C9 | -- | Project the modelled fold's CONTRIBUTION tier past the pay-period horizon on the same axis the accrual tier already runs on, superseding balance ruling **R-AG**, which let the half model stand before a total calendar existed. **MOVES MONEY.** Closes **P7**; carries **P42**, **P44** and **P50**. | #32 | -- | NOW |
| pay_calendar | C6 | -- | Let a payday be inserted mid-schedule, refusing only where `classify_period_lock` says the split period is locked. Starts with the two rulings section 3 names. Closes **P10**. | #33 | -- | NOW / pay_calendar:C4 (shipped) / recurrence:R7c (shipped) |
| recurrence | R5 | -- | Finish the date split `R17` began: add `due_on` (the installment) beside the `occurs_on` that step created and DELETE `compute_due_date`, which dates a row from its PERIOD -- so two occurrences inside one paycheck share a due date (**D18**). The index re-key and the `RecurrenceCadenceUnsupported` retirement this step used to carry SHIPPED at `R17`. A value-splitting migration; own PR. | #34 | -- | after #22 / balance:X-f4 (R5 edits `cash_ledger / _events.py`, which is inside that step's deletion set) |
| recurrence | R6 | -- | Delete `loan_params.payment_day` and collapse eight producers of "when is this installment due" into one `loan_installment_date` accessor. Kills **D4**; needs its own review pass. | #35 | -- | after #34 / recurrence:R5 (it READS `due_on`; "ships WITH balance:X-an" was unsatisfiable -- see section 0) |
| recurrence | R8-b | -- | Make the WEEK unit authorable -- the cadence the closed pattern set could never express -- by deleting `has_row_date_coordinate`, which R5's `occurs_on` makes unnecessary rather than merely satisfied. | #36 | -- | after #34 / recurrence:R5 |
| recurrence | R8-c | -- | Give a rule an nth-weekday coordinate ("the third Tuesday") as an EXCLUSIVE ARC of columns on `budget.recurrence_rules` under one CHECK, dropping the unwritten `budget.recurrence_weekday_anchors` table, and make `describe`'s coordinate dispatch on the coordinate KIND rather than on "WEEK or else". Closes **D29**. | #37 | -- | after #34 / recurrence:R5 |
| recurrence | R8-d | -- | Add the business-day shift over weekends plus a COMPUTED US federal holiday set, applied to the cash date alone, which needs `occurs_on` to exist because today's generated row is dated from its pay period and never from the occurrence. | #38 | -- | after #34 / recurrence:R5 |
| balance | X-k | -- | Reconcile `RecurrenceRule.end_date` against what was actually generated, and give the write door a consistent batch contract. Closes **N-18**, **N-19**, **N-23**, **N-24**. | #39 | -- | after #34 / recurrence:R5 (R5 deletes `compute_due_date` and re-keys the generation index; rebasing this over that is the cheaper direction) |
| balance | X-ad-b | -- | Stop the rolling top-up manufacturing history: an automatic writer creates nothing on a lapsed schedule, and the lapse is surfaced instead. Closes **N-124**. | #40 | -- | NOW |
| balance | X-x1 | -- | Build the ONE answer to "no pay period covers this date": `PayCalendarGapError`, `require_current_period` / `covers`, one handler and its repair page, taking the grid's two pre-checks as its first callers. | #41 | -- | NOW |
| balance | X-x2 | -- | Move the branches that publish a figure the app did not compute onto the raising accessor: the fabricated $0.00 in four producers, and `build_trend_periods`' `current_index = 0` into an empty list. | #42 | -- | after #41 / balance:X-x1 |
| balance | X-x3 | -- | Make `onboarding.has_periods` ask "does a period cover today" rather than "do any periods exist", so the checklist and the page it renders on cannot disagree. | #43 | -- | NOW |
| balance | X-x4 | -- | Stop answering an empty requested WINDOW with the absence card, and stop the card's copy naming two states. | #44 | -- | NOW |
| balance | X-x5 | -- | Delete `verify_savings_producers.py`'s dict-or-attribute `_get` reader and the two-spelling readers its own docstring says this step deletes. | #45 | -- | NOW |
| balance | X-y | -- | Move the fifteen surfaces that resolve the baseline scenario directly onto the seam, deciding what a WRITE may do without one. Closes **N-117**. | #46 | -- | after #45 / balance:X-x (the container ticks at #42) |
| balance | X-aj2 | -- | Make the status write door STRUCTURAL and DELETE the W9907 checker, ruling what a row may be BORN as. Closes **N-149**, **N-151**, **N-185**, **N-188**. | #47 | -- | NOW |
| balance | X-ak | -- | Rule the stored transfer-to-shadow copy first, then unify the THREE mirror implementations that already disagree. Closes **N-148**, **N-150**, **N-152**, **N-156**, **N-159**, **N-170**. | #48 | -- | after #47 / balance:X-aj (the container ticks at #44) |
| balance | X-ai-a | -- | Build the cash re-derive VERB from R-DV's sentence, with the union source set, the returned touched pairs and the per-account advisory lock in it from the start. Carries **N-162**, **N-165**, **N-166**. | #49 | -- | NOW |
| balance | X-ai-b | -- | Build the trigger: the commit-boundary grader, drained from the registry the writers populate. | #50 | -- | NOW |
| balance | X-ai-c | -- | Move the loan side onto the same re-derive verb. | #51 | -- | NOW |
| balance | X-ai-g | -- | Classify each of the 20 bulk-statement sites as unable to touch a posted row, routed through a writer, or named in the one docstring that states what the grader cannot see. Closes **N-163**. | #52 | -- | NOW |
| balance | X-ai-s | -- | Migrate `journal_entries` to an EXCLUSIVE ARC of typed FKs with an AT-MOST-ONE check, plus the reversal linkage. Held until the cutover, which deletes the correction family it would buy attribution for. Closes **N-167**. | #53 | -- | after #20 / balance:X-f3c |
| balance | X-d | -- | Make the posted account ledger a CHECKED PROJECTION: the posting writer consumes the walk instead of its own, with a per-visible-date assert. Carries **N-135**. | #54 | -- | after #53 / balance:X-ai (the container ticks at #50; N-155 parked this step and the assert's placement is the restructure's) |
| balance | X-i1 | -- | Give `BalanceContext` the FOUR remaining inputs of the tier the loan derivations already have -- the contribution feed, the override map, the standing extra, the contractual schedule -- since `pay_calendar:C2-c` took the calendar early, as a method beside `loan_walk` rather than through `_memoize_once`. Additive and byte-identical. | #55 | -- | NOW |
| balance | X-i2 | -- | Hand each memoized loader `ctx.as_of` and `ctx.scenario`, so one read pass has one clock. **MOVES MONEY** ($3,631.74 today against $3,722.53 at a 2027 read). Closes **FU-3**, **N-14**, **N-56**, **N-72**, **N-89**, **N-91**, **N-92**, **N-93**, **N-115**. | #56 | -- | after #55 / balance:X-i1 |
| balance | X-j | -- | Rule which producer answers "what is this account worth" for a given surface, or render the row that explains the difference. Closes **N-87**, **N-90**, and **N-83**'s display half. | #57 | -- | after #56 / balance:X-i2 (X-j moves three surfaces onto the modelled view whose contribution load X-i2 fixes, so the other order ships a regression and then removes it) |
| balance | X-m | -- | Make `growth_engine.project_balance` take its AXIS rather than boundaries its caller must compute to match; a caller that gets it wrong costs $1,000.00 of annual-limit room per period. Closes **N-86**. | #58 | -- | NOW |
| balance | X-n | -- | Stop `_redistribute_to_distinct_months` OVERWRITING the real installment a payment satisfies when it shifts a colliding due date. Closes **N-36**. | #59 | -- | NOW |
| balance | X-e | -- | Re-take the census the column deletions made historical, then resolve what remains: two callerless public seam entries and a falsified de-duplication rationale. Carries **N-96**, **N-85**, **N-180**. | #60 | -- | NOW |
| balance | X-ab | -- | Give the posting path ONE asset-vs-liability rule instead of asking X-z's question a second time, and decide what a re-class does to accounts already carrying postings. Closes **N-122**. | #61 | -- | NOW |
| balance | X-ac | -- | Stop the cockpit reducing `_sum_liquid_balances` twice per render and publishing the answer under two context keys. Closes **N-121**. | #62 | -- | NOW |
| balance | X-ag | -- | Build the instrument that refuses lax digit acceptance, shown FIRING on a planted defect; both earlier method-name designs were refuted by measurement. Closes **N-139**. | #63 | -- | NOW |
| balance | X-ah | -- | Rule each of the 34 `request.args.get(..., type=int)` sites and parse them like every other id, with a second rule that admits a meaningful zero. Closes **N-142**. | #64 | -- | NOW |
| balance | X-al | -- | Census the fifteen live `duplicate-code` disables, none of them re-measured, and build the arm that catches a stale one -- `useless-suppression` is blind to them. Closes **N-154**. | #65 | -- | NOW |
| balance | X-ba | -- | Collapse `app/ref_cache`'s twenty-six near-identical id accessors into one generic lookup, delete the two that have no caller anywhere, and migrate the call sites, so the repetition that took the module past pylint's line ceiling stops being paid for by splitting it. Closes **N-341**. | #66 | -- | NOW |
| credit_card | CC0a | -- | Add `has_revolving_credit` to account types with the `REVOLVING` projection kind and its classifier branch, behind behavior-preserving shims. | #67 | -- | after #22 / balance:X-f4 |
| credit_card | CC0b | -- | Add the `budget.credit_card_params` satellite model and its migration, inert by design. | #68 | -- | after #22 / balance:X-f4 |
| credit_card | CC0c | -- | Add the card params setup flow: create and update routes, the Marshmallow schema, and the REVOLVING setup redirect. | #69 | -- | after #22 / balance:X-f4 |
| credit_card | CC1a | -- | Make the card consume the shared instant-partition fold core rather than growing a second copy of it. | #70 | -- | after #22 / balance:X-f4 |
| credit_card | CC1b | -- | Build `balance_at/_revolving.py`: a card is an event stream of anchor facts, settled rows and projected rows, unwired and additive. | #71 | -- | after #22 / balance:X-f3c / balance:X-f4 |
| credit_card | CC1c | -- | Dispatch REVOLVING to the fold at four seam surfaces, deleting the CC0a shim and explaining every moved number. | #72 | -- | after #22 / balance:X-f4 |
| credit_card | CC2a | -- | Derive the statement cycle purely: `cycle_window`, `statement_sequence`, `due_date_for`, `statement_balance`, `grace_kept` and `minimum_payment`. | #73 | -- | after #22 / balance:X-f4 |
| credit_card | CC2b | -- | Fold the daily balance into a finance charge over effective-dated APR segments, with purchases joining the average daily balance on grace loss. | #74 | -- | after #22 / balance:X-f4 |
| credit_card | CC2c | -- | Ride card APR history on `rate_history` through a card-gated write route, pinning that the loan loaders never see the card. | #75 | -- | after #22 / balance:X-f4 |
| credit_card | CC3a | -- | Add `charge_to_card` and its undo as an additive action with row locking, guards and provenance, beside the still-live mark-credit. | #76 | -- | after #22 / balance:X-f4 |
| credit_card | CC3b | -- | Cut mark-credit over to charge-to-card at the transaction level, with an in-migration backfill of every live pair and a provenance-restoring downgrade. | #77 | -- | after #22 / balance:X-f1 (shipped; absorbed the X-f1b leaf this once named) / balance:X-f4 |
| credit_card | CC3c | -- | Rewrite `entry_credit_workflow.py` as `entry_card_charge.py` for split tender, rename `is_credit` to `is_card_tender`, and delete `credit_workflow.py` whole. | #78 | -- | after #22 / balance:X-f4 |
| credit_card | CC3d | -- | Refuse what the card cannot model -- transfers OUT of the card -- and give `active_accounts_query` an orthogonal `revolving` filter. | #79 | -- | after #22 / balance:X-f4 |
| bank_import | X-gg | -- | Run the ENVELOPE-SEMANTICS design loop with the developer (**R-GK**): how an envelope fills from rule-assigned swipes and stays open through its period, what closes it when statement coverage completes, what carry-forward then means, and which row identity the grid renders (the same-name double-render the review measured) -- the loop mints the build steps rather than building. | #80 | -- | after #78 / credit_card:CC3c (the loop designs over the card-tender entry shape CC3c rewrites; designing over the payback shape the card arc deletes is what withdrew balance:X-au-i) |
| credit_card | CC4a | -- | Add `card_payment_settings` with its payment-mode ref table and the creation flow mirroring the loan payment transfer. | #81 | -- | after #22 / balance:X-f4 |
| credit_card | CC4b | -- | Derive the projected card payment from the statement balance at last close minus redemptions since, as a CARD rule behind the amount resolver rather than a sixth entry in an override map. | #82 | -- | after #22 / balance:X-f4 |
| credit_card | CC4c | -- | Warn on underpayment with a one-click "pay statement balance", and maintain ONE projected finance-charge expense when grace fails. | #83 | -- | after #22 / balance:X-f4 |
| credit_card | CC4d | -- | Give CC4c's projected finance charge its OWN pricing link (**balance:R-IY**): a typed FK that `ck_transactions_one_pricing_link` counts, so the charge declares the relation pricing it as every other derived row does. **N-264** was written as a reason `amount_source_id` must survive; the ruling takes its other reading. Closes **N-264**. | #84 | -- | after #83 / credit_card:CC4c |
| balance | X-au-l | -- | DELETE `amount_source_id` (**R-IY**, **R-IZ**): the relation is derivable from the pricing link the row carries, so the column, `ref.amount_sources`, `AmountSourceEnum` and its reverse map go and `amount_rule` reads the LINK -- ONE walk. Both CHECKs become *a figure OR exactly one pricing link*, making **N-440** unrepresentable. A migration; moves no money. Closes **N-440**. | #85 | -- | after #84 / credit_card:CC4d / balance:X-au-d / balance:X-au-e / balance:X-au-f |
| credit_card | CC5a | -- | Accrue rewards as a derived figure over settled purchases minus redemptions, carrying the `system_origin_id` migration. | #86 | -- | after #22 / balance:X-f4 |
| credit_card | CC5b | -- | Add manual redemptions and the auto-redeem threshold, holding the one-live-row invariant under the concurrency shape. | #87 | -- | after #22 / balance:X-f4 |
| balance | X-p | -- | Put the analytics calendar's day chips and its balance line on one clock, or render the row that explains the gap. Closes **N-58**, **N-97**. | #88 | -- | after #22 / balance:X-f (the container ticks at #20; the import shrinks the date noise at its source, so ruling before it decides against numbers that then change) |
| balance | E2-0 | -- | Trace the super-package membership from the code: which modules are members, what the public re-export surface is, and whether any member imports a non-member. Expect it to DECOMPOSE. Carries **N-33**, **N-35**. | #89 | -- | NOW |
| balance | E2-n | -- | Make the move and delete the registry, with `_FENCED_MODULE_RULINGS` as the LAST commit rather than the first. Its decomposition is decided from `balance:E2-0`'s trace. | #90 | -- | NOW |
| balance | G1 | -- | Trace each allowlist entry to its real cause, then stop the ledger-model and balance-seam fences carrying name lists. Closes **N-147**. | #91 | -- | NOW |
| balance | G2 | -- | Build the `Money` and `DisplayLabel` value types that retire W9901, W9904 and W9902, taking the small label half first. Phase G runs INSIDE E2 by ruling R-DQ. | #92 | -- | NOW |
| balance | X-at | -- | Surface which tax year a figure was actually computed against, and give a new year's bracket set a write door, so the resolver's substitution stops being invisible and the settings screen can finish the year it starts. Closes **N-235**, **N-236**. | #93 | -- | NOW |
| balance | X-av | -- | Make the stored salary fact a DATED PER-PAYCHECK GROSS with the annual figure derived from it (**R-HW(b)**), which both gives the base an effective date -- so the app can tell a raise from a correction -- and lets the owner enter the `$3,526.00` his stub actually pays instead of an annual figure that divides to four cents less. Closes **N-237** and **N-391**. | #94 | -- | NOW |
| balance | X-ax | -- | Reconcile a carry-forward rollover against its source: when a settled source's spend changes, the target row's top-up moves by the same amount so the two always sum to the budget the rollover divided. **MOVES MONEY.** Closes **N-249**. | #95 | -- | NOW |
| balance | X-ay | -- | Post a modelled account's own CONTRIBUTIONS and ACCRUALS, so its balance-assertion residual is what the account earned rather than what it earned plus what was paid into it. Closes **N-277**. | #96 | -- | NOW |
| recurrence | R7d-c-2 | -- | Let GENERATION resolve the bound instead of reading the column: both engines' `resolve_generation_plan` applies `loan_payment_window`'s answer over the rule's own, which **MOVES MONEY** -- today's stale column drops the Van's `$531.94` installment due `2029-02-22`. Carries **D46**. | #97 | -- | after #8 / balance:X-au-f / balance:X-au-k (shipped) / recurrence:R7d-b (shipped) / recurrence:R7d-c-1 (shipped) |
| recurrence | R7d-h | -- | Give a loan ONE closing date, past AND future: `loan_payoff_date` folds FORWARD only, so a loan already at zero returns `None` -- not *never*, just *not my half* -- and three consumers each invent a meaning for it. One `loan_closing_date(account, ctx)` answers both sides, `recurrence_end_date` is DELETED, and a retired loan's stop stops tracking the read clock. No migration. | #98 | -- | NOW / recurrence:R7d-b (shipped) |
| recurrence | R7d-d | -- | Move the DISPLAY readers onto the resolver through a COMPOSED DOOR, so the Recurring surface's cadence sentence and next date name the loan's derived payoff and not the last value a chokepoint wrote: the authored bound and the derived stop become ONE `Closing` the walk already reads, and the answer SHAPES move where the phrase-writer can import them. | #99 | -- | after #98 / recurrence:R7d-h / recurrence:R7d-b (shipped) |
| recurrence | R7d-e | -- | Move `obligations_aggregator.has_ended` onto the resolver, so a RETIRED loan's payment leaves `/obligations` and the `/savings` emergency-fund baseline on the day the loan is finished rather than on the day some chokepoint last ran; it **MOVES MONEY**. | #100 | -- | NOW / recurrence:R7d-b (shipped) |
| recurrence | R7d-f | -- | Split `owns_validity_window` per bound and put the recurrence form's locked "Ends" control, its inverted-window refusal and its live preview on the resolver, deciding what an owner with no baseline scenario is shown (**R-R30**). | #101 | -- | NOW / recurrence:R7d-b (shipped) |
| recurrence | R7d-g | -- | Stop WRITING the column: nine of the ten `sync_recurring_payment_bounds` call sites go, `end_date` goes NULL for every loan payment in a migration, and `ck_recurrence_rules_valid_window` lands true by construction -- once what repairs the three shapes `_sync_loan_cadence` covers is decided. Re-points **D35**; carries **D50**. | #102 | -- | after #101 / recurrence:R7d-c-2 / recurrence:R7d-d / recurrence:R7d-e / recurrence:R7d-f |
| recurrence | R16-b-2 | -- | Let the ESTIMATED tier SUM every recurring definition paying into a loan, each expanded on its OWN cadence and each HONOURING the bound its owner authored, and move the charge calendar off the payments onto the loan's contractual installments so a definition sparser than monthly stops collapsing the charge set with the payment set. **MOVES MONEY, OWN PR.** Closes **D47**, **D48**, **D53**. | #103 | -- | after #102 / recurrence:R7d-g / recurrence:R16-a (shipped) / recurrence:R16-b-1 (shipped) |
| recurrence | R16-c | -- | Make a loan's PAST and FUTURE one event STREAM -- one list with `as_of` marking where recorded fact becomes projection, one seed, one set of record types. The fold RULE is already shared: `balance:X-au-g-2c-3b-2` (`3b7716f8`) shipped `replay_loan_events` and both tiers call it, so this step SHRANK because part of it SHIPPED EARLY rather than having scope dropped. **MOVES POSTED MONEY, OWN PR**. | #104 | -- | after #103 / recurrence:R16-b-2 |
| recurrence | R16-d | -- | Make the interest ACCRUAL CONVENTION a value on the loan -- a `ref` table, a column, and one total `accrued_interest(balance, rate, from, to, convention)` of which today's monthly formula is one member -- backfilling every loan to `MONTHLY_1_12` so the migration moves `$0.00`, and retiring `debt_strategy_service`'s inline copy. Closes **D52**. | #105 | -- | after #104 / recurrence:R16-c |
| recurrence | R7e | -- | Make the recurrence form's THREE-state fields one typed submission the schema emits, so `stated` / `cleared` / `not mentioned` stops being re-derived by a `KEY in data` read at each route site. Closes **D36**. | #106 | -- | NOW |
| recurrence | R7f | -- | Decide, then apply, what a PROGRAMMATICALLY created recurrence starts on: the contribution and salary-profile routes seat theirs at the schedule's OPENING and fan rows into closed pay periods, where the form defaults to today. **MOVES MONEY.** Closes **D34**. | #107 | -- | NOW |
| recurrence | R11 | -- | Rule and then add the LEAD placement -- funding an occurrence from a paycheck EARLIER than the one containing it -- which is what survived **D20** when measurement showed `CONTAINING_DATE` already funds on or before every occurrence. **MOVES MONEY.** Closes **D40**. | #108 | -- | NOW |
| recurrence | R12 | -- | Give `deploy/shekel-deploy.sh` an executable test for the two predicates a destructive release now rests on, `preflight_migrations` and `repin_is_safe`, deciding first where a shell harness runs in a repository whose gates scope to `app/`, `scripts/` and `tools/`. Closes **D41**. | #109 | -- | NOW |
| pay_calendar | C10 | -- | Take the FIVE `app/routes/salary/` reads of "which paycheck am I in" off the process clock and onto the owner's civil day, which is the half of that row `C2-f3a` did not reach. Closes **P49**. | #110 | -- | NOW |
| pay_calendar | C11 | -- | Close the last FIVE service doors that open their own read pass and assert the LAYER predicate in their place -- no module under `app/services/**` calls `BalanceContext.build` -- carving out `loan_recurrence_sync`, which is a writer and takes one by design. Closes **P56**, **P69**. | #111 | -- | NOW |
| pay_calendar | C12 | -- | Rule and then collapse the THREE implementations of "the owner's current paycheck breakdown" into one producer, which changes what two other pages publish and is what gives `income_service`'s amount basis a threaded calendar; it also owes the paycheck engine's own 1000-line ceiling, which no other step shrinks. Closes **P62**, **P63**, **P64**'s engine half. | #112 | -- | NOW |
| recurrence | R13 | -- | Give `budget.pay_schedule` a cadence KIND so a semi-monthly or monthly owner's paydays land on the days of the month they are actually paid, rather than on a fixed-length walk that drifts through it, branching the batch writer, the derivation and the paycheck count on the kind. Ruling **R-R28**. | #113 | -- | NOW |
| bank_import | X-f6g | -- | Stop the reconcile panel offering an account whose statements the owner imports, after a specification pass decides what statement coverage means for a day no import spans. | #114 | -- | NOW |
| balance | X-i5 | -- | Make every request a QUERY until it DECLARES a write, so the HTTP method stops standing in for "does this write": each of the 120 non-GET handlers puts its writes in a `write_transaction` block and the render that follows takes a fresh snapshot holding them, which is the shape `/grid` already has. Closes **N-358**. | #115 | -- | NOW / balance:X-i3-a (shipped) |
| balance | X-bc | -- | Make the collateral link's same-owner invariant STRUCTURAL: `budget.accounts.collateral_account_id` becomes a COMPOSITE foreign key on `(collateral_account_id, user_id)` against the `uq_accounts_id_user` superkey two other tables already target, and `_validate_collateral_link`'s ownership arm goes with the state it can no longer express. Closes **N-360**. | #116 | -- | NOW |
| balance | X-i6 | -- | Bind the per-account reads a pass does NOT memoize and so never reach `_memoize_once`: the seven seam entries that early-out before the funnel, the contribution feed that scopes its payroll loaders off the ACCOUNT's owner, and `bank_agreement`'s direct walk -- with the ARM that mechanizes "one door". Closes **N-362**. | #117 | -- | NOW / balance:X-i4 (shipped) |
| balance | X-be | -- | Resolve the THREE `app/` modules sitting at exactly pylint's 1000-line ceiling -- `calendar_service`, `balance_at/_asset_fold` and `anchor_service` -- by deciding per module whether it SPLITS or whether the ceiling is the wrong number, because in this corpus what the cap binds is what gets written down. Closes **N-365**. | #118 | -- | NOW |
| balance | X-bf | -- | Make a stale or FORKED test template self-reporting: record the ordered revision list the builder actually applied and refuse a bootstrap whose repo chain no longer produces it, because `alembic_version` records which revision is head and never which ones ran. Closes **N-385**. | #119 | -- | NOW |
| balance | X-bg | -- | Tell *this occurrence did not happen* apart from *archive this row*, which the transfer delete door conflates into one `is_deleted` flag read off the template link, so a settled instance the owner removes stays restorable by the recurrence conflict chooser with no control between it and the books. Closes **N-386**. | #120 | -- | NOW |
| pay_calendar | C4-d | -- | Collapse `ScheduleFacts`' four representable states to the two a row can hold: `resolve_schedule` returns an OPTIONAL `ScheduleFacts` whose `cadence_days` is non-optional, so a pair naming no cadence beside a stated `history_opens_on` stops being constructible, and `calendar_for`'s answer for a schedule-less owner is decided as this leaf's own fork. **OWN REVIEW PASS.** Ruling **R-PC44**. | SHIPPED | `327a70f2` | -- |
| balance | X-au-g-2c-3b-2 | -- | Let the SETTLED walk take the charge calendar -- ONE interest accrual and ONE escrow per INSTALLMENT rather than one per PAYMENT -- which DELETES `split_payment_cash`, the composition whose own docstring concedes it is correct only while a loan takes one payment per accrual period. **MOVES POSTED MONEY, OWN PR.** Carries **N-409**'s second half and satisfies **recurrence:D51**. | SHIPPED | `3b7716f8` | -- |
| balance | X-au-d | -- | Cut SALARY rows over: 59 non-override rows declare their definition, generation stops pricing them, and the read-time repair with its whole seam is deleted. Closes **N-224**, **N-228**, **N-258**, **N-261**, **N-253**, **N-437**. | SHIPPED | `ed06acf6` | -- |
| balance | X-au-e | -- | Cut TEMPLATE rows over to their template's amount series: generation stopped pricing, 525 non-override rows went NULL, and the conflict chooser kept its offer while losing its figure (**R-JD**). Closes **N-244**, **N-247**, **N-444**. | SHIPPED | `b846386a` | -- |
| balance | X-au-k | -- | Made `amount_ownership`'s "ONE writer" claim STRUCTURAL: a SQLAlchemy `composite()` maps a row's figure and its `amount_source_id` as ONE attribute over a value object total across R-FI's two states, so the illegal pair has no expression and the two splat sites a census cannot see are refused (**R-IW**). No migration. Closed **N-293**; opened **N-437** and **N-440**. | SHIPPED | `7315ecd9` | -- |
| balance | X-au-g-2c-3a | -- | Moved the ONE allocation down to `app/utils/money.py`, the leaf every walk reaches, and DELETED the three restatements the old placement FORCED -- two walks sat BELOW it, so reaching it was an import cycle. `$0.00`; 0 differences over 200,000 trials against `HEAD`. | SHIPPED | `becf76f8` | -- |
| balance | X-au-g-2c-3c | -- | Routed `debt_strategy_service._accrue_interest` through the ONE accrual primitive, deleting a FIFTH spelling that associated `(b*r)/12` against `b*(r/12)`. **`recurrence:D52`'s "agree on 200,000 randomised draws" is REFUTED**: at 500,000 they part, `$565.37` against `$565.36`. `$0.00` on both live loans. | SHIPPED | `cb6469b2` | -- |
| balance | X-au-g-2c-3b-1 | -- | Moved the CHARGE calendar to `loan_ledger._charges` -- the same inversion `2c-3a` fixed, one tier up: it sat in `balance_at._plan`, whose closure REACHES the settled walk, so that walk could not call it and restated the rule per payment instead. **This is what makes `3b-2` a taking rather than a rebuild.** `$0.00`; 0 mismatches over 20,000 trials against `HEAD`'s `_charges_for`. | SHIPPED | `fd3afc59` | -- |
| bank_import | X-gj-2b | -- | The DECOMPOSED parent of the refund filing, split three ways during the build on the boundaries the work turned out to have: what makes a negative purchase representable, what makes a standing rule file one, and what every reader of a signed amount does with it. It ticked with its third leaf. | SHIPPED | `a23315dc` | -- |
| bank_import | X-gj-2b-1 | -- | Relax `ck_transaction_entries_positive_amount` from `amount > 0` to `amount <> 0` (**R-II**) so a merchant credit can file as a contra-entry against the envelope its merchant rule names, making the act representable, editable and safe while no rule yet files one. | SHIPPED | `9920bed7` | -- |
| bank_import | X-gj-2b-2 | -- | Let a standing rule FILE a refund (**R-HT(a)**), auto-applied at import, by correcting the partition that let an unexplained line's SIGN choose its pipeline before the owner's rule was consulted at all. | SHIPPED | `1bfeff07` | -- |
| bank_import | X-gj-2b-3 | -- | The reader census **R-II**'s relaxation owed, which enumerated every reader of a signed entry amount and ruled on each rather than grepping for an absence, and ended with both hand-entry doors taking a MAGNITUDE plus a Charge/Refund control so a purchase's sign is PICKED and never typed (**R-IK**, **R-IL**, **R-IM**). | SHIPPED | `a23315dc` | -- |
| balance | X-au-g-1 | -- | Delete the pricing cycle by reading a loan's monthly P&I from its TERMS rather than from its own payment history, so the loan no longer prices the rows its price was derived from. | SHIPPED | `af61263d` | -- |
| balance | X-au-g-2a | -- | Move amount rule 4's producer DOWN into the amount model, `loan_payment_service._basis` / `._pricing` becoming `cash_ledger._loan_installment` / `._loan_pricing`, so the lower tier stops reaching UP into a loan service to price a row -- the unwind `row_valuation.py` says this arc owes, and what lets the loan stack name the resolver at all. Closes **N-266**(b). | SHIPPED | `b16908f7` | -- |
| balance | X-au-g-2b | -- | Put a loan's contractual terms on the INSTALLMENT they govern rather than on a read date (**R-IJ**) at three sites, after which `cash_ledger` makes no clock call at all; the escrow-subtraction threshold was built, MEASURED a regression and reverted, so **N-409** stands re-owned. Closed **N-40** and **N-410**. | SHIPPED | `6cd0ad44` | -- |
| balance | X-au-g-2c-1 | -- | Routed BOTH readers of a projected loan-side shadow through the amount model, `get_payment_history` and the plan tier, threading an `AmountBasis` through `load_loan_context` and its three callers; the finding named ONE and a census of nine call sites found TWO, seven settled-only. Byte-identical on production's 58 shadows. Closed **N-266**; opened **N-432**. | SHIPPED | `cdc2c7d9` | -- |
| balance | X-au-g-2c-2 | -- | Declared EVERY transfer shadow DERIVED (**R-IN**), not a loan payment's two legs alone, making Transfer Invariant 3 STRUCTURAL and deleting both repairs that maintained it. The owner's typed figure survives any save stating no NEW one (**R-IO**): two reviews found two doors handing a taken leg back, the second inside the first's fix. 350 rows, `$0.00`. Closed **N-401**, opened **N-436**. | SHIPPED | `1f2b98a4` | -- |
| balance | X-f3c-2b-2a | -- | Give an owner the DOOR that restates an account's opening -- append-only, carrying the day, the equity and a `user_declared` provenance, through the table's ONE writer, bounded by the movements the account records and by today but NOT by the calendar -- reachable from the shared account edit page and linked to from the balance-history card's opening row (**R-IE**). | SHIPPED | `59b485df` | -- |
| balance | X-f3c-2b-2b | -- | Made a matched bank LINE bound its account's opening at both tiers: three import doors and the restatement door refuse a line the books cannot hold, deferred triggers on `statement_match_members` and `bank_statement_lines` make the state unstorable, and the boundary's builders became ARM-EXPLICIT so a revision installs only the arm it censused (**R-IG**, **R-IH**).  Closed **N-383**. | SHIPPED | `7ef63899` | -- |
| bank_import | X-gk | -- | Build the MERCHANTS surface the audit's fix column names: one row per merchant this account has seen, carrying its standing answer or *I have not said*, edited on click through the door the receipt's offer and the register already post to, so that the question three partial surfaces share today has one durable home. | SHIPPED | `8569e5ec` | -- |
| balance | X-be-3 | -- | Widened the world to a salary profile, a transaction, a transfer, a goal, BOTH template kinds and a pension, then deleted `_UNREACHED_RULES` and the skip branch feeding it, so *every GET route is graded* became an equality against `url_map` rather than a list of the ones that were not: all 17 rules it had never requested answer, and **none 5xx**. Closed **N-388**. | SHIPPED | `0aa2cc80` | -- |
| balance | X-bh-2 | -- | Made the payday RHYTHM run BACKWARD as well as forward, bounded by a stored `budget.pay_schedule.history_opens_on` two doors ask for, so an owner who says when their paychecks began is counted from their real first one and an owner who says nothing is counted from the record, unchanged (**R-IA**, amended **R-IF**). Closed **N-390** and **N-396**; opened **N-398**, **N-399**. | SHIPPED | `49fdfb91` | -- |
| balance | X-bh-1 | -- | Made the paycheck engine read the owner's whole pay CALENDAR off its `PayrollBasis`, rather than an `all_periods` sequence every window and one-period sample satisfied, and collapsed its four calendar judgements onto two producers the analytics month card shares in its bounded form (**balance:R-IB**). Closed **N-390**'s type half; opened **N-394** and **N-395**. | SHIPPED | `b955d0c8` | -- |
| balance | X-aw | -- | Made a paycheck's gross a RATE rather than a share of a year (**R-HW**): the salary over the owner's paycheck count, one figure per salary segment, so the period LIST that decided which paychecks took a rounding-residue cent is gone and with it the defect that re-priced settled paychecks as the schedule grew. Closed **N-239**'s horizon half and opened **N-390** and **N-391**. | SHIPPED | `078077db` | -- |
| balance | X-be-2 | -- | Gave the suite a way to say WHAT WORLD a test starts in: a named seeded start state, built ONCE per worker and frozen into a snapshot every declaring test still takes its OWN private clone of, so the `url_map` sweep stopped rebuilding one identical world 236 times without weakening what isolates one test from another. Closed **N-387**, whose premise it measured FALSE; opened **N-388**. | SHIPPED | `167aab8d` | -- |
| bank_import | X-gf | -- | The DECOMPOSED parent of the review-screen rebuild, split 2026-08-27 into the disposition hole its model left open, the queue-versus-register split and the exception queue proper, whose own third leaf split again. Ticked with the last of its seven leaves; minted **X-gi**. | SHIPPED | `ff744d79` | -- |
| balance | X-bd | -- | Gave every route in the `url_map` sweep its OWN pytest item so the per-test wall clock stops containing the route table at all, rather than re-cutting arms that only divide it, and enumerated the routes without a database so collection cannot fail. Closed **N-364**; opened **N-387**. | SHIPPED | `39935763` | -- |
| bank_import | X-gf-3b-2 | -- | Rebuild what remains of the queue as ONE list of unexplained bank lines grouped by the DECISION each poses -- *is this money my books already hold, or is it new?* -- rather than the three cards that partition them by MECHANISM, which is the service's partition and stays load-bearing underneath the screen. | SHIPPED | `ff744d79` | -- |
| balance | X-f3c-2c-1 | -- | Stop every test fixture EDITING a stored balance assertion: the three re-stamping helpers become one that APPENDS, the three account factories take their day at `create_account`, and the seeded owner's origination assertion stays on the bootstrap day the account was opened on. Carries **N-392** and **N-393**. | SHIPPED | `930f06fc` | -- |
| balance | X-f3c-2c-2 | -- | Give `budget.account_anchor_history`, `budget.account_openings` and `budget.loan_anchor_events` ONE shared append-only refusal -- a database trigger refusing every actor and every spelling, plus the named object-layer exception (**R-HY**) -- with `Account.anchor_history` taking `passive_deletes` so an account can still be disposed of. Closes **N-287**. | SHIPPED | `930f06fc` | -- |
| balance | X-f3c-2c | -- | The DECOMPOSED parent of the append-only refusal, split 2026-08-30 (**R-HZ**): the fixtures stop editing an assertion in one commit, and the refusal that makes editing one impossible is the next, on one branch so `dev` never carries the gap between them. Carried **N-287**. | SHIPPED | `930f06fc` | -- |
| balance | X-f3c-2d | -- | Give the append-only refusal the three arms its three questions need -- `BEFORE UPDATE` per row, a DEFERRED constraint trigger `AFTER DELETE` because "is the owning account gone" is about the transaction's END state, and `BEFORE TRUNCATE` per statement (**R-IC**) -- closing the TRUNCATE that emptied a table unrefused and UNRECORDED and the recreate that destroyed an account's assertions. | SHIPPED | `249f66a7` | -- |
| balance | X-f3c-3 | -- | Derived the account's OUTSTANDING DIFFERENCE -- its latest asserted balance less its opening equity plus the postings through that day, ONE figure and not the per-assertion plug -- and put it on its own cash-detail card beside whether an imported statement's lines account for the span. Opened **N-433**, **N-434**. | SHIPPED | `a7dbfdca` | -- |
| balance | X-f3c-2b-1 | -- | Make a movement dated on or before its account's books opening UNSTORABLE (**R-HG**), refusing it at the one settle-day writer, at the reconcile panel's bulk writer and at three deferrable constraint triggers over all three tables, then legalise the twelve production rows by moving five openings back. **MOVES MONEY**: two balance sheets gain an opening at `$4,879.26`. Closes **N-378**. | SHIPPED | `2cf2ac0a` | -- |
| balance | X-f3c-2a | -- | Make opening equity the account's OWN stored fact (**R-GX**, shaped by **R-HE**): an append-only `budget.account_openings` read by BOTH the balance fold and the posted ledger, so the quantity has ONE definition and `is_opening` decides no figure. Seeded at the derived value. Carries **N-379**. | SHIPPED | `2aa2296d` | -- |
| balance | X-f3c-1 | -- | The assertion RESET left the kind-blind walk for the fold that owns the running total: `walk_cash_ledger` yields FACTS, `balance_at._assertions` states what an assertion does to a balance, and each fold applies the policy its own kind needs -- which is what makes the flip a deletion rather than a rewrite of a leaf **R-FO** keeps the reset for. Byte-identical. | SHIPPED | `2dad8512` | -- |
| balance | X-au-g-2c-3 | -- | The DECOMPOSED parent of the escrow rule, split 2026-09-02 when its own trace found the defect was not ONE floor but FOUR statements of one allocation rule and TWO of one charge calendar -- each duplicated because the shared rule sat ABOVE the walks that needed it, so reaching it was an import cycle. | container | -- | ticks with #26 |
| balance | X-au-g-2c-3b | -- | The DECOMPOSED parent of the CHARGE-CALENDAR half, split 2026-09-02: the calendar moves to the leaf both walks reach, then the SETTLED walk takes it, then the ENGINE FEED stops allocating. | container | -- | ticks with #26 |
| balance | X-bi | -- | The DECOMPOSED parent of the MOVEMENT UNIFICATION (**R-IT**): every plan item is satisfied by zero or more movements, so a bill is the one-movement case and an envelope the many-movement case, `budget.transaction_entries` becomes the general movement table, and the balance folds movements only. | container | -- | ticks with #16 |
| bank_import | X-gj-3 | -- | The DECOMPOSED parent of the standing-rule GROUP answer, split 2026-09-01 and ticked with `X-gj-3a` because its second leaf was WITHDRAWN (**R-JJ**): the ACT of landing a group's difference on a named member shipped, and the RULE that would have remembered the row set was refused on a census -- 3 of 221 acts name more than one row, each population deleted elsewhere. | SHIPPED | `e42dcd6b` | -- |
| bank_import | X-gj-3a | -- | Let a group's DIFFERENCE land on a member the OWNER names, which the door could do only for a match naming ONE row: `bank_cash_for` becomes a `DifferenceLanding` -- the bank's total less the OTHER members, which is the old answer at a lone row -- and the pane offers the choice with NO member pre-selected (**R-FZ(b)**). Split `_moving.py` out of `_accept.py` (**balance:R-IR**). | SHIPPED | `e42dcd6b` | -- |
| bank_import | X-gj-4c | -- | The DECOMPOSED parent of the SKIPPED TAB, split 2026-09-03 at its gate on the developer's ruling: the lines a standing *never a purchase* answer bars leave `parked` in one commit, and the tab's own reader over the recorded skips follows, because the second would otherwise put two KINDS of card on one tab while the first is what empties it. | container | -- | ticks with #1 |
| bank_import | X-gj-4c-1 | -- | Return the lines a standing *never a purchase* answer bars to the INBOX (**R-JH**), removing them from `parked` ALTOGETHER rather than merely dropping `_parked_tab`'s arm, which would total that list on TRANSFERS and put a line no source filed as a payment under a chip rendering a COUNT and a MAGNITUDE. | SHIPPED | `456d6bd2` | -- |
| bank_import | X-gj-4 | -- | The DECOMPOSED parent of the SKIP verb, split 2026-09-02 on the developer's ruling at its gate, in the SAFE order rather than the obvious one: the STORE needs a migration and renders nothing, the TAB gives a skip somewhere to be seen and undone, and the VERB is lit LAST because lighting it first would let an owner apply an act that appears on no tab and has no Undo (**R-GY**, **R-HU**). | container | -- | ticks with #2 |
| bank_import | X-gj-4a | -- | Record a SKIPPED line's disposition as its own act row (**R-JG**): `budget.statement_line_skips`, the skip and unskip doors, and the review pass and grid badge honouring both, so a skip is remembered, is undoable, and a line carrying one stops being asked about. It MOVES NO MONEY and can move none: the one table it writes holds no figure. | SHIPPED | `758bbe55` | -- |
| bank_import | X-gj-2 | -- | The DECOMPOSED parent of the standing-rule INFLOW answer (**R-HT(a)**), split 2026-08-31 on the developer's ruling: the deposit-signature half REPORTS and ships first, the merchant-credit refund half FILES and needs the positivity rule moved off the table (**R-II**). | SHIPPED | `a23315dc` | -- |
| balance | X-au-g | -- | The DECOMPOSED parent of the LOAN-PAYMENT cutover, split 2026-08-31 into the pricing cycle's deletion, the tier move that unwinds the amount model's reach into the loan service, the ruling that puts a loan's terms on the installment they govern, and the cutover those three unblock. | container | -- | ticks with #26 |
| balance | X-au-g-2c | -- | The DECOMPOSED parent of the loan-payment CUTOVER, split 2026-09-01: the readers route first and move nothing, the cutover declares both legs derived, and the escrow rule **N-409** needs is its own money-moving act. | container | -- | ticks with #26 |
| balance | X-f3c-2b | -- | The DECOMPOSED parent of the books boundary, split 2026-08-28 (**R-HG**) and re-cut 2026-08-28 (**R-HJ**): the INVARIANT and the restatement that legalises the existing rows are one commit, and the DOOR that restates an opening -- with the account-10 repair it is the only honest route for -- is another, and the ASSERTION side is a fourth leaf sequenced behind THE FLIP. Carries **N-379**. | container | -- | ticks with #20 |
| balance | X-f3c-2b-2 | -- | The DECOMPOSED parent of the DOOR, split 2026-08-31 (**R-ID**): the door is one commit, the bank line an import may not offer against it is the next, and the repair the door exists for is an OPERATOR act that cannot run until the first has deployed. Carries **N-379**, **N-382**. | container | -- | ticks with #7 |
| balance | X-f3c | -- | The DECOMPOSED parent of THE CUTOVER, re-decomposed 2026-08-27 (**balance:R-GW**) when a measurement against the developer's own bank export refuted its ORDER: the residual is RECORDED before the reset is DELETED, so the flip lands on the balance already asserted rather than six times further from the bank. Carries **N-172**, **N-174**. | container | -- | ticks with #20 |
| bank_import | X-gf-1 | -- | A bank line of money COMING IN that no app row explains became an uncategorized INCOME row (**bank_import:R-GW**), **MOVING MONEY**: two correct refusals had pointed at each other and left every unmatched deposit with no act at all -- 8 lines, `$58.87`, on the developer's own data. | SHIPPED | `a4db019f` | -- |
| bank_import | X-gf-2 | -- | THE REGISTER IS NOT THE QUEUE (**bank_import:R-GX**, **R-GY**): the 29 answered merchants and 221 accepted acts left the review screen for their own surface, taking the review body from 578,523 bytes to 149,103 and `review_set` from 146 SQL statements to 7. Closed **N-358** structurally and **N-349**; opened **bank_import:N-371**, **N-372**. | SHIPPED | `64cfca05` | -- |
| bank_import | X-gf-3 | -- | The DECOMPOSED parent of the exception queue proper, split 2026-08-27 on the developer's ruling into the rule verdict both the automatic door and the screen read, and the queue's own shape. | SHIPPED | `ff744d79` | -- |
| bank_import | X-gd-2 | -- | Ship the rule STORE (**R-GI**): a stated merchant answer gains a fourth answer, *ask me every time*, told apart from *never a purchase* by one boolean (**R-GS**); the withdrawal goes, so a rule is only ever restated; and a match act records whether a standing rule performed it (**R-GT**). Closed **N-353**; opened **N-358**. | SHIPPED | `154cfcec` | -- |
| bank_import | X-gd-1 | -- | Make a merchant a ROW rather than a string two tables each keep a 100-character copy of (**R-GR**), so the answer's key is a foreign key, the scope check becomes a sentence and *which merchants may be asked about* is one table read. | SHIPPED | `395b14f7` | -- |
| bank_import | X-gc | -- | Three import surfaces stopped stating what is false: the corroboration advice no longer prescribes an export option SECU dropped, the never-showed panel stops claiming the bank failed to pay 18 CC Payback rows it only ever shows inside a card payment, and the destructive dialog binds in `<head>` so it cannot be raced. Closed **N-345**'s race half; re-pointed its other half at `operator`. | SHIPPED | `0452eef3` | -- |
| bank_import | X-gb | -- | The transaction DELETE door got a surface and a disclosure, a `$0.00` row stopped rendering an unclickable chip, a row leaving the books withdraws the matches it was the last app row of (**R-GM**), and the dev books were repaired through those doors -- `$7,769.58` double-booked, 14 rows, 11 matches. Closed **N-344**; opened **N-348**. | SHIPPED | `ec346c46` | -- |
| balance | X-am | -- | Deleted the `Settled` status -- the terminal ARCHIVE, reachable from one dropdown, carrying zero rows in any snapshot or audit trail, and refusing every act that CORRECTS a row while permitting the DELETE that destroys it -- and ruled that no state may be both reachable and absorbing (**R-HA**). Closed **N-177**. | SHIPPED | `7b0ddae8` | -- |
| bank_import | X-gf-3b-1 | -- | THE WORKBENCH IS NOT THE QUEUE (**bank_import:R-HC**): the hand-build match form left the review screen for a surface of its own with its own write door, taking the review body from 150,853 bytes to 65,005 and its checkboxes from 112 to 17, and every exception now links to it with its own line already ticked. Closes **N-374**. | SHIPPED | `d2248fe6` | -- |
| bank_import | X-gf-3b | -- | The DECOMPOSED parent of the exception queue proper's own second leaf, split 2026-08-28 on measurement: the step's sentence carried two rulings, and **R-HC**'s half is 59% of the page while **R-HB**'s is a re-shaping of what is left. Merging the cards first would have rendered the same 27 lines twice on one page. It ticks with the last of its two leaves. | SHIPPED | `ff744d79` | -- |
| bank_import | X-gf-3a | -- | Make what a standing rule comes to for a line ONE verdict the automatic door and the review screen both read, so a line a rule would have filed and the pass withheld says why on the screen rather than only in the import's transient flash, and a parked line names the register only where a different answer would open the create door. Closes **N-359**, **N-371**. | SHIPPED | `44f1cc7b` | -- |
| bank_import | X-gd | -- | Promote a stated merchant answer from a suggestion to a standing RULE (**R-GI**), which the developer decomposed on 2026-08-25 into the merchant's own identity and the store that keys on it. | SHIPPED | `d1910c95` | -- |
| balance | X-f | -- | The DECOMPOSED parent of the whole "the app records when money moved" cluster, carrying **N-42**. | container | -- | ticks with #22 |
| balance | X-au | -- | The DECOMPOSED parent of the amount model (**R-FI**): a row's amount is either its OWN or DERIVED, and a derived amount is not stored at all. Supersedes **X-ar**; **X-au-i is WITHDRAWN** because the card arc's locked rulings delete the CC payback it would have cut over (`CC3b`, `CC3c`). Carries **N-224**, **N-228**, **N-238**. | container | -- | ticks with #85 |
| balance | X-au-c | -- | The DECOMPOSED parent of the amount model's SEAM, split into three leaves 2026-08-12: the schema and the declaration, the readers, then the freeze and its inverse. | container | -- | ticks with #6 |
| balance | X-ai | -- | The DECOMPOSED parent of the posted-ledger restructure: one verb and one trigger on both ledgers (**R-DU**, **R-DV**). Carries **N-144**, **N-153**, **N-155**, **N-157**, **N-158**, **N-160**, **N-164**. | container | -- | ticks with #53 |
| balance | X-aj | -- | The DECOMPOSED parent of the one-status-seam work; its merge half shipped as X-aj1. Carries **N-145**. | container | -- | ticks with #47 |
| balance | X-x | -- | The DECOMPOSED parent of the one-pay-calendar-precondition work. Closes **N-116**, **N-125**, **N-126**, **N-129**. | container | -- | ticks with #45 |
| balance | X-ad | -- | The DECOMPOSED parent of the pay calendar a new user can actually enter, split 2026-08-10 (**R-EZ**) into the door that CREATES a calendar and the door that GROWS it. | container | -- | ticks with #40 |
| balance | X-i | -- | The DECOMPOSED parent of the one-read-pass work: nine ledger rows with one root cause. | container | -- | ticks with #117 |
| balance | X-l | pay_calendar:C2 / recurrence:R-F12 | The DECOMPOSED parent under its BALANCE name of "the pay calendar answers any date". Closed **N-128** (at `C2-c`). It claimed **N-82** and **N-79**'s far half and delivered NEITHER, so both were re-pointed at the tick rather than closed. | SHIPPED | `4f134bf4` | -- |
| pay_calendar | C2 | balance:X-l / recurrence:R-F12 | The DECOMPOSED parent under its PAY-CALENDAR name: one calendar value answers every "which period" question, RULED on three forks 2026-08-10. | SHIPPED | `4f134bf4` | -- |
| recurrence | R-F12 | pay_calendar:C2 / balance:X-l | The DECOMPOSED parent under its RECURRENCE name: one `PeriodCalendar`, not three period-containing searches. Closes **F-12**. | SHIPPED | `4f134bf4` | -- |
| pay_calendar | C4 | -- | The DECOMPOSED parent of "the two derived columns go", split into seven leaves 2026-08-25 and into EIGHT on 2026-09-01 when `C4-b` split in two: five reader leaves by PACKAGE, the test corpus, the schedule-row foreign key, the drop, then the cadence TYPE. The drop shipped at `C4-c`; this parent waits on `C4-d`, which is the same defect one tier up in the type rather than in the schema. | SHIPPED | `327a70f2` | -- |
| pay_calendar | C13 | -- | The DECOMPOSED parent of "a transaction's owner is a COLUMN", split 2026-09-02 because the arc document already specified two leaves and this index carried one row (rule 12): the KEY lands first and changes no reader, then the readers it makes unnecessary go. | container | -- | ticks with #29 |
| pay_calendar | C4-b | -- | The DECOMPOSED parent of "an owner with paydays HAS a recorded cadence", split in two 2026-09-01 (developer) once the key's real prerequisite was measured: the TEST CORPUS builds by hand the exact owner the key forbids, so the fixture leaf goes first. | SHIPPED | `5db9f8a0` | -- |
| pay_calendar | C5 | -- | The DECOMPOSED parent of "the gap machinery goes, and a paycheck may owe one template twice", split 2026-08-09, ticked with `C5b`, its last leaf. | SHIPPED | `4e8b40b3` | -- |
| balance | X-f1 | -- | A settle carries the day the money moved; absorbed **S2-b**. Fourteen leaves, condensed into `archive/…2026-08-04.md` 1a. | SHIPPED | `8d812662` | -- |
| balance | X-f3 | -- | The DECOMPOSED parent of THE CUTOVER, re-decomposed 2026-08-13 when measurement refuted two of ruling R-EB's premises. Carries **N-171**, **N-172**, **N-174**. | container | -- | ticks with #21 |
| balance | X-f3a | -- | The DECOMPOSED parent of "clearing is a recorded fact", split 2026-08-14 (**R-FQ**) into the RECORDING half, which moves no money, and the half that makes a walked statement's SILENCE mean something, which cannot precede the cutover. Carries **N-273**. | container | -- | ticks with #21 |
| bank_import | X-f6 | -- | The DECOMPOSED parent of the statement importer, whose first leaf moved AHEAD of the cutover because what the cutover needed was clearing facts rather than an import surface. Carries **N-173**. | container | -- | ticks with #114 |
| bank_import | X-gj-1c | -- | Build the two tabs whose cards are ACTS already applied (**R-HU**, **R-GY**): Explained and Filed by rules, newest first with an act that has stopped holding at the top, Undo on the card, and the Explained chip's link, retiring the register as a page. | SHIPPED | `a43e8e2f` | -- |
| bank_import | X-gj-2a | -- | Let a standing rule answer a DEPOSIT: a deposit signature files as an income category through the fifth `RuleAnswer`, auto-applied at import, with a merchant credit reported UNRESOLVED and named as not-yet-built rather than falling back to income (**R-HX**); **MOVES MONEY, OWN PR**. | SHIPPED | `751eba5d` | -- |
| bank_import | X-gj-1 | -- | The DECOMPOSED parent of the Reconcile PAGE, split three ways along the services boundary on 2026-08-29 because one leaf measured ~3,500 lines over a service, a route pair, six templates, CSS, JS and two test modules: the view model, the page over it, then the opened card. | SHIPPED | `a43e8e2f` | -- |
| bank_import | X-gj-1a | -- | Derive the RECONCILE VIEW MODEL in the service (**R-HP**, **R-HQ**, **R-HR**, **R-HW**): one card per bank line carrying its verb, whether a rule, a tier or nothing suggested it, the sentence's parts, and which of the four verbs are OPEN with the reason a shut one is shut, plus the settled act's card, the five tab counts, the holding chips and `bank_agreement`'s headline day. | SHIPPED | `bc851df9` | NOW |
| bank_import | X-gj-1b | -- | Build the RECONCILE PAGE and the three tabs whose cards are BANK LINES (**R-HR**, **R-HS**, **R-HW**, **R-HX**): the route pair, the hero with the last import's provenance, the holding chips, the legend, the tab bar, the card and its opened four-tab panel on ONE footer, and the sticky footer's per-class sweeps and Apply (**R-FZ(c)**, **R-HD**), in both themes and both viewports. | SHIPPED | `cfcfcac9` | NOW |
| bank_import | X-gj | -- | The DECOMPOSED parent of the Reconcile screen rebuild the developer locked at Loop A round 4 on 2026-08-29 (**R-HP**..**R-HV**), which replaces review, register and workbench with one page on four verbs; it ticks with the last of its four leaves. | container | -- | ticks with #2 |
| recurrence | R16 | -- | The DECOMPOSED parent of the ESTIMATED tier's summing, split into FOUR leaves 2026-08-26 (**R-R36**) when a trace found the forward fold charging one month of interest per payment RECORD, so no cadence could be honoured until the accrual stopped riding on the payment. Carries **D47**, **D48**. | container | -- | ticks with #105 |
| recurrence | R16-b | -- | The DECOMPOSED parent of the ESTIMATED tier's summing, split into TWO leaves 2026-08-27 (**R-R37**) when the developer refused to have the sum IGNORE a closing bound an owner had entered: the walk the sum needs stops truncating on its own evidence, while the sum itself now waits for `end_date` to hold one fact. Carries **D47**, **D48**, **D53**. | container | -- | ticks with #103 |
| recurrence | R7d-c | -- | The DECOMPOSED parent of "generation takes the resolver", split into TWO leaves 2026-08-27 (**R-R38**) when the developer refused a producer opening its own read pass: the pass has to REACH generation before generation can resolve anything, and who opens it is a question about the WRITE DOORS rather than about the bound. | container | -- | ticks with #97 |
| recurrence | R7d | -- | The DECOMPOSED parent of "a loan payment's closing bound stops being a stored column", split into SEVEN leaves 2026-08-25 (**R-R34**) when a census found the step's own specification had enumerated its ten WRITERS and not one of the six surfaces that READ it. Carries **D35**. | container | -- | ticks with #102 |
| recurrence | R8 | -- | The DECOMPOSED parent of the ruled add-ons, split 2026-08-16 when measurement showed three of the four need a generated row's own `occurs_on`: `recurrence_engine.compute_due_date` dates a row from its PAY PERIOD and never from the occurrence, so a weekly row, an nth-weekday row and a shifted row would each carry a date the cadence never named. | container | -- | ticks with #38 |
| recurrence | R17 | pay_calendar:C5b (ticks it) | A generated row is the OCCURRENCE it answers, not the paycheck its money lands in: `occurs_on` is what every predicate asks about and what both unique indexes are keyed on. Closed **D57** -- 8 rows / `$1,482.93` from one pass, measured before and 0 after -- and retired the **D19** refusal with the paycheck key that forced it. | SHIPPED | `4e8b40b3` | -- |
| pay_calendar | C5b | recurrence:R17 (ticked by it) | The same commit under this arc's name: the skip predicate became occurrence-aware, a paycheck may owe one template more than once, and `refuse_unstorable_repeats` retired with the index re-key. Closed **P16**. | SHIPPED | `4e8b40b3` | -- |
| pay_calendar | C4-a-1 | -- | The balance seam's PLANNED tier stopped clamping a projected row against the STORED `end_date` and reads the span its paycheck DERIVES, through `PayCalendar.require_period`; the fold now carries the calendar it was clamped by and `_asset_fold.resolve` dropped its own. Closed **P38**. | SHIPPED | `8962e073` | -- |
| pay_calendar | C4-a-2 | -- | The reconcile panel came off BOTH columns and `utils.dates.attribution_date` was DELETED for `DerivedPeriod.attribution_day` (**R-PC31**); its own review then measured the first design's safety argument FALSE, so all THREE arms scope by the calendar's own period ids and `require_period` is unconstructible there. | SHIPPED | `82bd762c` | -- |
| pay_calendar | C4-a-3 | -- | The purchase-date warning came off the relationship and `DerivedPeriod.covers` LANDED (**R-PC31**), retiring the THREE sites that open-coded a period's span; `check_purchase_date_in_period` is DELETED and `entry_list_view` no longer takes a transaction. The five-argument fork was answered by DELETION: both entry maps left `_build_grid_row_data`, ending a copy the Plan tab discarded. `$0.00`. | SHIPPED | `a0fb14ba` | -- |
| pay_calendar | C4-a-4 | -- | The merchant-destination picker came off `txn.pay_period`: `destinations_for` takes the CALENDAR and no `owner_id`, and `PayCalendar.saved_by_id()`'s KEYS are its scan's scope while that SAME mapping answers each span, so a miss is unconstructible (**R-PC36**). `PurchaseDestination` carries the `DerivedPeriod` in place of three copies of its facts, DELETING a disable (**R-PC37**). `$0.00`. | SHIPPED | `f18a58af` | -- |
| pay_calendar | C4-a-5 | -- | The two LABEL readers came off the ORM row and the `PayPeriod.label` ACCESSOR is DELETED, so no screen can reach the stored `end_date` for a label; the COLUMN is `C4-c`'s. `require_period` takes ONE `FiledRow` (**R-PC39**) and `period_move_options` takes the CALENDAR. Opened **N-413**; amended **P70**, which this step made under-by-one. | SHIPPED | `95b2dc67` | -- |
| pay_calendar | C4-b-1 | -- | Make every test owner's pay calendar come from the doors that own it: every hand-built `PayPeriod(...)` that builds an ORDINARY owner (23 sites, 18 files) goes through `record_paydays`, the five calendar fixtures through `reset_pay_periods`, and `_drop_seed_user_bootstrap`'s 135 lines and raw `period_index` renumber go. Closes **N-392**; opens **P78**. | SHIPPED | `eb6597ae` | -- |
| pay_calendar | C4-b-2 | -- | Make an owner's recorded cadence a FOREIGN KEY: backfill a `budget.pay_schedule` row for every owner holding paydays, point `pay_periods.user_id` at it `ON DELETE CASCADE`, and DELETE `resolve_schedule`'s inferring fallback rather than leave a branch no state can reach. Closes **P8**, **P35**. | SHIPPED | `5db9f8a0` | -- |
| pay_calendar | C4-c | -- | The two derived columns are DROPPED with the three constraints that bounded them and the four surviving fences of section 1; `upgrade()` REPORTS a stored pair that disagrees with the paydays rather than discarding it in silence. Closed **P1**, **P4**, **P5**, **P9**, **P26**, **P27**, **P28**, **P33**, **P53**, **P70**; opened **P79**, **P80**. | SHIPPED | `c703e1c7` | -- |
| bank_import | X-ge | -- | Let a standing rule AUTO-APPLY at import for a NEW swipe line only (**R-GH**), **MOVES MONEY, OWN PR**: the line becomes a purchase in the rule's destination, dated by the bank and itemised on a receipt whose every item carries X-f6f's one-click undo, while every act that would modify a hand-made row keeps its tick. | SHIPPED | `6d3e3ca1` | -- |
| bank_import | X-ge-1 | -- | Make each matcher tier publish the refusals it used to swallow, so a pass reports three verdicts rather than two -- explained, unexplained, and DECLINED-with-a-reason -- and the auto-apply door withholds a line the search never positively finished looking at. | SHIPPED | `6d3e3ca1` | -- |
| balance | X-i3-b | -- | The three live accommodations for "under READ COMMITTED the two reads can differ" narrowed to the COMMAND each still describes, none deleted -- one is also an ownership scope and one pairs two different REQUESTS -- and the census re-taken, which found three sites the step never named and one claim `X-i3-a` had falsified. Opened **N-364**. | SHIPPED | `1feb0930` | -- |
| balance | X-i4 | -- | A read pass BINDS the account it values: `_memoize_once`, the ONE primitive creating `account.id`-keyed state on a pass, takes the account and refuses a foreign one, the cash fold joins the three loan derivations it memoizes, and the four mis-pairable readings take the assembled record. Closed **N-354**; opened **N-360**, **N-361**, **N-362**, **N-363**. | SHIPPED | `79a1730c` | -- |
| recurrence | R7d-c-1 | -- | The generate pass took the READ PASS it resolves in and the ROUTE became the layer that opens one: `GenerationSchedule` carries a `BalanceContext`, and the doors that create pay periods RECORD and return where they used to generate too (**R-R38**), settling the fork `pay_calendar:C11` states for this path. `$0.00` through all three generate doors; the caller census it took closed **D58**. | SHIPPED | `61d81c7f` | -- |
| recurrence | R16-b-1 | -- | The occurrence walk stopped TRUNCATING at the saved schedule's horizon: `PayCalendar.paychecks_from` composes `current_and_future_window` with a `projected_paychecks` continuation `axis_window` had been stepping a second copy of, so `occurrences(through=X)` answers 255 dates where it answered 62 and `_bounded` is again the only place a bound is applied. | SHIPPED | `1b818135` | -- |
| recurrence | R16-a | -- | The forward fold stopped charging a month of interest per payment RECORD: interest and escrow became ONE charge per accrual period and each payment allocates its cash on its own date, so 30 payments a fortnight apart no longer charge the same `$1,096.34` as 30 a month apart. `$0.00` on 776 production figures and 4,000 differential trials. Opened **D51**-**D55**. | SHIPPED | `e8baa3c0` | -- |
| recurrence | R7d-b | -- | The resolver a loan payment's closing bound comes from -- `loan_payment_window(template, ctx)` answering `ClosesOn` / `Indefinite` / `EMPTY`, the sum type a nullable date could not carry -- taking the DEFINITION rather than the loan (**R-R35**), so its SUBJECT is no longer tie-broken though its VALUE still is. Re-scoped **D47**; opened **D48**, **D49**, **D50**. | SHIPPED | `0462dc38` | -- |
| recurrence | R7d-a | -- | An installment NO ROW covers is priced by the loan's own recurring payment (**R-R33**) not the CONTRACT, so a payoff stopped depending on whether the rows were WRITTEN yet WHEREVER THEY AGREE WITH THEIR DEFINITION -- `2029-02-22` against `2030-02-22` through `regenerate_pay_periods` -- with **N-352** carrying the residue. | SHIPPED | `89cb0c1d` | -- |
| balance | X-i3-a | -- | A request is a QUERY or a COMMAND and its transaction says which: GET and HEAD run at REPEATABLE READ READ ONLY so a render is ONE snapshot and cannot write, every other method keeps READ COMMITTED because the reconciles' lock-then-reread depends on it, and the GET handlers that wrote either declare it in a `write_transaction` block or are deleted. Closed **N-353**; opened **N-358**, **N-359**. | SHIPPED | `765daebd` | -- |
| balance | X-au-c3 | -- | A settle RECORDS what moved instead of refreshing an amount: `settled_amount` + `settled_basis_id` + `settled_on` are one record the status seam alone writes, a revert releases the ASSERTION and KEEPS the fact, and both full-edit popovers correct a figure IN PLACE. Closed **N-241**, **N-242**, **N-257**, **N-259**, **N-265**, **N-282**, **N-298**; opened **N-301**-**N-304**. | SHIPPED | `3d1379d1` | -- |
| recurrence | R10-b | -- | A transfer regeneration MAINTAINS what it generated: an edit stopped destroying 99 transfers and 198 shadows to rewrite values already there, a note and a `$321.45` figure kept through a revert now survive one, and `update_transfer` learned to move a transfer's ACCOUNTS -- two of the six derived columns it could not write. Ruling **R-R32**. | SHIPPED | `ea776528` | -- |
| recurrence | R10-a | -- | A regeneration MAINTAINS the rows it already generated rather than destroying and rebuilding them, so a template edit can no longer take the purchases recorded against a part-spent envelope with it, and a row holding the owner's own records is retained and reported instead of rewritten. Closed **N-292**. | SHIPPED | `5fc13cdb` | -- |
| recurrence | R7c-c | -- | The closed pattern set DIED: six derived columns and the unwritten `budget.recurrence_month_anchors` dropped, `interval_n` re-pointed off the encoding so a Quarterly rule no longer reads as monthly, and the offer set re-based on the producer that refuses. Closed **D6**, **D32**, **D37**, **D38**, `pay_calendar:P11`; opened **D39**. | SHIPPED | `ee35bca7` | -- |
| recurrence | R7c | -- | The DECOMPOSED parent of THE CUTOVER as an expand / migrate / contract, ticked with R7c-c, its last leaf. | SHIPPED | `ee35bca7` | -- |
| pay_calendar | C2-f1 | -- | Three of `pay_period_service`'s six readers are DELETED across 12 call sites -- `get_overlapping_periods`, `get_next_period` and `get_current_and_future_periods` -- with two copies of one of them, `companion_service.get_previous_period` and `dashboard_pulse_service._next_paycheck_date`. Opened **P45**-**P49**. | SHIPPED | `792e3b21` | -- |
| balance | X-ad-a | -- | Registration ASKS for the most recent payday, the cadence and the horizon; the bootstrap payday is DELETED. Closed **N-123** (= `pay_calendar:P3`). | SHIPPED | `2a4eb477` | -- |
| pay_calendar | C1 | -- | The derivation exists and is proven equal to what is stored. Opened **P15**, **P16**. | SHIPPED | `f9d148fe` | -- |
| pay_calendar | C3 | -- | The writer writes paydays, forward-only: `pay_period_write` is the one place in `app/` that constructs or deletes a pay period. Two leaves, condensed into `historical/pay_calendar_as_built_2026-08-16.md`. Closed **P2**'s writer half, **P12**, **P13**, **P29**, **N-127**. | SHIPPED | `7e3fb33b` | balance:X-ad-a |
| pay_calendar | C5a | recurrence:R-F10 (ticked by it) | The gap machinery the derivation makes unconstructible is DELETED -- ticked at C2-b2 rather than after C4, because that leaf is what made its subject unreachable (developer 2026-08-11). The 430-shape baseline stayed byte-identical. | SHIPPED | `fe365de1` | -- |
| recurrence | R-F10 | pay_calendar:C5a (ticks it) | The gap machinery goes; the same commit as `pay_calendar:C5a`, under this arc's name. Closed **F-10**. | SHIPPED | `fe365de1` | -- |
| recurrence | R8-a | -- | The offer set stopped being gated on a derivation ruling R-R16 had deleted: `anchor_family` and its three families went, `authorable_cadences` derives from whether a row can be DATED and whether the placement can change it, and a year-scale cadence funded from a later paycheck became authorable. Closed **D20**; opened **D40**. | SHIPPED | `87e2c5b9` | -- |

## Cross-arc forks

**Two steps in different arcs that are competing remedies for ONE defect.** Whichever ships first
decides for both, so the gate REFUSES a tick on either until `ruled` NAMES one of the competing
remedies. A ruled fork's defect row is owned by the remedy that won, and the gate checks that too: a
ruling nobody re-points is a ruling that decided nothing.

**`pay_calendar:P3` = `balance:N-123` LEFT this table on 2026-08-10**, and how it left is the point:
it was ruled to `balance:X-ad` on 2026-08-09, that remedy SHIPPED as `X-ad-a` (`2a4eb477`), and the
defect row closed with it. A fork whose defect no longer exists binds nothing.

| defect | competing remedies | ruled |
|---|---|---|
