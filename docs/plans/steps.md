# The step order

**This is the single source of truth for WHAT TO DO NEXT.** Every step in every arc appears here
exactly once, and the order table below is sorted into EXECUTION ORDER. The next step is its first
row. You do not need to read anything else to find it.

**How to read a row.**

| column | what it holds |
|---|---|
| `arc` + `id` | The step's key. An `id` alone is not unique across the corpus, so cite both |
| `also` | The other names this same step is filed under. `C2`, `X-l` and `R-F12` are ONE commit |
| `what this step does` | One sentence. If you need more, the arc's own document specifies it |
| `order` | `#N` is its place in the sequence; `container` is a grouping, never work; `SHIPPED` is done |
| `commit` | The hash for a shipped step. The code it shipped is the record; prose about it is not |
| `starts` | `NOW` means every blocker has shipped. Otherwise the rank it waits for, then the blockers |

**Where a step's detail lives**, by its `arc`:

| arc | document and section |
|---|---|
| `balance` | `../audits/balance_architecture/README.md`, section 5 |
| `recurrence` | `implementation_plan_recurrence_redesign.md`, section 4 |
| `pay_calendar` | `implementation_plan_pay_calendar.md`, section 4 |
| `credit_card` | `implementation_plan_credit_card.md`, "The steps" |
| `bank_import` | `implementation_plan_bank_import.md`, "The steps" |
| `salary` | `implementation_plan_salary.md`, section 4 |

*A table in this file is found by its own HEADER, so one added for any other purpose joins no
registry: `tools/plan_gate` separated them by COLUMN COUNT until 2026-08-14, and a three-column
reference table read as four unruled forks. That was finding **N-234**, closed by `d8aed644`.*

**Anything under an `archive/` or `historical/` directory is a HISTORICAL RECORD and governs
nothing.** It may be cited for how something came to be; it may never be read as a live plan or as a
statement of the current state. The code as committed is the source of truth for what the app does.

## Working two steps at once

**Any row whose `starts` reads `NOW` can be started today, whatever its rank.** The rank is the
recommended sequence; `starts` is the hard constraint. That is how `X-f2-c2` and `C3` ran side by
side: both were unblocked and they shared no file.

**Two `NOW` rows are safe together when they are in different arcs or different phases.** Before
pairing two inside one arc, check that neither names a module the other deletes. A row marked
**MOVES MONEY** takes its own PR either way, so it is never the second lane.

**Lanes follow CHAINS, not arcs** (the coordinator's application of the 2026-09-15 tiers, below): a
lane takes the first `NOW` row of the highest-ranked outcome whose chain holds one it can work
without a file collision, else the first `NOW` reward row, else the first `NOW` upkeep row.

**The rank is a DECISION, not a derivation.** 103 of these steps are legal to start right now, so
the dependency graph alone cannot say which comes next; the sequence below follows the OUTCOME TIERS
ruled 2026-09-15 (next paragraph) and, within a tier, each arc's own stated sequencing -- the
balance README's ten blocks, and each plan's section 0.

**The order is OUTCOME-SCOPED, in three tiers (developer ruling, 2026-09-15).** An OUTCOME is a
thing the developer will experience in production, named by the step keys that deliver it; the
developer ruled these six, in this priority order, and the rulings that follow them the same day;
**O0, the bank import usable for matching, was ruled the same night, FIRST, when the production
import's first twenty minutes of matching stopped on a date the pane mislabelled**
(`bank_import:R-BI9`, **BI-498**), and was DELIVERED at `X-gz` on 2026-09-16, so its row has left
the table:

| outcome | scope |
|---|---|
| O1 the flip | balance:X-f3c-5 / balance:X-bj-2 |
| O2 the credit card arc in production | credit_card:CC-1 / credit_card:CC-2 / credit_card:CC-3 / credit_card:CC-3i / credit_card:CC-4 / credit_card:CC-5 / credit_card:CC-6 / credit_card:CC-7 / credit_card:CC-7o / credit_card:CC-8 / credit_card:CC-9 / credit_card:CC-10 / credit_card:CC-11 |
| O3 paycheck earnings lines | salary:R18 / salary:S11 |
| O4 the SimpleFIN daily feed | bank_import:X-f6b |
| O5 the week and nth-weekday cadences | recurrence:R5 / recurrence:R8-b / recurrence:R8-c / recurrence:R8-d |
| O6 the loan event stream | recurrence:R20 / recurrence:R16-c / recurrence:R16-d / recurrence:R16-e / recurrence:R16-f |

**The PATH tier is DERIVED and graded**: the transitive closure of the `starts` waits of those
scopes -- every open step an outcome cannot ship without -- and it is the table's LEADING BLOCK;
`tools/plan_gate/_order.py`'s tier arm recomputes it from the graph as it stands on every commit
that touches this file, the way the `starts` column is reconciled. A row on no outcome's path is
**REWARD or UPKEEP by the developer's GENEROUS test**: above the horizon when a figure he reads
moves to the correct one, a failure he can hit stops (a 500, a deadlock, a lost definition), a door,
screen or control he would use appears, or it is structural financial-correctness work that makes a
wrong state unrepresentable though no figure moves today (`X-au-m`, `X-bp`, `X-au-l`, `X-bq` and
their kind); structural work that moves no figure -- pure moves, splits, censuses, checkers,
tooling, and the rule-14 deletions that leave every state representable -- is upkeep.
**The horizon opens at `balance:X-cg`.** It is a KEY, never a count, and the arm holds it to an open
ranked row outside the path. Two rulings of the same day ride as rows in `rulings.md`:
`credit_card:R-CC13` (the card rows' blanket wait on `balance:X-f4` is re-derived per leaf; the
trace landed 2026-09-18 with `R-CC14` and `R-CC15`) and `bank_import:R-BI8` (`X-f6b` is an outcome,
superseding `R-JL`'s placement; `X-gl-1`'s wait on `credit_card:CC5b` is DELETED). The rewards leaf
`CC-9` (`CC5a` / `CC5b` until the 2026-09-18 re-mint) is ruled IN O2 and AT THE PATH'S TAIL, after
every other outcome's chain; `X-f3c-5` WAITS FOR `balance:X-bj-1`, the wait ruling `R-JN` implied
and the graph had never recorded.

**How this document APPLIES it, which is not part of the ruling.** Within the path the rows run
OUTCOME BY OUTCOME in the ruled priority, each outcome's chain in dependency order with its `NOW`
rows first; within the reward and upkeep tiers the rows keep each arc's own stated sequencing, as
before. The coordinator read the test's upkeep side to include test and dev infrastructure,
performance-only work, `$0.00`-and-unreachable fixes and display polish; put `S5` above the horizon
under the second arm (its refusals answer nowhere today) rather than below it as display, and `S6`
under the same arm (a stale rail's Save moves a raise with no warning, **SAL-557**); and read the
ownership superkeys `C13-c` and `X-bc` as the fourth arm, since a cross-owner row in a money table
is a wrong state made unrepresentable. A row below the horizon rises only when a path or reward row
names it in a `starts` wait, which the rank arm already enforces. The prior standing priority --
bank_import to production, then the card (developer 2026-08-19, re-ruled 2026-09-02) -- is
DISCHARGED: its scope `X-gj-4b`, `X-gj-4c` and `X-gi` shipped by 2026-09-12
(`historical/bank_import_x_gj_as_built_2026-09-04.md` holds the first two) and O2 carries its card
half forward; the two placements later ruled under it, the earnings-lines chain directly after that
scope (2026-09-02) and the six open bank_import rows below every other arc's `NOW` row (2026-09-12),
are superseded by the tiers.

**The `starts` column is DERIVED from the blocker keys beside it and the gate reconciles the two**,
so a rank can never contradict a real dependency and a stale `NOW` cannot survive a commit.

**326 steps, 176 open.** The dependency graph holds 88 edges over 72 rows.

## The order

| arc | id | also | what this step does | order | commit | starts |
| --- | --- | --- | --- | --- | --- | --- |
| balance | X-bi-6-3 | -- | Give the ledger shape C (**R-BAL45**): a Transfers-in-transit clearing account per owner, one posting per movement on its own day, the loan probes re-keyed onto the loan-side movement, and a data migration re-booking the 19 settled transfers (38 movements) graded ledger-net per (real account, day) byte-identical; it **RE-BOOKS THE LEDGER**, so its own PR and its own release. | #1 | -- | NOW / credit_card:CC-5-1 (shipped; R-BAL86's own words, "after CC-5-1 merges") |
| balance | X-bi-6-4 | -- | Re-parent the RECORD half onto `budget.transfers` after two financial forks (the link's shape; one status, two clearing days): `transaction_id` nullable under an exactly-one-parent CHECK, 38 movements backfilled, the seam GAINING a Transfer arm, the mirror and five restore clauses gone, carry-forward and recurrence walking transfers; a migration. Closes **N-304**, **BAL-503**, **BAL-528**. | #2 | -- | NOW / credit_card:CC-5-1 (shipped; R-BAL86's own words, "after CC-5-1") |
| credit_card | CC-5-4 | -- | Offer the payment MOVEMENT on the CARD's screen as the candidate for the bill it settled, matched as a movement member and dated through the bill's own door (`statement_match` candidates, moving, accept, the accepted register, undo; the card's reconcile panel's purchases arm), so a card-settled bill reconciles on the card's statement, never checking's (**R-CC40**, half 2); before any card import. | #3 | -- | NOW / credit_card:CC-5-3 (shipped; the movement it offers and the readers it re-keys) |
| credit_card | CC-5-5 | -- | Fix the five net-worth sign sites (`_liability.py`'s spliced owed series and `liability_owed_at_dates`, `_net_worth.py`'s hero and trend, `_debt_line.py`'s debt total) as ONE subject so a card in credit reads as the issuer owing and the trend's index 0 still reconciles to the hero (**R-CC41**); `$0.00` on production. Closes **CC-354**. | #4 | -- | NOW |
| credit_card | CC-6 | -- | Add the payment (**R-CC18**, **R-CC22**): `card_payment_settings` with four modes and a unique key over the card, the transfer setup flow, the CARD amount rule pricing at the end of the day before the row's day less what is not yet inside that balance (floor zero), a mode switch as a recurrence rewrite, the typed-over row as a partial payment and the underpayment notice (3.5). Closes **N-311**. | #5 | -- | after #4 / credit_card:CC-3 (shipped) / credit_card:CC-5 |
| credit_card | CC-7 | -- | Cut the cheat over (**R-CC17**, **R-CC20**, **R-CC21**): every frozen pair converts by the total rule, every envelope by the envelope clause, a still-live pair is reported; `is_credit`, the mark/unmark doors, the `Credit` status and both credit-workflow modules go (design 3.10). **MOVES MONEY** on the budget clock, `$0.00` on checking's fold. Closes **CC-352**, **N-337**, **N-350**, **N-351**. | #6 | -- | after #5 / credit_card:CC-5 / credit_card:CC-6 |
| credit_card | CC-11 | -- | Build the card cockpit and the grid's card affordances through the design loop (design 3.9), each screen held to the design language and verified on the dev clone; the far leg of a payment between two non-balance members drawn from its FROM side (**R-CC38**). Closes **CC-355**. | #7 | -- | after #5 / credit_card:CC-6 |
| salary | S11 | -- | Make a calibration the STUB TRANSCRIBED line by line and dated (**R-SAL41**): a same-shape paycheck reproduces its stub exactly, a differing shape takes the nearest stub plus the statutory marginal rates for the lines that differ, a line's true net is read off two stubs; absorbs `S1` (dated, never destroyed, the door ADDS); the rate columns go. Closes **SAL-564**, **N-441**, **N-535**, **N-530**. | #8 | -- | NOW |
| bank_import | X-f6b-2 | -- | Add the SimpleFIN adapter and feed: `budget.bank_feeds` (the per-owner access URL under the renamed field key) with the in-app claim and account mapping, one sync function (posted lines only, the ledger balance as the claim, declared windows, the rule pass) behind the nightly door and Sync now, a match carrying the reviewed day (**R-BI12**..**R-BI14**). Closes **N-338**, **BI-499**, **BI-503**. | #9 | -- | NOW / bank_import:X-f6b-1 (shipped) / bank_import:X-f6b-1b (shipped) |
| balance | X-bi-6-2 | -- | Offer a transfer LEG keyed (transfer, account) wherever statement match and the reconcile panel offer a shadow row today, settling through `settle_transfer` (**R-BAL86**); `$0.00` on the balance baseline. | #10 | -- | after #9 / bank_import:X-f6b-2 |
| balance | X-bi-6-5 | -- | Delete the 354 shadow rows and `transactions.transfer_id` with its three constraints by migration; the remaining branches (census 11 code lines `transfer_id is not None` in `app/**/*.py`), the exclusions, guards and Jinja branches its specification censuses die with them; the leg twins fold; the Transfer Invariants rewritten; byte-identical. Closes **BAL-475**, **BAL-526**, **BAL-527**. | #11 | -- | after #10 / balance:X-bi-6-2 / balance:X-bi-6-3 / balance:X-bi-6-4 |
| balance | X-bk | -- | Perform the ONE-TIME manual reconcile of the imported bank history against the app's own rows, through the app's doors and into the post-restructure shape so the whole history keeps its plan-versus-actual comparison; an OPERATOR act and a rehearsed runbook rather than a code change, and it **MOVES MONEY**. | #12 | -- | after #11 / balance:X-bi |
| balance | X-f3c-5 | -- | THE FLIP: the assertion stops resetting a PLAIN account, `balance(T)` becomes opening equity plus the sum of postings, and the reconcile reverses the 162 `account_trueup` entries that stand. **MOVES MONEY, OWN PR, NO BACKLOG**, and gated on the operator import **N-368** that no `blocked by` cell can hold. Closes **N-172**, **N-174**, **N-285**, **N-290**, **N-307**, **N-308**. | #13 | -- | after #12 / balance:X-bk / balance:X-bj-1b (shipped; R-JN: the reset reads the relation until the flip deletes it, and X-bj-2 rides the flip) |
| balance | X-bj-2 | -- | Land the clause the relation was built for, riding the flip (**R-JN**): a level moves no balance and yields `discrepancy = observed - computed`, so the bank's closings are observations and neither assertions that reset nor restatements -- the answer **N-314** left the ledger on. | #14 | -- | after #13 / balance:X-f3c-5 / balance:X-bj-1b |
| bank_import | X-f6b-3 | -- | Ship the launcher `task.sh` (the entrypoint's secret loader shared, `DATABASE_URL_APP`), `scripts/feed_sync.py`, the `deploy/systemd/` timer and service (**R-BI11**), the runbook's rows, and the statements page's feed panel (connect, mapped accounts, Sync now, disconnect, last-synced), with the imports table and merchant section paged. Closes **N-326**, **N-330**, **BI-502**. | #15 | -- | after #9 / bank_import:X-f6b-2 |
| credit_card | CC-7o | -- | Perform the RELEASE runbook (**R-CC21**; `salary:R18-d`'s shape under **R-HJ**): the card paid in full with every payback marked paid and no new charges, the release, the card created through the account door with its books opened on the payment day at that day's balance verbatim, the payment definition set up, and the first cycle reconciled on the feed; no per-row door act (design 3.10). | #16 | -- | after #15 / credit_card:CC-7 / bank_import:X-f6b (the feed reconciles the first cycle) |
| recurrence | R5 | -- | Finish the date split `R17` began: add `due_on` (the installment) beside the `occurs_on` that step created and DELETE `compute_due_date`, which dates a row from its PERIOD -- so two occurrences in one paycheck share a due date (**D18**). The index re-key and the `RecurrenceCadenceUnsupported` retirement it once carried SHIPPED at `R17`. A value-splitting migration, own PR. Carries **REC-518**. | #17 | -- | NOW / balance:X-bi-4 (shipped; R-R52: the X-f4 gate was merge hygiene, and the fold re-pointing to movements is the real dependency) |
| recurrence | R8-b | -- | Make the WEEK unit authorable -- the cadence the closed pattern set could never express -- by deleting `has_row_date_coordinate`, which R5's `occurs_on` makes unnecessary rather than merely satisfied. | #18 | -- | after #17 / recurrence:R5 |
| recurrence | R8-c | -- | Give a rule an nth-weekday coordinate ("the third Tuesday") as an EXCLUSIVE ARC of columns on `budget.recurrence_rules` under one CHECK, dropping the unwritten `budget.recurrence_weekday_anchors` table, and make `describe`'s coordinate dispatch on the coordinate KIND rather than on "WEEK or else". Closes **D29**. | #19 | -- | after #17 / recurrence:R5 |
| recurrence | R8-d | -- | Add the business-day shift to the cash date alone, consuming the shared business-day module and computed federal holiday set `pay_calendar:C14-a` builds, which needs `occurs_on` to exist because today's generated row is dated from its pay period and never from the occurrence. | #20 | -- | after #17 / recurrence:R5 / pay_calendar:C14-a (shipped) (the shared business-day module) |
| recurrence | R16-c-2 | -- | Charge every contractual installment from origination in the ONE stream (`first_installment_date` advanced by `_advance_one_month`), an assertion clearing what stands before it (**R-R72** (2)); delete the seed-slot partition and every `(year, month)` key in both tiers (**R-R89**); D53's past half. **MOVES POSTED MONEY, OWN PR**, postings re-synced in its migration. Closes **D55**. | #21 | -- | NOW / recurrence:R16-c-1 (shipped) |
| recurrence | R16-d | -- | Make the interest ACCRUAL CONVENTION a value on the loan -- a `ref` table, a column, and one total `accrued_interest(balance, rate, from, to, convention)` of which today's monthly formula is one member and `DAILY_ACTUAL_365` (`credit_card:CC-3i`) another -- backfilling every loan to `MONTHLY_1_12` so the migration moves `$0.00`, and retiring `debt_strategy_service`'s inline copy. Closes **D52**. | #22 | -- | after #21 / recurrence:R16-c |
| credit_card | CC-3i | -- | Price the finance charge as `recurrence:R16-d`'s ONE `accrued_interest` under the `DAILY_ACTUAL_365` member that step gains, summed per constant-balance segment of the closed-open cycle with purchases joining the path on grace loss (design 3.6; **R-CC19**: the card's interest waits its turn in O6). | #23 | -- | after #22 / recurrence:R16-d (gains the `DAILY_ACTUAL_365` member and keeps its O6 rank, R-CC19) / credit_card:CC-3 (shipped) |
| credit_card | CC-8 | -- | Add the finance-charge definition the card owns (`credit_card_params.finance_charge_template_id`) and its rule, monthly on the statement close and `$0.00` when grace held; `template_id` is the pricing link **R-IY** names, so no fourth FK (design 3.6). Closes **N-264**. | #24 | -- | after #23 / credit_card:CC-3i / credit_card:CC-6 |
| recurrence | R16-e | -- | Delete walk 3 over a loan, `rate_period_engine.replay_schedule`, whose balance has one consumption site every production read bypasses and whose two surviving calendar scalars need no walk (**R-R53**). | #25 | -- | after #21 / recurrence:R16-c |
| recurrence | R16-f | -- | Re-express walk 4, `amortization_engine.project_forward`, as the one replay run with CONTRACTUAL cash, so the contractual, the planned and the settled views are one fold with three cash sources (**R-R53**). **MOVES MONEY, OWN PR.** Closes **D60**. Carries **REC-517**. Shrunk by R7d-g-3: the loan page reads the seam's plan, not the composer's committed slice, and `monthly_override` is gone. | #26 | -- | after #25 / recurrence:R16-e |
| credit_card | CC-9 | -- | Accrue rewards as a derived figure over settled purchases minus redemptions and add manual redemptions with the auto-redeem threshold as rows, with no `system_origin_id` (design 3.7). | #27 | -- | after #5 / credit_card:CC-6 (ruled the tail by the 2026-09-15 order) |
| recurrence | R21 | -- | Make the occurrence walk's placement for PRICING run BACKWARD for an owner who has stated `history_opens_on`, reading the calendar as `balance:X-bh-2` made it run in both directions (generation still needs a saved period), so a payroll line's rule reaches backdated paydays and reproduces the retired ordinal reading below the record (**R-R87**). Closes **REC-527**. | #28 | -- | NOW |
| salary | S9 | -- | Make a BLANK start on a payroll line mean "since my paychecks began", STORED AS BLANK and resolved at read time to the owner's true opening, a typed start pinned; every payroll line becomes a rule and the no-rule shape goes; its migration gives every rule-less line a blank rule and blanks the starts R15-b derived, `$0.00` while no owner has stated a history (**R-SAL39**). Closes **SAL-562**. | #29 | -- | after #28 / recurrence:R21 |
| pay_calendar | C18 | -- | Let a payday be recorded BEFORE the schedule's earliest one, and stop a period that precedes the books generating rows into it (**R-PC62**): narrow `_reject_backward_payday` to what its docstring claims -- a payday strictly INSIDE an existing paycheck, which **C6** still owns -- and give the boundary an arm over generated rows. Closes **PC-499**, **PC-500**. | #30 | -- | NOW / pay_calendar:C14-e-3 (shipped) |
| balance | X-f3c-2b-2c | -- | Perform the account-10 repair as a rehearsed OPERATOR runbook (**R-HJ**, **R-HL**, **R-HM**, **R-BAL3**): both accounts' books open 2026-03-25 at their banks' own closes, 2026-03-26's four bank lines are RECORDED, the twin is zeroed, five transfers move onto the bank's days and five dividends are recorded. Act 4b is DELETED. **MOVES MONEY.** Closes **BAL-468**, **N-275**; carries **BAL-467**. | #31 | -- | after #30 / pay_calendar:C18 |
| balance | X-f3c-2b-3 | -- | Bound the ASSERTION door at its account's books opening -- both write doors and a deferrable constraint trigger, the shape **X-f3c-2b-1** gave the movement side -- so a balance cannot be declared for a day the books did not exist on. It legalises NOTHING: the bound is STRICTLY before `opened_on` and no row sits below its books. Closes **N-400**. | #32 | -- | after #13 / balance:X-f3c-5 |
| balance | X-f3c-4 | -- | Let an owner ACCEPT that difference as an uncategorized transaction dated on the latest assertion's own day, its amount DERIVED from the postings other than itself until they categorise it, offered only where an imported statement reconciles the span (**R-FN**, **R-GY**); it **MOVES MONEY**, taking out of equity a figure it must RE-DERIVE rather than quote. Closes **N-171**. | #33 | -- | after #13 / balance:X-f3c-5 |
| balance | X-f4 | -- | Delete what the cutover orphans: `ReconciledThrough`'s coverage rule, `_anchors.py`, the correction machinery and the R-I seed compensator, a set X-f3a has already narrowed. Closes **N-176**, **N-218**, **N-161**, **N-170**. | #34 | -- | after #33 / balance:X-f3c |
| balance | X-ci-2 | -- | Delete the ad-hoc transfer door (`POST /transfers/ad-hoc`, its schema and integrity handlers), move the one-time producer from `routes/transfers/_instances` into a service, and re-point the suite: two helpers (census 107 code lines `(^\|[^.\w])create_transfer\(` in `tests/**/*.py`) (census 136 code lines `create_settled_transfer\(` in `tests/**/*.py`) and every direct site. Closes **BAL-529**. | #35 | -- | NOW / balance:X-ci-1 (shipped) |
| balance | X-ci-3 | -- | The cutover migration: `transfer_template_id` NOT NULL and RESTRICT, `uq_transfers_adhoc_dedupe` and `ck_transfers_adhoc_owns_amount` dropped, `due_date` NOT NULL in place of its CHECK, the 2-row `occurs_on` backfill (**R-BAL94**) with 409 re-attached at `$0.00`, `_stated_amount`'s `template is None` refusal deleted, `TransferSpec` requiring a definition; graded byte-identical, moves no money. | #36 | -- | after #35 / balance:X-ci-2 |
| salary | S4 | -- | Make a payroll deduction's `annual_cap` a DATED figure that rises by year the way `tax_config_service` prices an unconfigured year from the latest configured one, so a statutory limit is no longer modelled as fixed over a long horizon; needs a RULING on the shape first. Closes **N-540**. | #37 | -- | NOW |
| salary | S5 | -- | Let each region of the retirement readiness page render its own refusals -- the assumed-return, months and contribution inputs and a stale or foreign raise id answer nowhere today -- word the SWR refusal in the percent domain the box shows, and stop the deduction form's refusal swapping a page body into its section. Display-only, `$0.00`. Closes **SAL-550**, **SAL-551**, **SAL-558**.  | #38 | -- | NOW |
| salary | S6 | -- | Make the `/retirement` rail's raise set the set the page projects for a multi-profile owner, refuse a Save whose version pin does not cover every row the form writes (a stale rail's raise row; a cadence-only edit bumps no version), and hand the regeneration one read pass per profile (**R-SAL24**); needs a RULING first. `$0.00`. Closes **SAL-552**, **SAL-553**, **SAL-554**, **SAL-557**.  | #39 | -- | NOW |
| salary | S8 | -- | Make the retirement gap's final-year figure read the paycheck the engine prices instead of BASE times a net-over-gross ratio, which mixes two figures once gross carries taxable earning lines and net after-tax ones (**R-SAL38**); `compute_gap_net_biweekly` and the take-home-rate chip share it, `$1.41` a paycheck on a `$3,000` base with a `$45` taxable line. Closes **SAL-561**. | #40 | -- | NOW / salary:R18-b (shipped) |
| pay_calendar | C20-b | -- | Give the month-day clamp ONE producer (`app.utils.dates.clamped_day`) where `rate_period_engine`, `salary_cockpit_service` and `recurrence/_row_date.py` still spell it, route `_pay_periods_manage.html`'s hand-written `min` / `max` through the macro, and give the manage card's macro-rendered inputs distinct ids per form. `$0.00`. Closes **PC-516**, **PC-517**, **PC-518**. | #41 | -- | NOW / pay_calendar:C20-a (shipped) |
| balance | X-bn | -- | Take the per-user write lock FIRST at every write-service entry -- the status seam, `update_transfer`, the delete and restore paths, `create_account`, and `statement_match.apply_reviewed` ABOVE its `lock_lines` read (`X-gi-5`) -- so the two REPRODUCED advisory-versus-row-lock deadlocks stop aborting a money route with a 500. Closes **N-193**, **N-202**. | #42 | -- | NOW |
| balance | X-au-m | -- | Stop an owner-priced pair storing ONE figure twice -- Transfer Invariant 3's amount clause, the half `X-au-g-2c-2` did not reach (**R-JA**): the TAKE arm states the figure on the parent AND each leg. Ruled **R-JM**: a leg reads its PARENT and the parent decides owner or contract, so the `$174.10` derive-mode exception is not a representable state. | #43 | -- | NOW / balance:X-au-f (shipped) |
| balance | X-f3a-2 | -- | Record that a STATEMENT was walked line by line, so a line it did not show is NOT CLEARED rather than unknown, and widen the offer set to every uncleared line including the already-settled ones, which **MOVES MONEY**; it closes **N-273** and **N-289**. | #44 | -- | after #33 / balance:X-f3c |
| recurrence | R6 | -- | Delete `loan_params.payment_day` and collapse eight producers of "when is this installment due" into one `loan_installment_date` accessor. Kills **D4**; needs its own review pass. | #45 | -- | after #17 / recurrence:R5 (it READS `due_on`; "ships WITH balance:X-an" was unsatisfiable -- see section 0) |
| balance | X-k | -- | Reconcile `RecurrenceRule.end_date` against what was actually generated, and give the write door a consistent batch contract. Closes **N-18**, **N-19**, **N-23**, **N-24**. | #46 | -- | after #17 / recurrence:R5 (R5 deletes `compute_due_date` and re-keys the generation index; rebasing this over that is the cheaper direction) |
| balance | X-au-g-2c-3b-3 | -- | Stop the ENGINE FEED allocating at all: `prepare_payments_for_engine` passes the CASH and `project_forward` charges the month's escrow, which DELETES the floor -- a SECOND allocation rule that reports a short payment as exactly on schedule -- and orphans `LoanContext.contractual_pi` and `compute_contractual_pi` with it. **MOVES MONEY, OWN PR.** Closes **N-409**. | #47 | -- | NOW / balance:X-au-g-2c-3b-2 (shipped) |
| balance | X-bq | -- | Make the flag's WRITE SET match its MEANING. `is_override` says *this row is the OWNER's, not the rule's* (**R-JR**), but `due_date`, `name` and `category_id` are DERIVED fields a popover edits without raising it, and the column still records a re-price and a move indistinguishably -- which **N-238** needs represented before the flag can go. Carries **N-238**, **F-11**, **N-245**, **N-246**. | #48 | -- | NOW / balance:X-au-h (shipped) |
| pay_calendar | C8 | -- | Give the forward forecast cadence its own control, separating it from the payday forms it is currently welded onto. Closes **P30**, **P47** (**R-PC50**). | #49 | -- | NOW / pay_calendar:C4 (shipped) |
| pay_calendar | C9 | -- | Project the modelled fold's CONTRIBUTION tier past the pay-period horizon on the same axis the accrual tier already runs on, superseding balance ruling **R-AG**, which let the half model stand before a total calendar existed. **MOVES MONEY.** Closes **P7** and **N-394**, the CASH tier joining the same projection; carries **P42**, **P44** and **P50**. | #50 | -- | NOW |
| pay_calendar | C6 | -- | Let a payday be inserted mid-schedule, refusing only where `classify_period_lock` says the split period is locked. Starts with the two rulings section 3 names. Closes **P10**, **P46** (**R-PC49**). | #51 | -- | NOW / pay_calendar:C4 (shipped) / recurrence:R7c (shipped) |
| pay_calendar | C13-c | -- | Hold six more tables' rows to ONE owner with a superkey and a composite foreign key each, in one migration -- transfers, transaction templates, ledger accounts, savings goals, statement imports, journal entries -- deleting integrity check DC-09 rather than joining it (**R-PC48**). Closes **P83**. | #52 | -- | NOW / pay_calendar:C13-b (shipped) |
| pay_calendar | C16 | -- | Rule and then fix the TWO attribution rules `journal_entries.pay_period_id` carries -- the BUDGET clock for the 332 source-linked entries and the CASH-DATE clock for the 218 anchor corrections -- which `ledger_report_service/_income_statement` sums into one per-period figure with nothing marking the difference. Closes **N-491**. | #53 | -- | NOW / pay_calendar:C7 (shipped) |
| balance | X-bp | -- | Delete `default_amount` from BOTH template tables (**R-IY**, **R-BAL16**) and re-point every site onto the series -- or, for the two kinds `owns_its_amount` refuses one, onto the live producer that already answers -- Python (census 28 code lines `default_amount` in `app/**/*.py`) and Jinja (census 7 lines `default_amount` in `app/templates/**/*.html`). Closes **N-446**, **N-450**. | #54 | -- | NOW / balance:X-au-e (shipped) |
| balance | X-ad-b | -- | Stop the rolling top-up manufacturing history: an automatic writer creates nothing on a lapsed schedule, and the lapse is surfaced instead. Closes **N-124**. | #55 | -- | NOW |
| balance | X-x1 | -- | Build the ONE answer to "no pay period covers this date": `PayCalendarGapError`, `require_current_period` / `covers`, one handler and its repair page, taking the grid's two pre-checks as its first callers. Carries **P40**, **P81** (**R-PC51**). | #56 | -- | NOW |
| balance | X-x2 | -- | Move the branches that publish a figure the app did not compute onto the raising accessor: the fabricated $0.00 in four producers, and `build_trend_periods`' `current_index = 0` into an empty list. | #57 | -- | after #56 / balance:X-x1 |
| balance | X-aj2 | -- | Make the status write door STRUCTURAL and DELETE the W9907 checker, ruling what a row may be BORN as. Closes **N-149**, **N-151**, **N-185**, **N-188**. | #58 | -- | NOW |
| balance | X-ak | -- | Rule the stored transfer-to-shadow copy first, then unify the THREE mirror implementations that already disagree. Closes **N-148**, **N-150**, **N-152**, **N-156**, **N-159**, **N-170**. | #59 | -- | after #58 / balance:X-aj |
| balance | X-ai-a | -- | Build the cash re-derive VERB from R-DV's sentence, with the union source set, the returned touched pairs and the per-account advisory lock in it from the start. Carries **N-162**, **N-165**, **N-166**. | #60 | -- | NOW |
| balance | X-ai-b | -- | Build the trigger: the commit-boundary grader, drained from the registry the writers populate. | #61 | -- | NOW |
| balance | X-ai-c | -- | Move the loan side onto the same re-derive verb. | #62 | -- | NOW |
| balance | X-ai-g | -- | Classify each of the 20 bulk-statement sites as unable to touch a posted row, routed through a writer, or named in the one docstring that states what the grader cannot see. Closes **N-163**. | #63 | -- | NOW |
| balance | X-ai-s | -- | Migrate `journal_entries` to an EXCLUSIVE ARC of typed FKs with an AT-MOST-ONE check, plus the reversal linkage. Held until the cutover, which deletes the correction family it would buy attribution for. Closes **N-167**. | #64 | -- | after #33 / balance:X-f3c |
| balance | X-d | -- | Make the posted account ledger a CHECKED PROJECTION: the posting writer consumes the walk instead of its own, with a per-visible-date assert. Carries **N-135**. | #65 | -- | after #64 / balance:X-ai (N-155 parked this step and the assert's placement is the restructure's) |
| balance | X-i1 | -- | Give `BalanceContext` the THREE remaining inputs of the tier the loan derivations already have -- the contribution feed, the standing extra, the contractual schedule the fourth having been deleted at `X-au-d` (census 0 lines `live_amount_overrides` in `app/**/*.py`) -- as a method beside `loan_walk` rather than through `_memoize_once`, additive and byte-identical. Shaped by **R-SAL27**. | #66 | -- | NOW |
| balance | X-i2 | -- | Hand each memoized loader `ctx.as_of` and `ctx.scenario`, so one read pass has one clock. **MOVES MONEY** ($3,631.74 today against $3,722.53 at a 2027 read). Closes **FU-3**, **N-14**, **N-56**, **N-72**, **N-89**, **N-91**, **N-92**, **N-93**, **N-115**. | #67 | -- | after #66 / balance:X-i1 |
| balance | X-j | -- | Rule which producer answers "what is this account worth" for a given surface, or render the row that explains the difference. Closes **N-87**, **N-90**, and **N-83**'s display half. Carries **P41** (**R-JP**). | #68 | -- | after #67 / balance:X-i2 (X-j moves three surfaces onto the modelled view whose contribution load X-i2 fixes, so the other order ships a regression and then removes it) |
| balance | X-m | -- | Make `growth_engine.project_balance` take its AXIS rather than boundaries its caller must compute to match; a caller that gets it wrong costs $1,000.00 of annual-limit room per period. Closes **N-86**. | #69 | -- | NOW |
| balance | X-n | -- | Stop `_redistribute_to_distinct_months` OVERWRITING the real installment a payment satisfies when it shifts a colliding due date. Closes **N-36**. | #70 | -- | NOW |
| balance | X-ab | -- | Give the posting path ONE asset-vs-liability rule instead of asking X-z's question a second time, and decide what a re-class does to accounts already carrying postings. Closes **N-122**. | #71 | -- | NOW |
| bank_import | X-gg | -- | Run the ENVELOPE-SEMANTICS design loop with the developer (**R-GK**): how an envelope fills from rule-assigned swipes and stays open through its period, what closes it when statement coverage completes, what carry-forward then means, and which row identity the grid renders (the same-name double-render the review measured) -- the loop mints the build steps rather than building. | #72 | -- | after #4 / credit_card:CC-5 (the loop designs over the card-tender entry shape CC-5 builds, a movement on the card; designing over the payback shape the card arc deletes is what withdrew balance:X-au-i) |
| balance | X-au-l | -- | DELETE `amount_source_id` (**R-IY**, **R-IZ**): the relation is derivable from the pricing link the row carries, so the column, `ref.amount_sources`, `AmountSourceEnum` and its reverse map go and `amount_rule` reads the LINK -- ONE walk. Both CHECKs become *a figure OR exactly one pricing link*, making **N-440** unrepresentable. A migration; moves no money. Closes **N-440**. | #73 | -- | NOW / balance:X-au-d (shipped) / balance:X-au-e (shipped) / balance:X-au-f (shipped) |
| bank_import | X-gl-1 | -- | Build `budget.line_dispositions` with its migration, its backfill from `budget.statement_matches` and `budget.statement_line_skips` and every reader repointed, so `undisposed()` becomes one anti-join and `_resolve.load_lines`' keyword-only `for_write` refusal is kept or its removal justified. | #74 | -- | NOW |
| bank_import | X-hc | -- | Make a row an accepted act names the BANK's: every door that would move its settle day, figure, status or existence refuses while the act stands, the members' row-side keys become NO ACTION like the line-side key, and `_still_holds`, the fold and R-GX(c)'s whatever-its-age clause are deleted (**R-BI18**). Closes **N-372**. | #75 | -- | NOW / balance:X-bi-4 (shipped) |
| bank_import | X-gl-2 | -- | Add the `AFTER DELETE` trigger that removes a disposition whose act no longer names an app row and delete `_candidates.act_still_names_a_row` with most of `app/services/match_withdrawal`, without which `X-gl-1`'s UNIQUE key is not earned because a stale MATCH disposition would block a later SKIP on a genuinely unanswered line (FORK 2, UNRULED; **R-BI18** adds a third option: neither). | #76 | -- | after #75 / bank_import:X-gl-1 / bank_import:X-hc |
| bank_import | X-gl-3 | -- | Give `budget.merchant_rules` a nullable DISPOSITION beside a separate REFUSAL flag so *never a purchase* leaves the answer set and `RuleAnswer` is replaced by the verb set, the disposition including SKIP with its `filed_total` exclusion (**R-JZ**), leaving FORK 3 -- whether *never a purchase* migrates or is re-stated per merchant -- UNRULED. | #77 | -- | after #74 / bank_import:X-gl-1 |
| bank_import | X-gl-4 | -- | Replace `ReviewedBatch`'s single value and its `__post_init__` policing with two batch types, a ticked pass carrying all four act lists and a rule pass carrying only the act classes **R-GH** consents to, so the maintained contract deletes whole. | #78 | -- | after #77 / bank_import:X-gl-3 |
| bank_import | X-gl-5 | -- | Rebuild the merchant control on the four verbs, extend the settled tabs' authorship split to SKIP so a rule-authored skip renders under *Skipped by your rule* (**R-JZ**), and delete `AddAct` as `LinePipeline`'s second spelling. | #79 | -- | after #77 / bank_import:X-gl-3 |
| bank_import | X-f6g | -- | Stop the reconcile panel offering an account whose statements the owner imports, reading `statement_import.covered_runs` -- the one definition of statement coverage (**R-BAL53**) -- rather than deciding it here (**R-GD(d)**). | #80 | -- | NOW |
| balance | X-p | -- | Put the analytics calendar's day chips and its balance line on one clock, or render the row that explains the gap. Closes **N-58**, **N-97**. | #81 | -- | after #44 / balance:X-f (the import shrinks the date noise at its source, so ruling before it decides against numbers that then change) |
| salary | X-at | -- | Surface which tax year a figure was actually computed against, and give a new year's bracket set a write door, so the resolver's substitution stops being invisible and the settings screen can finish the year it starts. Closes **N-235**, **N-236**. | #82 | -- | NOW |
| salary | X-av | -- | Make the stored salary fact a DATED PER-PAYCHECK GROSS with the annual figure derived from it (**R-HW(b)**), which both gives the base an effective date -- so the app can tell a raise from a correction -- and lets the owner enter the `$3,526.00` his stub actually pays instead of an annual figure that divides to four cents less. Closes **N-237** and **N-391**. | #83 | -- | NOW |
| balance | X-ax | -- | Reconcile a carry-forward rollover against its source: when a settled source's spend changes, the target row's top-up moves by the same amount so the two always sum to the budget the rollover divided. **MOVES MONEY.** Closes **N-249**. | #84 | -- | NOW |
| balance | X-ay | -- | Post a modelled account's own CONTRIBUTIONS and ACCRUALS, so its balance-assertion residual is what the account earned rather than what it earned plus what was paid into it. Closes **N-277**. | #85 | -- | NOW |
| recurrence | R7f | -- | Decide, then apply, what a PROGRAMMATICALLY created recurrence starts on: the contribution and salary-profile routes seat theirs at the schedule's OPENING and fan rows into closed pay periods, where the form defaults to today. **MOVES MONEY.** Closes **D34**. | #86 | -- | NOW |
| recurrence | R11 | -- | Rule and then add the LEAD placement -- funding an occurrence from a paycheck EARLIER than the one containing it -- which is what survived **D20** when measurement showed `CONTAINING_DATE` already funds on or before every occurrence. **MOVES MONEY.** Closes **D40**. | #87 | -- | NOW |
| pay_calendar | C10 | -- | Take the FIVE `app/routes/salary/` reads of "which paycheck am I in" off the process clock and onto the owner's civil day, which is the half of that row `C2-f3a` did not reach. Grows the pylint checker that forbids the process clock outside one module. Closes **P49**, **N-138**. | #88 | -- | NOW |
| balance | X-bc | -- | Make the collateral link's same-owner invariant STRUCTURAL: `budget.accounts.collateral_account_id` becomes a COMPOSITE foreign key on `(collateral_account_id, user_id)` against the `uq_accounts_id_user` superkey two other tables already target, and `_validate_collateral_link`'s ownership arm goes with the state it can no longer express. Closes **N-360**. | #89 | -- | NOW |
| balance | X-bg | -- | Tell *this occurrence did not happen* apart from *archive this row*, which the transfer delete door conflates into one `is_deleted` flag read off the template link, so a settled instance the owner removes stays restorable by the recurrence conflict chooser with no control between it and the books. Closes **N-386**. | #90 | -- | NOW |
| balance | X-cb | -- | Shut the second door `X-bl-2b` left open on the collision assignment and take the last place a payment's dates come apart: `schedule_dates` goes package-private so `slotted_dates` is the only way to it, `is_confirmed_payment_eligible` takes the `PaymentDates` value, and seven dead imports in three loan test files go too. Closes **BAL-472**, **BAL-473**, **BAL-474**. | #91 | -- | NOW / balance:X-bl-2b (shipped) |
| recurrence | R19-b | -- | Make `occurs_on` NOT NULL so a template-linked non-override row always records the occurrence it answers, which makes `R19-a`'s retain branch unreachable rather than quiet and deletes it, the NULL arm of `rows_claiming`'s claim query and `idx_transfers_template_scenario_undated`, once the one-time transfer branch has a rule for what its row answers. Closes **REC-516**. | #92 | -- | after #36 / recurrence:R19-a (shipped) / balance:X-ci-3 (R-BAL94: the two one-time transfer rows take `occurs_on` at X-ci's cutover; a NOT NULL before it refuses them) |
| bank_import | X-gn | -- | Let the Reconcile card's MATCH pane name a SECOND bank line (**R-KC**), restoring the door the workbench's deletion removes: **R-JY**'s act model keeps `group_id`, the accept door already takes a `frozenset[int]`, and only `reconcile_match_payload`'s single-line key stands in the way. | #93 | -- | NOW |
| bank_import | X-gu | -- | Take `lock_lines` over the import's own lines in `delete_import` BEFORE the import row is deleted, so the cascade that removes them waits behind the same `id` order a Reconcile press locks in rather than crossing it in the referential trigger's scan order; the cross-resource half of the cycle is `balance:X-bn`'s. Closes **BI-492**. | #94 | -- | NOW / bank_import:X-gi-5 (shipped) |
| balance | X-cq | -- | Make the savings horizon's ASSET band read the fold for PLAIN cash as its LIABILITY band reads it for a plain liability since `credit_card:CC-1`, one rule for both sides of the chart, so a planned card payment funded from checking no longer reads as a net-worth gain there (`$80.00` at every future sample on the lane's planted card). Closes **BAL-520**. | #95 | -- | NOW |
| balance | X-cg | -- | Make `scripts/integrity_check.py`'s DC-06 DATED arm stop filtering `is_override = FALSE`, the term migration `e7c3a1f9b482` (`X-au-h`) dropped from `idx_transactions_template_scenario_occurrence`, so it can report a re-priced row duplicating an occurrence after a partial restore, the class `X-au-h` measured at 8 rows / `$1,482.93`; the script and its test only. Closes **BAL-481**. | #96 | -- | NOW |
| balance | X-cr | -- | Make each test request run in its OWN app context (the test client pushes a fresh one per request; the fixture's outer context stays for `db.session`), the shape production has, so a second authenticated client is its own user and the seven two-client tests (three of them IDOR or security tests) measure what they claim; `$0.00`. Closes **BAL-521**. | #97 | -- | NOW |
| balance | X-cs | -- | Give the `[0, 1]` rate bound and the `1..31` day bound ONE spelling each in the schema package: the 19 inline rate and 3 inline day spellings re-point onto `_helpers.py`'s constants, whose `_DAY_OF_MONTH_RANGE` (`credit_card:CC-2`'s literals) becomes `_pay_rhythm.py`'s, the one reading the model's `DAY_OF_MONTH_MIN` / `MAX`; the census re-run first; `$0.00`. Closes **BAL-522**. | #98 | -- | NOW |
| balance | X-ct | -- | Fold the six remaining "latest row effective on or before a day" walks (three in `rate_period_engine`, one in `amortization_engine/_projection`, two in `template_amount_service`; listed in the row with their fallbacks) onto `app/utils/effective_dated.in_effect_on`, the leaf `credit_card:CC-3` gave the escrow versions and the card's APR; `$0.00`. Closes **BAL-524**. | #99 | -- | NOW / credit_card:CC-3 (shipped) |
| balance | X-bw | -- | Make `_RESTORE_FROM_DEFINITION_SQL` round-trip a paycheck's own figure rather than the template's `default_amount`, so `flask db downgrade` becomes a real rollback path instead of costing `-$9,677.29` of projected income across 43 future paychecks. Closes **BAL-464**. | #100 | -- | NOW |
| salary | S7 | -- | Split `app/services/projection_inputs.py` (994 of pylint's 1000 lines) by shape as a PURE move graded by AST (**R-PC74**'s shape), so the next edit there has room without shaving prose. `$0.00`. Closes **SAL-555**. | #101 | -- | NOW |
| salary | S10 | -- | Make the database's `server_default` agree with the model's for the four columns autogenerate reports drifted (`user_settings.safe_withdrawal_rate`, `salary_profiles.name`, `tax_bracket_sets.child_credit_amount` and `.other_dependent_credit_amount`) in one migration, so `flask db check` reads clean and a real drift cannot hide in the noise; `$0.00`. Closes **SAL-563**. | #102 | -- | NOW |
| pay_calendar | C17-e | -- | Make two nominal paydays that a closed run displaces onto ONE cash day ONE paycheck everywhere an era's grid is read, so an era a later holiday set puts under the collision floor derives correctly instead of coalescing silently (**R-PC77**), after which `reject_shift_on_short_cadence` is a fence the calendar no longer needs. `$0.00` and unreachable today. Closes **N-493**. | #103 | -- | NOW / pay_calendar:C17-d (shipped) |
| pay_calendar | C19 | -- | Split the `PayCalendar` door by SHAPE -- `searches` / `views` / `walks` -- as a PURE move graded by AST (**R-PC74**'s shape), so each caller-level argument lives in the namespace that owns it and the class keeps only what its data can answer, off both pylint ceilings `_calendar.py` sits at (20 of 20 methods, 981 of 1000 lines). Closes **PC-512**. | #104 | -- | NOW |
| balance | X-bm | -- | Make every batch pricing caller apply `pricing_load_options`; which callers are BATCH is no regex's answer, so the candidate set is the FILE difference `grep -rln 'amounts_by_id\|resolve_transfer_amount' app/` (31) less `grep -rln pricing_load_options app/` (10), and no total is stated here. Costs one statement per DEFINITION since `X-au-e`, with no figure moving. Closes **N-296**. | #105 | -- | NOW |
| pay_calendar | C15 | -- | Split the readiness card so the retire-later solve runs only when an assumption it reads has moved and a stepper move costs one probe (**R-PC52**), instead of nine projection walks on every refresh. Closes **P60**. | #106 | -- | NOW |
| balance | X-bo | -- | Rule, then apply, whether an entry CREATE may take `sync_purchase_postings` for the new purchase plus the parent's leg instead of re-emitting every sibling's legs, which makes filing N purchases into one budget line quadratic at `$0.00` of drift. Closes **N-406**. | #107 | -- | NOW |
| balance | X-x3 | -- | Make `onboarding.has_periods` ask "does a period cover today" rather than "do any periods exist", so the checklist and the page it renders on cannot disagree. | #108 | -- | NOW |
| balance | X-x4 | -- | Stop answering an empty requested WINDOW with the absence card, and stop the card's copy naming two states. | #109 | -- | NOW |
| balance | X-x5 | -- | Delete `verify_savings_producers.py`'s dict-or-attribute `_get` reader and the two-spelling readers its own docstring says this step deletes. | #110 | -- | NOW |
| balance | X-y | -- | Move the surfaces that resolve the baseline scenario directly (census 9 code lines `(?<!def )get_baseline_scenario\(` in `app/**/*.py`) onto the seam, deciding what a WRITE may do without one. Closes **N-117**. | #111 | -- | after #110 / balance:X-x |
| balance | X-e | -- | Re-take the census the column deletions made historical, then resolve what remains: two callerless public seam entries (its de-duplication rationale, N-180, dissolved at `recurrence:R16-c-1` `c88ed6ba`). Carries **N-96**, **N-85**. | #112 | -- | NOW |
| balance | X-ac | -- | Stop the cockpit reducing `_sum_liquid_balances` twice per render and publishing the answer under two context keys. Closes **N-121**. | #113 | -- | NOW |
| balance | X-ag | -- | Build the instrument that refuses lax digit acceptance, shown FIRING on a planted defect; both earlier method-name designs were refuted by measurement. Closes **N-139**. | #114 | -- | NOW |
| balance | X-ah | -- | Rule every `type=int` coercion (census 37 code lines `type=int` in `app/**/*.py`) over ten modules (census 10 code files `type=int` in `app/**/*.py`) and parse them like every other id, with a second rule that admits a meaningful zero. Closes **N-142**. Carries **N-329**. | #115 | -- | NOW |
| balance | X-al | -- | Census the live `duplicate-code` disables (census 16 comments lines `pylint: *disable=[^#]*duplicate-code` in `app/**/*.py`), none re-measured, and build the arm that catches a stale one -- `useless-suppression` is blind to them. Closes **N-154**. Carries **N-416**. | #116 | -- | NOW |
| balance | X-ba | -- | Collapse `app/ref_cache`'s near-identical id accessors (census 27 lines `^def [a-z_]*_id\(` in `app/ref_cache/_accessors.py`) into one generic lookup, delete the two with no production caller, and migrate the call sites, so the repetition that took the module past its line ceiling stops being paid for by splitting. Closes **N-341**. | #117 | -- | NOW |
| balance | E2-0 | -- | Trace the super-package membership from the code: which modules are members, what the public re-export surface is, and whether any member imports a non-member. Expect it to DECOMPOSE. Carries **N-33**, **N-35**. | #118 | -- | NOW |
| balance | E2-n | -- | Make the move and delete the registry, with `_FENCED_MODULE_RULINGS` as the LAST commit rather than the first. Its decomposition is decided from `balance:E2-0`'s trace. | #119 | -- | NOW |
| balance | G1 | -- | Trace each allowlist entry to its real cause, then stop the ledger-model and balance-seam fences carrying name lists. Closes **N-147**. | #120 | -- | NOW |
| balance | G2 | -- | Build the `Money` and `DisplayLabel` value types that retire W9901, W9904 and W9902, taking the small label half first. Phase G runs INSIDE E2 by ruling R-DQ. | #121 | -- | NOW |
| recurrence | R7e | -- | Make the recurrence form's THREE-state fields one typed submission the schema emits, so `stated` / `cleared` / `not mentioned` stops being re-derived by a `KEY in data` read at each route site. Closes **D36**. | #122 | -- | NOW |
| recurrence | R12 | -- | Pin `LC_ALL` in the image with a startup assertion (**R-R54**), covering every locale-sensitive site and every future one in one line. Closes **F-15**. **`D41` is DISCHARGED, not closed here**: it said no test drives `deploy/shekel-deploy.sh`, and one has since `398c332c` (2026-08-08), nine days before the finding was filed -- with all four arms this step once owed. | #123 | -- | NOW |
| pay_calendar | C11 | -- | Close the last FIVE service doors that open their own read pass and assert the LAYER predicate in their place -- no module under `app/services/**` calls `BalanceContext.build` -- carving out `loan_recurrence_sync`, which is a writer and takes one by design. Closes **P56**, **P69**. | #124 | -- | NOW |
| balance | X-i5 | -- | Make every request a QUERY until it DECLARES a write, so the HTTP method stops standing in for "does this write": each non-GET handler (listed by `grep -rnE 'methods=.[^]]*[PD]' app/routes/`) puts its writes in a `write_transaction` block and the render that follows takes a fresh snapshot holding them, which is the shape `/grid` already has. Closes **N-358**. | #125 | -- | NOW / balance:X-i3-a (shipped) |
| balance | X-i6 | -- | Bind the per-account reads a pass does NOT memoize and so never reach `_memoize_once`: the seven seam entries that early-out before the funnel, the contribution feed that scopes its payroll loaders off the ACCOUNT's owner, and `bank_agreement`'s direct walk -- with the ARM that mechanizes "one door". Closes **N-362**. | #126 | -- | NOW / balance:X-i4 (shipped) |
| balance | X-be | -- | Resolve the `app/` modules sitting at exactly pylint's 1000-line ceiling -- regenerated by `awk 'FNR==1000{print FILENAME}' $(find app -name '*.py')`, never listed here -- by deciding per module whether it SPLITS or whether the ceiling is the wrong number, because in this corpus what the cap binds is what gets written down. Closes **N-365**. | #127 | -- | NOW |
| balance | X-bf | -- | Make a stale or FORKED test template self-reporting: record the ordered revision list the builder actually applied and refuse a bootstrap whose repo chain no longer produces it, because `alembic_version` records which revision is head and never which ones ran. Closes **N-385**. Carries **P79**. | #128 | -- | NOW |
| balance | X-bt | -- | Make ONE producer answer whether a docker daemon is safe to spawn containers on: `test.sh` ASKS it (`docker info`), `conftest.py` matches a PATH ALLOWLIST, neither knows the other, and `CI=false` is truthy in both so it sanctions the production daemon -- `X-br-3` fixed that in one home only. The allowlist is DELETED. Closes **N-461**. | #129 | -- | NOW |
| balance | X-bs | -- | Make a test infrastructure failure FAIL rather than skip -- its `pytest.skip` calls (census 11 code lines `pytest\.skip` in `tests/test_deploy/test_proxy_trust_and_headers.py`) fire on something that RAN AND FAILED, not on something missing -- then delete the last two published ports, in THAT ORDER, since the tests now skipping are the collision's only witnesses. Closes **N-459**, **N-460**. | #130 | -- | NOW |
| balance | X-by | -- | Give each worktree its OWN virtualenv and correct the comment claiming they already share one: four of the seven hold no `.venv`, so `scripts/test.sh:114` falls through to `command -v python3` and their pytest is the MAIN checkout's, while `:259` states that sharing is universal and two worktrees falsify it. `X-br-4` isolated the DATABASE and stopped there. Closes **BAL-469**. | #131 | -- | NOW |
| balance | X-cc | -- | Stamp each fixture balance assertion at its own instant the way `append_balance_assertion` already does, across the 10 row shapes in 7 modules whose frozen-today idiom asserts a balance for a day AFTER the instant it carries, and THEN bind `ck_anchor_history_typed_on_or_after_observed`, which was built, proven to refuse and withdrawn on that collision. Closes **N-351**. | #132 | -- | NOW |
| balance | X-cd | -- | Make each transfer money-field bound STATE which question it answers, now that `$0.00` moved is a legal observation (**R-BAL15**): `settled_amount` keeps its inclusive minimum because it records what happened, and the create and update `amount` fields keep their exclusive one because a `$0.00` PLAN is not a thing. Closes **N-301**. | #133 | -- | NOW |
| balance | X-ce | -- | Classify every census-shaped claim in `app/` docstrings and comments (census 13 lines `[0-9]+ (sites\|callers\|readers\|files\|modules)\b` in `app/**/*.py`) and a larger half that spells the number as a WORD, into DATED records of a past act, which stand, and PRESENT-TENSE claims, which become the grep that regenerates them. Closes **BAL-479**. | #134 | -- | NOW |
| bank_import | X-gy | -- | Measure where the hosted runner's 5-13x per-test ratio over this host comes from (the 3x core oversubscription of `-n 12`, the per-item drop-and-reclone, the service container's disk; its durability is already off) in a matched A/B on the runner itself, fix the cause it names, and re-size `pytest.ini`'s per-test cap from CI's own `--durations` table. Closes **BI-496**. | #135 | -- | NOW |
| bank_import | X-gw | -- | Give `Tally` a `frozen()` producer beside both classes in `_outcome.py`, built from the field names so a counter added on one side and not the other refuses loudly, and make `apply_reviewed` return it. Closes **BI-494**. | #136 | -- | NOW / bank_import:X-gt (shipped) |
| bank_import | X-ha | -- | Bring the Reconcile screen's per-request cost down from what the 2026-09-15 import measured -- a 650-850 ms render, a 1.65 s Apply of 22 cards answering 974 KB, a 560-650 ms preview nine times over -- by naming the query or payload each one pays for; performance only, `$0.00`. Closes **BI-500**. | #137 | -- | NOW |
| balance | X-cj | -- | Make an audit row a MIGRATION or the LOGIN door writes name its SYSTEM origin instead of a NULL author, so an empty `audit_log.user_id` can never read as unknown: 13 `recurrence_rules` rows of 2026-09-14 and the login's `mfa_configs` UPDATE carry none while every business write carries the owner. Closes **BAL-497**. | #138 | -- | NOW |
| balance | X-ck | -- | Make the full-edit popover's delete confirmation count PURCHASES through `status_seam.covering_movements` (the rule `_row_rules.settles_from_entries` states) instead of `txn.entries`, and write a covering movement's `description` ONCE at its first cover so a re-record or a restore leaves it (**R-BAL39**); template, seam and tests only, `$0.00`. Closes **BAL-504**, **BAL-505**. | #139 | -- | NOW |
| balance | X-cl | -- | Delete `tests/manual/measure_entry_reconcile_rules.py`, the dead manual harness that reads and writes two columns the schema no longer holds (`is_cleared`, `entry_date`), so the proof instruments that remain are ones that compile. Closes **BAL-510**. | #140 | -- | NOW |
| balance | X-cm | -- | Make `BankAgreement.constant_offset` a per-run reading over each `RecordedRun`'s compared days, rendered in that run's block on `statement_agreement.html`, so a run whose assumed anchor is wrong by `K` beside a run whose is right still earns the N-342 signature (**R-BAL63** made the anchor per run); the report, the template and their tests only. Closes **BAL-512**, **BAL-514**. | #141 | -- | NOW / balance:X-bj-1b (shipped) |
| balance | X-cn | -- | Make `transfer_service.update_transfer` refuse an unknown key as `update_entry` does, so a renamed kwarg in a caller's bag is a raised error rather than a silently ignored edit that settles the row at its plan; the door, its docstring and every fixture that forwards a bag. Closes **BAL-513**. | #142 | -- | NOW |
| balance | X-co | -- | Give the plan gate a CROSS-TREE arm (its loop picks: a PR graded against every other open PR, or dev on every push) so the one-tree window that minted `R-BAL10` twice and merges a prose count clean is unrepresentable, and make the SessionStart hook read `origin/dev`'s steps.md rather than its checkout's; deletes `STANDING.md`'s 2026-09-09 line. Closes **BAL-515**. | #143 | -- | NOW |
| balance | X-cp | -- | Make the suite's dead controls honest: `test_grid`'s hidden-period case comparing the `baseline` it captures, and `test_race_conditions.py` running two live clients per race (the `settlement_columns` loop and its 16 copies, BAL-516 / BAL-517, were DELETED with the columns at `balance:X-bi-4b-2` `32c65cf1`, not consolidated). Closes **BAL-518**, **BAL-519**. | #144 | -- | NOW |
| bank_import | X-hb | -- | Make the import-delete confirmation and receipt say that this import's OWN placement leaves and which import becomes the balance-of-record, read off the level relation `balance:X-bj-1` built (a release is an appended row), with `ImportRemoval` gaining the field; the door's copy and its tests only, `$0.00`. Closes **BI-501** (was BAL-489). | #145 | -- | NOW |
| balance | X-ci-1 | -- | Gave the transfer twin `recurs` and re-keyed every no-cadence site onto it bar the three accessor bodies (`transaction_template`, `transfer_template`, `paycheck_line`; **R-BAL97**): the discardable count asks it, a move flags only a recurring row and re-places the day, `occurs_on` follows a move, a typed figure restates the definition (**R-BAL92**..**R-BAL96**). Closed **BAL-492**, **BAL-493**. | SHIPPED | `465f91cd` | -- |
| credit_card | CC-5-1 | -- | Re-cut the parent-account key: `fk_transaction_entries_parent_account` and its `ON UPDATE CASCADE` dropped, `owner_id` NOT NULL backfilled, and three keys holding a movement to its own account and to its row's owner (**R-BAL76**'s letter, **R-CC32**'s ON DELETE; migration `9900b309f0b0`); no door wrote cross-account yet, so every balance was byte-identical. Closed **CC-353**. | SHIPPED | `4045a9b1` | -- |
| credit_card | CC-5-2 | -- | Wrote the first cross-account movement: the purchase door takes an account gated on the ROW's owner (**R-CC37**, **R-CC39**); the reconcile scope re-keyed to the movement's account, `off_statement_sum` and guard 4 given a movement arm; the account-move retention retired (**R-CC36**); the picker from the door's tuple, hidden with one choice (**R-CC34**), the CC checkbox kept (**R-CC35**). | SHIPPED | `af3b9f5b` | -- |
| credit_card | CC-5-3 | -- | Added the settle-with-tender door: `Settlement.account_id`, ONE gate (`movement_account`) for all three writers, the covering movement booked on the tender on the movement's day, refused on or before the card's opening; where the money moved retained across a revert (**R-CC42**); the "Paid from" picker (**R-CC39**); `covered_cash_leg` takes the account (**R-CC40** half 1). | SHIPPED | `9a74658f` | -- |
| balance | X-bi-6-1 | -- | Drew a transfer on the grid (desktop, mobile, companion) as a `TransferLeg` read off its parent -- keyed (transfer, account), its side by the one `leg_of`, its record the dated covering movement (**R-BAL87**) -- with the cell's doors the transfer routes; `_shadow_mutations.py` and the five route guards deleted; byte-identical. Closed **N-303**, **N-420**. | SHIPPED | `45742c05` | -- |
| balance | X-bi-6-1b | -- | Drew a transfer on the dashboard's bills and pulse, the calendar and the Spending report as a leg read off its parent through ONE period-windowed transfer load and each row producer's leg twin (**R-BAL86**); `far_leg_clause` and `paycheck_rows_clause`, left with no reader, deleted; byte-identical over 844 day and 173 transfer entries. | SHIPPED | `6773418b` | -- |
| recurrence | R16-c-1 | -- | Merged a loan's recorded facts and its forward plan into ONE event stream replayed once from the origination (**R-R90**): no projected event before a recorded fact, a pass replays the facts visible by its `as_of` (**R-R91**), a RESET clears standing charges (**R-R72** (2)); `projection_seed`, `fold_forward` and `LoanPaymentSplit` deleted; byte-identical on the 09-19 clone. Closed **D61**. | SHIPPED | `c88ed6ba` | -- |
| balance | X-bi-4 | -- | The DECOMPOSED parent of the fold's re-point onto MOVEMENTS (**R-BAL75**..**R-BAL84**), three leaves shipped: the fold and the ledger read movements only (4a `babae2e3`), every reader reads the covering movement (4b-1 `21709195`), the figure columns go (4b-2 `32c65cf1`, migration `45f10b870c8b`). Closed **BAL-500**, **BAL-501**, **BAL-507**, **BAL-516**, **BAL-517**, **BAL-523**. | SHIPPED | `32c65cf1` | -- |
| salary | R18-d | -- | Performed the OPERATOR runbook through the doors (**R-HJ**): the Phone income template ends 2026-09-18 after its Received September row and the `$45.00` taxable line runs from the 2026-09-10 period, first paycheck a month. **MOVED MONEY**: 23 projected paychecks net `+$40.73` and lose the template's `$39.54` row, `+$1.19` a month on the plan; no record moved. Closed **D59**, opened **SAL-564**. | SHIPPED | `0345fbae` | -- |
| salary | R18 | -- | The DECOMPOSED parent of paycheck EARNINGS LINES (**R-SAL38**: a paycheck is base pay plus a list of lines, a line's kind its position in the waterfall), split 2026-09-15 into the storage rename, the two earning kinds with the engine arms and the form, every line's span on its own rule, and the operator runbook that moved the phone allowance onto a line; ticked with its last leaf. | SHIPPED | `0345fbae` | -- |
| recurrence | R20 | -- | Recorded the balance stated at loan setup as the `tracking_start` assertion it is (owner-dated, bounded origination..today; **R-R72** part 3); deleted `LoanParams.current_principal` and its CHECK (migration `22b23085394d`: 0 of 2 on the 09-19 clone; production prints its count) and the tracking-start door's earliest-payment refusal. **MOVED MONEY** for new loans; `$0.00` live. Closed **REC-519**. | SHIPPED | `b4da8068` | -- |
| credit_card | CC-10 | -- | Refused a transfer OUT of a card at both transfer doors through ONE composed loan-or-card set (`_validation._reject_unmodeled_source`, composing the loan guard and its card sibling; the investment contribution door translates the refusal instead of a 500) and kept a card out of a paycheck's deposit picker (`active_accounts_query(revolving=False)`); design 3.8; no migration. | SHIPPED | `32528a7c` | -- |
| credit_card | CC-4-3 | -- | Read the set through the one predicate and clause in the dashboard's upcoming bills (`_query_unpaid_expense_rows`), the spending report (`query_settled_expenses`, `_in_span`) and the calendar (`_query_transactions_for_range`) behind ONE member's balance line; both defaults are the set's primary (`resolve_analytics_account` deleted; the calendar's twin is `resolve_analytics_cash_flow_set`). | SHIPPED | `66409bc4` | -- |
| credit_card | CC-4 | -- | The DECOMPOSED parent of the paycheck's plan items across "checking and its cards" (**R-CC16**), split 2026-09-18 (developer) into the money (4-1 `b4e35783`), the affordances (4-2 `07063a13`) and the other three readers (4-3); ticked with its last leaf. | SHIPPED | `66409bc4` | -- |
| credit_card | CC-4-2 | -- | Chipped a cell or mobile card whose row is on another account than the balance line's (the neutral `.flag-chip`; a transfer between members never chips); a fragment's balance line resolved ONCE by the page's own call, the `?account_id=` override read off `HX-Current-URL` (**R-CC30**); the full-create popover and the Add Transaction modal pick over the set's members (**R-CC31**). | SHIPPED | `07063a13` | -- |
| credit_card | CC-3 | -- | Derived the statement cycle purely in `card_statement.py` (the due date the first due day strictly after the close, **R-CC26**; owed = minus the fold, the minimum floored at zero, **R-CC29**) and rode the card's APR on `rate_history` through `card_apr` (ONE effective-dated walk) with set-by-date and remove doors (**R-CC27**) inside the Card terms card (**R-CC28**); no migration. | SHIPPED | `e4c0b5ec` | -- |
| credit_card | CC-2 | -- | Added the `budget.credit_card_params` satellite (close day, due day, the minimum rule, the cashback rate and auto-redeem threshold, the limit; C13-c's ownership key) by migration `97f92340fffc`, ONE door and ONE schema requiring the five NOT NULL columns (**R-CC24**), the "Card terms" card on cash detail gated by `is_revolving` (**R-CC25**); the table empty. Moved no money. | SHIPPED | `c69bdbfe` | -- |
| bank_import | X-f6b-1b | -- | Made a line's merchant a READ over its sightings: the sighting carries the merchant its word names, the line's key and name are read-only hybrids over ONE correlated SQL producer (**R-BI16**), the word column and the line's key dropped by migration `3ef820b7dd52` (**R-BI17**; its UP sweeps merchants nothing names). No money (306 of 306 equal). Closed **BI-504**. | SHIPPED | `5daa1393` | -- |
| credit_card | CC-4-1 | -- | Made the grid read the owner's cash-flow set as ONE paycheck (**R-CC16**): one predicate (`resolve_cash_flow_set`) and one row clause (a transfer between two members counted once from the balance line's side, **R-CC23**); the seam composes the subtotals across the members and the identity gains `elsewhere`; `active_accounts_query` gained `revolving`. No migration. | SHIPPED | `b4e35783` | -- |
| bank_import | X-f6b-1 | -- | Built the sighting relation `budget.statement_line_sightings` (a row per import that showed a line: wording, id, balance, merchant word, day), moved those facts off the line, made coverage the DECLARED window with the solve's candidates the line days and the stated day (**R-BI10**, **R-BAL74**), deleted the derived span and count columns. Closed **N-303**, **N-313**, **N-331**, **N-434**. | SHIPPED | `d4eb2752` | -- |
| credit_card | CC-1 | -- | Made the card a FLAG on its account type (`has_revolving_credit`, the CHECK that a revolving type carries no engine flag, `is_revolving`, the seed, migration `a16516c8ec05`) and the liability band read the fold forward for every non-loan liability through the seam's one date-keyed dispatch `balance_at_dates` (**R-CC14**); the seed refresh finds a built-in type by name and owner. Moved no money. | SHIPPED | `6f7cb619` | -- |
| balance | X-bi-7d | -- | The DECOMPOSED parent of the cutover to ONE DEFINITION PER PLAN ITEM, split 2026-09-18 into the suite's bare CHECK builders (7d-1 `08230752`) and the migration (7d-2 `829c2c26`, review fix `9cf27a3a`), both shipped; `legacy_link_less_row_of`'s 22 calls in 21 files retired or moved, the constructor census 28 on `9cf27a3a` (3 link-less stays, 17 splat, 8 linked). | SHIPPED | `829c2c26` | -- |
| balance | X-bi-7 | -- | The DECOMPOSED parent of the ONE-DEFINITION shape (**R-BAL20**), all leaves shipped: every plan item has exactly one definition, so a one-off is a rule-less definition plus its placed rows, both flag cells left `budget.transactions` and the one-off half of every `template_id IS NULL` branch is unwritable (7b `321bf2e4`, 7c `4f15f222`, 7d `829c2c26`; `from_scratch_architecture.md` s.10). | SHIPPED | `829c2c26` | -- |
| balance | X-bj-1b | -- | Made the bank walk anchor PER RUN among the standing bank levels a run reaches, every other such level a CHECKPOINT listed per run (**R-BAL63**); a run with none anchors on nothing (**R-BAL64**); the fold TOTAL, `runs` replacing `anchor` and `imports` (**R-BAL65**); the import door opens from the run's anchor (**R-BAL66**). No migration; 0 leaves moved on production. Closes **N-343**. | SHIPPED | `82b534f6` | -- |
| balance | X-bj-1 | -- | The DECOMPOSED parent of the level relation (**R-BAL55**): 1a built ONE relation of the owner's true-ups and the bank's placements with releases appended; 1b made the bank walk choose within each run and read the rest as checkpoints. Ticked with `X-bj-1b`. | SHIPPED | `82b534f6` | -- |
| balance | X-bi-3 | -- | The DECOMPOSED parent of the COVERING MOVEMENT, split 2026-09-15 (**R-BAL41**), all five leaves shipped: every settle writes the payment row a purchase already is, its figure naming its source (**R-BAL39**), the existing settled rows cut over by the fail-closed migration `ad573b07bede` (**R-BAL40**, 3d `3a15daf4`), and the revert keeps the movement (3e `d1e3f7a6`). Closed **N-447**. | SHIPPED | `d1e3f7a6` | -- |
| salary | R18-c | -- | Gave EVERY line a start and an optional end on its own rule (**R-SAL38** (2); **R-SAL30**, **R-SAL31** amended): the line schema takes the recurrence mixin with the start OPTIONAL (blank = the opening payday), the partial's start and end-bound rows turn on for lines, the Frequency cell words the span; no migration, no engine change; drive `d97ca1b5`. `$0.00`. Opened **SAL-562** (**R-SAL39**). | SHIPPED | `34ad4bda` | -- |
| bank_import | X-gz | -- | Showed each candidate row on the Reconcile MATCH pane with its BUDGETED placement and, for an envelope entry, its `purchased_on`, every date labelled by kind beside the bank line's own date with the gap printed, the settle day never printed (**R-BI9** and its two sub-rulings); `CandidateRow` carries `period` and `purchased_on`, `_dating.py` presents. Closed **BI-498**. | SHIPPED | `08a66901` | -- |
| salary | R18-b | -- | Seeded the two EARNING kinds (`taxable_earning` joining gross, `after_tax_earning` joining net; migration `6c15d2a97b78`), gave the engine ONE line pass (`priced_gross` = base + taxable lines, replayed by the FICA year-to-date; a percentage line is a % of BASE), the LINE door with the four kinds and the cockpit its groups; byte-identical over the 64 saved paychecks. Opened **SAL-561**. | SHIPPED | `ad9fed61` | -- |
| salary | R18-a | -- | Renamed the storage: `salary.paycheck_deductions` became `salary.paycheck_lines` and `ref.deduction_timings` became `ref.paycheck_line_kinds` (migration `0a4d2c3e89f8`, every constraint, index, sequence and trigger renamed, the downgrade restoring all and refusing an earning-kind row), with the model, enum, ref cache, schema and form names; byte-identical over the 64 saved paychecks. `$0.00`. | SHIPPED | `ef0dc831` | -- |
| balance | X-bi-6a | -- | Re-pointed every PROJECTED-shadow READER onto the parent transfer's derived legs (**R-BAL13**, **R-BAL38**): `planned_cash_rows`, `balance_at/_plan.py`'s PLANNED tier, `_asset_contributions`, `shadow_contributions` and the loan partition's second consumer, all through the one leaf `transfer_legs.py`; Invariant 5 restated; `$0.00` measured. Opened **BAL-500**, **BAL-501**. | SHIPPED | `323400d9` | -- |
| recurrence | R7d-c-2 | -- | Generation resolves a loan payment's stop through the composed door (`resolve_generation_plan` reads `read_definition` over the pass the schedule carries), so a stale `end_date` binds nothing in either direction (**R-R56**) and R16-b-2's summed payoff reaches the ROWS: on the clone a `$50` sweep beside the Van moved 35/65 rows to 32/32 at `2028-11-22`; `$0.00` live. Re-cut on R16-b-2 (**R-R65**). | SHIPPED | `b8509c1e` | -- |
| pay_calendar | C20-a | -- | Made the shared form macros (`_form_macros.html`) take their attributes as a MAPPING rendered `k="v"`, values still autoescaped (the 16 `input_attrs` callers pass dicts), so `min` / `max` / `step` / `maxlength` / `autocomplete` and the ARIA pair reach the browser; a rendered-attribute test grades each hint and a hostile value stays escaped. Display-only, `$0.00`. Closed **PC-515**. | SHIPPED | `51a905b6` | -- |
| pay_calendar | C17-d-3 | -- | Offered the cadence KIND on the four schedule doors and registration as RADIO ARMS with no script (**R-PC84**): distinct wire keys per arm, the schema handing back the `Rhythm`, `rhythm_to_wire` the one inverse, one macro on every door; `add_months` folded onto the clamp; the event's description and the last dropped-column lines put right. `$0.00`. Closed **PC-508**, **PC-513**, **PC-514**. | SHIPPED | `4420df87` | -- |
| pay_calendar | C17-d | -- | The DECOMPOSED parent of the day-of-month cadence KIND, split 2026-09-13 (**R-PC81**) into the value's shape, the kinds with their migration, and the forms; ticked with `C17-d-3`, its last leaf. MOVED MONEY at `C17-d-2` for a month-kind owner, `$0.00` on production. | SHIPPED | `4420df87` | -- |
| pay_calendar | C17-d-2 | recurrence:R13 | Added the `Monthly` and `SemiMonthly` kinds (**R-PC79**): month arithmetic in `_grid`, the days stored, `kind_id` and `ref.pay_cadence_kinds` DROPPED (**R-PC80**, migration `3ec5291ca4e2`), `PayCadence` 12 / 24 without dividing, the event's phrase (**R-PC82**), registration's day (**R-PC83**). MOVED MONEY for a month-kind owner, `$0.00` on production. Closed **P78**. | SHIPPED | `d79986c7` | -- |
| recurrence | R13 | pay_calendar:C17-d-2 | Added the `Monthly` and `SemiMonthly` KINDS to the pay era's cadence, one commit with `pay_calendar:C17-d-2` (**R-PC68**, **R-R28**): such an owner's paydays land on the days of the month they are paid rather than on a fixed-length walk that drifts through it. **N-399** (later `salary:SAL-556`, closed at `R15-b`): the kind did not end the per-payday month walk. | SHIPPED | `d79986c7` | -- |
| pay_calendar | C17-d-1 | -- | Made the cadence a VALUE OF ITS OWN KIND whose kind is its type -- `pay_rhythm.FixedDays`, `Rhythm.cadence`, `Era` without a `kind` field -- with `_grid` dispatching on the value's type through one table, `PayCadence` taking the value, and `era_to_mint`'s on-grid test asking the grid's round trip rather than a second `%` spelling. `$0.00`, a pure refactor (**R-PC81**). | SHIPPED | `7a6bbaf5` | -- |
| pay_calendar | C17-c-2b | -- | Made extend and the rolling top-up MATERIALISE the plan through a CONTINUING writer entry (`pay_period_write.continue_paydays`, a prefix of `planned_paydays_after`, minting and retiring nothing) and drew the seam where the door that minted the later era drew it: its first payday REPLACES the old era's last planned payday at or before it (**R-PC75**). **MOVED MONEY**. Closed **PC-509**, **N-494**. | SHIPPED | `36c6b6af` | -- |
| pay_calendar | C17-c-2a | -- | Refused, from the one writer and through every door, a batch whose first new payday skips a whole paycheck of the owner's plan (**R-PC67**, **R-PC76**: `pay_period_batch.reject_skipped_paycheck` beside the floor, reading the plan's second projected payday), deleting the gap confirmation whole; and made the four payday doors ask for the SCHEDULED day (**PC-504**). Closed **P80**, **PC-504**. | SHIPPED | `3d635e4a` | -- |
| pay_calendar | C17-c-1 | -- | Moved the batch shape -- the size bound, `reject_undatable_payday`, `requested_paydays` and the floor `reject_backward_payday` -- out of `pay_period_write.py` (998 of pylint's 1,000 lines) into a new `pay_period_batch.py` as a pure move, the cut ruled **R-PC74**, so `C17-c-2` does not rewrite a file at the ceiling; the writer is 663 lines. Closed **PC-507**. | SHIPPED | `7d26ec2c` | -- |
| pay_calendar | C17-b-2 | -- | Made every reader ANCHOR on the phase of the era covering its own day, the projection PIECEWISE with seams in cash days and a recorded payday MATCHED to its nearest planned one (**R-PC72**), `_eras.py` taking the grid layer first (**R-PC73**). MOVED MONEY (`$0.00` on production). Closed **N-495**, **PC-502**, **N-492**, **PC-505**, **PC-510** (born here); carried **N-496**; opened **PC-509**. | SHIPPED | `06fc0d33` | -- |
| salary | S3 | -- | The DECOMPOSED parent of pricing every payday of the projected horizon, split 2026-09-05 into FIVE leaves once the RAISE MODEL fork was ruled (**R-SAL11**): the per-raise termination, the additive column, the cutover, the horizon on `project_profile`, and the deletion of `AccountPayrollFeed`'s hold, plus a SIXTH (`S3-f`) minted 2026-09-11; ticked with S3-f-4, the last leaf of that sixth. | SHIPPED | `329b663d` | -- |
| salary | R15-c | -- | Gave the deduction form the shared recurrence partial's CADENCE controls -- unit, interval, the per-month ceiling, the placement -- placed alone where the 26 / 24 / 12 select stood (**R-SAL31**), the derived first occurrence the unit-natural zero at the opening read off the STORED unit (**R-SAL36**), the picker's whole offer set (**R-SAL37**); graded by a browser drive. `$0.00`. | SHIPPED | `77901fe0` | -- |
| salary | R15-b | -- | Gave a rule its third owning arm `paycheck_deduction_id`, priced each deduction line through its resolved rule where `_deduction_applies_at` placed it, and migrated every 24 / 12 line to a rule (**R-SAL29**, **R-SAL30**), DROPPING `deductions_per_year` and the placer (migration `542c61e48ee8`); byte-identical over the 63 saved paychecks. Closed **F-21**, **SAL-549**, **SAL-556**. | SHIPPED | `4ed9b5b3` | -- |
| salary | R15 | -- | The DECOMPOSED parent of "a deduction's cadence is a recurrence rule" (**R-SAL3**), split into THREE leaves 2026-09-13 once its four forks were ruled (**R-SAL29**..**R-SAL32**): the per-month ceiling as the cadence's third value, then the owning arm with the engine and the migration that drops the count, then the form; ticked with R15-c, its last leaf (**R-SAL35**: one PR for the last two). | SHIPPED | `77901fe0` | -- |
| salary | R15-a | -- | Gave a rule its third value, the per-month CEILING `max_per_month` (**R-SAL29** as amended): the column and CHECK in migration `ef32dfe4cd8e`, `_ceilinged` between the walk and the bound counting a paycheck cadence's paydays on the OWNER'S calendar, `monthly_equivalent` off the aggregator, the template form end to end; three pure moves for the line ceiling. `$0.00`. | SHIPPED | `bdd77055` | -- |
| salary | C12-b | -- | Priced `/savings`' current pay through the read pass's pricer, calibrated (**R-SAL25**), SUMMED over the owner's active salary profiles -- net for an income-relative goal, gross for the DTI denominator (**R-SAL26**); the last direct `calculate_paycheck` site is gone. MOVED MONEY (developer's data: net `$2,541.49 -> $2,572.78`, Emergency Fund target `$16,519.69 -> $16,723.07`). Closed **P62**. | SHIPPED | `945651c2` | -- |
| salary | C12-a | -- | Split the paycheck engine into a package of one private leaf per verb (`paycheck_calculator/`, every public import path kept), made the amount basis take the read pass's PRICER (the twelve pass-less sites build theirs through `derived_amount_basis`; **R-SAL27**, **BAL-491**), and routed the four direct-engine sites a calibration cannot move through it. NO FIGURE MOVED. Closed **P63**, **P64**. | SHIPPED | `26a7b816` | -- |
| recurrence | R16-b-2 | -- | The ESTIMATED tier SUMS every definition into a loan on its cadence under its authored closing, pricing each unanswered placed occurrence (**R-R64**) as its row would be (**R-R67**, **R-R69**) over the contract's calendar from the last assertion (**R-R68**, **R-R71**, **R-R72**); rules memoised by spec (**R-R73**). **MOVES MONEY**; `$0.00` live. Closed **D46**, **D47**, **D48**, **D53**, **D54**. | SHIPPED | `7e2e6413` | -- |
| recurrence | R16-b | -- | The DECOMPOSED parent of the ESTIMATED tier's summing, split into TWO leaves 2026-08-27 (**R-R37**) when the developer refused to have the sum IGNORE a closing bound an owner had entered; ticked with R16-b-2, its last leaf. | SHIPPED | `7e2e6413` | -- |
| pay_calendar | C17-b-1 | -- | Moved `project_period_after` and `covering_projection` out of `_derive.py` (998 of pylint's 1,000 lines) into `pay_calendar/_projection.py` between `_derive` and `_searches`, a pure move so the money leaf does not land in a file at the ceiling. Closed **PC-498**. | SHIPPED | `1ae0cd02` | -- |
| pay_calendar | C17-a | -- | Created `budget.pay_eras` and `ref.pay_cadence_kinds`, backfilled one era per owner phased on the record's opening, dropped `cadence_days`, `shift_id` and `nominal_anchor` off `budget.pay_schedule`, and made every reader take the LATEST era's rhythm where it took the row's (`$0.00`), a batch minting an era only where it STATES a rhythm. Narrowed **N-494** to one path; closed N-492's write half. | SHIPPED | `6caf56bc` | -- |
| pay_calendar | C17 | -- | Make a pay schedule a SEQUENCE OF ERAS (**R-PC58**, **R-PC66**) -- one row per *how I have been paid since*, carrying the cadence, its KIND, the phase and the convention -- so a cadence change stops re-describing every past payday, and one relation supplies the calendar-monthly schedule `recurrence:R13` needs. The DECOMPOSED parent, split 2026-09-11 into four leaves (**R-PC69**). | container | -- | ticks with #103 |
| pay_calendar | C20 | -- | The DECOMPOSED parent of "the shared form macros say what their callers pass", split 2026-09-14 when the developer had the macros fixed at once in their own commit: the attribute mapping first, then the month clamp's one producer and the manage card's own residue. | container | -- | ticks with #41 |
| pay_calendar | C17-c | -- | The DECOMPOSED parent of the doors asking for the ERA, split 2026-09-11 (**R-PC71**) into the pure-move split of `pay_period_write.py` (at pylint's 1,000-line ceiling, **PC-507**) and the door rewrite it makes room for, so the money-adjacent PR carries no pure move in its diff. | SHIPPED | `36c6b6af` | -- |
| pay_calendar | C17-c-2 | -- | The DECOMPOSED parent of the doors, split 2026-09-12 (**R-PC78**) into the `$0.00` leaf -- the skipped-paycheck refusal, the gap confirmation's deletion and the scheduled-payday wording -- and the money leaf that materialises the plan and draws the seam, so the money PR carries neither. | SHIPPED | `36c6b6af` | -- |
| balance | X-au-f-2 | -- | Move a generated transfer's amount to its definition in ONE act, because giving the parent a producer (**R-BAL10**) and emptying its column are two halves of one move: the migration clears `transfers.amount` on 169 rows and every writer states an `AmountOwnership` (**R-BAL11**). Closes **N-263**, **N-451**, **BAL-476**, **N-449** and **N-352**; **N-450** goes to `X-bp`, not closed. | SHIPPED | `cb4239a2` | -- |
| balance | X-au-f | -- | The DECOMPOSED parent of the parent-transfer cutover, split 2026-09-09 into three leaves and RE-CUT into two on 2026-09-10 (**R-BAL14**): the readers take a resolved figure, then the cutover itself. **X-au-f-3 is ABSORBED into X-au-f-2**, because a producer leaf shipping ahead of its migration leaves every reader on a stale snapshot. | SHIPPED | `cb4239a2` | -- |
| balance | X-bl-2b | -- | Made a loan payment's three dates ONE value both records compose (**R-BAL9**), so the schedule replay takes a feed with no amount model behind it: the reconciliation oracle's reference and five other suite copies of the un-seeded replay went from a 101-module import closure to 46, and `ConfirmedPayment` -- a third spelling of those dates -- is deleted. Figures byte-identical. Closes **N-432**. | SHIPPED | `0e93ec9f` | -- |
| pay_calendar | C14-e-3 | -- | Switched the shift ON at the producer, so every projected and backdated payday is the nominal day displaced onto a business day and the forecast reproduces a holiday-shifted payday from the rule alone. **MOVED MONEY.** Closes **N-398**; ticked the obligation `C14-e-1` moved but could not grade. | SHIPPED | `ed267298` | -- |
| pay_calendar | C14-f | -- | Made the generate door ask ONE job's questions rather than two (**R-PC63**, superseding **R-PC55**): an owner who already holds a rhythm is asked only how many more paychecks, a rebuild that skips a whole paycheck asks first, and the gates moved to `pay_period_gates` (**PC-498**). **P80 does NOT close** and re-points to `C17`. | SHIPPED | `5d14e4d4` | -- |
| bank_import | X-gx | -- | The receipt's standing-rule offer names the merchant the create door filed under: `FiledMerchant` on `CreatedPurchase` and `AppliedItem` (one value, rule 14's smallest grain), `rules_worth_offering(creations, applied, scope, accepts)` filters the applied items, and the press builds `RuleDoorAccepts` off `RuleView.build` instead of running `review_set`. Closed **BI-495**, REPRODUCED first. | SHIPPED | `c2e22790` | -- |
| bank_import | X-gv | -- | Composed `populate_existing()` into `locked_for_write`, the one spelling of the bank-line lock, so a re-import's NULL-fill (merchant, transaction day, running balance, source category, external id) landing between `review_set` and the press reaches all FOUR doors deciding on it, not the two BI-493 named; acting at `load_lines` and `_line_on`. Closed **BI-493**, REPRODUCED first. | SHIPPED | `88f38feb` | -- |
| bank_import | X-gt | -- | Moved the receipt value (`AppliedItem`, `RefusedItem`, `BatchOutcome`, `_Tally` -> `Tally`) out of `_batch.py` (998 -> 679 lines) into a new `_outcome.py` as a PURE move graded by AST (**R-BI7**), then fixed the wording **N-474** named and two more exclusive sentences in a rider. Closed **BI-491**, **N-474**; opened **BI-494** (owner `X-gw`). | SHIPPED | `89e39a18` | -- |
| bank_import | X-gr | -- | Make the import doors say what they do: the import receipt carries the placements it releases (**BI-488**); the delete confirmation previews `anchors` and `merchants` from the one read the act counts with (**BI-490**); the delete door says it MOVES MONEY under **R-GG** (**BI-487**); the badge and the receipt assert no cause (**BI-489**, **R-BI6**; the cause is `balance:X-bj-1`'s as **BAL-485**). | SHIPPED | `303a907f` | -- |
| bank_import | X-gs | -- | Read `current_user.role_id` outright in `require_owner`, so a principal with no role raises where the login gate is off instead of passing as the OWNER; the fixture half was EMPTY, measured: a `User` always carries the mapped attribute, so the default only ever admitted `AnonymousUserMixin`. Closed **BI-486**. | SHIPPED | `eec1a2be` | -- |
| bank_import | X-gi-2a | -- | Price the Reconcile page's own MATCH pane from the SUBMITTED body when there is one, so a refused Apply answers with the owner's ticks rather than the tier's proposal: `reconcile_page`'s fifth parameter becomes the ask instead of the line id, and the route builds that ask the way the live fragment already does. Closes **BI-478**. | SHIPPED | `8fa4d0bc` | -- |
| bank_import | X-gi-5 | -- | Take every bank-line lock a pass needs in ONE read before `apply_reviewed`'s arms, ordered by `id` through the query `load_lines` shares, so two presses naming the same lines in different arms queue instead of crossing; the lock's mode corrected to `FOR NO KEY UPDATE` at both sites through one helper (**R-BI5**). Closed **N-471**, REPRODUCED first. | SHIPPED | `cd78499a` | -- |
| bank_import | X-gi-4 | -- | Close the three findings the deletion does not: render `anchors_released` and `merchants_forgotten` on the import-delete receipt (**N-470**); **N-405**'s CSP-dead inline style, SHIPPED at `af6f8a3f`; and for **N-402** delete every `@login_required` for ONE default-deny login gate (`app/login_gate.py`, **R-BI4**) whose sweep enumerates `url_map`, so a route is gated by existing. | SHIPPED | `ba5ae344` | -- |
| bank_import | X-gp | -- | Merge the MATCH pane's attribution select and its consent box into ONE control whose options ARE the acts, each labelled with the figures it writes and its value carrying BOTH the landing and the figure, so the door compares the whole reviewed decision rather than a projection of it that two different acts share (**R-BI2**, superseding **R-IV**). | SHIPPED | `40354328` | -- |
| bank_import | X-gq | -- | Move `DifferenceLanding`, `corrected_figure` and its reader out of `_variance.py` into a new `_landing.py`, a PURE move cutting on the seam that module's own first line names, so `X-gp` has room to write the consent gate's new argument into a file that stood at 999 of pylint's 1000-line ceiling. | SHIPPED | `86f8620d` | -- |
| bank_import | X-go | -- | Delete `_MAX_MATCH_MEMBERS` from `StatementMatchSchema`, whose stated reason was already held by two bounds that live where their own truth does and which had begun to contradict a pane offering every unexplained row as a tickbox (**R-BI2**, **R-HW**). | SHIPPED | `20f96d90` | -- |
| bank_import | X-gi-3 | -- | Delete what those pages orphan: `_queue.py` whole, `_register.py`, THREE `ReviewSet` members, two `ReviewBounds` properties, `_section.answered_merchants` with `MerchantRegister`, the two dead form readers `batch_payload` and `hand_match_payload` with their helpers, every re-export of these, and the tests that reach the schemas only through them. Closes **BI-479**, **BI-480**. | SHIPPED | `43ce313b` | -- |
| bank_import | X-gi-1 | -- | Clear the FOUR live inbound links left -- two statements-page repoints, one statements-page DELETION, and the Reconcile page's own `<noscript>` Build link (**R-KA**) -- and give the MATCH pane that scriptless path behind `?open=<line_id>`, offering every unexplained row because that render has no search (**R-BI1**). | SHIPPED | `8543c80f` | -- |
| bank_import | X-gi-2 | -- | Delete the review queue, the register and the workbench AS PAGES (**R-HU**): three route modules over nine endpoints, seven templates, `statement_review.js`, `.stmt-pick-list` and the three route test modules, none of which a surviving surface reached once `X-gi-1` had repointed the links. Closed **N-404**. | SHIPPED | `b462af07` | -- |
| salary | C12 | -- | The DECOMPOSED parent of one current-paycheck producer, split 2026-09-12 (**R-SAL28**) at the money line once **R-SAL25**-**R-SAL27** ruled the design asked for from scratch: the NO-MONEY leaf (the engine package, the amount basis over the pass's pricer, the four byte-identical direct-engine sites) and the MONEY leaf (`/savings`). | SHIPPED | `945651c2` | -- |
| pay_calendar | C14-c | -- | Corrected the derivation's boundary arithmetic ahead of the live shift (**R-PC54**): ONE end rule, a projected period ending the day before the NEXT payday, deleting the `start_date + cadence_days - 1` special case; `project_period_after` keeps its O(1) jump and probes the estimate's two NEIGHBOURS (**R-PC57**). `$0.00`. Opened **N-495**, **N-496**. | SHIPPED | `659260c0` | -- |
| pay_calendar | C14-d | -- | Stopped the two writer paths feeding a CASH date back into the rhythm (**R-PC54**): the floor asks `projected_payday` rather than restating `latest + cadence`, refusing **58** of production's 1,951 future paydays before and 0 after, and `extend_pay_periods` hands the NOMINAL grid day. The nominal grid became its own module (**R-PC60**). Opened **PC-497**. | SHIPPED | `c1ce08b5` | -- |
| pay_calendar | C14-e-1 | -- | Made the pay rhythm ONE value the derivation reads: `Rhythm` moved out of the session-holding `pay_schedule_service` into the pure leaf `app/services/pay_rhythm.py` (pylint **R0401** refused the pay-calendar package itself), every producer took it in place of a bare cadence, and `resolve_shift` and its per-request duplicate query were DELETED. `$0.00` -- the displacement is wired to nothing. | SHIPPED | `f32c9d7a` | -- |
| pay_calendar | C14-e-2 | -- | Gave `budget.pay_schedule` the NOMINAL grid's phase (**R-PC61**) and pointed extend at it rather than at its own last output, which is what stops each batch re-phasing the rhythm by the previous batch's displacement: **178** of 301 recorded paydays wrong under `prior` at a batch of one before, **0** after. Three of `C17`'s five era columns now live on the row. `$0.00`. | SHIPPED | `ab5b26bc` | -- |
| recurrence | R7d | -- | The DECOMPOSED parent of "a loan payment's closing bound stops being a stored column", split into SEVEN leaves 2026-08-25 (**R-R34**) when a census found the step's own specification had enumerated its ten WRITERS and not one of the six surfaces that READ it. Carried **D35** (closed at R7d-g-2). | SHIPPED | `b5ac8710` | -- |
| recurrence | R7d-g | -- | The DECOMPOSED parent of "the closing bound stops being written", split into THREE leaves 2026-09-13 once its four forks were ruled (**R-R80**..**R-R83**): the write deletion with its migration and CHECK, then the entry doors that keep the stored opening bound honest, then the loan dashboard's per-definition payment card. | SHIPPED | `b5ac8710` | -- |
| recurrence | R7d-f | -- | The DECOMPOSED parent of the recurrence form's locked bounds, split 2026-09-05 into THREE leaves (**R-R61**): the identity and the form's locks, then the pass's memo and horizon, then the create-side refusal. **Its own sentence was inexact and `R7d-f-1` measured it**: `owns_validity_window` splits no SET, and the live preview never read the column at all. | SHIPPED | `48e78700` | -- |
| recurrence | R7d-g-3 | -- | Gave the loan payment card its controls PER DEFINITION, doors keyed by template id (**R-R83**); deleted the resolver's loan-level `extra_principal` (**R-R88**: a row's cash carries its definition's extra, so the composer paid it twice) and re-cut the band chart, lever, allocation bar and schedule page onto the seam's forward plan. Closed **D49**, **REC-524**, **REC-525**, **REC-526**. | SHIPPED | `b5ac8710` | -- |
| recurrence | R7d-g-2 | -- | Completed the standing payment's start contract: every door where an existing definition BECOMES one calls ONE helper (**R-R85**), a SECOND transfer's start is its owner's, refused at or before origination (**R-R81**), the archived edit form locks as the standing payment's (**R-R86**), and the lifecycle doors moved to `transfers/lifecycle.py` (**R-R84**). Closed **D35**, **D50**, **REC-522**. | SHIPPED | `547cc43d` | -- |
| recurrence | R7d-g-1 | -- | Stopped WRITING the closing bound: nine of ten `sync_recurring_payment_bounds` sites went, the tenth became `sync_loan_payment_start` (opening-only, refusing a start moved past an authored stop), R-R56's arm `authored_closing` went with them, and migration `bf50951a3599` NULLed the cache on the **R-R80** set and landed `ck_recurrence_rules_valid_window`. Closed **D56**; ruled R-R81..R-R83. | SHIPPED | `a776b9df` | -- |
| recurrence | R7d-f-5 | -- | Gave the transfer EDIT form the create form's lock affordance, computed for THIS edit the way the update door decides it (`loan_destination_locks_for_edit`): a definition **R-R76** pins to its loan ships EMPTY sets beside a DISABLED destination control carrying the refusal (**R-R79**), the R-R76 union is ONE predicate, and the loan-destination helpers moved first (**R-R78**, `cd22baa6`). | SHIPPED | `48e78700` | -- |
| recurrence | R7d-f-4 | -- | Made the UPDATE door settle the destination the edit LEAVES ahead of the recurrence step (`settle_destination_for_update`): the first occurrence derived, a real stop refused where the loan holds no active payment, "Never" or nothing writing the unbounded rule even over a stored stop (**R-R77**), and a loan payment unable to change destination (**R-R76**). Closed **REC-521**; opened **REC-522**. | SHIPPED | `e0c67c0b` | -- |
| recurrence | R7d-f-2 | -- | Put the horizon ON `RuleReading` so `has_ended` takes no calendar, memoised the WALK on the pass keyed by the COMPOSED value (`placements_of`, R-R73's shape: a /savings render walks a goal transfer once), and gave the preview the door (`resolved_submission`, destination owner-checked) so it stops at the loan's payoff. Closed **N-513**, **N-514**, **REC-515**, **balance:BAL-483** (**R-R75**). | SHIPPED | `1d1f466f` | -- |
| recurrence | R7d-f-3 | -- | Refused a stated closing bound AT CREATE where the destination loan holds no active payment (**R-R60**), a real stop and never the key's presence (**R-R74**), with the server emitting which loan destinations derive the stop beside those deriving the start and the script locking the *Ends* row as an affordance. Closed **N-512**; the update door's twin is **REC-521**. | SHIPPED | `e3661f6f` | -- |
| recurrence | R7d-f-1 | -- | Made the form's locked *Ends* row read the RESOLVER and its locks read the pass: ONE renamed identity `is_standing_loan_payment(template, ctx)` off the pass, plus per-ROW `RecurrenceStart.locked` and `RecurrenceEnd.locked`, so the split is of MEANING (opening bound written, closing bound derived) and not of the set. Closed **N-511**; opened **REC-515**. | SHIPPED | `6af50d53` | -- |
| bank_import | X-gm | -- | Made the grid's bank badge and the Reconcile inbox ONE producer: `inbox_partition` is one Python walk the badge counts and the pass is built from, `to_explain` counts the cards the inbox BUILT rather than summing four lists, and the badge's LINK moved to Reconcile so the figure and the page agree. **27 against 18 -> 18 against 18.** Closed **N-476**. | SHIPPED | `1b722d52` | -- |
| recurrence | R7d-e | -- | Moved the monthly totals onto the resolver: `obligations_aggregator` reads each definition through the composed door and `has_ended` judges its composed closing, so a RETIRED loan's payment leaves the Recurring totals and the `/savings` floor on the day the loan closed (**R-R57**: R-R45's reading for the derived stop). `$0.00` on dev, `$200.00`/mo both ways in tests. Opened **N-513**, **N-514**. | SHIPPED | `89302ba4` | -- |
| salary | R14-b | -- | Applied **R-SAL2** to what a payroll deduction's gross is priced from: the contribution tier CONSUMES the engine's per-period breakdown instead of re-deriving it, retiring the second spelling of a deduction's amount. **MOVED `+$452.42`** on two independent bases. Ships an INTERIM tail rule whose two residues are measured (**N-541**). Closed **D45**, **N-532**. | SHIPPED | `e0f0c05f` | -- |
| pay_calendar | C14-b | -- | Gave `budget.pay_schedule` its shift convention keyed to `ref.business_day_shifts`, defaulted to `none` and asked on the FOUR cadence templates (**R-PC56**), with the pair a short cadence cannot carry refused at the column's ONE write door and NO CHECK constraint (**R-PC59**): that floor is derived from a holiday set that MOVES. Behaviour off, no figure moved. Opened **N-493**, **N-494**. | SHIPPED | `229f0e23` | -- |
| recurrence | R7d-d | -- | Moved the DISPLAY readers onto the resolver through a COMPOSED DOOR: the Recurring surface takes ONE read pass and reads each definition through `recurring_definition`, so its cadence sentence and next date name the loan's derived closing date; the resolver takes the resolved rule, and by **R-R56** the door reads an app-written `end_date` as the cache. Opened **N-511**, **N-512**. | SHIPPED | `4a839587` | -- |
| recurrence | R7d-h | -- | Gave a loan ONE closing date, past AND future: `loan_closing_date(account, ctx)` answers the forward crossing while the loan owes and the day it LAST became closed once it does not (**R-R51**: the later of two crossings), `recurrence_end_date` is DELETED with the read-clock bound it substituted, and the inverted-window refusal skips a definition whose window the app derives. No migration. | SHIPPED | `83dd4b8a` | -- |
| pay_calendar | C14-a | -- | Build the ONE shared business-day module (**R-PC47**) -- the weekend rule, the computed federal holiday set and the single displacement -- pure, so the pay-calendar package can import it, and consumed by `recurrence:R8-d` for a bill's cash date as well. | SHIPPED | `088339f5` | -- |
| recurrence | R19 | -- | The DECOMPOSED parent of finding **REC-516**, split 2026-09-08 at ruling **R-R63** into the guard and the root fix: `classify_maintain_work` hard-deleted a mutable, record-free row whose `occurs_on` was NULL, and the create arm refilled the freed occurrence so no row count could see it. | container | -- | ticks with #92 |
| recurrence | R19-a | -- | Made an undated row RETAINED rather than retired, because *answers no occurrence* is a property of the ROW and only *the rule dropped this one* licenses a deletion. Retain over a silent skip is **R-R63**: a reprice to `$250.00` leaves such a row at `$100.00` for good. Reachable since `balance:X-bz`; production 0, dev **598**. | SHIPPED | `2f6bab81` | -- |
| bank_import | X-gi | -- | The DECOMPOSED parent of the exception queue's RETIREMENT, split 2026-09-05 at its own census: the census measured what the Reconcile rebuild orphans, and the five leaves repoint the live links, delete the three pages, delete what they orphan in the service, close the three findings the deletion does not, and reorder one money door's locks. | SHIPPED | `cd78499a` | -- |
| bank_import | X-gl | -- | The DECOMPOSED parent of the ACT-MODEL rebuild the developer ruled from scratch on 2026-09-04 (**R-JY**), where ONE row per bank line names the VERB it ended on and WHO decided, so **R-HP**'s *exactly one verb per line* becomes a UNIQUE key rather than an invariant two doors maintain; the argument and the forks still open are `docs/design/statement_disposition_model.md`. | container | -- | ticks with #79 |
| bank_import | X-f6b | -- | The DECOMPOSED parent of the automated SOURCE ADAPTER (SimpleFIN, **R-FP**), its design loop closed 2026-09-18 (**R-BI10**..**R-BI14**, **R-BAL71**): a line held by the sightings of the imports that showed it and coverage the window an import declares (-1, -1b), the adapter, the per-owner claim and one sync function (-2), the launcher, the timer and the feed panel (-3); ticks with its last leaf. | container | -- | ticks with #15 |
| pay_calendar | C14 | -- | The DECOMPOSED parent of the pay schedule's SHIFT CONVENTION (**R-PC47**, **R-PC54**-**R-PC57**), split 2026-09-04 into the module, the column, TWO `$0.00` repairs the live shift would otherwise break, the money, and the check: the boundary arithmetic and the writer are corrected before the shift is switched on, because a shift over the old end rule puts a day in no pay period. | SHIPPED | `5d14e4d4` | -- |
| salary | S2 | -- | Named the input that moved a past paycheck `-$19.28`: a `calibration_overrides` row DELETED 2026-08-28 in the act that inserted the live one (`system.audit_log` 4212/4213). Eleven settled paychecks re-derive `$0.00` under it and the twelfth only under the live one, so NO single calibration reproduces the record -- **R-SAL4** measured. Refuted `balance:X-aw`. Closed **N-442**, opened **N-535**. | SHIPPED | `08638f61` | -- |
| balance | X-au-h | -- | Made `is_override` say ONE thing -- *this row is the OWNER's, not the rule's* -- and moved authorship onto the PAYLOAD (**R-JR**): a form posts the figure it RENDERED and a figure without one is REFUSED. Dropped the flag from the occurrence-keyed unique index. Closed **N-248**, **N-436**, **N-448**; opened **N-451**, **N-452**. **N-238** stays open and narrowed. | SHIPPED | `825fd791` | -- |
| balance | X-bj | -- | The DECOMPOSED parent of the level relation (**R-IS**, **R-JN**), split 2026-09-03 so the relation lands before the flip and the never-moves-a-balance clause lands with it, because building the clause early would run two balance semantics live on one account; **N-314** was answered by its ruling. | container | -- | ticks with #14 |
| pay_calendar | C7 | -- | Rule and then fix `journal_entries.pay_period_id`, a NOT NULL FK stored beside the `entry_date` it derives from, which is P1's defect on the ledger's header table. Closes **P18**. | SHIPPED | `a9e04106` | NOW / pay_calendar:C4 (shipped) |
| salary | R14-a | -- | Give an employer contribution the salary profile that FUNDS it (**R-SAL5**) and make the calendar-wide paycheck projection SINGLE (**N-443**), with the deduction write doors no longer accepting another owner's account. Moves no figure. Closes **N-443**, **N-533**, **N-534**. | SHIPPED | `9e81d9e7` | NOW |
| pay_calendar | C4-d | -- | Collapse `ScheduleFacts`' four representable states to the two a row can hold: `resolve_schedule` returns an OPTIONAL `ScheduleFacts` whose `cadence_days` is non-optional, so a pair naming no cadence beside a stated `history_opens_on` stops being constructible, and `calendar_for`'s answer for a schedule-less owner is decided as this leaf's own fork. **OWN REVIEW PASS.** Ruling **R-PC44**. | SHIPPED | `327a70f2` | -- |
| balance | X-au-g-2c-3b-2 | -- | Let the SETTLED walk take the charge calendar -- ONE interest accrual and ONE escrow per INSTALLMENT rather than one per PAYMENT -- which DELETES `split_payment_cash`, the composition whose own docstring concedes it is correct only while a loan takes one payment per accrual period. **MOVES POSTED MONEY, OWN PR.** Carries **N-409**'s second half and satisfies **recurrence:D51**. | SHIPPED | `3b7716f8` | -- |
| balance | X-au-d | -- | Cut SALARY rows over: 59 non-override rows declare their definition, generation stops pricing them, and the read-time repair with its whole seam is deleted. Closes **N-224**, **N-228**, **N-258**, **N-261**, **N-253**, **N-437**. | SHIPPED | `ed06acf6` | -- |
| balance | X-au-e | -- | Cut TEMPLATE rows over to their template's amount series: generation stopped pricing, 525 non-override rows went NULL, and the conflict chooser kept its offer while losing its figure (**R-JD**). Closes **N-244**, **N-247**, **N-444**. | SHIPPED | `b846386a` | -- |
| balance | X-au-g-2c-3 | -- | The DECOMPOSED parent of the escrow rule, split 2026-09-02 when its own trace found the defect was not ONE floor but FOUR statements of one allocation rule and TWO of one charge calendar -- each duplicated because the shared rule sat ABOVE the walks that needed it, so reaching it was an import cycle. | container | -- | ticks with #47 |
| balance | X-au-g-2c-3b | -- | The DECOMPOSED parent of the CHARGE-CALENDAR half, split 2026-09-02: the calendar moves to the leaf both walks reach, then the SETTLED walk takes it, then the ENGINE FEED stops allocating. | container | -- | ticks with #47 |
| balance | X-bi | -- | The DECOMPOSED parent of the MOVEMENT UNIFICATION (**R-IT**): every plan item is satisfied by zero or more movements, so a bill is the one-movement case and an envelope the many-movement case, `budget.transaction_entries` becomes the general movement table, and the balance folds movements only. | container | -- | ticks with #11 |
| balance | X-bi-6 | -- | The DECOMPOSED parent of the transfer shadow rows' deletion, split five ways 2026-09-20 (**R-BAL86**): the display readers first (6-1 the grid, 6-1b the rest), then statement match (6-2), the ledger's shape C (6-3), the record half's re-parent (6-4) and the ROWS (6-5), making Transfer Invariants 1, 2 and 4 unrepresentable (**R-BAL13**). Ticks with its last leaf. | container | -- | ticks with #11 |
| balance | X-ci | -- | The DECOMPOSED parent of the transfer twin's one-definition shape, split three ways 2026-09-20 (**R-BAL95**): `recurs` and the twin's two defects (X-ci-1), the ad-hoc door and the suite's one builder (X-ci-2), then the cutover (X-ci-3), making a transfer with no definition unrepresentable (**R-BAL20**). Ticks with its last leaf. | container | -- | ticks with #36 |
| credit_card | CC-5 | -- | The DECOMPOSED parent of the movement-on-card doors (design 3.2, **R-CC15**): the key (5-1), the purchase door with its readers and the picker (5-2), the settle-with-tender door (5-3), 5-1 and 5-2 in ONE PR (**R-CC33**), then the matcher's card-screen half (5-4, **R-CC40**) and the net-worth sign fix (5-5, **R-CC41**); ticks with its last leaf. | container | -- | ticks with #4 |
| bank_import | X-f6c | balance:X-bi-7b | The DECOMPOSED parent under its BANK-IMPORT name: a NEW-ENVELOPE merchant answer mints ONE rule-less definition the first time and names it thereafter, so a merchant rule's container carries an identity across pay periods instead of a name (**N-328**, **R-GA**); one step with `balance:X-bi-7b`, ticked with 7b-3. | SHIPPED | `321bf2e4` | -- |
| balance | X-bi-7b | bank_import:X-f6c | The DECOMPOSED parent of the one-off producer and every link-less writer on it, split 2026-09-13 (**R-BAL31**) into the producer with the grid doors, the popover's edits landing on the definition, and the bank door's minting and placing; `bank_import:X-f6c` one step with it (**R-BAL24**); ticked with 7b-3, its last leaf. | SHIPPED | `321bf2e4` | -- |
| salary | R14 | -- | The DECOMPOSED parent of what a payroll deduction's gross is priced from, split 2026-09-03 (**R-SAL6**) into the EXPAND and the MONEY: `R14-a` lands the funding-profile column, the shared calendar-wide projection and the deduction doors' ownership guard and moves no figure, then `R14-b` re-points the contribution feed onto the engine's per-period breakdown. Carried **D45**. | SHIPPED | `e0f0c05f` | -- |
| balance | X-au-g | -- | The DECOMPOSED parent of the LOAN-PAYMENT cutover, split 2026-08-31 into the pricing cycle's deletion, the tier move that unwinds the amount model's reach into the loan service, the ruling that puts a loan's terms on the installment they govern, and the cutover those three unblock. | container | -- | ticks with #47 |
| balance | X-au-g-2c | -- | The DECOMPOSED parent of the loan-payment CUTOVER, split 2026-09-01: the readers route first and move nothing, the cutover declares both legs derived, and the escrow rule **N-409** needs is its own money-moving act. | container | -- | ticks with #47 |
| balance | X-f3c-2b | -- | The DECOMPOSED parent of the books boundary, split 2026-08-28 (**R-HG**) and re-cut 2026-08-28 (**R-HJ**): the INVARIANT and the restatement that legalises the existing rows are one commit, and the DOOR that restates an opening -- with the account-10 repair it is the only honest route for -- is another, and the ASSERTION side is a fourth leaf sequenced behind THE FLIP. Carries **N-379**. | container | -- | ticks with #32 |
| balance | X-f3c-2b-2 | -- | The DECOMPOSED parent of the DOOR, split 2026-08-31 (**R-ID**): the door is one commit, the bank line an import may not offer against it is the next, and the repair the door exists for is an OPERATOR act that cannot run until the first has deployed. Carries **N-379**, **N-382**. | container | -- | ticks with #31 |
| balance | X-f3c | -- | The DECOMPOSED parent of THE CUTOVER, re-decomposed 2026-08-27 (**balance:R-GW**) when a measurement against the developer's own bank export refuted its ORDER: the residual is RECORDED before the reset is DELETED, so the flip lands on the balance already asserted rather than six times further from the bank. Carries **N-172**, **N-174**. | container | -- | ticks with #33 |
| balance | X-f | -- | The DECOMPOSED parent of the whole "the app records when money moved" cluster, carrying **N-42**. | container | -- | ticks with #44 |
| balance | X-au | -- | The DECOMPOSED parent of the amount model (**R-FI**): a row's amount is either its OWN or DERIVED, and a derived amount is not stored at all. Supersedes **X-ar**; **X-au-i is WITHDRAWN** because the card arc's locked rulings delete the CC payback it would have cut over (`CC3b`, `CC3c`; `CC-7` since the 2026-09-18 re-mint). Carries **N-224**, **N-228**, **N-238**. | container | -- | ticks with #73 |
| balance | X-ai | -- | The DECOMPOSED parent of the posted-ledger restructure: one verb and one trigger on both ledgers (**R-DU**, **R-DV**). Carries **N-144**, **N-153**, **N-155**, **N-157**, **N-158**, **N-160**, **N-164**. | container | -- | ticks with #64 |
| balance | X-aj | -- | The DECOMPOSED parent of the one-status-seam work; its merge half shipped as X-aj1. Carries **N-145**. | container | -- | ticks with #58 |
| balance | X-x | -- | The DECOMPOSED parent of the one-pay-calendar-precondition work. Closes **N-116**, **N-125**, **N-126**, **N-129**. | container | -- | ticks with #110 |
| balance | X-ad | -- | The DECOMPOSED parent of the pay calendar a new user can actually enter, split 2026-08-10 (**R-EZ**) into the door that CREATES a calendar and the door that GROWS it. | container | -- | ticks with #55 |
| balance | X-i | -- | The DECOMPOSED parent of the one-read-pass work: nine ledger rows with one root cause. | container | -- | ticks with #126 |
| balance | X-l | pay_calendar:C2 / recurrence:R-F12 | The DECOMPOSED parent under its BALANCE name of "the pay calendar answers any date". Closed **N-128** (at `C2-c`). It claimed **N-82** and **N-79**'s far half and delivered NEITHER, so both were re-pointed at the tick rather than closed. | SHIPPED | `4f134bf4` | -- |
| pay_calendar | C2 | balance:X-l / recurrence:R-F12 | The DECOMPOSED parent under its PAY-CALENDAR name: one calendar value answers every "which period" question, RULED on three forks 2026-08-10. | SHIPPED | `4f134bf4` | -- |
| recurrence | R-F12 | pay_calendar:C2 / balance:X-l | The DECOMPOSED parent under its RECURRENCE name: one `PeriodCalendar`, not three period-containing searches. Closes **F-12**. | SHIPPED | `4f134bf4` | -- |
| pay_calendar | C4 | -- | The DECOMPOSED parent of "the two derived columns go", split into seven leaves 2026-08-25 and into EIGHT on 2026-09-01 when `C4-b` split in two: five reader leaves by PACKAGE, the test corpus, the schedule-row foreign key, the drop, then the cadence TYPE. The drop shipped at `C4-c`; this parent waits on `C4-d`, which is the same defect one tier up in the type rather than in the schema. | SHIPPED | `327a70f2` | -- |
| pay_calendar | C13 | -- | The DECOMPOSED parent of "a transaction's owner is a COLUMN", split 2026-09-02 because the arc document already specified two leaves and this index carried one row (rule 12): the KEY lands first and changes no reader, then the readers it makes unnecessary go; RE-OPENED 2026-09-03 for a third leaf, `C13-c`, P75's defect on six more tables. | container | -- | ticks with #52 |
| pay_calendar | C13-a | -- | Make a transaction's OWNER a COLUMN held equal to BOTH its parents' by a composite foreign key each, so a row whose account and whose paycheck belong to different people becomes unconstructible rather than merely unwritten. Expand / backfill / contract in one revision. **OWN REVIEW PASS.** Ruling **R-PC32**. | SHIPPED | `8e707c4c` | -- |
| pay_calendar | C13-b | -- | Retire the NINETEEN hand-written ownership comparisons the column makes unnecessary, deciding per site whether the check becomes one equality on `transactions.user_id` or goes entirely now that the state it refuses cannot be stored. Closes **P75**; carries **N-373**. | SHIPPED | `e2c325dc` | -- |
| pay_calendar | C4-b | -- | The DECOMPOSED parent of "an owner with paydays HAS a recorded cadence", split in two 2026-09-01 (developer) once the key's real prerequisite was measured: the TEST CORPUS builds by hand the exact owner the key forbids, so the fixture leaf goes first. | SHIPPED | `5db9f8a0` | -- |
| pay_calendar | C5 | -- | The DECOMPOSED parent of "the gap machinery goes, and a paycheck may owe one template twice", split 2026-08-09, ticked with `C5b`, its last leaf. | SHIPPED | `4e8b40b3` | -- |
| balance | X-f1 | -- | A settle carries the day the money moved; absorbed **S2-b**. Fourteen leaves, condensed into `archive/…2026-08-04.md` 1a. | SHIPPED | `8d812662` | -- |
| balance | X-f3 | -- | The DECOMPOSED parent of THE CUTOVER, re-decomposed 2026-08-13 when measurement refuted two of ruling R-EB's premises. Carries **N-171**, **N-172**, **N-174**. | container | -- | ticks with #44 |
| balance | X-f3a | -- | The DECOMPOSED parent of "clearing is a recorded fact", split 2026-08-14 (**R-FQ**) into the RECORDING half, which moves no money, and the half that makes a walked statement's SILENCE mean something, which cannot precede the cutover. Carries **N-273**. | container | -- | ticks with #44 |
| bank_import | X-f6 | -- | The DECOMPOSED parent of the statement importer, whose first leaf moved AHEAD of the cutover because what the cutover needed was clearing facts rather than an import surface. Carries **N-173**. | container | -- | ticks with #80 |
| recurrence | R16 | -- | The DECOMPOSED parent of the ESTIMATED tier's summing, split into FOUR leaves 2026-08-26 (**R-R36**) when a trace found the forward fold charging one month of interest per payment RECORD, so no cadence could be honoured until the accrual stopped riding on the payment. **D47** and **D48** closed at `R16-b-2`. | container | -- | ticks with #26 |
| recurrence | R16-c | -- | The DECOMPOSED parent of the ONE event STREAM, split 2026-09-19 (**R-R90**): the MERGE first (c-1, a pure restructure graded byte-identical), then the CALENDAR (c-2, the money move); the fold RULE was already shared at `balance:X-au-g-2c-3b-2`. Ticks with its last leaf. | container | -- | ticks with #21 |
| recurrence | R7d-c | -- | The DECOMPOSED parent of "generation takes the resolver", split into TWO leaves 2026-08-27 (**R-R38**) when the developer refused a producer opening its own read pass: the pass has to REACH generation before generation can resolve anything, and who opens it is a question about the WRITE DOORS rather than about the bound. | SHIPPED | `b8509c1e` | -- |
| recurrence | R8 | -- | The DECOMPOSED parent of the ruled add-ons, split 2026-08-16 when measurement showed three of the four need a generated row's own `occurs_on`: `recurrence_engine.compute_due_date` dates a row from its PAY PERIOD and never from the occurrence, so a weekly row, an nth-weekday row and a shifted row would each carry a date the cadence never named. | container | -- | ticks with #20 |
| recurrence | R17 | pay_calendar:C5b (ticks it) | A generated row is the OCCURRENCE it answers, not the paycheck its money lands in: `occurs_on` is what every predicate asks about and what both unique indexes are keyed on. Closed **D57** -- 8 rows / `$1,482.93` from one pass, measured before and 0 after -- and retired the **D19** refusal with the paycheck key that forced it. | SHIPPED | `4e8b40b3` | -- |
| pay_calendar | C5b | recurrence:R17 (ticked by it) | The same commit under this arc's name: the skip predicate became occurrence-aware, a paycheck may owe one template more than once, and `refuse_unstorable_repeats` retired with the index re-key. Closed **P16**. | SHIPPED | `4e8b40b3` | -- |
| pay_calendar | C4-a-1 | -- | The balance seam's PLANNED tier stopped clamping a projected row against the STORED `end_date` and reads the span its paycheck DERIVES, through `PayCalendar.require_period`; the fold now carries the calendar it was clamped by and `_asset_fold.resolve` dropped its own. Closed **P38**. | SHIPPED | `8962e073` | -- |
| pay_calendar | C4-a-2 | -- | The reconcile panel came off BOTH columns and `utils.dates.attribution_date` was DELETED for `DerivedPeriod.attribution_day` (**R-PC31**); its own review then measured the first design's safety argument FALSE, so all THREE arms scope by the calendar's own period ids and `require_period` is unconstructible there. | SHIPPED | `82bd762c` | -- |
| pay_calendar | C4-a-3 | -- | The purchase-date warning came off the relationship and `DerivedPeriod.covers` LANDED (**R-PC31**), retiring the THREE sites that open-coded a period's span; `check_purchase_date_in_period` is DELETED and `entry_list_view` no longer takes a transaction. The five-argument fork was answered by DELETION: both entry maps left `_build_grid_row_data`, ending a copy the Plan tab discarded. `$0.00`. | SHIPPED | `a0fb14ba` | -- |
| pay_calendar | C4-a-4 | -- | The merchant-destination picker came off `txn.pay_period`: `destinations_for` takes the CALENDAR and no `owner_id`, and `PayCalendar.saved_by_id()`'s KEYS are its scan's scope while that SAME mapping answers each span, so a miss is unconstructible (**R-PC36**). `PurchaseDestination` carries the `DerivedPeriod` in place of three copies of its facts, DELETING a disable (**R-PC37**). `$0.00`. | SHIPPED | `f18a58af` | -- |
| pay_calendar | C4-a-5 | -- | The two LABEL readers came off the ORM row and the `PayPeriod.label` ACCESSOR is DELETED, so no screen can reach the stored `end_date` for a label; the COLUMN is `C4-c`'s. `require_period` takes ONE `FiledRow` (**R-PC39**) and `period_move_options` takes the CALENDAR. Opened **N-413**; amended **P70**, which this step made under-by-one. | SHIPPED | `95b2dc67` | -- |
| pay_calendar | C4-b-1 | -- | Make every test owner's pay calendar come from the doors that own it: every hand-built `PayPeriod(...)` that builds an ORDINARY owner (23 sites, 18 files) goes through `record_paydays`, the five calendar fixtures through `reset_pay_periods`, and `_drop_seed_user_bootstrap`'s 135 lines and raw `period_index` renumber go. Closes **N-392**; opens **P78**. | SHIPPED | `eb6597ae` | -- |
| pay_calendar | C4-b-2 | -- | Make an owner's recorded cadence a FOREIGN KEY: backfill a `budget.pay_schedule` row for every owner holding paydays, point `pay_periods.user_id` at it `ON DELETE CASCADE`, and DELETE `resolve_schedule`'s inferring fallback rather than leave a branch no state can reach. Closes **P8**, **P35**. | SHIPPED | `5db9f8a0` | -- |
| pay_calendar | C4-c | -- | The two derived columns are DROPPED with the three constraints that bounded them and the four surviving fences of section 1; `upgrade()` REPORTS a stored pair that disagrees with the paydays rather than discarding it in silence. Closed **P1**, **P4**, **P5**, **P9**, **P26**, **P27**, **P28**, **P33**, **P53**, **P70**; opened **P79**, **P80**. | SHIPPED | `c703e1c7` | -- |
| balance | X-i3-b | -- | The three live accommodations for "under READ COMMITTED the two reads can differ" narrowed to the COMMAND each still describes, none deleted -- one is also an ownership scope and one pairs two different REQUESTS -- and the census re-taken, which found three sites the step never named and one claim `X-i3-a` had falsified. Opened **N-364**. | SHIPPED | `1feb0930` | -- |
| balance | X-i4 | -- | A read pass BINDS the account it values: `_memoize_once`, the ONE primitive creating `account.id`-keyed state on a pass, takes the account and refuses a foreign one, the cash fold joins the three loan derivations it memoizes, and the four mis-pairable readings take the assembled record. Closed **N-354**; opened **N-360**, **N-361**, **N-362**, **N-363**. | SHIPPED | `79a1730c` | -- |
| recurrence | R7d-c-1 | -- | The generate pass took the READ PASS it resolves in and the ROUTE became the layer that opens one: `GenerationSchedule` carries a `BalanceContext`, and the doors that create pay periods RECORD and return where they used to generate too (**R-R38**), settling the fork `pay_calendar:C11` states for this path. `$0.00` through all three generate doors; the caller census it took closed **D58**. | SHIPPED | `61d81c7f` | -- |
| recurrence | R16-b-1 | -- | The occurrence walk stopped TRUNCATING at the saved schedule's horizon: `PayCalendar.paychecks_from` composes `current_and_future_window` with a `projected_paychecks` continuation `axis_window` had been stepping a second copy of, so `occurrences(through=X)` answers 255 dates where it answered 62 and `_bounded` is again the only place a bound is applied. | SHIPPED | `1b818135` | -- |
| recurrence | R16-a | -- | The forward fold stopped charging a month of interest per payment RECORD: interest and escrow became ONE charge per accrual period and each payment allocates its cash on its own date, so 30 payments a fortnight apart no longer charge the same `$1,096.34` as 30 a month apart. `$0.00` on 776 production figures and 4,000 differential trials. Opened **D51**-**D55**. | SHIPPED | `e8baa3c0` | -- |
| recurrence | R7d-b | -- | The resolver a loan payment's closing bound comes from -- `loan_payment_window(template, ctx)` answering `ClosesOn` / `Indefinite` / `EMPTY`, the sum type a nullable date could not carry -- taking the DEFINITION rather than the loan (**R-R35**), so its SUBJECT is no longer tie-broken though its VALUE still is. Re-scoped **D47**; opened **D48**, **D49**, **D50**. | SHIPPED | `0462dc38` | -- |
| recurrence | R7d-a | -- | An installment NO ROW covers is priced by the loan's own recurring payment (**R-R33**) not the CONTRACT, so a payoff stopped depending on whether the rows were WRITTEN yet WHEREVER THEY AGREE WITH THEIR DEFINITION -- `2029-02-22` against `2030-02-22` through `regenerate_pay_periods` -- with **N-352** carrying the residue. | SHIPPED | `89cb0c1d` | -- |
| balance | X-i3-a | -- | A request is a QUERY or a COMMAND and its transaction says which: GET and HEAD run at REPEATABLE READ READ ONLY so a render is ONE snapshot and cannot write, every other method keeps READ COMMITTED because the reconciles' lock-then-reread depends on it, and the GET handlers that wrote either declare it in a `write_transaction` block or are deleted. Closed **N-353**; opened **N-358**, **N-359**. | SHIPPED | `765daebd` | -- |
| recurrence | R10-b | -- | A transfer regeneration MAINTAINS what it generated: an edit stopped destroying 99 transfers and 198 shadows to rewrite values already there, a note and a `$321.45` figure kept through a revert now survive one, and `update_transfer` learned to move a transfer's ACCOUNTS -- two of the six derived columns it could not write. Ruling **R-R32**. | SHIPPED | `ea776528` | -- |
| recurrence | R10-a | -- | A regeneration MAINTAINS the rows it already generated rather than destroying and rebuilding them, so a template edit can no longer take the purchases recorded against a part-spent envelope with it, and a row holding the owner's own records is retained and reported instead of rewritten. Closed **N-292**. | SHIPPED | `5fc13cdb` | -- |
| recurrence | R7c-c | -- | The closed pattern set DIED: six derived columns and the unwritten `budget.recurrence_month_anchors` dropped, `interval_n` re-pointed off the encoding so a Quarterly rule no longer reads as monthly, and the offer set re-based on the producer that refuses. Closed **D6**, **D32**, **D37**, **D38**, `pay_calendar:P11`; opened **D39**. | SHIPPED | `ee35bca7` | -- |
| recurrence | R7c | -- | The DECOMPOSED parent of THE CUTOVER as an expand / migrate / contract, ticked with R7c-c, its last leaf. | SHIPPED | `ee35bca7` | -- |
| pay_calendar | C2-f1 | -- | Three of `pay_period_service`'s six readers are DELETED across 12 call sites -- `get_overlapping_periods`, `get_next_period` and `get_current_and_future_periods` -- with two copies of one of them, `companion_service.get_previous_period` and `dashboard_pulse_service._next_paycheck_date`. Opened **P45**-**P49**. | SHIPPED | `792e3b21` | -- |
| balance | X-ad-a | -- | Registration ASKS for the most recent payday, the cadence and the horizon; the bootstrap payday is DELETED. Closed **N-123** (= `pay_calendar:P3`). | SHIPPED | `2a4eb477` | -- |
| pay_calendar | C1 | -- | The derivation exists and is proven equal to what is stored. Opened **P15**, **P16**. | SHIPPED | `f9d148fe` | -- |
| pay_calendar | C3 | -- | The writer writes paydays, forward-only: `pay_period_write` is the one place in `app/` that constructs or deletes a pay period. Two leaves, condensed into `historical/pay_calendar_as_built_2026-08-16.md`. Closed **P2**'s writer half, **P12**, **P13**, **P29**, **N-127**. | SHIPPED | `7e3fb33b` | balance:X-ad-a |
| pay_calendar | C5a | recurrence:R-F10 (ticked by it) | The gap machinery the derivation makes unconstructible is DELETED -- ticked at C2-b2 rather than after C4, because that leaf is what made its subject unreachable (developer 2026-08-11). The 430-shape baseline stayed byte-identical. | SHIPPED | `fe365de1` | -- |
| recurrence | R-F10 | pay_calendar:C5a (ticks it) | The gap machinery goes; the same commit as `pay_calendar:C5a`, under this arc's name. Closed **F-10**. | SHIPPED | `fe365de1` | -- |
| recurrence | R8-a | -- | The offer set stopped being gated on a derivation ruling R-R16 had deleted: `anchor_family` and its three families went, `authorable_cadences` derives from whether a row can be DATED and whether the placement can change it, and a year-scale cadence funded from a later paycheck became authorable. Closed **D20**; opened **D40**. | SHIPPED | `87e2c5b9` | -- |

## Cross-arc forks

**Two steps in different arcs that are competing remedies for ONE defect.** Whichever ships first
decides for both, so the gate REFUSES a tick on either until `ruled` NAMES one of the competing
remedies. A ruled fork's defect row is owned by the remedy that won, and the gate checks that too: a
ruling nobody re-points is a ruling that decided nothing.

**`pay_calendar:P3` = `balance:N-123` LEFT this table on 2026-08-10**, and how it left is the point:
it was ruled to `balance:X-ad` on 2026-08-09, that remedy SHIPPED as `X-ad-a` (`2a4eb477`), and the
defect row closed with it. A fork whose defect no longer exists binds nothing.

| defect | competing remedies | ruled |
|---|---|---|
