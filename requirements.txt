# Shekel Budget App -- Production Dependencies
#
# This file contains ONLY packages required to run the application
# in production.  Dev/test/lint packages are in requirements-dev.txt.
#
# Do NOT add gunicorn here -- it is installed separately in the
# Dockerfile to keep it out of the local development venv where
# Flask's dev server is used instead.

# Flask core
Flask==3.1.3
Flask-Limiter==4.1.1
Flask-Login==0.6.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.1.0
Flask-WTF==1.2.2

# Database
SQLAlchemy==2.0.49
psycopg2==2.9.11
alembic==1.18.4

# Authentication and MFA
bcrypt==5.0.0
pyotp==2.9.0
qrcode[pil]==8.2
cryptography==46.0.7

# Input validation
marshmallow==4.3.0

# Environment
python-dotenv==1.2.2

# Logging
python-json-logger==4.1.0

# Rate limiting -- Redis client used by Flask-Limiter's RedisStorage
# backend (limits.storage.redis).  Required in production where the
# limiter writes counters to the bundled docker-compose ``redis``
# service so they are shared across Gunicorn workers and survive app
# container restarts.  See audit finding F-034.
redis==7.4.0

# HTTP client -- used by ``app/services/auth_service.py`` to query the
# Have I Been Pwned k-anonymity API at password-set time.  See audit
# finding F-086 / commit C-11.  Only this single outbound endpoint is
# called (``api.pwnedpasswords.com``), so the dependency is small in
# scope; ``requests`` is preferred over the stdlib ``urllib`` for its
# reliable timeout semantics and its already-pervasive presence in
# the wider Python ecosystem (matches what most operators expect to
# audit).
requests==2.32.5
